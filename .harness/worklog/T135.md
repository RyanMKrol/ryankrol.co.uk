# T135: link workout title to detail page on WorkoutCard

## Do

Wrapped the card title (`<h3 className="review-title">`) in `src/components/WorkoutCard.js`
with a `next/link` `Link` pointing to `/workouts/${workout.id}`. This renders unconditionally
for every card, unlike the existing per-exercise links which only render inside
`{exercises.length > 0 && (...)}`. Left the per-exercise `Link`s to
`/exercises/${exercise.title}` untouched.

## Verified

- Read the full diff: only the title `<h3>` block changed; exercise list/stat-line rendering
  is unchanged.
- `npm run lint` — 0 errors, same 3 pre-existing warnings as before the change.
- `npm test` — 246/246 passing, all 35 suites green.
- `npm run build` — succeeds, `/workouts/[id]` route present as before.
- Did not start `npm run dev` for manual click-through (headless run, no browser available);
  confirmed via code review that `workout.id` is always present on `Workouts` items (partition
  key, per README schema) so the link target is never undefined, and that
  `src/pages/workouts/[id].js` already exists and renders workout detail independent of the
  denormalized `exercises` field.
