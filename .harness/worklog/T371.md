# T371 — Remove Google Books provider (dead code cleanup)

## Summary
Successfully removed the dead Google Books search provider (fully migrated to Hardcover in T361–T364) and its associated infrastructure.

## What was done

### 1. Files deleted
- `src/lib/googlebooks.js` — the `mapGoogleBooksResult` normaliser
- `src/lib/googlebooks.test.js` — unit tests for the normaliser
- `src/scripts/migrateOpenLibraryBooksToGoogleBooks.js` — one-time migration script (targeted `BookRatingsV3`, already ran, no longer needed)
- `src/scripts/migrateOpenLibraryBooksToGoogleBooks.test.js` — tests for the migration script

All 4 files were safe to delete: the migration script was DRY-RUN-BY-DEFAULT and only referenced by its own test + a package.json npm script; nothing live imports the googlebooks lib anymore.

### 2. Code changes
- **package.json:** removed `"books:migrate-openlibrary"` npm script (line 28)
- **src/pages/reviews/books/edit/[id].js:** removed dead `provider: 'googlebooks'` parameter from `handleBackfillSearch` URLSearchParams (line 79)
- **CLAUDE.md:**
  - Removed `├─ googlebooks.js` entry from Architecture section
  - Removed entire `GOOGLE_BOOKS_API_KEY` row from "Key env vars" table
  - Removed `src/lib/googlebooks.js` entry from File map section
  - Updated `src/pages/api/books/search.js` entry in file map to remove mention of Google Books (now Hardcover-only)

### 3. Verification
All "Done when" criteria verified:
- `grep -rEn 'googlebooks|GOOGLE_BOOKS_API_KEY' src/ | grep -v '\.test\.js:'` returns **zero matches** ✓
- Test fixtures in `*.test.js` files **still contain** `source: 'googlebooks'` string literals (5 occurrences) as expected — these are legitimate historical record fixtures in:
  - `src/scripts/migrateBooksToIdTable.test.js`
  - `src/scripts/stripOldBookProviderMetadata.test.js`
  - `src/pages/api/reviews/books/add.test.js`
  - `src/pages/api/reviews/books/update.test.js`
- `npm run lint` — **no errors** (6 pre-existing warnings unrelated to this change)
- `npm test` — **469 tests passed** in 6.379s
- `npm run build` — **successful**, no broken imports

## Notes for the owner
The `GOOGLE_BOOKS_API_KEY` environment variable should also be removed from Vercel's project environment variables at https://vercel.com/dashboard (this repo cannot do that itself, as it's a live platform setting). It is no longer used anywhere in the codebase.
