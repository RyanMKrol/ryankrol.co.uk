# T175: give `.spine-cover-card` a real card treatment

## What changed
`src/styles/globals.css`:
- `.spine-cover-card` (books review list card): replaced the bottom-only divider
  (`border-bottom`) with a full `border`, `background-color: var(--color-card)`,
  `border-radius: var(--radius-card)`, and `padding: 1rem` (was `1rem 0`) — matches the
  design handoff's "cards" language and the 2px-border/18-20px-radius spec used elsewhere
  (e.g. `.spine-cover-tile`).
- `.spine-cover-list`: added `gap: 1rem` so cards have visible spacing between them now
  that they no longer share a divider line as their boundary.

No changes to `ReviewCard.js` or `reviews/books/index.js` — the `spine-cover-card` /
`spine-cover-list` className hooks were already correctly applied; this was a pure CSS fix.
`.spine-cover-tile` and the `-body/-heading/-title/-meta/-snippet` rules were left untouched
per the task spec (already matched the design).

## Verification
- `npm run lint` — 0 errors, 6 pre-existing warnings (img/exhaustive-deps), unchanged from
  before this task.
- `npm test` — 35 suites / 241 tests passed.
- `npm run build` — succeeded, all pages compiled/prerendered.
- Did not start `npm run dev` / load the page in a browser (headless session, no browser
  available) — verified the CSS change by reading the resulting rules and confirming they
  reference existing, correct design tokens (`--color-card`, `--color-ink`, `--radius-card`,
  `--border-width`) already used identically elsewhere (e.g. `.spine-cover-tile`,
  `.perfume-card`), so the visual result is expected to match the spec's white-card /
  2px-border / rounded-corner / gapped-list description.
