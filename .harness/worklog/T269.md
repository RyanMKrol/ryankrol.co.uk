# T269 — HotTakes table definition

## Do
- Added `HOT_TAKES_TABLE: 'HotTakes'` to `DYNAMO_TABLES` in `src/lib/constants.js`.
- Added `src/scripts/createHotTakesTable.js`, mirroring `createPerfumeTable.js`: AWS SDK v3
  `DynamoDBClient` (region `us-east-2`, credentials from env via `dotenv.config({ path:
  '.env.local' })`), single String partition key `id`, no sort key, no GSIs,
  `PAY_PER_REQUEST` billing, waits for ACTIVE, handles `ResourceInUseException` as
  "already exists".
- Added npm script `hot-takes:create-table` in `package.json`.
- Table is NOT provisioned in this task — creation script exists but was not executed.

## Verified
- `npm run lint` — no errors (pre-existing warnings only, no new ones).
- `npm test` — full suite passed.
- `npm run build` — succeeded.
