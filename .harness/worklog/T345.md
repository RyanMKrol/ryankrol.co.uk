# T345 — 2026-07-10

## Do
Changed the concentration-type badge's `Tooltip` label in
`src/components/perfumeVariants/Variant6Hybrid.js` from `"What it is"` to `"What I've got"`, so its
voice matches the sibling ownership badge's tooltip (`"What I own"`). Only that one JSX attribute
changed; the ownership tooltip and all other lines are untouched (confirmed via diff — single-line
change).

## Checks
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this file).
- `npm test` — 75 suites / 402 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=perfume node scripts/visual-check.mjs` — all 14 captures succeeded. Read
  `scripts/visual-out/perfumes-type-tooltip.png`: the tooltip bubble above the first card's
  EDP/FULL BOTTLE badge row renders "What I've got" in the hover state, laid out correctly above
  the badge with no clipping/overlap.

## Remaining
None — task complete.
