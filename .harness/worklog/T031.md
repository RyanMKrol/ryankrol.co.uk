# T031 worklog

## What was done

Lifted the 🎨 appearance trigger out of `Header.js` and into `MatrixLayout.js` so it is available
on every page including the homepage (which does not render Header).

### Changes

- **`src/components/MatrixLayout.js`**: Added `useState` + `AppearancePicker` import. Renders a
  fixed top-right `<button className="global-appearance-btn">` and the `AppearancePicker` modal.
  The button renders regardless of `active` (matrix) state.

- **`src/components/Header.js`**: Removed `useState`, `AppearancePicker` import, and the 🎨
  button. Header now only renders the back-link and `NowPlaying`.

- **`src/styles/globals.css`**: Replaced `.header-appearance-btn` with `.global-appearance-btn`
  (fixed position, top: 1rem, right: 1rem, z-index: 10000 — above the CRT overlay at 9999).
  Uses CSS custom properties for colours (theme-driven). No hardcoded colours.

- **`CLAUDE.md`**: Updated the Components note to record that `MatrixLayout` owns the global
  appearance trigger.

## Checks

- `npm run lint`: 0 errors, 1 pre-existing warning (cache.js exhaustive-deps) — tolerated.
- `npm test`: 108 tests passed.
- `npm run build`: succeeded.

## Empirical verification

Manual UI verification is needed by the loop/human after push. Code inspection confirms:
- `MatrixLayout` wraps every page (via `_app.js`) — button will appear on `/` and all other pages.
- `Header` no longer imports or renders `AppearancePicker` — no duplicate.
- z-index 10000 > CRT overlay (9999), and CRT has `pointer-events: none` — button is clickable.
