# T031 worklog

## What was done

- `MatrixLayout.js`: imported `AppearancePicker` and `useState`; added `pickerOpen` state; rendered fixed `.global-appearance-btn` trigger button and `AppearancePicker` modal unconditionally (outside matrix conditionals so it's always present).
- `Header.js`: removed `AppearancePicker` import, `useState` import, `pickerOpen` state, and the 🎨 button. Header now only renders the back-link and `NowPlaying`.
- `globals.css`: replaced `.header-appearance-btn` / `.header-appearance-btn:hover` with `.global-appearance-btn` / `.global-appearance-btn:hover` using `position: fixed; top: 1rem; right: 1rem; z-index: 10001` — above the CRT overlay (9999) and page content, below the modal overlay (10000+); all colours via CSS custom properties.
- `CLAUDE.md`: updated the Components blurb to note that `AppearancePicker`'s trigger now lives in `MatrixLayout` (globally fixed top-right).

## Checks

- `npm run lint`: 0 errors, 1 pre-existing warning (dev/cache.js exhaustive-deps) — no change.
- `npm test`: 11 suites, 108 tests, all passed.
- `npm run build`: succeeded, 28 static pages generated.

## Observations

No live external calls needed. All verification done via build + static analysis.
