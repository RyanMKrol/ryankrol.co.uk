# T334 — Ownership pill tooltip on perfume cards

2026-07-08

## What I did
Wrapped the ownership `Badge` in `src/components/perfumeVariants/Variant6Hybrid.js` (~line 45)
in `<Tooltip label="What I own">`, matching the existing Longevity/Projection Tooltip usage in
the same file. No `position` prop and no extra `className` were passed, so it uses `Tooltip`'s
default `position='top'` behavior.

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 74 suites / 403 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=perfume node scripts/visual-check.mjs` — all 12 captures succeeded.
  Read `reviews-perfumes.png`: every card with an `item.ownership` value (SAMPLE / TRAVEL SIZE /
  FULL BOTTLE) still renders correctly with no layout regression — Longevity/Projection tooltip
  bars, seasons, application-spot list, and Fragrantica link all intact.
- Read `src/components/Tooltip.js` and `src/styles/globals.css` (`.tooltip-bubble` /
  `.tooltip-wrapper:hover`/`:focus-within`) to confirm the bubble placement: default (no
  `position` prop) renders `.tooltip-bubble` without the `--bottom` modifier, which has
  `bottom: 100%` — i.e. positioned above the wrapped element, shown on `:hover`/`:focus-within`.
  This can't be captured in a static screenshot (no synthetic hover/focus state), so placement
  was verified by reading the CSS rather than pixels, per the task's own verify guidance.

## Remaining
Nothing — task complete within scope (single file change).
