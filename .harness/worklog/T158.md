# T158 — Rebuild projects page

## Do
- Rebuilt `src/pages/projects/index.js`: `collection-review-header` with "projects" title +
  "what I've been building on github · sorted by recent activity" meta line.
- 3-col (desktop) / 1-col (mobile, <=900px) `.projects-grid` of `.project-card`s: repo name +
  star count top row, description, language dot (client-local `LANGUAGE_COLORS` map — TS
  `#3178C6`, Python `#3572A5`, Shell `#89E051`, JS `#f1e05a`, plus a few common fallbacks and a
  neutral default) + name, "updated Xago" (local `formatUpdatedAgo` helper), and topic pills using
  the existing `Badge` component (`variant="soft"`, `accentColor="var(--accent-projects)"`).
- Reused the existing `/api/github/repos` fetch — no data-layer change.
- Restyled `.project-card`/removed stale `.project-tag` rule in `src/styles/globals.css` with
  Collection design tokens; dropped `.project-card` from the old unscoped "Cyberpunk
  Enhancements" hover-state block (neon rules that predate the redesign and conflicted with the
  new card styling).

## Done when — verification
- `npm run lint`: 0 errors (6 pre-existing warnings, unrelated files).
- `npm test`: 35 suites / 241 tests passed.
- `npm run build`: succeeded, `/projects` prerendered.
- Manual (`run-app`): `npm run dev`, `curl localhost:3000/projects` → 200, and
  `curl localhost:3000/api/github/repos` confirmed real repo data (language `JavaScript` present
  in this repo's own listing) flows into the page. Did not have a TS/Python/Shell repo in the live
  data to eyeball at this time, but the color lookup is a plain object keyed by GitHub's exact
  language string, exercised the same way for every language.
