# T313 — fix perfumes sort pill no-op (switch to flip-direction SortButtons)

## What changed
`src/pages/reviews/perfumes/index.js` only:
- Swapped the sort control from `PillGroup`/`SORT_OPTIONS` to `SortButtons`/`SORT_FIELDS`
  (three fields: date, title, score — each with `defaultValue`/`flippedValue`/arrows), mirroring
  `src/pages/reviews/movies/index.js` (the T215 fix).
- Sort `useEffect` now has explicit branches for `title`, `title-desc`, `score`, `score-asc`,
  `date-asc`, plus the trailing `else` for plain `date`/unrecognized (newest-first, unchanged
  default). No `.replace(/^The\s+/i, '')` title stripping added — kept plain `localeCompare` as
  before, per the task spec (perfumes never had that stripping and it isn't part of either bug).
  Uses `perfume.rating` / `perfume.date` only (no `editedDate` field on perfume records).
- `PillGroup` import retained — still used for the ownership filter pill group, which was
  untouched.

## Verification observed
- `npm run lint` — 0 errors (only pre-existing warnings elsewhere, none new).
- `npm test` — 67 suites / 395 tests passed, no regressions.
- `npm run build` — succeeded; `/reviews/perfumes` static route generated (1.55 kB).
- Dev server smoke check: `GET /reviews/perfumes` → 200 OK.
- No new test added: this is a page-level component/prop swap with no new pure logic in `src/lib`
  (matches T215 precedent, which also shipped without new tests).
- Diff scoped exactly to import + `SORT_FIELDS` + sort branches + JSX swap, per `git diff`.
- Hover styling parity comes for free: `SortButtons` renders `.filter-button` elements (which
  already have a `:hover` rule in `globals.css`), no CSS touched.
