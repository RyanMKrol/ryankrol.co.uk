# T231: fix vertical-alignment bug in review-list header row

## Do

Removed the stray `margin-top: 1rem;` declaration from `.sort-buttons-container` in
`src/styles/globals.css` (was lines 797-803). The rule now reads:

```css
.sort-buttons-container {
  display: flex;
  align-items: center;
  gap: 1rem;
  flex-wrap: wrap;
}
```

No other files touched (single CSS-file fix, per scope).

## Verify

- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated: `no-img-element`,
  `exhaustive-deps` on `dev/cache.js`).
- `npm test` — 43 suites / 287 tests passed.
- `npm run build` — succeeded, all 32 pages generated.
- Manual UI check: started `npm run dev`, confirmed `/reviews/movies` and `/reviews/albums`
  both return HTTP 200. No screenshot-diffing tooling exists in this repo/environment, so
  verified the underlying CSS directly instead: `.collection-search-input`
  (`globals.css:2593-2601`) carries no `margin-top`/vertical margin of its own, and
  `.sort-buttons-container` no longer does either after this fix — with both flex siblings
  of `.collection-review-controls` (`align-items: center`) now having zero vertical margin,
  their margin boxes are symmetric and will center against each other instead of the
  sort-buttons row sitting 1rem lower than the search box.
