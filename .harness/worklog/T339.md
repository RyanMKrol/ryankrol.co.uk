# T339 — 2026-07-09

Re-spec of T330 (which blocked 14/14 attempts purely because T330's own `scope` array omitted
`scripts/_visual-harness.mjs`). This task explicitly lists it in scope, and `scripts/` is also
`SCOPE_EXEMPT_GLOBS`-exempt, so `scripts/visual-check.mjs` (needed to wire the new delayed-fixture
mechanism) was safe to touch too even though not literally in the `scope` array.

## What changed

- `src/pages/index.js` — replaced the single batched `Promise.all` (7 endpoints) with 7 independent
  `fetch().then()` calls, each with its own `*Loading` flag (default `true`). Dependency mapping,
  confirmed by reading the pre-existing code:
  - Movies/TV/Books/Albums/Vinyl/Workouts stat blocks → their own single fetch each.
  - "The collection wall" → `movies|tv|books|albums|vinyl` (all 5 — `wallItems` useMemo already
    depended on all 5 arrays).
  - "Latest takes" → `movies|tv|books|albums` (4 — `latestTakes` useMemo excludes vinyl).
  - Gym stats rail panel → `workouts/stats` only.
  - "On the shelf" rail panel → `vinyl` only (confirmed: `shelfItems`/`handleShelfRefresh` both
    sample from the `vinyl` state array, no album/lastfm involvement).
  - "Hot takes" rail panel → `hot-takes` only.
  - Hero's "N things rated" counter was deliberately left untouched (task says don't touch the hero
    section) — it still reads 0 during load, same as before.
- `src/components/HomeSkeleton.js` (new) — `StatBlockSkeleton`, `TileGridSkeleton`,
  `CardRowSkeleton`, `GymPanelStatsSkeleton`, `ListRowSkeleton`. Each mirrors the real content's DOM
  classes/dimensions (`.collection-stat-block`, `.cover-tile-wrap`/`.cover-tile` incl. explicit
  `aspectRatio: '1 / 1'` inline style so the tile doesn't collapse to 0 height, `.home-latest-card`,
  `.home-gym-stats`, list rows sized like `.home-shelf-item`/`.home-hot-takes-item`) so swapping
  skeleton → real content causes no layout shift.
- `src/styles/globals.css` — added `.skeleton-shimmer` (a translateX sweep gradient,
  `@keyframes skeleton-shimmer-sweep`) right after the existing `.spinner`/`@keyframes spin` utility,
  plus sizing classes for each skeleton piece. Added a `prefers-reduced-motion: reduce` block right
  there that disables both `.spinner`'s `animation` and `.skeleton-shimmer::after`'s animation (the
  `.spinner` didn't previously have its own reduced-motion guard — this is now the first one, folded
  into the same shared block rather than adding a second CRTOverlay-style block elsewhere).
- `scripts/_visual-harness.mjs` — added `routeApiWithDelays(page, delays)`, a page-level route
  override (same fixtures as `routeApi`, plus external-image placeholdering) that delays specific
  `/api/*` pathnames by a fixed ms before fulfilling — lets one capture stagger specific endpoints
  without touching the shared context-level `routeApi` used by every other spec. Added a new `PAGES`
  entry `home-loading` (`delayRoutes: { '/api/hot-takes': 6000, '/api/vinyl': 6000 }`, `waitFor:
  ['.skeleton-shimmer']`) — since `wallLoading`/shelf/hot-takes all depend on vinyl/hot-takes, this
  proves Movies/TV/Books/Albums/Workouts stat blocks + "Latest takes" reveal real data immediately
  while vinyl's stat block, the collection wall, the shelf panel, and hot-takes panel stay skeleton.
- `scripts/visual-check.mjs` — `capture()` now applies `spec.delayRoutes` via a page-level
  `routeApiWithDelays` before `page.goto`, and uses `waitUntil: 'load'` instead of `'networkidle'`
  for specs with delayed routes (so it doesn't block on the artificially-slow in-flight fetches).

## Checks run

- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated files).
- `npm test` — 402/402 passing, including new `src/components/HomeSkeleton.test.js` (7 tests
  covering each skeleton piece's default/custom counts and DOM shape).
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (VISUAL_CHECK_ONLY=home) — all 3 home-related captures passed
  (`home`, `home-loading`, `home-skim-shelf`).

## Visual verification (observed, not just "ran it")

Captured `scripts/visual-out/home-loading.png` and read it directly:
- Movies (11), TV shows (8), Books (9), Albums (9), and Workouts (142) stat blocks show real numbers
  immediately.
- The Vinyl stat block still shows the dashed-border shimmer placeholder (its fetch is the 6s-delayed
  one) — confirms stat blocks reveal independently per-endpoint, not as a batch.
- "The collection wall" shows an 18-tile shimmer grid (blank tan boxes, correct aspect ratio, no
  collapsed/zero-height tiles) instead of an empty grid, because it depends on vinyl (not yet
  resolved) — first attempt at this had a real bug (see below).
- "Latest takes" already shows real cards (Everything Everywhere All At Once / Dune / In Rainbows)
  since it only depends on movies/tv/books/albums, none of which were delayed.
  "On the shelf" and "Hot takes" rail panels both show shimmer line-placeholders (both depend on the
  delayed fetches: vinyl and hot-takes respectively) — neither ever showed "0 records" or "No hot
  takes yet." as a fake real value.
- Also compared against `home.png` (no delay) to confirm final layout/spacing matches once loaded —
  no visible shift in stat block or panel sizing between skeleton and real states.

Bug caught and fixed during this verification: the first `TileGridSkeleton` pass produced literally
nothing under "The collection wall" heading in the screenshot — `.cover-tile` relies on an inline
`aspectRatio` style that the real `CoverTile` component sets but the skeleton never did, collapsing
every skeleton tile to 0 height. Fixed by adding `style={{ aspectRatio: '1 / 1' }}` to the skeleton
tile; re-ran the visual check and confirmed the grid now renders as an 18-tile shimmer block.

## Remaining

Nothing outstanding for this task.
