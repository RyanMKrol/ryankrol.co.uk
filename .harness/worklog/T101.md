# T101 — rate-limit TMDB search route

## Do
Added `checkRateLimit`/`getClientIp` gate to `src/pages/api/tmdb/search.js` immediately after the
GET-method check and before param validation (20 req/60s per IP). Created
`src/pages/api/tmdb/search.test.js` (did not previously exist), mocking `../../../lib/rateLimit`
and `../../../lib/apiCache` (withApiCache passthrough) plus `global.fetch`, following the
`mockRes()` pattern from `src/pages/api/reviews/movies/add.test.js`.

## Observed
- Allowed case: `checkRateLimit` mocked `{allowed:true}` → handler proceeds, calls `fetch`, returns 200.
- Blocked case: `checkRateLimit` mocked `{allowed:false, retryAfterSeconds:42}` → handler sets
  `Retry-After: 42` header, returns 429 with a JSON `message`, and `fetch` is never called
  (asserted via `expect(global.fetch).not.toHaveBeenCalled()`).
- `npm run lint` — 0 errors, pre-existing warnings only.
- `npm test` — 24 suites / 178 tests passed, including the 2 new tests.
- `npm run build` — succeeded.

## Docs
Updated CLAUDE.md's file map row for `src/pages/api/tmdb/search.js` to note the new rate limit.
