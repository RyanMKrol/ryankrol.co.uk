# T350 — Owner picks a winning Top of Mind placement variant

## Decision

Winning variant: **2 — Hero band**.

A full-width band (`<section className="tom-variant-hero-band">`, src/pages/index.js:184-191) rendered
directly below the hero section (after the `NowPlaying` widget, before the `home-stats` stat tiles),
with a "Top of mind" label and the note's markdown body.

No further tweaks requested — land variant 2 exactly as built.

## For the finalize task (T351)

T351's spec has been updated to bake in this choice directly (variant 2 / Hero band), rather than
leaving it as "consume the winning variant number" — so T351 no longer depends on reading this worklog
to know which variant to keep.
