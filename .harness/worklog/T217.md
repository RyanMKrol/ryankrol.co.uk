# T217

Added `src/lib/dateFormat.js` exporting `formatEnglishDate(date)` and
`formatReviewDate(dateString)`, plus co-located `src/lib/dateFormat.test.js` (8 assertions
covering plain Date, ISO string, falsy, unparseable, valid DD-MM-YYYY, single-digit
day/month, and malformed input). Added a File map row for the new file to `CLAUDE.md`.

Nothing consumes the new helper yet — that's the three follow-up tasks in this cluster.

## Verified

- `npm run lint` — 0 errors, 6 pre-existing warnings (unchanged).
- `npm test` — 43 suites / 287 tests pass, including the new file.
- `npm run build` — succeeds.
