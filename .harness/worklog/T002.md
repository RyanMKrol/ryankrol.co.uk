# T002 Worklog — Register perfumes table + create-table script

## Changes Made

- `src/lib/constants.js`: Added `PERFUME_RATINGS_TABLE: 'PerfumeRatings'` to `DYNAMO_TABLES`.
- `src/scripts/createPerfumeTable.js`: Created new script mirroring `createWorkoutTables.js`, defines `PerfumeRatings` table with a single String partition key `id` (no sort key, no GSIs), waits for ACTIVE status using `waitUntilTableExists`.
- `package.json`: Added `"perfume:create-table": "node src/scripts/createPerfumeTable.js"` npm script.

## Verification

- `npm run build` — passed cleanly (27 static pages generated, no errors).
- Script not executed (T003 handles provisioning).
- Build output: only pre-existing `react-hooks/exhaustive-deps` warning in `dev/cache.js` (known, pre-existing).
