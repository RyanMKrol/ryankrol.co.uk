# T353 — 2026-07-10

## What I did
- `src/components/BulkBackfillList.js`: bumped the candidate cap from `.slice(0, 3)` to
  `.slice(0, 10)` in `onItemSettled` (line 63). Single shared component, so all 4 backfill
  pages (movies/tv/books/albums) get the new cap uniformly. Left `.bbl-candidates` as a plain
  vertical radio list — no scroll/collapse wrapper, per the owner's confirmation in the spec.
- `scripts/_visual-harness.mjs`: expanded `tmdbSearchResults` from 5 to 11 synthetic "Blade
  Runner" candidates so the harness actually exercises the new cap-at-10; updated the comment
  above it to reference T353/cap-at-10; updated the `reviews-movies-backfill-apply-all` FLOWS
  description from "capped at 3 candidates each" to "capped at 10 candidates each".
- `src/components/BulkBackfillList.test.js`: the existing cap test asserted 3-of-5 candidates
  and `'3 candidates found'`. Updated it to assert 10-of-15 and `'10 candidates found'` (test
  file, in scope per the harness's always-allowed test-file carve-out). The other three tests in
  the file mock only 3 candidates (under the new cap) so they were unaffected.

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated `<img>`/exhaustive-deps).
- `npm test` — 76 suites / 414 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=backfill node scripts/visual-check.mjs` — both captures passed:
  - `reviews-movies-backfill`: viewed the PNG — both rows show "10 candidates found" and render
    all 10 radio candidates as a plain vertical list, no clipping/overlap, just a longer scroll.
  - `reviews-movies-backfill-apply-all`: viewed the PNG — selecting a candidate from the
    expanded 10-item list on each row and clicking "Apply all selections" still saves both rows
    correctly ("saved" status shown for Blade Runner and Moon).

## Remaining
Nothing outstanding — task complete as specified.
