# T329 — 2026-07-08

## What I did
Converted the "Session volume" chart on individual exercise pages
(`src/components/ExerciseProgressCharts.js`) from a Bar chart to a Line chart, matching the
visual style of the adjacent "Estimated 1RM progress" / "Max weight progress" charts:

- `volumeChartData` dataset now uses `borderColor: chartIndigo`, `backgroundColor:
  gradientFill(chartIndigo)`, `borderWidth: 2`, `fill: true` — dropped the old per-point
  `backgroundColor` array (which highlighted the most recent session in `chartGrape`) and
  `borderRadius`.
- Swapped `<Bar data={volumeChartData} .../>` for `<Line data={volumeChartData} .../>`.
- Removed the now-unused `Bar` import (`react-chartjs-2`) and `BarElement` import +
  `ChartJS.register(...)` entry (`chart.js`) — nothing else in the file used them.
- Removed `chartGrape` from the `useChartTheme()` destructure since its only use was the
  dropped last-point highlight.

`ProgrammeOverviewCharts.js` (the /workouts page bar chart) was not touched — out of scope.

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files), no new warnings.
- `npm test` — 74 suites / 403 tests passed.
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (targeted via `VISUAL_CHECK_ONLY=exercise`) — captured
  `scripts/visual-out/exercise.png`. LOOKED at the screenshot: the "Session volume" card now
  shows an indigo line with a gradient-filled area beneath it, visually matching the coral
  1RM chart and marigold max-weight chart in the same row. No bars visible anywhere on the
  page.

## Remaining
Nothing outstanding — scope was limited to this one chart-shape conversion.
