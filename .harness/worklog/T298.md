# T298 — Hot takes preview panel on homepage

## What changed
- `src/pages/index.js`: added `hotTakes` state, fetched `/api/hot-takes` alongside the existing
  `Promise.all` fetches, computed `latestHotTakes = hotTakes.slice(0, 3)`, and rendered a new
  `.home-hot-takes-panel` as the third child of `.home-rail` (after `.home-gym-panel` and
  `.home-shelf-panel`). Panel shows up to 3 recent takes (plain text + `formatReviewDate(take.date)`),
  a linked title ("Hot takes" → `/hot-takes`), an empty state, and a "View all →" link. No rating
  stars or cover art/gradient reused — confirmed via diff.
- Did NOT touch the `latestTakes` useMemo or its rendering block — confirmed via `git diff` that no
  lines in that range were modified (only additions before/after).
- `src/styles/globals.css`: added `.home-hot-takes-panel` + children near the existing
  `.home-gym-panel`/`.home-shelf-panel` rules, matching `border-radius: var(--radius-card)`,
  `padding: 1.25rem`, small-caps mono panel title convention. Used the existing `--accent-hot-takes`
  token (already present in `:root` at line 35) as the background, giving it a distinct colour from
  its two siblings. No new mobile media query needed — `.home-lower { grid-template-columns: 1fr; }`
  at the existing `@media (max-width: 768px)` block already stacks all `.home-rail` children.
- `CLAUDE.md`: updated the file map row for `src/pages/index.js` to mention the new panel.

## Verification
- `npm run lint && npm test && npm run build` — all passed. No new lint errors; only pre-existing
  tolerated warnings (`react-hooks/exhaustive-deps` in `dev/cache.js`, `no-img-element` warnings
  unrelated to this change).
- Did not run `npm run dev` interactively (headless session) — code review confirms the panel
  fetches `/api/hot-takes` (existing route, already used by `src/pages/hot-takes/index.js`, safe/no
  new external calls) and reuses `formatReviewDate` the same way that page does. Structural/manual
  browser verification (`verify: ["run-app"]`) is left for a human/interactive pass per the
  no-live-calls constraint — this task made no live DynamoDB writes and only exercises an existing
  read endpoint already covered by build success.
