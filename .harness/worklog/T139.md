# T139: swap "Edited:" label for "Updated:" in ReviewCard

## Do

Changed the literal text `Edited:` to `Updated:` in `src/components/ReviewCard.js` (the
conditional block rendered when `item.editedDate` is truthy). No other files touched — the
`editedDate` field/prop name is unchanged everywhere (API routes, sort comparators, tests).

## Verify

- `npm run lint` — 0 errors, 3 pre-existing warnings (unchanged).
- `npm test` — 35 suites / 249 tests passed, none referenced the old "Edited:" string so
  none needed updating.
- `npm run build` — succeeded.
- `verify: ["run-app"]` was not performed live (no dev server run in this headless session);
  the change is a one-line JSX text swap confirmed by reading the file and grep — no other
  occurrence of "Edited:" exists in `src/`. The uppercase CSS rule
  (`.review-style-2 .review-date { text-transform: uppercase; }`) is generic and untouched,
  so it will apply to "Updated:" automatically.
