# T075 — Perfume "Application notes" field — form UI

## Done
- Added `APPLICATION_SPOTS` constant (Wrists, Elbows, Clavicles, Beard, Back of neck, Behind ears)
  to both `src/pages/reviews/perfumes/add.js` and `src/pages/reviews/perfumes/edit/[id].js`.
- Rendered six checkboxes for application spots; toggling builds an `applicationSpots` array on
  `formData`, wired into the existing submit payloads to `/api/reviews/perfumes/add` and
  `/api/reviews/perfumes/update` (T074 backend already accepts this field).
- Edit form pre-populates `applicationSpots` from the loaded review (`perfume.applicationSpots || []`),
  so reviews predating this field load with none ticked.
- Relabeled the "Notes" field to "Application notes" on both forms and updated the add form's
  placeholder from "Top/heart/base notes..." to "Any other notes about this wear...". `formData.notes`
  itself is unchanged (still a free-text textarea, unchanged wire format).

## Verified
- `npm run lint` — 0 errors (4 pre-existing warnings, unrelated files).
- `npm test` — 141/141 passing (no new tests needed; this is presentational form wiring, no new pure
  logic was added — `applicationSpots` reuses the existing generic array-toggle/submit pattern already
  covered by the Seasons checkboxes on the same forms).
- `npm run build` — succeeds, `/reviews/perfumes/add` and `/reviews/perfumes/edit/[id]` compile.
- Did NOT start `npm run dev` / exercise the live form + DynamoDB write, per harness rule against
  live calls to metered backends during verification — the JSON payload shape was confirmed by
  reading `src/pages/api/reviews/perfumes/{add,update}.js` (both already destructure and store
  `applicationSpots` from T074) and matches what these forms now submit.
