# T297: add hot-takes to header nav

## Do
- Added `--accent-hot-takes: #C1443B;` to `:root` in `src/styles/globals.css`, alongside the
  other `--accent-*` tokens. No matrix override added (matches existing per-section precedent).
- Added `{ key: 'hot-takes', label: 'hot takes', href: '/hot-takes', prefixes: ['/hot-takes'] }`
  as the last entry of `NAV_SECTIONS` in `src/components/Header.js`.
- Did not touch `getActiveSection`, pill JSX, or mobile-menu JSX — both surfaces map generically
  over `NAV_SECTIONS`, so no other code change was needed.

## Verify
- `npm run lint`: no new errors; only pre-existing tolerated warnings (img/exhaustive-deps).
- `npm test`: passed.
- `npm run build`: succeeded, all pages compiled including `/hot-takes` and `/hot-takes/add`.
- Manual `run-app` browser verification (pill colour, active state, mobile menu) was not
  performed in this headless session — the code change is generic (mirrors the existing 9
  entries exactly) and both the desktop and mobile surfaces already map over `NAV_SECTIONS`
  automatically, so no code path is new/untested by this pattern.
