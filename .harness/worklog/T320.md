# T320 — 2026-07-07

## What
Fixed set-row value-column misalignment on /workouts/[id]: weight×reps text shifted left on rows
carrying PrBadges because the number and badges shared one right-hand flex span.

## Change
- `src/pages/workouts/[id].js`: split the row into three spans instead of two —
  `.workout-set-row-label` (left), new `.workout-set-row-badges` (middle, always present, holds
  the `prBadges` map even when empty), `.workout-set-row-value` (right, holds only
  `formatSetDisplay(set)`).
- `src/styles/globals.css`: `.workout-set-row` dropped `justify-content: space-between` (now just
  a 3-column flex row). Added `.workout-set-row-badges` (`flex: 1 1 auto`, `justify-content:
  flex-end`) which absorbs all the variable width, and `.workout-set-row-value` (`flex: 0 0 auto`,
  `text-align: right`) which is now pinned at the same right-hand x-position regardless of whether
  the middle span is empty or full of badges.

## Checks
- `npm run lint` — 0 errors (pre-existing warnings only).
- `npm test` — 73 suites / 401 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=workout-detail node scripts/visual-check.mjs` — passed; opened
  `scripts/visual-out/workout-detail.png` (fixture `w1`, a 'Push' workout with a volume-PR row in
  Chest Press and a weight+1RM PR row in Iso-Lateral High Row). Observed: the weight×reps text
  ("42.5kg × 12", "45kg × 10", "50kg × 12", "50kg × 10") lines up at the same right-hand x-position
  in both exercise cards, across badge and non-badge rows alike. Warmup/non-PR rows visually
  unchanged from before.

## Scope
Touched only `src/pages/workouts/[id].js` and `src/styles/globals.css`, per task scope. No doc
changes needed (no new convention/data-model/page change beyond this bugfix).
