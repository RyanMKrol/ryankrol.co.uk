# T107 — SpraySpotCounter whole-card tap target + smaller buttons

## Change
`src/components/PerfumeCharacteristics.js`: `SpraySpotCounter` outer `<div>` converted to a
single `<button type="button" onClick={spray} onContextMenu={unspray}>` wrapping the emoji, poof
spans, spot-name label, and spray-count text — HTML forbids nested buttons, so the inner button
was demoted to a plain `<span>`. Neutralized default button chrome (`font: 'inherit'`,
`textAlign: 'center'`, `cursor: 'pointer'`) so the visual look is unchanged, just now the whole
card is clickable.

Shrunk footprint: emoji `fontSize` 1.75rem → 1.2rem, card `minWidth` 6.5rem → 5rem, `padding`
0.75rem 0.5rem → 0.5rem 0.4rem, `gap` 0.4rem → 0.3rem. Label/count font sizes left untouched
(`form-label` / 0.875rem).

`applicationSpotCounts`, `countsToApplicationSpots`, `ApplicationSpotsSprayer`,
`LongevitySlider`, `ProjectionSlider`, `SeasonsCheckboxes` untouched.

## Verify
- `npm run lint` — 0 errors, same 3 pre-existing warnings (unrelated files).
- `npm test -- PerfumeCharacteristics` — 5/5 pass, unchanged.
- `npm run build` — succeeds, `/reviews/perfumes/add` prerenders.
- Manual dev-server check not run (headless loop); verified via code read that clicking anywhere
  in the button (label/count/emoji) fires `spray()`, right-click fires `unspray()`, exactly as
  before, since the handlers moved from the inner element to the (now single) outer button which
  wraps all three visible pieces.

## Scope
Only `src/components/PerfumeCharacteristics.js` touched, plus this worklog.
