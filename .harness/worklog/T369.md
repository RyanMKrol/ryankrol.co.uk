# T369 — perfumes search: match designer, not just title

2026-07-11

## Do
- `src/pages/reviews/perfumes/index.js`: filtering `useEffect` now matches when the search term
  case-insensitively appears in EITHER `perfume.title` OR `perfume.designer` (guarded with
  `(perfume.designer || '')`), ANDed with the existing ownership-filter condition — same shape as
  albums (`artist`) and books (`author`).
- Updated the `SearchInput` placeholder from `search perfumes by title...` to
  `search by title or designer...`, matching the albums/books wording convention.

## Checks
- `npm run lint` — 0 errors (pre-existing warnings only, unrelated files).
- `npm test` — 80 suites / 480 tests passed.
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (via `VISUAL_CHECK_ONLY=perfumes`) — all 13 perfume captures
  succeeded. Looked at:
  - `reviews-perfumes.png` — page renders normally, new placeholder text visible in the search box.
  - `reviews-perfumes-search.png` — searching "aventus" (a title match) still correctly narrows to
    the single "Aventus" card ("Found 1 perfume") — no regression to title search.
  - Ownership pill flows (`perfumes-ownership-*`) still render/filter correctly, confirming the
    AND-combination with the search term is unchanged in structure.
- Did not add a new `FLOWS` entry for a designer-only search term — this is an expansion of the
  existing search state's matching logic, not a new interactive state, so the existing
  `reviews-perfumes-search` flow (title-based) remains the representative capture per the harness's
  own guidance (new state required only for a new sort/filter/toggle).

## Scope
Only `src/pages/reviews/perfumes/index.js` touched, per task scope.
