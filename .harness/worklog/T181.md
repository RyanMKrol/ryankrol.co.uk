# T181 — Variant2Editorial perfume card

## What was built
- `src/components/perfumeVariants/Variant2Editorial.js`: standalone presentational component,
  `{ item }` prop, no dependency on ReviewCard or a parent container. Editorial/magazine layout:
  large title, `by {designer}` byline, understated uppercase `type` label, `description` rendered
  as a bordered pull-quote, minimal `{rating}/10` text, italic "Best worn: ..." seasons line,
  and a "Continue reading on Fragrantica →" link. Deliberately omits longevity, projection, and
  applicationSpots per the spec.
- `src/styles/globals.css`: added new `.perfume-v2-*` rules only, appended after the existing
  `.perfume-v1-fragrantica-link` block. No existing rule edited or removed.

## Verification performed
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 245/245 passed.
- `npm run build` — succeeded.
- Manual render check: mounted the component on a temporary scratch page
  (`src/pages/__scratch_v2.js`, deleted before commit) with a sample `PerfumeRatings`-shaped
  record, started `npm run dev`, curled the page (200 OK), confirmed all `perfume-v2-*` classes
  appear in the HTML output and no errors appeared in the dev server log. Scratch file removed
  after verification — not part of the commit.

## Notes
- No DynamoDB / third-party API calls made.
