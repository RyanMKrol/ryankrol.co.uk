# T321 — 2026-07-07

## What
The `.pr-badge` pill (medal + axis label used by `PrBadge` in `src/components/Badge.js`) only
showed its full tinted background + visible border on `:hover`/`:focus-visible`; at rest it fell
back to the `soft` variant's washed-out ~10%-opacity tint with a `transparent` border.

## Change
`src/styles/globals.css` — merged the hover-only treatment into the base `.pr-badge` rule so it
applies permanently: `background-color: color-mix(in srgb, currentColor 22%, transparent)` +
`border: 1px solid currentColor`. Removed the separate `:hover`/`:focus-visible` background/border
override (kept `:focus-visible { outline: none }` only, since the visible border already serves as
the focus indicator). `currentColor` resolves from `PrBadge`'s existing `accentColor="var(--accent-workouts)"`
prop (unchanged, `src/components/Badge.js`), so this reuses the existing workouts accent token per
the task instructions rather than introducing a new colour. No JS change was needed — `Badge.js`
was already passing the CSS custom property through as `color`, which the CSS class now keys off
via `currentColor` at rest instead of only on interaction.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated).
- `npm test` — 401/401 passing, all 73 suites.
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (targeted via `VISUAL_CHECK_ONLY=workouts` then
  `VISUAL_CHECK_ONLY=workout-detail`, hermetic fixtures, no network/DynamoDB):
  - `workouts` — observed "volume PR" pills on every workout card rendering with the full
    tinted background + visible border, no interaction performed.
  - `workout-detail` — observed "volume PR", "weight PR", "1RM PR" pills on the exercise set
    breakdown all rendering identically at rest (medal + label legible against the tint).
  - Did not re-check Matrix theme rendering pixel-by-pixel (harness doesn't toggle it), but the
    fix stays entirely within the existing `currentColor`/CSS-custom-property system already used
    by the (previously working) hover state, so it inherits the same Matrix-theme compatibility.

## Scope
Only `src/styles/globals.css` touched; `src/components/Badge.js` needed no change since it already
threaded the workouts accent colour through as `color` for `currentColor` to pick up.
