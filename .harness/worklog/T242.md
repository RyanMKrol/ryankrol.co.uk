# T242: fix masonry reading order on /reviews/tv

## Do

Migrated `src/pages/reviews/tv/index.js` to use the shared `MasonryColumns` component +
`useResponsiveColumnCount` hook, mirroring the already-completed `src/pages/reviews/movies/index.js`
migration:

- Imported `MasonryColumns` (`../../../components/MasonryColumns`) and `useResponsiveColumnCount`
  (`../../../hooks/useResponsiveColumnCount`).
- Called `useResponsiveColumnCount(3, 900)` at top level (before the loading/error early returns),
  matching the movies page's hook placement.
- Replaced the plain `<div className="poster-banner-grid">{pagedTvShows.map(...)}</div>` block with
  `<MasonryColumns items={pagedTvShows} columnCount={columnCount} className="poster-banner-grid"
  columnClassName="poster-banner-grid-col" renderItem={...} />`, keeping the existing `tvGradients`
  indexing unchanged.

`src/styles/globals.css` already had the flex-based `.poster-banner-grid` / `.poster-banner-grid-col`
CSS from the movies task, so no CSS changes were needed (verified via `grep`).

## Done when

- `npm run lint` — clean (only pre-existing warnings unrelated to this file).
- `npm test` — 48 suites / 308 tests passed.
- `npm run build` — succeeded, `/reviews/tv` and `/reviews/movies` both built as static pages.
- Manual verification in a browser (dev server) was not performed in this headless session — code
  changes exactly mirror movies/index.js's already-verified working pattern, and the shared CSS was
  confirmed unchanged/already-migrated.
