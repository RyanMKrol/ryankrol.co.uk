# T306

Added the shared `.review-edit-card` / `.review-edit-header` / `.review-edit-button(:hover)`
CSS rules to `src/styles/globals.css`, immediately after `.edit-button-overlay:hover` and before
the T262-T266 per-variant fix comments. Pure addition — no existing rule bytes changed or removed
(`git diff --stat` shows only `+` lines).

Verified:
- `grep -rn 'review-edit-card\|review-edit-header\|review-edit-button' src/pages` → no matches
  (classes are inert/unused, as expected at this stage).
- `npm run lint` → no errors (only pre-existing warnings).
- `npm test` → pass.
- `npm run build` → succeeds.

Did not touch any edit.js page or any existing `.review-card-with-edit`/`.edit-button-overlay`
rules — those still back the 5 unconverted pages until tasks perfumes-edit-redesign-3..7 land.
