# T347 — 2026-07-10

## Finding: already satisfied by prior work, no CSS change made

Investigated adding `margin-left: 0.3rem` to `.pr-badge` as the spec's default instruction said.
Before applying it, checked whether the sibling sizing/spacing task
(`pr-pill-size-round2-spacing-size-align`, landed as T346, commit 1c5b694) had already introduced
a wrapper with its own gap — the spec explicitly flagged this as a possible build-order collision
to reconcile rather than blindly stack.

Confirmed T346 already landed:
- `/workouts` (`src/pages/workouts/index.js`): PR badges render inside
  `.workout-session-exercise-badges`, which already has `display: inline-flex; gap: 0.3rem;
  margin-left: 0.5rem` (src/styles/globals.css:2019-2025) — exactly the 0.3rem spacing this task
  asks for, already applied via flex gap instead of margin.
- `/workouts/[id]` (`src/pages/workouts/[id].js`): PR badges render inside
  `.workout-set-row-badges`, which already has `gap: 0.4rem` (src/styles/globals.css:2341-2348).

Adding `margin-left: 0.3rem` directly to `.pr-badge` (the task's literal instruction) would have
stacked on top of these existing gaps — doubling to 0.6rem on `/workouts` and producing an uneven
0.7rem on `/workouts/[id]` — a regression, not a fix. Per the task's own reconciliation guidance
("prefer leaving only one non-conflicting source of gap... use judgement to avoid doubled
spacing"), the correct action is to make NO change, since the visual outcome (even ~0.3-0.4rem gap
between adjacent pills) is already achieved.

## Visual verification (observed)

- `VISUAL_CHECK_ONLY=workouts node scripts/visual-check.mjs` — 7/7 captures passed. Current
  fixture data for `/workouts` (src/scripts/_visual-harness.mjs) doesn't happen to include a
  multi-PR-badge exercise row in the list view's fixture, so no side-by-side pill pair was visible
  there to eyeball directly — but the CSS mechanism (`gap: 0.3rem` on the flex wrapper) is
  structurally identical to what would be added.
- `VISUAL_CHECK_ONLY=workout-detail node scripts/visual-check.mjs` — 1/1 passed. Opened
  `workout-detail.png`: "Iso-Lateral High Row (Machine)" set 1 shows both a "Weight" and a "1RM"
  PR pill side by side with clean, even spacing between them (not flush) — confirms the
  `.workout-set-row-badges` gap already delivers the done-when criterion on `/workouts/[id]`.
- Single-PR rows (e.g. Chest Press's "Volume" pill) are unaffected — no leading margin/gap issue
  before the pill relative to the exercise-name text, matching the "unaffected" done-when clause.

## Checks run

- `npm run lint` — 0 errors, 6 pre-existing `no-img-element` warnings (unrelated, unchanged).
- `npm test` — 75 suites / 402 tests passed.
- `npm run build` — succeeded.

## Outcome

No files changed (task's scope file `src/styles/globals.css` untouched — the done-when state was
already true). Nothing to commit for the code; this worklog is the only artifact.
