# T277 — Masonry layout for albums grid

## Do
- Imported `MasonryColumns` + `useResponsiveColumnCount` into `src/pages/reviews/albums/index.js`,
  mirroring `src/pages/reviews/books/index.js`.
- Added `const columnCount = useResponsiveColumnCount(2, 700);` alongside other page state.
- Replaced the plain `<div className="square-cover-grid">{...map...}</div>` block with a
  `<MasonryColumns>` call using `className="square-cover-grid"` /
  `columnClassName="square-cover-grid-col"`, keeping `albumGradients[index]` and the key keyed off
  the true original index that `MasonryColumns`' `renderItem` callback supplies.
- In `src/styles/globals.css`, converted `.square-cover-grid` from CSS grid (2 cols, collapsing to
  1 col under a 700px media query) to a flex row (`display: flex; gap: 1.5rem;`) and added
  `.square-cover-grid-col` (flex column, `flex: 1 1 0; min-width: 0;`), mirroring
  `.poster-banner-grid` / `.poster-banner-grid-col`. Removed the now-dead `@media` block — column
  collapse is handled by the `useResponsiveColumnCount` hook, same as the other masonry pages.
- Left `Albums()`'s sort/filter/pagination logic untouched — pure layout-mechanism swap.

## Done when — verified
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this change).
- `npm test` — full suite passes.
- `npm run build` — succeeds; `/reviews/albums` builds statically same as before.
- Not run live in a browser (headless session) — verified by code inspection: `MasonryColumns`
  distributes items round-robin across `columnCount` columns and its `renderItem(item, index)`
  callback passes back the true original index (`indexInColumn * columnCount + columnIndex`), so
  `albumGradients[index]` still lines up correctly with the pre-distribution album order — same
  mechanism already used and verified on `/reviews/books`.
