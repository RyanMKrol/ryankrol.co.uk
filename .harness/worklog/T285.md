# T285: perfume Type select caret/padding fix

## Do
- Added `.collection-form-select` in `src/styles/globals.css` right after the
  `.collection-form-input:disabled` rule: `appearance: none` (+ webkit/moz prefixes), a custom
  inline-SVG chevron (`--color-ink-mute` value `#8A837A` hardcoded, since data-URIs can't reference
  CSS vars) positioned `right 1rem center` at `0.6em`, and `padding-right: 2.5rem` to clear it.
- Added `collection-form-select` alongside the existing `collection-form-input` className on both
  `<select id="type">` elements: `src/pages/reviews/perfumes/add.js` and
  `src/pages/reviews/perfumes/edit/[id].js`. No other attributes/markup touched.

## Verify
- `npm run lint` — 0 errors, same 6 pre-existing warnings as before the change.
- `npm test` — 375/375 passed.
- `npm run build` — succeeded, same pre-existing warnings only.
- Visual check not run live (headless session); the CSS change is scoped and mirrors the existing
  `.collection-form-input` pattern, and both select elements now share the identical class pair.
