# T340 — 2026-07-09

## What
Added a "total volume" stat cell to the home page's black "This year at the gym" panel
(`src/pages/index.js`), between the existing "sessions" and "best session vol" cells.

- Added `const totalVolume = workoutStats?.totalVolume ?? null` alongside the existing
  `bestSessionVolume` destructuring (~line 129).
- Inserted a new `.home-gym-stat` cell inside `.home-gym-stats` as the second child, mirroring
  the existing best-session-volume cell's rendering exactly: value or `—` fallback, conditional
  trailing `<span className="home-gym-stat-unit">kg</span>`, label `total volume`.
- No CSS changes — `.home-gym-stats` is `display:flex; gap:2rem` with no fixed column count, so
  the extra cell laid out naturally without any wrapping/overflow issue observed.

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files), no new warnings.
- `npm test` — 75 suites / 402 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=home node scripts/visual-check.mjs` — all 3 matching captures passed
  (`home`, `home-loading`, `home-skim-shelf`).

## Observed
Opened `scripts/visual-out/home.png` (1440×900 desktop viewport, the only viewport this harness
captures for the home page — there is no dedicated mobile-width home screenshot in
`scripts/_visual-harness.mjs`'s `PAGES`/`FLOWS`). The gym panel renders three cells in the
correct order — "142 SESSIONS", "982340kg TOTAL VOLUME", "11200kg BEST SESSION VOL" — all
legible, no overlap or clipping, consistent kg-unit styling across the two volume cells.

Mobile-width rendering was NOT independently verified via a screenshot, since the harness has no
mobile capture for the home route. Given `.home-gym-stats` is a plain flex row with `gap` and no
fixed column count (`src/styles/globals.css` ~3187-3191), a third cell is expected to wrap onto a
new line at narrow widths the same way a fourth stat-block cell already does elsewhere on this
page — no new CSS was added since there was nothing to observe that indicated a problem.

## Remaining
None — task scope (`src/pages/index.js` only) fully covered.
