# T114 — books id-keyed table + id-based edit URLs

## Do
- `src/lib/constants.js`: `BOOK_RATINGS_TABLE` → `'BookRatingsV4'`.
- `add.js`: assigns `id: randomUUID()` on create.
- `update.js`: accepts `originalId`, `GetCommand`/`PutCommand` keyed `{ id: originalId }`,
  removed the delete-old-item/recreate branch (was dead code since title/author inputs are
  disabled in the edit form); `id` never regenerated.
- `delete.js`: accepts `{ id, password }`, `DeleteCommand` keyed `{ id }`.
- `index.js`: unchanged — table swap is automatic via `constants.js`.
- `edit.js`: list `key`/edit link now use `book.id` instead of the `'|'`-joined composite.
- `edit/[id].js`: resolves the book via `b.id === decodeURIComponent(id)`; submit/delete payloads
  send `originalId`/`id` instead of `originalTitle`+`originalAuthor` / `title`+`author`.
- Updated `add.test.js` / `update.test.js` for id-based add/update.
- Updated `CLAUDE.md` data-model row and `README.md`'s `#### BookRatingsV4 Table` section.

## Verify (observed)
- `npm run lint` — 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test` — 30 suites / 212 tests passed.
- `npm run build` — succeeded, all routes generated including `/reviews/books/edit/[id]`.
- `grep -rn "originalTitle\|originalAuthor" src/pages/api/reviews/books src/pages/reviews/books`
  — no matches (albums has its own unrelated `originalTitle`/`originalArtist`, left untouched).
- `grep -rn BookRatingsV3 src/` still matches `src/scripts/*` (out of scope for this task — those
  are the T113 migration/backfill scripts that intentionally read from the old table for
  one-time/rollback purposes and are left as-is per their own docstrings).
