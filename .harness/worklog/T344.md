# T344 — Review perfume card rating/Best-for layout variants and record a decision

## Decision

Winner: **rating next to title** — the rating (pips + numeric score) sits inline beside the title,
with no dedicated row of its own. Full reasoning and the comparison history recorded in
`.harness/docs/designs/perfume-card-variants-decision.md` under "Decision — T343/T344 rating
position sweep (2026-07-10)".

The `best-for` position question and the enlarged-rating variants (`big-rating`/`header-rating`)
were dropped from the comparison before it reached a side-by-side review — the owner rejected
enlarged rating text outright without needing to see it, and asked to strip the sweep down to a
rating-position-only decision between 3 layouts (baseline / rating-below-header / rating-next-to-title).

## Landed directly (not deferred to a separate task)

Per the owner's explicit request to keep going in this same session:
- `src/components/perfumeVariants/Variant6Hybrid.js` now renders ONLY the winning layout — the
  `variant` prop and all switching logic have been removed entirely.
- `src/pages/dev/perfume-card-variants.js` (the T343 comparison page) has been deleted.
- The losing layouts' CSS (`.perfume-t343-rating-number-big`, the header-scoped rating-row margin
  rule) was already removed in an earlier pass this session; no further dead CSS remains.
- `scripts/_visual-harness.mjs`'s stale `dev-perfume-card-variants` PAGES entry was removed — the
  production `/reviews/perfumes` page's existing visual-check coverage (`reviews-perfumes` +
  ownership-filter/tooltip flows) already exercises `Variant6Hybrid.js` directly.

## Verification

`npm run lint` (clean, only pre-existing warnings), `npm test` (414/414 pass), `npm run build`
(succeeds). Visually confirmed via Playwright screenshots of both the (now-deleted) dev comparison
page before removal and the live `/reviews/perfumes` page after landing — the winning layout
renders correctly with no regression to any other card content.
