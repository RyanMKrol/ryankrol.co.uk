# T207

Added `'/programmes'` to the `workouts` section's `prefixes` array in `NAV_SECTIONS`
(`src/components/Header.js`), so `getActiveSection('/programmes')` matches the `workouts`
section via its existing `startsWith` check. No other lines changed.

Verified:
- `npm run lint` — 0 errors, 6 pre-existing warnings (unchanged).
- `npm test` — 267 passed.
- `npm run build` — succeeds; `/programmes` route present in output.
- Not verified live in a browser (headless session) — the change is a one-line array
  addition consumed by `getActiveSection`'s existing `pathname.startsWith(prefix)` logic,
  which already has this exact behavior for `/exercises` against the same section, so the
  mechanism is proven by the existing code path.
