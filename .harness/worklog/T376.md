# T376 — Fix home page rail panel headers

## Built
Made all three rail panel headers on the home page into links with consistent styling:

1. **src/pages/index.js (line 236)**: Converted gym header from `<p>` to `<Link href="/workouts">` 
2. **src/styles/globals.css**: 
   - Added `.home-gym-panel-title.home-gym-panel-link` with margin reset, text-decoration: none, cursor: pointer
   - Added `.home-gym-panel-title.home-gym-panel-link:hover` with underline
   - Fixed stray divider by changing `.home-hot-takes-item:first-of-type` to `.home-hot-takes-panel-header + .home-hot-takes-item` (adjacent sibling selector)
   - Added white color to `.home-hot-takes-panel-title.home-hot-takes-panel-link` and `:visited` state

## Verification
- **npm run lint**: 0 errors, 6 pre-existing warnings ✓
- **npm test**: 469 tests pass ✓
- **npm run build**: Success ✓
- **Visual check**: Ran full `node scripts/visual-check.mjs`, all 82 captures succeeded. Reviewed home page screenshot ("home.png"): all three headers render correctly:
  - Gym header "THIS YEAR AT THE GYM" is now a white link matching shelf/hot-takes styling
  - Hot takes header "HOT TAKES" renders in white (not blue/purple default link colour)
  - No divider line above first hot take ("Pineapple belongs on pizza..."), but dividers between successive takes remain intact

All three panels now have consistent link styling with proper colours and hover states.
