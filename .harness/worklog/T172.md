# T172: Random click-through collection wall across 5 content types

## What changed
- `src/pages/index.js`:
  - Added `pickRandomSample` (Fisher-Yates) helper; removed now-dead `sortByRatingDesc`.
  - Added `bookCoverUrl` import from `../lib/openlibrary` and `WALL_KIND_HREF` map.
  - Rebuilt `wallItems` as a `useMemo` over movies/tv/books/albums/vinyl: up to 4 random
    items sampled per type, then shuffled and capped to 18 overall, each item carrying
    `kind` + `href`.
  - Wrapped each `CoverTile` in a `next/link` `Link` pointing at the item's `href`
    (`/reviews/movies`, `/reviews/tv`, `/reviews/books`, `/reviews/albums`, `/vinyl`).
- `src/styles/globals.css`: added `.home-wall-tile-link` (block display, no underline/link
  color, `translateY(-2px)` lift on hover) near `.home-wall-grid`.
- `CLAUDE.md`: updated the file-map row for `src/pages/index.js` to describe the wall's
  new random/click-through behaviour.

## Verification observed
- `npm run lint`: 0 errors, 6 pre-existing warnings (no new ones).
- `npm test`: 35 suites / 241 tests passed.
- `npm run build`: succeeded, `/` still prerenders static.
- `npm run dev` + `curl localhost:3000/`: 200 OK, `home-wall-grid` present in the
  server-rendered HTML (wall items themselves are client-fetched in `useEffect`, same as
  before this change, so they don't appear in the initial SSR payload — expected, matches
  existing site pattern).
- Confirmed via grep that `sortByRatingDesc` has no remaining call sites before removal.
- Did not make any live third-party/DynamoDB calls; verified by reading code + build only.
