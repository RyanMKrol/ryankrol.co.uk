# T385 Worklog

## Summary
Fixed the warm-up volume filter on the /workouts per-session cards by removing the `set.type !== 'warmup'` guard from the `getTotalVolume` helper function in `src/pages/workouts/index.js`.

## Changes Made
- **File:** `src/pages/workouts/index.js` (lines 73–81)
- **Change:** Removed the `set.type !== 'warmup'` condition from the `getTotalVolume` reducer
- **Before:** `set.type !== 'warmup' && set.weight_kg && set.reps`
- **After:** `set.weight_kg && set.reps`

This ensures warm-up sets with weight and reps are now included in the per-session volume total displayed on each workout card, matching the stored `totalVolume` in DynamoDB (which includes warm-ups) and the volume shown in programme-stats and homepage.

## Verification Completed

### Lint Check
✅ `npm run lint` — no new errors (only pre-existing warnings)

### Test Suite
✅ `npm test` — all tests pass

### Build
✅ `npm run build` — succeeds with no errors

### Visual Check
✅ `node scripts/visual-check.mjs` — completed successfully
- Captured `/workouts` page screenshot (`workouts.png`)
- Volume metric renders correctly on the workout cards
- All ~65 page and interaction flow screenshots captured without issues

## Result
All Definition of Done criteria met. The card's displayed volume now includes warm-up volume and aligns with stored/homepage/programme-stats surfaces.
