# T284 — display ownership on perfume review card

## Do
Added ownership display to `src/components/perfumeVariants/Variant6Hybrid.js`. Third element in
the existing `.perfume-v1-rating-row`, conditionally rendered only when `item.ownership` is set:

```jsx
{item.ownership && (
  <Badge accentColor="var(--accent-perfumes)" variant="soft">{item.ownership}</Badge>
)}
```

Reused the existing `Badge` import (already present for the header `item.type` badge). Confirmed
`Badge` supports `variant="soft"` (tinted background/coloured text) vs the default `solid` used by
the header badge, so the two are visually distinct per the doc comment in `src/components/Badge.js`.
No new CSS — the existing `.perfume-v1-rating-row` flex row already lays out its children with gap.
No changes to form pages, API routes, or `src/lib/perfumes.js`.

## Verify
- `npm run lint` — no errors (only pre-existing warnings unrelated to this file).
- `npm test` — 375 passed, 375 total.
- `npm run build` — succeeded, `/reviews/perfumes` built as static page.
- `npm run dev` — started clean, `GET /reviews/perfumes 200`. The page fetches review data
  client-side via `useEffect`, so a plain `curl` of the server HTML doesn't show the badge (data
  isn't in the initial markup) — this is expected per the existing client-fetch architecture, not a
  regression. Did not seed/write real DynamoDB data to visually confirm the badge with a live
  `ownership` value, per the no-live-writes rule; the conditional rendering logic was verified by
  code inspection instead (renders nothing when `item.ownership` is undefined, renders the exact
  string when set).

## Result
done T284
