# T151 — Rebuild TV list page with poster-banner ReviewCard variant

## What changed
- Rebuilt `src/pages/reviews/tv/index.js` to mirror the movies list page structure from T150:
  header/meta line (`{rated} rated · avg {avg}★ · {n} this year`), `SearchInput`, `PillGroup`
  sort (date/title/score), 3-col `poster-banner-grid` of `ReviewCard` with `styleVariant="poster-banner"`.
- Added `summarizeTvShows` (mirrors `summarizeMovies`) for the meta line stats.
- Used `accentColor="var(--accent-tv)"` for the sort pills — the `--accent-tv: #4B4DED` token
  already existed in `globals.css` (added in an earlier task), so no CSS changes were needed.
- `ReviewCard.js` was NOT modified — the poster-banner variant already reads `item.title` /
  `item.date` / `item.rating` / `item.review_text` generically for both `movie` and `tv` types,
  so it worked unchanged for TV data.

## Verify
- `npm run lint` — 0 errors, pre-existing warnings only.
- `npm test` — 235 tests passed.
- `npm run build` — succeeded, `/reviews/tv` built as a static page.
- `npm run dev` + `curl localhost:3000/reviews/tv` — 200, page compiles and renders (loading
  state shown since data fetch is client-side via `useEffect`, same as movies page).

## Notes
- No pagination in this task per spec (deferred to T155).
- Pixel-fidelity tuning against the `3b.png`/`4g.png` renders deferred to T166 per spec.
