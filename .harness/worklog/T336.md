# T336 — 2026-07-08

## What
`src/pages/workouts/[id].js` was formatting the date StatBlock with a local `formatDate`
helper (`toLocaleDateString('en-GB', {day:'2-digit', month:'2-digit', year:'numeric'})`
then `.replace(/\//g, '-')`), producing stale numeric `DD-MM-YYYY` output (e.g.
`05-07-2026`) while `src/pages/workouts/index.js` already used the shared
`formatEnglishDate` from `src/lib/dateFormat.js` for English long-form dates.

## Change
- Removed the local `formatDate` function (was lines ~68-73).
- Imported `formatEnglishDate` from `../../lib/dateFormat`.
- Updated the DATE StatBlock's `value` prop to `formatEnglishDate(workout.start_time)`.
- No other StatBlock/value on the page touched.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated files).
- `npm test` — 74 suites / 403 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=workout node scripts/visual-check.mjs` — 8/8 captures passed.
  Viewed `scripts/visual-out/workout-detail.png`: the DATE StatBlock now renders
  "5 July 2026" (English long-form), matching the list page's date style. Duration,
  volume, exercises StatBlocks and PR badges unchanged.

## Remaining
None — task complete.
