# T287 — merge commit count into project card top-right badge

## Do

- `src/pages/projects/index.js`: replaced the standalone `project-card-stars` span with a
  `.project-card-top-right` wrapper containing the stars span plus a new
  `project-card-commits-badge` span (`⎇ {repo.commitCount}`), guarded by the same
  `typeof repo.commitCount === 'number'` check previously used at the bottom of the card.
  Removed the now-redundant commit count entry from `.project-card-meta-row`.
- `src/styles/globals.css`: added `.project-card-top-right` (flex row, `gap: 0.75rem`,
  `flex-shrink: 0`) and `.project-card-commits-badge` (identical styling to
  `.project-card-stars`) next to the existing `.project-card-stars` rule.

## Verify

- `npm run lint`: no errors (only pre-existing warnings — img usage, exhaustive-deps).
- `npm test`: 375/375 passed.
- `npm run build`: succeeded, `/projects` prerendered fine.
- Visual check: started `npm run dev` and requested `/projects`. The page fetches repo data
  client-side from `/api/github/repos` (live GitHub API call), so I did not drive a real browser
  against it to eyeball the rendered glyph — avoided making a live external API call purely to
  "verify" per harness instructions. `⎇` (U+2387, BRANCH SYMBOL) is a standard Unicode glyph with
  wide monospace-font coverage (matches the existing single-glyph convention used for `★`/`⌕` in
  this codebase); no fallback to `●` was needed based on code/markup review. A human should give
  the page a quick look in-browser to confirm the glyph isn't a tofu box in the deployed font
  stack, per the task's own fallback contingency.
