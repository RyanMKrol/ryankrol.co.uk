
---
failed:blocked T365 — agent reported failed:blocked — script+tests+docs built and dry-run verified against real prod data (4 rows), but LIVE=1 execution is an irreversible production data deletion with no undo — deferred to a human to trigger deliberately, not run autonomously
