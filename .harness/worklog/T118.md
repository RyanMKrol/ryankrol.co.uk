# T118: switch vinyl to id-keyed VinylCollectionV2 table

## What changed
- `src/lib/constants.js`: `VINYL_COLLECTION_TABLE` now `'VinylCollectionV2'`.
- `src/pages/api/vinyl/add.js`: generates `id: randomUUID()` (via `crypto`) alongside the
  existing `title`/`artist`/`thumbnail`/`lastfm` fields on create.
- `src/pages/api/vinyl/index.js`: unchanged — already reads via
  `scanTable(DYNAMO_TABLES.VINYL_COLLECTION_TABLE)`, which now resolves to `VinylCollectionV2`.
- `src/pages/api/vinyl/add.test.js`: added an assertion that the created item's `id` matches a
  UUID pattern.
- Docs: `CLAUDE.md` data model row and `README.md`'s `#### VinylCollectionV2 Table` section
  updated to describe the new `id`-keyed shape, matching the pattern used for the four review
  tables' V4/V3 cutovers.

## No new UI
Confirmed `src/pages/vinyl/` still only has `index.js` + `add.js`, and `src/pages/api/vinyl/`
still only has `index.js` + `add.js` (plus their test files) — no edit/delete page or route was
added, per the task's explicit scope note.

## Verification observed
- `npm run lint`: 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test`: 32 suites / 234 tests passed, including the updated `add.test.js`.
- `npm run build`: succeeded; `/api/vinyl`, `/api/vinyl/add`, `/vinyl`, `/vinyl/add` all built.
- `grep -rn "VinylCollection[^V]" src/` only matches `src/scripts/migrateVinylToIdTable.js`
  (comments + its `SOURCE` constant, intentionally referencing the legacy table) — no other bare
  `VinylCollection` references remain in application code.
- Did not make any live DynamoDB calls; verified end-to-end behavior by reading the code path
  and the passing unit tests (which mock `docClient`).
