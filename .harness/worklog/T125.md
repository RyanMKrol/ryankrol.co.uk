# T125

## Do
Updated both date-sort branches (`date-desc` and default most-recent-first) in
`src/pages/reviews/tv/index.js` to prefer `editedDate` over `date` when computing the
comparison date, matching the pattern already applied to movies (T124).

## Verify
- `npm run lint` — 0 errors, same 3 pre-existing warnings as before (dev/cache.js exhaustive-deps,
  movies/edit and tv/edit `<img>` warnings).
- `npm test` — 34 suites / 238 tests passed.
- `npm run build` — succeeded, all 32 pages generated.
- Read-through confirms: records without `editedDate` fall back to `date` (no regression);
  records with `editedDate` sort using it instead.

## Result
done
