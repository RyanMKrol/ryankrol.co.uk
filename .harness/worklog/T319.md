# T319 — outline style for perfume card ownership pill

2026-07-07

## What
Added a new `Badge` variant, `"outline"` (`src/components/Badge.js`), alongside the existing
`"solid"`/`"soft"` values — extended the ternary at Badge.js:13-15 into a 3-way branch. The
`outline` style: `{ backgroundColor: 'var(--color-card)', color: fill, border: '1px solid
var(--color-border-strong)' }`.

Changed only the ownership pill instantiation in
`src/components/perfumeVariants/Variant6Hybrid.js` (line 46) from `variant="soft"` to
`variant="outline"`. The adjacent type pill (`variant="solid"`, no prop change) and every other
`Badge`/`PrBadge` call site (`PrBadge` in Badge.js, GitHub topic pills in
`src/pages/projects/index.js`) were left untouched — both still use `variant="soft"`.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated).
- `npm test` — 73 suites / 401 tests passed.
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (targeted `VISUAL_CHECK_ONLY=perfume`, then `workouts`, then
  `projects`) — all captures passed.

## Visual verification observed
- `reviews-perfumes.png`: on every card, the ownership pill (FULL BOTTLE / SAMPLE / TRAVEL SIZE)
  now renders with a solid white/card background and a visible thin border, text in the perfume
  accent (purple). The adjacent type pill (EDP/PARFUM) is unchanged — solid purple fill, white
  text.
- `workouts.png`: the `PrBadge` "volume PR" medal pills render exactly as before — tinted soft
  background, no border.
- `projects.png`: the green GitHub topic pills (nextjs/react/dynamodb/etc.) render exactly as
  before — tinted soft background, no border.

## Remaining
Nothing outstanding for this task.
