# T194 — Strip emoji + lowercase the h1 title on all 6 /add pages

## What changed
Edited the `<h1 className="page-title">` line in each of the 6 `/add` pages: removed the
leading emoji + following space, lowercased the remaining text. No other lines touched.

- `src/pages/reviews/movies/add.js:89` → `add movie review`
- `src/pages/reviews/tv/add.js:89` → `add tv show review`
- `src/pages/reviews/books/add.js:96` → `add book review`
- `src/pages/reviews/albums/add.js:97` → `add album review`
- `src/pages/reviews/perfumes/add.js:127` → `add perfume review`
- `src/pages/vinyl/add.js:91` → `add vinyl record`

## Verification
- `grep -rn "page-title" <6 files>` — confirmed all 6 titles match the target text exactly,
  no emoji, fully lowercase.
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated to this change).
- `npm test` — 37 suites / 248 tests, all passed.
- `npm run build` — succeeded, all pages compiled including the 6 `/add` routes.
- `git diff --stat` — confirmed exactly 6 files changed, 1 line each (insert+delete pair),
  no other files touched.
- Did not run `npm run dev` + manual browser check (headless/unattended run, no browser
  available) — relying on lint/test/build passing plus the grep confirming exact text; the
  change is a pure static JSX text-node edit so a compiled build is a reliable proxy.
