# T033 Worklog

## Changes made

- `src/pages/api/books/search.js`: Now accepts `title` (required) + `author` (optional) params, using Open Library's dedicated `title=` and `author=` search fields for better matching. Cache key includes both params. Falls back to `q=query` for backwards compat. Missing `title` AND `query` → 400.
- `src/components/BookSearch.js`: Removed standalone `<input>`, internal `query` state, `initialQuery` prop, and `handleKeyDown`. Now accepts `title` and `author` props (controlled from parent form). Search button disabled when `title` is empty/whitespace.
- `src/pages/reviews/books/add.js`: Passes `title={formData.title}` and `author={formData.author}` to `BookSearch` instead of `initialQuery`.
- `CLAUDE.md`: Updated API table for `/api/books/search` to document `title`/`author` params.

## Verification

- `npm run lint`: 0 errors (1 pre-existing warning in dev/cache.js)
- `npm test`: 108/108 passed
- `npm run build`: succeeded, all 28 pages generated
