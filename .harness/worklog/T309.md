# T309: migrate tv edit page to shared review-edit-card header strip

Applied the shared header-strip pattern (from perfumes-edit-redesign-2) to
`src/pages/reviews/tv/edit.js`'s `renderItem` callback:

- Wrapper div className: `review-card-with-edit` → `review-edit-card`.
- Edit `<Link>` moved to be the first child, wrapped in a new
  `<div className="review-edit-header">`, className changed from
  `edit-button-overlay edit-button-overlay--tv-banner` → `review-edit-button`.
- `<ReviewCard styleVariant="poster-banner" .../>` is now the second child.

In `src/styles/globals.css`, removed the now-dead T264 rules
`.edit-button-overlay--tv-banner` / `:hover` plus their explanatory comment
(between the movies-banner fix block and the albums/square-cover fix block).
Left the base `.review-edit-card`/`.review-edit-header`/`.review-edit-button`
rules and the books/albums/perfumes fix blocks untouched.

## Verify

- `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/tv/edit.js` → no matches.
- `npm run lint` → no errors (pre-existing warnings only, none new).
- `npm test` → 67 suites / 395 tests passed.
- `npm run build` → succeeded, `/reviews/tv/edit` compiled.
