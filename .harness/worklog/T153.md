# T153 — 2-col square-cover ReviewCard variant + albums list rebuild

## Do
- Added `styleVariant="square-cover"` to `ReviewCard.js`: square `CoverTile` on the left, title +
  read-only star rating, artist beneath, and a "HIGHLIGHTS · ..." mono line.
- `highlights` is stored as free text (textarea, `src/pages/api/reviews/albums/add.js`), not a
  delimited track list, so `splitHighlights()` splits on newline/comma/period/semicolon and takes
  up to 4 fragments, joined with " · ".
- Rebuilt `src/pages/reviews/albums/index.js` on the T150/T152 header/meta/search/sort pattern
  (`collection-review-header`, `SearchInput`, `PillGroup`) with a 2-col `square-cover-grid` instead
  of the 3-col poster grid. No pagination (deferred to T155).
- Added `.square-cover-*` + `.square-cover-grid` rules to `globals.css`, following the existing
  poster-banner/spine-cover CSS block conventions (design tokens, `--accent-albums` for the
  HIGHLIGHTS label).

## Verify
- `npm run lint`: 0 errors, 5 pre-existing warnings (unchanged).
- `npm test`: 34 suites / 235 tests passed.
- `npm run build`: succeeded, `/reviews/albums` built as static.
- `npm run dev` + `curl localhost:3000/reviews/albums`: 200, renders the loading state (data fetch
  is client-side via `useEffect`, matching every other review page) with no server errors in the
  dev log.
