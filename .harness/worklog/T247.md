# T247

## Do

Changed the wrapping container in `src/pages/reviews/albums/edit.js` from
`<div className="reviews-wrapper">` to `<div className="square-cover-grid">`
so the edit page uses the same 2-column CSS Grid layout as `/reviews/albums`
(`.square-cover-grid` in `globals.css`), instead of the full-width
`.reviews-wrapper` layout. No JS/masonry logic needed — pure class swap.
Everything inside the wrapper (the `review-card-with-edit` items, `ReviewCard`
with `styleVariant="square-cover"`, and the `edit-button-overlay` Edit link)
is unchanged.

No search/sort/pagination controls were added, per spec.

## Verify

- `npm run lint` — clean (only pre-existing warnings, no errors).
- `npm test` — 308 passed, 308 total.
- `npm run build` — succeeded; `/reviews/albums/edit` built as a static page
  (770 B route size).
- Manual browser verification not performed (headless session) — reviewed
  `square-cover-grid` CSS (globals.css, real `display: grid;
  grid-template-columns: repeat(2, 1fr); gap: 1.5rem;`) and confirmed it is
  identical to what `/reviews/albums/index.js` uses, and that the edit
  button overlay markup/link target is untouched.
