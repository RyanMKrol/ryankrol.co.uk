# T264: fix tv/poster-banner Edit button contrast against banner gradients

## Do
- `src/pages/reviews/tv/edit.js`: Edit `<Link>` className changed from
  `"edit-button-overlay"` to `"edit-button-overlay edit-button-overlay--tv-banner"`.
- `src/styles/globals.css`: added `.edit-button-overlay--tv-banner` (+`:hover`) rule
  block after the existing `.edit-button-overlay--movies-banner` block, mirroring the
  same opaque dark background / white text / light border treatment. No existing
  rules removed or reordered.

## Verify
- `npm run lint`: no errors (only pre-existing warnings, e.g. `no-img-element`,
  `react-hooks/exhaustive-deps` in unrelated files).
- `npm test`: full suite passed.
- `npm run build`: succeeded; `/reviews/tv/edit` built (108 kB) alongside all other
  review edit pages with no changes to their sizes.
- Did not run `npm run dev` interactively (headless session) — verified via code
  read that the new class only affects `/reviews/tv/edit`'s Edit button styling
  (background/color/border), leaves position/layout rules in `.edit-button-overlay`
  untouched, and does not touch movies/books/albums/perfumes edit pages or
  `ReviewCard.js`.

## Result
done
