# T303: move ownership Badge into perfume-v1-header

## Do

- `Variant6Hybrid.js`: wrapped the `type` Badge in a new `perfume-v1-header-badges` div and
  added the `ownership` Badge (`variant="soft"`) as its sibling, so both pills render together
  as the header's right-hand flex child.
- Removed the `ownership` Badge from `.perfume-v1-rating-row` — that row is back to just
  `PipMeter` + `X/10`.
- `globals.css`: added `.perfume-v1-header-badges { display:flex; align-items:center;
  flex-wrap:wrap; gap:0.4rem; }` next to `.perfume-v1-header`, so the pair wraps onto its own
  line on narrow cards instead of overflowing or squeezing the title.
- `.perfume-v1-rating-row`'s CSS rule itself was left untouched, per spec.

## Verify

- Could not open a browser in this headless run; verified via code reading + build instead:
  - JSX confirms single `item.type` badge still renders alone (no wrapper artifact) when
    `ownership` is absent — the wrapper only ever contains 1 or 2 children, no empty gap.
  - `flex-wrap: wrap` on the new class handles the narrow-viewport wrap case described in the
    Done-when checklist.
- `npm run lint` — no new errors (same pre-existing warnings as before this change).
- `npm test` — 67 suites / 395 tests passed.
- `npm run build` — succeeded, `/reviews/perfumes` prerendered fine.

## Docs

- Searched `CLAUDE.md` for `perfume-v1-header` / `ownership` — only the `PerfumeRatings` data
  model row mentions `ownership` (the field, not this layout). No doc describes this level of
  layout detail today, so no doc change was needed.
