# T220 — worklog

## Do
Replaced the local numeric `formatDate` helper in `src/pages/workouts/index.js` with a
delegate to the shared `formatEnglishDate` from `src/lib/dateFormat.js`, imported alongside
the other lib imports. `formatDate(dateString)` now simply returns
`formatEnglishDate(dateString)`, relying on `formatEnglishDate`'s own `new Date(...)`
coercion (workout `start_time` is already an ISO-8601 datetime string). `formatTime` and
`getDuration` were untouched.

## Verify
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 43 suites / 287 tests passed, including `test/pages/workouts/index.test.js`
  and `src/tests/workoutsIndex.test.js` (neither asserted on the old numeric format string,
  so no test changes were needed).
- `npm run build` — succeeded, `/workouts` prerendered as static content.
- Manual/code verify (no dev server run, per harness rule against unnecessary live checks —
  build output confirms the page compiles and lint/tests confirm behaviour): with
  `formatEnglishDate`'s options (`day:'numeric', month:'long', year:'numeric'`), a
  `start_time` such as `2026-05-17T18:00:00Z` now renders as `17 May 2026` instead of the
  previous `17-05-2026`.

## Result
done T220
