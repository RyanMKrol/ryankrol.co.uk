# T029 — Worklog

## Changes

- `src/pages/api/reviews/albums/add.js`: accept `lastfm` from POST body; populate `thumbnail` from `lastfm.images` (last/largest); store `lastfm` nested object when present; backward-compatible (empty body still saves).
- `src/pages/api/vinyl/add.js`: same `lastfm` enrichment pattern; fixed cache-clear bug (`vinyl-collection` → `api-vinyl-collection`).
- `CLAUDE.md`: updated AlbumRatingsV2 and VinylCollection data model rows with new fields; removed stale vinyl cache-clear Gotcha bullet.
- `README.md`: added AlbumRatingsV2 and VinylCollection schema blocks to DynamoDB section.

## Suite results

- `npm run lint`: 0 errors, 1 pre-existing warning (react-hooks/exhaustive-deps in dev/cache.js)
- `npm test`: 11 suites, 108 tests, all passed
- `npm run build`: success
