# T270 — Provision the HotTakes table

## Do

Ran `npm run hot-takes:create-table` using AWS credentials from `.env.local`.

## Observed

```
🚀 Creating DynamoDB table for hot takes...
✅ HotTakes table created successfully: "HotTakes"
⏳ Waiting for table HotTakes to be active...
✅ Table HotTakes is now active
🎉 Table created successfully!
📋 HotTakes table: Primary key = id (synthetic randomUUID key)
```

`HotTakes` table is confirmed ACTIVE in DynamoDB (`us-east-2`), partition key `id` (String).

## Checks

- `npm run lint` — no errors (pre-existing warnings only)
- `npm test` — 51 suites / 332 tests passed
- `npm run build` — succeeded

No source files touched — this task is a one-time infra provisioning step (no code diff).
