# T281 — replace considerTravelSize/considerFullBottle with ownership enum (backend)

## What changed
- `src/lib/perfumes.js`: added `VALID_OWNERSHIP` + `validateOwnership`, exported.
- `src/lib/perfumes.test.js`: added `describe('validateOwnership', ...)` accept/reject cases.
- `src/pages/api/reviews/perfumes/add.js` + `update.js`: destructure `ownership` instead of the two
  booleans, import `validateOwnership`, validate it (optional, 400 on invalid value), persist via
  conditional spread `...(ownership !== undefined && { ownership })`.
- `CLAUDE.md` PerfumeRatings row + `README.md` PerfumeRatings Table schema updated to describe
  `ownership?` instead of the two booleans.

## Out of scope (left for other tasks)
- No changes to `src/pages/reviews/perfumes/**` (form UI).
- No migration script for existing rows.

## Verification observed
- `npm run lint`: no errors (pre-existing warnings only).
- `npm test`: 65 suites / 372 tests passed, including new validateOwnership cases.
- `npm run build`: succeeded.
