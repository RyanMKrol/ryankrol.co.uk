# T374 — Review the 5 book-card designs and pick the one to ship

2026-07-14

## What I did

Reviewed the 5 candidate spine-cover book-card designs from the T373 workshop preview
(`src/pages/dev/book-card-designs.js`, `SpineCoverCard` in `src/components/ReviewCard.js`)
against the two example books (a long-body/read-more case and a Markdown ordered-list case),
using the component/CSS source directly (`ReviewCard.js:149-175` for the JSX, `globals.css:438-476`
for `.spine-v4-*`) alongside the workshop page's rendering.

**Decision: Variant 4 — "Mono Label"** — all-mono `label:value` metadata pairs (`rating`/`details`)
separated from the review snippet by a thin rule.

Confirmed the two DoD checks:
- **Title↔meta spacing:** `.spine-v4-body` sets `gap: 0.15rem`, tighter than the base
  `.spine-cover-body` gap (`0.2rem`, itself already tightened in T373) — reads correctly tight,
  no awkward gap between title and the mono meta line.
- **Markdown ordered-list indent:** the list-indent fix (`.spine-cover-snippet ol/ul` in
  `globals.css`) is a generic rule scoped to the snippet container, not variant-specific, so it
  applies identically under Variant 4 — the Garro example's ordered list renders with the correct
  left indent.

Recorded the winning variant (4, "Mono Label") directly in `.harness/tasks/T375.md`'s spec (a new
"Decision (from T374)" section citing file/line for the JSX + CSS to keep) so the T375 hardcode
unit has an unambiguous, self-contained instruction rather than depending on this worklog.

## Verification

Read the actual rendered JSX/CSS for Variant 4 (not just the workshop label/description) and
cross-checked both DoD-named concerns (spacing, list indent) against the source directly.

## Result

Done — T375 now names Variant 4 explicitly as the design to hardcode.
