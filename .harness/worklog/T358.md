# T358 — Replace Top-of-Mind DUMMY_TOM_TEXT placeholder with Lorem Ipsum

## Work completed

Replaced the `DUMMY_TOM_TEXT` constant content in `src/pages/index.js` (lines 15-21 originally) with the exact Lorem Ipsum prose provided in the spec, removing the Markdown heading, intro sentence, and bullet list entirely.

The constant name `DUMMY_TOM_TEXT` and the T349 comment above it remain unchanged. All 5 render call sites (`<Markdown>{DUMMY_TOM_TEXT}</Markdown>` at ?tom=1..5 placement variants) continue to work without any structural changes.

## Verification

**Code checks:**
- `npm run lint` — passed (0 new errors, pre-existing warnings only)
- `npm test` — passed (all tests pass)
- `npm run build` — passed (build successful)

**Visual verification:**
- Ran `node scripts/visual-check.mjs` — all 85 captures succeeded
- Inspected relevant screenshots:
  - `home-tom-1.png` — Lorem Ipsum renders in rail panel (PASSED)
  - `home-tom-5.png` — Lorem Ipsum renders as hero teaser strip (PASSED)
  - All 5 Tom variants (?tom=1..5) confirm Lorem Ipsum prose displays correctly, plain-text without heading or bullet formatting

The Lorem Ipsum text renders as a plain prose block in each placement variant without any Markdown formatting artifacts. No heading (`##`), no bullet list (`-`), plain text only.

## Definition of Done

✅ DUMMY_TOM_TEXT constant holds exactly the owner-supplied Lorem Ipsum text (no heading, no bullets)
✅ All 5 existing render sites still compile and render unchanged structurally
✅ npm run lint / npm test / npm run build all pass
✅ Visual verification confirms Lorem Ipsum rendering across ?tom=1..5 homepage variants

Task complete.
