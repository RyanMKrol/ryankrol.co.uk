# T200 — Add SVG favicon matching the nav wordmark's red-circle "r" mark

## Note on the prior `failed:blocked` attempt
The autonomous loop previously attempted this task and correctly stopped at `failed:blocked`: this
is a genuine scope-coverage gap (per `.harness/CLAUDE.md`'s documented pattern) — the task's `##
Done when` requires a `CLAUDE.md` file-map update, but the task's `scope` array
(`["public/**", "src/pages/_document.js"]`) never included `CLAUDE.md`, so the loop's structural
scope-gate correctly refused that edit rather than faking success. Completed interactively at the
owner's request instead, which isn't bound by that scope gate. `TASKS.json`'s `scope` field for this
task was deliberately left as-authored (unedited) — the sanctioned interactive-completion exception
is limited to flipping `status`, not touching other task-object fields.

## Do
Created `public/favicon.svg` (new `public/` directory) exactly per spec: a `#FF5C39` circle with a
white, bold lowercase "r" centered in a 32×32 viewBox — matching `.wordmark-avatar`
(`src/styles/globals.css:828-838`, rendered in `src/components/Header.js:35`). Wired it into
`src/pages/_document.js`'s `<Head>` as the first child, above the font `preconnect`/`<link>` tags:
`<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`. No `.ico`/`.png` fallback was added
(explicitly out of scope per spec — SVG favicons are supported by all current evergreen browsers).
Updated `CLAUDE.md`'s file map with a new `public/favicon.svg` row and a note on the `_document.js`
row that it now references the favicon. `README.md` was left untouched — it doesn't describe
individual page assets at that granularity, so a favicon mention would be an unnatural addition.

## Verify
- `npm run lint` — 0 errors, only pre-existing warnings (img/exhaustive-deps), no new ones.
- `npm test` — 279/279 tests passed.
- `npm run build` — succeeded.
- Manual verification performed (real browser, via Playwright): started `npm run dev`, confirmed
  `GET /favicon.svg` returns 200 with the exact SVG content, confirmed the rendered home page HTML
  contains exactly one `<link rel="icon" type="image/svg+xml" href="/favicon.svg"/>` tag, and
  rendered the SVG standalone to visually confirm the white "r" glyph reads clearly centered on the
  coral circle (compared side-by-side against the live `.wordmark-avatar` header mark — same color,
  weight, and centering, no nudge to `x`/`y`/`font-size` was needed).

Built interactively at the owner's request (not routed through `.harness/loop.sh`) — per
`.harness/CLAUDE.md`'s convention for owner-directed interactive completion of a task, marking
`status: "done"` directly in this same change rather than force-running the loop against it.
