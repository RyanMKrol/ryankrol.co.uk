# T363 — 2026-07-10

## What
`src/pages/reviews/books/backfill.js`'s `handleSearch` still sent a stale
`provider: 'googlebooks'` query param to `/api/books/search` — a leftover from before
hardcover-migration-a removed provider branching from that route entirely. Removed it; the
URLSearchParams now only carries `title`/`author`.

Verified the 429 contract is still intact by reading `src/pages/api/books/search.js`: on a
Hardcover 429 the route's catch block sets `Retry-After: 60` and returns HTTP 429 — the same
shape `backfill.js`'s existing `if (res.status === 429)` block already reads (`res.headers.get('Retry-After')`
→ `err.retryAfterSeconds`). No change needed there.

For pacing: threaded a `minSpacingMs` prop through `src/components/BulkBackfillList.js` — new
prop, defaults to `1500` (the existing `runBackfillQueue` default) so any other caller omitting
it is unaffected — down into its `runBackfillQueue(...)` options object. `backfill.js` now passes
`minSpacingMs={1200}` explicitly, with a one-line comment noting Hardcover's 60 req/min limit is
the reason for the explicit value (1200ms gives headroom over the 1000ms bare-minimum for 60/min).

## Checks run
- `npm run lint` — clean (0 errors, 6 pre-existing warnings unrelated to these files).
- `npm test` — 79 suites / 441 tests passed, including the existing `BulkBackfillList.test.js`
  (still green with the new optional prop).
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=backfill node scripts/visual-check.mjs` — both matching captures
  (`reviews-movies-backfill`, `reviews-movies-backfill-apply-all`) passed and were visually
  inspected: the shared `BulkBackfillList` component (used by both movies and books backfill
  pages) still renders its candidate rows and the "Apply all selections" flow still saves
  correctly (both rows show "saved"). Note: `scripts/_visual-harness.mjs` has no dedicated
  `reviews/books/backfill` `PAGES`/`FLOWS` entry at all — a pre-existing gap (only
  movies/tv backfill are covered), not introduced by this task and out of this task's scope
  (`src/pages/reviews/books/backfill.js`, `src/components/BulkBackfillList.js` only) to add.
  Since the component change is shared and purely behavioral (request param removal + pacing
  config, no markup/style change), the movies-backfill screenshots are sufficient visual
  confirmation that nothing broke.

## Remaining
None — task fully done within scope.
