# T259: Bulk metadata-backfill list page for TV

## Do

- Added `src/pages/reviews/tv/backfill.js`, mirroring the existing `src/pages/reviews/movies/backfill.js`
  exactly: same page-level password field, `needsTvBackfill` filter from `src/lib/backfillEligibility.js`,
  `BulkBackfillList` with `pageSize={15}`, `onSearch` hitting `/api/tmdb/search?type=tv` (with
  `retryAfterSeconds` attached on 429), the poster/title/year/overview `renderCandidate` JSX copied
  from `src/pages/reviews/tv/edit/[id].js`, and `onConfirm` POSTing the full field set (title, rating,
  gist, originalId, password, tmdbId, mediaType, posterPath, tmdbOverview, tmdbDate) to
  `/api/reviews/tv/update`.
- Added a "Backfill metadata" link from `src/pages/reviews/tv/edit.js` to `/reviews/tv/backfill`,
  matching the movies edit page's link + styling.

## Verify

- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test`: full suite passes.
- `npm run build`: succeeds; build output lists `○ /reviews/tv/backfill` as a static page.
- Not run live (per harness rules, no live TMDB/DynamoDB calls) — verified by code inspection that
  it mirrors the already-shipped movies backfill page 1:1 aside from the tv-specific
  endpoint/field swaps called for in the spec.
