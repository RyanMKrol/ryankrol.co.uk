# T288: archived repo corner tag

## Do
- `src/styles/globals.css`: added `position: relative;` to `.project-card`, and a new
  `.project-card-archived-tag` rule (absolute top-left, solid `--color-ink-mute` background,
  `--color-bg` text, `--font-mono`, rounded corner matching `--radius-card`).
- `src/pages/projects/index.js`: added `{repo.archived && (<div className="project-card-archived-tag">archived</div>)}`
  as the first child of the `.project-card` div, and removed the old soft-text `Badge` from
  `.project-card-meta-row` so the archived signal only appears once per card.

## Verify
- `npm run lint` — no errors (pre-existing `no-img-element` / `exhaustive-deps` warnings only).
- `npm test` — all suites pass.
- `npm run build` — succeeds, `/projects` prerenders statically.
- Did not run `npm run dev` interactively (headless run); verified by reading the rendered JSX/CSS —
  corner tag is scoped to `.project-card` via `position: relative`, uses only existing design
  tokens, and non-archived cards render no extra markup/CSS since the tag div is conditionally
  rendered and reuses the existing `.project-card` layout (flex column, no reflow from an
  absolutely-positioned sibling).
