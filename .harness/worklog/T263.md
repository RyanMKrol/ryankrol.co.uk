# T263: movies/edit spine-cover/poster-banner Edit button contrast fix

## Do
- `src/pages/reviews/movies/edit.js`: Edit `<Link>` className changed from
  `"edit-button-overlay"` to `"edit-button-overlay edit-button-overlay--movies-banner"`.
- `src/styles/globals.css`: added `.edit-button-overlay--movies-banner` +
  `:hover` rules immediately after the existing spine-cover fix block (books task's
  block ended at the `.review-card-with-edit--spine-cover .edit-button-overlay` rule,
  right before `/* Workout Styling Variants */`). Gives an opaque dark background +
  white text/border so the button stays legible against any poster-banner gradient.

## Verified
- `npm run lint` — no new errors (pre-existing warnings only).
- `npm test` — full suite passes.
- `npm run build` — succeeds; `/reviews/movies/edit` in the build output.
- Did not touch tv/books/albums/perfumes edit pages or ReviewCard.js, per scope.
- Did not run the dev server / browser click-through (headless run) — relied on
  code review + build success; the new CSS block mirrors the existing
  `.edit-button-overlay--*` pattern used by the sibling books fix.
