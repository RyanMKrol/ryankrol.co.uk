# T084 — Fix invisible rating stars on the Ocean theme (dark mode)

## Change

`src/styles/globals.css:841` — Ocean dark-mode `--color-star-empty` changed from `#00223a`
(near-identical to that block's `--color-bg: #001828` / `--color-surface: #002238`) to
`#2a5878`, a muted mid-tone blue clearly lighter/more saturated than the background but still
visually subordinate to `--color-star-filled: #00c8e8`. Matches the pattern used by the other 9
theme blocks (e.g. cyberpunk `#2a2a4a`, line 45; forest `#1a3a1a`, line 166).

Ocean light-mode block (`globals.css:907` onward) left untouched. No other theme's star colors
were modified.

## Verification

- `npm run lint` — 0 errors, 4 pre-existing warnings (unrelated files).
- `npm test` — 151/151 passing.
- `npm run build` — succeeds.
- Manual browser check of the Ocean dark-mode `/reviews/movies/add` page was NOT performed (no
  running dev server / browser available in this headless session) — verified by contrast
  reasoning instead: old value `#00223a` vs `--color-bg:#001828` is a ~10% luminance delta,
  effectively invisible; new value `#2a5878` is several times lighter/more saturated, matching
  the contrast ratio other theme blocks use for the same variable.
