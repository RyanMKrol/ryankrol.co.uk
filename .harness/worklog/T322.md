
---
failed:blocked T322 — agent reported failed:blocked — needs src/components/Badge.js (out of scope) to add accessible aria-label passthrough on PrBadge

---
2026-07-08 — Resolved manually, outside the loop. Status stays "blocked" as an accurate historical
record of the scope-authoring bug (the fix wasn't built via loop.sh, so no outcomes.jsonl row is
warranted); the underlying problem is fixed on main.

Root cause confirmed: T322's original scope only listed the two workout pages, but its spec required
extending src/components/Badge.js's PrBadge to add an accessible-name prop — a real out-of-scope need,
not a false block.

Fix: added an `ariaLabel` prop to `Badge`/`PrBadge` (src/components/Badge.js), forwarded to the
underlying `<span aria-label>`. Updated the label-building arrays in src/pages/workouts/index.js and
src/pages/workouts/[id].js so each PR pill drops the literal "PR" suffix and reads "Weight"/"1RM"/
"Volume" (capitalized first letter — supersedes this task's original lowercase spec per a later,
owner-confirmed decision made while converting the medal-restyle idea), with `ariaLabel` set to
"<axis> personal best" for screen readers. Updated test/pages/workouts/index.test.js's two assertions
on `getExercisePreview`'s output shape to match.

Verified: `npm run lint` (0 errors, pre-existing warnings only), `npm test` (403/403 passing, including
the updated assertions), `npm run build` (succeeds), and `VISUAL_CHECK_ONLY=workout node
scripts/visual-check.mjs` (8/8 captures — visually confirmed on workouts.png and workout-detail.png
that pills render "Volume"/"Weight"/"1RM" with no "PR" text, no layout regression).

Known overlap, deliberately not reconciled per owner instruction: T333 (hardcode the medal-restyle
chooser's winning variant, still pending on T332) also plans to touch these same label strings and
PrBadge's visual styling. When T333 eventually builds, its spec's references to the old 'weight PR'
etc. strings will already be stale — whoever builds/reviews T333 should re-verify its spec against the
current code rather than assuming the old label text is still there.
