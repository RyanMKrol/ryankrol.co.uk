# T126: Books list — sort by editedDate when present, else date

## Do

Updated both date-sort branches in `src/pages/reviews/books/index.js` (`date-desc` and the
default 'most recent first' branch) to compute the comparison date from
`(item.editedDate || item.date)` instead of `item.date` alone, matching the already-landed
`editedDate` field written by `src/pages/api/reviews/books/update.js` on edit.

No other sort branches, `SORT_FIELDS`, or other files touched.

## Verify

- `npm run lint` — 0 errors, 4 pre-existing warnings (unrelated files), no new warnings.
- `npm test` — 235/235 passing, 34 suites.
- `npm run build` — succeeded, all routes built including `/reviews/books`.
- Manual code read: never-edited records (no `editedDate`) fall back to `a.date`/`b.date` via
  `||`, so sort order for those is unchanged. Edited records now sort using `editedDate`.
