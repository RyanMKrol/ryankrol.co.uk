# T209: 'all' filter button on /programmes

## Do
- Added `const FILTER_OPTIONS = ['all', ...PROGRAMMES];` in `src/pages/programmes/index.js`,
  used it in place of `PROGRAMMES` for the filter-button `.map`. `PROGRAMMES` itself is
  unchanged.
- No other changes needed: label logic, onClick, active-class check, `selected` default
  ('push'), and the `aggregateProgramme(filteredWorkouts, selected)` call site already handle
  'all' correctly (aggregateProgramme's 'all' support landed in T208).

## Verified
- `npm run lint` — 0 errors, pre-existing warnings only (img/exhaustive-deps, unrelated files).
- `npm test` — 271 tests passed, 40 suites.
- `npm run build` — succeeded; `/programmes` compiled fine.
- Did not run a live browser click-through (headless session) — code path was verified by
  reading `aggregateProgramme` (`src/lib/programmeStats.js`) and its existing tests, which cover
  the 'all' mode combining totals across push/pull/legs (workouts and totalVolume summed,
  avgVolumePerWorkout recomputed). No new pure logic was added here, so no new test was written.
