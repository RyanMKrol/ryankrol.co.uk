# T117 — migrateVinylToIdTable

Wrote `src/scripts/migrateVinylToIdTable.js`, mirroring `migrateAlbumsToIdTable.js`'s structure:
scans `VinylCollection` (title+artist composite key), creates `VinylCollectionV2` (`id` String
HASH-only), copies every field unchanged (including nested `lastfm`) plus a fresh `randomUUID()`
id, batch-writes in chunks of 25, guards against double-migration unless `--force`, and supports
`--verify` (re-scans both tables, matches old→new on title+artist, deep-compares every field via
exported `diffMigratedItem`, ignoring `id`). Default run performs migration then auto-verifies.

Added `src/scripts/migrateVinylToIdTable.test.js` covering `buildMigratedItem` (field
preservation incl. nested `lastfm`, `id` presence, v4 UUID shape, uniqueness) and
`diffMigratedItem` (clean match, changed field, changed nested object, missing item) and
`findMatchingNewItem`.

Diff touches only the two new script files (plus this worklog).

## Checks
- `npm run lint` — 0 errors (3 pre-existing warnings, none new)
- `npm test` — 234 passed, 32 suites (includes 11 new tests for this script)
- `npm run build` — succeeded

## Production migration run

Ran `node src/scripts/migrateVinylToIdTable.js` against real production tables using AWS
credentials from `.env.local`. `VinylCollectionV2` did not exist — created with `id` (String)
as sole HASH key. Result:

```
📊 Old count: 190, New count: 190
VERIFY: PASS
```

`VinylCollection` (source) was only scanned, never written to or deleted — left fully intact.
