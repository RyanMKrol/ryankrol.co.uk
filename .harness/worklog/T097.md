# T097 — Remove the "Application notes" field from the perfume forms — UI

## What changed

- `src/pages/reviews/perfumes/add.js`: removed `notes` from initial `formData` state and
  removed the "Application notes" label + textarea. `notes` is no longer sent in the POST
  body (it was only ever included via the `formData` spread).
- `src/pages/reviews/perfumes/edit/[id].js`: removed `notes` from initial `formData` state,
  removed `notes: perfume.notes || ''` from the fetched-perfume mapping, and removed the
  "Application notes" label + textarea. `notes` is no longer sent in the update POST body.
- Left the "Description" field and the "Application spots" checkboxes/spray-count UI
  untouched on both forms, as required.

## Verification

- `npm run lint` — 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test` — 22 suites / 167 tests passed.
- `npm run build` — succeeded.
- Manually started `npm run dev` and curled `/reviews/perfumes/add` and
  `/reviews/perfumes/edit/foo` — "Application notes" string no longer appears in either
  page's rendered output.
