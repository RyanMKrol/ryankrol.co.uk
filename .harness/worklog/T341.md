# T341 — 2026-07-09

## What I did
- Wrapped the perfume card's type Badge (EDT/EDP/Parfum) in `<Tooltip label="What it is">` in
  `src/components/perfumeVariants/Variant6Hybrid.js`, mirroring the ownership pill's existing
  `Tooltip` pattern one line below. Left the type Badge's variant unchanged (no `variant` prop,
  so it stays solid/filled) — only the ownership pill uses `variant="outline"`.
- Added a new `FLOWS` entry `perfumes-type-tooltip` in `scripts/_visual-harness.mjs`: navigates to
  `/reviews/perfumes` and hovers `.perfume-v6-card:first-child .tooltip-wrapper` (the first
  matching tooltip wrapper on the page, which is the type pill's — it precedes the ownership
  pill's wrapper in DOM order) before the screenshot.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this change).
- `npm test` — 402/402 passed.
- `node scripts/visual-check.mjs` with `VISUAL_CHECK_ONLY=perfume` — all 13 captures succeeded,
  including the new `perfumes-type-tooltip` flow.

## Visual verification (observed)
Read `scripts/visual-out/perfumes-type-tooltip.png` directly. The "What it is" tooltip bubble is
visibly showing above the "EDP" type pill on the first perfume card (Bleu de Chanel), positioned
above and centered over the pill, matching the existing "What I own" tooltip's visual pattern.
The type pill itself remains solid/filled (unchanged), and the "FULL BOTTLE" ownership pill next
to it is unaffected (still outline style, no tooltip shown since it wasn't hovered).

## Remaining
None — task complete.
