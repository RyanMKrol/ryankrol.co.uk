# T307

Migrated `src/pages/reviews/books/edit.js` off the T262 spine-cover-specific fix onto the
shared `review-edit-card` / `review-edit-header` / `review-edit-button` header-strip pattern
(already defined in globals.css from the perfumes migration).

## Changes

- `src/pages/reviews/books/edit.js`: wrapper div className `review-card-with-edit
  review-card-with-edit--spine-cover` → `review-edit-card`. Edit `<Link>` moved to be the
  first child, wrapped in a new `review-edit-header` div, className `edit-button-overlay` →
  `review-edit-button`. `<ReviewCard>` is now the second child.
- `src/styles/globals.css`: deleted the dead `.review-card-with-edit--spine-cover` and
  `.review-card-with-edit--spine-cover .edit-button-overlay` rules plus their explanatory
  comment (T262's per-variant fix, now superseded).

## Verification

- `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/books/edit.js` →
  no output (fully migrated).
- `npm run lint` → 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` → 67 suites / 395 tests passed.
- `npm run build` → succeeded, `/reviews/books/edit` built as static (5.88 kB).
- Did not run `npm run dev` + browser check (headless session) — verified structurally by
  reading the resulting JSX against the shared CSS rules used by the already-shipped
  perfumes-edit-redesign-2 migration (same classes, same nesting order).
