
---
failed:blocked T237 — agent reported failed:blocked — backfillMissingExerciseRows.js (from T236) is CommonJS require()-ing an ESM-only src/lib/workoutBackfill.js and crashes with ERR_MODULE_NOT_FOUND before any dry-run/live scan can happen — needs a script-scoped fix (convert to import syntax like its sibling scripts) before this task can be retried
