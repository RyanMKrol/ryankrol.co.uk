# T212: remove white-rectangle background from .container / .review-container

## Change

In `src/styles/globals.css`, removed `background-color: var(--color-surface)` and
`min-height: 100vh` from both `.container` (was lines ~639-648) and `.review-container`
(was lines ~650-659). All other declarations (`max-width`, `margin`, gutter padding, and
the shared `@media (max-width: 768px)` override) are untouched. `body`'s own
`background-color: var(--color-bg)` / `min-height: 100vh` (lines 578-584) were left as-is
and now show through everywhere `.container`/`.review-container` used to paint white.

## Verification

- `npm run lint` — 0 errors, 6 pre-existing warnings (img/exhaustive-deps), no new ones.
- `npm test` — 271/271 passing, no changes needed.
- `npm run build` — succeeded, all 32 pages generated.
- Ran `npm run dev` headlessly (no browser available in this environment) and confirmed via
  `curl localhost:3000` that the site serves (200 OK).
- Confirmed via `grep -n "background-color: var(--color-card)" src/styles/globals.css` that
  all card component classes (`.poster-banner-card`, `.spine-cover-card`,
  `.square-cover-card`, `.workout-session-card`, `.project-card`, `.now-playing-chip`, etc.)
  still declare their own `background-color: var(--color-card)` independently — 8 occurrences
  remain untouched, so cards keep their white background/contrast regardless of the
  container change.
- Could not visually screenshot `/`, `/workouts`, `/reviews/movies`, `/vinyl` in this headless
  session (no browser tool available) — verified instead by code inspection: `.container` /
  `.review-container` no longer set any background or min-height, so the cream `body`
  background (`--color-bg`) is the only page-level background painted; card components
  retain their own `--color-card` background so they remain visually distinct. A human/browser
  check of the four listed pages is recommended as a follow-up if visual confirmation is
  desired beyond this static analysis.
