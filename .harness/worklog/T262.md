# T262: fix books/edit spine-cover Edit button overlapping star rating

## Do
- `src/pages/reviews/books/edit.js`: wrapper `<div>` className changed from
  `"review-card-with-edit"` to `"review-card-with-edit review-card-with-edit--spine-cover"`.
- `src/styles/globals.css`: added `.review-card-with-edit--spine-cover` (flex column) and
  `.review-card-with-edit--spine-cover .edit-button-overlay` (position: static, self-aligned
  right, small top margin) immediately after the existing `.edit-button-overlay:hover` rule.
  No other lines in that block touched.

## Verify
- `npm run lint` — no new errors (pre-existing `no-img-element` / `exhaustive-deps` warnings only).
- `npm test` — 51 suites / 332 tests passed.
- `npm run build` — succeeded, all pages including `/reviews/books/edit` built.
- Did not run `npm run dev` + manual browser click-through (headless session) — reasoned about
  the CSS instead: the new rule only applies under the page-scoped
  `.review-card-with-edit--spine-cover` class added solely in `books/edit.js`, so
  movies/albums/tv/perfumes edit pages (which never carry that class) keep the original
  `.edit-button-overlay` absolute-positioning behavior unchanged.

## Result
done
