# T280: Remove the redundant mobile-only quicklinks section from the homepage

## What changed
- `src/pages/index.js`: deleted the `QUICK_LINKS` constant and the `<section className="home-quicklinks">` JSX block (was rendering pill links to `~/vinyl`, `~/listening`, `~/projects`, `~/hot-takes`, mobile-only). Left `.home-wall` (the actual "collection wall" image grid, already hidden on mobile) untouched.
- `src/styles/globals.css`: deleted the `.home-quicklinks` and `.home-quicklink` rule blocks (desktop-hidden defaults, ~line 3038), and the `.home-quicklinks { display: flex; }` override inside the mobile media query (~line 3318). Left the sibling `.home-wall { display: none; }` rule in that same media query untouched.

## Verify
- `grep -rn "QUICK_LINKS\|home-quicklink" src/` → no matches (confirmed no dangling references before/after deletion).
- `npm run lint` → no errors, only pre-existing warnings (img/exhaustive-deps).
- `npm test` → 65 suites / 363 tests passed.
- `npm run build` → succeeded, `/` prerendered as static content.
- Did not run a live dev-server visual check (headless session, no browser) — verified by code removal + successful build with no dangling references to the deleted constant/classes. The removed section was mobile-only and CSS-hidden on desktop already, so no desktop visual delta is expected.
