# T243: fix masonry reading order on /reviews/books

## Do

- `src/styles/globals.css`: replaced `.spine-cover-list` `column-count: 2` (+ `column-count: 1`
  mobile override) with `display: flex; gap: 1.5rem;`, added `.spine-cover-list-col`
  (`display: flex; flex-direction: column; gap: 1.5rem; flex: 1 1 0; min-width: 0;`). Removed
  `margin-bottom: 1.5rem` from `.spine-cover-card` (redundant with column `gap`), kept
  `break-inside: avoid`.
- `src/pages/reviews/books/index.js`: imported `MasonryColumns` + `useResponsiveColumnCount`,
  called `useResponsiveColumnCount(2, 700)` at top level (before the loading/error early returns,
  matching `src/pages/projects/index.js`'s pattern), replaced the raw `.spine-cover-list` div/map
  with `<MasonryColumns items={pagedBooks} columnCount={columnCount} className="spine-cover-list"
  columnClassName="spine-cover-list-col" renderItem={...} />`. `isLast`/`gradient` still index
  against the original `pagedBooks` array since `MasonryColumns` passes back the original index.

## Verified

- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test`: 308/308 passed.
- `npm run build`: succeeded, `/reviews/books` bundle size unchanged in kind (2.44 kB).
- Did not run `npm run dev` / manual browser check (headless session) — code path mirrors the
  already-shipped `src/pages/projects/index.js` conversion (T240/T241/T242 lineage), which was
  manually verified in its own task.
