# T311: perfumes edit page header-strip migration

## Do

- `src/pages/reviews/perfumes/edit.js`: `renderItem` wrapper div className changed from
  `review-card-with-edit review-card-with-edit--perfume` to `review-edit-card`. Edit `<Link>`
  moved to be the first child, wrapped in `<div className="review-edit-header">`, with its
  className changed from `edit-button-overlay` to `review-edit-button`. `<Variant6Hybrid>` is now
  the second child — matches the pattern already landed in albums/books/movies/tv edit pages.
- `src/styles/globals.css`: removed the dead T262-T266 perfume fix block
  (`.review-card-with-edit--perfume` + `.review-card-with-edit--perfume .edit-button-overlay`
  plus its explanatory comment), immediately before `/* Workout Styling Variants */`. Did not
  touch the shared `.review-edit-card`/`.review-edit-header`/`.review-edit-button` rules or any
  other type's fix block.
- Did not touch `Variant6Hybrid.js` (out of scope for this task; a separate concurrently-tracked
  task owns the ownership-badge-placement change there).

## Verified

- `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/perfumes/edit.js` →
  no matches.
- `npm run lint` — no errors (only pre-existing warnings).
- `npm test` — 67 suites / 395 tests passed.
- `npm run build` — succeeded, `/reviews/perfumes/edit` compiles.
- Did not start `npm run dev` / visually inspect (headless run) — relying on the markup being
  byte-for-byte the same pattern already visually verified for albums/books/movies/tv edit pages
  in prior tasks (T305-T310).
