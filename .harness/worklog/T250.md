# T250 — live rating label on PipMeter hover

## Do
- `PipMeter` now accepts an optional `onHoverChange` callback (default no-op), invoked with
  the pip value in `handlePipHover` and with `0` in `handleMouseLeave`. The `readOnly` branch
  is untouched (no hover handlers there, as specified).
- `src/pages/reviews/perfumes/add.js`: added `hoveredRating` state, wired
  `onHoverChange={setHoveredRating}` into `PipMeter`, label now reads
  `Rating (0-10): {hoveredRating || formData.rating}`.
- `src/pages/reviews/perfumes/edit/[id].js`: identical fix.
- Did not touch `LongevitySlider`/`ProjectionSlider` (native range inputs, out of scope).

## Verify
- `npm run lint` — 0 errors, 6 pre-existing warnings (no-img-element, exhaustive-deps),
  none new.
- `npm test` — full suite passed.
- `npm run build` — succeeded, `/reviews/perfumes/add` and `/reviews/perfumes/edit/[id]`
  both built as static pages with no errors.
- Manual `npm run dev` browser check of the hover behaviour was not performed in this
  headless run (no browser available); verified by code review that `hoveredPip` state
  driving pip fill and `onHoverChange` firing at the same call sites (`handlePipHover`/
  `handleMouseLeave`) will keep the label and the pip fill in sync.
