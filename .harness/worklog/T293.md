# T293 — per-exercise PR medal on /workouts card previews

## Do

- `getExercisePreview` (src/pages/workouts/index.js) changed from returning bare name strings
  to `{ name, hasPersonalBest }` objects, computing `hasPersonalBest` as
  `exercise.sets?.some(set => set.isWeightPR || set.is1RMPR || set.isVolumePR)`.
- Render call site updated: the exercise-name `<li>` now renders a compact `Badge`
  (`variant="soft"`, `mono={false}`) with a 🏅 glyph plus a visually-hidden " personal best"
  span (inline clip-based hidden style, since no `visually-hidden` utility class exists in
  `globals.css` and that file is out of this task's scope) immediately after any exercise
  whose `hasPersonalBest` is true.
- No new fetch — reused the already-fetched `workout.exercises[].sets[]` PR flags from
  `/api/workouts?mode=all`.
- Updated the existing test file `test/pages/workouts/index.test.js` (previously asserted the
  bare-string shape) to the new object shape, and added cases for PR-flag detection and a
  missing `sets` array.

## Verify (observed)

- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files), no new ones.
- `npm test` — 67 suites / 392 tests passed, including the 7 updated/added
  `getExercisePreview` cases.
- `npm run build` — succeeded; `/workouts` page compiled (6.82 kB / 188 kB First Load JS).
- Did not run `npm run dev` + manual browser check (no live/synthetic backfilled workout data
  available in this headless run to visually confirm marker placement) — verified the render
  logic by reading the JSX and by unit-testing `getExercisePreview`'s derived shape directly,
  which is what the render call site consumes.

## Scope

Only changed `src/pages/workouts/index.js` (in scope) plus `test/pages/workouts/index.test.js`
(test file, always allowed) and this worklog.
