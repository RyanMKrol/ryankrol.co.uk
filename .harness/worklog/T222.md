# T222: lowercase emoji-free titles on review edit pages

## Do
Removed the leading emoji + space and lowercased the `page-title` h1 text in each of the 10
scoped files (5 edit.js list pages + 5 edit/[id].js detail pages for movies/tv/books/albums/perfumes).

## Verify
- `grep -rn "page-title" ...` across all 10 files: confirmed no emoji, all lowercase, matching
  the spec's target strings exactly.
- `npm run lint`: 0 errors, only pre-existing warnings (img/exhaustive-deps), none new.
- `npm test`: 43 suites / 287 tests passed.
- `npm run build`: succeeded, all 10 review edit pages compiled statically.
- `git diff --stat`: exactly the 10 scoped files, one line changed each.
- Did not run `npm run dev` + visual browser check (headless environment) — relied on the
  build output + grep confirmation of exact text instead.

## Result
done
