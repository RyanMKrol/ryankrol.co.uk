# T310: albums edit page — shared header-strip pattern

## Do

- `src/pages/reviews/albums/edit.js`: card wrapper className changed from
  `review-card-with-edit review-card-with-edit--square-cover` to `review-edit-card`.
  Edit `<Link>` moved to be the first child, wrapped in `<div className="review-edit-header">`,
  className changed from `edit-button-overlay` to `review-edit-button`. `<ReviewCard>` is now
  the second child. Outer `.square-cover-grid` left untouched (out of scope per spec).
- `src/styles/globals.css`: removed the now-dead T265 rules `.review-card-with-edit--square-cover`
  and `.review-card-with-edit--square-cover .edit-button-overlay` plus their explanatory comment,
  between the tv-banner fix block and the perfume fix block. Base rules and other variant fix
  blocks (books/movies/tv/perfumes) untouched.

## Verify

- `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/albums/edit.js` → no output.
- `npm run lint` → 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` → 67 suites / 395 tests passed.
- `npm run build` → succeeded, `/reviews/albums/edit` prerendered as static content.
- Did not run `npm run dev` + manual browser check (headless session) — verified via code review:
  card markup now matches the perfumes/books/movies/tv header-strip pattern exactly, and
  `ReviewCard` markup/props for the card body are unchanged from the view page's usage, so card
  content below the strip matches `/reviews/albums`.

## Result

done
