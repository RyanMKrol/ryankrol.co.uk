# T189: remove wrongly-shipped perfume design-note elements

## Do

Removed two elements from `src/pages/reviews/perfumes/index.js` that T154 shipped as literal
UI copy but were actually design-handoff annotations, per owner clarification:

1. `<p className="perfume-scale-chip">…</p>` in the `collection-review-header` (the paraphrased
   "perfumes use a 0-10 scale…" pill).
2. `<div className="perfume-empty-card">+ your next review lands here</div>` at the end of the
   `perfume-card-grid`.

The legitimate `collection-review-meta` "rated out of 10 · a new & growing shelf" line, which
already satisfies the brief's "page header states the scale rule explicitly" requirement, was
left untouched.

Confirmed via `grep -rn 'perfume-scale-chip\|perfume-empty-card' src/` that neither class was
referenced anywhere else, then removed the now-dead `.perfume-empty-card` and
`.perfume-scale-chip` rule blocks from `src/styles/globals.css`.

## Done when — observed

- `grep -rn "perfume-scale-chip\|perfume-empty-card" src/` → no matches.
- `npm run lint` → 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` → 245/245 passing.
- `npm run build` → succeeds; `/reviews/perfumes` builds as a static route.
- `verify: ["run-app"]`: not run live (headless harness run, no browser available) — verified
  by reading the diff instead: the chip `<p>` and empty-card `<div>` are removed from the JSX,
  the `collection-review-meta` line and the `ReviewCard` `.map(...)` are unchanged, and the
  corresponding CSS rules are deleted with no remaining references.
