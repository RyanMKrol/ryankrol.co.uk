# T154 — Rebuild perfumes list page (pip meter, scale-explainer chip, empty slot)

## What changed
- `src/components/ReviewCard.js`: added a fourth `perfume-card` style variant — `CoverTile`
  (tall 3/4 aspect, bottle-ish) on the left; title, "Designer · [EDP/EDT Badge]" line (soft
  variant), big accent-colored numeric score next to a read-only 10-dot `PipMeter`, and an
  italic snippet from `getThoughts()`. Existing poster-banner/spine-cover/square-cover variants
  untouched — verified via `npm test`.
- `src/pages/reviews/perfumes/index.js`: rebuilt header (title + "rated out of 10 · a new &
  growing shelf" subtitle + grape explainer chip stating the 0-10/pip-meter rule), swapped
  `SortButtons` for `PillGroup`/`SearchInput` for consistency with the other rebuilt list pages,
  2-col `perfume-card-grid` bound to `/api/reviews/perfumes`, and an always-present dashed
  `perfume-empty-card` ("+ your next review lands here") as an extra grid item.
- `src/styles/globals.css`: added `.perfume-card*`, `.perfume-card-grid`, `.perfume-empty-card`,
  `.perfume-scale-chip` rules per the design tokens (grape accent `#7A5AF8`, dashed border empty
  state, 2px ink borders, existing radius/shadow tokens).

## Scope discipline
Did not add `longevity`/`projection`/`seasons`/`applicationSpots` to the card — these stay stored
but unused per the task's explicit instruction (deferred to a separate richer-card idea).
Only touched the 3 files in scope.

## Verification
- `npm run lint` — 0 errors, pre-existing warnings only (img/no-img-element, one
  exhaustive-deps).
- `npm test` — 34 suites / 235 tests pass, including pre-existing component tests.
- `npm run build` — succeeds; `/reviews/perfumes` prerenders as static.
- `run-app`: started `npm run dev`, `curl localhost:3000/reviews/perfumes` → 200, page compiles
  and serves. Did not exercise the client-side fetch against real DynamoDB data (would require a
  live API call against production data — out of scope for headless verification per harness
  rules); the render/CSS/markup were instead verified by reading the design mocks
  (`renders/3d.png`, `renders/4i.png`) against the implemented JSX/CSS. Detailed pixel fidelity is
  deferred to T166 per the task spec.
