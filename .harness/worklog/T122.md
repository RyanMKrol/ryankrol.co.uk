## Build (2026-07-03)

Built against the corrected spec (`.harness/tasks/T122.md`, rewritten during the 2026-07-03
recovery pass referenced below) and the corrected `scope` (now includes `CLAUDE.md` and
`README.md` alongside `src/pages/api/reviews/books/update.js`).

### What changed
- `src/pages/api/reviews/books/update.js`: added `editedDate` (today's date, `DD-MM-YYYY`,
  computed fresh via `new Date().toLocaleDateString('en-GB').replace(/\//g, '-')`) to the
  `reviewData` object, written unconditionally on every successful edit. The existing
  `originalDate`/`preservedDate` logic (lines 33-45) is unchanged — `date` is still preserved
  across edits exactly as before.
- `CLAUDE.md`'s Data model table and `README.md`'s `#### BookRatingsV4 Table` schema section
  both updated to document the new `editedDate` field.

### Verification
- `npm run lint` — 0 errors, 4 pre-existing warnings in unrelated files (tolerated per DoD).
- `npm test` — 34 suites / 235 tests passed, including the existing
  `src/pages/api/reviews/books/update.test.js` (not modified by this task; stayed green with the
  new field added).
- `npm run build` — succeeded.
- No live DynamoDB/API calls were made to verify — confirmed by reading the code per the
  harness's no-live-calls rule.

---

## Recovery note (2026-07-03)

Original attempt correctly stopped rather than fake success: the spec instructed updating
`CLAUDE.md`'s and `README.md`'s Data model tables with the new `editedDate` field, but neither file
was in this task's `scope` array (only `src/pages/api/reviews/books/update.js` was). The build never
touched any source file — no work to preserve. The agent's own report noted the code change itself
is trivial and was already verified working.

Root cause fixed: `CLAUDE.md` and `README.md` added to `T122`'s `scope` in `TASKS.json`. Separately,
while investigating this gap the whole spec turned out to be stale — it described the pre-migration
`BookRatingsV3` composite title+author key and a delete/recreate code path, neither of which exist in
the current `id`-keyed `BookRatingsV4` handler. The spec (`.harness/tasks/T122.md`) has been rewritten
against the actual current `update.js`. This task was built fresh against the corrected spec (see
"Build" above).
