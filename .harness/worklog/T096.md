# T096 — Remove "Application notes" field from perfume schema — backend

## What changed
- `src/pages/api/reviews/perfumes/add.js`: removed `notes` from the destructured request body
  and from the `reviewData` object written to DynamoDB. `description` untouched.
- `src/pages/api/reviews/perfumes/update.js`: same removal, both in destructuring and in the
  `reviewData` object. `description` untouched.

## Verification
- No existing tests referenced `notes` on these two routes (grep for "notes" under
  `src/pages/api/reviews/perfumes/` returned nothing), so no test updates were needed.
- `npm run lint` — 0 errors (3 pre-existing warnings, unrelated).
- `npm test` — 22 suites / 167 tests passed.
- `npm run build` — succeeded.

## Notes
- No migration/data deletion performed — existing DynamoDB items with a stored `notes` value are
  left as-is; the app simply stops reading/writing it going forward, per spec.
