# T170: Final docs sync for the Collection redesign

## What I found
- Theming convention was already clean — the only "AppearancePicker" hit is the historical note
  explaining it was retired (explicitly allowed by the spec), no other stale multi-theme terms
  (cyberpunk/terraria/cotton-candy/ocean/sunset) anywhere in CLAUDE.md or README.md.
- `src/lib/pagination.js` existed but wasn't listed in CLAUDE.md's file map.
- The Components list was badly out of date vs `src/components/` — missing `Footer`, `Pill`,
  `PillGroup`, `SearchInput`, `Badge`, `PipMeter`, `CoverTile`, `StatBlock`, `Pagination`,
  `SortButtons`, `DateRangeFilter`, `MetadataBackfillModal`, `PerfumeCharacteristics`,
  `ProgrammeOverviewCharts`, the search-widget components, and the `ReviewCard` variant system.
- The "Mid-redesign note" was stale — all of T141–T169 (tagged `collection-redesign` in
  TASKS.json) are `done`, so removed the transitional caveat.
- Found a genuine pre-existing gap unrelated to timing of the redesign: the `perfumes` review
  type (added back in T005, long before the redesign) was never documented in either file —
  missing from "What this project is", the file map, and the Data model table, despite having
  its own DynamoDB table, API routes, and `src/lib/perfumes.js`. Fixed since leaving it out means
  a new session reading only these docs would be surprised by `src/pages/reviews/perfumes/*`.
- Added the light "paper" Collection palette description (`--color-bg: #FBF7EF`) and confirmed
  the Matrix easter egg is unchanged (still swaps the same CSS custom properties, no markup
  changes) by reading `globals.css` and `MatrixLayout.js`.
- Mirrored the perfumes content-area + PerfumeRatings schema into README.md for consistency.

## Verification
- `grep -rniE "appearancepicker|cyberpunk|terraria|cotton.candy|ocean theme|sunset theme" CLAUDE.md README.md`
  → only the allowed historical AppearancePicker mention.
- `npm run lint` → 0 errors, 6 pre-existing warnings (img/exhaustive-deps), no new ones.
- `npm test` → 35 suites / 241 tests passed.
- `npm run build` → succeeded, `/reviews/perfumes*` routes present in output.

## Result
done T170
