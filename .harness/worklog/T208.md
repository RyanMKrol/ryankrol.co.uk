# T208

Extended `aggregateProgramme` in `src/lib/programmeStats.js` to support `programme === 'all'`:
filters to any workout where `classifyProgramme(w) !== null` (push/pull/legs combined),
excluding unclassifiable workouts (e.g. 'Rest Day'). push/pull/legs exact-match behavior unchanged.

Added tests in `src/lib/programmeStats.test.js` covering: totals.workouts for 'all' equals sum of
push+pull+legs; unclassifiable workouts excluded; sessions sorted date-ascending; frequencyByMonth
merges counts across programmes into the same month bucket.

Verified:
- `npm run lint` — 0 errors, pre-existing warnings only.
- `npm test` — all pass (14/14 in programmeStats.test.js).
- `npm run build` — succeeds.
