# T022 worklog

## What was done

Extended `src/pages/api/reviews/books/add.js` to accept and persist the optional Open Library
metadata fields: `olid`, `coverId`, `bookAuthors`, `firstPublishedYear`, `isbn`, `subjects`,
`pageCount`, `publisher`.

Each field is spread into the DynamoDB `PutCommand` item only when present (`!== undefined`),
so books saved without Open Library metadata continue to succeed unchanged.

All existing behaviour is preserved: password gate, rating validation (0–5), `clearApiCache('api-books')`.

## Checks

- `npm run build`: ✓ passed (28 static pages generated, no errors)
- No live API calls made.
