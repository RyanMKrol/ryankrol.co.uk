# T102: rate-limit book search route

## Do
- Added `checkRateLimit`/`getClientIp` gate to `src/pages/api/books/search.js`, mirroring the
  TMDB route: `book-search:<ip>` key, 20 req/60s window, 429 + `Retry-After` header + JSON
  message when blocked, before the `title`/`author` validation.
- Created `src/pages/api/books/search.test.js` (didn't exist) following the
  `src/pages/api/tmdb/search.test.js` pattern: mocks `rateLimit` + `apiCache`, asserts a 200 path
  under the limit and a 429/Retry-After/no-fetch path when blocked.
- Updated CLAUDE.md's file map row for this route to note the rate limit. Also corrected the row
  to match actual code — the existing row described an Open Library/Google Books dual-provider
  `?provider=` param that doesn't exist in `search.js` (it only calls Google Books). Flagging this:
  the two-provider design described elsewhere in CLAUDE.md may be a docs/code drift predating this
  task; out of scope to reconcile further here.

## Verify
- `npm run lint`: 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test`: 25 suites / 180 tests passed, including new `search.test.js`.
- `npm run build`: succeeded.
