# T238 — Deploy pending site changes to production

## Status: done. Completed interactively at the owner's direction.

## What was run

1. `git fetch origin && git status` — local `main` matched `origin/main` exactly, working tree clean.
2. `npm run build` locally — succeeded (only pre-existing lint warnings, no errors).
3. `vercel whoami` — authenticated as `ryankrolm-1385`.
4. `vercel --prod --yes` — exited 0.
5. Deployment reached `readyState: "READY"`, aliased to the production domain.

## Verification

- **Production URL**: `https://ryankrol-co-26meeqfc6-ryans-projects-80af0799.vercel.app`
  (deployment id `dpl_HWXKR6QLoKuVQyzzfsQ32Qhjongv`), aliased to `https://www.ryankrol.co.uk`.
- **Timestamp**: 2026-07-04T20:07:01Z.
- `curl -s -o /dev/null -w '%{http_code}' https://www.ryankrol.co.uk` → `200`.
- **Spot-checks** (read the real committed code first, then verified against the live site via a
  headless Playwright render — this app fetches all page data client-side in `useEffect`, so a plain
  `curl` on the page HTML only sees the shell, not rendered content; confirmed this pattern in
  `src/pages/workouts/index.js`, `src/pages/reviews/books/index.js`, `src/pages/projects/index.js`
  before deciding how to check):
  - **T217-T220 (English date format)**: `src/pages/workouts/index.js` imports and renders dates via
    `formatEnglishDate` (`src/lib/dateFormat.js`, `toLocaleDateString('en-GB', {day:'numeric',
    month:'long', year:'numeric'})`). Live: `/workouts` renders `4 July 2026` — confirmed.
  - **T223-T225 (CSS multi-column masonry for review lists)**: `src/pages/reviews/books/index.js`
    renders its list inside `.spine-cover-list`, which `src/styles/globals.css` gives
    `column-count: 2` (collapsing to 1 under 700px). Live: `/reviews/books` renders 1
    `.spine-cover-list` container holding 12 `.spine-cover-card` items — confirmed.
  - **T226-T227 (Projects forks-on-card + sort)**: `src/pages/projects/index.js` renders a
    `.project-card-forks` badge only when `repo.forks > 0`, and a sort-buttons control
    (`sortBy` state + `SortButtons`). Live: sort buttons render; the forks badge renders 0 times —
    checked this wasn't a bug by querying `/api/github/repos` directly: genuinely 0 of 58 repos have
    any forks, so the `forks > 0` guard is correctly suppressing the badge everywhere, not failing to
    render it. Confirmed correct, not broken.

## Dependencies covered

T217-T227, T229-T231, T233-T237 (19 buildable tasks from the 2026-07-04 `/convert-ideas` sweep;
T228/T232 are `needs-human` decision tasks with no code diff, correctly excluded per spec).
