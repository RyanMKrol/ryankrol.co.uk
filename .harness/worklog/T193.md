# T193: Deploy pending site changes to production

Pure operational task, no code changes (scope: []).

## Pre-deploy checks
- `git fetch origin && git status`: local `main` matched `origin/main` exactly (nothing to
  commit, working tree clean, no ahead/behind).
- Dependency tasks (T172-T175, T177-T179, T187, T189-T192) all confirmed `status: done` in
  `.harness/TASKS.json`.
- `npm run build` locally: succeeded (pre-existing `react-hooks/exhaustive-deps` /
  `no-img-element` warnings only, no errors).
- `vercel whoami`: authenticated as `ryankrolm-1385` — not blocked.

## Deploy
- `vercel --prod --yes` exited 0.
- Deployment id: `dpl_GqJuCKb7psRtMkyAbHDqpcvdJRt3`
- Production URL: `https://ryankrol-co-c00hrluyo-ryans-projects-80af0799.vercel.app`
- Aliased to: `https://www.ryankrol.co.uk`
- readyState: `READY`
- Timestamp: 2026-07-03T20:10:15Z

## Post-deploy verification
- Raw deployment URL: `curl -s -o /dev/null -w '%{http_code}'` → `302` (Vercel deployment
  protection/SSO redirect on the internal hostname — expected, not a failure, per T171
  precedent).
- Aliased production domain `https://www.ryankrol.co.uk`: `curl -s -o /dev/null -w '%{http_code}'`
  → `200`.

## Spot-checks (step 6)
Pages fetch their review/vinyl data client-side (see root CLAUDE.md architecture notes), so a
plain `curl` of `/vinyl` or `/reviews/perfumes` only returns the pre-hydration loading shell —
not useful for checking dynamically-rendered markup. Verified instead against the deployed,
compiled CSS bundle (`/_next/static/css/0692b33ae2434ae3.css`), which is static and directly
reflects what shipped from `main`:

- **T173 (container width fix)**: `src/styles/globals.css` on `main` defines
  `.container, .review-container { max-width: 1400px; ... }`. Fetched from the live CSS bundle:
  `.container,.review-container{max-width:1400px;margin:0 auto;padding:var(--gutter-desktop);...}`
  — present and matches. Confirms the no-longer-narrow container fix is live.
- **T187 (perfume card variant promotion)**: `src/pages/reviews/perfumes/index.js` on `main`
  imports and renders `Variant6Hybrid` directly (bypassing `ReviewCard`), whose root element is
  `<div className="perfume-v6-card">`. Fetched from the live CSS bundle:
  `.perfume-v6-card{display:flex;flex-direction:column;gap:.75rem;padding:1.25rem;...}` — present
  and matches. Confirms the promoted variant's styles are live (the component itself is
  statically bundled into the page's JS chunk, and its CSS class is only emitted because the
  component is actually rendered in the page tree).

## Result
All "Done when" criteria met. No code changes made (scope: [] respected — diff is this worklog
only).
