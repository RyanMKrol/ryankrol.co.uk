# T138: swap 🧴 → 👃 across perfume review pages

## Do

Like-for-like emoji swap at 5 exact sites:
- `src/pages/reviews/perfumes/add.js:127` page title
- `src/pages/reviews/perfumes/edit.js:60` page title
- `src/pages/reviews/perfumes/edit/[id].js:222` page title
- `src/components/PerfumeCharacteristics.js:121` `SpraySpotCounter` button glyph
- `src/components/PerfumeCharacteristics.js:173` `ApplicationSpotsSprayer` form label

No surrounding text/wording touched; `💦` poof elements untouched.

## Verified

- `grep -rn "🧴" src/` → no matches (exit 1).
- `npm run lint` → 0 errors, same 3 pre-existing warnings as before (dev/cache exhaustive-deps,
  movies/tv edit `<img>` no-img-element) — no new warnings.
- `npm test` → 35 suites / 249 tests passed, including `PerfumeCharacteristics.test.js`.
- `npm run build` → succeeded, all routes generated including `/reviews/perfumes/*`.
- `verify: ["run-app"]`: not run live (headless/unattended run, no browser available). Code
  inspection confirms 👃 replaces 🧴 exactly at the intended render sites with structure/text
  unchanged; visual glyph rendering could not be manually screenshotted in this environment.
