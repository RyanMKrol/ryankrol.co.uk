# T218: wire formatReviewDate into ReviewCard

## Do
Imported `formatReviewDate` from `../lib/dateFormat` in `src/components/ReviewCard.js` and used it
in both render spots that previously rendered raw `DD-MM-YYYY` strings:

1. `poster-banner` variant: `<span className="poster-banner-date">{item.date}</span>` →
   `{formatReviewDate(item.date)}`, keeping the truthy check on the raw `item.date`.
2. Default fallback branch (hit today by all four review types' edit-list pages via
   `styleVariant={2}`): `Date: {item.date}` → `Date: {formatReviewDate(item.date)}` and
   `Updated: {item.editedDate}` → `Updated: {formatReviewDate(item.editedDate)}`, truthy checks
   unchanged.

`SpineCoverCard` and `SquareCoverCard` were left untouched per the task spec (separate follow-up).

## Verify
- `npm run lint` — 0 errors, 6 pre-existing warnings (no new ones).
- `npm test` — 43 suites / 287 tests passed.
- `npm run build` — succeeded, all 32 pages generated.
- Manual verify was NOT run via a live dev server (headless run); verified by code inspection
  instead: `formatReviewDate('17-05-2026')` (per `src/lib/dateFormat.test.js`, already covered)
  parses `DD-MM-YYYY` and formats via `toLocaleDateString('en-GB', {day:'numeric', month:'long',
  year:'numeric'})`, producing `'17 May 2026'`. Both edited call sites now route through this
  function wherever `item.date`/`item.editedDate` are truthy, so:
  - `/reviews/movies` and `/reviews/tv` (poster-banner cards) will render dates like '17 May 2026'
    instead of '17-05-2026'.
  - `/reviews/movies/edit`, `/reviews/tv/edit`, `/reviews/books/edit`, `/reviews/albums/edit`
    (all pass `styleVariant={2}`, hitting the default branch) will render 'Date: 17 May 2026' /
    'Updated: 17 May 2026' instead of the raw string.
