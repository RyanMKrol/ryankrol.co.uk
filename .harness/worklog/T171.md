# T171: Deploy the Collection redesign to production

Pure operational task (scope: [], no code changes) — the deploy checkpoint for T141-T170.

## Steps taken

1. `git fetch origin && git status` — local `main` matched `origin/main` exactly, working tree
   clean, nothing to pull.
2. `npm run build` locally — succeeded (pre-deploy sanity check, mirrors CI).
3. `vercel whoami` — authenticated as `ryankrolm-1385`. Not blocked.
4. First `vercel --prod --yes` attempt failed: local Vercel CLI was 46.0.2, and the deploy API now
   requires >=47.2.2 ("Your Vercel CLI version is outdated"). Ran `npm i -g vercel@latest`, which
   installed 54.20.0. This is a global tool upgrade, not a repo change — no files in the repo were
   touched by this step.
5. Re-ran `vercel --prod --yes` — succeeded.

## Result

- Deployment ID: `dpl_4goKJ87xrAhecvv3aPCUpLpTAQ7K`
- Deployment URL: `https://ryankrol-co-ck7ul52a9-ryans-projects-80af0799.vercel.app` (readyState: READY)
- Aliased production domain: `https://www.ryankrol.co.uk`
- Timestamp: 2026-07-03T15:20:32Z (UTC, captured immediately after the verification curls below)

**Note:** the raw per-deployment URL (`ryankrol-co-ck7ul52a9-...vercel.app`) returned `302` when
curled directly — this is Vercel's deployment-protection/SSO redirect on the internal deployment
URL, not a deploy failure. The actual production alias (`www.ryankrol.co.uk`, the URL real users
and the `CLAUDE.md`-documented domain hit) returned `200` and is the correct signal for "is this
live." Verified against that domain instead.

- `curl -s -o /dev/null -w '%{http_code}' https://www.ryankrol.co.uk/` → `200`
- `curl -s https://www.ryankrol.co.uk/ | grep -o "Bricolage+Grotesque"` → matched (`Bricolage+Grotesque`)
- `curl -s https://www.ryankrol.co.uk/ | grep -o "background-color:#FBF7EF"` → matched (`background-color:#FBF7EF`)

Both post-redesign markers are present on the live homepage HTML — the Collection redesign
(T141-T170) is now confirmed live in production, not just merged to `main`.

## Outcome

`done T171` — build/lint gates were already covered by CI on every prior commit; this task made no
code changes (as scoped), only ran the deploy and verified it live.
