# T171: Deploy the Collection redesign to production

Pure operational task (scope: [], no code changes) — the deploy checkpoint for T141-T170.

## Steps taken (this run)

1. `git fetch origin && git status` — local `main` matched `origin/main` exactly, working tree
   clean, nothing to pull.
2. `npm run build` locally — succeeded (pre-deploy sanity check, mirrors CI).
3. `vercel whoami` — authenticated as `ryankrolm-1385`. Not blocked.
4. `vercel --prod --yes` — succeeded, exit 0.

## Result

- Deployment ID: `dpl_5rfEmdGYTScjXPrbQ57nPeVJyoYj`
- Deployment URL: `https://ryankrol-co-fl5iippue-ryans-projects-80af0799.vercel.app` (readyState: READY)
- Aliased production domain: `https://www.ryankrol.co.uk`
- Timestamp: 2026-07-03T15:44:41Z (UTC, captured immediately after the verification curls below)

**Note:** the raw per-deployment URL returned `302` when curled directly — this is Vercel's
deployment-protection/SSO redirect on the internal deployment URL, not a deploy failure. The actual
production alias (`www.ryankrol.co.uk`, the URL real users and `CLAUDE.md` document) returned `200`
and is the correct signal for "is this live." Verified against that domain instead.

- `curl -s -o /dev/null -w '%{http_code}' https://www.ryankrol.co.uk/` → `200`
- `curl -s https://www.ryankrol.co.uk/ | grep -o "Bricolage+Grotesque"` → matched (`Bricolage+Grotesque`)
- `curl -s https://www.ryankrol.co.uk/ | grep -o "background-color:#FBF7EF"` → matched (`background-color:#FBF7EF`)

Both post-redesign markers are present on the live homepage HTML — the Collection redesign
(T141-T170) is confirmed live in production.

## Note on prior run

A previous build of this task (worklog + `.harness/worklog/T171.audit.md`, audit PASS) already
completed the same deploy and verification, but `TASKS.json` still showed `status: "pending"` for
T171 — the known interrupt-orphan pattern documented in `.harness/CLAUDE.md`'s "Known-but-deferred
issues". The loop reselected the task cold and this run re-deployed (idempotent — same code, same
result) and re-verified independently, arriving at the same passing outcome.

## Outcome

`done T171` — build/lint gates were already covered by CI on every prior commit; this task made no
code changes (as scoped), only ran the deploy and verified it live.

## Recovery note (2026-07-03, owner-directed)

The 16:45:22 commit (`59446e7`, matching the "Steps taken (this run)" above) landed as `[skip ci]` —
the loop tags worklog-only/no-code-diff commits that way, correctly, since `T171` is `scope: []`.
But `wait_ci_green()` was called unconditionally afterward and sat waiting for a CI run that could
never exist (GitHub never runs a workflow for a `[skip ci]`-tagged commit) — confirmed via
`gh run list`, zero runs for that SHA. This is a second occurrence of the same shape that hit the
prior attempt (`ef0b146`, 16:20:57 → 24 min later reselected cold as `59446e7`); left running it
would have kept timing out and harmlessly-but-wastefully redeploying every `CI_TIMEOUT` (20 min)
indefinitely — the indeterminate-CI path deliberately leaves `main` untouched rather than reverting,
so the live site was never at risk, just the loop's own time/cost.

Independently re-verified before marking done (owner stopped the loop first, confirmed via `ps`/lock
check): `curl` of `https://www.ryankrol.co.uk/` → `200`, both `Bricolage+Grotesque` and
`background-color:#FBF7EF` markers present, and `vercel ls --prod` shows two successful READY
production deployments (22m and 47m old at verification time) — matching the two build attempts.
Root cause fixed in `loop.sh`: `wait_ci_green` (or its caller) now skips the CI wait entirely when
the just-pushed commit message contains `[skip ci]`, going straight to `mark_done`, so no future
`scope: []`/worklog-only task hits this.
