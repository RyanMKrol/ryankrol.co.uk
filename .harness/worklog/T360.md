# T360 — 2026-07-10

## What I did
Replaced the raw CSS grid (`repeat(auto-fit, minmax(280px, 1fr))`, 3-per-row at desktop widths) in
`src/pages/dev/perfume-card-variants.js` with the exact same layout mechanism the real
`/reviews/perfumes` page uses: `useResponsiveColumnCount(2, 700)` feeding `<MasonryColumns
items={perfumes} columnCount={columnCount} className="perfume-card-grid"
columnClassName="perfume-card-grid-col" renderItem={...} />`, once per each of the 4 VARIANTS
sections. Each section keeps its own `key`/`variant` closure in `renderItem` so `Variant6Hybrid`
still receives the correct `variant` prop per section. No changes to `globals.css`,
`MasonryColumns.js`, `masonryColumns.js`, `useResponsiveColumnCount.js`, or the real perfume page
— reused as-is per the task spec.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this file).
- `npm run build` — succeeded, `/dev/perfume-card-variants` built as a static page (992 B).
- `node scripts/visual-check.mjs` (via `VISUAL_CHECK_ONLY=perfume-card-variants`) — ran, 1/1
  capture passed.

## Visual verification (observed)
Opened `scripts/visual-out/dev-perfume-card-variants.png`. All 4 variant sections (Baseline,
Big rating, Header rating, Best-for moved to bottom) render exactly 2 equal-width columns at the
1440px capture viewport (well above the 700px breakpoint) — Nuée Bleue and Aventus in column 1,
Bleu de Chanel alone in column 2 (masonry round-robin distribution of the 3-item sample), no
3-per-row packing. Column widths and the ~1.5rem gap visually match the real `/reviews/perfumes`
page's card sizing.

## Remaining
None — task scope only covers this one dev page file.
