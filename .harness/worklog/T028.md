# T028 worklog — Last.fm album-search proxy routes + normaliser

## What was built

- `src/lib/lastfm.js` — pure normalisers: `mapAlbumSearchResult` and `mapAlbumInfo`
  - `pickLargestImage` helper iterates `['extralarge','large','medium','small']` to find the first non-empty URL
  - `mapAlbumInfo` handles both string and object `artist` fields; defaults all optional fields to null/[]/0
- `src/lib/lastfm.test.js` — 11 unit tests; all pass
- `src/pages/api/lastfm/album-search.js` — GET `?query=`; blank query returns `{ results: [] }`; 405 on non-GET; cached under `lastfm-album-search`
- `src/pages/api/lastfm/album-info.js` — GET `?artist=&album=` or `?mbid=`; 405 on non-GET; cached under `lastfm-album-info`
- `CLAUDE.md` — file map updated with the two new routes + `src/lib/lastfm.js`

## Suite results

- `npm run lint` — 0 errors (1 pre-existing warning in dev/cache.js, untouched)
- `npm test` — 108 tests passed across 11 suites
- `npm run build` — succeeded; both routes visible as `ƒ (Dynamic)` in route table

## Empirical checks

No live API calls made (metered). Code correctness verified via unit tests and build output.
