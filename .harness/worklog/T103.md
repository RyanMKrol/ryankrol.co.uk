# T103: rate-limit all four Last.fm routes

## Do

Applied the same `checkRateLimit`/`getClientIp` gate used by TMDB/Google Books to all four
Last.fm proxy routes, inserted immediately after the existing method-check, before any other
logic:

- `now-playing.js` — key prefix `lastfm-now-playing`
- `top-albums.js` — key prefix `lastfm-top-albums`
- `album-search.js` — key prefix `lastfm-album-search`
- `album-info.js` — key prefix `lastfm-album-info`

Each uses a distinct cache-key prefix (`windowMs: 60_000, max: 20`) so the four routes don't
share one counter. On block: `Retry-After` header + `429` JSON `{ message }`.

Added `src/pages/api/lastfm/album-search.test.js` (none existed for `src/pages/api/lastfm/*`
before), mocking `../../../lib/rateLimit` the same way as `src/pages/api/books/search.test.js`.
It covers: (a) a normal 200 response when under the limit, fetch called; (b) 429 + `Retry-After`
+ JSON message when `checkRateLimit` reports blocked, with the underlying `fetch` never called.
The other three routes apply the identical pattern (verified by reading their diffs), so one
test file exercises the shared logic per the task's own allowance.

Updated root `CLAUDE.md`'s file map rows for the four `src/pages/api/lastfm/*.js` routes to
note the rate limit.

## Verify

- `npm run lint` — 0 errors, pre-existing warnings only.
- `npm test` — 26 suites / 182 tests pass, including the new `album-search.test.js`.
- `npm run build` — succeeds.
