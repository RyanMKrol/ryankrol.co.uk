# T228 — Decide whether commit-count sourcing (GitHub GraphQL migration) is worth building

## Status: done. Decision recorded via a fresh owner idea, converted into real tasks.

The owner filed a fresh, explicit, unprompted idea directly requesting commit count (display +
sort) on the Projects page, plus asked about lines-of-code "if available." Converted via
`/convert-ideas` into:

- **T254** — source per-repo commit counts in `src/pages/api/github/repos.js` via GitHub REST N+1
  (`GET /repos/{full_name}/commits?per_page=1"` + `Link` header `rel="last"` page-count parsing) —
  the recommended default from this task's own research, since `package.json` still has zero
  GraphQL client/dependency and the existing 4-hour cache TTL means the N+1 fan-out only fires on
  a cache miss, comfortably inside GitHub's rate ceiling. GraphQL migration was NOT chosen.
- **T255** — render commit count on Projects cards + add a commits sort option, depends on T254.

**Lines-of-code was explicitly declined by the owner** (asked directly via `AskUserQuestion`,
citing this task's own finding that LOC has no real GitHub API answer): "Drop LOC entirely... No
LOC task, no new infra for LOC at all." Recorded here for posterity — not a silent drop, a real
decision.

This task's own "Done when" is satisfied: a decision was recorded (build commit count via REST,
decline LOC) and follow-up tasks were authored. No code was written under this task id itself.
