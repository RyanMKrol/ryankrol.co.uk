# T373 — Workshop 5 alternate spine-cover book-card designs

2026-07-12

## What I did

- `src/components/ReviewCard.js`: `SpineCoverCard` now accepts a `designVariant` prop (1-5) and
  branches to render one of five genuinely-distinct layouts (Ledger, Badge, Ribbon, Mono Label,
  Serif Feature). No `designVariant` (the public `/reviews/books` path) renders the original
  default layout, now with the tightened gap + list-indent fix applied (see below) — the public
  page's design itself is unchanged, per the task's "keeps current design until the winner is
  hardcoded" instruction. `designVariant` is threaded through the top-level `ReviewCard` export.
- `src/styles/globals.css`:
  - Reduced `.spine-cover-body`'s flex `gap` from `0.4rem` to `0.2rem`, tightening the title↔meta
    spacing for every spine-cover card (base + all 5 variants).
  - Added a scoped Markdown list-indent fix: `.spine-cover-snippet ol, .spine-cover-snippet ul`
    get `padding-left: 1.4rem` + explicit `list-style-type`/`position` — this does NOT touch the
    global `list-style: none` resets (confirmed the two hits at the old L1995/L3774 line numbers
    are `.workout-session-exercise-list` / `.perfume-v4-checklist`, unrelated to spine-cover or
    nav pills).
  - Added `.spine-v1-*` … `.spine-v5-*` blocks for each variant's distinct treatment.
- `src/pages/dev/book-card-designs.js` (new): workshop-only preview page, not linked from any nav,
  rendering all 5 variants stacked, each against two example books: "Ultra-Processed People" (long
  body → truncates → Read more) and "The Horus Heresy 42: Garro" (Markdown ordered-list body).
- `scripts/_visual-harness.mjs`:
  - Added both example books to the `books` fixture array (so `/reviews/books` itself also now
    has a truncating book and an ordered-list book).
  - Added a `book-card-designs` `PAGES` entry (baseline: all 5 variants, ordered list visible,
    read-more collapsed).
  - Added a `book-card-designs-read-more` `FLOWS` entry that clicks every `.spine-cover-expand-btn`
    on the page, expanding all 10 cards (5 variants × Ultra-Processed People) to prove the
    read-more/expanded state renders correctly in every variant.

## Checks run

- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated files).
- `npm test` — 78 suites / 469 tests passed.
- `npm run build` — succeeded; `/dev/book-card-designs` built as a static page.
- `VISUAL_CHECK_ONLY=book-card node scripts/visual-check.mjs` — both captures passed
  (`book-card-designs`, `book-card-designs-read-more`). **Looked at both PNGs**: all 5 variants
  are visibly distinct (rating placement, typography, meta treatment), the title↔meta gap is
  tight in every variant, the Horus Heresy ordered list renders with a clear left indent (numerals
  no longer flush against the card edge) in every variant, and every variant's "Read more" click
  correctly expands to "Show less" with the full body showing.
- `VISUAL_CHECK_ONLY=reviews-books node scripts/visual-check.mjs` — all 9 captures (baseline +
  search + 3 sort fields × 2 directions) passed. **Looked at** `reviews-books.png`: the public
  page still renders the original (non-variant) spine-cover design, now with the tighter gap, and
  the new Horus Heresy card's ordered list also renders correctly there (list-indent fix applies
  globally to every spine-cover card, not just the 5 workshop variants).

## Remaining

None — a follow-up task will pick a winning variant and hardcode it into the public
`/reviews/books` page (this task deliberately leaves that page on the current design, per spec).
