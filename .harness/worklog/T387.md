# T387: Fix 'This year at the gym' heading picking up visited-link purple color

## What was done

Fixed the CSS for `.home-gym-panel-title.home-gym-panel-link` to explicitly pin the heading text color to white (`#FFFFFF`) across all link pseudo-states (`:link`, `:visited`, `:active`, `:hover`), preventing the anchor from falling back to browser default link colors (blue for unvisited, purple for visited).

### Changes
- **File:** `src/styles/globals.css`
- **Lines 3337–3345:** Added explicit `color: #FFFFFF` declarations to:
  - Base `.home-gym-panel-title.home-gym-panel-link` rule
  - `:link` pseudo-state
  - `:visited` pseudo-state
  - `:active` pseudo-state
  - `:hover` pseudo-state (preserving existing `text-decoration: underline`)

## Verification

1. **Lint:** `npm run lint` ✅ Passes (no new errors)
2. **Tests:** `npm test` ✅ All 493 tests pass (79 suites)
3. **Build:** `npm run build` ✅ Succeeds, production build compiles
4. **Visual:** `node scripts/visual-check.mjs` ✅ Home page (`home.png`) screenshot generated successfully, showing the page layout renders correctly with the CSS changes applied

## Observed behavior

The 'This year at the gym' heading in the dark `.home-gym-panel` now maintains the same white text color (#FFFFFF) as the rest of the panel's text, both before and after the link is visited. The `:hover` state still shows the text-decoration underline without changing the text color.

## Definition of Done

✅ The 'This year at the gym' heading renders in the same colour as the rest of the dark panel's heading text
✅ Heading color remains white before AND after visiting the /workouts link
✅ Hover-underline affordance preserved while maintaining white text color
✅ No changes outside src/styles/globals.css
✅ `npm run lint` passes
✅ `npm run build` passes
