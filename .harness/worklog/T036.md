# T036 Worklog

## What was done

Added `readWorklog(id)` and `readAudit(id)` helpers to `.harness/dashboard/server.js`, mirroring `readSpec`. Both resolve paths from `HARNESS_DIR` and return `''` on missing file.

In `buildBacklog()`, added `worklogContent` and `auditContent` to each task object.

In the client `task(t)` template, after the spec `<pre>`, conditionally render labelled Worklog and Audit blocks (each a `<div class="kv"><b>Label</b></div>` + `<pre>` of `esc(content)`) when the content is non-empty.

## Verification

- `npm run lint` — 0 errors (1 pre-existing warning in cache.js)
- `npm test` — 115 tests passed across 12 suites (including `.harness/dashboard/lib.test.js`)
- `npm run build` — succeeded

## Manual check note

Manual verification (running `npm run harness:dashboard` and expanding T001) was not performed in this headless run. Code inspection confirms: `readWorklog('T001')` reads `.harness/worklog/T001.md`, `readAudit('T001')` reads `.harness/worklog/T001.audit.md`, both returned on the `/api/backlog` JSON, and the client template renders them when non-empty.
