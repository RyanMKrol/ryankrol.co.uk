# T256: remove dead getWorkoutsPaginated legacy path

## Changes
- `src/lib/workoutQueries.js`: deleted `getWorkoutsPaginated` (with its JSDoc), the only
  caller was `src/pages/api/workouts.js`.
- `src/pages/api/workouts.js`: removed the `getWorkoutsPaginated` import and the
  `mode === 'all'` conditional; the handler now always runs what was the `mode=all` body
  (full list via `getAllWorkouts`, cache key `workouts-dynamo-all`, backfill trigger on
  cache miss). Legacy `page`/`pageSize`/`workouts-dynamo` cache-key branch deleted entirely.
- `CLAUDE.md`: removed the Gotchas bullet describing the now-deleted pagination-scans-then-slices
  behaviour (the described bug path no longer exists — the current `mode=all` + client-side
  `filterWorkouts`-then-`paginate` flow filters the full list before slicing).
- `src/lib/workoutBackfill.js` left untouched, per spec (its wildcard `clearApiCache` call still
  matches the live `workouts-dynamo-all` key).

## Verification
- `grep -rn "getWorkoutsPaginated" src/` → no matches.
- `grep -n "workouts-dynamo'" src/pages/api/workouts.js` → no matches.
- `npm run lint` → 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` → all suites pass, including `workoutQueries.test.js` unmodified.
- `npm run build` → succeeded, `/api/workouts` route present in output.
