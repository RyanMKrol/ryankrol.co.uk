# T092 — Delete all three UI rewrite variants (v1/v2/v3)

## What was done

Deleted the experimental UI rewrite trees entirely (owner reviewed all three and judged
them "pretty bad" — none promoted):

- `src/pages/v1/**` + `src/components/v1/**`
- `src/pages/v2/**` + `src/components/v2/**`
- `src/pages/v3/**` + `src/components/v3/**`
- `.harness/designs/T042-ui-rewrite-briefs.md` (decision already recorded in git history /
  this task; no longer needed once the variants are gone)

Production pages/components and `src/pages/api/**` were left untouched.

## Verification

- `grep -rE "/v[123]" src/` — only incidental matches remain (Hevy `api.hevyapp.com/v1/...`,
  Google Books `googleapis.com/books/v1/...`); no reference to the deleted variant routes.
- `npm run lint` — 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test` — 21 suites / 161 tests passed; no suite referenced the deleted files.
- `npm run build` — succeeded; route manifest shows no `/v1`, `/v2`, `/v3` entries.
