# T087 — Require a confirmed Google Books match to add a book review

## Changes

- `src/pages/reviews/books/add.js`:
  - `handleSubmit` now returns early (with an inline error message, no fetch) if `bookMatch`
    is null.
  - The match payload fields are now spread unconditionally (they're only reachable once
    `bookMatch` is guaranteed non-null).
  - "Book Match (optional)" label changed to "Book Match"; added an inline `error-message`
    hint under `BookSearch` when no match is selected yet.
  - Submit button `disabled` now also requires `bookMatch` to be truthy.
- `src/pages/api/reviews/books/add.js`: added `!volumeId` to the required-fields 400 check,
  alongside title/author/rating/overview.
- Added `src/pages/api/reviews/books/add.test.js` (route had no prior test file): asserts a
  400 when `volumeId` is missing (other required fields present) and a 201 happy path when
  `volumeId` is present.

## Verification

- `npm run lint` — 0 errors (4 pre-existing warnings, unrelated to this change).
- `npm test` — 19 suites / 157 tests passed, including the new `add.test.js`.
- `npm run build` — succeeded, `/reviews/books/add` compiled cleanly.
- Did not touch `src/pages/reviews/books/edit/[id].js` or `src/pages/api/reviews/books/update.js`,
  per the task's scope.
