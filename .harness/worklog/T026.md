# T026 worklog — Proportional time x-axis on all date charts

## What was done

- Added `chartjs-adapter-date-fns` and `date-fns` to `package.json`.
- Created `src/lib/chartTime.js` with:
  - `toTimeSeries(rows, getDate, getValue)` — maps rows to `{x: <ms-timestamp>, y}` sorted ascending, dropping invalid/blank dates.
  - `timeScaleOptions` — shared chart.js `time` scale x-axis fragment.
- Created `src/lib/chartTime.test.js` with 6 hermetic unit tests covering: correct shape, numeric timestamp, ascending sort, missing dates dropped, bad dates dropped, empty input.
- Updated `src/components/ExerciseProgressCharts.js`: replaced `CategoryScale` with `TimeScale`, added adapter import, switched data from parallel arrays to `toTimeSeries(...)` calls, spread `timeScaleOptions` into `scales.x`, updated insights section to use `.y` on data points.
- Updated `src/components/CardioProgressCharts.js`: same pattern as above. Also fixed `hasPaceData` check to use `pt.y > 0`.
- Updated `src/components/ProgrammeOverviewCharts.js`: registered both `CategoryScale` (for the Bar frequency chart) and `TimeScale` (for the two Line charts), split `baseOptions` into `lineOptions` (time x-axis) and `barOptions` (category x-axis), converted session Line charts to `toTimeSeries`.
- Updated `CLAUDE.md` conventions section with the "date charts use a chart.js time scale" rule.

## Verification

- `npm run lint` — 0 errors, 1 pre-existing warning (react-hooks/exhaustive-deps in dev/cache.js — not introduced by this task).
- `npm test` — 10 suites, 97 tests, all pass (including 6 new chartTime tests).
- `npm run build` — succeeded cleanly, all 28 static pages generated.
- Live UI check not performed (headless environment) but build verified the adapter is correctly wired — Next.js would fail static generation if the import were broken.
