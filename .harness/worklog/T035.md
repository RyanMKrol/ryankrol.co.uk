# T035 worklog

## What was done

- `src/components/BookSearch.js`: replaced single "Search" button with two provider buttons
  ("Search Open Library", "Search Google Books"). Each calls
  `/api/books/search?provider=<key>&title=…&author=…`. Both disabled until title is entered.
  Added `currentProvider` state + a source label above results. Cover resolution now checks
  `coverUrl` first (Google Books), then falls back to `bookCoverUrl(coverId)` (Open Library).
  `handleSelect` now passes `source`, `coverUrl`, and `volumeId` through `onSelect`.

- `src/pages/reviews/books/add.js`: added `source`, `coverUrl`, `volumeId` to the submit body
  spread when `bookMatch` is set. Updated label from "Open Library Match" to "Book Match".

- `src/pages/api/reviews/books/add.js`: destructured `source`, `coverUrl`, `volumeId` from
  `req.body`; added them to `reviewData` using the existing conditional-spread pattern so they
  are only stored when defined.

- `CLAUDE.md`: updated `BookRatingsV3` table row to document `source`, `coverUrl`, `volumeId`,
  and all optional Open Library / Google Books fields.

- `README.md`: added full `BookRatingsV3` schema section (before Workouts Table).

## Suite results

- `npm run lint`: 0 errors (1 pre-existing warning in dev/cache.js — not modified)
- `npm test`: 115 tests passed, 12 suites
- `npm run build`: succeeded

## Verify note

Manual UI verification requires a running dev server + real API calls (Google Books is keyless
so this would not incur cost, but is out of scope for the headless build pass). Code review:
- Both buttons correctly pass `provider` param to the search URL
- `getCoverSrc` covers both `coverUrl` (Google) and `coverId` (Open Library) paths
- API route persists all three new fields conditionally — no regressions to Open Library flow
