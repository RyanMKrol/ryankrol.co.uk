
---
failed:blocked T234 — agent reported failed:blocked — needs src/lib/workoutBackfill.js (out of scope) — its internal imports lack .js extensions, breaking plain-node ESM resolution once auditWorkouts.js imports it
