# T227: sort options for /projects

Added a `SortButtons` control (stars, date) to `src/pages/projects/index.js`, reusing the
same component/pattern already used on the review-list pages (`SORT_FIELDS` config +
`sortBy` state, mirrored from `src/pages/reviews/movies/index.js`).

- New `sortedRepos` `useMemo` derives from `filteredRepos` (search/tag filtering unchanged)
  and sorts by `lastPush` (date/date-asc) or `stars` (stars/stars-asc, `|| 0` fallback).
  Default `'date'` preserves the existing newest-first order from the API.
- Grid now renders `sortedRepos` instead of the raw `filteredRepos`.
- Dropped the now-inaccurate "sorted by recent activity" clause from the page's meta copy.
- Left `src/components/SortButtons.js` and `src/pages/api/github/repos.js` untouched per
  the task's scope — the API's own sort remains just the initial fetch order.

Verified: `npm run lint` (0 errors, pre-existing warnings only), `npm test` (287 passed),
`npm run build` (succeeds, `/projects` route builds). Did not run `npm run dev` +
manual click-through (no interactive/browser session available headless) — the sort logic
is a direct copy of the already-shipped review-page pattern (same field/flip semantics via
`SortButtons`), and the `sortedRepos` derivation was verified by reading the code paths and
confirming filter → sort ordering is preserved.
