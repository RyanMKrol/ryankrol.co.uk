# T216: Deploy pending site changes to production

## Actions taken

1. `git fetch origin && git status` — local `main` was already up to date with `origin/main` (clean tree).
2. `npm run build` locally — succeeded (only pre-existing warnings: `no-img-element` x4, one
   `react-hooks/exhaustive-deps` on `dev/cache.js`).
3. `vercel whoami` — authenticated as `ryankrolm-1385`. Proceeded.
4. `vercel --prod --yes` — exited 0.
   - Deployment: `dpl_HNj28A2xXjqBz4boc31p9RVZmZUw`
   - Raw URL: `https://ryankrol-co-6aggigy49-ryans-projects-80af0799.vercel.app`
   - `readyState`: `READY`
   - Aliased to production domain: `https://www.ryankrol.co.uk`
5. `curl -s -o /dev/null -w '%{http_code}' https://www.ryankrol.co.uk` → `200`.

## Spot-checks (derived from reading committed code on `main` before checking prod)

- **T200 (favicon):** `public/favicon.svg` exists, referenced in `src/pages/_document.js:14` as
  `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`. Live: `GET
  https://www.ryankrol.co.uk/favicon.svg` → `200`.
- **Add-page title copy (T194/T195):** `src/pages/reviews/movies/add.js:89` renders
  `<h1 className="page-title">add movie review</h1>` — lowercase, no emoji. Live:
  `curl https://www.ryankrol.co.uk/reviews/movies/add` contains the literal string
  `add movie review`. Matches.
- **T212 (`.container`/`.review-container` white-background fix):** `src/styles/globals.css`
  lines 639–663 define `.container` / `.review-container` with no `background-color` property
  (only max-width/margin/padding). Fetched the live production CSS bundle
  (`_next/static/css/eef79fccf8e9b897.css`) and grepped for `.review-container{...}` — the shipped
  rule is `max-width:1400px;margin:0 auto;padding:var(--gutter-desktop)` (plus the mobile padding
  override), no `background-color`. Matches.

## Result

All done-when criteria met. Deployment is live and reflects the batch of T194–T215 changes.
