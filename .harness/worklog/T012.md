# T012 worklog — Date-range filter on /programmes dashboard

## What was done

Added `DateRangeFilter` + `filterByDateRange` to `src/pages/programmes/index.js`.

Changes:
- Imported `DateRangeFilter` (component) and `filterByDateRange` (helper from `src/lib/dateRange.js`)
- Added `dateRange` state defaulting to `'1y'`
- Added `filteredWorkouts` memo: `filterByDateRange(allWorkouts, dateRange, (w) => w.start_time)`
- Passed `filteredWorkouts` (not `allWorkouts`) into `aggregateProgramme` so all charts + headline totals reflect the chosen window
- Rendered `DateRangeFilter` in the search container below the programme selector, labelled "Period:"

## Verification

- `npm run build` — passed cleanly
- No new files created; only `src/pages/programmes/index.js` modified (within scope)
- Both filters (programme + date range) compose independently via two sequential memos
