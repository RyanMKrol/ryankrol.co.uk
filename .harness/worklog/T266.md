# T266 — fix perfumes/edit Edit button overlapping the type Badge

## Do

- `src/pages/reviews/perfumes/edit.js`: added `review-card-with-edit--perfume` modifier
  class to the per-card wrapper div (alongside the existing `review-card-with-edit`).
- `src/styles/globals.css`: added `.review-card-with-edit--perfume` /
  `.review-card-with-edit--perfume .edit-button-overlay` rules after the existing
  `--square-cover` block (same pattern as the books/albums fixes: pull the edit button
  out of absolute positioning into normal flow, right-aligned below the card content).

## Verify

- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated files) — no new warnings.
- `npm test`: 51 suites / 332 tests passed.
- `npm run build`: succeeded, `/reviews/perfumes/edit` built at 761 B.
- Did not run `npm run dev` + manual browser click-through (headless run) — verified by
  code inspection: the new CSS rule mirrors the already-shipped `--square-cover` fix
  (T265) which resolved the equivalent overlap for albums, and the Edit link's `href`
  was untouched, so navigation behaviour is unchanged.
