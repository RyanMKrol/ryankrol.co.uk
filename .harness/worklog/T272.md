# T272: Hot takes page + homepage link

## Do
- Added `src/pages/hot-takes/index.js` — fetches `/api/hot-takes` (already built by T271) in a
  `useEffect`, renders a plain newest-first bulleted list (`<ul>`/`<li>`, no card grid, no cover
  art/rating) using `.page-title` typography + `formatReviewDate` for the DD-MM-YYYY date. Shows
  a simple "No hot takes yet." empty state.
- Added `src/pages/hot-takes/add.js` — password-gated form mirroring `src/pages/vinyl/add.js`'s
  structure (loading/message/error state, POST to `/api/hot-takes/add`, redirect to `/hot-takes`
  on success). Client-side rejects an empty/whitespace-only take before submitting, mirroring
  `validateHotTakeText`.
- Added a `~/hot-takes` entry to `QUICK_LINKS` in `src/pages/index.js`. Confirmed via `git diff`
  that only the `QUICK_LINKS` array changed — the `latestTakes` useMemo and its rendering block
  (`home-latest` section) are untouched.
- Docs: added a `HotTakes` row to CLAUDE.md's Data model table (`{ id, text, date 'DD-MM-YYYY' }`),
  added the new pages + API routes to CLAUDE.md's file map, and added a `HotTakes Table` schema
  section to README.md matching the existing per-table doc style.

## Verify
- `npm run lint` — no errors (only pre-existing warnings, e.g. `no-img-element`,
  `exhaustive-deps` on `dev/cache.js`).
- `npm test` — 52 suites / 338 tests passed.
- `npm run build` — succeeded; `/hot-takes` and `/hot-takes/add` both built as static pages.
- Did not make any live DynamoDB/API calls (would touch real data) — verified via code
  inspection + build output that the routes wire up to the T271 API endpoints as expected.
