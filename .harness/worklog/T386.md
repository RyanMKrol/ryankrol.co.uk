# T386: Remove dead WorkoutCard component

## Summary
Removed the dead `src/components/WorkoutCard.js` component which was imported nowhere in the application. Updated CLAUDE.md to remove WorkoutCard from the Components list in the file map.

## Work Done

### 1. File Deletion
- Deleted `/src/components/WorkoutCard.js` (180 lines, dead code not imported anywhere)

### 2. Documentation Update
- Updated `CLAUDE.md` Components section to remove `WorkoutCard` from the list
- Component list now reads: `...`PerfumeCharacteristics`, `ExerciseProgressCharts` / `CardioProgressCharts` / `ProgrammeOverviewCharts`...` (removed `WorkoutCard` from between `PerfumeCharacteristics` and `ExerciseProgressCharts`)

### 3. CSS Classes Verification
- Confirmed that CSS classes referenced in WorkoutCard (`.exercise-item`, `.workout-meta`, `.workout-stats`, `.workout-detail-link`, `.review-title`, `.exercise-title`) are still defined in `src/styles/globals.css` and remain in use
- No CSS was removed; the classes are shared with live workout pages and continue to be used

### 4. Import Verification
- Confirmed via `grep` that no imports of `WorkoutCard` exist anywhere in the `src/` directory

## Checks Run

### Lint (`npm run lint`)
- ✅ PASS: 0 errors, 6 pre-existing warnings (unchanged)

### Tests (`npm test`)
- ✅ PASS: 485 tests across 78 test suites completed successfully
- Duration: ~5 seconds

### Build (`npm run build`)
- ✅ PASS: `next build` completed successfully
- 43 static pages generated
- No build errors

### Visual Verification (`node scripts/visual-check.mjs`)
- ✅ PASS: Hermetic visual check completed successfully
- Generated 37 screenshots covering all PAGES and FLOWS routes
- Confirmed all pages render correctly including:
  - workouts.png (workout list page)
  - workout-detail.png (single workout detail page)
  - exercise.png (exercise progress page)
- No rendering issues detected from WorkoutCard deletion

## Observations
- The deletion of the unused WorkoutCard component has no impact on the live application
- All CSS classes that were in WorkoutCard remain in use elsewhere and are still properly styled via globals.css
- The workouts pages render correctly with full styling, indicating that the inline workout rendering (which doesn't use WorkoutCard) is working properly
- Visual verification shows no missing elements, broken layouts, or styling issues

## Result
✅ Task complete: WorkoutCard component successfully removed, documentation updated, all verification checks passing.
