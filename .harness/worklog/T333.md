# T333 — 2026-07-09

Landed the "Compact tint" medal-pill style as the real `.pr-badge`, and deleted the T331 dev
comparison route now that the owner has decided (T332).

## Do

- `src/styles/globals.css`: resized `.pr-badge` to match `.pr-variant-compact`'s dimensions —
  `padding: 0.1rem 0.45rem`, `font-size: 0.7rem`, `gap: 0.2rem` (was inheriting
  `.collection-badge`'s `0.25rem 0.75rem` / `0.75rem` / `0.3rem`). Left `background-color`/`border`
  untouched — already identical to the compact-tint treatment.
- Deleted `src/pages/dev/pr-badge-variants.js`.
- Removed the dead `/* ===== T331: dev-only PrBadge restyle comparison ===== */` block and its
  `.pr-variant-compact` / `.pr-variant-dot` / `.pr-variant-dot-mark` / `.pr-variant-outline` rules.
- `src/components/Badge.js`'s `PrBadge` was untouched, as specced — pure CSS + deletion diff.

## Checks

- `grep -rn "pr-variant-" src/` → no matches.
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated).
- `npm test` — 73 suites / 391 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=workout node scripts/visual-check.mjs` — all 8 captures passed. Looked at
  `workout-detail.png`: the "🥉 Volume" / "🥉 Weight" / "🥇 1RM" pills render visibly smaller/tighter
  (reduced padding + font) than before, medal emoji/tint background/border all intact, no layout
  overlap or clipping. Also spot-checked `workouts.png` — programme stats block unaffected.

Done.
