# T333 — land Compact tint PR badge, delete dev comparison page

2026-07-09

## What I did
- Resized `.pr-badge` in `src/styles/globals.css` to match the "Compact tint" variant chosen in
  T332: `padding: 0.1rem 0.45rem`, `font-size: 0.7rem`, `gap: 0.2rem` (was inheriting
  `.collection-badge`'s larger padding/font, and `gap: 0.3rem`). Left `background-color`/`border`/
  `border-radius` untouched — they already matched the compact variant.
- Deleted `src/pages/dev/pr-badge-variants.js` (its comparison purpose is resolved).
- Removed the dead `.pr-variant-compact` / `.pr-variant-dot` / `.pr-variant-dot-mark` /
  `.pr-variant-outline` CSS block (and its T331 comment header) from `globals.css`.
- Made no changes to `src/components/Badge.js` — `PrBadge` already renders the medal + label with
  `className="pr-badge"`; this was a pure CSS resize. No label-text changes (already done in T322).

## Checks
- `npm run lint` — 0 errors (6 pre-existing warnings, all in files untouched here).
- `npm test` — 73 suites / 391 tests passed.
- `npm run build` — succeeded; `/dev/pr-badge-variants` no longer in the route list.
- `grep -rn "pr-variant-" src/` — no matches.

## Visual verification
`VISUAL_CHECK_ONLY=workout node scripts/visual-check.mjs` — all 8 captures passed. Looked at:
- `workouts.png` — programme list PR pills ("🏅 Volume") render visibly smaller/tighter than
  before, tint background + border intact, no layout regression across the list or split/period
  filter states.
- `workout-detail.png` — per-exercise set PR pills ("🏅 Volume", "🏅 Weight", "🏅 1RM") same compact
  sizing, sit cleanly inline with set rows, no clipping/overlap.

## Diff scope
CSS-only (`src/styles/globals.css`) plus the page deletion, as required by the spec. No doc
changes needed (no convention/feature/data-model change).
