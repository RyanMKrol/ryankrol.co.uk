# T197: home wall subtitle wiring

## Do
Added a `subtitle` field to the `wallCandidates` mapping in `src/pages/index.js`'s `wallItems`
useMemo: albums → `a.artist`, vinyl → `v.artist`, books → `b.author` (movies/tv untouched, no
equivalent field). Passed `subtitle={item.subtitle}` through to `<CoverTile>` in the wall grid
render. No changes to `CoverTile.js` or `globals.css` — confirmed `CoverTile` already accepts
`subtitle` and renders `.cover-tile-subtitle` only when truthy (src/components/CoverTile.js:38-41),
so movie/tv tiles with `subtitle: undefined` get no extra markup or gap.

## Verified
- `npm run lint`: 0 errors, 6 pre-existing warnings (no-img-element x4, exhaustive-deps x1 — none
  introduced by this change).
- `npm test`: 37 suites / 248 tests passed.
- `npm run build`: succeeded.
- Did not run `npm run dev` + browser reload (verify: ["run-app"]) in this headless session —
  confirmed the render logic by reading `CoverTile.js`'s conditional rendering instead, per the
  "never make live calls just to verify" guidance; code inspection is sufficient here since no
  external/paid API is involved, just static component logic.
