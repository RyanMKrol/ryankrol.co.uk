# T076 — Slider input for the perfume rating on the add form

## What changed
- `src/pages/reviews/perfumes/add.js`: replaced the `type="number"` rating input with a
  `type="range"` slider (min 0, max 10, step 1), keeping the same `formData.rating` /
  `handleRatingChange` wiring. The label now shows `Rating (0-10): {formData.rating}` so the
  current value is visibly displayed as the slider is dragged (range inputs have no built-in
  value label).

## Verification
- `npm run lint` — 0 errors (4 pre-existing warnings, unrelated to this file).
- `npm test` — 141 tests passed across 14 suites.
- `npm run build` — succeeded; `/reviews/perfumes/add` (and `/v1`, `/v2`, `/v3` variants) compiled.
- Read-only verification (no dev server / live API calls): the input element's `type`, `min`,
  `max`, `step`, `value`, and `onChange` wiring are unchanged apart from the widget type, so the
  existing `handleRatingChange` clamp-to-number logic and the unchanged `handleSubmit` POST body
  continue to send an integer `rating` 0-10, validated server-side by `validatePerfumeRating`
  (untouched, out of scope).

## Scope
Only `src/pages/reviews/perfumes/add.js` was modified, plus this worklog.
