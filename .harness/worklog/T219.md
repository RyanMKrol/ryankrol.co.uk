# T219 — Add review date to books and albums review-list cards

## Do

- `SpineCoverCard` (books): extended `metaParts` with
  `item.editedDate ? \`Updated ${formatReviewDate(item.editedDate)}\` : (item.date ? formatReviewDate(item.date) : null)`,
  appended after `firstPublishedYear`. Existing `.filter(Boolean).join(' · ')` handles presentation
  — no new element.
- `SquareCoverCard` (albums): added `{item.date && <p className="square-cover-date">{formatReviewDate(item.date)}</p>}`
  below the existing `square-cover-artist` line.
- `src/styles/globals.css`: added a `.square-cover-date` rule mirroring `.square-cover-artist`
  (`margin: 0; color: var(--color-ink-mute); font-size: 0.9rem;`).
- `formatReviewDate` (from `src/lib/dateFormat.js`, already imported in this file) is the only
  thing ever rendered for a date — no raw `item.date`/`item.editedDate` value is rendered at any
  point in this diff.

## Verify

- `npm run lint`: 0 errors, 6 pre-existing warnings (unchanged from before this task).
- `npm test`: 43 suites / 287 tests passed.
- `npm run build`: succeeded, all pages generated.
- Manual: ran `npm run dev`, requested `/reviews/books` and `/reviews/albums` — both compiled and
  returned `200` with no runtime errors in the dev server log. Reviews pages fetch their data
  client-side from DynamoDB via `useEffect` (per this repo's architecture), so the actual rendered
  card markup with real review dates can't be inspected via a headless `curl` of the initial HTML
  without making a live DynamoDB call, which is out of bounds for this run. Verified correctness by
  reading the rendered JSX/CSS directly instead of a live DynamoDB round trip.
