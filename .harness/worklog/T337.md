# T337 — 2026-07-08

## What
Replaced the bare `new Date(session.workout_date).toLocaleDateString()` call in the Recent
Sessions history table on `src/pages/exercises/[exerciseName].js` with
`formatEnglishDate(session.workout_date)` (imported from `../../lib/dateFormat`), matching the
same fix already applied to the workout detail page (T336).

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated `<img>`/exhaustive-deps).
- `npm test` — 74 suites / 403 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=exercise node scripts/visual-check.mjs` — PASS. Opened
  `scripts/visual-out/exercise.png` (manifest name `exercise`) and confirmed the Recent Sessions
  table renders dates as English long-form ("10 January 2026", "10 February 2026", etc.) instead
  of numeric slash-separated form.

## Scope
Only touched `src/pages/exercises/[exerciseName].js`. Left `src/components/WorkoutCard.js`
untouched per the task spec (confirmed dead/unimported, explicitly out of scope).

## Remaining
None — task complete.
