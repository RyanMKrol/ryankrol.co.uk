# T380 — 2026-07-13

## What
Moved the password-gated Top-of-Mind editor page from `src/pages/top-of-mind/edit.js`
(`/top-of-mind/edit`) to `src/pages/tom.js` (`/tom`), a clean move (no redirect, per the
task's default decision — nothing links to the old route).

- Created `src/pages/tom.js` as a byte-for-byte copy of the old page component, with
  relative imports adjusted one directory shallower (`../../components/...` →
  `../components/...`, `../../lib/...` → `../lib/...`).
- Deleted `src/pages/top-of-mind/edit.js`, leaving `src/pages/top-of-mind/` empty (dir
  removed as it now has no pages).
- Did not touch `/api/top-of-mind/*` routes or the `TopOfMind` DynamoDB table.
- Added a file-map row to `CLAUDE.md` for `src/pages/tom.js`.

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 78 suites / 477 tests passed.
- `npm run build` — succeeded; build output confirms `/tom` is now a static page and
  `/top-of-mind/edit` no longer appears in the route list.

## Remaining
Nothing outstanding for this task.
