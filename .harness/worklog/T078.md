
---
A prior attempt correctly stopped rather than faking success: `src/components/BookSearch.js`
imports `bookCoverUrl` from `../lib/openlibrary`, which was outside this task's original scope
(`src/lib/openlibrary.{js,test.js}`, `CLAUDE.md`, `README.md` only). This was a genuine
scope-coverage gap, not a false alarm — investigation also found a second, previously-unnoticed
importer: `src/pages/index.js`.

**Fixed:** `src/components/BookSearch.js` and `src/pages/index.js` added to this task's `scope`,
and the spec (`.harness/tasks/T078.md`) updated with explicit instructions for both files —
replace their `bookCoverUrl(coverId)` fallback with a direct `coverUrl` read, since T080's worklog
confirms production `BookRatingsV3` had zero `source === 'openlibrary'` records at migration time
and T077 already removed the ability to create new ones, making the `coverId` fallback dead code.
The spec's grep-based "Done when" check was also corrected to exclude
`src/scripts/migrateOpenLibraryBooksToGoogleBooks.js` (+ its test), which legitimately keeps
`'openlibrary'` as a historical-data marker string per T079/T080 and was never meant to be deleted
by this task. Retryable now.
