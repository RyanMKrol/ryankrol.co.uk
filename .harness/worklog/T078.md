
---
A prior attempt correctly stopped rather than faking success: `src/components/BookSearch.js`
imports `bookCoverUrl` from `../lib/openlibrary`, which was outside this task's original scope
(`src/lib/openlibrary.{js,test.js}`, `CLAUDE.md`, `README.md` only). This was a genuine
scope-coverage gap, not a false alarm — investigation also found a second, previously-unnoticed
importer: `src/pages/index.js`.

**Fixed:** `src/components/BookSearch.js` and `src/pages/index.js` added to this task's `scope`,
and the spec (`.harness/tasks/T078.md`) updated with explicit instructions for both files —
replace their `bookCoverUrl(coverId)` fallback with a direct `coverUrl` read, since T080's worklog
confirms production `BookRatingsV3` had zero `source === 'openlibrary'` records at migration time
and T077 already removed the ability to create new ones, making the `coverId` fallback dead code.
The spec's grep-based "Done when" check was also corrected to exclude
`src/scripts/migrateOpenLibraryBooksToGoogleBooks.js` (+ its test), which legitimately keeps
`'openlibrary'` as a historical-data marker string per T079/T080 and was never meant to be deleted
by this task. Retryable now.

---
**Built directly, interactively, by owner request — not via `loop.sh`.**

- `src/components/BookSearch.js`: dropped the `bookCoverUrl` import and the `coverId` fallback in
  `getCoverSrc` — now just `r.coverUrl || null`.
- `src/pages/index.js`: dropped the `bookCoverUrl` import and the `coverId` fallback in the
  collection-wall book mapping — now just `b.coverUrl || null`.
- Deleted `src/lib/openlibrary.js` and `src/lib/openlibrary.test.js`.
- `CLAUDE.md`: removed the `src/lib/openlibrary.js` file-map row; `BookRatingsV4` data-model row no
  longer lists `olid`/`coverId`/`'openlibrary'` as currently-written fields, with a one-line note
  that historical rows may still carry them (T077/T079/T080 cross-referenced).
- `README.md`: `BookRatingsV3` schema block's `source` comment narrowed to `'googlebooks'` only,
  `olid`/`coverId` example fields removed, same historical-rows note added.
- Also corrected the spec's own "no longer mention Open Library anywhere" Done-when bullet, which
  contradicted its own Do section's instruction to keep a historical-rows caveat — narrowed to "no
  longer describe Open Library as a live/current data source."

**Verification:**
- `grep -rn "openlibrary" src/ -i` (excluding `migrateOpenLibraryBooksToGoogleBooks.js`/`.test.js`)
  → zero matches.
- `npm test` → 73/73 suites, 391/391 tests passed.
- `npm run lint` → 0 errors (6 pre-existing tolerated warnings, none new).
- `npm run build` → succeeded.
- Hermetic visual check (`VISUAL_CHECK_ONLY=home node scripts/visual-check.mjs`) → both `home` and
  `home-skim-shelf` captures passed; the `home` screenshot was inspected directly — collection-wall
  book tiles render cleanly with no blank/broken tiles from the removed `coverId` fallback path.

`done T078` — no `outcomes.jsonl` row added (built directly in an interactive session, not through
the loop's escalation ladder, so there's no real rung/model data to record).
