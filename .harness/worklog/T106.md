# T106 — batch mark-done.sh / mark-reviewed.sh + dashboard bulk endpoints into one commit per action

## Finding: work was already complete on `main`

Starting cold on this task, `git log` showed `main` HEAD already at commit `69a9e173` —
"T106: batch mark-done.sh / mark-reviewed.sh + dashboard bulk endpoints into one commit per
action" — with the exact scope this task specifies (`.harness/mark-done.sh`,
`.harness/mark-reviewed.sh`, `.harness/dashboard/server.js`, `.harness/README.md`, plus the new
test file `.harness/mark-done-bulk.test.sh`). `TASKS.json` still had `status: "pending"` for T106.
This matches the documented harness pathology in `.harness/CLAUDE.md` ("A manual loop interrupt
can orphan a task whose work already merged") — the commit landed on `main` in a prior run but the
status update never made it back to `TASKS.json`, likely due to an interrupt between push and
`mark_done`.

Since the tree was clean and `main` already contained the implementation, I did NOT re-implement
anything. Instead I verified the existing implementation actually satisfies the spec end to end
before treating it as done:

## Verification performed

1. **Read both scripts in full.** `mark-done.sh` / `mark-reviewed.sh` both now accept
   `[--undo] <TNNN> [TNNN...]`, validate every id fail-fast/atomically before any write (id regex
   → exists in TASKS.json → for mark-done.sh only, `gate == needs-human`), write the whole id set
   into the overlay JSON in a single `jq` pass, then do exactly one `git add` / one
   `diff --cached --quiet` no-op check / one `git commit` / one 3-attempt push-retry loop — matching
   the "single-id usage byte-identical" and "N ids → one commit" requirements.
2. **Read `.harness/dashboard/server.js`** around the bulk handlers (lines ~296-308): confirmed
   `runScript(script, ids)` is called exactly once with the full id array, then
   `ids.map((id) => ({ id, ...outcome }))` builds the response — no `for`/`.forEach` loop over
   `ids` around `runScript`. `grep -n "runScript(" .harness/dashboard/server.js` shows a single
   call site per endpoint, confirming the "Done when" check literally.
3. **Ran the hermetic test directly:** `bash .harness/mark-done-bulk.test.sh` → all 17 assertions
   passed (`ALL PASS`, exit 0). It builds its fixtures in `mktemp -d` + `git init` (never this
   checkout's history), covers: multi-id single-commit for both scripts, single-id/--undo
   commit-message back-compat, multi-id --undo, fail-fast atomic rejection of a nonexistent id and
   of a non-`needs-human` id (asserting zero new commits AND byte-unchanged overlay in both cases),
   and a static grep-based check that the dashboard handler has no per-id loop.
4. **Ran the full local DoD suite:**
   - `npm run lint` → 0 errors, 3 pre-existing warnings (unrelated files: `dev/cache.js`
     exhaustive-deps, two `<img>` LCP warnings in review edit pages) — no new warnings introduced.
   - `npm test` → 22 suites / 169 tests, all passed.
   - `npm run build` → `next build` succeeded, all 32 pages generated.
5. **Confirmed docs are in lockstep:** `.harness/README.md`'s `mark-done.sh` / `mark-reviewed.sh`
   rows already describe multi-id/batch usage in a single commit.

## Outcome

No code changes were needed — the prior run's commit fully and correctly satisfies every "Done
when" criterion. This worklog is the only new artifact, closing the loop so `TASKS.json` can be
reconciled to `status: done` for T106.
