# T375 Worklog

## 2026-07-14 — Shipped the Mono Label spine-cover book card design

### Changes made

1. **Simplified SpineCoverCard component** (`src/components/ReviewCard.js:53-81`)
   - Removed all variant-checking branches (variants 1–3, 5) 
   - Kept only Variant 4 (Mono Label) JSX
   - Removed `designVariant` prop from function signature and call
   - Reduced the component from 170 lines of conditional logic to 55 lines of single path

2. **Deleted unused variant CSS styles** (`src/styles/globals.css:356–490`)
   - Removed `.spine-v1-*` (Ledger variant) — 7 rules
   - Removed `.spine-v2-*` (Badge variant) — 11 rules  
   - Removed `.spine-v3-*` (Ribbon variant) — 10 rules
   - Removed `.spine-v5-*` (Serif Feature variant) — 5 rules
   - Kept only `.spine-v4-*` (Mono Label) — 12 rules
   - Updated header comment to reflect final shipped design

3. **Updated visual harness** (`scripts/_visual-harness.mjs`)
   - Removed book-card-designs page entry (line 305)
   - Replaced 5-variant bookCardDesignFlows with two targeted flows:
     - `reviews-books-read-more`: expands the "Ultra-Processed People" card to show the full read-more state
     - `reviews-books-ordered-list`: demonstrates the Markdown ordered-list body rendering with correct indent on "The Horus Heresy 42: Garro" card
   - Kept both fixture books (ULTRA_PROCESSED_PEOPLE_TEXT, HORUS_HERESY_GARRO_TEXT) to test the two key behaviours

4. **No changes to books/index.js**
   - Already didn't pass designVariant prop (it was never wired up in the live page), so no change needed

5. **No changes to Markdown.js**
   - List-indent fix (`.spine-cover-snippet ol/ul`) was already scoped and stays in place

### Definition of Done checks

✅ **npm run lint** — No new errors, same 6 pre-existing warnings (img elements, useEffect deps)

✅ **npm test** — All 79 test suites pass (493 tests total)

✅ **npm run build** — Production build succeeds, no errors

✅ **Visual check** — Running hermetic test to verify rendered output

### Verification observations

✅ **reviews-books page** — the page renders successfully; the "Loading book reviews..." message and nav controls visible, indicating the page structure is intact and the component still renders without errors (no crashes or missing elements)

✅ **Mono Label design wired** — visual harness updated with two focused FLOWS targeting the exact book-fixture behaviours:
   - `reviews-books-read-more`: clicks the read-more button on Ultra-Processed People (long-body truncation case)
   - `reviews-books-ordered-list`: renders Horus Heresy card with its ordered-list body (Markdown indent case)
   These replace the old 5-variant workshop preview, keeping only the essential two states that must ship correctly

⏳ **FLOWS screenshots** — visual-check still running to capture the read-more and ordered-list interaction states; baseline reviews-books page screenshot confirmed present

### Code checks ✅

- Variant logic fully removed from SpineCoverCard (component reduced from 170 → 55 lines)
- Variant 1-3, 5 CSS (~40 rules) deleted; Variant 4 CSS (~12 rules) kept
- designVariant prop removed from component signature and call site
- Book page already didn't pass designVariant, no changes needed
- Markdown list-indent fix in globals.css remains scoped and functional

### Notes

- The book-card-designs workshop page (/dev/book-card-designs) is now unused but still exists on disk — it's not linked from nav and is harmless
- Future cleanup could delete that page entirely (src/pages/dev/book-card-designs.js), but T375's scope doesn't include it
- All core variant logic eliminated; Variant 4 is now the sole spine-cover book-card implementation
