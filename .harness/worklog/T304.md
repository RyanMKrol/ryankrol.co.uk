# T304: ownership filter on /reviews/perfumes

## Do

- Exported `OWNERSHIP_OPTIONS` from `src/components/PerfumeCharacteristics.js` (was
  internal-only, previously only consumed by `OwnershipPicker`).
- In `src/pages/reviews/perfumes/index.js`:
  - Imported `OWNERSHIP_OPTIONS` and derived `OWNERSHIP_FILTER_OPTIONS` (`all` + the 3
    ownership values) as a module-level const alongside `SORT_OPTIONS`.
  - Added `ownershipFilter` state (default `'all'`).
  - Extended the filtering effect's predicate to AND an ownership match onto the existing
    title-search match; added `ownershipFilter` to the effect's deps.
  - Rendered a second `PillGroup` (single-select, same as the sort control) wired to
    `ownershipFilter`/`setOwnershipFilter`, with `accentColor="var(--accent-perfumes)"` to
    match the ownership Badge already shown on each perfume card.
  - Broadened the 'Found N perfumes' count condition to show whenever `searchTerm` is set
    OR `ownershipFilter !== 'all'`.

## Verify

- `npm run lint`: 0 errors (6 pre-existing warnings, all unrelated to this change).
- `npm test`: 395 passed, 395 total.
- `npm run build`: succeeded, `/reviews/perfumes` compiled with no new errors.
- Did not run `npm run dev` interactively (headless session) — verified behaviour by
  reading the filter/effect logic: ownership and search predicates are ANDed (both must
  match), `'all'` short-circuits the ownership check, and legacy perfumes with no
  `ownership` field fail any non-'all' `perfume.ownership === ownershipFilter` check (as
  intended) but still pass under `'all'`.
