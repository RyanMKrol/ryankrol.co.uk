# T206 — fix poster-banner aspect-ratio mismatch

## Do
Changed `.poster-banner` `aspect-ratio` in `src/styles/globals.css` from `16 / 10` (1.6:1) to
`18 / 5` (3.6:1), matching the measured proportions from `design/renders/2a.png`. No other
property on `.poster-banner` was touched. This affects both `/reviews/movies` and `/reviews/tv`
via the shared `poster-banner` style variant — no page files needed editing.

## Verify
- `npm run lint` — 0 errors, only pre-existing warnings (img/exhaustive-deps).
- `npm test` — 267/267 tests passed.
- `npm run build` — succeeded.
- Manual browser check of `/reviews/movies` and `/reviews/tv` was not performed (headless
  session, no browser available) — the change is CSS-only (single property value), verified by
  reading the rule and confirming no other selectors reference or depend on this aspect ratio.
