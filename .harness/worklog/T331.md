# T331 — 2026-07-08

## What I did
Added a localhost-only dev comparison page `src/pages/dev/pr-badge-variants.js` (same
localhost-gating pattern as `src/pages/dev/cache.js`, no password gate — read-only, static
sample data, no DynamoDB) rendering three self-contained restyle options for the PR pill,
each showing all three axes (weight / est. 1RM / set volume) with realistic labels:

- **Compact tint** — the existing soft-tint + currentColor-border pill, shrunk padding/font,
  medal emoji kept, "PR" suffix dropped ("Weight" not "weight PR").
- **Dot indicator** — medal replaced by a small colored dot, no border, smaller font, label only.
- **Outline chip** — Badge's outline treatment instead of soft tint, smaller pill, no emoji,
  label only.

New CSS added to `src/styles/globals.css`: `.pr-variant-compact`, `.pr-variant-dot` /
`.pr-variant-dot-mark`, `.pr-variant-outline` — all new classes, scoped to the new dev page only.
Did not touch `src/components/Badge.js`, `src/pages/workouts/index.js`, or
`src/pages/workouts/[id].js`.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this change).
- `npm test` — 403/403 passing.
- `npm run build` — succeeds; `/dev/pr-badge-variants` built as a static page.
- `VISUAL_CHECK_ONLY=workouts node scripts/visual-check.mjs` — all 7 captures pass.
- `VISUAL_CHECK_ONLY=workout-detail node scripts/visual-check.mjs` — capture passes.
- Visually inspected `workout-detail.png`: production PR pills unchanged — still render as
  "🏅 weight PR" / "🏅 1RM PR" / "🏅 volume PR" in the existing soft-tint style, confirming
  production markup/output is pixel-identical to before this task.
- Ran `next dev` locally and screenshotted `/dev/pr-badge-variants` directly (not part of the
  hermetic harness's `PAGES` list, since it's a one-off dev comparison tool, not a page that
  needs ongoing fixture coverage): confirmed all three variant groups render side by side, each
  clearly labelled, each showing three pills (Weight / Est. 1RM / Set volume), all visibly
  smaller than the production `.pr-badge` pill, none using a "PR" suffix.

## Remaining
None — task complete.
