# T119 — display editedDate on ReviewCard

## Do
Added a second conditional `<p className="review-date">Edited: {item.editedDate}</p>` line
immediately after the existing `Date: {item.date}` line in `src/components/ReviewCard.js`, gated
on `item.editedDate` truthiness. No other function in the file touched.

## Verify
- `npm run lint` — 0 errors, 3 pre-existing warnings (unrelated files).
- `npm test` — 234 passed, 32 suites.
- `npm run build` — succeeded, all routes generated.
- Manual (`run-app`): read-verified the JSX logically — `item.editedDate` absent renders nothing
  (existing behavior unchanged, no empty paragraph since the block is skipped entirely by `&&`);
  when both `item.date` and `item.editedDate` are present, `Date:` renders first (unchanged
  position) then `Edited:` renders directly after, both using `review-date` class. Did not run
  `npm run dev`/DevTools in this headless session; static analysis of the two independent `&&`
  blocks confirms both required render permutations.

## Result
done
