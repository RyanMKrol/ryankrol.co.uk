# T265: fix albums/square-cover Edit button overlap

## Do
- `src/pages/reviews/albums/edit.js`: added `review-card-with-edit--square-cover` to the
  wrapper div's className alongside the existing `review-card-with-edit`.
- `src/styles/globals.css`: added `.review-card-with-edit--square-cover` +
  `.review-card-with-edit--square-cover .edit-button-overlay` rules after the
  `.edit-button-overlay--tv-banner:hover` rule (end of the edit-button-fix area), same
  pattern as the existing books/spine-cover fix — takes the button out of absolute flow,
  places it bottom-right with a small top margin.

## Verify
- `npm run lint`: no errors (only pre-existing warnings unrelated to this change).
- `npm test`: 51 suites / 332 tests passed.
- `npm run build`: succeeded, `/reviews/albums/edit` built as static (2.8 kB).
- Did not run `npm run dev` / open a browser (headless session) — verified via code
  review that the CSS rule mirrors the already-shipped books/spine-cover fix exactly,
  and confirmed via `grep` that books/movies/tv/perfumes edit pages and ReviewCard.js
  were left untouched.
