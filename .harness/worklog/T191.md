# T191 — Home vinyl shelf: link, random sample, refresh

## Do

- `src/pages/index.js`: reused the existing `pickRandomSample` Fisher-Yates helper (already
  present in this file, functionally identical to the spec's `sampleRandomVinyl`) instead of
  adding a duplicate.
  - Added `shelfItems` state, seeded via `pickRandomSample(vinylData || [], 5)` in `fetchAll`
    alongside `setVinyl`.
  - Removed the deterministic `vinyl.slice(0, 4)` derived value.
  - Added `handleShelfRefresh`, which re-samples 5 items from the already-fetched `vinyl` state
    (no network call).
  - Shelf panel header restructured: `.home-shelf-panel-title` is now a `Link` to `/vinyl`
    (`On the shelf · N records`), paired with a `.home-shelf-refresh` button
    ("&#8635; Skim the shelf", `type="button"`, explicit `aria-label` framing it as a browsing
    affordance) inside a new `.home-shelf-panel-header` flex row.
- `src/styles/globals.css`: added `.home-shelf-panel-header` (flex row), overrode
  `.home-shelf-panel-title.home-shelf-panel-link` (drop block margin, no underline until hover),
  and added `.home-shelf-refresh` (ghost pill button matching site conventions). Left
  `.home-shelf-item` / `.home-shelf-panel` untouched.

## Verified

- `npm run lint` — 0 errors, pre-existing warnings only (no new warnings).
- `npm test` — 245/245 passing.
- `npm run build` — succeeds, `/` still statically prerendered.
- Read-through confirms: record count text still reads `vinyl.length` (unaffected by which 5
  render), refresh button re-samples from client-side `vinyl` state only (no `/api/vinyl`
  re-fetch), and no other home sections (stats, wall, latest takes, gym panel) were touched.
