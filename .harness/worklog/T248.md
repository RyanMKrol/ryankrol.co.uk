# T248: fix reviews/books/edit masonry layout to match /reviews/books

## What changed
`src/pages/reviews/books/edit.js` previously wrapped each `ReviewCard` in a plain
`<div className="reviews-wrapper">`, producing full-width cards instead of the packed
masonry layout used by the real list page. Replaced it with `<MasonryColumns>` +
`useResponsiveColumnCount(2, 700)`, matching the pattern already in
`src/pages/reviews/books/index.js` (`className="spine-cover-list"`,
`columnClassName="spine-cover-list-col"`). The existing `review-card-with-edit` wrapper
and `edit-button-overlay` Link markup are unchanged — only the outer layout container
changed.

## Verify
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this file).
- `npm test` — 308 passed, 48 suites.
- `npm run build` — succeeded; `/reviews/books/edit` compiles to a static page
  (1.08 kB, matches the size of the already-fixed movies/tv/albums edit pages).
- Did not run `npm run dev` / open a browser (headless run) — verified via code
  reading + build output that the JSX now mirrors the already-converted
  `books/index.js` masonry usage exactly.
