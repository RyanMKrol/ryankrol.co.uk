## Decision

Owner reviewed the 3 variants built by T331 at `/dev/pr-badge-variants` and chose **"Compact tint"** (soft `color-mix` tint background, `currentColor` border, medal emoji kept, shrunk padding/font vs. the current `PrBadge`) — decision captured in the ideas inbox (`.harness/tracking/IDEAS.jsonl` idea #3) and consumed here during a `/implementation-harness-convert-ideas` sweep.

Recorded for T333 (the hardcode task) to consume: T333's spec (`.harness/tasks/T333.md`) has been rewritten with the exact `.pr-badge` CSS resize needed to match `.pr-variant-compact`'s dimensions (`padding: 0.1rem 0.45rem; font-size: 0.7rem; gap: 0.2rem;`), and its `scope` narrowed to drop the two workout pages (their label-string work was already shipped separately in T322, commit `15ab725`) — T333 is now CSS-only plus deleting the dev comparison page.

The capitalization sub-decision this gate originally also covered (sentence-case "Weight"/"Volume", "1RM" unchanged) was already resolved and consumed by T322, per this task's own spec note — nothing further needed for that here.

Built directly, interactively — not via `loop.sh` (this is a `needs-human` gate; the decision itself is the deliverable, nothing to build/test/lint).
