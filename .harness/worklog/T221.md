# T221: fix legacy styleVariant={2} on review edit-list pages

## What changed
- `movies/edit.js`, `tv/edit.js`: `styleVariant={2}` → `styleVariant="poster-banner"` (matches public list page).
- `books/edit.js`: `styleVariant={2}` → `styleVariant="spine-cover"`.
- `albums/edit.js`: `styleVariant={2}` → `styleVariant="square-cover"`.
- `perfumes/edit.js`: swapped `ReviewCard` import/usage for `Variant6Hybrid` (same component the public
  perfumes list page renders directly), keeping the surrounding `review-card-with-edit` wrapper and
  `edit-button-overlay` Link unchanged.

## Verification
- `grep -rn "styleVariant={2}" src/pages/reviews/{movies,tv,books,albums,perfumes}/edit.js` → no matches.
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files: CoverTile, dev/cache, index,
  listening, movies/edit/[id], tv/edit/[id]).
- `npm test` — 43 suites / 287 tests passed.
- `npm run build` — succeeded, all 5 edit pages compiled with expected bundle sizes.
- Manual browser check of `/reviews/*/edit` pages was not performed (no dev server driven in this
  headless run); confidence comes from the components being drop-in equivalents of what each type's
  already-verified public list page renders (`ReviewCard` with the same named variant, or
  `Variant6Hybrid` directly) plus a successful production build.
