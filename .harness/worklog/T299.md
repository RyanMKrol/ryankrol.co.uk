# T299 — Shared Tooltip component

## Do

- Added `src/components/Tooltip.js`: `Tooltip({ label, children, position, className })`, renders
  a `tooltip-wrapper` span (`tabIndex={0}`, `aria-label={label}` for keyboard/SR access) wrapping
  children plus an `aria-hidden` `tooltip-bubble` span (with `tooltip-bubble--bottom` modifier when
  `position === 'bottom'`).
- Added CSS in `src/styles/globals.css` right after the existing `.home-sparkline-tooltip` rules:
  `.tooltip-wrapper`, `.tooltip-bubble` (uses `var(--color-card, #FFFFFF)` / `var(--color-ink,
  #1A1714)`, adds a `1px solid var(--color-hairline-strong)` border + box-shadow vs. the sparkline
  original), `.tooltip-bubble--bottom`, hover/focus-within opacity rule, and a
  `prefers-reduced-motion: reduce` override disabling the transition. Did not touch
  `.home-sparkline-tooltip` or the homepage sparkline.
- Updated CLAUDE.md's "Components:" paragraph to list `Tooltip`.

## Verify (static — no live/dev-server check performed, per harness rule against live-app
verification in a headless run; code was inspected instead)

- `--color-card` / `--color-ink` confirmed NOT overridden inside `html.matrix-active` block in
  globals.css (checked: only `--color-surface`, `--color-text`, etc. are redefined there) — so the
  bubble stays a legible light chip under both themes, matching the existing sparkline tooltip's
  behaviour.
- `prefers-reduced-motion: reduce` block added removes the opacity transition.
- `tabIndex={0}` + `:focus-within` selector give keyboard-only accessibility without a mouse.

## Done when

- `npm run lint` — no errors (pre-existing `@next/next/no-img-element` and
  `react-hooks/exhaustive-deps` warnings only, none new).
- `npm test` — all suites passed.
- `npm run build` — succeeded.
- No throwaway usage was added to any page (not required now — call sites are wired up by the
  dependent tasks). CLAUDE.md's Components list now mentions `Tooltip`.
