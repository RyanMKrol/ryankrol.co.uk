# T124: prefer editedDate over date in movies date-sort

## Do
Updated both date-sort branches (`date-desc` and default 'most recent first') in
`src/pages/reviews/movies/index.js` to compute comparison dates from
`(item.editedDate || item.date)` instead of `item.date` alone, matching the already-landed
`editedDate` field written by `src/pages/api/reviews/movies/update.js`.

## Verify
- `npm run lint` — 0 errors, 3 pre-existing warnings (unrelated files), no new issues.
- `npm test` — 34 suites / 238 tests passed.
- `npm run build` — succeeded, all routes generated.
- Manually reasoned through the two Done-when scenarios: a review with only `date` falls
  back unchanged (no regression); a review with `date: '01-01-2020'` and a later `editedDate`
  now sorts by `editedDate`, surfacing as most recent under the default sort.

No live/paid API calls made. No other files touched.
