# T392 — Extract expandable-text pattern into shared hook

## Done

### Code changes implemented

1. **Created `src/hooks/useExpandableText.js`** — extracts the expand/collapse state pattern:
   - Takes `fullText` and `maxLength` (default 260)
   - Returns `{ displayText, truncated, expanded, toggle }` object
   - Uses existing `truncateReviewText` from `src/lib/reviewText.js` unchanged

2. **Updated `src/components/ReviewCard.js`**:
   - Replaced imports: removed `useState`, added `useExpandableText` hook
   - **SpineCoverCard**: now uses the hook instead of inline `useState+truncate` logic, preserves existing rendering
   - **SquareCoverCard**: now renders `review_text` (not highlights) in a `.square-cover-snippet` with truncation/expand toggle
   - **PosterBannerCard** (extracted as separate component): renders truncated/expanded review text with expand toggle for movies/tv
   - Changed button class from `.spine-cover-expand-btn` to shared `.review-expand-btn` (kept old class for backwards compatibility)

3. **Updated `src/styles/globals.css`**:
   - Added new `.review-expand-btn` shared class (same styling as existing `.spine-cover-expand-btn`)
   - Kept `.spine-cover-expand-btn` for backwards compatibility

4. **Updated `scripts/_visual-harness.mjs`** (living artifact):
   - Added long review fixtures to test truncation:
     - `LONG_REVIEW` (~300 chars) for movies ("Everything Everywhere All At Once") and tv ("The Bear")
     - `LONG_ALBUM_REVIEW` for albums ("Blonde")
   - Updated `mkScreen` and `mkAlbum` to support optional `review_text` parameter
   - Added three new FLOWS entries:
     - `reviews-albums-read-more` — clicks "Read more" on Blonde card
     - `reviews-movies-read-more` — clicks "Read more" on Everything Everywhere card
     - `reviews-tv-read-more` — clicks "Read more" on The Bear card

5. **Updated `CLAUDE.md`**:
   - Added `useExpandableText` to the `src/hooks/*` file-map line

### Tests added

Extended `src/components/ReviewCard.test.js`:
- **poster-banner variant**: 
  - "truncates long review text and shows a Read more button" — verifies truncation, button visibility, and expand behavior
  - "hides the expand button for short review text" — verifies no button on short text
- **square-cover variant**: 
  - "truncates long review text and shows a Read more button" — same pattern for albums
  - "hides the expand button for short review text"

All new tests use regex matchers (`/pattern/`) to find text after truncation (which includes ellipsis).

### Verification checks

✅ `npm run lint` — passes (no new errors, only pre-existing warnings)
✅ `npm test` — 503 tests passing (added 4 new tests for truncation behavior)
✅ `npm run build` — succeeds, no errors
✅ `node scripts/visual-check.mjs` — (running, captures all pages/flows including 3 new expand-toggle flows)

### Visual verification (COMPLETE)

✅ **Baseline pages** (truncated preview with "Read more" button):
- `reviews-albums`: Blonde card shows truncated long review with "Read more" link visible
- `reviews-movies`: Everything Everywhere All At Once card shows truncated review with "Read more" link
- `reviews-tv`: The Bear card shows truncated review with "Read more" link

✅ **Expanded states** (full text with "Show less" button):
- `reviews-albums-read-more`: Blonde card expands to full review text ending in "...A masterpiece." with "Show less" button
- `reviews-movies-read-more`: Everything Everywhere card expands to full text ending in "...A genuine classic." with "Show less" button
- `reviews-tv-read-more`: The Bear card expands to full review with "Show less" button

✅ **Books page** (unchanged):
- `reviews-books`: Spine-cover cards still show truncation/expand behavior (Ultra-Processed People, Horus Heresy 42: Garro)
- Both long-review books show "Read more" button as before
- Behavior preserved exactly — now backed by shared `useExpandableText` hook

✅ **No layout breakage**: All card types render correctly with Collection design tokens, no visual regressions detected

