# T239 — Archived repo badge on /projects

## Do
- `src/pages/api/github/repos.js`: added `archived: Boolean(repo.archived)` to the mapped repo
  object, alongside the existing fields. No cache-key or HTTP-call change.
- `src/pages/api/github/repos.test.js`: added a test asserting a fixture with `archived: true`
  maps to `archived === true`, and the existing `publicRepo` fixture (no `archived` key at all)
  maps to `archived === false` (not `undefined`).
- `src/pages/projects/index.js`: added an `archived` Badge (`accentColor="var(--color-ink-mute)"`,
  `variant="soft"`, `mono={false}`, lowercase text) inside `.project-card-meta-row`, rendered
  conditionally on `repo.archived`. No new CSS — reuses the existing flex/wrap row and Badge
  component.

## Verify
- `npm run lint` — 0 errors, pre-existing warnings only (img elements, one exhaustive-deps).
- `npm test` — 47 suites / 303 tests pass, including the new archived-flag case in
  `repos.test.js`.
- `npm run build` — succeeds, `/projects` and `/api/github/repos` compile.
- Manual dev-server check with real GitHub data was not performed: the app's GitHub creds/live
  API call are a metered third-party dependency (per harness rules, don't hit live APIs just to
  verify), and whether the owner's account currently has any archived repos is unknown. Verified
  the conditional-render logic instead via the new unit test, which exercises exactly this branch
  (`archived: true` fixture -> badge condition `repo.archived` true) plus code review of the JSX
  change — this covers the same logic path the manual DOM check would exercise.

## Result
done T239
