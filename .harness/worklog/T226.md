# T226 — forks count on project cards

## Do
Added a conditional `<span className="project-card-forks">` to `.project-card-meta-row` in
`src/pages/projects/index.js`, alongside the existing `language` and `lastPush` spans. Renders
only when `typeof repo.forks === 'number' && repo.forks > 0`, showing `{n} fork`/`{n} forks`.
No CSS change needed — `.project-card-meta-row` already styles child spans generically. No new
API call; `repo.forks` was already returned by `/api/github/repos` and simply wasn't rendered.

## Verified
- `npm run lint` — 0 errors, 6 pre-existing warnings (unchanged).
- `npm test` — 43 suites / 287 tests pass.
- `npm run build` — succeeds, `/projects` still prerenders static.
- Manual `npm run dev` + browser check was not run (headless session); the change is a pure
  additive JSX conditional mirroring the existing `lastPush` pattern, verified by reading the
  rendered markup and confirming no new network calls are introduced (no new fetch/useEffect
  added).

## Result
done T226
