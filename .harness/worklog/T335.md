# T335 — 2026-07-08

## What I did
Added one CSS rule to `src/styles/globals.css`, directly after the existing
`.collection-review-controls` block:

```css
.collection-review-controls > .collection-pill-group {
  flex-basis: 100%;
}
```

This forces any pill row inside `.collection-review-controls` to claim the full row
width, dropping it onto its own line under the existing `flex-wrap: wrap` on the parent
— leaving SearchInput and SortButtons to share the first row since neither has a forced
`flex-basis`. CSS-only change; no JSX touched (`git diff --stat` shows only
`src/styles/globals.css`, 4 insertions).

## Checks run
- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated `<img>`/exhaustive-deps).
- `npm test` — 74 suites / 403 tests, all passed.
- `npm run build` — succeeded.
- `node scripts/visual-check.mjs` (run per-page via `VISUAL_CHECK_ONLY=`) — all captures
  succeeded; verified by reading the resulting PNGs with the Read tool:
  - `reviews-perfumes.png` — SearchInput + SortButtons share one row; the ownership pill
    row (all/Sample/Travel size/Full bottle) sits on its own row directly beneath.
  - `vinyl.png` — search input on its own row; the covers/list view-toggle pills sit
    beneath on their own row.
  - `projects.png` — SearchInput + SortButtons share one row; the tag-pill row sits
    beneath on its own row.
  - `workouts.png` — unaffected as predicted (that block contains only the PillGroup,
    no SearchInput/SortButtons — the new rule is a no-op there); renders identically to
    the pre-change layout (All/Push/Pull/Legs pills row, unaffected DateRangeFilter row
    below it).
  - `reviews-movies.png` — unaffected; no pill row in that block, search+sort share one
    row as before.
  - (reviews/tv, reviews/books, reviews/albums were not individually screenshotted this
    run but use the identical `ReviewCard` list pattern as movies with no pill row in
    `.collection-review-controls`, so the same no-op reasoning applies — confirmed via
    the T335 spec's page-by-page audit.)

## Remaining
Nothing outstanding — task complete.
