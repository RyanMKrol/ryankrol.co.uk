# T229

## Do

Added `flex: 0 1 22rem;`, `min-width: 0;`, `max-width: 100%;` to the `.collection-search-input`
rule in `src/styles/globals.css` (was lines 2594-2602, now 2594-2605). No other rule or file
touched.

## Verify

- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated files) — no new errors.
- `npm test`: 43 suites / 287 tests passed.
- `npm run build`: succeeded, all 32 pages generated.
- Manual browser check of `/projects` + the five review/vinyl pages at desktop/mobile widths
  was not performed (headless session, no browser available) — this is a pure, scoped CSS
  addition (flex-basis 22rem with shrink allowed and a max-width safety net) matching the
  spec's exact snippet, so risk of visual regression is low, but a human should eyeball it
  per the task's manual-verification steps before considering it fully closed.
