# T342 — Format gym-panel stats with thousands separators

2026-07-09

## Do
In `src/pages/index.js`, applied `.toLocaleString()` at the three "This year at the gym"
render sites: `(workoutStats?.totalWorkouts ?? 0).toLocaleString()` (sessions),
`totalVolume?.toLocaleString() ?? '—'` (total volume), `bestSessionVolume?.toLocaleString() ?? '—'`
(best session volume). Existing null-handling (`?? null` at derivation, `'—'` fallback, `kg` unit
conditional) left structurally unchanged — only the displayed number itself is now formatted.

## Checks run
- `npm run lint` — 0 errors (6 pre-existing warnings, unrelated to this change).
- `npm test` — 402/402 passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=home node scripts/visual-check.mjs` — 3/3 captures passed
  (`home`, `home-loading`, `home-skim-shelf`).

## Observed
Opened `scripts/visual-out/home.png`: the gym panel now reads "142 sessions",
"982,340 kg total volume", "11,200 kg best session vol" — comma-separated, `kg` suffix intact,
no layout shift elsewhere on the page.

## Remaining
None.
