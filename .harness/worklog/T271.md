# T271: Hot takes API routes

## Built
- `src/lib/hotTakes.js`: `validateHotTakeText` (non-string/empty/whitespace-only/>500 chars all invalid),
  `parseUkDate` + `sortByDateDesc` (mirrors `parseUkDate` in `src/pages/index.js`).
- `src/pages/api/hot-takes/index.js`: GET, `withApiCache(generateCacheKey('hot-takes'), ...)` →
  `scanTable(DYNAMO_TABLES.HOT_TAKES_TABLE)`, sorted newest-first via `sortByDateDesc`.
- `src/pages/api/hot-takes/add.js`: POST, gates on `RYANKROL_SITE_KEY` (401), validates `text` via
  `validateHotTakeText` (400), stores `{ id: randomUUID(), text, date }` via `PutCommand`, then
  `clearApiCache('api-hot-takes')`.
- No `update.js`/`delete.js` — matches the vinyl precedent per the task spec.
- `DYNAMO_TABLES.HOT_TAKES_TABLE` already existed in `src/lib/constants.js` (added by an earlier task).

## Verified
- `npm run lint`: 0 errors (6 pre-existing warnings, unrelated to this change).
- `npm test`: 52 suites / 338 tests passed, including new `src/lib/hotTakes.test.js` (6 tests:
  valid sentence, empty string, whitespace-only, non-string types, over-max-length, exactly-max-length).
- `npm run build`: succeeded; `/api/hot-takes` and `/api/hot-takes/add` both listed as dynamic routes.
- No live DynamoDB/API calls made — verified by reading code + build output only.
