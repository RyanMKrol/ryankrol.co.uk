# T253

Added a `.perfume-v4-scale-heading` CSS rule (mirrors `.perfume-v4-best-for-label`, with a
smaller bottom margin since it sits above another text line rather than a chip row) and
used it in `Variant6Hybrid.js` to add "Longevity" / "Projection" headings above the
existing spectrum labels on the perfume review card's longevity/projection bars.

No JS logic, math, or other markup changed.

Verified:
- `npm run lint` — no errors (pre-existing warnings only, unrelated files).
- `npm test` — all passing.
- `npm run build` — succeeded.
- Did not run `npm run dev` + browser click-through (headless session); change is template-only
  and CSS-declaration-only (copied verbatim from an existing rule), so build+lint+test coverage
  is sufficient confidence for this task's scope.
