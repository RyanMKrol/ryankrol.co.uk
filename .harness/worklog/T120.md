# T120: Movies update API — stamp editedDate on every edit

## What changed
- `src/pages/api/reviews/movies/update.js`: kept the existing `originalDate`/`preservedDate`
  logic untouched (original `date` is still preserved across edits). Added a fresh
  `editedDate` computed via `new Date().toLocaleDateString('en-GB').replace(/\//g, '-')`
  (same convention as `date`/`add.js`), unconditionally included in `reviewData` on every
  successful update.
- Added `src/pages/api/reviews/movies/update.test.js` covering:
  - `date` preserved from the existing item while `editedDate` is set to today.
  - Fallback case (no existing item found) where `date` falls back to today, and
    `editedDate` is also today.

## Docs
- Task scope is limited to `src/pages/api/reviews/movies/update.js` (+ tests/worklog only);
  `CLAUDE.md`/`README.md` are not in scope for this task, so they were not edited, matching
  the precedent set by other narrowly-scoped tasks (e.g. T119). Flagging that a follow-up doc
  task should add `editedDate 'DD-MM-YYYY', set on every edit` to the `MovieRatingsV4` row in
  both `CLAUDE.md`'s Data model table and `README.md`'s `MovieRatingsV4` schema section.

## Verification
- `npm run lint` — 0 errors (3 pre-existing warnings, unchanged).
- `npm test` — 234/234 passed (32 suites), including the 2 new tests.
- `npm run build` — succeeded.
- No live DynamoDB/API calls were made; verified by reading the code path and via the new
  unit tests mocking `docClient`.
