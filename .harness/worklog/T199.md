# T199

## Do

Seeded `searchTerm` from `?q=` in the 5 listed pages (movies, tv, books, albums reviews +
vinyl). Each now calls `useRouter()` and has a `useEffect` that sets `searchTerm` once
`router.isReady` is true and `router.query.q` is a string, per the spec's exact snippet.
No changes to filtering/sort logic or `SearchInput` wiring.

## Verify

- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 248/248 passing.
- `npm run build` — succeeded, all 5 touched routes built as static pages.
- Did not run `npm run dev` + manual click-through (headless session, no browser) — code
  review confirms the effect matches the spec's snippet exactly and mirrors the established
  `useRouter` pattern used in `movies/add.js` / `vinyl/add.js`.
