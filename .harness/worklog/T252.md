# T252: Render review date + "last edited" date on the perfume review card

## What changed
- `src/components/perfumeVariants/Variant6Hybrid.js`: imported `formatReviewDate` from
  `../../lib/dateFormat` and rendered `item.date` / `item.editedDate` (when present) as
  trailing metadata lines after the Fragrantica link block, mirroring `ReviewCard.js`
  lines 207-217.
- `src/styles/globals.css`: added `.perfume-v6-date` (small muted text, matching the
  spirit of `.review-date`) near the other `perfume-v6-*`/`perfume-v4-*` rules.

## Verification
- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test`: 49 suites / 310 tests passed, including the existing
  `Variant6Hybrid.test.js` (unaffected — covers `formatApplicationSpotLine` only).
- `npm run build`: succeeded, `/reviews/perfumes` and `/reviews/perfumes/edit/[id]`
  routes built cleanly.
- Did not run the dev server against live DynamoDB data (would require a live call to
  production perfume records) — verified by reading the code path: both live records
  have `date` populated and no `editedDate`, so the new markup renders a single
  "Date: ..." line and no "Updated: ..." line, per the done-when criteria.

## Notes
- Did not touch any API route (`add.js`/`update.js`) per the task's explicit scope —
  `editedDate` population is task perfume-review-dates-1's responsibility.
- No new test added: `expectsTest: false` for this task, and there's no existing
  component-render test harness for this file (its only test covers a pure helper
  function) — didn't want to introduce new test infra for a small rendering addition.
