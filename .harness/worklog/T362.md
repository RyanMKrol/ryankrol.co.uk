# T362 — 2026-07-10

## What
`src/components/BookSearch.js` still hardcoded `providerLabel = 'Google Books'` and its top
comment said "Google Books search + confirm component", even though `/api/books/search`
already switched to Hardcover server-side in the previous unit (hardcover-migration-a). Updated
both:
- top comment → "Hardcover search + confirm component for the book add flow."
- `const providerLabel = 'Hardcover';`

This flows into both UI strings that reference the provider: the results header ("Results from
{providerLabel}") and the confirmed-match banner ("✓ {providerLabel} match selected:").

No change to the fetch/request logic — confirmed the existing call
(`/api/books/search?${params}` with only `title`/`author`) needs no new query param; the
provider switch already happened entirely server-side.

Confirmed `grep -n "Google Books\|google" src/components/BookSearch.js` returns no matches.

## Checks run
- `npm run lint` — clean (0 errors, 6 pre-existing warnings unrelated to this file).
- `npm test` — 79 suites / 441 tests passed.
- `npm run build` — succeeded.
- `VISUAL_CHECK_ONLY=book node scripts/visual-check.mjs` — all 9 matching captures passed
  (reviews-books index/edit/search/sort flows). Note: the visual harness has no existing
  `PAGES`/`FLOWS` capture that actually exercises `BookSearch`'s search-results or
  confirmed-match state (no `books/add` or `books/edit/[id]` capture with a triggered search) —
  a pre-existing gap, not something in this task's scope (scope is `BookSearch.js` only) to add.
  Verified the label wiring by reading the component instead.

## Remaining
None — task fully done within scope.
