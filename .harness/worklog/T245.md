# T245: fix movies/edit masonry layout

## Do
`src/pages/reviews/movies/edit.js` wrapped cards in a plain `.reviews-wrapper` div instead of
the shared masonry-column layout used by `src/pages/reviews/movies/index.js`. Replaced it with
the same `<MasonryColumns>` usage (`items={movies}`, `columnCount={useResponsiveColumnCount(3, 900)}`,
`className="poster-banner-grid"`, `columnClassName="poster-banner-grid-col"`), with `renderItem`
returning the existing `review-card-with-edit` wrapper (ReviewCard + edit-button-overlay Link)
unchanged. No search/sort/pagination added — page still just lists everything sorted by date via
the existing effect.

## Verify
- `npm run lint`: 0 errors (6 pre-existing warnings, unrelated files).
- `npm test`: 48 suites / 308 tests passed.
- `npm run build`: succeeded; `/reviews/movies/edit` built as a static page (1.08 kB).
- Manual browser check (`npm run dev`) not run in this headless session — verified via code
  parity with `movies/index.js`'s already-shipped MasonryColumns usage instead.

## Result
done T245
