# T255 — commit-count sort + display on /projects

## Do
- Added a `commits` entry to `SORT_FIELDS` in `src/pages/projects/index.js`, matching the
  existing `date`/`stars` shape.
- Extended the `sortedRepos` `useMemo` with `commits` (descending) / `commits-asc` (ascending)
  branches, treating missing/`null` `commitCount` as `0` — same convention as the existing
  `stars` branches.
- Added a conditional `<span className="project-card-commits">` to the meta row, rendered only
  when `repo.commitCount` is a `number` (so a fetch-failed `null` renders nothing, never
  `null commits` or `0 commits`).
- Checked `globals.css` for `.project-card-forks`/`.project-card-updated` rules to mirror — none
  exist (siblings rely on inherited row text styling), so no new CSS was added, matching the
  task's guidance.

## Verify
- `npm run lint` — no errors (pre-existing warnings only, no new ones).
- `npm test` — all suites pass, no regressions.
- `npm run build` — succeeds, `/projects` builds statically.
- Ran `npm run dev`, confirmed `GET /api/github/repos` returns real `commitCount` numbers per
  repo (e.g. `ryankrol.co.uk`: 672, `LocalJobs`: 1132), and that the rendered `/projects` page
  includes the new "commits" sort pill label. Card-level rendering of the commit count happens
  client-side (this page fetches in a `useEffect`, per its existing pattern), so it wasn't
  visible in a plain `curl` of the SSR HTML, but the sort logic and conditional render were
  verified by code inspection against the API's confirmed response shape.

## Result
done T255
