# T195: fix zero vertical gap between page-title and collection-form-card

## Do

Added `margin-top: 2rem` to `.collection-form-card` in `src/styles/globals.css` (line ~2589),
combined into the existing `margin: 0 auto` shorthand as `margin: 2rem auto 0;`. This preserves
horizontal auto-centering while adding the vertical gap. `.page-title`'s `margin-bottom: 0` was
left untouched, since it's used site-wide on headers that don't have this pairing issue.

## Verify

- `npm run lint`: 0 errors, 6 pre-existing warnings (unrelated `<img>` / exhaustive-deps) — pass.
- `npm test`: 37 suites / 248 tests passed.
- `npm run build`: succeeded, all 32 static/dynamic pages generated.
- Ran `npm run dev` and curled `/reviews/movies/add` — confirmed the rendered HTML includes the
  `collection-form-card` class, so the CSS change applies to that page (and by the same shared
  rule, to all 11 affected pages). Did not do a full browser screenshot pass across all pages
  (headless env), but the CSS is a single shared rule change with no per-page markup involved, so
  the fix generalizes identically across all 11 pages listed in the task spec.
