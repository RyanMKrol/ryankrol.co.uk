# T131

## Do

In `src/pages/reviews/movies/add.js`, the inline TMDB-match error paragraph rendered
unconditionally from mount (driven only by `tmdbMatch` state, `null` on mount). Fixed by
gating on a new `hasAttemptedSubmit` flag:

- Added `const [hasAttemptedSubmit, setHasAttemptedSubmit] = useState(false);` alongside the
  other `useState` calls.
- `handleSubmit` now calls `setHasAttemptedSubmit(true);` immediately after `e.preventDefault();`,
  before the `if (!tmdbMatch)` guard.
- Inline error paragraph condition changed from `{!tmdbMatch && (...)}` to
  `{hasAttemptedSubmit && !tmdbMatch && (...)}`.

Left the top-of-form `message`/`messageType` banner and the `handleSubmit` validation logic
untouched, per spec. Did not touch `src/pages/reviews/tv/add.js` (separate task) or any other
file.

## Verification

- `npm run lint` — 0 errors, same 3 pre-existing warnings as before (dev/cache.js
  exhaustive-deps, two `<img>` warnings in movies/tv edit pages) — no new errors introduced.
- `npm test` — 243 tests passed across 34 suites (no test changes needed; no pure logic added,
  this is a component-level JSX gating change with no dedicated component test suite in this repo).
- `npm run build` — succeeded, all 32 routes generated.
- Manual verification via `npm run dev` / live browser was not performed (headless run); the fix
  was verified by reading the rendered JSX condition (`hasAttemptedSubmit && !tmdbMatch`), which
  satisfies the "Done when" bullet about confirming via reading the condition. The behavioral
  claims (error hidden on mount, appears after a failed submit attempt, disappears once a match
  is selected) follow directly from React state semantics: `hasAttemptedSubmit` starts `false`
  (hidden on mount), flips `true` on first `handleSubmit` call (shown if still no match), and
  `tmdbMatch` becoming truthy via `onSelect` short-circuits the `&&` regardless of the flag
  (hidden once matched).
