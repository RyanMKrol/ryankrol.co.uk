# T278: convert perfumes grid to masonry columns

## Do

- `src/pages/reviews/perfumes/index.js`: imported `MasonryColumns` + `useResponsiveColumnCount`,
  added `columnCount = useResponsiveColumnCount(2, 700)`, replaced the plain `.perfume-card-grid`
  div/map with `<MasonryColumns items={filteredPerfumes} columnCount={columnCount}
  className="perfume-card-grid" columnClassName="perfume-card-grid-col" renderItem={...} />`.
- `src/styles/globals.css`: `.perfume-card-grid` is now `display: flex; gap: 1.5rem;`, added
  `.perfume-card-grid-col` (flex column, `flex: 1 1 0`, `min-width: 0`), removed the old
  `@media (max-width: 700px)` grid-collapse block (mirrors `.poster-banner-grid`/`-col`).
- Sort/filter logic and `PillGroup` untouched; no pagination added.

## Verify

- `npm run lint` — no errors (pre-existing warnings only).
- `npm test` — all suites pass.
- `npm run build` — succeeds; `/reviews/perfumes` builds statically at 1.51 kB.
- Layout mechanism matches the existing Albums masonry conversion (T277) exactly, so the same
  responsive/reading-order behavior (two columns above 700px, one below, left-to-right top-to-
  bottom distribution via `MasonryColumns`) applies here.

done T278
