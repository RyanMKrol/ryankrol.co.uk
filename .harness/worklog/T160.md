# T160 — Rebuild workout detail page (Collection redesign)

## Do

Rebuilt `src/pages/workouts/[id].js` to match `renders/3g.png` (desktop) / `renders/4l.png`
(mobile) from the design handoff:

- `← back to workouts` link (new `.collection-back-link` class).
- Title (workout split name) + split-type `Badge` (`solid` variant, same `--split-push` /
  `--split-pull` / `--split-legs` colors introduced in T159).
- A row of 4 `StatBlock`s (neutral/bordered mode) for date / duration / volume / exercises
  (`.workout-detail-stats` grid, 2-col on mobile).
- One card per exercise (`.workout-exercise-card`, 2-col grid `.workout-exercise-grid`,
  1-col on mobile) with the exercise name as the card title (links to `/exercises/[name]`
  as before) and each set rendered as a mono row (`.workout-set-row`) showing
  `label` left / `weight × reps` right via the existing `formatSetDisplay` helper.
- Warmup vs. working-set detection reuses the existing `set.type === 'warmup'` logic already
  present in this file (same field the old grid-card layout used). Warmup rows get
  `.workout-set-row.warmup` → `var(--color-warmup-bg)` / `var(--color-warmup-text)` (T142
  tokens, unchanged). Working rows use `var(--color-set-row-bg)` (already defined in
  `globals.css`, previously only used by `WorkoutCard.js`'s list-page set chips).

`WorkoutCard.js` was inspected but is **not used** by `src/pages/workouts/index.js` (T159
rebuilt the list page with its own inline `workout-session-card` markup) or by the detail
page — left untouched, in scope but no change needed.

Replaced the old `.workout-exercise-card` / `.set-card` CSS rules in `globals.css` with the
new Collection-styled rules (`.collection-back-link`, `.workout-detail-stats`,
`.workout-exercise-grid`, `.workout-exercise-card`, `.workout-exercise-card-title`,
`.workout-set-rows`, `.workout-set-row`) — confirmed via grep that no other `.js` file
referenced the removed class names before deleting them.

## Verify

- `npm run lint` — 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` — 35 suites / 241 tests passed. No new pure logic was added (UI restructuring
  only), so no new test file.
- `npm run build` — succeeded, `/workouts/[id]` route built.
- `run-app`: started `npm run dev`, hit `GET /api/workouts?mode=all` to get a real workout id,
  loaded `GET /workouts/<id>` — 200 OK. Confirmed via the raw API response that this workout's
  first exercise has `type:"warmup"` vs `type:"normal"` sets, matching the warmup/working
  branch used by the new `.workout-set-row` styling. Did not have a headless-browser screenshot
  tool available in this environment to visually confirm pixel colors — deferred to T168's
  visual audit per the task's "Done when" note.
