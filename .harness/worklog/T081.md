# T081 — Fix production crash: WorkoutCard assumes `exercises` always exists

## What changed
`src/components/WorkoutCard.js`:
- `getTotalVolume`/`getTotalSets` now default their `exercises` param to `[]`.
- Added `const exercises = workout.exercises || [];` and used it for the stat line
  (Exercises/Sets/Volume) instead of `workout.exercises` directly.
- The per-exercise breakdown (`.workout-exercises` block) is now wrapped in
  `{exercises.length > 0 && (...)}` so it isn't rendered at all when there's no data.

## Verify performed
- `npm run lint` — 0 errors (4 pre-existing warnings, unrelated).
- `npm test` — 147/147 passing (no new tests added; `expectsTest: false` for this task).
- `npm run build` — succeeded.
- Manual verification: temporarily added a scratch RTL test rendering `WorkoutCard` with
  `{id, title, start_time, end_time}` (no `exercises` key, matching the real incomplete
  production shape). Confirmed it rendered without throwing and showed "Exercises: 0".
  Scratch test file was removed before committing, per the task's acceptance bar (no
  component-test harness exists in this repo yet).
- Did not hit live DynamoDB/Hevy — verified via code + local render only.
