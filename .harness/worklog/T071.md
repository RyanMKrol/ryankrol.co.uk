# T071 — Backfill-from-TMDB button on the tv edit page

## What was done

Mirrored T070 (movies edit page) exactly, targeting `type=tv`:

- Imported `MetadataBackfillModal` and `tmdbPosterUrl` in
  `src/pages/reviews/tv/edit/[id].js`.
- On load, seed `tmdbId`/`mediaType`/`posterPath`/`tmdbOverview`/`tmdbDate` from the
  fetched TV show record into form state.
- Added `handleBackfillSearch` — calls `GET /api/tmdb/search?query=<title>&type=tv`.
- Added `handleBackfillConfirm` — maps the chosen result's `overview` → `tmdbOverview`
  (never `gist`) and `date` → `tmdbDate`, plus `tmdbId`/`mediaType`/`posterPath`. Does not
  touch `rating`.
- Rendered `MetadataBackfillModal` + poster preview `<img>` in the form, above the
  Rating field. Save button behavior unchanged (still requires explicit click).

## Verification

- `npm run lint` — 0 errors (4 pre-existing warnings, one new `no-img-element` warning
  matching the identical pattern already present on the movies edit page).
- `npm test` — 139/139 passing.
- `npm run build` — succeeds, `/reviews/tv/edit/[id]` compiles.
- Did not make any live TMDB/DynamoDB calls (per harness rules) — verified via code
  reading and static build only.

## Scope

Only `src/pages/reviews/tv/edit/[id].js` was modified, matching the allowed scope.
