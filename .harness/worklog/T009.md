# T009 worklog

## What was built

- `src/components/ProgrammeOverviewCharts.js` — three charts (Line: volume/session, Line: working-sets/session, Bar: frequency/month) using react-chartjs-2 with `useChartTheme()` for all colours.
- `src/pages/programmes/index.js` — dashboard page fetching `/api/workouts?mode=all`, computing per-programme stats client-side via `aggregateProgramme`, Push/Pull/Legs selector, headline totals (total workouts, total volume, avg volume/session).
- `src/pages/workouts/index.js` — added "View programme stats →" link to `/programmes`.

## Verification

- `npm run build` passes; `/programmes` appears as `○ (Static)` in build output at 3.33 kB.
- No new API endpoints added — reuses existing `/api/workouts?mode=all` path from T001.
- All colours read via `useChartTheme()` — no hardcoded values.
