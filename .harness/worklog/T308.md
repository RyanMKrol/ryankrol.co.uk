# T308 worklog

Applied the shared `review-edit-card` header-strip pattern (from perfumes-edit-redesign-2 /
T307 books) to the movies edit page, replacing T263's contrast-only overlay fix.

## Changes

- `src/pages/reviews/movies/edit.js`: `renderItem` wrapper div className
  `review-card-with-edit` → `review-edit-card`; Edit `<Link>` moved to be the first child,
  wrapped in a new `<div className="review-edit-header">`, with its className changed from
  `edit-button-overlay edit-button-overlay--movies-banner` → `review-edit-button`;
  `<ReviewCard styleVariant="poster-banner" .../>` is now the second child.
- `src/styles/globals.css`: deleted the dead T263 rules `.edit-button-overlay--movies-banner`
  and `.edit-button-overlay--movies-banner:hover` plus their explanatory comment (the button no
  longer overlays the poster-banner gradient, so the opaque-for-contrast styling is moot). Left
  the base `.review-card-with-edit`/`.edit-button-overlay` rules and the tv/albums/perfumes fix
  blocks untouched.

## Verification

- `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/movies/edit.js` →
  no matches.
- `npm run lint` → 0 errors, 6 pre-existing warnings (unrelated files).
- `npm test` → 67 suites / 395 tests passed.
- `npm run build` → succeeded; `/reviews/movies/edit` page built.
- Did not run `npm run dev` interactively (headless session) — verified structurally by reading
  the books/edit.js reference implementation (T307) which movies/edit.js now mirrors exactly for
  the header-strip markup/classes.
