## Do

In `src/lib/perfumes.js`, add the string `'Clothes'` as a 7th entry to the `VALID_APPLICATION_SPOTS` array (currently lines 19-26: `['Wrists', 'Elbows', 'Clavicles', 'Beard', 'Back of neck', 'Behind ears']`). This is the server-side allow-list read by `validateApplicationSpots` (same file, ~lines 28-40) to validate the `applicationSpots` field accepted by `POST /api/reviews/perfumes/add` and `POST /api/reviews/perfumes/update` (`src/pages/api/reviews/perfumes/add.js`, `src/pages/api/reviews/perfumes/update.js`) — each accepted entry has the shape `{ spot, sprays }` and `spot` must be one of `VALID_APPLICATION_SPOTS`, `sprays` a positive integer.

Also update `src/lib/perfumes.test.js`'s `describe('validateApplicationSpots', ...)` block (currently lines 66-87) to add an assertion that `validateApplicationSpots([{ spot: 'Clothes', sprays: 1 }])` returns `true`, without removing any existing assertions in that block.

Scope note: this task ONLY changes the server-side allow-list in `src/lib/perfumes.js` (+ its test). Do NOT touch `src/components/PerfumeCharacteristics.js` — the client-side `APPLICATION_SPOTS` array and the one-line-layout fix are a separate task.

## Done when

`VALID_APPLICATION_SPOTS` in `src/lib/perfumes.js` contains exactly these 7 values (order doesn't matter functionally, but append 'Clothes' at the end for a minimal diff): 'Wrists', 'Elbows', 'Clavicles', 'Beard', 'Back of neck', 'Behind ears', 'Clothes'.
`npm test` passes, including a new/updated assertion in `src/lib/perfumes.test.js` proving `validateApplicationSpots([{ spot: 'Clothes', sprays: 1 }])` returns `true`.
`npm run lint` reports no errors.
No files outside `src/lib/perfumes.js` and `src/lib/perfumes.test.js` are changed.
