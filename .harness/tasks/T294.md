## Do

Read `src/pages/workouts/[id].js` in full — specifically the set-row rendering block (lines 248-273) inside each exercise card, and `formatSetDisplay` (lines 99-132).

Owner confirmed: the medal belongs at SET-ROW level — next to the literal set that broke a personal best, not a whole-exercise-card indicator.

Each `set` object returned by `GET /api/workouts/[id]/exercises` (backed by `getExercisesByWorkout`, reading the `Exercises` table) now carries `isWeightPR`/`is1RMPR`/`isVolumePR` booleans directly on the specific set(s) that achieved a new all-time best this session (added by dependency task `workout-pr-medals-2`) — no new fetch needed, the existing `exercisesData.exercises` response already includes them.

In the set-row rendering (the `exercise.sets.map((set, setIndex) => {...})` block), after computing `setLabel`/`formatSetDisplay(set)` as today, render a small medal marker inline within the `<div className="workout-set-row ...">` when `set.isWeightPR || set.is1RMPR || set.isVolumePR` is true. A single set can achieve more than one axis at once (e.g. heaviest weight AND best 1RM simultaneously) — a per-axis label (e.g. distinct "1RM PR" / "Weight PR" / "Volume PR" markers) is recommended since these are independently meaningful achievements the owner explicitly asked to track separately, but a single combined marker is acceptable if it keeps the dense per-set row visually clean; use your judgement, and include an accessible label either way (not emoji-only).

Do not touch `/workouts/index.js` (a separate dependent task) or the API routes — no new fields are needed there since the underlying data already carries the flags after the dependency task ships.

## Done when

- Each set row on `/workouts/[id]` that carries any of `isWeightPR`/`is1RMPR`/`isVolumePR` shows an accessible medal marker distinguishing which axis (or axes) it broke; rows without any flag show no marker.
- No new network request added.
- `npm run lint` / `npm run build` green.
- Manual check via the `run-app` verifier: `npm run dev`, open a workout detail page (seed synthetic data if the historical backfill hasn't run yet), confirm the marker appears on exactly the right set row(s) and correctly reflects which axis/axes it broke.
