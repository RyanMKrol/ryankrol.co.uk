## Do

Replace the two independent boolean fields `considerTravelSize`/`considerFullBottle` on `PerfumeRatings` with ONE new field: `ownership`, a string enum with exactly one of three values: `'Sample'`, `'Travel size'`, `'Full bottle'`. This task is backend-only (validator + the two API routes + docs) — a separate task handles the add/edit form UI, and another separate task handles migrating the ~2 existing production rows; do not touch `src/pages/reviews/perfumes/**` or write any migration script here.

1. **Add a validator to `src/lib/perfumes.js`**, following the exact pattern already used for `validateSeasons`/`VALID_SEASONS` (lines 5-17 today): add a `VALID_OWNERSHIP = ['Sample', 'Travel size', 'Full bottle']` array and a `function validateOwnership(value) { return VALID_OWNERSHIP.includes(value); }`, then add `validateOwnership` to the `export { ... }` block at the bottom (currently lines 71-79) alongside the existing exports. Do not export `VALID_OWNERSHIP` itself — mirror how `VALID_SEASONS`/`VALID_APPLICATION_SPOTS` stay module-internal today.

2. **Add tests to `src/lib/perfumes.test.js`** for `validateOwnership`: import it alongside the existing imports (top of file, `import { validatePerfumeRating, validateLongevity, ... } from './perfumes';`), and add a `describe('validateOwnership', ...)` block mirroring the existing `describe('validatePerfumeRating', ...)` shape — `test.each(['Sample', 'Travel size', 'Full bottle'])('accepts %p', ...)` returning true, and `test.each(['sample', 'Full Bottle', '', null, undefined, 123])('rejects %p', ...)` returning false (case-sensitive exact match only — no fuzzy/lowercase matching).

3. **Update `src/pages/api/reviews/perfumes/add.js`**: replace `considerTravelSize, considerFullBottle,` in the `req.body` destructure (currently lines 26-27) with `ownership,`. Import `validateOwnership` alongside the other validator imports (currently lines 6-11). Add a validation check right after the existing `applicationSpots` check (currently lines 67-71): `if (ownership !== undefined && !validateOwnership(ownership)) { return res.status(400).json({ message: "Ownership must be one of 'Sample', 'Travel size', 'Full bottle'" }); }` — keep it optional (`!== undefined` guard), matching how `longevity`/`projection`/`seasons`/`applicationSpots` are all optional today (this field has no prior data so must not become suddenly required). In the `reviewData` object (currently lines 77-92), replace the unconditional `considerTravelSize,\n      considerFullBottle,` lines with a conditional spread matching the established style: `...(ownership !== undefined && { ownership }),`.

4. **Update `src/pages/api/reviews/perfumes/update.js`** identically: replace `considerTravelSize, considerFullBottle,` in the destructure (currently lines 27-28) with `ownership,`, import `validateOwnership`, add the same validation check after the `applicationSpots` check (currently lines 68-72), and replace the unconditional `considerTravelSize,\n      considerFullBottle,` lines in `reviewData` (currently lines 109-110) with `...(ownership !== undefined && { ownership }),`.

5. **Update docs in the same commit** (this repo's Definition of Done requires it):
   - `CLAUDE.md` line 203 (the `PerfumeRatings` data-model table row): replace `considerTravelSize, considerFullBottle,` in the shown shape with `ownership?,` (optional, since old rows won't have it until the separate migration task runs).
   - `README.md` lines 261-287 (the `#### PerfumeRatings Table` schema block): replace the `considerTravelSize: true,` / `considerFullBottle: false,` lines (currently lines 275-276) with a single `ownership: "Full bottle",  // 'Sample' | 'Travel size' | 'Full bottle'` line in the same position.

Do not add a migration script and do not touch the frontend forms in this task — both are separate, dependent tasks.

## Done when

- `src/lib/perfumes.js` exports a new `validateOwnership` function that accepts exactly `'Sample'`, `'Travel size'`, `'Full bottle'` and rejects everything else (wrong case, empty string, null, undefined, numbers, arrays).
- `src/lib/perfumes.test.js` has a passing `describe('validateOwnership', ...)` block covering both accept and reject cases.
- `POST /api/reviews/perfumes/add` and `POST /api/reviews/perfumes/update` no longer read or write `considerTravelSize`/`considerFullBottle` anywhere; both accept an optional `ownership` field, reject the request with a 400 and a clear message when `ownership` is present but not one of the three valid values, and persist `ownership` on the stored item only when it was provided (existing rows without it are untouched, consistent with how `longevity`/`projection`/`seasons`/`applicationSpots` are already handled as optional).
- `CLAUDE.md`'s `PerfumeRatings` row and `README.md`'s `PerfumeRatings Table` schema block both describe `ownership` (not the two old booleans) as the current shape.
- `npm run lint` reports no errors.
- `npm test` passes, including the new `validateOwnership` tests.
- `npm run build` succeeds.
