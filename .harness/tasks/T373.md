## Overview
Build FIVE genuinely-distinct alternate designs for the book-review spine-cover card and make all five previewable together, so the owner can compare rendered options and pick one. Every design must tighten the too-wide title↔metadata gap and render Markdown ordered lists with proper left indent (idea #3).

## Do
In ReviewCard.js (SpineCoverCard, ~L53) render the spine-cover book card through one of 5 selectable design variants, and build a DEDICATED preview page at src/pages/dev/book-card-designs.js (owner-confirmed approach) that renders all 5 variants stacked against the two example books, keeping the workshop off the public list. Each variant is a distinct visual take (typography / spacing / rating placement / tile treatment) with FREE REIN within the Collection design tokens (--color-*, --font-*, --radius-*) — five clearly-differentiated designs. In every variant: (1) reduce the vertical gap between .spine-cover-title and .spine-cover-meta so the metadata sits closer under the title than today; (2) fix the Markdown ordered/unordered list indent so list markers no longer hug the left edge — via a SCOPED selector (a wrapper className added in Markdown.js's block output, or targeting .spine-cover-snippet ol/ul in globals.css), NOT by loosening the global list-style:none resets (~globals.css L1995/L3774) that nav pills and other lists rely on. Add the .spine-cover-* variant styles in globals.css. Update scripts/_visual-harness.mjs so the hermetic visual check captures every variant against the two required book shapes: add 'Ultra-Processed People' (long body that truncates -> 'Read more' expand state) and 'The Horus Heresy 42: Garro' (body containing a Markdown ORDERED list) to the books fixtures, plus the PAGES/FLOWS entries so each variant's read-more state AND ordered-list rendering are screenshotted (the /dev/book-card-designs page is the natural PAGES entry). Keep the public /reviews/books rendering working (it keeps the current design until the winner is hardcoded in the follow-up).

## Done when
All 5 distinct spine-cover book-card designs render without error and are viewable together via the chosen preview mechanism. In every design: the title↔metadata spacing is visibly tighter than the current card, and a Markdown ordered list renders with a proper left indent (numbers no longer flush against the content edge) via a scoped selector that leaves the global list-style:none resets and nav-pill lists untouched. scripts/_visual-harness.mjs includes 'Ultra-Processed People' (with a body long enough to truncate and show 'Read more') and 'The Horus Heresy 42: Garro' (with a Markdown ordered-list body), and the visual check produces screenshots covering each variant in both the read-more/expanded state and the ordered-list state.
