## Do

Create a new standalone presentational component at `src/components/perfumeVariants/Variant3ShelfTile.js`, default-exported, accepting props `{ item }` (same raw `PerfumeRatings` record shape — see README.md's 'PerfumeRatings Table' section and src/pages/api/reviews/perfumes/add.js). This variant's direction is a VISUAL 'bottle shelf' tile, meant to sit in a dense grid like a shelf of bottles rather than a stack of read-heavy cards: a tall tile (aspect-ratio around 3/4, similar proportions to `CoverTile`'s usage in `src/components/ReviewCard.js`'s `PerfumeCard`) filled with a colour gradient keyed off the perfume's identity — reuse `gradientForKey` exported from `src/components/CoverTile.js` (`gradientForKey(item.id || item.title)`) as the tile background, since there are no real bottle photos stored. Overlay the title and a compact `PipMeter` rating directly on the tile. Below the tile, in a slim caption strip, show designer + the `type` badge. On top of that, surface `seasons` (if present) as tiny corner icon-like chips on the tile itself (e.g. one-letter chips: 'W','Sp','Su','A','D','N'). Do NOT surface longevity, projection, applicationSpots, or the description text in this variant — it is intentionally a scan-fast visual grid, not a detail card (contrast with variants 1/2). Still render a small Fragrantica link in the caption strip. Add new, uniquely-prefixed CSS classes (e.g. `.perfume-v3-*`) to `src/styles/globals.css` — ONLY ADD new rules, never edit/remove existing ones. Standalone component; the surrounding grid container is not this task's concern (assume it will sit in some grid/flex-wrap container).

## Done when

`src/components/perfumeVariants/Variant3ShelfTile.js` exists and renders a gradient tile with overlaid title/rating, a caption strip with designer/type/seasons-chips/fragrantica-link, deliberately omitting longevity/projection/applicationSpots/description. `npm run dev` and confirm in a running dev server (via the paired preview-page task, or a temporary scratch mount) that several real perfume records each get a visually distinct gradient tile with no console errors, and that it reads as a compact, image-grid-like presentation clearly different from variants 1 and 2. `npm run build` succeeds and no pre-existing rule in `src/styles/globals.css` was modified or removed.
