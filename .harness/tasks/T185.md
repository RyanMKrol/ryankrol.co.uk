## Do

Create a new page at `src/pages/dev/perfume-variants.js` (a dev-only, unlinked preview route — do not add it to any nav/home grid) that fetches real perfume data client-side from the existing, unchanged `GET /api/reviews/perfumes` endpoint (same pattern as `src/pages/reviews/perfumes/index.js`'s `useEffect` fetch — do not add or modify any API route) and renders it through ALL FIVE variant components built in the sibling tasks: `Variant1StatBlock`, `Variant2Editorial`, `Variant3ShelfTile`, `Variant4SeasonPlanner`, `Variant5TableRow` (import from `src/components/perfumeVariants/*`). Lay the page out as five clearly labelled sections stacked vertically in order (a heading per section, e.g. 'Variant 1 — Stat Block'), each rendering the SAME set of real perfume records (use every record `GET /api/reviews/perfumes` returns, or the first 6-8 if there are many, but use the same subset across all five sections so the comparison is apples-to-apples) laid out in whatever container suits that variant (a grid for the tile/card variants, a list for the table-row variant). The goal is a SINGLE page the owner can scroll through once to compare all five directly against each other with real data — this is a deliberate change from the prior full-site three-variant rewrite (T046-T063/T064), which required navigating between three separate route trees to compare and whose review was high-friction; keep this page self-contained and low-friction. Use inline styles or minimal ad-hoc classes scoped to this page for the section headings/spacing (do not add new rules to `src/styles/globals.css` — out of this task's scope) so it doesn't collide with any variant's own styling.

## Done when

`src/pages/dev/perfume-variants.js` exists, is not linked from any nav/home page, and when visited in a running dev server (`npm run dev`, open `http://localhost:3000/dev/perfume-variants` or whatever port `npm run dev` reports) shows five labelled sections, each rendering the same real perfume records (fetched live from `GET /api/reviews/perfumes`, no mock/hardcoded data) through a different one of the five variant components, with no console errors and no 404s. Confirm by hand that scrolling the page lets you compare all five without navigating anywhere else.
