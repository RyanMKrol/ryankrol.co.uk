## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Workouts list" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/3f.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4k.png` (mobile).

Rebuild `src/pages/workouts/index.js`:
- Header: big "workouts" title, "N sessions logged · via Hevy" meta line, and an All/Push/Pull/Legs
  `PillGroup` (from T143) filter — reuse whatever split-type filtering logic already exists in this
  page, just restyle the control.
- A 2-column grid of session cards: title (the split name), a split-type `Badge` (from T143, solid
  fill) using the workout split colors from the design — Push=coral `#FF5C39`, Pull=pink `#F25CA2`,
  Legs=indigo `#4B4DED` (these are NOT the same as `--accent-movies`/`--accent-tv`/etc. even though
  the hex values happen to match some section accents — add them as their own CSS custom properties
  in `globals.css` if not already present, e.g. `--split-push`, `--split-pull`, `--split-legs`, so
  the workout split badges aren't accidentally coupled to a review section's token), mono
  date/time/duration line, and exercise-count + volume `StatBlock`-style number pairs (reuse
  `StatBlock` from T148 in its neutral/compact mode, or plain styled numbers if `StatBlock` doesn't
  fit this compact two-number layout well — your call).
- Reuse the `Pagination` component + `src/lib/pagination.js` helper from T155 (368 total sessions
  per the design brief — this page needs paging just like the reviews lists did).

## Done when

- The workouts list page shows the new header/meta/filter-pills, a 2-col grid of session cards with
  correctly-colored split badges (Push=coral, Pull=pink, Legs=indigo) and exercise/volume stats, and
  working pagination via the shared `Pagination` component.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/workouts`, confirm the All/Push/Pull/Legs filter
  correctly filters sessions, split badges show the correct color per type, and pagination works.
  Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T168.
