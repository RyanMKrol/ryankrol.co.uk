## Do

In each of the 6 files listed in `scope`, edit ONLY the `<h1 className="page-title">...</h1>` line: remove the leading emoji + following space, and lowercase the remaining text. Do not touch any other text, markup, styling, or logic in these files - this is a pure copy change to one JSX text node per file. See the `design` field above for the exact current text and exact target text for all 6 files (confirmed directly against the source on 2026-07-03; if line numbers have drifted since, locate the h1 by its `className="page-title"` and current emoji-prefixed text, not by line number).

## Done when

1. `grep -rn "page-title" src/pages/reviews/movies/add.js src/pages/reviews/tv/add.js src/pages/reviews/books/add.js src/pages/reviews/albums/add.js src/pages/reviews/perfumes/add.js src/pages/vinyl/add.js` shows all 6 titles with no emoji characters and fully lowercase text, matching exactly: `add movie review`, `add tv show review`, `add book review`, `add album review`, `add perfume review`, `add vinyl record`. 2. `npm run dev`, open at least two of the 6 `/add` pages in a browser (e.g. `/reviews/movies/add` and `/vinyl/add`) and visually confirm the page title renders lowercase with no emoji, matching the lowercase convention already visible on the corresponding list pages (e.g. `/reviews/movies` renders `movies`). 3. `npm run lint`, `npm test`, and `npm run build` all still pass. 4. The diff touches ONLY the 6 files in scope, each with exactly one changed line (the h1 text), and nothing else (no button labels, form labels, success/error messages, or the vinyl page's `<Head><title>` changed).
