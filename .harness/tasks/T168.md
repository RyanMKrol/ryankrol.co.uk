## Do

This is a **visual audit and tweak** task, not a from-scratch build — the workouts list, workout
detail, and exercise detail pages already exist (built in T159-T161) and pass their own functional
Definition of Done. Your job here is different: compare the LIVE running pages against the design
renders pixel-by-pixel and make free-form CSS/markup tweaks to close the gap.

**Reference material** (all under `/Users/ryankrol/Downloads/design_handoff/`):
- `README.md` — the token/spec doc.
- Workouts list: `renders/3f.png` (desktop), `renders/4k.png` (mobile).
- Workout detail: `renders/3g.png` (desktop), `renders/4l.png` (mobile).
- Exercise detail: `renders/2c.png` (desktop), `renders/4d.png` (mobile).

**Process:**
1. `npm run dev`, load each page at desktop and mobile viewports, comparing against its paired
   render: workouts list's filter pills + split-badge colors (Push coral/Pull pink/Legs indigo) +
   2-col card layout; workout detail's stat-chip row + per-exercise set cards + warmup(amber)/working
   row treatment; exercise detail's 5 stat cards (2 accent-filled) + the 3 charts' colors (coral 1RM
   area, indigo volume bars with the last bar grape, marigold max-weight area) + the dark insights
   panel + the recent-sessions table.
2. Pay particular attention to exact color values on the charts (via `useChartTheme`) — these are
   easy to get subtly wrong (e.g. right hue, wrong saturation) and stand out clearly against the
   renders.
3. For every gap found, fix it directly — prefer fixing shared components/hooks (`StatBlock.js`,
   `Badge.js`, `useChartTheme.js`) over page-specific overrides when the gap is systemic.

## Done when

- All 3 pages (both breakpoints) visually match their paired renders closely — exact hex colors
  (including chart colors), correct fonts, correct badge/row treatments.
- `npm run lint`, `npm test`, and `npm run build` all pass after your tweaks.
- No functional regression: workout filter pills, the exercise detail time-range toggle and its
  chart data updates, still work correctly after your CSS/markup changes.
- Document in your worklog which specific gaps you found and fixed.
