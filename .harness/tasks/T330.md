## Overview
The home page (src/pages/index.js) currently renders all six stat blocks, the collection wall, latest takes, and the rail's workout/shelf/hot-takes panels with zero/empty placeholder values (0, empty arrays, 'No hot takes yet.') the instant the page mounts, then pops to real data all at once when a single batched Promise.all across 7 API endpoints resolves. Replace that zero-value flash with proper skeleton/wireframe placeholders shown while loading, so the page never displays fake '0' counts as if they were real data.

## Do
In src/pages/index.js, replace the single batched `Promise.all` (currently ~lines 49-78) with 7 independent fetches (movies/tv/books/albums/vinyl/workouts-stats/hot-takes), each firing immediately and each tracked by its OWN loading flag (default true, flipped false in that fetch's own resolution, independent of the other six). Read the current code carefully to establish the exact dependency mapping before implementing: which loading flag(s) gate each home-page section (the six stat blocks — most likely 1:1 with a single fetch each; 'The collection wall' mosaic — likely depends on ALL FIVE review/vinyl fetches since it samples across all of them; 'Latest takes' — the hot-takes fetch; the rail's workout-stats/shelf/hot-takes panels — verify shelfItems' actual source in the current code, do not assume). While a section's dependency flag(s) are still true, render a skeleton placeholder reserving the same layout footprint as its real content (no layout shift when it flips to real data) — build a small reusable skeleton component (or a couple of shape variants: stat-block skeleton, tile-grid skeleton, card-row skeleton) styled from existing Collection CSS custom properties, with a subtle pulse animation guarded by `prefers-reduced-motion`. Each section must reveal its real content as soon as ITS OWN dependency resolves — sections must NOT all wait for the slowest fetch. Do not touch the hero/NowPlaying section (unaffected — independent data flow).

## Done when
Loading the home page with artificially staggered/delayed fixture responses (e.g. via a test harness or manual network throttling per-endpoint) shows each section revealing independently AS SOON AS its own underlying fetch(es) resolve — e.g. if the movies endpoint resolves fast and the hot-takes endpoint resolves slow, the Movies stat block shows real data well before the 'Latest takes' panel does, rather than everything popping in together. No section ever shows '0', an empty grid, or 'No hot takes yet.' as a fake real-data value — only an explicit skeleton placeholder or real data. No visible layout shift when any section's skeleton is replaced by real content. `npm run lint` and `npm test` stay green; the new skeleton component(s) ship with a co-located test. `scripts/_visual-harness.mjs` is updated with a new FLOWS/PAGES entry capturing at least one section's loading state (e.g. via a fixture that delays one endpoint's response while others resolve immediately, to prove independent per-section reveal), per this repo's visual-check convention.
