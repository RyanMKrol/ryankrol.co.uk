## Do

The Collection redesign (T141-T169) is now complete and visually audited. Per this repo's root
`CLAUDE.md` "Documentation is part of every change" rule, do a final confirmation pass that the docs
fully and accurately describe the new architecture — much of this may already have been kept current
incrementally by individual tasks (T142 updated the theming section, T147 may have touched the
component list, etc.), so this task is a VERIFICATION + CLEANUP pass, not a from-scratch rewrite.

Read `CLAUDE.md` and `README.md` in full and confirm/fix:
- The "Theming is CSS-custom-property driven" convention bullet (and any other mention of themes)
  describes the single Collection palette, with NO remaining references to the retired
  AppearancePicker/multi-theme system (Cyberpunk/Terraria/Cotton Candy/Sunset/Ocean, font/mode
  pickers) — grep for "AppearancePicker", "cyberpunk", "terraria", "cotton-candy", "sunset theme",
  "ocean theme" across both docs and remove/rewrite any stale hits.
- The Components list (`CLAUDE.md`'s file map / Components bullet) includes every new shared
  component built in this redesign: `Nav`/`Header`, `Footer`, `Pill`, `PillGroup`, `SearchInput`,
  `Badge`, `StarRating` (now with `readOnly` mode), `PipMeter`, `CoverTile`, `StatBlock`,
  `Pagination`, and the `ReviewCard` variant system (poster-banner/horizontal-spine/2-col-square/
  perfume-card).
- `src/lib/pagination.js` is listed alongside the other `src/lib` entries.
- The "Matrix terminal theme easter egg" description is still accurate (it should be, since T141-T169
  deliberately preserved it unchanged — confirm, don't just assume).
- Any workflow/screenshot/description referencing the OLD site look (dark cyberpunk default, if any
  such description survived) is updated to describe the new light "paper" Collection aesthetic.
- `README.md`'s equivalent sections (app description, any screenshots-in-prose) are consistent with
  the above.

Do not describe the design_handoff folder itself in these docs (it's an external, gitignored-adjacent
reference the docs shouldn't depend on) — describe the RESULTING architecture as it now exists in the
codebase, the same way the rest of these docs describe every other feature.

## Done when

- `grep -rniE "appearancepicker|cyberpunk|terraria|cotton.candy|ocean theme|sunset theme" CLAUDE.md README.md`
  returns nothing (aside from this task's own historical mention, if you choose to leave one — not
  required).
- `CLAUDE.md`'s Components list and file map include every new shared component and `src/lib/pagination.js`.
- `CLAUDE.md`'s theming convention describes only the Collection token system + the Matrix easter egg.
- Both docs read as accurate, current descriptions of the app as it exists after this redesign — a
  new session reading only these two files should have zero surprises when it opens `src/`.
