# T076 — Slider input for the perfume rating on the add form

## Do

On `src/pages/reviews/perfumes/add.js`, replace the current `Rating (0-10)` input (currently
`<input type="number" min="0" max="10" step="1">` around line 130-142, bound to
`formData.rating`/`handleRatingChange`) with a slider (`<input type="range" min="0" max="10"
step="1">`), keeping the same `formData.rating`/`handleRatingChange` wiring — this is purely an
input-widget swap, not a change to validation, storage, or the 0-10 range (already validated
server-side by `validatePerfumeRating` in `src/lib/perfumes.js`, unchanged by this task). Display
the current numeric value next to the slider as it's dragged (a range input has no built-in value
label).

This is the perfume ADD form only, matching the original request — no other review type's rating
input (they use the shared `StarRating` component, out of scope here) and no other perfume page
(the edit page is out of scope for this task).

## Done when

- `/reviews/perfumes/add` in a running dev server shows a draggable slider (not a number spinner)
  for the rating field, ranging 0-10, with the current value visibly displayed as it's dragged.
- Submitting the form with the slider at various positions (e.g. 0, 5, 10) sends the corresponding
  integer `rating` value to `POST /api/reviews/perfumes/add`, and the review is saved successfully
  for values within 0-10.
- No file outside `src/pages/reviews/perfumes/add.js` was modified.
