# T097 — Remove the "Application notes" field from the perfume forms — UI

## Do

T096 removed backend handling of the `notes`/"Application notes" field. Remove the corresponding
UI from both `src/pages/reviews/perfumes/add.js` and `src/pages/reviews/perfumes/edit/[id].js`:
delete the "Application notes" label + textarea (bound to `formData.notes`) entirely, and stop
sending `notes` in the submitted payload from both forms.

**Do NOT remove the separate "Description" field** on these same forms (a different field,
`formData.description` or similar, placeholder "Share your thoughts about this perfume...") — only
the wear-specific "Application notes" textarea is being removed. Also do not touch the
"Application spots" checkboxes/spray-count UI (from T074/T075/T095) — those stay.

## Done when

- `/reviews/perfumes/add` no longer shows an "Application notes" field, and submitting the form no
  longer sends a `notes` value.
- `/reviews/perfumes/edit/[id]` no longer shows an "Application notes" field either, and submitting
  no longer sends `notes`.
- The "Description" field and the "Application spots" checkboxes/spray-count controls are
  unaffected on both forms.
- Manually verify in a running dev server (`npm run dev`): load both the add and edit perfume
  forms and confirm "Application notes" is gone from each while everything else still works.
