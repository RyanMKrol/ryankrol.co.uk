# T044 — Variant 2 — shared layout/nav shell + home page

## Do

T042 wrote three design briefs to `.harness/designs/T042-ui-rewrite-briefs.md`. Read the
**"## Variant 2"** section (only that section — the other two variants are separate,
independent tasks and must not be built here).

Build this variant's foundational, shared layout: a wrapper component (under
`src/components/v2/**`) implementing Variant 2's navigation paradigm and shared chrome
(header/footer/nav as described in the brief), and a home page at `src/pages/v2/index.js`
using it, styled per the brief (not by reusing the production site's `MatrixLayout`/existing
theme CSS variables — this variant gets its own look, per the brief). Link to the five content
areas (reviews, vinyl, workouts, listening, projects) from this home page/nav, even though most of
those routes don't exist yet under `/v2/**` (they land in later, dependent tasks
T046–T063) — it's fine if those links 404 for now.

This task's scope is strictly `src/pages/v2/**` and `src/components/v2/**` — do not modify
any file under `src/pages/reviews/**`, `src/pages/vinyl/**`, etc. (the live production pages) or
any file under `src/pages/api/**` (APIs are shared/reused unchanged by every variant).

## Done when

- `src/pages/v2/index.js` renders a home page using a shared layout wrapper from
  `src/components/v2/**`, implementing the navigation paradigm and shared chrome described in
  Variant 2's brief section.
- Loading `/v2` in a running dev server (`npm run dev`) shows this variant's home page with
  working navigation links toward the five content areas (reviews/vinyl/workouts/listening/projects
  under `/v2/**`), even if those linked pages don't exist yet.
- No file outside `src/pages/v2/**` or `src/components/v2/**` was modified.
