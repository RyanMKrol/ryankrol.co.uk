## Do

Add a new page `src/pages/reviews/albums/backfill.js`, the albums counterpart of the shared bulk-backfill pattern (see the movies bulk-backfill task's spec for the full shape — mirror it, swapping the albums-specific endpoints/fields below). Read this task's `design` field first — albums have an extra two-step confirm that the other three types don't.

1. On mount, `fetch('/api/reviews/albums')`, filter with `needsAlbumBackfill` from `src/lib/backfillEligibility.js` (only albums missing `lastfm.mbid`).
2. Single page-level password field at the top, reused by every row's confirm.
3. Render `<BulkBackfillList>` with the filtered albums as `items`, `pageSize=15`, and:
   - `onSearch`: `GET /api/lastfm/album-search?query=<title or artist>` (mirror `handleBackfillSearch` in `src/pages/reviews/albums/edit/[id].js:79-85`, reading `.results` off the response body), attaching `error.retryAfterSeconds` on a 429.
   - `renderCandidate`/`getCandidateKey`: reuse the render JSX from `src/pages/reviews/albums/edit/[id].js:263-282` (image thumbnail, title, artist).
   - `onConfirm(album, candidate)`: mirror `handleBackfillConfirm` in `src/pages/reviews/albums/edit/[id].js:87-117` — first `GET /api/lastfm/album-info` (by `mbid` if the candidate has one, else by `artist`+`album`), map the result into the `lastfm` object shape used there, THEN POST to `/api/reviews/albums/update` with `title: album.title`, `artist: album.artist`, `rating: album.rating`, `highlights: album.highlights`, `originalId: album.id`, `password`, and `lastfm: <the mapped object>` — the update endpoint requires the full field set every time (see `src/pages/api/reviews/albums/update.js`), not just the new metadata. Surface an error on that row if EITHER the album-info fetch or the update POST fails, without affecting other rows.
4. Add a link/button from `src/pages/reviews/albums/edit.js` to `/reviews/albums/backfill`.
5. Empty-state message when there's nothing to backfill.

## Done when

Same verification shape as the movies bulk-backfill task, applied to albums: link from `/reviews/albums/edit` works; `/reviews/albums/backfill` lists only albums missing `lastfm.mbid`, paginated at 15/page; password entered once; at least one row shows a multi-candidate picker, confirming a candidate does the two-step album-info-then-update flow and results in that album's `lastfm` object being populated via `/api/reviews/albums/update` while leaving `title`/`artist`/`rating`/`highlights` unchanged; Network tab shows sequential, spaced-out `/api/lastfm/album-search` requests (the follow-up `/api/lastfm/album-info` calls happen only after a row's candidate is actually confirmed, not up front for every candidate). `npm run lint` and `npm run build` both succeed.
