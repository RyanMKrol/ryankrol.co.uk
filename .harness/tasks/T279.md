## Do

- Add a new pure helper file `src/lib/reviewText.js` exporting `truncateReviewText(text, maxLength = 260)`: if `text.length <= maxLength`, return `{ text, truncated: false }` unchanged. Otherwise cut at the LAST whitespace character at-or-before index `maxLength` (falling back to a hard cut exactly at `maxLength` if there is no whitespace in that range, e.g. one long unbroken token) and return `{ text: '<cut text>…', truncated: true }` (a single ellipsis character `…`, not three periods).
- Add `src/lib/reviewText.test.js` covering: text shorter than `maxLength` returned unchanged with `truncated:false`; text longer than `maxLength` truncated at a word boundary with `truncated:true` and a trailing `…`; an empty string; a string with no whitespace before `maxLength` (hard-cut fallback path); a custom `maxLength` override (don't hardcode 260 as the only tested value).
- In `src/components/ReviewCard.js`: add `useState` to the existing React import (there is currently no React hook import at all — the file only imports `CoverTile`/`gradientForKey`, `StarRating`, `formatReviewDate`, lines 1-3) and add `import { truncateReviewText } from '../lib/reviewText';`.
- In `SpineCoverCard` (lines 45-73) ONLY: add `const [expanded, setExpanded] = useState(false);`, then `const fullText = getThoughts();` and `const { text: previewText, truncated } = truncateReviewText(fullText, 260);`. Replace the existing block `{getThoughts() && (<p className="spine-cover-snippet">{getThoughts()}</p>)}` (lines 67-69) with: a `<p className="spine-cover-snippet">{expanded ? fullText : previewText}</p>` rendered whenever `fullText` is non-empty, immediately followed (still inside `.spine-cover-body`) by `<button type="button" className="spine-cover-expand-btn" onClick={() => setExpanded((e) => !e)}>{expanded ? 'Show less' : 'Read more'}</button>` rendered ONLY when `truncated` is true.
- Do NOT modify `SquareCoverCard`, the `poster-banner` branch, or the default-variant branch later in the same file — this change is scoped to the `SpineCoverCard` sub-component only.
- In `src/styles/globals.css`, add a new `.spine-cover-expand-btn` rule near the existing `.spine-cover-snippet` rule (around lines 304-309): `background: none; border: none; padding: 0; align-self: flex-start; cursor: pointer; font-family: var(--font-mono); font-size: 0.8rem; color: var(--color-accent-secondary); text-decoration: underline;` (books' existing accent color — `--color-accent-secondary` resolves to `--accent-books`).
- No change needed to `src/pages/reviews/books/index.js`, `src/pages/reviews/books/edit.js`, or any API route — this is a presentational change inside the shared `ReviewCard` component only; both books pages pick up the truncate+expand behavior automatically since both already render `styleVariant="spine-cover"`.

## Done when

- `npm run lint`, `npm test` (including the new `src/lib/reviewText.test.js`), and `npm run build` all pass.
- On `npm run dev`, `/reviews/books`: a book with a short review (under 260 chars) renders unchanged with no button. A book with a long review (e.g. 'Ultra-Processed People', ~2145 chars) renders truncated to ~260 characters ending in `…` with a 'Read more' button beneath it; clicking it reveals the full review text in place and the button now reads 'Show less'; clicking again re-collapses it.
- With the existing masonry columns on `/reviews/books` (2 columns above 700px), the 'Ultra-Processed People' card's collapsed rendered height is now comparable to a typical card's height (no longer a dramatic outlier), and the originally-reported symptom — one column ending with several extra cards past where the other column ran out, on page 1 sorted by date descending — no longer reproduces.
- `/reviews/books/edit` shows the same truncate+expand behavior (it reuses the same `SpineCoverCard`).
- Albums (`SquareCoverCard`) and movies/TV (poster-banner) review cards are visually unchanged — this task did not touch those code paths.
