## Do

Enrich the "Backfill from TMDB/Google Books/Last.fm" re-match modal (`src/components/MetadataBackfillModal.js`, a generic modal driven by a `renderResult(result)` render-prop supplied by each caller) on ALL FOUR review-type edit pages so results are shown as rich cards (thumbnail/cover image + title/year + a secondary line) instead of the current bare `<strong>title</strong> (year)` text row. This is the same underlying fix applied in 4 places — `MetadataBackfillModal.js` itself needs NO changes (do not edit it), only the `renderResult` prop each edit page passes in.

The add-flow search components already do exactly the rich-card layout to mirror — read them for the pattern (image-or-placeholder + title/year + secondary text), all under a `.form-button`/border-card convention already used by this codebase:
- `src/components/TmdbSearch.js` (movie/tv add flow, ~lines 90-115) — renders `tmdbPosterUrl(r.posterPath)` as a 60x90 `<img>` (with a placeholder `<div>` when there's no poster), title + year (`r.date.slice(0,4)`), and `r.overview.slice(0, 160)` truncated with an ellipsis when the overview is longer.
- `src/components/BookSearch.js` (book add flow, ~lines 66-136) — renders a cover image via a `getCoverSrc(r)` helper (prefers `r.coverUrl`, falls back to `bookCoverUrl(r.coverId,'S')` from `src/lib/openlibrary.js`) as a 40x60 `<img>` with a placeholder fallback, title + `firstPublishedYear`, and `r.authors.join(', ')`.
- `src/components/LastfmAlbumSearch.js` (album/vinyl add flow, ~lines 118-145) — renders `r.image` as a 50x50 `<img>` with a placeholder fallback, title, and artist.

Update the `renderResult` prop passed to `MetadataBackfillModal` in each of these 4 files to build an equivalent card using fields already present on each API's mapped result shape — no API or lib changes are needed, every field used below already exists on the objects returned by that page's `onSearch`/`handleBackfillSearch`:

1. `src/pages/reviews/movies/edit/[id].js` (`renderResult` currently ~lines 233-238) and `src/pages/reviews/tv/edit/[id].js` (`renderResult` currently ~lines 233-238) — `result` is a `mapTmdbResult` object: `{ tmdbId, mediaType, title, overview, posterPath, date }`. Both files already `import { tmdbPosterUrl } from '../../../../lib/tmdb'` for the confirmed-poster preview rendered below the modal — reuse that same import inside `renderResult`. Show: poster via `tmdbPosterUrl(result.posterPath)` (~60x90, placeholder `<div>` if null), `<strong>{result.title}</strong>` + year from `result.date`, and a truncated `result.overview` (~160 chars with ellipsis, matching `TmdbSearch.js`'s truncation).

2. `src/pages/reviews/books/edit/[id].js` (`renderResult` currently ~lines 251-259) — `result` is a `mapGoogleBooksResult` object: `{ source, volumeId, title, authors, firstPublishedYear, coverUrl, isbn, subjects, pageCount, publisher }`. This route only ever returns Google Books results (see the informational note in the companion needs-human task about the unused `?provider=` param — not in scope here), so use `result.coverUrl` directly for the cover (placeholder `<div>` if null — no need for `BookSearch.js`'s `coverId`/Open Library fallback since this result shape never has `coverId`). Show: cover, `<strong>{result.title}</strong>` + `firstPublishedYear`, and `result.authors.join(', ')` (already partially rendered here — keep it, just add the cover image alongside).

3. `src/pages/reviews/albums/edit/[id].js` (`renderResult` currently ~lines 266-271) — `result` is a `mapAlbumSearchResult` object: `{ title, artist, mbid, url, image }`. Show: `result.image` as a small cover (placeholder `<div>` if null), `<strong>{result.title}</strong>`, and `result.artist` (already partially rendered here — keep it, just add the cover image alongside).

Add whatever minimal CSS is needed for the thumbnail + text layout (flex row, fixed-size image or placeholder, text column) — either inline `<style jsx>` scoped per page file (simplest, matches how `TmdbSearch.js`/`BookSearch.js`/`LastfmAlbumSearch.js` each keep their own scoped `<style jsx>`), or, if the 4 call sites end up needing near-identical markup+CSS, factor a small shared presentational component (e.g. `src/components/BackfillResultCard.js`) used by all 4 `renderResult` props instead of duplicating styles 4 times — either approach is acceptable; prefer whichever keeps the diff smallest and avoids CSS duplication. Match the modal's existing look (dark theme via CSS custom properties like `var(--color-border, #333)`, `var(--color-accent)` — see `MetadataBackfillModal.js`'s own `<style jsx>` block for the vars already in play) — never hardcode colours, per this repo's theming convention.

Do not change `MetadataBackfillModal.js`'s own markup/props/behaviour, the `onSearch`/`onConfirm` wiring in any edit page, or any API route — this is a presentational-only change to what each `renderResult` prop returns.

## Done when

All 4 edit pages' `MetadataBackfillModal` `renderResult` props (`src/pages/reviews/movies/edit/[id].js`, `src/pages/reviews/tv/edit/[id].js`, `src/pages/reviews/books/edit/[id].js`, `src/pages/reviews/albums/edit/[id].js`) render a thumbnail/cover image (or a placeholder when the result has none) alongside the title/year/secondary text, instead of the current bare text-only row. `npm run lint` and `npm run build` both succeed.

Manual verify (`verify: ["run-app"]` — this repo has no screenshot-diffing tooling): run `npm run dev`, open an existing movie or TV review's edit page, click "Backfill from TMDB", search a title, and confirm the "Select a match" list shows poster thumbnails (or placeholders) next to each result the same way `reviews/movies/add` already does. Repeat for a book review edit page ("Backfill from Google Books") and an album review edit page ("Backfill from Last.fm") to confirm covers/placeholders render there too, and confirm nothing else in the modal (radio selection, Confirm/Cancel, Escape-to-close) regressed.
