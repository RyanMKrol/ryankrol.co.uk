## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Exercise detail" screen section (the
most data-dense screen) and open `/Users/ryankrol/Downloads/design_handoff/renders/2c.png` (desktop)
and `/Users/ryankrol/Downloads/design_handoff/renders/4d.png` (mobile).

Rebuild `src/pages/exercises/[exerciseName].js`:
- "← back to workouts" link, exercise name as title, and the existing 3-months/1-year/all-time
  toggle rebuilt as a `PillGroup` (from T143) — keep its current underlying behavior/data-fetching
  exactly as-is, this is a restyle only.
- 5 `StatBlock`s: sessions, all-time 1RM, max weight, total volume, 1RM progress % — the design
  accent-fills the "all-time 1RM" and "1RM progress" cards specifically (coral and teal/green
  respectively per the render) while the other 3 stay neutral/bordered.
- Restyle the existing charts (`src/components/ExerciseProgressCharts.js` and/or
  `CardioProgressCharts.js`, whichever this page renders) via `src/hooks/useChartTheme.js` — do NOT
  hardcode chart colors inline, extend `useChartTheme`'s returned palette so it reflects the new
  Collection tokens: the "Estimated 1RM progress" area chart uses the coral accent with a gradient
  fill, "Session volume" bar chart uses indigo with the LAST bar highlighted in grape (a one-off
  per-bar color override, not a global theme change), "Max weight progress" area chart uses
  marigold. Check how `useChartTheme` currently exposes colors to chart configs before changing its
  shape — keep it a single source of truth other charts also consume (e.g. the workout/album charts
  elsewhere in the app, if any) rather than hardcoding these 3 colors directly in this page.
- A dark "Progress insights" panel (dark ink background, light text) summarizing 1RM
  improvement/max-weight change/avg volume + a trend line of text, per the render.
- A "Recent sessions" table: Date/Split · Volume · Max Wt · Est. 1RM columns, reusing whatever
  session data this page already fetches.

## Done when

- The exercise detail page shows the back link, restyled time-range pills (with unchanged
  underlying behavior), 5 stat blocks (2 accent-filled), 3 charts restyled to the new palette via
  `useChartTheme`, the dark insights panel, and the recent-sessions table.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load a real exercise detail page, toggle between
  3-months/1-year/all and confirm the charts still update correctly (behavior unchanged, only
  restyled), and confirm chart colors match coral/indigo+grape-last-bar/marigold. Detailed
  pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T168.
