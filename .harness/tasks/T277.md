## Do

- Import `MasonryColumns` (`src/components/MasonryColumns.js`) and `useResponsiveColumnCount` (`src/hooks/useResponsiveColumnCount.js`) into `src/pages/reviews/albums/index.js`, mirroring the import lines in `src/pages/reviews/books/index.js:10-11`.
- Add `const columnCount = useResponsiveColumnCount(2, 700);` inside `Albums()` alongside the other page state (mirror `src/pages/reviews/books/index.js:46`).
- Replace the plain grid block at `src/pages/reviews/albums/index.js:171-181` — currently `<div className="square-cover-grid">{pagedAlbums.map((album, index) => <ReviewCard .../>)}</div>` — with a `<MasonryColumns>` call: `items={pagedAlbums}`, `columnCount={columnCount}`, `className="square-cover-grid"`, `columnClassName="square-cover-grid-col"`, and `renderItem={(album, index) => <ReviewCard key={...} item={album} type="album" styleVariant="square-cover" gradient={albumGradients[index]} />}` — use the `index` MasonryColumns' `renderItem` callback supplies (the true original pre-distribution index) for `albumGradients[index]` and the key, exactly like the current `.map` callback does, so gradients/keys still line up per album after the switch.
- In `src/styles/globals.css`, change the `.square-cover-grid` rule (lines 389-399) from `display: grid; grid-template-columns: repeat(2, 1fr);` (plus its `@media (max-width: 700px) { grid-template-columns: 1fr; }` override) to a flex row container: `display: flex; gap: 1.5rem;`. Add a new `.square-cover-grid-col` rule: `display: flex; flex-direction: column; gap: 1.5rem; flex: 1 1 0; min-width: 0;` — mirror `.poster-banner-grid` / `.poster-banner-grid-col` at `src/styles/globals.css:241-252` exactly. Remove the now-dead `@media` block entirely — column-count collapse is handled by the `useResponsiveColumnCount` JS hook on every other page using this mechanism, not by CSS.
- Do not change `Albums()`'s existing sort/filter/pagination logic (`sortBy` state at line 40, the sort branches at lines 76-92, the `paginate(...)` call) — this is a pure layout-mechanism swap, nothing else about the page's behavior should change.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass.
- On `npm run dev`, `/reviews/albums` renders as two masonry columns above a 700px viewport width and collapses to one column below it, with cards sized to their own content (a shorter card no longer stretches to match a taller neighbor in the same visual row).
- Cycling every `sortBy` option (title, title-desc, score, score-asc, date, date-asc) still produces strict left-to-right, top-to-bottom reading order across the two columns — same guarantee already verified on `/reviews/books`.
- Each card's gradient/cover assignment (`albumGradients`) still lines up with the correct album after the switch to `MasonryColumns`' index (no swapped covers).
