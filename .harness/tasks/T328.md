## Overview
The Edit-button header strip on all 5 review edit list pages (movies/tv/books/albums/perfumes) still visibly floats above its card as two separate boxes — T306 authored the shared CSS with the exact gap/mismatched-corner defect it was meant to prevent, and T307-T312 only wired class names into pages without ever touching the header CSS itself, so the bug rode through the whole sequence untouched.

## Do
In src/styles/globals.css: make `.review-edit-card` the single unified box (add border + border-radius + background + overflow:hidden), remove `.review-edit-header`'s `margin-bottom` and its own `border-radius` (parent's overflow:hidden now clips it), keep the header's `border-bottom` as an internal divider, and add a scoped override (`.review-edit-card .spine-cover-card, .review-edit-card .square-cover-card, .review-edit-card .poster-banner-card, .review-edit-card .perfume-v6-card { border: none; border-radius: 0; box-shadow: none; }`) so the nested variant card stops drawing its own competing border — scoped so it does NOT affect these classes on the public (non-edit) list pages. In scripts/_visual-harness.mjs, add PAGES entries for all 5 `/reviews/<type>/edit` routes (none exist today) so this fix has an actual screenshot target.

## Done when
On EACH of the 5 review edit list pages (/reviews/movies/edit, /reviews/tv/edit, /reviews/books/edit, /reviews/albums/edit, /reviews/perfumes/edit), the Edit-button header strip and the card content below it read as ONE continuous bordered/rounded box with no visible gap and no doubled/mismatched border or corner-radius seam between them — confirmed by actually RUNNING `node scripts/visual-check.mjs` (after adding the 5 new PAGES entries) and LOOKING at all 5 resulting screenshots in scripts/visual-out/, not by inspecting CSS source alone (checking that `margin-bottom` bytes are gone is NOT sufficient — this exact task previously 'passed' a code-only DoD in T306 while remaining visually broken through T307-T312; this DoD is not satisfied until the screenshots are actually viewed and confirmed to show one joined card on all 5 pages). Public (non-edit) review list pages — /reviews/movies, /reviews/tv, /reviews/books, /reviews/albums, /reviews/perfumes — must be re-screenshotted too and show NO change (their card borders must be unaffected, since the override is scoped to `.review-edit-card` descendants only).
