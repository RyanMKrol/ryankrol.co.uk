## Do

`src/pages/index.js` has zero reference to hot takes anywhere, so the homepage currently surfaces them nowhere. The one section that looks superficially relevant, "Latest takes" (the `latestTakes` useMemo at lines 101-119, rendered at lines 180-201 as `.home-latest`/`.home-latest-card`), is a review-only aggregation: it merges `movies`/`tv`/`books`/`albums` state, expects a `rating` (0-5, rendered as stars) and a cover image (`thumbnail` or TMDB `posterPath`, falling back to a generated gradient via `assignGradients`), and remaps albums' `highlights` field to `review_text`. A hot take (`{ id, text, date }` from the `HotTakes` DynamoDB table, served by `GET /api/hot-takes` which already returns items pre-sorted newest-first via `sortByDateDesc` in `src/lib/hotTakes.js`) has no rating and no cover art at all — it is structurally incompatible with that section's shape. Do NOT modify the `latestTakes` useMemo or its rendering block in any way; this task adds a separate, new section instead.

The homepage's `home-lower` section (`src/pages/index.js` ~line 179, CSS grid `.home-lower` at `src/styles/globals.css` line 3079, `grid-template-columns: 1.6fr 1fr`) currently has two children: `.home-latest` (the 1.6fr column, described above) and `.home-rail` (the 1fr column, a `flex-direction: column` stack of two existing panels — `.home-gym-panel` at `globals.css` line 3140 and `.home-shelf-panel` at line 3249, each `border-radius: var(--radius-card)`, `padding: 1.25rem`, with a `background` colour and a small-caps title row). Add a THIRD panel to `.home-rail`, styled consistently with its two existing siblings, that gives hot takes a real content-based presence (not just a link):

1. In `src/pages/index.js`: add a new `hotTakes` state (`useState([])`) and fetch `/api/hot-takes` inside the existing `fetchAll` function's `Promise.all` (alongside the existing `fetchJson('/api/reviews/movies')` etc. calls at lines 56-63), then `setHotTakes(hotTakesData || [])`. Import `formatReviewDate` from `../lib/dateFormat` (already used the same way by `src/pages/hot-takes/index.js`) to format each take's `date` field for display.
2. Compute `const latestHotTakes = hotTakes.slice(0, 3)` (no need to re-sort — the API already returns them newest-first).
3. In the JSX, inside `<div className="home-rail">` (~line 207), add a new panel as the LAST child (after the existing `.home-shelf-panel` block, so the two established rail panels keep their current relative order and this is a pure addition):
```jsx
<div className="home-hot-takes-panel">
  <div className="home-hot-takes-panel-header">
    <Link href="/hot-takes" className="home-hot-takes-panel-title home-hot-takes-panel-link">
      Hot takes
    </Link>
  </div>
  {latestHotTakes.length === 0 && (
    <p className="home-hot-takes-empty">No hot takes yet.</p>
  )}
  {latestHotTakes.map((take) => (
    <div key={take.id} className="home-hot-takes-item">
      <span className="home-hot-takes-text">{take.text}</span>
      <span className="home-hot-takes-date">{formatReviewDate(take.date)}</span>
    </div>
  ))}
  {latestHotTakes.length > 0 && (
    <Link href="/hot-takes" className="home-hot-takes-viewall">View all &rarr;</Link>
  )}
</div>
```
(`Link` is already imported at the top of `index.js`.) Adjust exact class names/markup only if needed for valid JSX/styling — the important contract is: a linked panel title going to `/hot-takes`, a rendered preview of up to 3 most-recent hot takes (text + formatted date, no rating/cover art — do not fabricate either), a sensible empty state when there are none, and a "view all" link to `/hot-takes` when there is at least one.
4. In `src/styles/globals.css`, add `.home-hot-takes-panel`, `.home-hot-takes-panel-header`, `.home-hot-takes-panel-title` (+ `.home-hot-takes-panel-title.home-hot-takes-panel-link` hover-underline, mirroring `.home-shelf-panel-title.home-shelf-panel-link` at line 3273-3281), `.home-hot-takes-item`, `.home-hot-takes-text`, `.home-hot-takes-date`, `.home-hot-takes-empty`, and `.home-hot-takes-viewall` rules near the existing `.home-gym-panel`/`.home-shelf-panel` rules (~line 3140-3320). Match the established visual language: `border-radius: var(--radius-card)`, `padding: 1.25rem` on the outer panel, a small-caps `font-family: var(--font-mono); font-size: 0.75rem; letter-spacing: 0.06em; text-transform: uppercase;` panel title (as `.home-gym-panel-title`/`.home-shelf-panel-title` both use), and a background colour distinct from the other two panels (e.g. reuse the new `--accent-hot-takes` token if the header-nav task has already landed it in `globals.css`'s `:root`, i.e. `background: var(--accent-hot-takes);` — if that variable doesn't exist yet when you do this work, add it yourself the same way: a new `--accent-hot-takes` token in `:root` alongside the other `--accent-*` tokens). No new mobile media-query rule should be needed: `.home-lower` already collapses to a single column under the existing `@media (max-width: ...)` block (`globals.css` ~line 3339, `.home-lower { grid-template-columns: 1fr; }`), which will naturally stack the new panel with its siblings — verify this holds rather than assuming it.
5. Docs (same commit, per this repo's Definition of Done): update `CLAUDE.md`'s file map row for `src/pages/index.js` to mention the new homepage section (alongside its existing description of the hero, stats, collection wall, and link grid), e.g. append something like "a 'Hot takes' preview panel in the rail column showing the 3 most recent hot takes with a link to the full `/hot-takes` list".

## Done when

- `src/pages/index.js` fetches `/api/hot-takes` on load and renders a new panel inside `.home-rail` showing up to 3 of the most recent hot takes (plain text + formatted date, no rating stars, no cover art/gradient — confirm by reading the diff that this panel does NOT reuse `assignGradients` or any rating markup), each with a link to `/hot-takes` (both the panel title and a "view all"-style link).
- The existing `latestTakes` useMemo (lines 101-119) and its rendering block (lines 180-201) are byte-for-byte unchanged — confirm via `git diff` that no lines inside that block were touched.
- `src/styles/globals.css` has new rules for the panel (`.home-hot-takes-panel` and its children) that visually match the existing `.home-gym-panel`/`.home-shelf-panel` siblings (same border-radius/padding/title-typography conventions), with its own distinct background colour.
- `CLAUDE.md`'s file map row for `src/pages/index.js` mentions the new hot-takes homepage panel.
- Manual verification (required — `verify: ["run-app"]`): run `npm run dev`, open `/` in a browser. Confirm a new panel appears in the right-hand rail column below (or alongside) the existing gym-stats and "on the shelf" panels, showing the latest hot takes with dates and a working link to `/hot-takes`; clicking through actually lands on `/hot-takes` and shows the same (or a superset of the same) takes. Resize to a mobile-width viewport (~375-414px) and confirm the panel stacks sensibly with its siblings rather than overflowing or breaking layout. If the `HotTakes` table currently has zero rows (check via `/hot-takes` directly), also confirm the panel's empty state reads sensibly rather than rendering a blank gap.
- `npm run lint`, `npm test`, and `npm run build` all succeed with no new errors or warnings beyond the pre-existing tolerated `react-hooks/exhaustive-deps` warnings.
