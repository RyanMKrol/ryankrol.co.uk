## Do

Add a single-select ownership filter (an 'all' / 'Sample' / 'Travel size' / 'Full bottle' tab row) to /reviews/perfumes, next to the existing sort control. Owner decision (confirmed): single-select tab/radio behaviour, NOT multi-select OR — only one ownership value (or 'all') is active as a filter at a time.

Chosen UI mechanism: reuse the existing `PillGroup` component (src/components/PillGroup.js) exactly as it's already used for the page's sort control (single-select via `value`/`onChange`, one active pill at a time) — do NOT build a custom toggle/deselect interaction (e.g. 'click the active pill again to clear'), since that would require changing Pill/PillGroup's onClick contract, which is shared by the sort pills, DateRangeFilter, etc., elsewhere in the codebase. Instead give the filter row an explicit 'all' option so clearing back to the unfiltered view is just clicking 'all' — this is the standard tab-like interaction PillGroup already supports everywhere else in the app (e.g. workouts' DateRangeFilter), so it's the lowest-risk, most consistent choice.

Steps:
1. In src/components/PerfumeCharacteristics.js:4, `OWNERSHIP_OPTIONS` (the canonical 3-value list: 'Sample', 'Travel size', 'Full bottle', currently only consumed internally by `OwnershipPicker`) is not exported. Add `export` to that const so it can be imported and reused, avoiding a second hand-maintained copy of the same 3 values drifting out of sync.
2. In src/pages/reviews/perfumes/index.js:
   - Import `OWNERSHIP_OPTIONS` from '../../../components/PerfumeCharacteristics'.
   - Add a module-level `OWNERSHIP_FILTER_OPTIONS = [{ value: 'all', label: 'all' }, ...OWNERSHIP_OPTIONS.map(o => ({ value: o.value, label: o.label }))]` alongside the existing `SORT_OPTIONS` (line 9-13).
   - Add `const [ownershipFilter, setOwnershipFilter] = useState('all');` next to the existing `sortBy` state (line 19).
   - In the filtering `useEffect` (lines 42-60), extend the `filtered = perfumes.filter(...)` predicate (line 43-45) to also require `(ownershipFilter === 'all' || perfume.ownership === ownershipFilter)`, ANDed with the existing title-search match (perfumes with no `ownership` field at all — legacy rows per src/scripts/migratePerfumeOwnership.js — correctly fall out of any non-'all' filter, which is expected). Add `ownershipFilter` to the effect's dependency array (line 60).
   - Render a second `PillGroup` in the `.collection-review-controls` div (around line 95-106), after the existing sort `PillGroup`, wired to `options={OWNERSHIP_FILTER_OPTIONS}`, `value={ownershipFilter}`, `onChange={setOwnershipFilter}`, and `accentColor="var(--accent-perfumes)"` (this CSS var already exists in globals.css and is used for the perfume ownership Badge in Variant6Hybrid.js:54, so the filter pill's active-fill colour will visually match the ownership badge shown on each card).
   - The existing 'Found N perfumes' count block (lines 109-113, currently gated on `searchTerm`) should also appear when a non-'all' ownership filter is active with no text search, so the owner gets a result count either way — broaden its condition to `(searchTerm || ownershipFilter !== 'all')`.

## Done when

npm run dev, visit /reviews/perfumes. Confirm: (1) an 'all / Sample / Travel size / Full bottle' pill row renders next to the sort pills; (2) clicking 'Sample' shows only perfumes with ownership === 'Sample' and highlights that pill active; (3) clicking 'Travel size' or 'Full bottle' likewise narrows to just that value and only one pill is ever active at once; (4) clicking 'all' restores the full unfiltered (by ownership) list; (5) with an ownership filter active, typing into the search box further narrows results to only items matching BOTH the ownership filter AND the text search (AND, not OR) — confirm by picking an ownership value with 2+ matches, then typing a search term that matches only one of them, and seeing the list narrow to that one; (6) the 'Found N perfumes' count updates correctly whether the narrowing came from search, ownership filter, or both; (7) perfumes with no ownership field saved (if any exist) simply don't appear under any specific ownership filter but do appear under 'all'; (8) npm run lint and npm run build both stay green.
