## Do

In `src/pages/projects/index.js`, the top row of each project card (`.project-card-top`, ~lines 225-237) currently renders the repo name on the left and a single `<span className="project-card-stars">★ {repo.stars || 0}</span>` on the right (lines 234-236). The commit count is currently rendered separately, buried in the meta row at lines 263-267 as plain text: `{typeof repo.commitCount === 'number' && (<span className="project-card-commits">{repo.commitCount} {repo.commitCount === 1 ? 'commit' : 'commits'}</span>)}`.

1. Replace the standalone `project-card-stars` span (lines 234-236) with a new wrapper `<div className="project-card-top-right">` containing, in order: the existing stars span unchanged, then a new sibling `<span className="project-card-commits-badge">⎇ {repo.commitCount || 0}</span>` rendered only `{typeof repo.commitCount === 'number' && (...)}` (same guard style already used at line 263). `.project-card-top`'s two direct children become the name link and this new wrapper div.
2. Remove the now-redundant bottom-of-card commit block at lines 263-267 from `.project-card-meta-row` entirely — it is promoted to the top row, so leaving it would duplicate the same number twice on one card.
3. In `src/styles/globals.css`, near the existing `.project-card-stars` rule (~line 1799), add:
   `.project-card-top-right { display: flex; align-items: center; gap: 0.75rem; flex-shrink: 0; }`
   and a new `.project-card-commits-badge` rule styled identically to `.project-card-stars`: `font-family: var(--font-mono); font-size: 0.875rem; color: var(--color-ink-soft); white-space: nowrap;`
4. Use the glyph `⎇` (U+2387) for the commit badge, matching the single-Unicode-glyph convention already used for `★` (stars) and `⌕` (SearchInput.js's search icon) — do not introduce an SVG or emoji icon, this codebase has neither. Visually check via the dev server that `⎇` renders cleanly in the deployed font stack; if it renders as a missing-glyph box, substitute a plain filled circle `●` (U+25CF) instead.
5. Leave `SORT_FIELDS` (lines 11-15) and every other meta-row item (language, updated, forks, archived) untouched.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass.
- Running `npm run dev` and opening `/projects`: each card's top-right corner shows both the star count and the commit count together (e.g. `★ 12` next to `⎇ 1142`), visually confirmed in the browser.
- The bottom-of-card meta row no longer shows a separate 'N commits' text entry for any card — the commit number now appears exactly once, top-right.
- A card whose `commitCount` is not a number (undefined/null) shows no commit glyph in the top-right area, matching the previous conditional guard's behavior.
