## Overview
Land the placement variant chosen in top-of-mind-note-review as the one permanent Top-of-Mind homepage section, removing the other 4 variants and the `?tom=` query-param switch entirely, and replace the dummy markdown with a real fetch of the saved note from GET /api/top-of-mind (added in top-of-mind-note-storage). The section must render read-only on the homepage (editing stays on /top-of-mind/edit) and must be omitted from the DOM entirely — not shown empty/placeholder — whenever there is no saved note yet OR the saved note is expired (more than 90 days since its `updatedAt`, per src/lib/topOfMind.js's `isExpired`, added in top-of-mind-note-storage).

## Do
In src/pages/index.js: remove the `tom` query-param branching and the 4 non-chosen variant blocks added in top-of-mind-note-chooser, keeping only the chosen variant's markup/CSS placement (consume the winning variant number recorded by top-of-mind-note-review). Replace the hardcoded dummy markdown with a real fetch: add a `fetchJson('/api/top-of-mind')` call alongside this page's existing `useEffect` fetches (movies/tv/books/albums/vinyl/workoutStats/hotTakes, ~lines 64-75), storing `{ text, updatedAt }` in state with its own loading flag (matching the existing per-section loading-flag convention, e.g. `topOfMindLoading`). Compute expiry client-side via `isExpired(updatedAt)` imported from src/lib/topOfMind.js (added in top-of-mind-note-storage). Render the chosen variant's section ONLY when not loading AND a note exists AND it is not expired — otherwise render nothing at all for that section (no empty box, no skeleton once loaded-and-absent; a loading skeleton while the fetch is in flight is fine, matching this page's other sections' loading-skeleton pattern e.g. `GymPanelStatsSkeleton`/`ListRowSkeleton` from src/components/HomeSkeleton.js — reuse or add a comparable skeleton if needed, but the resting empty/expired state renders literally nothing). Render the real note's markdown via the existing `Markdown` component. Remove the now-dead `.tom-variant-*` CSS for the 4 rejected variants from src/styles/globals.css, keeping only the chosen one's styles (rename/tidy the surviving class name if it was written as `.tom-variant-N` to something permanent like `.home-top-of-mind-panel`, consistent with this file's other `.home-*` panel class names). Update `scripts/_visual-harness.mjs`'s fixtures/PAGES/FLOWS: remove the per-variant `?tom=` entries added in the chooser task and add fixture coverage for GET /api/top-of-mind (both a populated-and-fresh case showing the section, and an empty/expired case showing it hidden) so the homepage's visual-check capture reflects the real wired behavior going forward.

## Done when
src/pages/index.js contains exactly one permanent, unconditional (data-dependent) Top-of-Mind section matching the variant chosen in top-of-mind-note-review — no `?tom=` query-param branching or rejected-variant markup remains anywhere in the file. The section fetches real data from GET /api/top-of-mind and renders it via the Markdown component. It is completely absent from the rendered DOM (not an empty/placeholder box) when no note has ever been saved, and equally absent once the saved note is more than 90 days past its updatedAt per isExpired — verified by testing both a fresh-note fixture and an expired/absent-note fixture through the visual-check harness. grep confirms no leftover `tom-variant-`/`?tom=` references in src/. npm run lint / npm test / npm run build stay green. Visual verification (page-layer change, auto-fires) shows the homepage with the section visible (fresh-content fixture) and absent (expired/no-content fixture), with no layout regression to any other homepage section.
