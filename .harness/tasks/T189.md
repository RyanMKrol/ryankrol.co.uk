## Do

T154's rebuild of `/reviews/perfumes` (`src/pages/reviews/perfumes/index.js`) shipped two elements to real site visitors that the owner has confirmed were meant only as annotations inside the design handoff document, not literal UI copy to render. The owner's own words: 'that's more of a design note. It's not meant to actually be displayed on the page.' Remove both.

The design brief this was built from is `/Users/ryankrol/Downloads/design_handoff/README.md`, under its `### Perfumes (`3d` / `4i`)` section, which reads verbatim:

> **Rated 0–10, not 0–5.** Deliberately does NOT use stars. Shows a large numeric score (e.g. `7.0`) + a **10-dot pip meter** (filled grape / empty lilac). Designer byline + type chip (EDP/EDT). The page header states the scale rule explicitly so it never reads as a /5 rating. Collection is new/small — empty "+ your next review" slot included.

T154 read 'the page header states the scale rule explicitly' and 'empty "+ your next review" slot included' as instructions to render literal on-page copy, and built:
1. A purple `<p className="perfume-scale-chip">` pill in the page header reading 'perfumes use a 0-10 scale — shown as a pip meter, not stars, so it never gets confused with the /5 reviews.' — note this exact sentence is NOT even a verbatim quote from the brief; it's T154's own paraphrase of the brief's design rationale, written as if it were end-user-facing copy.
2. A dashed-border `<div className="perfume-empty-card">+ your next review lands here</div>` empty-state slot at the end of the perfume grid — again, the literal string '+ your next review lands here' does not appear in the brief; the brief only says an 'empty slot' concept should exist in the design file's own layout.

The owner has now clarified directly that both of these were annotations aimed at whoever was READING the design handoff (explaining the design decision / documenting the layout concept for the design file itself), not copy that was ever meant to render to real visitors. The brief's ACTUAL instruction — 'the page header states the scale rule explicitly' — is already correctly satisfied by the existing, legitimate `<p className="collection-review-meta">rated out of 10 · a new &amp; growing shelf</p>` line directly under the `perfumes` title (this line stays; it is real UI and is out of scope for this task).

Concrete edits, in `src/pages/reviews/perfumes/index.js`:
1. Delete the entire `<p className="perfume-scale-chip">…</p>` element (currently the second child of the `<div className="collection-review-header">`, sitting as a sibling of `<div className="collection-review-title-group">`), including its full text 'perfumes use a 0-10 scale — shown as a pip meter, not stars, so it never gets confused with the /5 reviews.'
2. Delete the `<div className="perfume-empty-card">+ your next review lands here</div>` element, currently the last child of `<div className="perfume-card-grid">`, rendered after the `.map(...)` of `ReviewCard`s.

In `src/styles/globals.css`: after the above markup is removed, `.perfume-empty-card` and `.perfume-scale-chip` become dead CSS (verify with `grep -rn 'perfume-scale-chip\|perfume-empty-card' src/` — confirm no other file references either class before deleting). Remove both rule blocks:
- `.perfume-empty-card { … }` (the dashed-border/grape-text rule)
- `.perfume-scale-chip { … }` (the grape-filled pill rule)

Out of scope: do NOT touch the perfume card's own rendering (the `ReviewCard` component, `styleVariant="perfume-card"`, the numeric score, or the pip meter itself) — a separate, larger effort is redesigning the perfume card presentation itself; this task only removes the two wrongly-shipped standalone elements described above. Do NOT touch the `collection-review-meta` 'rated out of 10 · a new & growing shelf' line — it is legitimate, correctly-scoped UI and stays exactly as-is.

## Done when

- `grep -rn "perfume-scale-chip\|perfume-empty-card" src/` returns no matches anywhere in the repo (neither the JSX usage nor the CSS rule definitions).
- `npm run lint` reports no new errors.
- `npm run build` succeeds.
- `verify: ["run-app"]` — start the dev server (`npm run dev`), open `/reviews/perfumes` in a browser, and confirm:
  - The purple pill/chip that read 'perfumes use a 0-10 scale — shown as a pip meter, not stars, so it never gets confused with the /5 reviews.' is gone from the page header.
  - The dashed-border box reading '+ your next review lands here' is gone from the end of the perfume card grid.
  - The 'rated out of 10 · a new & growing shelf' meta line under the 'perfumes' title is still present, unchanged.
  - The perfume cards themselves (title, score, pip meter) still render normally — only the two removed elements are gone, nothing else regressed.
