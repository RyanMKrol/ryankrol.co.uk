## Do

Apply the shared header-strip pattern from perfumes-edit-redesign-2 to the perfumes edit page, replacing T266's fix. Depends on perfumes-edit-redesign-1 (the column-count fix) landing first since both touch the same `renderItem` callback in src/pages/reviews/perfumes/edit.js — build on top of that file's post-MasonryColumns-conversion state. In the `renderItem` callback: change the wrapper div's className from `"review-card-with-edit review-card-with-edit--perfume"` to `"review-edit-card"`; move the Edit `<Link>` to be the FIRST child, wrapped in a new `<div className="review-edit-header">`, with the Link's className changed from `"edit-button-overlay"` to `"review-edit-button"`; the `<Variant6Hybrid item={perfume} />` becomes the second child. In src/styles/globals.css, delete the now-dead T266 rules: `.review-card-with-edit--perfume` and `.review-card-with-edit--perfume .edit-button-overlay`, plus their explanatory comment (the last of the T262-T266 fix blocks, immediately before '/* Workout Styling Variants */') — do NOT touch the base rules or the books/movies/tv/albums fix blocks. AWARENESS: a separate concurrently-tracked task ('perfume-ownership-badge-placement') also touches src/components/perfumeVariants/Variant6Hybrid.js itself (repositioning the ownership pill near the type badge in `perfume-v1-header`) — this task does NOT touch Variant6Hybrid.js at all (only perfumes/edit.js's wrapper markup + globals.css), so there's no direct file conflict, but if that task has already landed, sanity-check the rendered card visually since the new header strip now sits directly above whatever `perfume-v1-header` looks like post-badge-move.

## Done when

`npm run dev`, visit `/reviews/perfumes/edit`: every card shows the header strip with only the right-aligned Edit button — no more absolutely-positioned button overlapping the type Badge in `perfume-v1-header`. Still exactly 2 columns (per perfumes-edit-redesign-1). Clicking Edit navigates correctly. Card content below the strip (Variant6Hybrid's title/designer/badge/description/rating/etc.) matches `/reviews/perfumes` (view page) for the same perfume, including if the ownership-badge-placement change has already landed. `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/perfumes/edit.js` returns NOTHING. `npm run lint`, `npm test`, `npm run build` all pass.
