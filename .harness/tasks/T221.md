## Do

In each of the 5 review edit-list pages (src/pages/reviews/movies/edit.js, src/pages/reviews/tv/edit.js, src/pages/reviews/books/edit.js, src/pages/reviews/albums/edit.js, src/pages/reviews/perfumes/edit.js), replace the legacy `styleVariant={2}` prop passed to `ReviewCard` (a numeric value that matches none of ReviewCard.js's named-string variant checks, so it silently falls through to the plain pre-redesign default card with a colored left-border stripe) with the SAME named string variant each type's own PUBLIC list page already uses, so the edit list visually matches the public list for that type:

- `movies/edit.js`: change `<ReviewCard item={movie} type="movie" isLast={false} styleVariant={2} />` so it passes `styleVariant="poster-banner"` instead of `{2}` (matches src/pages/reviews/movies/index.js).
- `tv/edit.js`: change `<ReviewCard item={tvShow} type="tv" isLast={false} styleVariant={2} />` so it passes `styleVariant="poster-banner"` instead of `{2}` (matches src/pages/reviews/tv/index.js).
- `books/edit.js`: change `<ReviewCard item={book} type="book" isLast={false} styleVariant={2} />` so it passes `styleVariant="spine-cover"` instead of `{2}` (matches src/pages/reviews/books/index.js).
- `albums/edit.js`: change `<ReviewCard item={album} type="album" isLast={false} styleVariant={2} />` so it passes `styleVariant="square-cover"` instead of `{2}` (matches src/pages/reviews/albums/index.js).
- `perfumes/edit.js`: perfumes' public list page (src/pages/reviews/perfumes/index.js) bypasses ReviewCard entirely and renders `Variant6Hybrid` directly — there is no named ReviewCard variant for perfumes to switch to. Do the equivalent here: remove the `import ReviewCard from '../../../components/ReviewCard';` line, add `import Variant6Hybrid from '../../../components/perfumeVariants/Variant6Hybrid';` (identical relative path to the one already used in perfumes/index.js, since edit.js sits at the same directory depth), and replace `<ReviewCard item={perfume} type="perfume" isLast={false} styleVariant={2} />` with `<Variant6Hybrid item={perfume} />`. Keep it inside the SAME surrounding `<div className="review-card-with-edit">...<Link className="edit-button-overlay">Edit</Link></div>` wrapper, completely unchanged — Variant6Hybrid only takes an `item` prop and the edit overlay is an absolutely-positioned sibling layered on by CSS regardless of the wrapped card's contents, so this is a drop-in replacement.

Do not modify src/components/ReviewCard.js or src/components/perfumeVariants/Variant6Hybrid.js — every named variant this task needs already exists on both. Do not touch the now-possibly-unused `.review-style-2` CSS rules in globals.css (harmless dead code once no page references styleVariant={2}; removing it is out of scope here — leave it as-is). Do not change anything else in these 5 files: data fetching, sorting, loading/error states, the edit-button overlay markup, or the `<h1>` page titles are all out of scope for this task (the h1 titles are a separate sibling task).

## Done when

`grep -rn "styleVariant={2}" src/pages/reviews/movies/edit.js src/pages/reviews/tv/edit.js src/pages/reviews/books/edit.js src/pages/reviews/albums/edit.js src/pages/reviews/perfumes/edit.js` returns nothing. `movies/edit.js` and `tv/edit.js` both now pass `styleVariant="poster-banner"`; `books/edit.js` passes `styleVariant="spine-cover"`; `albums/edit.js` passes `styleVariant="square-cover"`; `perfumes/edit.js` no longer imports or renders `ReviewCard` at all and instead imports and renders `Variant6Hybrid` from `../../../components/perfumeVariants/Variant6Hybrid`. `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify: `npm run dev`, open `/reviews/movies/edit`, `/reviews/tv/edit`, `/reviews/books/edit`, `/reviews/albums/edit`, and `/reviews/perfumes/edit`, and confirm each renders cards in the SAME visual style as its corresponding public list page (`/reviews/movies`, `/reviews/tv`, `/reviews/books`, `/reviews/albums`, `/reviews/perfumes`) rather than the old plain-white-card-with-colored-left-border look, and that the 'Edit' overlay button still appears and works on every card across all 5 pages. Record the observation in the worklog.
