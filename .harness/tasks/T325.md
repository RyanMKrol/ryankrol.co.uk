## Overview
Re-attempt of T273 (status: failed, manual-fail reason: 'The Markdown does not appear to be rendering on the actual site under the Review card... testing was not adequate'). Root cause found with certainty: T273 (d4d5f3b) correctly wired Markdown into the books/spine-cover card, but the very next commit that same day, T279 (cea04e9, unrelated: added a Read more/Show less truncation toggle for long book reviews), rewrote the exact same JSX block and silently dropped the `<Markdown>` wrapper, reverting the books card to raw plain-text interpolation. This is still the current live state of the code. Movies/TV (poster-banner), albums (square-cover), and perfumes (Variant6Hybrid) all correctly render Markdown today — only books (spine-cover) is broken. The first attempt's testing missed this because (a) Markdown.test.js only unit-tests the Markdown component in total isolation, never ReviewCard's usage of it, so a later commit silently un-wiring it in one variant is invisible to that test, and (b) no ReviewCard-level test existed at all, and (c) the hermetic visual-check harness (scripts/visual-check.mjs) wasn't built until later that same day (after the owner's manual-fail report) — and even now, being screenshot-based, it requires a human to actually open and look at the reviews-books screenshot, which apparently didn't happen since the still-broken output is sitting right there in the repo's own scripts/visual-out/reviews-books.png.

## Do
In src/components/ReviewCard.js's SpineCoverCard function, change `<p className="spine-cover-snippet">{expanded ? fullText : previewText}</p>` to wrap the text in the existing Markdown component: `<div className="spine-cover-snippet"><Markdown>{expanded ? fullText : previewText}</Markdown></div>` (switch from `<p>` to `<div>` since Markdown's block mode renders `<p>` internally and a `<p>` cannot legally contain another `<p>` — match the pattern already used for poster-banner/perfume descriptions, which wrap in a `<div>` not a `<p>`, for the same reason). Keep the truncate/expand behavior (`truncateReviewText`, the `expanded` state, the Read more/Show less button) exactly as-is — just render whichever string it currently produces through Markdown instead of as raw text. Add `src/components/ReviewCard.test.js` with React Testing Library: render ReviewCard with `styleVariant="spine-cover"` and a `review_text` containing `**bold**`, assert a `<strong>` renders (not literal asterisks); if the review text is long enough to truncate, click 'Read more' and assert the expanded view also renders `<strong>` rather than plain markdown syntax. Add `Markdown.js` to the `covers` array of the `reviews-books` entry in scripts/_visual-harness.mjs's PAGES list.

## Done when
src/components/ReviewCard.js's SpineCoverCard renders both the truncated preview and the expanded state through the Markdown component (grep confirms `<Markdown>` appears in that function, not just elsewhere in the file). npm run lint, npm test (including the new ReviewCard.test.js), and npm run build all pass. Must ALSO be verified against the real rendered page, not just the new isolated test: run `node scripts/visual-check.mjs` (or `VISUAL_CHECK_ONLY=reviews-books node scripts/visual-check.mjs` if supported) and open the resulting scripts/visual-out/reviews-books.png (and the reviews-books-search/sort variants if regenerated) — confirm 'Piranesi' and 'The Left Hand of Darkness' now show real bold text and a real bulleted list, not literal '**staggering**' / '- the craft' syntax, matching how scripts/visual-out/reviews-movies.png already renders the equivalent fixture text. If running the harness isn't possible in the environment, fall back to `npm run dev` and manually edit/view a real book review with `**bold**`/`- list` content at /reviews/books and confirm it renders, not just passes a component test in isolation — the whole point of this re-attempt is that an isolated test previously passed while the live page stayed broken.
