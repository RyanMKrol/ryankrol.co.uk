## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md` and open
`/Users/ryankrol/Downloads/design_handoff/renders/2a.png` (movies list — shows sort pills, search
box) and `/Users/ryankrol/Downloads/design_handoff/renders/3f.png` (workouts list — shows filter
pills) for reference. Build three new shared presentational components under `src/components/`,
using the Collection CSS custom properties from `src/styles/globals.css` (landed in T142) — no
hardcoded hex/px values.

- **`Pill.js`** — a single pill: rounded-999px, 2px ink border, `active` prop fills it (ink
  background + white text by default, or an `accentColor` prop to fill with a specific section
  accent instead — e.g. the movies nav pill fills coral when active). Accepts `onClick`, `children`,
  `active`, `accentColor`.
- **`PillGroup.js`** — a single-select group of `Pill`s: takes an array of `{ value, label }`
  options, a `value`/`onChange` pair (controlled), and renders one `Pill` per option with `active`
  set on the matching one. This is the shared primitive for sort pills (date/title/score), filter
  pills (all/push/pull/legs), and time-range pills (3 months/1 year/all) — those pages will consume
  this component in later tasks, don't wire it into any page here.
- **`SearchInput.js`** — a bordered pill-shaped search box: 2px ink border, white background,
  leading search icon/glyph (the design uses `⌕`), placeholder text prop, controlled
  `value`/`onChange`.
- **`Badge.js`** — a small filled pill for labels like split-type badges (PUSH/PULL/LEGS) and
  type chips (EDP/EDT): takes `children`, a `color`/`accentColor` prop for the fill, and a `variant`
  prop (`"solid"` = filled with white text, `"soft"` = tinted background with colored text — the
  design uses solid for split badges and soft for the perfume EDP/EDT chip and GitHub topic pills).

Add whatever CSS these need to `src/styles/globals.css` (or co-located CSS Modules if you prefer —
match whatever styling approach the rest of `src/components` already uses; check a couple of
existing components first, e.g. `SortButtons.js`, to match the codebase's existing convention rather
than introducing a new one).

## Done when

- All four components exist, are exported as default exports, accept the props described above, and
  read all colors/spacing/radius from the CSS custom properties landed in T142 (no hardcoded hex).
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): temporarily drop each component into any page (e.g. the home page) with
  a couple of test props, run `npm run dev`, and visually confirm they render sensibly (pill shape,
  border, active-state fill, accent-color fill) before removing the temporary usage — these
  components aren't wired into real pages until later tasks, so there's nothing else to check them
  against yet. Detailed pixel-fidelity tuning (exact border widths, padding, font sizes matching the
  renders precisely) is deferred to the Phase 6 visual-audit tasks (T165-T169) — don't block
  completion on exact px/hex matching here, just get a reasonably faithful, working component.
