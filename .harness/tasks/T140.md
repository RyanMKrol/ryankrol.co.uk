## Do

On the perfume review add/edit forms, the 'Type' field currently renders with its label stacked above a full-width native <select> (label on its own line, then a full-width dark dropdown below it with the caret pinned to the far right edge). The owner finds this stacked layout odd and wants the label and the <select> placed on the SAME line instead — e.g. 'Type:' to the left, the dropdown to the right, sized to its content rather than full width. This is a CSS/layout-only change: keep the native <select> element and its existing options (PERFUME_TYPES = ['EDP', 'EDT', 'Parfum']) exactly as they are — do NOT replace it with buttons/toggles or change the options list.

Two files contain byte-identical markup for this field and BOTH must be updated:
1. src/pages/reviews/perfumes/add.js — the Type field is the `<div className="form-group">` block at lines 170-184: a `<label className="form-label" htmlFor="type">Type</label>` (line 171) immediately followed by a `<select id="type" name="type" ... className="form-input">` (lines 172-183) that maps over PERFUME_TYPES.
2. src/pages/reviews/perfumes/edit/[id].js — the identical block at lines 258-272 (label at 259, select at 260-271).

IMPORTANT constraint: `.form-label` (display:block, defined once in src/styles/globals.css around line 1447) and `.form-input` (width:100%, defined around line 1455) are SHARED, sitewide classes used by every other form field across ALL review types (movies, tv, books, albums), vinyl, and workouts — do NOT change the global `.form-label` or `.form-input` rules in globals.css, and do NOT change the stacked layout of any other field. The same-line layout must be scoped ONLY to this one Type field, in both files.

Suggested approach (implementer's discretion on exact technique, but must satisfy the constraints above): wrap the label+select in the existing `.form-group` div but give that specific div an inline style (or a new, narrowly-scoped className such as `form-group-inline` added alongside `form-group`, with its own small CSS rule) that lays the two children out as a flex row (e.g. `display:flex; align-items:center; gap:0.75rem`) with the label not stretching (e.g. `flex-shrink:0` or removing the inherited `display:block`/`width:100%` just for this instance via an inline style override on the label/select themselves) so the select sizes to content rather than filling the row. Keep the `form-label` and `form-input` classNames on the elements themselves (for consistent font/color/border styling) — only override the layout-affecting properties (display/width) for this instance, not the shared class definitions. Apply the identical fix to both files so they stay in sync, matching this repo's established pattern of keeping the two forms' markup identical (see CLAUDE.md's perfume review conventions).

## Done when

Both src/pages/reviews/perfumes/add.js and src/pages/reviews/perfumes/edit/[id].js render the 'Type' label and its <select> dropdown side-by-side on one line (not stacked), while every other form-group field on both pages (Designer, Rating, Longevity, Projection, Seasons, Application spots, etc.) remains visually unchanged — still stacked, still full-width where it was before. The native <select> with options EDP/EDT/Parfum is unchanged (no buttons/toggle group introduced). src/styles/globals.css's shared `.form-label`/`.form-input` rules are either untouched, or any new rule added is scoped to a new, narrowly-named class that only applies to this Type field wrapper (verify by checking no other page/field's layout shifted).

Run `npm run dev`, open http://localhost:3000/reviews/perfumes/add (password-gate with RYANKROL_SITE_KEY if prompted, or view the page source/DOM directly if the gate blocks rendering) and visually confirm the Type label and dropdown sit on the same horizontal line. Repeat the same visual check on the edit page (src/pages/reviews/perfumes/edit/[id].js) for an existing perfume record. Confirm no other field's layout on either page changed. Then run the verify:["run-app"] check (dev server starts and the perfume add/edit pages render without console/runtime errors) plus the repo's standard Definition of Done: `npm run lint` (no new errors), `npm test` (still green — no test changes expected since this is pure CSS/layout with no new logic), and `npm run build` (succeeds).
