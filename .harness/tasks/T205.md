## Do

Redesign the perfume "Application spots" spray-tapper widget in `src/components/PerfumeCharacteristics.js` (the `SpraySpotCounter` function, currently lines ~70-162, and `ApplicationSpotsSprayer`, currently lines ~164-195) so every spot renders as a consistently-sized circle, and migrate this widget's inline `style={{...}}` objects to CSS classes in `src/styles/globals.css`.

**Visual redesign (decided - implement exactly this, don't re-derive):**
- Each spot is a fixed **4.5rem x 4.5rem circle** (`width:4.5rem; height:4.5rem; border-radius:50%`) - the SAME size for all 7 spots regardless of label length. No `minWidth`/content-driven sizing anywhere.
- The circle's content is ONLY the nose emoji (plus the transient poof animation) and the spray count number, vertically centered. The spot's text label ("Wrists", "Back of neck", etc.) moves OUTSIDE and BELOW the circle as a separate line - this is what makes the fixed circle work for both short and long labels without truncation or squeezing.
- Inactive state (0 sprays): `border: 2px solid var(--color-hairline-strong)`, transparent background, count text in `var(--color-ink-mute)`.
- Active state (>0 sprays): border AND count text in `var(--accent-perfumes)` (not the previous generic `var(--color-accent)`), background tinted `var(--color-hairline)`.
- Keep the tap-to-add-spray / right-click-to-remove interaction, the poof animation, and the horizontal scrollable row of 7 circles all unchanged - this is a visual/structural cleanup only, not an interaction change.

**New CSS in `src/styles/globals.css`** (add a new section near the existing perfume classes, e.g. after the `.perfume-v4-*` rules around line ~3335 - exact location doesn't matter as long as it's grouped with the other perfume-specific classes):
- `.perfume-spray-spots` - the horizontal row container. Replaces the inline style currently on `ApplicationSpotsSprayer`'s row `<div>` (~lines 174-182): `display:flex; gap:0.4rem; flex-wrap:nowrap; overflow-x:auto; -webkit-overflow-scrolling:touch; padding-bottom:0.25rem;`
- `.perfume-spray-spot` - wrapper around one circle+label pair: `display:flex; flex-direction:column; align-items:center; gap:0.4rem; flex:0 0 auto;`
- `.perfume-spray-spot-circle` - the button itself: `width:4.5rem; height:4.5rem; border-radius:50%; display:flex; flex-direction:column; align-items:center; justify-content:center; gap:0.15rem; border:var(--border-width) solid var(--color-hairline-strong); background:transparent; font:inherit; padding:0; cursor:pointer; transition:border-color 0.2s, background-color 0.2s;`
- `.perfume-spray-spot-circle.is-active` - `border-color:var(--accent-perfumes); background:var(--color-hairline);`
- `.perfume-spray-spot-emoji` - `position:relative; display:inline-block; font-size:1.15rem; line-height:1; transform-origin:bottom center;`
- `.perfume-spray-spot-emoji.is-spraying` - `animation: spray-squeeze 0.25s ease;` (applied conditionally instead of the current inline `animation: poofs.length ? '...' : 'none'`)
- `.perfume-spray-spot-poof` - the transient droplet: `position:absolute; top:-0.25rem; left:50%; transform:translateX(-50%); font-size:1rem; pointer-events:none; animation:spray-poof 0.6s ease-out forwards;`
- `.perfume-spray-spot-count` - `font-family:var(--font-mono); font-weight:700; font-size:0.75rem; color:var(--color-ink-mute);` with a `.is-active` modifier setting `color:var(--accent-perfumes);`
- `.perfume-spray-spot-label` - `font-family:var(--font-mono); font-weight:700; font-size:0.7rem; letter-spacing:0.06em; text-transform:uppercase; color:var(--color-ink-mute); text-align:center; line-height:1.2; max-width:4.5rem;` (matches the type treatment already used by `.collection-form-label`)
- Move the two `@keyframes` blocks (`spray-poof`, `spray-squeeze`) out of the component's `<style jsx>` (currently ~lines 149-159) into this same globals.css section, values unchanged, then delete the `<style jsx>` block from the component entirely.

**Component changes in `src/components/PerfumeCharacteristics.js`:**
- `SpraySpotCounter`: remove every inline `style={{...}}` prop on the button/emoji/poof/count elements and use the class names above instead (adding `is-active`/`is-spraying` conditionally, e.g. via a template string or a small helper, matching this repo's existing conditional-classname style elsewhere). Move the spot label (currently `<span className="form-label">{spot}</span>`, ~line 139, rendered INSIDE the button) to a sibling `<span className="perfume-spray-spot-label">{spot}</span>` OUTSIDE and after the button. Drop the verbose "no sprays"/"N sprays" text (~lines 140-148) in favor of the bare count number in `.perfume-spray-spot-count` (e.g. render `count` directly - `0` when empty). Wrap the button + label together in a new `<div className="perfume-spray-spot">`.
- `ApplicationSpotsSprayer`: replace the inline `style={{...}}` on the row `<div>` (~lines 175-182) with `className="perfume-spray-spots"`. Leave the `<div className="form-group">` wrapper and the `<span className="form-label">Application spots - tap the bottle to spray</span>` heading (~lines 172-173) untouched.
- Do NOT touch `LongevitySlider`, `ProjectionSlider`, or `SeasonsCheckboxes` in the same file - their inline styles are out of scope for this task (a separate concern if ever revisited).
- Leave `applicationSpotCounts`/`countsToApplicationSpots` (the pure functions covered by `src/components/PerfumeCharacteristics.test.js`) completely unchanged - this task is UI-only, no data-shape changes.

## Done when

- All 7 application-spot buttons on `/reviews/perfumes/add` render as identically-sized circles (4.5rem diameter) regardless of label length - verified visually per the manual check below, not just by reading the CSS.
- `grep -n "style={{" src/components/PerfumeCharacteristics.js` shows no remaining inline style objects inside `SpraySpotCounter`/`ApplicationSpotsSprayer` (inline styles in `LongevitySlider`/`ProjectionSlider`/`SeasonsCheckboxes` in the same file are untouched and expected to remain - do not remove those).
- `grep -n "style jsx" src/components/PerfumeCharacteristics.js` returns no matches (the component no longer defines its own scoped `<style jsx>` block).
- `grep -n "spray-poof\|spray-squeeze" src/styles/globals.css` shows both keyframes now defined there.
- `npm run lint` reports no new errors.
- `npm test` passes, including the existing, UNCHANGED `applicationSpotCounts`/`countsToApplicationSpots` tests in `PerfumeCharacteristics.test.js`.
- `npm run build` succeeds.
- `verify: ["run-app"]` - run `npm run dev`, open `/reviews/perfumes/add`, and confirm: all 7 spot buttons ("Wrists", "Elbows", "Clavicles", "Beard", "Back of neck", "Behind ears", "Clothes") render as consistently-sized circles of the same diameter regardless of label length (no more stretched ovals on long labels); tapping a circle increments its spray count (shown inside the circle, styled with the perfume grape accent once active) and briefly shows the poof animation; right-clicking a circle with a nonzero count decrements it; each circle's spot label is legible on its own line below the circle. Record this observation in the task's worklog.
- Confirm (and note in the worklog) that `CLAUDE.md`'s "Components:" file-map entry for `PerfumeCharacteristics` needs no wording change - it's still just named `PerfumeCharacteristics` with no per-widget behavior description to update; this is a pure internal restyling, not a new component or a changed public description.
