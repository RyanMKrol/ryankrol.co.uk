## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s reviews-list section: "the old site
rendered all 188 at once — introduce paging/denser grids." Open
`/Users/ryankrol/Downloads/design_handoff/renders/2a.png` for the pagination control reference
(numbered pills `1 2 3 … 32` + `next →`).

This repo already has a pure pagination-slicing helper for workouts —
`src/lib/workoutPagination.js` (+ `src/lib/workoutPagination.test.js`) — mirror that established
pattern rather than inventing a new one:

1. **`src/lib/pagination.js`** — a pure function (or small set of functions) that takes a full array
   and a page number + page size, and returns the sliced page plus derived metadata (total pages,
   current page, whether next/prev exist) — generic over item type (reviews here, but keep it
   type-agnostic, not review-specific). Model its signature/shape on
   `src/lib/workoutPagination.js`'s existing function.
2. **`src/lib/pagination.test.js`** — unit tests covering: exact page-size boundaries, the last
   (possibly partial) page, an out-of-range page number, and a single-page (fewer items than page
   size) case.
3. **`src/components/Pagination.js`** — the presentational control: numbered page pills (using `Pill`
   from T143) with an ellipsis for far-away pages (per the render's `1 2 3 … 32`), and a `next →`
   pill. Controlled via `currentPage`/`totalPages`/`onPageChange` props.
4. Wire `Pagination` + the `src/lib/pagination.js` slicing helper into all four list pages rebuilt in
   T150-T153 (`src/pages/reviews/movies/index.js`, `tv/index.js`, `books/index.js`,
   `albums/index.js`), replacing their current render-all-at-once behavior. Pick a sensible page size
   per layout (e.g. movies/tv's 3-col grid might page at 6 or 9 items; books' single-column list and
   albums' 2-col grid might page at a different count — match roughly what the reference renders show,
   don't overthink exact counts). Pagination state resets to page 1 when the search term or sort
   changes (don't leave the user stranded on page 5 of a filtered list that now has 2 pages).

## Done when

- `src/lib/pagination.js` exists as a pure, type-agnostic slicing helper with `pagination.test.js`
  covering the boundary cases listed above; `npm test -- pagination.test` passes.
- `Pagination.js` renders numbered page pills + ellipsis + next, wired to real page state.
- All four list pages (movies/tv/books/albums) page their real data instead of rendering everything
  at once, and reset to page 1 on search/sort change.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/movies` (or whichever type currently has
  enough seed data to span multiple pages), click through page numbers and `next`, confirm the
  correct slice of items renders each time, then change the sort or search and confirm it resets to
  page 1. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T166.
