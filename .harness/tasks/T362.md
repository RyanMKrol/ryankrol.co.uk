## Overview
src/components/BookSearch.js is the search+confirm widget used by the book add and edit forms. It currently calls /api/books/search with no `?provider=` param, so it always hit Google Books. Since hardcover-migration-a made /api/books/search.js call Hardcover directly (no provider branching), this component's fetch call needs no query-param change — but its hardcoded UI label `const providerLabel = 'Google Books'` (line 67) is now wrong and must be updated, since it's shown to the user both in the results header ('Results from Google Books') and the confirmed-match banner ('✓ Google Books match selected:').

## Do
In src/components/BookSearch.js, change `const providerLabel = 'Google Books';` to `const providerLabel = 'Hardcover';` (or equivalent constant naming already in place). Verify no other Google-Books-specific wording, comments, or assumptions exist in this file (e.g. the file's top comment 'Google Books search + confirm component' at line 3 should also be updated to say Hardcover). No change to the request logic itself is needed since the provider switch happened server-side in the previous unit — confirm this by reading the current fetch call (`/api/books/search?${params}` with only `title`/`author`) and leaving it as-is unless the previous unit introduced a required query param.

## Done when
The book add and edit pages' search results header reads 'Results from Hardcover' and the confirmed-match banner reads '✓ Hardcover match selected:' after a real search; no remaining references to 'Google Books' exist in src/components/BookSearch.js; `npm run lint` and `npm run build` stay clean; the project's visual-check harness (scripts/visual-check.mjs) still renders the add/edit book pages without error (auto-covered by the page/ui VISUAL_VERIFY_HOOK — no FLOWS/manifest change needed since this doesn't add a new interactive state, only relabels an existing one).
