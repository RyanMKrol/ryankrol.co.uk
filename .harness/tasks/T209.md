## Do

In src/pages/programmes/index.js:

- The PROGRAMMES constant (line 8, `const PROGRAMMES = ['push', 'pull', 'legs']`) is used in exactly one place in this file: the `.map` that renders the filter buttons (around line 82). Add a new array (e.g. `const FILTER_OPTIONS = ['all', ...PROGRAMMES];`) and use it in place of `PROGRAMMES` for that `.map` — keep `PROGRAMMES` itself unchanged (it's still meaningful as 'the three real programmes') since other code may reason about it distinctly from the 'all' pseudo-option.
- The button label logic already in the map body (`p.charAt(0).toUpperCase() + p.slice(1)`) will correctly render 'all' as 'All' with no special-casing needed.
- `onClick={() => setSelected(p)}` and the `active` class check (`selected === p ? 'active' : ''`) need no changes — 'all' just becomes another valid value of the existing `selected` state.
- Leave the `selected` state's default (line 14, `useState('push')`) as `'push'` — do not change the default landing view, this task only adds the ability to explicitly pick 'all', it does not change what loads first.
- No changes needed in aggregateProgramme's call site (line ~44, `aggregateProgramme(filteredWorkouts, selected)`) — once programmes-total-filter-1 lands, 'all' flows through as a valid `programme` argument automatically.
- No changes needed to ProgrammeOverviewCharts — it already just renders whatever `data` shape aggregateProgramme returns, and 'all' produces the same shape as any other programme.

## Done when

- Visiting /programmes shows 4 filter buttons in order: All, Push, Pull, Legs (defaulting to Push selected, unchanged from current behavior).
- Clicking 'All' shows combined totals across all three programmes: the three chart-card totals (Total Workouts, Total Volume, Avg Volume/Session) for 'All' reconcile with summing/recomputing from the individual Push + Pull + Legs views on the same date range (workouts and totalVolume should sum exactly; avg is recomputed, not summed).
- Clicking Push/Pull/Legs individually still behaves exactly as before.
- Manual verification required (no screenshot-diff tooling in this repo): run `npm run dev`, open http://localhost:3000/programmes (or the fallback port printed by the dev server), click through all 4 filter buttons, and confirm the totals and charts update correctly for each, with 'All' matching the combined sum described above.
- `npm run lint` reports no errors.
- `npm run build` succeeds.
