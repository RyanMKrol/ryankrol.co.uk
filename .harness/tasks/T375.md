## Overview
Ship only the design the owner picked: make it the single spine-cover book-card rendering and delete the workshop switch/preview scaffolding and the four unused variants.

## Decision (from T374)
The owner picked **Variant 4 — "Mono Label"** (`designVariant === 4` in `SpineCoverCard`,
`src/components/ReviewCard.js:149-175`; styles `.spine-v4-*` in `src/styles/globals.css:438-476`):
all-mono `label:value` metadata pairs (`rating` / `details`), separated from the review snippet by
a thin `<hr>` rule. This is the variant to keep — variants 1 (Ledger), 2 (Badge), 3 (Ribbon), and 5
(Serif Feature) are the four to delete.

## Do
Take the winning design (Variant 4 — Mono Label, see Decision above) and make it the sole rendering path for the book spine-cover card in ReviewCard.js's SpineCoverCard. Remove the variant-selection switch and the preview mechanism (dedicated preview surface / query-param toggle / inline switcher — whichever the chooser used), and delete the other four variants' JSX and their now-unused .spine-cover-* variant styles from globals.css. Keep the winner's tightened title↔meta spacing (the `.spine-v4-body` gap) and the scoped Markdown list-indent fix (retained in Markdown.js and/or globals.css). Trim scripts/_visual-harness.mjs back to what's needed for the single shipped design while retaining the 'Ultra-Processed People' (read-more) and 'The Horus Heresy 42: Garro' (ordered-list) fixtures so those two states stay covered. Update the file map / component notes in CLAUDE.md if the spine-cover card's structure changed materially.

## Done when
The book spine-cover card renders only the chosen design; the variant switch, preview mechanism, and the four non-chosen variants (JSX + their CSS) are gone with no dead code left. /reviews/books renders the chosen design with the tighter title↔meta spacing and correctly-indented Markdown lists, and the visual check still captures the read-more state and an ordered-list body against the two example books for the single shipped design.
