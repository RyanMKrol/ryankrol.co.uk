## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Perfumes" screen description and open
`/Users/ryankrol/Downloads/design_handoff/renders/3d.png` for the pip-meter reference, and
`/Users/ryankrol/Downloads/design_handoff/renders/2a.png` for the star reference.

**Re-skin `src/components/StarRating.js`** to the Collection tokens: filled stars use
`var(--color-star-gold)` (`#F4A72C`), empty stars use `var(--color-star-empty)` (`#D8D2C8`).
This component is currently an INTERACTIVE rating-input widget (click a star to set `rating`, used
in add/edit forms) — the design's list/card views show STATIC, non-interactive stars. Rather than
building a separate duplicate component, add a `readOnly` boolean prop to `StarRating`: when true,
render the same star glyphs with the same fill logic but no click handlers, no hover state, and
no `<span className="rating-text">{rating}/5</span>` trailing text (the design's card stars are just
the glyphs, no "/5" suffix). Keep the existing interactive behavior fully intact when `readOnly` is
falsy/omitted — every current caller must keep working unchanged.

**Build `src/components/PipMeter.js`** — new component for perfume ratings (0-10 scale, explicitly
NOT stars, per the design README: "Perfumes use a 0-10 scale... shown as a pip meter, not stars, so
it never gets confused with the /5 reviews"). Renders 10 small circular dots in a row; the first
`value` dots filled with `var(--pip-filled)` (`#7A5AF8`), the rest `var(--pip-empty)` (`#E1D9F5`).
Support both a `readOnly` display mode (for the perfumes list page, later task) and an interactive
input mode (click a dot to set the value, mirroring `StarRating`'s existing interactive pattern —
the perfume add/edit forms currently must have SOME rating input; check
`src/pages/reviews/perfumes/add.js` for how rating is currently captured and make sure `PipMeter`
can be dropped in as its replacement in a later task without changing the underlying `rating` value
contract, an integer 0-10).

## Done when

- `StarRating.js` supports both existing interactive behavior (unchanged) and a new `readOnly` mode,
  using the Collection gold/empty token colors.
- `PipMeter.js` exists, renders a 10-dot meter in both read-only and interactive modes, uses
  `var(--pip-filled)`/`var(--pip-empty)`, and accepts a `value`/`onChange` pair for interactive mode
  matching `StarRating`'s prop shape closely enough that later form-restyle tasks can swap it in.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, temporarily render both components with a few different
  values on any page to confirm the fill logic and colors are correct, then remove the temporary
  usage. Neither component is wired into a real page yet — that happens in later tasks. Detailed
  pixel-fidelity tuning is deferred to the Phase 6 visual-audit tasks (T165-T169).
