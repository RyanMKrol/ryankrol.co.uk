## Do

Two changes in the perfume "Application spots" tap-to-spray UI, both in `src/components/PerfumeCharacteristics.js`:

1. Add 'Clothes' as a 7th application spot. Add the string `'Clothes'` to the `APPLICATION_SPOTS` array at line 5 (currently `['Wrists', 'Elbows', 'Clavicles', 'Beard', 'Back of neck', 'Behind ears']`). This array drives the `ApplicationSpotsSprayer` component's grid of tappable spray-spot cards (rendered via `.map` around line 174) as well as `applicationSpotCounts`/`countsToApplicationSpots` (lines 56-68), which build/flatten the `{spot, sprays}` list sent to the perfume add/update API routes. The server-side allow-list (`VALID_APPLICATION_SPOTS` in `src/lib/perfumes.js`) already accepts `'Clothes'` from a prior task — this task only needs the client-side list and UI.

   Update `src/components/PerfumeCharacteristics.test.js` to match: the hardcoded expected-counts objects in the `applicationSpotCounts` tests (currently lines 4-24) list all 6 existing spots each defaulting to `0` — add `Clothes: 0` to each of those expected objects, and add `Clothes: 0` to the two hardcoded input/expected objects in the `countsToApplicationSpots` tests (currently lines 38-67) so the fixtures reflect the full 7-spot set. Don't change the underlying assertions' logic, only keep the fixtures in sync.

2. Resize/relayout so all 7 cards fit on one line/row. Currently the container (line 173) is `<div style={{ display: 'flex', gap: '0.75rem', flexWrap: 'wrap' }}>` and each `SpraySpotCounter` card (defined ~lines 70-161) has `minWidth: '5rem'` (line ~104). At that size 6 cards need roughly 33.75rem of row width and were already reported wrapping to 2 rows on some viewports; a 7th card makes this strictly worse (~39.5rem needed). Shrink the cards and/or container so the full set of 7 spots reads as one row instead of wrapping: reduce `minWidth` on the card (e.g. try roughly 3.5-4rem) and/or the container `gap` (e.g. roughly 0.4-0.5rem) and/or trim internal padding/font-size just enough to fit 7 comfortably at typical mobile widths (~375-430px), while keeping each card easily tappable — don't shrink so far that the emoji/spot-name/spray-count text becomes illegible or the tap target becomes too small for a thumb. If, after reasonable shrinking, 7 cards still can't fit at typical mobile widths without hurting usability/legibility, instead keep `flexWrap: 'nowrap'` and make the row horizontally scrollable (e.g. `overflowX: 'auto'`, `WebkitOverflowScrolling: 'touch'` on the container) rather than wrapping to a second row. Either outcome — all 7 fitting statically in one row, or all 7 sitting in one horizontally-scrollable row — satisfies the goal; wrapping to 2+ rows does not.

## Done when

`APPLICATION_SPOTS` in `src/components/PerfumeCharacteristics.js` contains exactly these 7 entries: 'Wrists', 'Elbows', 'Clavicles', 'Beard', 'Back of neck', 'Behind ears', 'Clothes'.
`src/components/PerfumeCharacteristics.test.js` fixtures are updated to include `Clothes: 0` everywhere the other 6 spots are listed, and `npm test` passes.
`npm run lint` reports no errors; `npm run build` succeeds.
Manual verification (run-app): run `npm run dev`, open the perfume add form (`/reviews/perfumes/add`) with the browser viewport set to a mobile width (~375-430px, e.g. via devtools device toolbar), and confirm: (a) all 7 application-spot cards — including 'Clothes' — appear on a single row, either all fitting with no wrap or all reachable via horizontal scroll on one row, but never wrapped onto a second row; (b) tapping the 'Clothes' card increments its spray count and the poof animation still plays; (c) right-click (or long-press, if applicable) removes a spray from 'Clothes'; (d) saving the review with at least one spray recorded against 'Clothes' succeeds (no 400 from the add API) and the value persists after reloading/opening the edit form.
