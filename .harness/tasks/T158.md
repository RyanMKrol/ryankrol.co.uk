## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Projects" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/3e.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4j.png` (mobile).

Rebuild `src/pages/projects/index.js`:
- Header: big "projects" title, "what I've been building on github · sorted by recent activity"
  meta line.
- A 3-column (desktop) grid of repo cards: repo name + star count (top row), description text, a
  language indicator row (a small colored dot + language name — use the standard GitHub language
  colors: TypeScript `#3178C6`, Python `#3572A5`, Shell `#89E051`, and a couple of sensible fallbacks
  for any other languages your real repo data returns, e.g. JavaScript `#f1e05a`) + "updated Xago"
  text, and a row of topic tag pills (soft-filled `Badge` from T143, projects-green tint per the
  design).

Reuse whatever GitHub API data-fetching this page already does (`/api/github/repos` per CLAUDE.md's
file map) — this is a visual rebuild, not a data-layer change. If the current API response doesn't
already include a language color mapping, build the color lookup client-side in this page (a small
local object mapping language name → hex, not a new shared lib file, since this is presentational
flavor rather than reusable business logic).

## Done when

- The projects page shows the new header/meta line and a 3-col (responsive to 1-col mobile) grid of
  repo cards with name/stars/description/language-dot/updated-time/topic pills, bound to real GitHub
  API data.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/projects`, confirm real repo data renders with the
  correct language dot color for at least TypeScript/Python/Shell repos if present in the real data.
  Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T167.
