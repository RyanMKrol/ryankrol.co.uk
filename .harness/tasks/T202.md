## Do

For each of the seven call sites listed in the `design` field, replace the direct/implicit `gradientForKey` usage with a page-level call to `assignGradients` (imported from `../../components/CoverTile` or the appropriate relative path) over the correctly-ordered, correctly-filtered key list for that render, then thread the per-item result into the `gradient` prop on `CoverTile`/`ReviewCard` (or use it directly as an inline style at the two sites — home page 'Latest takes' and the listening page — that build their own gradient div without going through `CoverTile`/`ReviewCard`). Concretely:

1. `src/pages/index.js`: in the `wallItems` `useMemo`, after building the final sampled/shuffled array, derive the ordered list of items lacking `imageUrl`, call `assignGradients` on their keys, zip results back by index onto `wallItems`, and pass `gradient={item.gradient}` to `<CoverTile>` in the wall grid `.map()`. Separately, for `latestTakes`, derive the ordered list of items lacking a real thumbnail/poster (`item.thumbnail || tmdbPosterUrl(item.posterPath)`), call `assignGradients`, zip back, and use the assigned value instead of calling `gradientForKey(...)` in the `home-latest-thumb` inline style.
2. `src/pages/listening/index.js`: derive the ordered list of albums lacking `album.image`, call `assignGradients`, zip back onto `albums` (or a parallel array), and use the assigned value instead of `gradientForKey(...)` in the `listening-cover-thumb` inline style.
3. `src/pages/vinyl/index.js`: before the `sortedLetters.map(...)` render, build the full concatenated vinyl order (respecting `sortedLetters`), filter to items lacking `thumbnail`, call `assignGradients` once across that full ordered list, zip results back per-record, then pass `gradient={record.gradient}` to each `<CoverTile>` in the covers view.
4. `src/pages/reviews/movies/index.js` and `src/pages/reviews/tv/index.js`: for the current page's items (`pagedMovies`/`pagedTv` or equivalent), call `assignGradients` across the full page in render order (no filtering — this variant is always a gradient), zip back, and pass `gradient={...}` to `<ReviewCard>`.
5. `src/pages/reviews/books/index.js`: same pattern for `pagedBooks` (`spine-cover` variant, always a gradient) — call `assignGradients` across the full page, zip back, pass `gradient={...}` to `<ReviewCard>`.
6. `src/pages/reviews/albums/index.js`: for the current page's items, filter to those lacking `thumbnail`/`coverUrl`, call `assignGradients`, zip back, pass `gradient={...}` to `<ReviewCard>` (which forwards it into `SquareCoverCard`'s internal `CoverTile`).

Do not touch `src/pages/reviews/perfumes/index.js` or `src/components/perfumeVariants/Variant6Hybrid.js` — perfumes don't use the gradient mechanism.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass, per this repo's Definition of Done.
- Every one of the seven call sites in scope now calls `assignGradients` at the page level (in the correct render order, filtering out real-artwork items where the variant supports them) instead of relying on `gradientForKey` being called per-item inside `CoverTile`/`ReviewCard`/inline.
- `verify: ["run-app"]`: run `npm run dev` and manually check each of: `/reviews/books` (the idea's own example — reload/page through until you see several consecutive gradient-fallback entries; confirm no colour repeats within 4 positions of itself), `/reviews/movies`, `/reviews/tv`, `/reviews/albums` (with enough items lacking cover art to show multiple gradients in a row), `/` (both the collection wall grid and the 'Latest takes' list), `/listening`, and `/vinyl` (covers view, checking across a letter-group boundary too, not just within one letter). On each, confirm: (a) no gradient colour reappears within 4 items of its own most recent occurrence in that page's rendered order; (b) items that DO have real artwork still show their real artwork, unaffected; (c) reloading a page with the SAME underlying data mostly keeps items showing their same 'identity colour' (the pure-hash preferred colour) except where a collision forced a deterministic substitute — spot-check a couple of items that appear in both a review list and the home page wall/latest-takes and confirm they show the same colour in both places when no collision applies at either render, demonstrating the invariant is preserved in the common case.
