## Overview
On a perfume review card (/reviews/perfumes), the ownership pill (top-right, next to the type pill -- reads 'SAMPLE'/'Travel size'/'Full size' depending on `item.ownership`) should get a white background + a border, instead of its current tinted-soft look. Anchor: `<Badge accentColor="var(--accent-perfumes)" variant="soft">{item.ownership}</Badge>` at src/components/perfumeVariants/Variant6Hybrid.js:46. The adjacent type pill ('PARFUM', Variant6Hybrid.js:42-44, `variant="solid"`, no `variant` prop change) is explicitly NOT part of this task and must render identically to before.

Why this can't be a global change to Badge's `soft` variant: `variant="soft"` (src/components/Badge.js:13-15, `{ backgroundColor: fill+'1A', color: fill }`, no border in either variant today) is a SHARED branch also used by `PrBadge` (Badge.js:31-38, rendered on /workouts and /workouts/[id]) and the GitHub topic pills (src/pages/projects/index.js:287, `variant="soft" mono={false}`). Restyling the shared `soft` branch would incorrectly re-skin those two unrelated call sites, which the idea explicitly rules out ("only the ownership pill should change").

## Do
Add a new Badge variant value, `"outline"`, alongside the existing `"solid"`/`"soft"` values in `src/components/Badge.js` (extend the ternary at Badge.js:13-15 into a 3-way branch keyed on `variant`). The `outline` branch's style object should be:
```js
{ backgroundColor: 'var(--color-card)', color: fill, border: '1px solid var(--color-border-strong)' }
```
(Owner-confirmed values: a solid `var(--color-card)` background (the existing white token, unaffected by Matrix mode) and a `1px solid var(--color-border-strong)` border -- the lighter 1px hairline-strong border tier used elsewhere for small chrome like the search input / tooltip bubble / list dividers, e.g. globals.css:790,1011,1350,1396,1477,3214, rather than the heavier 2px card/panel border tier or the perfume accent colour.)

Then change ONLY the ownership Badge instantiation at `src/components/perfumeVariants/Variant6Hybrid.js:46` from `variant="soft"` to `variant="outline"`. Do not change any other `Badge`/`PrBadge` call site (leave `Badge.js:33`'s `PrBadge` and `src/pages/projects/index.js:287`'s topic pills exactly as they are -- both must keep using `variant="soft"` and render with zero visual change). Do not change the type pill at Variant6Hybrid.js:42-44 (stays `variant="solid"`, no `variant` prop at all currently -- leave it that way).

## Done when
On /reviews/perfumes, the ownership pill (SAMPLE/Travel size/Full size) on every perfume card renders with a solid white/light background and a visible border, its text stays perfume-accent-coloured, and the adjacent type pill (PARFUM) is visually unchanged. Run `node scripts/visual-check.mjs` (or `VISUAL_CHECK_ONLY=perfume`) and confirm via the `reviews-perfumes` manifest screenshot. Also visually spot-check /workouts (or /workouts/[id]) for the PrBadge medal pills and /projects for the GitHub topic pills (both `variant="soft"`) and confirm they are pixel-identical to before this change -- no border, same tinted background as today. `Badge.js`'s existing `"solid"`/`"soft"` branches are unmodified; only a new `"outline"` branch is added.
