## Overview
The perfume review card's TYPE pill (EDT/EDP/Parfum) has no tooltip, while the ownership pill directly beneath it already has one ('What I own', shipped in T334). Add a matching 'What it is' tooltip above the type pill for consistency.

## Do
In src/components/perfumeVariants/Variant6Hybrid.js, wrap the existing type Badge (currently unwrapped at lines 42-44: `{item.type && (<Badge accentColor="var(--accent-perfumes)">{item.type}</Badge>)}`) in `<Tooltip label="What it is">...</Tooltip>`, mirroring the ownership pill's pattern one line below it (lines 45-49: `<Tooltip label="What I own"><Badge accentColor="var(--accent-perfumes)" variant="outline">{item.ownership}</Badge></Tooltip>`). `Tooltip` is already imported (line 4) and defaults to `position="top"`, so no extra prop is needed. Do NOT change the type Badge's variant — keep it as the default solid/filled style (no `variant` prop), unlike the ownership pill's `variant="outline"`.

**Owner explicitly asked for this to be visually verified in a hover state, not just a static render.** `Tooltip` (src/components/Tooltip.js) is a pure CSS `:hover`/`:focus-within` component — the `.tooltip-bubble` is ALWAYS present in the DOM (`aria-hidden="true"`) and only becomes visible via CSS on hover/focus, so a plain RTL/jsdom test cannot meaningfully assert the visible state (jsdom doesn't compute real CSS pseudo-class-triggered visibility) — this is the exact same limitation T334's own worklog already documented for the ownership pill's tooltip. The reliable way to actually SHOW the tooltip visible is a real-browser hover, which the visual-check harness can do since it drives real headless Chromium via Playwright (unlike jsdom). Add a new entry to the `FLOWS` array in scripts/_visual-harness.mjs (see the existing perfume FLOWS entries ~line 278-280 for the pattern) that navigates to `/reviews/perfumes`, and in its `actions(page)` callback calls `await page.hover('.perfume-v6-card .tooltip-wrapper')` (or the more specific selector the type pill's Tooltip wrapper actually renders — confirm the exact selector by reading the rendered markup, since `Tooltip.js` applies `className="tooltip-wrapper"` to its outer `<span>`; scope the hover to the FIRST matching type-pill tooltip on the page, not the ownership pill's, e.g. via `.perfume-v6-card:first-child .tooltip-wrapper` or a more specific selector if multiple `.tooltip-wrapper` elements exist per card) before the screenshot is taken, so the resulting capture shows the 'What it is' bubble actually visible above the type pill.

## Done when
The perfume review card (src/pages/reviews/perfumes/index.js, rendering Variant6Hybrid) shows a 'What it is' tooltip on hover/focus of the type pill, appearing above it, with the type pill's existing solid/filled visual style unchanged. The ownership pill's 'What I own' tooltip and styling are unaffected. `npm run lint` and `npm test` remain green; no new unit test is required since this is a markup-only wrap with no new pure logic (matches T334's own precedent). A NEW `FLOWS` entry in scripts/_visual-harness.mjs performs a real Playwright hover over the type pill's tooltip wrapper before screenshotting, and running it (`VISUAL_CHECK_ONLY=perfume` or the new flow's specific name) produces a screenshot where the 'What it is' bubble is visibly showing above the type pill — read that screenshot directly to confirm, the same way T334's own tooltip was verified by reading CSS rather than assumed.
