## Do

Add a new page `src/pages/reviews/movies/backfill.js` that lets the owner bulk-backfill TMDB metadata for movies missing it, reusing the shared `BulkBackfillList` component and `backfillEligibility`/`backfillQueue` lib from the dependency task (`src/components/BulkBackfillList.js`, `src/lib/backfillEligibility.js`).

1. On mount, `fetch('/api/reviews/movies')`, then filter the result with `needsMovieBackfill` from `src/lib/backfillEligibility.js` — this page shows ONLY movies missing `tmdbId` (the owner's explicit answer: not all items, only ones missing rich metadata already).
2. Render a single password field at the top of the page (matching the site's write-gate convention — see `src/pages/reviews/movies/edit/[id].js`'s password field for the pattern), entered ONCE for the whole page/session rather than per row — this is what keeps the click-through fast, which is the entire point of this feature. Every row's confirm action reuses this one password value.
3. Render `<BulkBackfillList>` with the filtered movies as `items`, `pageSize=15`, and:
   - `renderItemLabel`: show the movie's title (+ year/rating if you want, keep it simple — title is enough).
   - `onSearch`: for a given movie item, call `GET /api/tmdb/search?query=<title>&type=movie` (mirror `handleBackfillSearch` in `src/pages/reviews/movies/edit/[id].js:81-90` exactly, including throwing an `Error` with `.message` from the response body on a non-OK response). IMPORTANT: when the response status is 429, attach `retryAfterSeconds` (parsed from the response body / `Retry-After` header) onto the thrown `Error` object as `error.retryAfterSeconds`, per `backfillQueue.js`'s documented contract, so the shared queue's retry-once behavior works.
   - `renderCandidate` / `getCandidateKey`: reuse the exact same rendering JSX already written for the per-item modal in `src/pages/reviews/movies/edit/[id].js:233-259` (poster thumbnail via `tmdbPosterUrl`, title, year, overview snippet) — copy it, don't abstract it into a new shared component, since it's TMDB-shape-specific and duplicating a small render function per type is fine here (matches how the per-item edit pages already each define their own `renderResult`).
   - `onConfirm(movie, candidate)`: POST to `/api/reviews/movies/update` with a body containing: `title: movie.title`, `rating: movie.rating`, `gist: movie.review_text` (the update endpoint requires ALL of `title`/`rating`/`gist`/`originalId`/`password` on every save, not just the changed metadata fields — see `src/pages/api/reviews/movies/update.js:11-24` — omitting any of these gets a 400), plus `originalId: movie.id`, `password` (the page-level password field's current value), and the new TMDB fields from `candidate`: `tmdbId`, `mediaType`, `posterPath`, `tmdbOverview: candidate.overview`, `tmdbDate: candidate.date` (same mapping as `handleBackfillConfirm` in the existing edit page, `src/pages/reviews/movies/edit/[id].js:92-101`). Throw/reject on a non-OK response so `BulkBackfillList` can show the row's error state.
4. Add a small link/button from the existing list page `src/pages/reviews/movies/edit.js` (e.g. near the page title) to `/reviews/movies/backfill`, so the new page is actually reachable without typing the URL by hand.
5. If the filtered list is empty, show a plain 'Nothing to backfill' message instead of an empty `BulkBackfillList`.

## Done when

`npm run dev`, visit `/reviews/movies/edit` and confirm a link/button to the new backfill page is present and works. Visit `/reviews/movies/backfill` directly: it lists only movies currently missing `tmdbId` (cross-check against `/api/reviews/movies` — any movie that already has a `tmdbId` must NOT appear), paginated at 15 per page using the shared component. Enter the site password once at the top. For at least one row, candidates load (a compact picker with more than one candidate visible when the search returns several, not just a single auto-picked one), select a candidate, click that row's Confirm, and verify: the row shows a saved/success state, and re-fetching `/api/reviews/movies` shows that movie now has `tmdbId`/`posterPath`/etc set AND unchanged `title`/`rating`/`review_text`(review text)/`date` (confirm the update did not blank out or corrupt the existing rating/review text — this is the main correctness risk of sending the full required-field body per row). Open the browser Network tab while a page of 15+ items loads and confirm requests to `/api/tmdb/search` are NOT all fired simultaneously (sequential, spaced out) — do not just trust the code, actually watch the waterfall. `npm run lint` and `npm run build` both succeed.
