## Do

In `src/components/MarkdownEditor.js`, fix `applyChange(newValue, cursorStart, cursorEnd)` (currently at lines 30-39) to also capture and restore the textarea's `scrollTop`, mirroring the exact pattern it already uses for the cursor/selection restore: (1) before calling `onChange(...)`, read `const el = textareaRef.current;` (it's not yet captured at the top of `applyChange` — currently `el` is only captured inside the `requestAnimationFrame` callback; you'll need it earlier too) and capture `const scrollTop = el ? el.scrollTop : null;` (guard for the ref being null, matching the existing `if (el)` guard style used lower down); (2) call `onChange({ target: { name, value: newValue } })` as today; (3) inside the SAME `requestAnimationFrame(() => { ... })` callback that already does `el.focus()` and `el.setSelectionRange(cursorStart, cursorEnd)`, ALSO assign `el.scrollTop = scrollTop;` (guarded the same way the existing code is — inside the existing `if (el) { ... }` block, added as a new line alongside the other two restores, order doesn't matter but keep it grouped with them for readability). Do not introduce a second `requestAnimationFrame` call or a new ref — reuse the exact one that already exists. In `src/components/MarkdownEditor.test.js`, add a new test (alongside the existing 5) that: builds a controlled demo textarea with enough content to be meaningfully 'long' (e.g. `'line\n'.repeat(500) + 'hello world'` as the initial value, same shape used to empirically validate this spec), renders it, focuses the textarea and sets a real text selection near the end (e.g. `textarea.setSelectionRange(len - 11, len)` to select 'hello world'), manually sets `textarea.scrollTop = 300` to simulate a scrolled-down view (jsdom will not clamp or reject this — confirmed empirically), spies on the scrollTop setter via `const setSpy = jest.spyOn(Object.getPrototypeOf(textarea), 'scrollTop', 'set');` placed AFTER the manual `scrollTop = 300` line (clear it with `setSpy.mockClear()` right after so the setup assignment itself isn't counted), then clicks a toolbar button (e.g. Bold) via `userEvent`, and — after letting the already-scheduled `requestAnimationFrame` callback flush (e.g. `await new Promise((resolve) => requestAnimationFrame(resolve));` after the click, since jsdom implements a real timer-backed `requestAnimationFrame`) — asserts `expect(setSpy).toHaveBeenCalledWith(300)` (the exact captured pre-click value being explicitly written back) AND `expect(textarea.scrollTop).toBe(300)` as a belt-and-suspenders end-state check. Restore the spy (`setSpy.mockRestore()`) at the end of the test. This test must fail against the current (unfixed) `applyChange` — verify this yourself by running it once against the pre-fix code before making the actual fix, then again after, to confirm the fail→pass transition (this is the concrete proof the test isn't trivially-passing, matching what the empirical scratch investigation already showed for today's code).

## Done when

`applyChange` in `src/components/MarkdownEditor.js` captures `el.scrollTop` before calling `onChange` and restores it (`el.scrollTop = <captured value>`) inside the same `requestAnimationFrame` callback that restores the selection range, guarded the same way the existing `el.focus()`/`el.setSelectionRange(...)` calls are (no crash if `textareaRef.current` is null at capture time). The new Jest test in `MarkdownEditor.test.js` spies on the `scrollTop` setter (via `jest.spyOn(Object.getPrototypeOf(textarea), 'scrollTop', 'set')`) and asserts it is called with the pre-click captured value after a toolbar action on a scrolled, long-content textarea — verify BEFORE finishing that this exact test fails on a checkout of the pre-fix code (temporarily `git stash` the fix, run the test, confirm red; restore the fix, confirm green) so it's proven to be a real regression guard, not a trivially-passing one (the naive alternative — 'set scrollTop, click toolbar, assert scrollTop unchanged' — was empirically confirmed to pass even with ZERO fix applied, because jsdom does not reproduce the real browser's scrollTop-reset-on-value-reassignment quirk at all; do not use that shape). `npm test`, `npm run lint`, and `npm run build` all pass. Finally, since this bug is fundamentally only observable in a real browser (jsdom cannot reproduce the underlying quirk, as established above) and the whole point is restoring a real user-visible scroll position, do a manual real-browser check as part of closing this out: run the dev server, open any review add/edit form (e.g. `/reviews/movies/add`), paste in enough text to make the textarea scroll, scroll down, select some text, click Bold (or Italic/List/Link), and confirm the textarea's visible scroll position does NOT jump back to the top — note in the worklog that this manual check was performed and passed.
