## Overview
Re-attempt of T391, which blocked on its very first attempt (rung 0, haiku-4-5) with a scope-creep structural-gate failure — the diff touched CLAUDE.md, which the spec itself explicitly instructs updating ('CLAUDE.md's component notes are updated ... add it to the src/hooks/* line in the file map') but which was never added to the task's `scope` array. This is the documented 'scope-coverage gap' failure pattern (see .harness/custom/CLAUDE.md's 'Scope-coverage gaps' section, previously seen in T141/T147/T151/etc.) — the spec told the builder to do something scope forbade. Verified the underlying feature still genuinely does not exist: src/hooks/ has no useExpandableText.js, and ReviewCard.js's SquareCoverCard and poster-banner block still render full, untruncated review text — only SpineCoverCard (books) truncates today. No later commit (checked git log on the four original scope paths) touched this. The follow-up is identical in substance to T391's Do/Done-when — only the scope array is fixed to include CLAUDE.md so the instructed doc update is no longer out-of-bounds.

## Do
Same as T391's original ## Do, unchanged in substance:
1. Extract the repeated expand/collapse pattern (useState for `expanded`, calling truncateReviewText(fullText, maxLength) from src/lib/reviewText.js, computing `displayText = expanded ? fullText : previewText`, and a toggle callback) into a small shared hook, `src/hooks/useExpandableText.js` exporting `useExpandableText(fullText, maxLength = 260)` returning `{ displayText, truncated, expanded, toggle }`. Do NOT change truncateReviewText's signature or behaviour.
2. Update SpineCoverCard (books, in src/components/ReviewCard.js) to use the new hook in place of its current inline useState+truncate logic, preserving its exact current rendering/behaviour (no visible change for books).
3. Update SquareCoverCard (albums, same file) to call the same hook over its review text (from getThoughts()) with maxLength=260, and render a 'Read more'/'Show less' toggle button (only shown when `truncated` is true) beneath its highlights/snippet content, following the existing book-card button's interaction pattern.
4. Update the poster-banner card block (movies + tv, same file) the same way: hook over getThoughts() with maxLength=260, render truncated/expanded text in place of the current always-full render, same toggle button shown only when truncated.
5. Add or reuse CSS in src/styles/globals.css for the new buttons — either generalize `.spine-cover-expand-btn` into a shared class (e.g. `.review-expand-btn`) applied by all three cards, or add matching sibling rules (`.square-cover-expand-btn` / `.poster-banner-expand-btn`) consistent with each card's existing visual language — prefer the shared class if it drops in cleanly without visual regressions.
6. Update scripts/_visual-harness.mjs: ensure synthetic fixtures for the albums/movies/tv PAGES entries include at least one review long enough to trigger truncation, and add a FLOWS entry per newly-truncating variant (or one combined flow) that clicks the new toggle and screenshots the expanded state.
7. Add or extend a co-located test (e.g. ReviewCard.test.js) covering: SquareCoverCard and the poster-banner card each show the toggle only when text exceeds the truncation length, hide it when short, and that clicking swaps displayed text + button label.
8. Update CLAUDE.md's `src/hooks/*` file-map line to add `useExpandableText` — this step is now IN scope (CLAUDE.md has been added to this task's scope array specifically so this instruction is buildable; do not skip it and do not let it trip a scope gate, since it is explicitly authorized here).

## Done when
Loading /reviews/albums, /reviews/movies, and /reviews/tv with a long review present, that card shows a truncated preview (~260 chars, whitespace-safe cut ending in '…') with a 'Read more' button; clicking it expands to the full review and swaps the label to 'Show less', with no page reload; a short review on the same pages renders in full with no toggle shown at all. Books' existing SpineCoverCard behaviour is unchanged (still truncates/expands exactly as before, now backed by the shared hook). `npm run lint`, `npm test`, and `npm run build` all stay green, including new/updated tests for the albums and poster-banner truncation behaviour. `node scripts/visual-check.mjs` is run and the relevant screenshots (per scripts/visual-out/manifest.json) are actually inspected: truncated-state and expanded-state shots for albums/movies/tv render correctly with no layout breakage, and scripts/_visual-harness.mjs is left updated with new long-review fixtures + FLOWS entries. CLAUDE.md's `src/hooks/*` file-map line is updated to list `useExpandableText` (this file is in scope for this task, unlike the original T391).
