## Do

On /projects, cards for archived repos render a black corner tag (`.project-card-archived-tag`, src/pages/projects/index.js:225-227, markup `<div className="project-card-archived-tag">archived</div>` inside `.project-card`) that the owner says doesn't read as one continuous curve with the card's own top-left rounded corner — it looks slightly disjoint where the tag meets the card's border.

Relevant CSS today:
- `.project-card` (src/styles/globals.css:1850-1861): `position: relative; border: var(--border-width) solid var(--color-ink); border-radius: var(--radius-card);` — `--border-width: 2px` (globals.css:57), `--radius-card: 18px` (globals.css:53).
- `.project-card-archived-tag` (src/styles/globals.css:1863-1876): `position: absolute; top: 0; left: 0;` with NO inset, plus `border-top-left-radius: var(--radius-card)` — i.e. it reuses the exact same 18px radius as the outer card, flush at `top:0;left:0` with no offset for the card's 2px border.

STEP 1 — Diagnose visually before touching any fix. Run `npm run dev`, open `/projects` in a browser, and find (or temporarily force via devtools/data — at least one repo in the GitHub response has `archived: true`, or temporarily hardcode `repo.archived = true` on one card in devtools to guarantee a visible case) a card showing the archived tag. Zoom into the top-left corner (browser devtools zoom, or a screenshot cropped tight to that corner) and describe precisely what's wrong: e.g. is the tag's curve radius visibly larger/smaller than the card's so there's a sliver of mismatched curvature, does the tag sit outside/inside the card's border line by ~2px so the corner looks doubled, etc. Do not skip this — the idea text explicitly flags that the root cause (border-width not accounted for) is a plausible CSS-reading guess, NOT a confirmed diagnosis.

STEP 2 — Based on what you actually observe, pick a fix. Plausible directions (pick whichever the observed mismatch calls for, don't apply blindly):
  a) Inset the tag by `var(--border-width)` (`top: var(--border-width); left: var(--border-width);`) so it sits inside the card's border line, and reduce `border-top-left-radius` on the tag to account for the inset (an inner corner nested against an outer radius R with inset I conventionally wants radius `max(R - I, 0)`, i.e. here `calc(var(--radius-card) - var(--border-width))`), OR
  b) Some other adjustment if the visual diagnosis in step 1 points elsewhere (e.g. the mismatch is actually about the bottom-right radius, or about the tag overlapping the border stroke itself).
  Keep the tag's other properties (background, padding, font, `border-bottom-right-radius`) as-is unless the visual check shows those also need adjusting.

STEP 3 — After editing, reload `/projects` in the browser and re-inspect the same corner at the same zoom level. Confirm the tag's outer curve now reads as one continuous, smoothly nested arc with the card's own bordered corner — no visible seam, no doubled line, no mismatched curvature radius.

Files: src/pages/projects/index.js:225-227 (markup, likely unchanged), src/styles/globals.css:1863-1876 (`.project-card-archived-tag`) and :1850-1861 (`.project-card`, for reference values only — don't change the card itself unless the diagnosis requires it).

## Done when

`npm run dev` is run and a REAL browser render of an archived-repo card on `/projects` is inspected zoomed into the top-left corner, BOTH before the CSS change (to confirm/characterize the reported mismatch in your own words — what specifically looked disjoint) AND after the CSS change (to confirm the fix). The done-when write-up must describe both observations concretely (not just 'looks fine now') — e.g. what the corner looked like before (sliver of mismatched curve / doubled border line / whichever it actually was) and what changed after. The after-state must show the tag's rounded corner and the card's rounded border corner reading as a single continuous curve with no visible seam or curvature mismatch at any zoom level reasonably used for manual QA. `npm run lint`, `npm test`, and `npm run build` all still pass. No unrelated visual regression to non-archived cards (their corners are untouched, since they never render `.project-card-archived-tag`). Docs: this is a self-contained CSS nit with no new convention, data model, page, or env var — CLAUDE.md/README.md need no update; if the fix happens to introduce anything doc-worthy (e.g. a new reusable CSS pattern), update CLAUDE.md's file map/conventions in the same commit per the repo's Definition of Done.
