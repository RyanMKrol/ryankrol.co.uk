## Overview
The owner confirmed idea #2's 'fully migrate' means Google Books should be fully removed once Hardcover is wired in, not kept as a fallback. This unit deletes the now-dead Google Books code path and its config once hardcover-migration-a/b/c (which already stopped calling it) have shipped.

## Do
First grep across `src/` for `googlebooks` and `GOOGLE_BOOKS_API_KEY` to confirm no remaining references now that hardcover-migration-a/b/c have landed (the API route, BookSearch.js, and the Bulk Backfill page should all be Hardcover-only by this point) — if anything unexpected still references it, stop and report rather than deleting underneath a live reference. Delete src/lib/googlebooks.js and its test file src/lib/googlebooks.test.js. Remove the `GOOGLE_BOOKS_API_KEY` row entirely from CLAUDE.md's Key env vars table, and note in the worklog that the owner should also remove `GOOGLE_BOOKS_API_KEY` from Vercel's project environment variables (this repo/task cannot do that itself). Double check CLAUDE.md's file map row for src/pages/api/books/search.js no longer mentions Google Books.

## Done when
`grep -rE 'googlebooks|GOOGLE_BOOKS_API_KEY' src/` returns no matches; src/lib/googlebooks.js and its test file no longer exist; CLAUDE.md's env var table no longer lists GOOGLE_BOOKS_API_KEY; `npm run lint`, `npm test`, and `npm run build` all pass (no broken imports from the deletion).
