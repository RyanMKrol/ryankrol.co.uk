## Overview
The owner confirmed the target page is src/pages/listening/index.js (not any of the other candidates originally considered — Hot Takes, review lists, vinyl, projects, workouts). That page fetches Last.fm top albums via GET /api/lastfm/top-albums?period=3month&limit=50, and Last.fm's own API only supports specific period values (overall/7day/1month/3month/6month/12month), not arbitrary day counts — so per the owner's explicit correction, this is NOT a client-side date-filter of an already-loaded array (no shared dateRange.js/DateRangeFilter.js involved at all), and NOT day-labeled either. It's three hardcoded pill options — 1 month / 3 months / 6 months — that change which `period` is requested from the existing API route (src/pages/api/lastfm/top-albums.js, which already accepts an arbitrary `period` query param with zero changes needed), using the same PillGroup component already used for filters on Vinyl (src/pages/vinyl/index.js) and Perfumes (src/pages/reviews/perfumes/index.js, its ownership filter). The owner confirmed the default should change from today's hardcoded 3-month default to 1 month.

## Do
In src/pages/listening/index.js: import PillGroup from '../../components/PillGroup' (controlled `options`/`value`/`onChange` shape — see src/pages/reviews/perfumes/index.js's `OWNERSHIP_FILTER_OPTIONS`/`PillGroup` usage around lines 123-128 for the exact pattern to mirror). Add three options whose `value` fields are Last.fm's own period strings and double as the pill labels: {value:'1month', label:'1 month'}, {value:'3month', label:'3 months'}, {value:'6month', label:'6 months'} — no separate mapping/translation layer, the pill's value IS the API's period param. Add `useState('1month')` for the selected period (1 month is the new default, replacing today's hardcoded `period=3month` in the fetch call). Update the existing `fetchTopAlbums` effect so it depends on this state and passes it as the `period` query param to `/api/lastfm/top-albums` (replacing the hardcoded `period=3month`), refetching — and showing the existing loading state — whenever the pill selection changes. Update the hardcoded subtitle text (`top 50 albums · last 90 days · via last.fm`, in the `collection-review-meta` paragraph) to reflect the currently-selected period instead of a fixed 'last 90 days' (e.g. 'top 50 albums · last 1 month · via last.fm', pluralizing appropriately for 3/6 months). Render the PillGroup above or alongside the existing page header. Do not touch src/pages/api/lastfm/top-albums.js — it already accepts any `period` value with no changes needed.

## Done when
The Listening page renders a 3-pill filter (1 month / 3 months / 6 months, using the site's existing PillGroup/Pill visual pattern) above its ranked album list, defaulting to '1 month' selected on first load. Selecting a different pill refetches /api/lastfm/top-albums with the matching period and re-renders the ranked list for that window, showing the existing loading state during the refetch. The subtitle text updates to match the active period. npm run lint / npm test / npm run build all pass. A new FLOWS entry is added to scripts/_visual-harness.mjs for this filter interaction (selecting a different period pill) per this repo's visual-verification convention, and node scripts/visual-check.mjs is run with the relevant screenshots reviewed.
