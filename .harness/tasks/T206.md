## Do

In `src/styles/globals.css`, the `.poster-banner` rule (currently ~lines 193-198) sets `aspect-ratio: 16 / 10` on the gradient banner element itself. That ratio makes the banner box far taller than its content (the bottom-anchored title), so the empty space above the title reads as a huge mismatched gap versus the real `1rem` bottom padding below it.

Measured the intended proportions directly from the design reference (`design/renders/2a.png`, the chosen movies-list render): each card is ~280px wide; the gradient banner band is consistently ~77-79px tall across multiple cards (both row 1 and row 2) — a width:height ratio of ~3.6:1. (For reference, the OLD `16/10` = 1.6:1 ratio actually matches the design's WHOLE CARD proportions, not the banner alone — strong evidence the ratio was applied to the wrong element/box when this was implemented.)

Change `.poster-banner`'s `aspect-ratio` from `16 / 10` to `18 / 5` (= 3.6, matching the measured design proportion). Leave every other property on `.poster-banner` unchanged (`position: relative`, `display: flex`, `align-items: flex-end`, `padding: 1rem 1.25rem`) — the fix is the single aspect-ratio value, not a padding change (padding top/bottom is already equal; the visible gap was unused banner height, not padding). Do not touch `.poster-banner-title`, `.poster-banner-body`, `.poster-banner-meta`, `.poster-banner-date`, `.poster-banner-snippet`, or any gradient-generation code (e.g. `gradientForKey` / `CoverTile.js`) — the gradient mechanism itself is out of scope for this task, only the box's height. Both `src/pages/reviews/movies/index.js` and `src/pages/reviews/tv/index.js` already pass `styleVariant="poster-banner"` to `ReviewCard`, so this single shared-class change fixes both pages at once — no page file needs editing.

## Done when

`npm run lint`, `npm test`, and `npm run build` all still pass (no logic changed, but keep the gate green). Then `npm run dev`, open `/reviews/movies` and `/reviews/tv` (both render the `poster-banner` cards) and visually confirm: the gradient band is now noticeably shorter/squatter (roughly a 3.6:1 width:height band, not the old ~1.6:1 near-square-ish block); the empty space above each card's title now reads as roughly proportionate to the `1rem` bottom padding below the title text, instead of a large disconnected gap; the gradient still fills the banner corner-to-corner with no clipping/distortion; the title text remains fully legible and doesn't overflow or collide with the white body section below; the 3-column card grid still lays out cleanly with no visual breakage on both the movies and TV list pages.
