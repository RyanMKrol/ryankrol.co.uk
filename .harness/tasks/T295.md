## Do

Edit `.claude/commands/convert-ideas.md` (the /convert-ideas skill file) to make Stage 2's per-unit agent brief put real, non-negotiable stress on nailing down a verifiable Definition of Done before a task is shaped — today the only mention of rigor is a soft parenthetical on the `specDoneWhen` schema-example line ('concrete and runnable where possible', currently line 279), with no actual interview-time interrogation step forcing it. This is a `needs-human` task under the '.harness/-scoped tasks are always needs-human' rule in `.harness/CLAUDE.md` (this file is the /convert-ideas consolidation pipeline's authoring instructions, i.e. harness-adjacent tooling), so build it directly in an interactive session rather than via the autonomous loop.

Motivating incident (cite this in the new text, don't just gesture at 'rigor' abstractly): T273 ('Render full Markdown in review display components') was marked done — lint/tests/build all passed per its Definition of Done — but the owner later had to manually-fail it via the dashboard because, in their own recorded words (`.harness/manual-fail.json`): "The Markdown does not appear to be rendering on the actual site under the Review card... clearly the testing was not adequate and we need to try this again." That's a DoD satisfied on paper by green automated checks while the actual observable feature didn't work in production — exactly the gap this task closes at the authoring stage, before a task ever reaches a builder. A second, related precedent already in the ledger is worth citing too: T082's backfill script had tests that passed only because its fixtures encoded the same wrong field names as its own bug, and it crashed on first contact with real production data (see `.harness/manual-fail.json`'s T082 entry) — the same 'tests green, real world broken' failure mode, from a non-UI example.

Concrete edits to make (as a starting draft for the owner to refine, not a rigid mandate):

1. Insert a new paragraph into Stage 2's interview step (currently step '2. Interview…', between the existing paragraph ending '...don't manufacture a question for something you could reasonably call yourself.' at line ~205 and the 'If you have genuine open questions...' paragraph at line 207), something like:

   '**Definition of Done rigor (non-negotiable) — interrogate "done" before you shape anything.** Before finalizing any task, answer for yourself: if this task is marked done, what specific, observable thing will be true in the real running site — not just in a test file or lint/build output? A Definition of Done that cites only lint/test/build passing is not sufficient by itself for any task that changes user-visible behavior — automated checks can pass while the actual feature silently does not work; this already happened once (T273: Markdown rendering shipped with lint/tests/build all green, then had to be manually-failed because the Markdown never actually rendered on the live Review card — the owner's own words are in `.harness/manual-fail.json`). Concretely:\n  - For any task touching UI/rendered output (`facets.layer == "ui"`, or anything a human would look at and judge), `specDoneWhen` MUST name a concrete manual/visual verification step in addition to (not instead of) automated checks — a specific input to try and the specific visual outcome that proves it worked (e.g. "open /reviews/movies, add a review with a Markdown body containing **bold** and a bullet list, confirm the rendered card shows real bold text and a real bullet, not literal asterisks/dashes"). "Verify it renders correctly" does not meet this bar.\n  - For any task where tests could plausibly pass against fixtures that don\'t reflect real/production data shape (new integrations, data transforms, caching changes — see T082\'s post-mortem in `.harness/manual-fail.json`, whose own tests passed because its fixtures encoded the same wrong field names as its bug), `specDoneWhen` must say what evidence beyond a green test suite would actually convince a skeptical reviewer the feature works against real data.\n  - If you genuinely cannot articulate a concrete, verifiable Done-when — you\'re not sure what "working" would even look like, or the idea text doesn\'t pin one down — that is itself a genuine open question. Do not write a vague or automated-checks-only `specDoneWhen` just to have something to submit; surface it via `.harness/.pending-questions/<slug>.json` and ask the owner directly what observable outcome would prove the task done.'

2. Strengthen the `specDoneWhen` line in the shaping-step JSON schema example (currently line 279: `"specDoneWhen": "<the '## Done when' body text, concrete and runnable where possible>"`) so the schema comment itself echoes the rule instead of just a soft parenthetical, e.g.: `"specDoneWhen": "<the '## Done when' body text — MUST be concrete and verifiable: state exactly what a reviewer would run/open/click and what they would observe if it worked, not just 'tests pass'. For any facets.layer == \"ui\" task, this includes the manual/visual check required by the Definition-of-Done-rigor rule above, worded so a future reader can actually carry it out.>`

3. Leave the existing UI-verification note just below the schema block (currently lines ~286, 'Any task with facets.layer == "ui" should have a specDoneWhen that requires verification via this repo's actual convention: verify: ["run-app"] plus a concrete manual check...') intact but cross-reference it from the new paragraph in (1) rather than duplicating it wholesale — the new paragraph should point at this existing convention as the mechanism, not invent a second one.

Out of scope / explicitly deferred, don't attempt as part of this task: adding a new mechanical/automated check (e.g. a `check-task-scope.sh`-style script that lints `specDoneWhen` text for vagueness) — that would be a heavier, separate follow-up (a new script, likely under `.harness/`, with its own needs-human authoring) and isn't needed to satisfy the owner's actual ask here, which is about the interview brief's wording. Also don't touch any 'worked example' walkthrough section — the file was checked in full and contains only the JSON schema block itself, no separate prose worked-example to update.

## Done when

`.claude/commands/convert-ideas.md` contains all three edits described above, verified by direct inspection after the change:
1. A new named paragraph inside Stage 2's interview step (before the 'genuine open questions' paragraph) that: (a) explicitly states an automated-checks-only DoD (lint/test/build) is not sufficient by itself for user-visible-behavior tasks, (b) names the T273 incident by task id and quotes or closely paraphrases the owner's manual-fail reason from `.harness/manual-fail.json`, (c) requires any `facets.layer == "ui"` task's `specDoneWhen` to name a concrete manual/visual check (specific input + specific observable outcome, not a vague phrase like 'verify it renders correctly'), (d) requires non-UI tasks whose tests could pass against unrealistic fixtures to state what evidence beyond green tests would convince a skeptical reviewer, and (e) explicitly instructs the agent to treat an unanswerable 'what does done look like' as a genuine open question to be surfaced via `.harness/.pending-questions/<slug>.json` rather than papered over with a vague specDoneWhen.
2. The `specDoneWhen` line in the Stage 2 shaping-step JSON schema example (originally the line reading '<the '## Done when' body text, concrete and runnable where possible>') is reworded to require concrete, observable, verifiable Done-when text rather than the current soft parenthetical, and references the new rule from (1).
3. The pre-existing `facets.layer == "ui"` → `verify: ["run-app"]` manual-check convention (originally just below the schema block) is preserved and cross-referenced by the new text rather than duplicated or contradicted.

Additionally confirm: the edit does not break the file's existing Markdown structure (the `>` blockquote nesting under Stage 2 is preserved so the agent-brief block still renders/parses as one continuous quoted brief), the JSON-schema code fence inside the brief is still valid illustrative JSON-with-comments (i.e. still recognizable as the same shape other tasks in this repo's `TASKS.json`/`.pending-tasks/*.json` files use), and no other Stage (0, 1, 3, 4) instructions were altered as a side effect. Since this is `needs-human` with `expectsTest: false`, there is no automated lint/test/build gate — "done" is the owner's own read-through confirming the three points above are present and don't conflict with the rest of the file.
