## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/3c.png` (desktop albums list) and
`/Users/ryankrol/Downloads/design_handoff/renders/4h.png` (mobile) for reference.

Add a new **2-col square-cover** presentation to `src/components/ReviewCard.js` (a third variant
alongside poster-banner from T150 and horizontal-spine from T152): a square cover tile on the left
(reuse `CoverTile` in its square aspect), and on the right: title + read-only star rating, artist
name beneath, then a "HIGHLIGHTS · track · track · track" line (mono, accent-colored "HIGHLIGHTS"
label per the design, using `item.highlights` — check `ReviewCard.js`'s existing `getThoughts()`
helper, which already returns `item.highlights` for the album type; format it into the
"· "-joined track list shown in the render, or if `highlights` is stored as free text rather than a
delimited list, split it into a reasonable count of "highlight" fragments — check
`src/pages/api/reviews/albums/add.js` for the actual stored shape of `highlights` before assuming).

Rebuild `src/pages/reviews/albums/index.js` with this variant: same header/meta/search/sort pattern
as T150, but a 2-column grid instead of 3-column. No pagination in this task (T155).

## Done when

- `ReviewCard.js` supports the 2-col square-cover variant with a HIGHLIGHTS line, without breaking
  the poster-banner (T150) or horizontal-spine (T152) variants.
- The albums list page shows the header/meta/search/sort pattern and a 2-col grid bound to real data
  from `/api/reviews/albums`.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/albums`, confirm search/sort work, cards
  show artist + a highlights line correctly, and movies/tv/books pages from T150-T152 still render
  correctly. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T166.
