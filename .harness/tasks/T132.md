## Do

In `src/pages/reviews/tv/add.js`, the inline TMDB-match error paragraph at lines ~117-121 (`{!tmdbMatch && (<p className="error-message">Search and select a TMDB match before saving</p>)}`) renders unconditionally from the moment the page mounts, because it's driven only by `tmdbMatch` state, which is `null` on initial mount — so the red error box is visible before the user has done anything (e.g. with the title field still empty). Fix this by gating the paragraph on an 'attempted submit' flag instead of showing it by default:

1. Add a new state variable alongside the existing `useState` calls near the top of the component (after `const [messageType, setMessageType] = useState('');` around line 17): `const [hasAttemptedSubmit, setHasAttemptedSubmit] = useState(false);`
2. In `handleSubmit` (starts around line 34), add `setHasAttemptedSubmit(true);` as the first statement inside the function, right after `e.preventDefault();` and before the `if (!tmdbMatch) { ... }` check. This marks the form as 'attempted' on every submit click, whether it succeeds or fails.
3. Change the inline error render condition (around line 117) from `{!tmdbMatch && (` to `{hasAttemptedSubmit && !tmdbMatch && (` — so the paragraph only renders once the user has tried to submit at least once AND still has no confirmed match.

Do NOT change: the top-of-form `message`/`messageType` banner block (around lines 90-94, `{message && (<div className={...}>{message}</div>)}`) — it already only populates on a submit attempt via `setMessage(...)` calls inside `handleSubmit`/the fetch response handler, so it's working correctly today. Do NOT change the `handleSubmit`-time validation itself (`if (!tmdbMatch) { setMessage('Search and select a TMDB match before saving'); setMessageType('error'); return; }` around lines 37-41) — leave that logic exactly as is; this task only changes when the SEPARATE inline paragraph next to the TmdbSearch component is shown. Do NOT touch `src/pages/reviews/movies/add.js` — an identical, separate task covers that file. Do not touch any other file (API routes, other pages, components).

## Done when

- `src/pages/reviews/tv/add.js` does NOT render the inline `error-message` paragraph ('Search and select a TMDB match before saving', the one directly under the `TmdbSearch` component) on initial page load, before any user interaction — confirmed by reading the rendered JSX condition (`hasAttemptedSubmit && !tmdbMatch`) and by manual check below.
- After clicking the 'Add Review' submit button with no TMDB match selected, the inline error paragraph DOES appear (in addition to the existing top-of-form message banner), and the submit is still blocked exactly as before (no regression to the T086 'require a confirmed TMDB match' behavior — the button remains `disabled` while `!tmdbMatch`, and `handleSubmit`'s own guard still returns early).
- Once the user selects a TMDB match (via `TmdbSearch`'s `onSelect` → `setTmdbMatch`), the inline error paragraph disappears again, same as it does today.
- `npm run lint` passes with no new errors.
- `npm run build` succeeds.
- Manual verification (`verify: ["run-app"]`): run `npm run dev`, open `/reviews/tv/add` in a browser in a fresh state (no prior submit attempt on this page load) — confirm the red 'Search and select a TMDB match before saving' box is NOT visible while the title field is still empty and nothing has been clicked. Then click 'Add Review' (it will be disabled if rating is also 0 — set a rating first, or otherwise trigger `handleSubmit`, e.g. by temporarily filling required fields) without selecting a TMDB match — confirm the inline message now appears next to the TMDB search box.
