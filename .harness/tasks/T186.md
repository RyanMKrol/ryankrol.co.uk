## Do

All five perfume card variants are built and rendered together with real data at `/dev/perfume-variants`. Run the dev server (`npm run dev`) and open that page. Compare the five presentational directions (Variant 1 Stat Block — data-dense; Variant 2 Editorial — magazine/reading-focused; Variant 3 Bottle Shelf — visual gradient-tile grid; Variant 4 Season & Occasion Planner — practical/scale-bar framing; Variant 5 Minimal Table Row — dense scannable rows with an expandable disclosure) against real perfume data. Decide which one (if any) you want to become the live `/reviews/perfumes` card, or whether none of them are good enough to promote (a legitimate outcome — see T092, where an earlier five/three-variant chooser sweep on this repo ended with the owner rejecting all variants and deleting the experiment; if that happens here, note in your decision that none are promoted and the follow-up promote task should be treated as superseded/no-op rather than forced through).

## Done when

You've reviewed all five variants live at `/dev/perfume-variants` with real data. You've recorded your decision by creating/appending a `## Decision` section to `.harness/designs/perfume-card-variants-decision.md` (create the file if it doesn't exist), stating either which variant number (1-5) won and a brief one-line reason, or explicitly that none were good enough to promote and why — this is the durable record the next task reads. This task is marked done via `.harness/mark-done.sh <its-real-task-id>` (or the dashboard's 'Mark done' button) once the decision is recorded.
