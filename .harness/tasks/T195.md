## Do

In `src/styles/globals.css`, the `.collection-form-card` rule (currently around lines 2587-2594, under the `/* ===== Collection review forms (T162): movies/tv add + edit ===== */` comment) has no `margin-top` — its `margin: 0 auto` shorthand only centers the card horizontally. Meanwhile `.page-title` (around lines 663-669) has `margin-bottom: 0` explicitly. Wherever a page renders `<h1 className="page-title">` immediately followed by `<div className="collection-form-card">`, there is currently zero vertical gap between the title and the card, which the owner described as looking "a little funky." This exact pairing appears on all 11 of these pages: `src/pages/reviews/movies/add.js`, `src/pages/reviews/movies/edit/[id].js`, `src/pages/reviews/tv/add.js`, `src/pages/reviews/tv/edit/[id].js`, `src/pages/reviews/books/add.js`, `src/pages/reviews/books/edit/[id].js`, `src/pages/reviews/albums/add.js`, `src/pages/reviews/albums/edit/[id].js`, `src/pages/reviews/perfumes/add.js`, `src/pages/reviews/perfumes/edit/[id].js`, and `src/pages/vinyl/add.js`.

Fix it by adding a single `margin-top` declaration to the `.collection-form-card` rule in `src/styles/globals.css` — do NOT change `.page-title`'s `margin-bottom`, since `.page-title` is used site-wide on plain list/section headers that don't have this gap problem and already look correct; touching it globally would ripple into unrelated pages.

Use `margin-top: 2rem;` for consistency with this design system's existing spacing scale — `.collection-review-header` (around line 401-408) uses `margin-bottom: 2rem`, and `.home-hero` (around line 2768-2770) uses `margin-top: 2rem` for an analogous title-to-content gap. Do not touch any other rule or any of the 11 page files themselves — this is a single shared CSS fix, not a per-page change.

## Done when

`.collection-form-card` in `src/styles/globals.css` has a `margin-top: 2rem;` declaration added alongside its existing `margin: 0 auto;` (both may coexist, or combine them into one `margin` shorthand — either is fine as long as the net effect is `margin-top: 2rem` and horizontal auto-centering is preserved). `.page-title`'s `margin-bottom` is untouched (still `0`). Verify via `run-app`: run `npm run dev`, then open at least 2-3 of the 11 affected pages in a browser — e.g. `/reviews/movies/add`, `/reviews/vinyl/add` (i.e. `/vinyl/add`), and `/reviews/books/edit/<some-existing-book-id>` — and confirm a visible vertical gap now exists between the page's `<h1>` title and the form card below it, on both desktop and mobile widths. `npm run lint`, `npm test`, and `npm run build` all still pass (a pure CSS addition shouldn't affect any of them, but confirm).
