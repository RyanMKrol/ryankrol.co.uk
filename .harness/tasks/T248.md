## Do

Fix /reviews/books/edit (src/pages/reviews/books/edit.js) so it visually matches the real books list page (/reviews/books, src/pages/reviews/books/index.js) instead of rendering full-width cards, while keeping the existing top-right 'Edit' button overlay.

Confirmed: books/edit.js has the exact same bug as movies/tv/albums' edit pages — src/pages/reviews/books/edit.js (around lines 63-80) wraps each `ReviewCard` in a `<div className="reviews-wrapper">` instead of the masonry-column layout the real list page uses. By the time this task runs, `src/pages/reviews/books/index.js` has already been converted (in a separate, already-completed task) to render via the shared `<MasonryColumns>` component (`src/components/MasonryColumns.js`) + `useResponsiveColumnCount` hook (`src/hooks/useResponsiveColumnCount.js`), using className `spine-cover-list` / columnClassName `spine-cover-list-col` and breakpoint `useResponsiveColumnCount(2, 700)` — read that file NOW for the exact current pattern before making this change.

Do:
1. In `src/pages/reviews/books/edit.js`, replace the `<div className="reviews-wrapper">{books.map((book) => ( <div key={book.id} className="review-card-with-edit"> ... </div> ))}</div>` block (around lines 63-80) with the same `<MasonryColumns>` usage books/index.js uses (`items={books}`, `columnCount={useResponsiveColumnCount(2, 700)}`, `className="spine-cover-list"`, `columnClassName="spine-cover-list-col"`), except each `renderItem` returns the EXISTING `<div className="review-card-with-edit">` wrapper containing the `ReviewCard` (`styleVariant="spine-cover"`) and the existing `<Link href={...} className="edit-button-overlay">Edit</Link>` — keep the edit-button-overlay markup unchanged, only change what contains it.
2. Import `MasonryColumns` and `useResponsiveColumnCount` (relative paths `../../../components/MasonryColumns`, `../../../hooks/useResponsiveColumnCount`).
3. Do NOT add search/sort/pagination controls — only the wrapping/layout technique changes.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/books/edit — cards render in the same multi-column packed masonry layout as /reviews/books (not full-width), each card still shows the pill-shaped 'Edit' button top-right, and clicking it still navigates to /reviews/books/edit/<id>.
