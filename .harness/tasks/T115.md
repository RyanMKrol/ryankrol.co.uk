## Do

Same shape as `review-edit-url-id-1`/`-5` but for albums. Source table `AlbumRatingsV2` is currently keyed on a COMPOSITE key `title`+`author`... correction, `title`+`artist` (both String) — see `src/pages/api/reviews/albums/update.js:53-58` (`Key: { title: originalTitle, artist: originalArtist }`) and `delete.js:24-30`. Fields to preserve per item: `title`, `artist`, `rating`, `highlights`, `date`, `thumbnail`, and the optional `lastfm` object (`{ mbid, url, listeners, playcount, tags, trackCount, summary, releaseDate, images }` — see `README.md:117-144` for the full documented shape).

Write `src/scripts/migrateAlbumsToIdTable.js` — note this repo ALREADY has `src/scripts/migrateAlbumsToV2.js` from a previous migration (`AlbumRatings` → `AlbumRatingsV2`); read it first as the direct precedent for both style AND for how a prior album migration handled table creation + `ScanCommand`/`BatchWriteCommand` copying, then follow the same shape for `AlbumRatingsV2` → `AlbumRatingsV3`. Create `AlbumRatingsV3` with `id` (String) as the SOLE HASH partition key (replacing the `title`+`artist` composite — no sort key on the new table); scan all of `AlbumRatingsV2`; guard against double-migration; generate `id: randomUUID()` per item, copying every existing field (including the nested `lastfm` object) unchanged, with `title`/`artist` becoming ordinary non-key attributes; batch-write in chunks of 25; log old-count vs new-count; leave `AlbumRatingsV2` fully intact; touch nothing under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js`.

Extract the pure transform into a testable function and add `src/scripts/migrateAlbumsToIdTable.test.js` covering field preservation (including the nested `lastfm` object surviving unchanged), `id` presence, and UUID shape.

## Done when

`npm test` and `npm run lint` pass. Diff touches only the two new script files. `needs-human`: a human must run `node src/scripts/migrateAlbumsToIdTable.js` against production `AlbumRatingsV2`/`AlbumRatingsV3`, confirm counts match, and mark this task done via `.harness/mark-done.sh`.
