## Do

In `src/components/PerfumeCharacteristics.js`, the `SpraySpotCounter` function (defined around lines 70-160) renders each "application spot" as a card: an outer `<div>` (lines 87-102, with inline styles for border/background/padding/minWidth — this is the visual card) containing an inner `<button type="button" onClick={spray} onContextMenu={unspray}>` (lines 103-118) that wraps ONLY the 🧴 emoji glyph and its transient 💦 "poof" spans. Below/outside that button, still inside the outer div, sit two more elements that are NOT part of the button: the spot-name `<span className="form-label">` (line 137) and the spray-count `<span>` (lines 138-146). Because only the emoji glyph is wrapped in the clickable `<button>`, tapping the spot-name label or the count text does nothing — this is the reported bug ("only part of it registers the tap").

Fix both issues in this one component, keeping the existing interaction model exactly as-is (tap = increment via `spray()`, right-click = decrement via `unspray()`, the poof animation, the `applicationSpotCounts`/`countsToApplicationSpots` helpers, and the `ApplicationSpotsSprayer` parent — none of that changes):

1. **Whole-card tap target.** Restructure `SpraySpotCounter` so the ENTIRE card (emoji + spot label + count text) is one clickable target, not just the emoji. The cleanest approach: convert the outer container from a `<div>` to a `<button type="button">`, moving `onClick={spray}` and `onContextMenu={unspray}` and the `title` attribute up to it, and demote the current inner `<button>` around the emoji to a plain `<span>` (or `<div>`) — HTML doesn't allow a `<button>` nested inside another `<button>`, so there must only be one button element per card, and it must be the outer one so every child (emoji, poofs, spot label, count) sits inside it and receives the tap. Preserve all existing visual styling (border, background, border-radius, flex layout, hover/active states implied by the border/background swap on `count > 0`) — a `<button>` element accepts the same inline `style` object the `<div>` already has; just also neutralize default browser button chrome that would otherwise leak in (e.g. inherited font, default border/background, text-align, cursor) so the card looks the same as before, just now fully clickable. Keep the poof `<span>` elements' positioning/animation working identically inside the new structure.
2. **Smaller buttons.** Reduce the button/card's footprint so more fit comfortably per row and the tap target feels lighter — e.g. reduce the emoji `fontSize` from `1.75rem` to something in the `1.1rem`-`1.35rem` range, reduce the card's `minWidth` from `6.5rem` down to roughly `4.5rem`-`5.5rem`, and tighten the `padding` (currently `0.75rem 0.5rem`) and inter-element `gap` (currently `0.4rem`) proportionally. Exact values are a judgment call — prioritise the tap target staying comfortably thumb-sized (don't go so small it becomes hard to hit) while visibly shrinking from the current size shown in the screenshot. Keep the spot-name label and spray-count text legible (don't shrink their font sizes below the existing `form-label` / `0.875rem` conventions used elsewhere in this file).

Do not change `ApplicationSpotsSprayer`, `applicationSpotCounts`, `countsToApplicationSpots`, the other exported components in this file (`LongevitySlider`, `ProjectionSlider`, `SeasonsCheckboxes`), or any file outside `src/components/PerfumeCharacteristics.js` — the add/edit pages and the `src/pages/api/reviews/perfumes/{add,update}.js` persistence routes consume this component as-is and need no changes for this fix.

## Done when

- `src/components/PerfumeCharacteristics.js`'s `SpraySpotCounter` card is a single clickable/tappable element: clicking anywhere within the card's bounds — including on the spot-name text (e.g. "Wrists") and the spray-count text (e.g. "no sprays"), not just the 🧴 emoji glyph — calls `spray()` and increments that spot's count. There is no more than one `<button>` element per `SpraySpotCounter` card (no nested buttons).
- Right-clicking anywhere on the card still calls `unspray()` and decrements the count when it's above 0, exactly as before.
- The card's visible footprint is smaller than before (e.g. reduced `fontSize` on the emoji, reduced `minWidth`/padding on the card) — a plausible before/after diff shows these values decreased, not just reformatted.
- `applicationSpotCounts`/`countsToApplicationSpots` and the existing tests in `src/components/PerfumeCharacteristics.test.js` are untouched and still pass: `npm test -- PerfumeCharacteristics`.
- `npm run lint` reports no new errors.
- `npm run build` succeeds.
- Manual check in a running dev server: run `npm run dev`, open the perfume review add page (`/reviews/perfumes/add`), and confirm (a) the Application Spots buttons visually render smaller than the previous 1.75rem-emoji / 6.5rem-wide cards, and (b) clicking on a spot's label text or count text (not just the emoji) increments that spot's spray count exactly like clicking the emoji does.
- No files outside `src/components/PerfumeCharacteristics.js` were modified.
