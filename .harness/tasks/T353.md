## Overview
The Bulk Backfill flow (src/pages/reviews/{movies,tv,books,albums}/backfill.js — all render the same shared src/components/BulkBackfillList.js component) currently caps the number of search candidates shown per row at 3, via a single shared constant: `candidates: error ? null : candidates.slice(0, 3),` inside the `onItemSettled` callback passed to `runBackfillQueue` (src/components/BulkBackfillList.js line 63). The owner has been running these backfills and found the correct match is sometimes not among the first 3 candidates. The cap is applied at STORE time (in onItemSettled), not render time, so the existing status-label code at ~line 125 (`${row.candidates.length} candidate(s) found`) automatically reflects the new count. The owner confirmed: a plain constant bump is sufficient (no scrollable/collapsible candidate list needed), and the visual-harness fixture should be expanded to 10+ candidates so the harness actually exercises the new cap.

## Do
In src/components/BulkBackfillList.js, change line 63 from `candidates: error ? null : candidates.slice(0, 3),` to `candidates: error ? null : candidates.slice(0, 10),`. This single change applies uniformly to all 4 review types since they all render this shared component — do not duplicate the cap per-page, and leave the `.bbl-candidates` rendering block (~lines 161-178, a plain vertical radio-list) as a plain list with no scroll/collapse wrapper — the owner confirmed a plain constant change is sufficient. Additionally, in scripts/_visual-harness.mjs: (1) update the comment above `tmdbSearchResults` (currently 'TMDB search results for the backfill flow — more than 3 so the T338 cap-at-3 is visible') to reflect the new cap of 10, and expand that fixture (currently only 5 synthetic 'Blade Runner' candidates) to 10+ entries so the harness actually exercises the new cap; (2) update the FLOWS entry named 'reviews-movies-backfill-apply-all' (~lines 332-337), whose description ends '...capped at 3 candidates each', to say 10.

## Done when
src/components/BulkBackfillList.js line 63 uses `.slice(0, 10)` instead of `.slice(0, 3)`, verified to affect all 4 backfill pages identically. scripts/_visual-harness.mjs's comment, fixture (now 10+ candidates), and the 'reviews-movies-backfill-apply-all' flow description no longer reference a cap of 3. `node scripts/visual-check.mjs` runs successfully and the relevant backfill screenshots are visually reviewed to confirm rows with 10 candidates remain usable. npm run lint / npm test / npm run build all pass.
