## Do

Follow-up to T211 ("Add title/description search and multi-select tag filtering to the Projects page"): the tag-filter pill row it added derives `allTags` from ALL distinct GitHub topics across every fetched repo with no cap (`src/pages/projects/index.js:76-79`, currently `[...new Set(repos.flatMap((repo) => repo.topics || []))].sort()`), rendered as one flat `Pill` per tag inside a `.collection-pill-group` div (lines ~123-135). In practice, across the owner's real repo list this produces roughly 44 distinct pills spanning about 5 full rows above the project cards — the owner calls this an undifferentiated, 'ugly' flat list, even though any individual repo's own topic list is short (it's the total DISTINCT set across MANY repos that balloons). T211's own agent flagged this exact risk in its report and chose 'no cap' as a judgment call that didn't hold up visually.

Change the tag row to rank tags by frequency and cap what's shown by default, with a way to reveal the rest. This is a self-contained state/render change confined to `src/pages/projects/index.js` (component: `ProjectsPage`, ~line 49 onward) — do not add a new API route, change `/api/github/repos`, or modify `src/components/Pill.js` / `src/components/PillGroup.js`.

1. **Rank by frequency, not alphabetically.** Replace the `allTags` derivation (lines 76-79) so it counts, for each distinct topic string, how many repos in `repos` carry it (`repo.topics || []`), then produces the array of distinct topics sorted by that count DESCENDING (the topic tagged on the most repos comes first), tie-breaking equal counts alphabetically for a stable, predictable order. Keep this in a `useMemo` keyed on `repos`, same as the current `allTags`. A convenient shape: build a `Map<topic, count>` first (one pass over `repos.flatMap(...)`), then derive the sorted `allTags` array from that map's keys.

2. **Cap the default view at 12 pills.** Add a constant `const TAG_DISPLAY_LIMIT = 12;` near the top of the file. When `allTags.length > TAG_DISPLAY_LIMIT`, the row shows only the top 12 by frequency by default (everything beyond that is hidden until expanded).

3. **'Show more' / 'show less' toggle, reusing the existing `Pill` component.** Add `const [tagsExpanded, setTagsExpanded] = useState(false);`. When there are more tags than the limit: while collapsed, append ONE extra `Pill` at the end of the row (import already exists) with no `active`/`accentColor` props (so it renders as a plain outlined pill, visually distinct from the selectable tag pills) whose label is `` `+${allTags.length - TAG_DISPLAY_LIMIT} more` `` and whose `onClick` sets `tagsExpanded` to `true`. While expanded, render every tag (not just the top 12) and change that same toggle pill's label to `show less`, with `onClick` setting `tagsExpanded` back to `false`. If `allTags.length <= TAG_DISPLAY_LIMIT`, never render the toggle pill — there's nothing to collapse and today's small-list behaviour must be unchanged.

4. **Don't strand an active filter behind the fold.** `selectedTags` persists independently of what's currently visible, so if the owner has a tag selected that is NOT among the top 12 by frequency, it must still be visible (and still show as `active`) even while collapsed — otherwise a real selection silently becomes invisible and undeselectable. Compute the collapsed view's visible tag list as the top 12 by frequency UNIONED with any tag(s) currently in `selectedTags` that fall outside that top 12 (dedupe, keep the frequency ordering for the top 12 then append any such extra selected tags after). When `tagsExpanded` is true this is moot since every tag renders anyway.

5. **Filtering behaviour must not change.** `toggleTag`, `selectedTags` state, and the OR-across-selected-tags filter logic added in T211 (`filteredRepos`'s `matchesTags`) keep working exactly as before — this task changes ONLY what's displayed in the pill row (which tags render, in what order, behind what toggle), never which repos match once a tag is toggled, including a tag currently hidden behind 'show more' before being revealed/selected.

6. **Docs.** Update `CLAUDE.md`'s file map entry for `src/pages/projects/index.js` (currently ends '...with client-side search (name + description) and multi-select OR tag filtering over the already-fetched repo list') to mention that tags are now frequency-ranked and capped with a show-more/show-less toggle, in the same commit — per root `CLAUDE.md`'s Definition of Done.

## Done when

- `npm run lint` and `npm run build` both pass with no new errors.
- `verify: ["run-app"]`: start the dev server (`npm run dev`), open `/projects` in a browser, and confirm:
  - Given the real repo list has more than 12 distinct topics (it does today — roughly 44), the tag row shows only 12 tag pills by default PLUS one extra toggle pill reading `+N more` (N = the remaining count) at the end of the row — not the full flat list of ~44.
  - The 12 pills shown by default are the highest-frequency ones (spot check: pick a topic you can see repeated across several project cards' topic `Badge`s vs. one that appears on only a single card — the more common one should be visible by default in the filter row; the rarer one should not be, unless it happens to tie into the top 12).
  - Clicking the `+N more` pill reveals every remaining tag pill in the row and changes that pill's own label to `show less`.
  - Clicking `show less` collapses the row back down to the top 12 plus the `+N more` toggle.
  - While collapsed, clicking one of the visible top-12 tag pills still filters the project grid to matching repos exactly as before (and selecting a second tag while collapsed still widens the result set via OR semantics, matching T211's existing behaviour).
  - Expand the row, select a tag that is NOT among the default top 12 (a lower-frequency one), then click `show less` — confirm that selected tag's pill remains visible in the now-collapsed row (still shown as active, still filtering the grid), rather than disappearing or having its filter silently cleared.
  - Deselecting that tag (click it again while it's visible) removes it from the filter and it's then free to disappear from the collapsed view on the next re-render (no longer forced-visible since it's no longer selected).
  - If the fetched repo list ever has 12 or fewer distinct topics total, no toggle pill appears at all and all tags render directly, unchanged from today's behaviour for a small tag set.
- Confirm `CLAUDE.md`'s file map entry for `src/pages/projects/index.js` was updated to mention the tags are frequency-ranked and capped with a show-more/show-less toggle.
