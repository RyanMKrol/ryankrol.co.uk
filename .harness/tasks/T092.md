# T092 — Delete all three UI rewrite variants (v1/v2/v3), promote none

## Do

The three full-site UI rewrite variants (`/v1`, `/v2`, `/v3`, built by T042-T063) were reviewed by
the owner, who judged all three "pretty bad" — none are being kept. See the `## Decision` section
appended to `.harness/designs/T042-ui-rewrite-briefs.md` for the record. Unlike the original
plan (T064/T065, now closed as superseded — see that design doc), NO variant is being promoted to
replace the production pages. The production site (`src/pages/reviews/**`, `src/pages/vinyl/**`,
`src/pages/workouts/**`, `src/pages/exercises/**`, `src/pages/listening/**`,
`src/pages/projects/**`, `src/pages/index.js`, and all other existing production pages/components)
must be left completely untouched — this task only deletes the experimental variant trees.

Delete entirely:
- `src/pages/v1/**` and `src/components/v1/**`
- `src/pages/v2/**` and `src/components/v2/**`
- `src/pages/v3/**` and `src/components/v3/**`
- `.harness/designs/T042-ui-rewrite-briefs.md` (the design brief doc for these variants — now
  historical, moot once the variants themselves are gone; the decision it recorded is already
  preserved in this task's own git history and this task's own worklog, so deleting the doc loses
  no information that matters going forward)

Do NOT delete or modify anything under `src/pages/api/**` (the variants reused unchanged
production APIs) or any production page/component outside the `v1`/`v2`/`v3` trees.

## Done when

- `src/pages/v1/`, `src/pages/v2/`, `src/pages/v3/`, `src/components/v1/`, `src/components/v2/`,
  `src/components/v3/`, and `.harness/designs/T042-ui-rewrite-briefs.md` no longer exist anywhere
  in the repo.
- `npm run build` succeeds with no references to `/v1`, `/v2`, or `/v3` remaining anywhere in the
  codebase (`grep -rE "/v[123]" src/` returns nothing, aside from incidental unrelated matches you
  should sanity-check by hand — e.g. a version string or similar false positive).
- Loading the site's normal routes (`/`, `/reviews/movies`, `/vinyl`, `/workouts`, `/listening`,
  `/projects`) in a running dev server shows the SAME production UI as before this task — nothing
  about the existing site changed, only the experimental variant trees were removed.
- `npm test` still passes (no test suite referenced the deleted variant files).
