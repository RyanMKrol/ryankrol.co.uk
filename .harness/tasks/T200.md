## Do

Add a favicon to the site matching the existing nav wordmark mark, and wire it into the pages-router `<Head>` (this repo has no `app/` directory, so there is no automatic favicon file-convention detection — it must be an explicit `<link>` tag).

1. **Create the `public/` directory** (it does not exist yet in this repo) with a single new file, `public/favicon.svg`, containing exactly:

```svg
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <circle cx="16" cy="16" r="16" fill="#FF5C39"/>
  <text x="16" y="23" text-anchor="middle" font-family="Arial, Helvetica, sans-serif" font-weight="700" font-size="20" fill="#FFFFFF">r</text>
</svg>
```

This mirrors `.wordmark-avatar` (`src/styles/globals.css:828-838`, rendered in `src/components/Header.js:35`): a solid circle in `--accent-movies` (`#FF5C39`, `src/styles/globals.css:26`), a white lowercase `r`, bold weight. The `text` element's `x`/`y`/`font-size` values above are chosen to visually center a lowercase "r" in the 32×32 circle — if you render the SVG and the glyph looks obviously off-center (noticeably left/right or high/low of the circle's middle), you may nudge `x`/`y`/`font-size` slightly to correct it, but do not change the circle's `fill`, the text's `fill`, `font-weight`, or the overall 32×32/`r=16` proportions.

2. **Wire it into `src/pages/_document.js`'s `<Head>`.** Add one line inside the existing `<Head>` block (anywhere inside `<Head>...</Head>` is fine; placing it as the first child, before the `preconnect` links, is the suggested spot since it keeps the head's own metadata grouped ahead of the font-loading block):

```jsx
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
```

Do not touch anything else in `_document.js` (the `prePaintScript`, the font `<link>` tags, or the `<body>` block) — this is a single additive line inside `<Head>`.

3. **Do NOT attempt an `.ico`/`.png` fallback.** SVG favicons are supported by all current evergreen browsers, and rasterizing a `.ico`/`.png` needs image-generation tooling this task doesn't have available — SVG-only is the deliberate, complete scope of this task, not a partial stretch goal left undone.

4. **Update docs in the same change** (per this repo's `CLAUDE.md` "Documentation is part of every change" rule): add a `public/favicon.svg` row to `CLAUDE.md`'s file map table (e.g. `| \`public/favicon.svg\` | Site favicon — SVG circle-and-"r" mark matching the nav wordmark's \`.wordmark-avatar\` |`), and add one line to the `_document.js` file-map row (or the surrounding prose) noting it now references the favicon. If `README.md` describes the site's pages/assets at a level of detail where a favicon would be expected to appear, add a one-line mention there too — otherwise it's fine to leave `README.md` untouched (use judgement: don't force an unnatural addition just to touch the file).

## Done when

- `public/favicon.svg` exists and contains a `<circle>` filled `#FF5C39` and a white (`#FFFFFF`) lowercase `r` `<text>` element, `font-weight="700"`, at a 32×32 (or proportionally equivalent) viewBox.
- `src/pages/_document.js`'s `<Head>` contains exactly one new `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />` line; the `prePaintScript`, the Google Fonts `<link>` tags, and the `<body>` block are all otherwise unchanged.
- `npm run lint` reports no new errors.
- `npm run build` succeeds.
- `CLAUDE.md`'s file map includes a row for `public/favicon.svg` (and the `_document.js` row/prose notes the favicon reference).
- Manual verification (UI-facing; this repo's dashboard has no screenshot-diffing tooling): run `npm run dev`, load any page (e.g. `http://localhost:3000/`) in a real browser, and confirm the browser tab shows the red-circle "r" favicon (not a blank/generic icon) — a hard refresh or a new tab may be needed since browsers cache favicons aggressively.
