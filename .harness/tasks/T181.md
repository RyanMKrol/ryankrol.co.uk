## Do

Create a new standalone presentational component at `src/components/perfumeVariants/Variant2Editorial.js`, default-exported, accepting props `{ item }` (same raw `PerfumeRatings` record shape as variant 1 — see README.md's 'PerfumeRatings Table' section and src/pages/api/reviews/perfumes/add.js). This variant's direction is EDITORIAL / magazine-style, optimised for reading rather than scanning stats: large prominent title typography, designer rendered as a byline ('by {designer}'), the `type` shown as a small understated label (not a loud badge), and `item.description` given the most visual weight — render it as a pull-quote-styled paragraph (larger type, a decorative quote mark or left border accent) since it's the one field none of the other variants should foreground this heavily. Show the rating minimally (small numeric '{rating}/10', no big meter). Surface `seasons` (if present) as a short italic line like 'Best worn: Summer, Night' rather than chips. Do NOT surface `longevity`, `projection`, or `applicationSpots` in this variant at all — the whole point of this direction is restraint/editorial focus, contrasting with variant 1's density; those fields belong to the denser variants. Render `item.fragranticaUrl` as a subtle text link styled like a magazine 'continue reading →' call to action. Add new, uniquely-prefixed CSS classes (e.g. `.perfume-v2-*`) to `src/styles/globals.css` — ONLY ADD new rules, never edit/remove existing ones. Standalone component, no dependency on ReviewCard.js or a specific parent container.

## Done when

`src/components/perfumeVariants/Variant2Editorial.js` exists and renders title/designer/type/description/rating/seasons/fragrantica-link per the editorial direction above, deliberately omitting longevity/projection/applicationSpots. `npm run dev` and confirm in a running dev server (via the paired preview-page task, or a temporary scratch mount) that it renders real perfume records with no console errors and visibly reads as a different, more text-forward layout than variant 1. `npm run build` succeeds and no pre-existing rule in `src/styles/globals.css` was modified or removed.
