## Do

No screen in `/Users/ryankrol/Downloads/design_handoff/` explicitly mocks the review add/edit forms
— only list pages are mocked. Infer the visual treatment from the movies list page rebuilt in T150
(open `/Users/ryankrol/Downloads/design_handoff/renders/2a.png` again for the general card/type/
color language: white bordered rounded cards, `Bricolage Grotesque` headings, `Nunito` body,
`Space Mono` for small labels, ink borders, pill-shaped controls) and apply that same visual
language to the forms — this is a restyle, not a new design.

Restyle the movies and TV add/edit forms to Collection tokens and the shared component library
(`Pill`, `SearchInput`, `Badge`, `StarRating` from T143/T144, plus general card/typography tokens
from T142):
- `src/pages/reviews/movies/add.js`, `src/pages/reviews/movies/edit.js`,
  `src/pages/reviews/movies/edit/[id].js`
- `src/pages/reviews/tv/add.js`, `src/pages/reviews/tv/edit.js`, `src/pages/reviews/tv/edit/[id].js`

Keep ALL existing functionality completely unchanged — the TMDB search-and-confirm flow
(`TmdbSearch.js`), the `MetadataBackfillModal` backfill flow, password gating, form validation and
submission. This task is purely presentational: swap hardcoded/legacy CSS classes for the Collection
tokens and shared components (e.g. the interactive `StarRating` for rating input — NOT the
`readOnly` mode built for list cards — search inputs, submit buttons, etc.), without touching any
`onSubmit`/`onSearch`/API-calling logic. If a component like `TmdbSearch.js` needs visual updates to
fit the new look, restyle it in place (it's shared with the `tv` add flow already) rather than
forking it.

## Done when

- All 6 files use Collection design tokens/components (no legacy hardcoded colors), and every
  existing behavior (TMDB search-and-confirm required to add, edit save/delete, password gate,
  backfill modal) still works exactly as before.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, go through the full add flow for a movie (search TMDB,
  confirm a match, fill in rating/review, submit) and confirm it still works end-to-end with the new
  styling; repeat for editing an existing movie review (including triggering the backfill modal).
  Confirm the TV add/edit flows still work identically. Detailed pixel-fidelity tuning is deferred to
  the Phase 6 visual-audit task T169.
