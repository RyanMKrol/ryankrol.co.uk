## Do

In `src/styles/globals.css`, the `.collection-nav-pills` rule (around line 922) currently reads:
```css
.collection-nav-pills {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  flex-wrap: wrap;
  justify-content: center;
}
```
This wraps the 9 pills onto multiple rows at any viewport width between the mobile `☰`-menu breakpoint (`@media (max-width: 768px)` around line 2274, which sets `.collection-nav-pills { display: none; }` and shows `.nav-menu-toggle`/`.nav-mobile-menu` instead) and the natural width where all 9 pills fit on one row (roughly ~900-1000px+, varies by theme font). Fix: replace the vertical wrap with a horizontally-scrollable strip. Change `.collection-nav-pills` to `flex-wrap: nowrap`, add `overflow-x: auto`, keep `overflow-y: hidden`, and change `justify-content` from `center` to `flex-start` (centering a scrollable overflowing row fights the scroll — flex-start reads naturally as 'starts here, more to the right'). Add `scrollbar-width: none;` (Firefox) and a `.collection-nav-pills::-webkit-scrollbar { display: none; }` rule to hide the raw scrollbar, since the fade affordance is the intended cue instead. Give each `Link`/`Pill` child `flex-shrink: 0` (add `.collection-nav-pills > a { flex-shrink: 0; }` or similar, matching how `Link` wraps `Pill` in Header.js) so pills don't compress. For the 'visually obvious affordance that there's more to scroll to', add a CSS mask-image fade on the trailing (right) edge of `.collection-nav-pills`: `mask-image: linear-gradient(to right, black calc(100% - 2rem), transparent 100%); -webkit-mask-image: linear-gradient(to right, black calc(100% - 2rem), transparent 100%);` — this fades the last ~2rem of pill content toward transparent whenever the row overflows, giving a visible 'more content' cue without JS scroll-position tracking (simple, matches the 'reasonable, simple approach' bar; a JS-driven conditional fade that only shows when actually scrollable/not-at-end is a nice-to-have but not required). Do not touch `src/components/Pill.js` or `src/components/PillGroup.js` — confirm while implementing that Header.js only imports `Pill` (not `PillGroup`; `PillGroup` is a separate primitive used for sort/filter pills elsewhere and is unrelated to this nav). Do not change the `@media (max-width: 768px)` mobile block — the `☰` menu / `.nav-mobile-menu` behavior must stay untouched.

## Done when

`npm run dev`, open the site, and resize the browser to several widths: (1) full desktop width (~1100px+) — the 9 pills fit on one row, no visible scrollbar or fade, layout matches current behavior; (2) a width where 9 pills don't fit but the viewport is still above 768px (e.g. ~800-850px, a 'narrowed window / split-screen' width matching the reported screenshot) — confirm the pill row no longer wraps to multiple lines, instead stays on one row and scrolls horizontally (drag/trackpad-scroll it and confirm it moves), the wordmark and now-playing chip stay aligned on one line (`.header-content` no longer pushed tall by wrapped pills), and a fade/gradient is visible at the trailing edge of the pill row indicating more content is scrollable; (3) below 768px — confirm the `☰` toggle and mobile menu still work exactly as before (unaffected by this change). Run `npm run lint` and `npm run build` and confirm both stay green.
