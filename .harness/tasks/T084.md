# T084 — Fix invisible rating stars on the Ocean theme (dark mode)

## Do

`src/components/StarRating.js` renders `★` glyphs colored via `.star-button` (color:
`var(--color-star-empty)`) and `.star-button.filled`/`:hover` (color: `var(--color-star-filled)`),
defined in `src/styles/globals.css:1413-1429`. The Ocean theme's DARK-mode block
(`html[data-theme="ocean"]`, `globals.css:806` onward) defines `--color-star-empty: #00223a`
(`globals.css:841`) — nearly identical to that same block's own `--color-bg: #001828` and
`--color-surface: #002238` (`globals.css:813-814`), making unfilled stars essentially invisible
against the page background until hover swaps them to `--color-star-filled: #00c8e8`
(`globals.css:840`).

Change `--color-star-empty` inside the Ocean dark-mode block (`globals.css:841` only — do NOT
touch the light-mode block at `globals.css:907` onward, which already has adequate contrast:
`--color-star-empty: #b8d8ee` against `--color-bg: #eef8ff`) to a value with clear, visible
contrast against that block's own `--color-bg`/`--color-surface` (`#001828`/`#002238`) — follow
the same pattern every other theme in this file already uses for its own `--color-star-empty`
(see the other 9 theme blocks' star-empty values at `globals.css` lines 45, 166, 261, 354, 453,
548, 647, 742, 1090 for reference points): a muted/mid-tone color clearly distinguishable from the
dark background, but visually subordinate to the bright `--color-star-filled: #00c8e8` once
filled/hovered.

## Done when

- `globals.css`'s Ocean dark-mode block's `--color-star-empty` value has been changed to a color
  with clear visual contrast against `--color-bg: #001828` and `--color-surface: #002238` (not a
  near-identical dark navy anymore).
- Manually verify in a running dev server (`npm run dev`): switch to the Ocean theme (dark mode,
  the default), load any review add/edit page with a `StarRating` (e.g. `/reviews/movies/add`),
  and confirm all 5 unfilled star glyphs are visibly distinguishable from the page background
  BEFORE hovering — not just on hover.
- The Ocean light-mode variant (`globals.css:907` onward) is untouched — its star colors were
  already fine.
- No other theme's `--color-star-empty`/`--color-star-filled` values were modified.
