## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Home" screen section in full and open
`/Users/ryankrol/Downloads/design_handoff/renders/1a.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4a.png` (mobile).

**Do NOT open or take any inspiration from `renders/1b.png`.** That file is a rejected alternate
homepage concept (a dark, neon, "terminal" visual style) — the design README explicitly labels it
"not chosen, kept for reference only." The chosen homepage design, and the ONLY one this task should
implement, is the warm/paper "Collection" look shown in `1a.png`/`4a.png` and described in the rest
of this spec. If you browse the `renders/` folder, ignore `1b.png` entirely.

Rebuild `src/pages/index.js` using the shared components landed in T141-T148 (`Header`/nav via
`_app.js` already, `Footer` via `_app.js` already, `StatBlock`, `CoverTile`):

- **Hero**: a small accent pill/badge ("A HOME FOR MY TASTE" per the render), oversized "Howdy."
  display heading, tagline paragraph (reuse the existing copy: "A running record of everything I
  watch, read, hear, spin and lift. Content consumption & gym attendance, kept honestly." or close
  to it), right-aligned mono meta text ("logging since 2019 · N things rated · updated today" —
  derive "N things rated" from real counts if easy, otherwise a reasonable static/derived value is
  fine, this is flavor text not a hard data requirement).
- **6 `StatBlock`s** in a row: movies/tv/books/albums/vinyl/workouts counts, each accent-filled with
  that section's color. Fetch real counts from the existing list APIs (`/api/reviews/movies`,
  `/api/reviews/tv`, `/api/reviews/books`, `/api/reviews/albums`, `/api/vinyl`, and workout count via
  `/api/workouts/stats` or `/api/workouts` — check `src/lib/workoutQueries.js`/the stats route for
  the cheapest existing way to get a total count without a heavy new endpoint). Prefer reusing
  existing cached API routes over adding a new aggregate endpoint; only add a new route if genuinely
  necessary and if so, follow this repo's existing `withApiCache` convention (see CLAUDE.md
  Architecture section).
- **"The collection wall"**: a 9-column grid of `CoverTile`s (per `renders/1a.png` — 2 rows of 9,
  mixing movie/album/vinyl covers). Pull real cover data from a handful of the most-recent-or-highest-rated
  items across content types (movies/albums/vinyl are shown in the reference render); this can be a
  simple client-side merge of a few existing API responses, no new endpoint required. Use
  `imageUrl` when a real cover exists (posterPath/coverUrl per each type's schema), otherwise
  `CoverTile`'s gradient placeholder handles the rest automatically.
- **"Latest takes"**: 3 most recent review cards (title, star rating, review snippet) — reuse
  whichever review data is already being fetched for the wall, or fetch the movies/books/albums/tv
  latest few, sorted by date descending.
- **Gym mini-dashboard rail**: session count + best-1RM stat pair + a small bar sparkline (reuse
  chart theming conventions from `useChartTheme` if a chart library is warranted, or a lightweight
  CSS bar-sparkline if that's simpler for ~10 bars) — source from `/api/workouts/stats`.
- **Vinyl shelf list**: a short list of a few vinyl records (title + artist), reusing `/api/vinyl`.

**Mobile** (`renders/4a.png`): stat blocks collapse to a 3-col grid, the collection wall is replaced
by 3 quick-link rows (`~/vinyl`, `~/listening`, `~/projects` — arrow-suffixed pill rows linking to
those sections), nav is the ☰ menu (already handled by `Header`/T146).

## Done when

- The home page renders the hero, 6 stat blocks with real counts, the collection wall grid with real
  cover data (falling back to `CoverTile`'s placeholder gradient where no cover exists), latest-takes
  cards, the gym rail, and the vinyl shelf list — all bound to real API data, not hardcoded mock
  values (aside from the flavor-text meta line, which may be static).
- At a mobile viewport (~390px), stats collapse to 3 columns and the wall is replaced by the 3
  quick-link rows.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/`, confirm real data appears in every section
  (not loading spinners stuck forever, not empty states unless the underlying data is genuinely
  empty), and confirm the mobile collapse behavior at a narrow viewport. Detailed pixel-fidelity
  tuning (exact spacing, the precise wall grid density, exact sparkline styling) is deferred to the
  Phase 6 visual-audit task T165 — don't block completion on exact matching to `renders/1a.png`,
  just get the real structure and real data in place.
