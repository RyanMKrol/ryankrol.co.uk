## Do

Replace the local numeric `formatDate` helper in `src/pages/workouts/index.js` (currently lines 36-40: `new Date(dateString).toLocaleDateString('en-GB', { day: '2-digit', month: '2-digit', year: 'numeric' }).replace(/\//g, '-')`) with the shared English-language formatter from the new `src/lib/dateFormat.js` helper (added by a prior task in this cluster). Import `{ formatEnglishDate }` (NOT `formatReviewDate` — workouts' `start_time` is an ISO-8601 datetime string already parseable directly by `new Date(...)`, not a `DD-MM-YYYY` string, so the review-specific parsing in `formatReviewDate` would be the wrong tool here). Change the local `formatDate(dateString)` function body to delegate to `formatEnglishDate` (e.g. `return formatEnglishDate(dateString);` relying on `formatEnglishDate`'s own `new Date(...)` coercion, or `return formatEnglishDate(new Date(dateString));` if you prefer to construct the Date explicitly first — either is fine given `formatEnglishDate`'s accepted-input contract). Keep the local wrapper named `formatDate` (minimal diff, same call sites). Do not touch `formatTime` or `getDuration`, which are unrelated helpers in the same file.

## Done when

The workouts list page (`/workouts`) renders each session's date as a written English date (e.g. '17 May 2026') instead of numeric DD-MM-YYYY. `formatDate` in `src/pages/workouts/index.js` now delegates to `formatEnglishDate` from `src/lib/dateFormat.js` instead of doing its own `toLocaleDateString('en-GB', {day:'2-digit',...}).replace(...)` construction. `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify: `npm run dev`, open `/workouts`, and confirm each session card's date line now reads as a written date rather than numeric DD-MM-YYYY. Record the observation in the worklog.
