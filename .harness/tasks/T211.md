## Do

Add client-side search + tag filtering to the Projects page (`src/pages/projects/index.js`), reusing the established pattern from the review list pages (e.g. `src/pages/reviews/movies/index.js`) rather than inventing a new one. This page currently has NO search/filter state — only `repos`/`loading`/`error` (lines 48-50) fetched once from `/api/github/repos` in a `useEffect` (lines 52-70) and rendered straight through. All of the below is purely client-side filtering over the already-fetched `repos` array already in React state — do NOT add a new API route or change `/api/github/repos`.

**1. Search (title + description, substring match):**
- Add `const [searchTerm, setSearchTerm] = useState('')`.
- Import and render `SearchInput` (`src/components/SearchInput.js`, the same controlled `value`/`onChange`/`placeholder` component used at `src/pages/reviews/movies/index.js:125-129`) inside a `.collection-review-controls` wrapper (reuse this exact class — the page's header already uses `.collection-review-header`/`.collection-review-title-group`/`.collection-review-meta` at lines 81-88, `.collection-review-controls` is the sibling class the review pages nest their search+pill row inside, e.g. `src/pages/reviews/movies/index.js:124`).
- A repo matches the search term (case-insensitive substring, mirroring the movies page's `movie.title.toLowerCase().includes(searchTerm.toLowerCase())` at line 62) if EITHER `repo.name` OR `repo.description` contains it. Treat a null/undefined `repo.description` as an empty string so it doesn't throw. Placeholder text: something like `search by title or description...`.

**2. Tag filter (multi-select, OR semantics):**
- Read `src/components/PillGroup.js` first: it is SINGLE-select only (`active={value === option.value}`, one `value` prop) — it is the wrong primitive here and must NOT be reused as-is or modified (it's shared by every review page's sort pills; changing its API risks breaking them). Instead, use the lower-level `Pill` component (`src/components/Pill.js`) directly, rendering your own multi-select row.
- Derive the full set of distinct topics across ALL repos with a `useMemo` keyed on `repos`: `repos.flatMap(r => r.topics || [])`, deduplicated (e.g. via `[...new Set(...)]`), sorted alphabetically for a stable, predictable order. Critically, this must read each repo's FULL `topics` array, not the `.slice(0, 5)` currently used only for the per-card `Badge` display at lines 147-153 (leave that slice untouched — it's a separate, intentional per-card display cap, unrelated to the filter's tag universe).
- Add `const [selectedTags, setSelectedTags] = useState([])` (an array of selected topic strings). Render one `Pill` per distinct topic inside a `<div className="collection-pill-group">` (the same wrapper class `PillGroup` itself uses, so the visual styling — spacing, wrapping — matches with zero new CSS), each with `active={selectedTags.includes(topic)}`, `accentColor="var(--accent-projects)"` (matching the accent already used for topic `Badge`s on this page), and `onClick` that TOGGLES that topic in `selectedTags` (add if absent, remove if present) rather than replacing the whole selection — this is the multi-select behavior PillGroup's single-`value` model can't express.
- A repo passes the tag filter if `selectedTags.length === 0` (no filter applied — show everything) OR `repo.topics` contains AT LEAST ONE of the selected tags (OR/"any-of" semantics, not AND/"all-of") — chosen because it's the more useful default for exploring a small tag set (AND would very quickly produce empty results once more than one tag is selected, since a given repo's topic list is short) and is simpler to reason about/verify. If a repo has no `topics` array at all, treat it as `[]` (fails the tag filter whenever any tag is selected, always passes when none is).

**3. Combine + render:**
- Combine both conditions with AND: a repo is shown only if it passes the search test AND the tag filter test. Compute the filtered list with a `useMemo`/derived value (don't mutate `repos` itself), keyed on `repos`, `searchTerm`, and `selectedTags`.
- Render the existing `.projects-grid` of `.project-card`s (lines 108-159) unchanged in structure, just over the filtered list instead of the raw `repos` array.
- Distinguish the two empty cases: keep the existing "No active repositories found." message (lines 102-106) for when `repos` itself is empty (nothing fetched), but show a DIFFERENT message — e.g. "No projects match your search/filters." — when `repos.length > 0` but the filtered list is empty (search/tags excluded everything). Don't conflate the two.
- No pagination is needed or in scope here (the review list pages paginate; the Projects page does not today and this task shouldn't add it) — just render the full filtered list.

**4. Docs:** Per root `CLAUDE.md`'s Definition of Done, update the file map entry for `src/pages/projects/index.js` (currently "GitHub repo cards (stars/forks/last-push/topics)") to mention the new search/tag-filter capability, in the same commit.

Do not touch `src/components/PillGroup.js`, `src/components/SearchInput.js`, `src/components/Pill.js`, `src/pages/api/github/repos.js`, or any review page — this task is scoped entirely to `src/pages/projects/index.js` (plus the one doc line in `CLAUDE.md`).

## Done when

- `npm run lint` and `npm run build` both pass with no new errors.
- `verify: ["run-app"]`: start the dev server (`npm run dev`), open `/projects` in a browser, and confirm:
  - A search box is visible above the project grid. Typing a substring of some repo's NAME narrows the grid to matching repos only; typing a substring that only appears in some repo's DESCRIPTION (not its name) also narrows correctly to that repo — confirms the OR-across-fields substring match works, not title-only.
  - A row of tag pills is visible, covering the FULL set of distinct topics across all fetched repos (spot-check: pick a repo whose topics badges are NOT among the first 5 shown on its own card — i.e. a topic beyond the card's `.slice(0,5)` cutoff, if any repo has more than 5 topics — and confirm a pill for it still exists in the filter row).
  - Clicking one tag pill narrows the grid to only repos carrying that topic; clicking a SECOND tag pill (without deselecting the first) WIDENS the result set back out to repos carrying either topic (confirms OR/any-of semantics, not AND). Clicking an active pill again deselects it and its constraint is removed.
  - Typing a search term AND selecting a tag together narrows to repos satisfying both (confirms AND-combination between search and tag filter).
  - Clearing the search box and deselecting all tags restores the full original repo list.
  - Typing a search term (or tag combination) that matches nothing shows a distinct "no results for your search/filter" message, NOT the same message shown when there are zero repos at all, and NOT a blank/broken grid.
- Confirm the per-card topic `Badge`s still show at most 5 per card (the `.slice(0, 5)` display cap is unchanged) even though the filter pill row now reflects each repo's full topic list.
- Confirm the page still makes exactly one `GET /api/github/repos` request (check the browser Network tab) — no new API route, no additional network calls introduced by search/filtering.
- Confirm `CLAUDE.md`'s file map entry for `src/pages/projects/index.js` was updated to reflect the new search/filter capability.
