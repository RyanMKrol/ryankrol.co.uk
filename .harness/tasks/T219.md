## Do

In `src/components/ReviewCard.js`, add a review-date element to `SpineCoverCard` (books) and `SquareCoverCard` (albums) — both currently show no date at all, unlike the `poster-banner` variant (movies/tv) which already does. Import and use `formatReviewDate` from `src/lib/dateFormat.js` (added by a prior task in this cluster: `import { formatReviewDate } from '../lib/dateFormat';`) directly — do NOT render a raw `item.date`/`item.editedDate` value at any point, even transiently; this is being added fresh so it must use the written English format from the start.

- **Books (`SpineCoverCard`)**: extend the existing `metaParts` array (currently `[item.author, item.pageCount ? ... : null, item.firstPublishedYear || null]`) with one more entry computed as: `item.editedDate ? \`Updated ${formatReviewDate(item.editedDate)}\` : (item.date ? formatReviewDate(item.date) : null)` — mirroring this repo's existing `editedDate || date` precedence convention (edited date wins if present, else the original review date). Let the existing `.filter(Boolean)` + `' · '` join handle presentation, so it appears as part of the existing `spine-cover-meta` line — no new element needed for books.
- **Albums (`SquareCoverCard`)**: add a new element (its own line, not folded into the `highlights` text) rendered below the existing `square-cover-artist` paragraph, showing `formatReviewDate(item.date)` (albums, per `AlbumRatingsV3`'s schema, have no `editedDate` field — just use `item.date`), rendered only when `item.date` is truthy. Give it a class name consistent with this card's existing naming convention, e.g. `square-cover-date`.

If `square-cover-date` needs baseline styling to avoid looking broken/unstyled (spacing, secondary-text color), add a small CSS rule for it in `src/styles/globals.css` near the existing `.square-cover-artist` rule, matching that rule's font-size/color treatment for secondary card text — keep it minimal, no new design system tokens.

## Done when

Books cards (`/reviews/books` list page) show a date (the review date, or 'Updated <date>' if edited) formatted like '17 May 2026' as part of the existing meta line. Albums cards (`/reviews/albums` list page) show their review date formatted the same way, in its own element beneath the artist line. Neither ever rendered a raw/numeric date at any point in this task's diff. `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify: `npm run dev`, open `/reviews/books` and `/reviews/albums`, and confirm each card now shows a written English-language date somewhere on the card. Record the observation in the worklog.
