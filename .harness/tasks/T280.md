## Do

Naming note (read this first, it matters): the owner's plain-language request was 'remove the collection wall on mobile,' but the codebase's OWN section literally named "The collection wall" (`src/pages/index.js`, `<section className="home-wall">`, ~line 174-183, rendering `wallItems` as `CoverTile` image tiles) is ALREADY hidden on mobile via an existing rule in `src/styles/globals.css` (`.home-wall { display: none; }` inside the mobile media query block that starts around line 3184). Do NOT touch `.home-wall` or its JSX — leave that section and its mobile hide-rule exactly as they are.

What the owner actually means (confirmed by matching their screenshot description — plain pill-shaped text links with a trailing arrow, no images) is the SEPARATE `.home-quicklinks` section: `src/pages/index.js`, `<section className="home-quicklinks">` (~line 185-192), which maps over a `QUICK_LINKS` constant (~line 19-23: three entries, `~/vinyl` → `/vinyl`, `~/listening` → `/listening`, `~/projects` → `/projects`) rendering `.home-quicklink` links each followed by a `→` arrow span. This section is CURRENTLY MOBILE-ONLY already: its default/desktop CSS rule in `src/styles/globals.css` (`.home-quicklinks { display: none; ... }`, ~line 2920-2925) hides it everywhere by default, and the mobile media query block explicitly overrides it back on (`.home-quicklinks { display: flex; }`, ~line 3200-3202). So removing it satisfies 'remove it on mobile' completely — it is never shown on desktop today.

Do the following, fully removing this dead-weight section rather than just hiding it further:
1. In `src/pages/index.js`: delete the entire `<section className="home-quicklinks">...</section>` JSX block (the one mapping over `QUICK_LINKS`).
2. In `src/pages/index.js`: delete the now-unused `QUICK_LINKS` constant declaration, since nothing else in the file will reference it after step 1. Confirm via a project-wide search that `QUICK_LINKS` has no other usages before deleting it.
3. In `src/styles/globals.css`: delete the `.home-quicklinks { ... }` and `.home-quicklink { ... }` rule blocks (the default/desktop-hidden versions, ~line 2920-2938).
4. In `src/styles/globals.css`: delete the `.home-quicklinks { display: flex; }` override inside the mobile media query block (~line 3200-3202). Leave every sibling rule in that same media query block (including `.home-wall { display: none; }` immediately above it) untouched.
5. Do not touch the `.home-wall` section, its JSX, or its CSS anywhere — it's a different section and already correctly hidden on mobile; it is out of scope for this task entirely.

## Done when

`npm run dev`, then open the site at a mobile viewport width (browser dev tools responsive mode at ~375-414px wide, or a narrow physical browser window) on the homepage (`/`):
- The stat blocks (movies/tv/books/albums/vinyl/workouts counts) are still visible.
- Immediately below the stat blocks there is NO pill-shaped link row for `~/vinyl`, `~/listening`, `~/projects` anymore — the page goes straight from the stat blocks to the "Latest takes" section.
- "Latest takes" and everything below it still renders normally.
- Also check a desktop-width viewport (e.g. 1280px wide): the homepage still looks and behaves exactly as before this change (the quicklinks section was already invisible there, so there should be zero visible difference at desktop width).
- `npm run lint` and `npm run build` both succeed with no new errors (confirms no dangling reference to the deleted `QUICK_LINKS` constant or the deleted CSS classes).
