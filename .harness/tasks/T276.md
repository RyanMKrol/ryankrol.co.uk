## Do

Same swap as the 'add' forms task, but for the edit detail forms - replace the plain `<textarea>` with `src/components/MarkdownEditor.js` (built in task 2) in each of these 5 files, at the exact `<textarea ... className="collection-form-textarea" .../>` block:
- `src/pages/reviews/movies/edit/[id].js` line ~277-285 (field name `gist`)
- `src/pages/reviews/tv/edit/[id].js` line ~277-285 (field name `gist`)
- `src/pages/reviews/books/edit/[id].js` line ~284-292 (field name `overview`)
- `src/pages/reviews/albums/edit/[id].js` line ~307-315 (field name `highlights`)
- `src/pages/reviews/perfumes/edit/[id].js` line ~289-297 (field name `description`)

As with the add forms, each existing textarea already passes the exact prop shape `MarkdownEditor` expects (`id`/`name`/`value`/`onChange`/`className`/`placeholder`/`required`) so this should be a like-for-like swap with no change to state management, the `GetCommand`-then-`PutCommand` update flow, or the delete button. Scope is strictly the textarea-to-editor swap in these 5 files.

## Done when

All 5 `edit/[id].js` files import and render `MarkdownEditor` in place of the old `<textarea>`, same field names/props as before. `npm run lint`, `npm test`, `npm run build` pass. Manually verify with `npm run dev`: open an existing review's edit page for each of the 5 types (e.g. `/reviews/tv/edit/<id>`), confirm the editor loads pre-populated with the existing (possibly plain, pre-markdown) review text, edit it to add some markdown formatting via the toolbar, save, and confirm the update round-trips correctly (both the DynamoDB write via the update API and the re-rendered display on the list page).
