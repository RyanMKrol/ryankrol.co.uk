## Overview
The password-gated /reviews/albums/edit page currently renders every album card in one endless, horizontally-scrolling row instead of the 2-column masonry grid the public /reviews/albums page uses, because it applies the masonry CSS class directly to a plain `.map()` div instead of routing through the `MasonryColumns` component. Fix it to use `MasonryColumns` + `useResponsiveColumnCount`, matching the already-correct sibling pattern in `src/pages/reviews/books/edit.js`.

## Do
In src/pages/reviews/albums/edit.js, import `MasonryColumns` and `useResponsiveColumnCount`, add `const columnCount = useResponsiveColumnCount(2, 700);`, and replace the bare `<div className="square-cover-grid">{albums.map(...)}</div>` with `<MasonryColumns items={albums} columnCount={columnCount} className="square-cover-grid" columnClassName="square-cover-grid-col" renderItem={(album) => (...)} />`, moving the existing per-card JSX (Edit link + ReviewCard) into `renderItem` unchanged.

## Done when
/reviews/albums/edit renders albums into a 2-column masonry layout matching /reviews/albums (no endless single horizontal row, no horizontal scroll) — verify visually via `node scripts/visual-check.mjs` (or `VISUAL_CHECK_ONLY=albums`), looking specifically at the albums edit page screenshot(s) in scripts/visual-out/ per manifest.json's `covers`; each album's Edit link and card content render identically to before, just laid out in 2 columns instead of 1 row.
