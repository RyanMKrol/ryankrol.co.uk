## Do

Both /workouts (src/pages/workouts/index.js) and /workouts/[id] (src/pages/workouts/[id].js) currently render an unlabeled medal Badge for personal bests, with the descriptive PR-type text rendered but visually hidden via a screen-reader-only CSS clip pattern (position:absolute, 1x1px, clip:rect(0,0,0,0), negative margin) — see workouts/index.js:243-269 and workouts/[id].js:263-267 (the `prBadges` array) + 280-304 (the rendering). Owner has confirmed all open design questions:

1. AXIS GRANULARITY (list page must match detail page): the detail page's `prBadges` array (workouts/[id].js:263-267) already derives axis-specific labels ('weight PR' / '1RM PR' / 'volume PR') per set from `set.isWeightPR` / `set.is1RMPR` / `set.isVolumePR`. The list page's `getExercisePreview` (workouts/index.js:57-69) currently collapses this down to a single boolean `hasPersonalBest = sets.some(s => s.isWeightPR || s.is1RMPR || s.isVolumePR)` and renders one generic 'personal best' badge per exercise line. Owner wants the list page upgraded to be axis-specific too, naming weight/1RM/volume just like the detail page. IMPORTANT — this is confirmed to be a pure rendering change, NOT a new data-plumbing/API layer: `getAllWorkouts()` in src/lib/workoutQueries.js already scans the Workouts table with `exercises` included in its ProjectionExpression, and each returned exercise's `sets[]` already carries the same `isWeightPR`/`is1RMPR`/`isVolumePR` flags used by `getExercisePreview`'s existing boolean check — the axis data is already present in the object the list page already fetches, just discarded during the boolean collapse. No new DynamoDB read, API route change, or schema change is needed or wanted.

2. HOVER BEHAVIOR: owner confirmed making the label visible IS the ask — do NOT build a richer tooltip showing old-vs-new PR values. Add only a simple, purely-visual hover/focus affordance on the pill (e.g. a subtle background or border-color shift on `:hover`/`:focus-visible`) as a small interactive-feel touch on top of the now-always-visible label — it must not carry any additional information beyond what's already visibly printed on the pill.

3. VISUAL TREATMENT: owner confirmed medal + PR-type text must always be shown side by side in one pill — never hidden-until-hover/tap (that would reproduce the exact problem being fixed).

Implementation:
- In workouts/index.js, change `getExercisePreview` to return, per exercise, the deduplicated set of PR'd axes across that exercise's sets in the session (e.g. an array like `['weight', 'oneRepMax', 'volume']`, since different sets of the same exercise can each PR a different axis within one workout) instead of the single `hasPersonalBest` boolean. Update the rendering at lines 243-269 to render one visible medal+label pill per PR'd axis on that exercise's line, using the exact same wording as the detail page ('weight PR', '1RM PR', 'volume PR').
- In workouts/[id].js, keep the existing `prBadges` array (already axis-specific) but change the Badge rendering (~280-304) so the label text is a normal visible span next to the medal instead of the screen-reader-only clip-rect span — delete the now-unnecessary hidden span once the same text is visible.
- Keep both surfaces visually consistent (same pill shape/colours/spacing) for what is conceptually the same 'PR badge'. Since the exact same badge markup + CSS-clip pattern is currently duplicated verbatim in both files, factor the medal+label pill into one shared implementation (e.g. a small `PrBadge` presentational component in src/components/, or at minimum a shared CSS class in globals.css used by both pages) rather than fixing the duplication twice with two diverging implementations — your call on the exact factoring, but avoid drift between the two surfaces.
- Non-PR'd exercises/sets must be completely unaffected — no badge renders at all when there is no PR on that exercise line / set, exactly as today.
- Do not touch the Exercises/Workouts DynamoDB schema, the write path, or `storeExercises`/`detectPersonalBests` — this is a pure read-side rendering change using PR-flag data both pages already fetch.

## Done when

Run `npm run dev`. On /workouts, find a workout session containing an exercise that recorded a personal best — confirm that exercise's line shows an always-visible medal+label pill naming the SPECIFIC axis (or axes, if more than one PR'd on that exercise in that session) that PR'd — e.g. 'weight PR' and/or '1RM PR' and/or 'volume PR' — not a generic 'personal best', and confirm (via inspecting the rendered DOM) that no screen-reader-only clip-rect(0,0,0,0) hidden-label span remains for this badge. On /workouts/[id] for that same (or another) workout with a PR'd set, confirm each PR'd set's badge(s) likewise show the medal + visible axis-specific label side by side, still matching the axes actually flagged on that set (`isWeightPR`/`is1RMPR`/`isVolumePR`). Hover the badge on both pages (and, if it was made focusable, Tab to it) and confirm a visible hover/focus affordance appears (e.g. a background/border change) — confirm no separate tooltip with additional old-vs-new numeric data appears. Confirm an exercise (list page) or set (detail page) with NO personal best renders with no PR badge at all — no regression for the non-PR case. Confirm the pill's visual treatment (colours, spacing, shape) reads as the same component/style on both pages. Confirm `npm run lint`, `npm test`, and `npm run build` all pass, and update CLAUDE.md's file map / component list if a new shared `PrBadge`-style component was introduced.
