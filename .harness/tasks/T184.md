## Do

Create a new standalone presentational component at `src/components/perfumeVariants/Variant5TableRow.js`, default-exported, accepting props `{ item }` (same raw `PerfumeRatings` record shape — see README.md's 'PerfumeRatings Table' section and src/pages/api/reviews/perfumes/add.js). This variant's direction is the OPPOSITE end of the density spectrum from variant 2 (Editorial): a dense single-line/table-row layout meant for scanning many perfumes at once — title | designer | type (small badge) | rating (compact numeric, no meter) | longevity (raw '6/8' or '—' if absent) | projection (raw '3/4' or '—' if absent) | seasons (comma-joined short codes, or '—' if absent) laid out as a horizontal flex/grid row with clear column alignment (use CSS grid with fixed/fr column widths, not floats). `applicationSpots` and `description` should NOT be shown inline (there isn't room in a row) — instead render a small disclosure control (a `<details>`/`<summary>` or a toggle button) that expands to show them when the row is clicked, collapsed by default. Include the Fragrantica link inside that expandable disclosure too, not in the main row. Add new, uniquely-prefixed CSS classes (e.g. `.perfume-v5-*`) to `src/styles/globals.css` — ONLY ADD new rules, never edit/remove existing ones. Standalone component; expects to be rendered inside some list/table container (not a card grid) — do not assume a specific parent, but the row itself should look correct without one.

## Done when

`src/components/perfumeVariants/Variant5TableRow.js` exists and renders a single-line row with aligned columns for title/designer/type/rating/longevity/projection/seasons (using '—' for absent optional fields, not blank/undefined), plus a collapsed-by-default disclosure revealing applicationSpots/description/Fragrantica-link on demand. `npm run dev` and confirm in a running dev server (via the paired preview-page task, or a temporary scratch mount) that several real records render as aligned rows with no console errors, and that clicking/opening the disclosure reveals the extra fields correctly. `npm run build` succeeds and no pre-existing rule in `src/styles/globals.css` was modified or removed.
