## Do

In src/lib/programmeStats.js, extend aggregateProgramme(workouts, programme) (currently lines ~22-56) to support programme === 'all' as a new mode meaning 'combined totals across push/pull/legs together':

- When programme === 'all', compute `filtered` as `workouts.filter(w => classifyProgramme(w) !== null)` — i.e. keep any workout that classifies as push, pull, OR legs, excluding workouts that don't match any of the three (e.g. a workout titled 'Rest Day' or 'Cardio', which classifyProgramme returns null for). This is a deliberate choice: 'all' means 'all three programmes combined', not literally every workout in the table regardless of type.
- When programme is one of 'push'|'pull'|'legs', keep the existing exact-match filter (`classifyProgramme(w) === programme`) completely unchanged.
- The rest of the function — the date sort, the `sessions` map, `frequencyByMonth` bucketing, and the `totals` (workouts/totalVolume/avgVolumePerWorkout) computation — is already programme-agnostic and operates purely on `filtered`, so it should need no changes once `filtered` is computed correctly for the 'all' case.
- Do not rename the function, change its call signature, or change the shape of its return value ({ sessions, frequencyByMonth, totals }) — src/pages/programmes/index.js calls `aggregateProgramme(filteredWorkouts, selected)` and destructures `{ totals }` from the result, feeding the whole `data` object straight into `<ProgrammeOverviewCharts data={data} />`.
- classifyProgramme itself (lines 8-14) does not need to change.

## Done when

- `aggregateProgramme(workouts, 'all')` returns sessions/frequencyByMonth/totals computed across every workout classified as push, pull, or legs combined.
- For any given workouts array, `aggregateProgramme(workouts,'all').totals.workouts` equals the sum of `aggregateProgramme(workouts,'push').totals.workouts` + `'pull'` + `'legs'` on that same array (and likewise for totalVolume) — this is the core correctness property to test.
- Existing push/pull/legs behavior in aggregateProgramme and classifyProgramme is unchanged — all pre-existing tests in src/lib/programmeStats.test.js still pass.
- New tests are added to src/lib/programmeStats.test.js covering the 'all' mode, at minimum: (a) totals.workouts for 'all' equals the sum of the three individual programme counts on the existing WORKOUTS fixture; (b) a workout with an unclassifiable title (e.g. the existing 'Rest Day' fixture entry) is excluded from 'all' totals/sessions/frequencyByMonth; (c) sessions returned for 'all' are still sorted date-ascending; (d) frequencyByMonth for 'all' merges counts across programmes into the same month bucket where applicable.
- `npm test` passes.
- `npm run lint` reports no errors.
