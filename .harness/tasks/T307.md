## Do

Apply the shared header-strip pattern from perfumes-edit-redesign-2 to the books edit page, replacing T262's narrower spine-cover fix. In src/pages/reviews/books/edit.js's `renderItem` callback: change the wrapper div's className from `"review-card-with-edit review-card-with-edit--spine-cover"` to `"review-edit-card"`; move the Edit `<Link>` to be the FIRST child, wrapped in a new `<div className="review-edit-header">`, with the Link's className changed from `"edit-button-overlay"` to `"review-edit-button"`; the `<ReviewCard .../>` becomes the second child (after the header div), i.e. content now sits below the strip instead of ReviewCard-then-overlaid-button. In src/styles/globals.css, delete the now-dead T262 rules: the `.review-card-with-edit--spine-cover` block and the `.review-card-with-edit--spine-cover .edit-button-overlay` block, plus their explanatory comment ('/* Edit button fix for books/spine-cover: ... */'), located immediately after the base `.edit-button-overlay:hover` rule (and before the movies/poster-banner comment block) — do NOT touch the base `.review-card-with-edit` / `.edit-button-overlay` rules or any other type's fix blocks; those remain until every type is converted.

## Done when

`npm run dev`, visit `/reviews/books/edit`: every card now shows a top strip (background visibly a slightly different warm tone from the card body via `--color-surface-alt`, with a visible bottom border) containing ONLY the right-aligned Edit button — no more absolutely-positioned button overlapping the spine-cover heading/star-rating. Clicking Edit still navigates to `/reviews/books/edit/<id>`. Below the strip, the card's own content (title, star rating, meta, snippet, read-more) renders identically to how `/reviews/books` (view page) renders the same card, confirmed by opening both side by side. `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/books/edit.js` returns NOTHING (fully migrated off the old classes). `npm run lint`, `npm test`, `npm run build` all pass.
