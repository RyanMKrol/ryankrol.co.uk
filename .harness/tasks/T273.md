## Do

Wire the new `src/components/Markdown.js` component (see design notes for what to build) into every existing read-only review-text render site, replacing the current plain-text interpolation. There are 5 exact call sites across 2 files:

1. `src/components/ReviewCard.js` line ~166-167 (poster-banner variant, movies via src/pages/reviews/movies/index.js and tv via src/pages/reviews/tv/index.js, both styleVariant="poster-banner"): currently `<p className="poster-banner-snippet">{getThoughts()}</p>` rendering `item.review_text` - replace the plain text interpolation with `<Markdown>{getThoughts()}</Markdown>` in BLOCK mode (paragraphs/lists/bold/italic/links all wanted here - this is the main free-text review body).
2. `src/components/ReviewCard.js` line ~68-69 (spine-cover variant, books, styleVariant="spine-cover"): currently `<p className="spine-cover-snippet">{getThoughts()}</p>` - same treatment, BLOCK mode.
3. `src/components/ReviewCard.js` line ~201-205 (the default/no-styleVariant branch - not currently exercised by any index.js page since every review type sets an explicit styleVariant, but fix it too for consistency since it's the same underlying pattern and could be reused later): same treatment, BLOCK mode.
4. `src/components/ReviewCard.js` lines ~5-12 + ~33-39 (square-cover variant, albums, styleVariant="square-cover"): `getThoughts()` (item.highlights) is first run through `splitHighlights()`, which explicitly tokenizes on `\n`/`,`/`.`/`;` into up to 4 short fragments joined with ' · ' - this is a DIFFERENT, deliberate chip-style display, not a paragraph. Do NOT block-render markdown here (a `<ul>` or heading inside a 4-word chip joined by middots would look broken). Instead render each individual fragment through `<Markdown inline>` so inline marks (bold/italic/links) still work within a fragment, but list/heading syntax has no effect (falls through to plain inline text per the Markdown component's inline-mode config from task 1's design notes).
5. `src/components/perfumeVariants/Variant6Hybrid.js` line ~43-44: currently `<p className="perfume-v1-description">{item.description}</p>` - replace with `<Markdown>{item.description}</Markdown>` in BLOCK mode.

Add `react-markdown` + `remark-gfm` to `package.json` (`npm install react-markdown remark-gfm` - use whatever current major versions install cleanly against this repo's React 19 / Next 15; if npm's peer-dependency resolution complains, check react-markdown's changelog for the minimum version line that supports React 18/19 ESM environments, and prefer that over forcing an old major with `--legacy-peer-deps`). Add a co-located `src/components/Markdown.test.js` (React Testing Library is already a devDependency, per CLAUDE.md's 'Known gaps' note that RTL is installed but unused) asserting: `**bold**` renders inside a `<strong>`, `*italic*` renders inside an `<em>`, a `- list item` renders inside a `<ul><li>` in block mode but NOT in inline mode, and a `[link](url)` renders an `<a href="url">`.

## Done when

`package.json` has `react-markdown` and `remark-gfm` as new dependencies; `src/components/Markdown.js` exists and is used (not just imported) by all 5 call sites listed above, each in the mode specified (block vs inline); no `rehype-raw` or `dangerouslySetInnerHTML` was introduced. `npm run lint`, `npm test` (including the new `Markdown.test.js`), and `npm run build` all pass. Manually verify with `npm run dev`: edit an existing TV review (or movie/book/perfume) to include `**bold**`, `*italic*`, a `- ` list, and a `[link](https://example.com)` in its review text/description, save, then view the type's list page (`/reviews/tv`, `/reviews/movies`, `/reviews/books`, `/reviews/perfumes`) and confirm each renders as real bold/italic/list/link, not literal markdown syntax or a collapsed run-on line. Separately confirm `/reviews/albums` still shows highlight chips (fragments joined by ' · ') with inline bold/italic honoured but no block-level markdown breaking the chip layout.
