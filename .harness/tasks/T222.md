## Do

In each of the 10 files listed in scope (the 5 review edit-list pages and their 5 corresponding edit/[id].js detail pages), edit ONLY the `<h1 className="page-title">...</h1>` line: remove the leading emoji and the single space that follows it, then lowercase every letter in the remaining text. Do not touch any other text, markup, styling, or logic in these files — this is a pure copy change to one JSX text node per file, mirroring the already-shipped T194 fix for the /add pages' titles (same lowercase-'tv'-not-'TV' convention).

Confirmed current -> target text for all 10 files (re-verified directly against source 2026-07-04; if line numbers have drifted since, locate the h1 by its `className="page-title"` and current emoji-prefixed text, not by line number):

- src/pages/reviews/movies/edit.js:61 `🎬 Edit Movie Reviews` -> `edit movie reviews`
- src/pages/reviews/movies/edit/[id].js:203 `🎬 Edit Movie Review` -> `edit movie review`
- src/pages/reviews/tv/edit.js:61 `📺 Edit TV Show Reviews` -> `edit tv show reviews`
- src/pages/reviews/tv/edit/[id].js:203 `📺 Edit TV Show Review` -> `edit tv show review`
- src/pages/reviews/books/edit.js:61 `📚 Edit Book Reviews` -> `edit book reviews`
- src/pages/reviews/books/edit/[id].js:203 `📚 Edit Book Review` -> `edit book review`
- src/pages/reviews/albums/edit.js:61 `🎵 Edit Album Reviews` -> `edit album reviews`
- src/pages/reviews/albums/edit/[id].js:219 `🎵 Edit Album Review` -> `edit album review`
- src/pages/reviews/perfumes/edit.js:60 `👃 Edit Perfume Reviews` -> `edit perfume reviews`
- src/pages/reviews/perfumes/edit/[id].js:222 `👃 Edit Perfume Review` -> `edit perfume review`

This task may land before or after the sibling task migrating these same 5 edit.js files' card styleVariant — both touch disjoint lines (the `<h1>` title vs. the `<ReviewCard>`/`<Variant6Hybrid>` render further down), so there is no real conflict regardless of build order.

## Done when

`grep -rn "page-title" src/pages/reviews/movies/edit.js "src/pages/reviews/movies/edit/[id].js" src/pages/reviews/tv/edit.js "src/pages/reviews/tv/edit/[id].js" src/pages/reviews/books/edit.js "src/pages/reviews/books/edit/[id].js" src/pages/reviews/albums/edit.js "src/pages/reviews/albums/edit/[id].js" src/pages/reviews/perfumes/edit.js "src/pages/reviews/perfumes/edit/[id].js"` shows all 10 titles with no emoji characters and fully lowercase text, matching exactly: `edit movie reviews`, `edit movie review`, `edit tv show reviews`, `edit tv show review`, `edit book reviews`, `edit book review`, `edit album reviews`, `edit album review`, `edit perfume reviews`, `edit perfume review`. `npm run dev`, open at least 2 of the 5 list pages and 2 of the 5 detail pages (e.g. `/reviews/movies/edit` and a perfume detail edit page) and visually confirm each page title renders lowercase with no emoji. `npm run lint`, `npm test`, and `npm run build` all pass. The diff touches ONLY the 10 files in scope, each with exactly one changed line (the h1 text), and nothing else (no button labels, loading/error text, or form field labels changed).
