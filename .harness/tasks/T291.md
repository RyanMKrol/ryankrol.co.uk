## Do

Read `src/scripts/migrateLegacyExerciseRows.js` and `src/scripts/backfillMissingExerciseRows.js` first as the closest style precedent (dry-run-by-default via `LIVE=1`, pure helpers exported for tests, `_env.js`/dotenv setup).

This script is necessary because the live write path (the dependency task) only ever compares a NEW workout against history that ALREADY has these flags computed — every row that existed before that task shipped has never been compared against anything and needs a one-time retroactive pass across the ~378 existing workouts / 1,314+ exercise rows.

1. **Write `src/scripts/backfillPersonalBests.js`.** Scan the `Exercises` table (the canonical flat one-row-per-exercise-instance source), GROUP rows by `exercise_name`, then within each group sort CHRONOLOGICALLY ASCENDING by `workout_date` (oldest first — this is the OPPOSITE order from the live write path, which only ever sees the newest workout; a historical replay must walk oldest-to-newest to correctly reconstruct "was this a genuine PR at the time"). Maintain an in-memory running best `{heaviestWeight, bestEstimated1RM, bestSetVolume}` per `exercise_name` as you walk forward. For each row, recompute its candidate session metrics AND set indices fresh from its own stored `sets[]` via `calculateExerciseMetrics` (do not trust the row's already-stored aggregate `heaviestWeight`/`bestEstimated1RM` fields for this — pre-existing rows won't have `bestSetVolume`/set-indices at all, so recompute all three axes fresh), call the shared `detectPersonalBests(sessionMetrics, runningBest)`, then update `runningBest` for any axis this row just beat.
2. For each row where at least one axis is a new PR: write the flag(s) onto the SPECIFIC set index/indices via an update (reuse `buildSetUpdateParams` from `src/lib/dynamo.js` if it fits this shape, else a minimal hand-rolled equivalent matching its style) against BOTH: (a) the `Exercises` table row (`exercise_id` key) — `sets[i].isWeightPR` etc — and (b) the MATCHING `Workouts` item's embedded `exercises[j].sets[i]` (same exercise position / set index) — both must be updated since both are read by different pages (list vs. detail), per the dependency task's rationale.
3. **Dry-run by default** (`LIVE=1` to write), matching sibling scripts exactly — log every row that would change and why (which axis, which set, old vs. new running-best).
4. **Export the pure logic** (grouping, chronological sort, running-best walk) for testing — not the AWS calls — mirroring how `migrateLegacyExerciseRows.js`/`backfillMissingExerciseRows.js` export `isLegacyShape`/`missingExerciseIndexes`.
5. Add an npm script entry to `package.json`'s `"scripts"` block (e.g. `"workouts:backfill-personal-bests": "node src/scripts/backfillPersonalBests.js"`).
6. Add `src/scripts/backfillPersonalBests.test.js` covering: correct grouping + chronological sort with multiple exercises interleaved out-of-order in the input; running-best correctly carried forward across a sequence; per-row PR flags matching what `detectPersonalBests` would independently produce given the same running-best (don't re-test `detectPersonalBests`'s own rules here, just that this script calls it correctly, in the right order, per exercise_name group).

Do NOT run this script live against production as part of this task — dry-run correctness only, verified by tests + a dry-run console read; a human runs it for real via the separate, explicitly needs-human follow-up task.

## Done when

- `src/scripts/backfillPersonalBests.js` exists, defaults to dry-run, exports its pure grouping/chronological-walk/running-best logic, and (when `LIVE=1`) would update both the `Exercises` row and the matching `Workouts` embedded exercise entry for every set that was a genuine historical PR, processed strictly oldest-to-newest per `exercise_name`.
- `src/scripts/backfillPersonalBests.test.js` passes, covering multi-exercise interleaving and correct running-best carry-forward.
- `package.json` has a script entry that runs the new file.
- `npm run lint` / `npm test` / `npm run build` all green.
