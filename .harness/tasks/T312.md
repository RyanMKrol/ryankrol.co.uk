## Do

By the time this task builds, all 5 per-type tasks have replaced `.review-card-with-edit`/`.edit-button-overlay` usage with `.review-edit-card`/`.review-edit-header`/`.review-edit-button` and deleted their own T262-T266 modifier rules — so the original base rules `.review-card-with-edit { position: relative; }` and `.edit-button-overlay { ... }` + `.edit-button-overlay:hover { ... }` (the '/* Edit page styling */' section's original two rules, now with no per-type modifier blocks left around them since tasks -3..-7 removed those) are fully dead code with zero references anywhere in src/pages. First run `grep -rn 'edit-button-overlay\|review-card-with-edit' src/pages` to CONFIRM zero hits before deleting anything (if any hit remains, STOP and fail soft rather than deleting still-used CSS — it means one of the 5 per-type tasks didn't fully land or was reverted). Once confirmed clean, delete the base `.review-card-with-edit` rule and the base `.edit-button-overlay`/`.edit-button-overlay:hover` rules from globals.css entirely (the '/* Edit page styling */' comment can stay as the section header for the new `.review-edit-*` rules that remain).

## Done when

`grep -n 'edit-button-overlay\|review-card-with-edit' src/styles/globals.css` returns NOTHING (fully removed) and the same grep over `src/pages` also returns NOTHING (nothing was ever relying on it). `npm run dev`, spot-check all 5 edit pages (`/reviews/{books,movies,tv,albums,perfumes}/edit`) still render correctly with the header-strip pattern intact (no visual regression from the deletion). `npm run lint`, `npm test`, `npm run build` all pass.
