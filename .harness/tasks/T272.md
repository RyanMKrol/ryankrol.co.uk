## Do

Add the dedicated `/hot-takes` page, wired to the prior task's API routes. Design reconciliation (do this, don't silently deviate): the owner picked 'the fuller/dedicated version' of the homepage's existing 'Latest takes' section when asked how this relates to it, but also described hot takes as a genuinely NEW, independent content type (just freeform text + date) rather than an aggregation of existing review text. Read those together as: `/hot-takes` should be the full, newest-first running list of these new bullet-point takes — analogous in *spirit* to how the homepage's 'Latest takes' section shows a preview of 3 recent items — but it is its OWN page backed by its OWN data (the `HotTakes` table), NOT a rebuild of the existing homepage section and NOT a fold-in of review snippets. Do NOT modify the homepage's existing `latestTakes` logic/section (`src/pages/index.js`, the `home-latest` block sourced from movies/tv/books/albums) — that stays exactly as-is; it is a separate, pre-existing concept about *reviews*, and reworking it is out of scope here.

- `src/pages/hot-takes/index.js` — fetch `/api/hot-takes` in a `useEffect` (matching the site's client-side-fetch convention), and render the results as a simple, newest-first bulleted list (this is explicitly NOT a card-grid/`ReviewCard` layout — no cover art, no star rating; just the take's text and its date, one line/bullet per take, styled with the Collection design tokens already in `globals.css`, e.g. reuse `.page-title`/existing typography classes rather than inventing a new design language). Show a simple empty-state message when there are zero takes yet.
- `src/pages/hot-takes/add.js` — a password-gated form: a `textarea` for the take's text (client-side require non-empty, mirroring the server's `validateHotTakeText`) and the existing password field pattern (see `src/pages/vinyl/add.js` for the form structure to mirror — loading/message/error state, POST on submit, redirect back to the list on success). The `date` is set server-side; the form does not collect it.
- Add a `~/hot-takes` entry to the `QUICK_LINKS` array in `src/pages/index.js` (alongside the existing `~/vinyl`/`~/listening`/`~/projects` entries) so the new page is discoverable from the homepage, without touching the `latestTakes` section itself.

Docs (same commit, per this repo's Definition of Done): add a `HotTakes` row to the Data model table in `CLAUDE.md` (`{ id, text, date 'DD-MM-YYYY' }`), add `src/pages/hot-takes/{index,add}.js` and the new API routes to `CLAUDE.md`'s file map, and add a short 'Hot takes' bullet to README.md's schema section describing the `HotTakes` table shape, matching how other tables are documented there.

## Done when

`/hot-takes` renders the full list of hot takes, newest first, as a plain bulleted list (no rating/cover art), with a sensible empty state when there are none yet. `/hot-takes/add` is password-gated, rejects an empty take client-side and server-side, and on success a new take appears at the top of `/hot-takes`. The homepage gets a new `~/hot-takes` quicklink; the existing `latestTakes` homepage section (movies/tv/books/albums review snippets) is untouched — confirm via diff that `src/pages/index.js`'s `latestTakes` useMemo and its rendering block are unchanged. `CLAUDE.md`'s Data model table and file map, and `README.md`'s schema section, both document the new `HotTakes` table and pages. `npm run lint`, `npm test`, `npm run build` all pass; manually load `/hot-takes` and confirm the list renders and an add round-trips to the top of the list.
