## Overview
A human decision is required: which of the 5 dummy-content Top-of-Mind placement variants built in top-of-mind-note-chooser (Rail panel / Hero band / Collapsible section / Latest-takes card / Hero inline strip, reachable via ?tom=1..5 on the real homepage) should become the real, permanent placement.

## Do
Run the site locally (npm run dev) and visit `/?tom=1` through `/?tom=5` in turn, comparing how each placement reads alongside the real hero/stat-tiles/collection-wall/rail content (not just the dummy text in isolation). Pick one variant number as the winner, or record 'none of these — reconsider' if none are satisfactory. Record the chosen variant number (and any specific tweaks wanted, e.g. 'variant 1 but move it above the shelf panel') in the worklog/task notes for the finalize task to consume.

## Done when
A winning variant number (1-5, or an explicit rejection with follow-up guidance) is recorded and available for the finalize task.
