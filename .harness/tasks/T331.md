## Overview
The owner finds the current PrBadge medal pill (src/components/Badge.js's PrBadge, styled by .pr-badge/.collection-badge in src/styles/globals.css) too visually heavy and wants to compare a few smaller/lighter restyle options live before committing to one, rather than picking from a written description.

## Do
Add a dev-only route src/pages/dev/pr-badge-variants.js (same localhost-only convention as the existing src/pages/dev/cache.js — no DynamoDB reads, no password gate needed since it's read-only and renders only static sample data) that renders, side by side with a visible label under each, at least 3 self-contained variant components built on top of the current T320/T321 pill treatment (do not regress the row alignment or always-visible-at-rest contrast those tasks just fixed): (A) 'Compact tint' — the existing soft-tint + currentColor-border pill, shrunk in padding and font-size, medal emoji kept, 'PR' suffix dropped (e.g. 'Weight' not 'weight PR'); (B) 'Dot indicator' — medal emoji replaced by a small colored dot, no border, smaller font, label only; (C) 'Outline chip' — Badge's existing outline variant instead of soft tint, smaller pill, no emoji, label only. Each variant must render all three axes (weight/1RM/volume) using realistic sample data so the owner can see how each looks with each label length. Do not touch src/components/Badge.js, src/pages/workouts/index.js, or src/pages/workouts/[id].js — this task is comparison-only and must not change any real page's rendered output.

## Done when
Visiting /dev/pr-badge-variants locally (npm run dev, localhost) shows at least 3 clearly labelled variant groups, each rendering weight/1RM/volume-style pills with no 'PR' suffix, visibly smaller than the current production .pr-badge, and the real /workouts and /workouts/[id] pages are pixel-identical to before this task (verify via VISUAL_CHECK_ONLY=workouts and VISUAL_CHECK_ONLY=workout-detail node scripts/visual-check.mjs — both must show the unchanged existing pill, since production markup wasn't touched).
