## Do

In `src/pages/workouts/index.js`: (1) Change `getExercisePreview()` (currently lines 52-58) so it no longer joins exercise names into a single ' · '-separated string. Instead have it return a small structured value the render can map over one-per-line, e.g. `{ shown: string[], remaining: number }` (export the function so it's unit-testable). (2) Update the JSX currently at lines 186-190 (the `<p className="workout-session-exercise-preview">` block) to render the exercise names as a proper vertical list — one name per line — e.g. a `<ul className="workout-session-exercise-list">` with one `<li className="workout-session-exercise-item">` per shown name, plus a trailing `+N more` line/item when `remaining > 0`. (3) Move the total-volume stat so it reads as visually grouped with/next to this new vertical exercise list rather than only appearing in the separate bottom `.workout-session-stats` row (lines ~192-203) — a reasonable approach is a small `.workout-session-volume` callout placed alongside the exercise list (e.g. both inside a shared flex row, list on one side and the volume figure on the other); you decide the exact placement, but it must read naturally as "this session's exercises, and its total volume" grouped together. You may keep the existing bottom `.workout-session-stats` row showing exercise count only (dropping the now-redundant volume from it), or leave both if there's a clear reason — avoid two disconnected renderings of the same number with no visual relationship between them. In `src/styles/globals.css`: add/update styles for the new markup (e.g. `.workout-session-exercise-list`, `.workout-session-exercise-item`, `.workout-session-volume`) reusing the existing Collection design tokens the old preview line used (`--font-mono`, `--color-ink-mute` etc. — see the current `.workout-session-exercise-preview` rule, currently lines ~1853-1860, for the tokens to match). Also add `min-width: 0` to `.workout-session-card` (currently lines ~1821-1831) — the likely root cause of the horizontal-overflow bug is CSS grid's default `min-width: auto` on grid items in `.workout-session-grid` (currently lines ~1808-1819, `grid-template-columns: repeat(2, 1fr)`), which lets long unbroken content force a grid item (and therefore the whole grid/page) wider than the viewport; `.project-card` (currently lines ~1759+) already sets `min-width: 0` for exactly this reason — follow that existing precedent. The vertical list change should independently reduce/eliminate the overflow risk (shorter per-line content), but add the `min-width: 0` fix regardless as belt-and-suspenders since it's the documented root cause. After both changes, check in the browser across a range of desktop widths (at least ~1024px, ~1280px, and a full ~1920px viewport) that `/workouts` never triggers horizontal scrolling or clips card content off-screen. You do NOT need to change any other grid-based list page (`poster-banner-grid`, `square-cover` grids, etc.) — briefly note in the worklog whether they look like they share this overflow risk, but do not modify them; that's out of scope here.

## Done when

1. `npm run lint`, `npm test`, and `npm run build` all pass. 2. A new/updated colocated test (e.g. `src/pages/workouts/index.test.js`) covers the reshaped `getExercisePreview()`: given a list of exercise objects, it returns the correct `shown` names (capped at the max shown count) and the correct `remaining` count, including the zero-exercises and no-truncation-needed cases. 3. `npm run dev`, open `/workouts`, and confirm: (a) each workout session card shows its exercise names as a vertical list — one name per line, not one long ' · '-joined line; (b) the total volume figure is visually grouped with/adjacent to that exercise list, not only buried in an unrelated stats row; (c) resizing the browser/devtools across ~1024px, ~1280px, and a full-width ~1920px viewport shows no horizontal scrollbar on the page and no card content clipped off-screen. Record what was checked (including the brief other-grid-pages overflow-risk note) in the worklog.
