## Do

`src/pages/api/reviews/perfumes/update.js` currently writes `date: date || new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` (line 103) — it trusts whatever `date` value the client-side form happened to submit in the request body (the edit form at `src/pages/reviews/perfumes/edit/[id].js:135` does pass `date: originalData.date`, so this mostly works today, but it's a weaker/more fragile pattern than the other review types and there is NO `editedDate` field anywhere in the perfume write path). Adopt the exact pattern already used in `src/pages/api/reviews/movies/update.js` (lines 31-60), which this task should mirror closely:

1. Before building `reviewData`, use a `GetCommand` against `DYNAMO_TABLES.PERFUME_RATINGS_TABLE` keyed by `{ id: originalId }` (note: perfumes' delete-and-reinsert flow for a changed id already exists above this point — do the `GetCommand` BEFORE the `idChanged` delete branch, using `originalId`, so the original item is still there to read) to fetch the existing review.
2. Compute `originalDate = existingReview.Item?.date` and `preservedDate = originalDate || new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` (fallback only if no original date is found, matching movies).
3. Compute `editedDate = new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` unconditionally on every update.
4. In `reviewData`, replace the current `date: date || new Date()...` line with `date: preservedDate` and add a new `editedDate` field alongside it (both stored on every update, same as movies).
5. The route no longer needs to read `date` from `req.body` for this purpose — leave the destructured `date` variable removed or unused-clean (check whether anything else in the file still references it; if not, drop it from the destructuring to avoid an unused-var lint warning).

Add `src/pages/api/reviews/perfumes/update.test.js`, mirroring `src/pages/api/reviews/movies/update.test.js` exactly in structure (mock `docClient`/`clearApiCache`, a `mockRes()` helper, a `todayDDMMYYYY()` helper): one test asserting that when the `GetCommand` mock resolves with an existing item carrying `date: '01-01-2024'`, the resulting `PutCommand`'s `Item` matches `{ date: '01-01-2024', editedDate: todayDDMMYYYY() }`; a second test asserting that when the `GetCommand` mock resolves with no `Item` (no original date found), `date` falls back to today AND `editedDate` is still set to today. Use a realistic perfume request body (title/designer/type/rating/fragranticaUrl/originalId/password — check `src/lib/perfumes.js` validators for what's required) so the handler's existing validation doesn't short-circuit before reaching the update logic.

Docs (same commit, per this repo's Definition of Done): update `README.md`'s `PerfumeRatings Table` schema block (around line 261-282) to add an `editedDate` field example (e.g. `editedDate: "20-01-2024"  // DD-MM-YYYY, set on every edit`) right after the existing `date` line, matching how the `BookRatingsV4` schema entry documents its own `editedDate`. Also update this repo's root `CLAUDE.md` Data model table row for `PerfumeRatings` to append `, editedDate` after `date` in its schema summary.

Do NOT touch `src/pages/reviews/perfumes/edit/[id].js` — it can keep passing `date: originalData.date` in its request body harmlessly (the server now ignores that field and derives the preserved date itself from a fresh `GetCommand`), and touching the edit page is not required for this fix. Do NOT touch `src/components/perfumeVariants/Variant6Hybrid.js` or any rendering — that is a separate task.

## Done when

`npm test` passes, including the new `src/pages/api/reviews/perfumes/update.test.js` (both the preserve-original-date case and the no-original-date-found fallback case, each asserting `editedDate` is stamped to today's date in `DD-MM-YYYY` format). `npm run lint` and `npm run build` both still pass. Manually confirm (or via the test) that submitting an update no longer lets a stale/incorrect client-supplied `date` silently overwrite the DynamoDB record's original review date — the server derives it itself via `GetCommand` on `originalId`. `README.md`'s `PerfumeRatings Table` schema block and `CLAUDE.md`'s Data model row both mention `editedDate` after this change.
