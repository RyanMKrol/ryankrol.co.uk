## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/3b.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4g.png` (mobile) for reference — structurally
identical to the movies list page rebuilt in T150, just with the `tv` accent color (`#4B4DED`
indigo) and TV-specific data.

Rebuild `src/pages/reviews/tv/index.js` following the exact same structure as the movies list page
from T150 (header/meta line, `SearchInput`, `PillGroup` sort, 3-col poster-banner `ReviewCard` grid)
— reuse the poster-banner `ReviewCard` variant built in T150 unchanged; this task should not need to
modify `ReviewCard.js` at all. If it turns out `ReviewCard.js` DOES need a change to fit TV data
correctly, that's a signal T150's variant wasn't generic enough — fix it there rather than
special-casing TV. No pagination in this task (that's T155).

## Done when

- The TV list page shows the same header/meta/search/sort/grid structure as movies, with the TV
  section accent, bound to real data from `/api/reviews/tv`.
- `ReviewCard.js` was not modified (or if it was, only to genuinely generalize the poster-banner
  variant, not to special-case TV).
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/tv`, confirm search/sort work and cards
  show real TV show data. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task
  T166.
