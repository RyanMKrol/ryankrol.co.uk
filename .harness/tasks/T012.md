# T012 — Date-range filter on the /programmes dashboard

## Do

Add the same date-range filter (T010) to the programmes overview page
(`src/pages/programmes/index.js`, built in T009), defaulting to **Last 1 year**.

- Render `DateRangeFilter` on the programmes page, keeping the selected range in state
  (default `1y`), alongside the existing Push/Pull/Legs selector.
- Apply `filterByDateRange` to the workout list (keying on each workout's
  `start_time`/`workoutDate`) before aggregating via `programmeStats`, so the volume /
  working-sets / frequency charts and the headline totals all reflect the chosen window.

## Done when

- `/programmes` shows a 3-months / 1-year / all-time control (default 1 year) that works
  together with the Push/Pull/Legs selector.
- Changing the range re-scopes every chart AND the headline totals for the selected
  programme to that window.
- The same `DateRangeFilter` component + `filterByDateRange` helper are reused (no
  duplicate control or filtering logic).
- `npm run lint`, `npm test`, `npm run build` pass; manually load `/programmes`, switch
  ranges + splits, and confirm the dashboard updates.
