## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/2a.png` (desktop movies list) and
`/Users/ryankrol/Downloads/design_handoff/renders/4b.png` (mobile) for reference.

`src/components/ReviewCard.js` already handles all 4+1 review types via a `type` prop and an
existing (unused-here) `styleVariant` prop (see `item.rating`/`getMaxRating()`/`getThoughts()`
helpers already inside it). Add a new **poster-banner** presentation to this component: a colored
gradient banner (per-item stable gradient — reuse the same hash-based gradient approach built for
`CoverTile` in T145, or better, delegate to `CoverTile` itself in a wide-aspect mode if that's a
clean fit) with the title overlaid at the bottom, then below the banner: star rating (read-only
`StarRating` from T144), the review date (mono font, right-aligned alongside the stars), and an
italic review-text snippet. This is the variant movies and TV both use (TV consumes it in the next
task, T151 — build it generically enough here that TV needs zero changes to this component).

Rebuild `src/pages/reviews/movies/index.js` around this variant:
- Header: big "movies" display title, meta line ("N rated · avg X.X★ · N this year" — compute avg
  rating and this-year count from the real fetched data, matching the existing page's fetch logic).
- `SearchInput` (from T143) for title search — reuse the existing search-filtering logic already in
  this page, just swap the input markup.
- `PillGroup` (from T143) for Date/Title/Score sort, Date active by default — reuse the existing
  `SortButtons`/sort logic already in this page (check whether to keep using `SortButtons.js` or
  replace it with `PillGroup` for consistency with the rest of the redesign; if `SortButtons` is used
  by other pages not yet redesigned, prefer swapping just this page to `PillGroup` without deleting
  `SortButtons.js` — later tasks handle the other pages).
- A 3-column grid of poster-banner `ReviewCard`s.

Do NOT add pagination in this task — that's a separate task (T155) that touches this file again
afterward; leave the current render-all-at-once behavior in place for now.

## Done when

- `ReviewCard.js` supports a poster-banner variant (gradient banner + overlaid title, stars, date,
  snippet below) usable by both movies and TV.
- The movies list page shows the new header/meta line, working search, working date/title/score sort
  (defaulting to date), and a 3-column poster-banner card grid, all bound to real data from
  `/api/reviews/movies`.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/movies`, confirm search filters the list,
  each sort pill re-orders the list correctly, and cards show real titles/ratings/dates/snippets.
  Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T166.
