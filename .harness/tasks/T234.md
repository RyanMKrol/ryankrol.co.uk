## Do

`src/scripts/auditWorkouts.js`'s `backfillMissing()` (lines ~155-242) hand-builds its own `workoutItem`/`exerciseItem` objects and issues its own `PutCommand`s against the `Workouts`/`Exercises` tables, duplicating (with slight, undocumented differences — e.g. no `backfilled_at` handling nuance, its own rate-limiting) `src/lib/workoutBackfill.js`'s logic. Import `storeWorkoutInDynamoDB` (and rely on it to call `storeExercises` internally — do not also call `storeExercises` separately) from `../lib/workoutBackfill.js`, now exported by the `wdi-shared-writes` task, and replace the hand-rolled item construction + Put calls in `backfillMissing()`'s loop with a single call to the shared function per missing workout. The shared functions use the shared `docClient` from `src/lib/dynamo.js` (same AWS credentials/region as this script's own local `dynamodb` client) — once nothing else in this file needs its own client instance for writes, it is fine to keep the local client only for the read-side Hevy/Dynamo comparison scans (`fetchAllWorkoutsFromDynamo`) if simplest, or switch those to the shared `docClient`/`paginatedScan` from `src/lib/dynamo.js` too for full consistency — prefer the latter if it doesn't meaningfully expand this task's diff. The granular `exercisesStored`/`exercisesSkipped` counters in the current summary print can't be derived from the shared function's return value (it doesn't return per-exercise counts) — it's fine to simplify the end-of-run summary to report workouts stored/skipped plus each workout's `exercises.length` attempted, rather than inventing a new count-returning API on the shared function just for cosmetic logging.

## Done when

auditWorkouts.js no longer constructs its own workoutItem/exerciseItem objects or issues its own PutCommands for the Workouts/Exercises tables in backfillMissing() — it calls the imported storeWorkoutInDynamoDB from src/lib/workoutBackfill.js for each missing workout instead. A new src/scripts/auditWorkouts.test.js mocks '../lib/workoutBackfill' (jest.mock) and asserts backfillMissing() calls storeWorkoutInDynamoDB exactly once per missing workout, passing the raw Hevy workout object, instead of asserting on raw AWS SDK PutCommand payloads. The `workout:audit` and `workout:audit-backfill` npm scripts still resolve and the CLI's `--backfill` flag behavior is unchanged. npm run lint, npm test, and npm run build all pass.
