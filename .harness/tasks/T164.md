## Do

No screen in `/Users/ryankrol/Downloads/design_handoff/` explicitly mocks the review/vinyl add/edit
forms — only list pages are mocked. Infer the visual treatment from the perfumes and vinyl list
pages rebuilt in T154/T156 (open `/Users/ryankrol/Downloads/design_handoff/renders/3d.png` and
`renders/2b.png` again for the general card/type/color language) and apply that same visual language
to the forms — this is a restyle, not a new design.

Restyle the perfume add/edit forms and the vinyl add form to Collection tokens and the shared
component library (`Pill`, `SearchInput`, `Badge`, and the `PipMeter` from T144 for the perfume
rating input specifically — replacing whatever rating input the perfume forms currently use, while
keeping the underlying `rating` value contract, an integer 0-10, unchanged):
- `src/pages/reviews/perfumes/add.js`, `src/pages/reviews/perfumes/edit.js`,
  `src/pages/reviews/perfumes/edit/[id].js`
- `src/pages/vinyl/add.js`

Keep ALL existing functionality completely unchanged, including perfume-specific fields not shown on
the redesigned list card (designer, type, `fragranticaUrl`, `longevity`, `projection`, `seasons`,
`applicationSpots` — these fields and their validation stay exactly as they are today, restyle their
inputs but do not remove or hide any of them from the FORM, only from the list card per T154's
scope), the Last.fm album search flow on the vinyl add form, password gating, and all validation.
This task is purely presentational.

## Done when

- All 4 files use Collection design tokens/components (no legacy hardcoded colors), the perfume
  rating input uses the interactive `PipMeter` while every other perfume-specific field remains
  present and functional, and every existing behavior (vinyl's Last.fm search, perfume field
  validation, password gate) still works exactly as before.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, go through the full add flow for a perfume (fill in all
  fields including the pip-meter rating input, submit) and a vinyl record (Last.fm search, submit),
  and confirm both still work end-to-end with the new styling; repeat for editing an existing perfume
  review. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T169.
