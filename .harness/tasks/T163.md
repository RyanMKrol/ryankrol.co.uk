## Do

No screen in `/Users/ryankrol/Downloads/design_handoff/` explicitly mocks the review add/edit forms
— only list pages are mocked. Infer the visual treatment from the books and albums list pages
rebuilt in T152/T153 (open `/Users/ryankrol/Downloads/design_handoff/renders/3a.png` and
`renders/3c.png` again for the general card/type/color language) and apply that same visual language
to the forms — this is a restyle, not a new design.

Restyle the books and albums add/edit forms to Collection tokens and the shared component library
(`Pill`, `SearchInput`, `Badge`, `StarRating` from T143/T144, plus general card/typography tokens
from T142):
- `src/pages/reviews/books/add.js`, `src/pages/reviews/books/edit.js`,
  `src/pages/reviews/books/edit/[id].js`
- `src/pages/reviews/albums/add.js`, `src/pages/reviews/albums/edit.js`,
  `src/pages/reviews/albums/edit/[id].js`

Keep ALL existing functionality completely unchanged — the Google Books search-and-confirm flow
(`BookSearch.js`), the Last.fm album search flow (`LastfmAlbumSearch.js`), the `MetadataBackfillModal`
backfill flow, password gating, form validation and submission. This task is purely presentational:
swap hardcoded/legacy CSS classes for the Collection tokens and shared components (e.g. the
interactive `StarRating` for rating input, search inputs, submit buttons), without touching any
`onSubmit`/`onSearch`/API-calling logic. If `BookSearch.js`/`LastfmAlbumSearch.js` need visual
updates to fit the new look, restyle them in place rather than forking.

## Done when

- All 6 files use Collection design tokens/components (no legacy hardcoded colors), and every
  existing behavior (Google Books search-and-confirm required to add a book, Last.fm search for
  albums, edit save/delete, password gate, backfill modal) still works exactly as before.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, go through the full add flow for a book (search Google
  Books, confirm a match, fill in rating/review, submit) and an album (Last.fm search), and confirm
  both still work end-to-end with the new styling; repeat for editing existing reviews of each type
  (including triggering the backfill modal). Detailed pixel-fidelity tuning is deferred to the Phase
  6 visual-audit task T169.
