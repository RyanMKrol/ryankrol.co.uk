## Overview
The PrBadge pill (medal + axis label) is washed out at rest: `variant="soft"` (src/components/Badge.js line 14) gives only a ~10%-opacity tint background, and the base `.pr-badge` rule (src/styles/globals.css ~2706-2710) sets an invisible `border: 1px solid transparent` -- full contrast only appears via the `.pr-badge:hover,:focus-visible` rule (~2712-2717). The owner wants the pill equally visible at rest and on hover/focus, with NO rest-vs-hover style difference at all.

## Do
In src/styles/globals.css, remove or equalize the `.pr-badge:hover, .pr-badge:focus-visible` rule so it produces no visual change from the resting `.pr-badge` state -- the full background + visible border treatment should apply permanently. Set the resting colour to a stronger-opacity `--accent-workouts` tint (the owner confirmed reusing the existing workouts accent colour rather than introducing a new gold/yellow token); implement it via the `.pr-badge` base rule and/or an adjusted style/variant passed from `PrBadge` in src/components/Badge.js, staying inside the existing CSS-custom-property system rather than a one-off hardcoded colour.

## Done when
Loading /workouts and /workouts/[id] with no mouse/keyboard interaction, every visible PrBadge pill already shows a non-transparent border and its full background tint -- inspecting computed styles shows `.pr-badge`'s resting `background-color`/`border-color` are identical to its `:hover`/`:focus-visible` computed values (hovering or focusing the pill produces no visible change). The medal + label text stays legible against the new background in both the Collection theme and the Matrix theme.
