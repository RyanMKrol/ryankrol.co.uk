## Do

Read `src/pages/workouts/index.js` in full — specifically `getExercisePreview` (lines 53-58) and the exercise list rendering (lines 192-205) inside the `MasonryColumns` `renderItem`.

Owner confirmed: the medal appears PER PR'd EXERCISE LINE within a workout card (not once per whole card).

`workout.exercises[]` (the embedded raw array this page already reads `.title` from) now ALSO carries, per exercise, a `sets[]` array whose individual set objects may carry `isWeightPR`/`is1RMPR`/`isVolumePR` booleans (added by the dependency task `workout-pr-medals-2` — its write path mutates this SAME embedded array, not just the separate `Exercises` table, precisely so this page can read PR status with no new fetch). Derive whether a given exercise achieved ANY personal best this session purely from existing data already in the page's response: `exercise.sets?.some(set => set.isWeightPR || set.is1RMPR || set.isVolumePR)` — no new API call.

`getExercisePreview` currently maps exercises to bare NAME strings (`exercises.map(e => e.title)`), losing the reference back to each exercise's `sets[]`. Restructure it (or the render call site) to preserve enough per-exercise data to derive PR status alongside the name — e.g. change its return shape to `{ name, hasPersonalBest }` objects instead of bare strings, updating its one existing usage/test accordingly if one exists (check for a `getExercisePreview`-specific test).

Render a small, accessible medal indicator (e.g. reuse the existing `Badge` component in a compact `variant="soft"` form, or a simple 🏅 glyph with an `aria-label`/visually-hidden text like "personal best" — an emoji-only marker isn't announced usefully by a screen reader alone) immediately next to any exercise name whose `hasPersonalBest` is true, within the existing `<li className="workout-session-exercise-item">` markup — keep it visually small/inline so it doesn't break the existing compact list layout.

Do not touch `/workouts/[id]` (a separate dependent task handles the detail page) or any API route.

## Done when

- A workout card on `/workouts` shows a small, accessible medal marker next to EVERY exercise-name line in its preview list where at least one of that exercise's sets this session carried `isWeightPR`/`is1RMPR`/`isVolumePR`, and no marker next to exercise lines with no PR sets.
- No new network request added — the page still only calls `/api/workouts?mode=all` once.
- `npm run lint` / `npm run build` green.
- Manual check via the `run-app` verifier: `npm run dev`, open `/workouts`, confirm at least one card (seed a temporary synthetic flag if no real backfilled data exists yet) shows the medal correctly positioned next to the right exercise name(s), and cards/exercises without any PR show no marker.
