## Overview
The home page's black 'This year at the gym' panel (src/pages/index.js ~lines 211-237) currently renders only two stat cells (sessions, best session volume) inside `.home-gym-stats`, even though the API it already fetches from (`/api/workouts/stats` → `getWorkoutStats()` in src/lib/workoutQueries.js lines 229-286) already computes and returns a `totalVolume` field (line 275) summing every workout's volume across the trailing 12 calendar months (cutoff at lines 231-234). This field is fetched into `workoutStats` state (src/pages/index.js line 44) but never read for `totalVolume` — only `bestSessionVolume` is destructured (line 127) and rendered (lines 218-224).

## Do
In src/pages/index.js, insert a NEW `.home-gym-stat` cell inside the `.home-gym-stats` div (currently lines 213-225) as the SECOND cell — between the existing 'sessions' cell (lines 214-217, stays first) and the existing 'best session vol' cell (lines 218-224, moves to third, unchanged otherwise). Owner-confirmed order: sessions → total volume → best session vol. The new cell reads `workoutStats?.totalVolume` (mirror the existing `bestSessionVolume` destructuring pattern at line 127, e.g. add `const totalVolume = workoutStats?.totalVolume ?? null` near it) and render it the same way `bestSessionVolume` is rendered — value plus a conditional trailing `<span className="home-gym-stat-unit">kg</span>`, falling back to '—' when null/0, with a `.home-gym-stat-label` of 'total volume'. Plain numeric formatting only — no thousands separator, no compact 'k' notation, matching the existing best-session-volume cell's own formatting exactly (owner-confirmed). No CSS changes are required: `.home-gym-stats` (src/styles/globals.css lines 3187-3191) is `display:flex; gap:2rem` with no fixed column count, so reordering/adding a child lays out naturally; only add CSS if the visual check (see doneWhen) shows real overflow/wrapping problems on mobile widths. The panel's title 'This year at the gym' is explicitly OUT OF SCOPE (owner-confirmed) — do not reword it even though it's a known misnomer for the actual trailing-12-month window.

## Done when
The home page's gym panel renders three stat cells in this exact order — sessions, total volume (new, labelled 'total volume', sourced from `workoutStats.totalVolume`), then best session vol — formatted consistently with the existing best-session-volume cell (same kg unit styling, same '—' fallback when absent, no thousands separator or compact notation). `node scripts/visual-check.mjs` (or the relevant filtered subset covering the homepage) is run and the resulting screenshot is inspected to confirm all three stats render legibly in the correct order and do not visually break the panel at both desktop and mobile widths captured by the harness.
