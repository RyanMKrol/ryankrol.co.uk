## Do

Root cause (confirmed by reading src/components/NowPlaying.js and src/styles/globals.css in full, and diffing the idle-state JSX branch against the active-track JSX branch directly): the idle/loading branches of NowPlaying.js (lines 34-50) render a flat pair of sibling spans — `<span className="now-playing-dot" /><span className="now-playing-label">not listening rn :)</span>` — directly inside `.now-playing-chip` (an `inline-flex; align-items:center` row, globals.css lines 1640-1651). The active-track branch (lines 54-76) never renders `.now-playing-label` at all; instead it renders `<span className="now-playing-dot playing" /><div className="now-playing-split">…MarqueeText/.single-line-track…</div>`. So the two branches use genuinely different markup/classes for the text side (`.now-playing-label` span vs `.now-playing-split` div > `.single-line-track`), and only the `.now-playing-label` path is affected. globals.css has a DEAD, pre-T146 CSS block (lines 1524-1562, header comment 'Now Playing Component - Last.fm') left over from before the pill-chip restyle, styled for an old, no-longer-rendered `.now-playing` (non `-chip`) wrapper: `.now-playing` (1525-1534), `.now-playing.loading, .now-playing.offline` (1536-1541), `.now-playing-label { font-size:0.875rem; font-weight:500; opacity:0.9; display:block; margin-bottom:0.5rem; }` (1543-1549), `.now-playing.loading .now-playing-label, .now-playing.offline .now-playing-label { margin-bottom:0; }` (1551-1554), the old `.now-playing-split { display:flex; align-items:center; gap:0.5rem; width:100%; }` (1557-1562), and unused `.now-playing-label-inline` (1564-1570). Confirmed via grep that no component anywhere renders className="now-playing" (bare) any more — only `now-playing-chip` (NowPlaying.js) — and `.now-playing-label-inline` has zero JSX references, so this whole block is dead code EXCEPT that its `.now-playing-label` selector still has the SAME class name as the one still rendered by the idle/loading chip branches. Because this stale rule (1543-1549) is declared BEFORE the current pill-chip `.now-playing-label` rule (1665-1671: font-size 0.8rem, color, white-space:nowrap, overflow:hidden, text-overflow:ellipsis — no display/margin properties), the two rules merge in the cascade rather than one replacing the other: the current `.now-playing-label` therefore renders with `display:block; margin-bottom:0.5rem;` (from the dead rule) PLUS the newer font/color/overflow properties. That asymmetric bottom-only margin on a flex item shifts its visible text upward off the chip's vertical center — while the dot (`.now-playing-dot`, no margin) sits correctly centered — producing exactly the idle-only misalignment the owner reports; loading state shares the same span/class so it has the identical bug (untested by the owner but same root cause). The active-track branch never touches `.now-playing-label` (it uses `.now-playing-split`/`.single-line-track`, both defined AFTER the dead block at 1572-1587 and NOT shadowed by any earlier conflicting rule), which is exactly why alignment is correct there and only there. FIX: delete the entire dead legacy block in src/styles/globals.css from the `/* Now Playing Component - Last.fm */` comment through the old `.now-playing-split` rule — i.e. remove `.now-playing`, `.now-playing.loading, .now-playing.offline`, `.now-playing-label` (the first, pre-restyle definition with display:block/margin-bottom), `.now-playing.loading .now-playing-label, .now-playing.offline .now-playing-label`, the old `.now-playing-split`, and `.now-playing-label-inline` (globals.css lines 1524-1570, ending right before the `/* Marquee Component */` comment / `.track-link-single` rule at line 1572, which IS still in use and must be kept). After deletion, `.now-playing-label` will resolve to ONLY the current pill-chip rule at (post-deletion, renumbered) globals.css ~1618-1624, which has no display/margin override, so it will sit as a normal flex item centered by `.now-playing-chip`'s `align-items:center` — matching the dot. Do not change NowPlaying.js or Header.js — the JSX/markup is correct as-is; this is purely a stale-CSS cascade bug. Double-check after editing that `.now-playing-chip`, `.now-playing-dot`, `.now-playing-dot.playing`, `.now-playing-label` (chip version), `.now-playing-split` (chip version, min-width:0/flex:1), `.header-now-playing .single-line-track`, and `.header-now-playing .marquee-container` (globals.css ~1635 onward) all remain intact and unchanged — only the dead pre-restyle block above them is removed.

## Done when

verify: ["run-app"]. Start the dev server (`npm run dev`) and load any page in a normal browser so the header's now-playing chip renders in its default IDLE state ('not listening rn :)', which is the common case since Last.fm scrobbling is usually off) — visually confirm the status dot and the 'not listening rn :)' text are now vertically centered with each other (previously the text sat visibly below-center relative to the dot). Then confirm the loading state (briefly visible on page load before the fetch resolves, or by throttling network in devtools) shows the same correct alignment, since it shares the same markup/class as idle. To verify the ACTIVE-track state without needing a live Last.fm scrobble: temporarily hardcode a fake response in NowPlaying.js's fetchCurrentTrack (e.g. `setTrack({ isPlaying: true, track: { name: 'Test Song', artist: 'Test Artist', lastFmUrl: null } })` in place of the real fetch, or stub `/api/lastfm/now-playing`'s response), reload, and confirm the dot and the scrolling/marquee track text are STILL vertically aligned (no regression from removing the dead CSS block) — then revert the temporary hardcode before finishing. Also confirm via browser devtools that the dead `.now-playing`/`.now-playing.loading`/`.now-playing.offline`/`.now-playing-label-inline` selectors no longer appear in globals.css and that no other rendered element relied on them (grep confirmed none did). Run `npm run lint` and `npm run build` and keep them green; no test changes are expected since this is CSS-only with no new pure logic.
