## Do

Run the create-table script from the prior task to provision the `HotTakes` table in DynamoDB, using the AWS credentials already available in this checkout's `.env.local` (root `CLAUDE.md`'s standard `vercel env pull --environment production` convention) — the same precedent as T083's autonomous production DynamoDB operation.

```
npm run hot-takes:create-table
```

If the command fails because `.env.local` is missing or the AWS credentials it contains are invalid/expired, record `failed:blocked` naming the specific error — do not attempt to work around a genuine credentials gap.

## Done when

`npm run hot-takes:create-table` has completed successfully against the owner's AWS account, and the `HotTakes` table shows status ACTIVE in DynamoDB (region `us-east-2`) with a single String partition key `id`.
