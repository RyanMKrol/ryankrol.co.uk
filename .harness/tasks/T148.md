## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/1a.png` ("Life in numbers" 6-stat row) and
`/Users/ryankrol/Downloads/design_handoff/renders/2c.png` (5 stat cards, exercise detail page) for
reference — both use the same underlying block shape with different fill treatments.

Build `src/components/StatBlock.js`: a rounded block (`var(--radius-stat)`) showing a big number
(`var(--font-display)`, 40-52px per the design) and a small label beneath. Support two fill modes via
a prop: `accentColor` (solid fill in a given section accent, white text — used by the home page's 6
stat blocks, one per accent color) and a neutral/outlined mode (white/card background, ink border —
used where the design shows plain bordered stat chips, e.g. workout detail's date/duration/volume
chips). Also support an optional smaller `sublabel`/unit suffix (e.g. exercise detail's "79" + "kg"
shown together, per `renders/2c.png`'s "ALL-TIME 1RM / 79kg" card).

## Done when

- `StatBlock.js` exists, supports both accent-filled and neutral-bordered modes, and renders a
  big-number + label (+ optional unit) matching the general shape in the reference renders.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): temporarily render a few `StatBlock`s with different accent colors and
  the neutral mode on any page to confirm both fill modes look correct, then remove the temporary
  usage. Not wired into a real page yet — that happens in T149 (home) and later workout/exercise
  tasks. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit tasks (T165-T169).
