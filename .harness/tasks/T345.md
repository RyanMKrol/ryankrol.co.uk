## Overview
The perfume review card (Variant6Hybrid, used directly by src/pages/reviews/perfumes/index.js since perfumes bypass the generic ReviewCard component) shows a concentration-type badge (EDT/EDP/Parfum/etc.) wrapped in a Tooltip whose label currently reads 'What it is'. The user wants this reworded to "What I've got" so its voice matches the sibling ownership badge's tooltip, which already reads 'What I own' (line 48) in first person.

## Do
In src/components/perfumeVariants/Variant6Hybrid.js, change line 43 from `<Tooltip label="What it is">` to `<Tooltip label="What I've got">`. This is the only place the string 'What it is' appears in src/ (confirmed by grep) — no other file or component references this tooltip text. Do not touch the ownership badge's tooltip (line 48, 'What I own') or any other line in the file. Use a plain ASCII apostrophe in "I've" to match the file's existing quoting style (the surrounding JSX already uses double-quoted JSX attribute strings, e.g. line 48's "What I own").

## Done when
src/components/perfumeVariants/Variant6Hybrid.js line 43 reads `<Tooltip label="What I've got">` and no other line in the file changed. `npm run lint`, `npm test`, and `npm run build` all stay green. Visual verification (auto-fires for this ui-layer/style-workType change per VISUAL_VERIFY_LAYERS/VISUAL_VERIFY_WORKTYPES) shows the perfumes review page's type badge tooltip rendering "What I've got" on hover/focus.
