## Do

Apply the shared header-strip pattern from perfumes-edit-redesign-2 to the tv edit page, replacing T264's contrast-only fix (same rationale as movies — the opaque header strip makes contrast-against-the-gradient moot). In src/pages/reviews/tv/edit.js's `renderItem` callback: change the wrapper div's className from `"review-card-with-edit"` to `"review-edit-card"`; move the Edit `<Link>` to be the FIRST child, wrapped in a new `<div className="review-edit-header">`, with the Link's className changed from `"edit-button-overlay edit-button-overlay--tv-banner"` to `"review-edit-button"`; the `<ReviewCard .../>` (styleVariant="poster-banner") becomes the second child. In src/styles/globals.css, delete the now-dead T264 rules: `.edit-button-overlay--tv-banner` and `.edit-button-overlay--tv-banner:hover`, plus their explanatory comment, located between the movies-banner fix block and the square-cover (albums) fix block — do NOT touch the base rules or the books/albums/perfumes fix blocks.

## Done when

`npm run dev`, visit `/reviews/tv/edit`: same acceptance as movies (perfumes-edit-redesign-4) applied to tv shows — neutral header strip, Edit button right-aligned above the poster banner, no more dark-opaque contrast styling needed or present. Clicking Edit navigates correctly. Card content below the strip matches `/reviews/tv` (view page) for the same show. `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/tv/edit.js` returns NOTHING. `npm run lint`, `npm test`, `npm run build` all pass.
