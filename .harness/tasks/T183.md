## Do

Create a new standalone presentational component at `src/components/perfumeVariants/Variant4SeasonPlanner.js`, default-exported, accepting props `{ item }` (same raw `PerfumeRatings` record shape — see README.md's 'PerfumeRatings Table' section and src/pages/api/reviews/perfumes/add.js). This variant's direction is a PRACTICAL 'what should I reach for' planner card, organised around when/how to wear the perfume rather than reviewing it: `seasons` (if present) rendered as large, prominent chips at the TOP of the card under a small 'Best for' label (this is the variant that gives seasons top billing — no other variant does) — if `seasons` is absent, show nothing in that slot rather than a placeholder. Below that, render `longevity` and `projection` (when present) as horizontal labelled progress/scale bars with the scale endpoints spelled out in words (e.g. longevity bar labelled 'Light ↔ All-day' with the fill proportional to `longevity/8`; projection bar labelled 'Skin scent ↔ Room-filling' with fill proportional to `projection/4`) — do not just print the raw integers, the point of this variant is translating the numeric scale into plain-language framing. Below that, if `applicationSpots` is present, render it as a short practical checklist ('2 sprays — Wrists', '1 spray — Back of neck', ...). Title/designer/type/rating stay present but de-emphasised (small header line), and skip `description` entirely in this variant — it is intentionally utility-first, not a review-reading surface. Include a small Fragrantica link. Add new, uniquely-prefixed CSS classes (e.g. `.perfume-v4-*`) to `src/styles/globals.css` — ONLY ADD new rules, never edit/remove existing ones. Standalone component, no dependency on ReviewCard.js or a specific parent container.

## Done when

`src/components/perfumeVariants/Variant4SeasonPlanner.js` exists and renders seasons-first, then labelled longevity/projection scale bars (translated to plain language, not raw numbers), then an applicationSpots checklist, with a de-emphasised title/designer/type/rating header and no description text. `npm run dev` and confirm in a running dev server (via the paired preview-page task, or a temporary scratch mount) that real records with and without these optional fields render correctly (no 'undefined' bars, no empty 'Best for' label when seasons is absent) with no console errors. `npm run build` succeeds and no pre-existing rule in `src/styles/globals.css` was modified or removed.
