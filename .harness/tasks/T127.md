## Do

`src/pages/reviews/albums/index.js` fetches album reviews and sorts them client-side by `sortBy` state — same overall shape as the other review-type list pages, but with two extra `artist`/`artist-desc` sort branches. The date-sort branches (confirmed: `date-desc` around lines 77-78, default/most-recent-first around lines 84-85) do `new Date(a.date.split('-').reverse().join('-'))` / `new Date(b.date.split('-').reverse().join('-'))`.

A separate, already-landed task added an optional `editedDate` field (same `DD-MM-YYYY` string format as `date`) to album review records, written by `src/pages/api/reviews/albums/update.js` whenever a review is edited — `editedDate` is present only on records that have been edited at least once; older/never-edited records only have `date`.

Update BOTH date-sort branches so they prefer `editedDate` when present, falling back to `date` otherwise: replace `a.date` with `(a.editedDate || a.date)` and `b.date` with `(b.editedDate || b.date)` in the `.split('-').reverse().join('-')` expressions in both branches, e.g.:

```js
const dateA = new Date((a.editedDate || a.date).split('-').reverse().join('-'));
const dateB = new Date((b.editedDate || b.date).split('-').reverse().join('-'));
```

Do not touch the `title`/`title-desc`/`artist`/`artist-desc`/`score`/`score-desc` sort branches, the `SORT_FIELDS` config, or any other file. Do not touch `src/pages/reviews/movies/index.js`, `tv/index.js`, or `books/index.js` — those are separate, independent tasks.

## Done when

- Both date-sort branches in `src/pages/reviews/albums/index.js` (`date-desc` and the default 'most recent first' case) compute their comparison date from `(item.editedDate || item.date)` instead of `item.date` alone.
- An album review with only `date` (no `editedDate`) sorts by `date`, exactly as before — no regression for never-edited reviews.
- An album review with both `date` and a later `editedDate` sorts using `editedDate` — e.g. an old review (`date: '01-01-2020'`) edited today (`editedDate` = today) now sorts as the most recent item under the default 'most recent first' sort.
- `npm run lint` passes with no new errors.
- `npm run build` succeeds.
