## Do

`src/pages/api/reviews/albums/update.js` is the POST handler that edits an existing album review in the `AlbumRatingsV2` DynamoDB table (composite key `title` + `artist`). Unlike the movies/tv/books update routes, this one uses `UpdateCommand` (not `GetCommand`+`PutCommand`) and its `updateExpressionParts` array (built at lines 30-51) never mentions `date` at all — so today the `date` field is implicitly preserved (DynamoDB's `UpdateCommand` only touches attributes named in the `UpdateExpression`), but there is still no record of when an edit happened, matching the same gap as the other review types.

Add a new field, `editedDate`, to track when the album review was last edited, using the exact same `DD-MM-YYYY` string format/convention used elsewhere in this codebase (`new Date().toLocaleDateString('en-GB').replace(/\//g, '-')`, the same one-liner used in the sibling `add.js` routes):

1. Because `#rating` is already an alias in `expressionAttributeNames` (`'#rating': 'rating'`, line 31) to work around DynamoDB reserved-word rules, follow the same pattern for consistency: add `'editedDate = :editedDate'` to the `updateExpressionParts` array at line 30 (no attribute-name aliasing is actually required for `editedDate` since it isn't a reserved word, but do add it as a plain expression part alongside `highlights = :highlights`).
2. Add `expressionAttributeValues[':editedDate'] = new Date().toLocaleDateString('en-GB').replace(/\//g, '-');` alongside the existing `:rating`/`:highlights` value assignments (around lines 32-35), computed unconditionally (every call to this route is an edit).
3. Do not touch the `lastfm`-conditional block (lines 37-51) or the `UpdateCommand` params structure (lines 53-63) beyond incorporating the new expression part/value — the `Key`, `ReturnValues: 'ALL_NEW'`, password gating, validation, and cache clear (`clearApiCache('api-albums')`, line 68) are all unchanged.

Also update the repo's docs in the same change per `CLAUDE.md`'s Definition of Done: in the root `CLAUDE.md`'s Data model table, the `AlbumRatingsV2` row currently reads `{ title, artist, rating, highlights, date, thumbnail (Last.fm cover URL or ''), lastfm? {...} }` — extend it to mention the new optional `editedDate 'DD-MM-YYYY', set on every edit` field. Also check `README.md`'s schema section for the equivalent row and update it the same way if it duplicates this shape.

Do not touch `src/pages/api/reviews/movies/update.js`, `tv/update.js`, or `books/update.js` — those are separate, independent tasks. Do not touch `src/components/ReviewCard.js` or any `index.js` sort logic — those are separate tasks too.

## Done when

- `src/pages/api/reviews/albums/update.js`'s `UpdateExpression` includes an `editedDate = :editedDate` part, and `expressionAttributeValues[':editedDate']` is computed as today's date in `DD-MM-YYYY` format on every call.
- A manual/curl POST to `/api/reviews/albums/update` with a valid `password`, `title`, `artist`, `rating`, `highlights`, `originalTitle`, `originalArtist` for an existing album results in the DynamoDB item's `date` field remaining untouched (still implicitly preserved via `UpdateCommand`, unchanged behavior) and `editedDate` set to today's date in `DD-MM-YYYY` format.
- Calling update again on the same item on a later date updates `editedDate` to the new date while `date` still stays at its original value.
- `CLAUDE.md`'s Data model table row for `AlbumRatingsV2` documents the new `editedDate` field, and `README.md`'s schema section is updated to match if it independently describes this table's shape.
- `npm run lint` passes with no new errors.
- `npm test` passes.
- `npm run build` succeeds.
