## Do

Precondition: `review-edit-url-id-9` has produced a human-verified `VinylCollectionV2` table (partition key `id`, replacing the old `title`+`artist` composite key). Vinyl has no edit/delete page or API route today (only `src/pages/vinyl/{index,add}.js` and `src/pages/api/vinyl/{index,add}.js` exist), so unlike the four review-type cutover tasks, there is no edit-page routing to change here — this task is narrowly about repointing vinyl's storage to the id-keyed table and starting to generate ids on write, for consistency with the other four tables. Do NOT add any new edit/delete UI — that is out of scope for this idea.

Changes: (1) `src/lib/constants.js`: `VINYL_COLLECTION_TABLE: 'VinylCollection'` → `'VinylCollectionV2'`. (2) `src/pages/api/vinyl/add.js`: generate `id: randomUUID()` on create (alongside the existing `title`/`artist`/`thumbnail`/`lastfm` fields — see current shape at lines 28-46). (3) `src/pages/api/vinyl/index.js`: no logic change needed (it already does `scanTable(DYNAMO_TABLES.VINYL_COLLECTION_TABLE)`, which now resolves to `VinylCollectionV2` via the constants.js change) — confirm returned items include `id` (the `src/pages/vinyl/index.js` list view doesn't currently need `id` for anything, but it's harmless to have it present). (4) Update `src/pages/api/vinyl/add.test.js` to assert a generated UUID `id`. (5) Docs: update `CLAUDE.md`'s Data model row for `VinylCollection` (now `VinylCollectionV2`, partition key `id`), and rewrite the `#### VinylCollection Table` section in `README.md` (`README.md:145-169`) to `#### VinylCollectionV2 Table` reflecting the new single `id` key with `title`/`artist` as plain attributes.

## Done when

`npm run lint`, `npm test`, `npm run build` all pass. Adding a vinyl record works end-to-end and the created item has a generated UUID `id`. `src/lib/constants.js`'s `VINYL_COLLECTION_TABLE` points at `'VinylCollectionV2'`. `grep -r VinylCollection[^V]` (i.e. bare `VinylCollection`, not `VinylCollectionV2`) `src/` returns no matches outside comments/docs. `CLAUDE.md`/`README.md` updated. Diff includes a changed test file. No new edit/delete page or route is added (confirm `src/pages/vinyl/` still only has `index.js`+`add.js` and `src/pages/api/vinyl/` still only has `index.js`+`add.js`, now plus their test files).
