## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Interactions & behavior" → Nav section
and open `/Users/ryankrol/Downloads/design_handoff/renders/1a.png` (desktop nav) and
`/Users/ryankrol/Downloads/design_handoff/renders/4a.png` (mobile ☰ nav) for reference.

Rebuild `src/components/Header.js` in place (KEEP the filename/default-export — every page currently
imports `Header` from this path; renaming it would force a mechanical import-path edit across every
page for no benefit, so don't) into the persistent Collection nav:

**Desktop**: wordmark (small circular "r" avatar + "ryan krol" text, per the design) on the left;
9 section pills in the center (books/movies/tv/albums/perfumes/vinyl/workouts/listening/projects,
each a `Link` wrapped in the `Pill` component from T143) — the pill matching the current route is
`active` with that section's accent color (`var(--accent-movies)` etc., from T142 — use Next.js
`useRouter().pathname` to determine the active section, matching on the route prefix, e.g.
`/reviews/movies` → movies accent, `/vinyl` → vinyl accent, `/workouts` or `/exercises` →
workouts accent); the existing `NowPlaying` component on the right, wrapped in the pill-shaped
bordered chip treatment shown in the design (dot indicator + "not listening rn :)" / track text) —
reuse `NowPlaying`'s existing polling logic unchanged, only restyle its container/wrapper.

**Mobile**: collapses to a ☰ button that opens/closes a simple menu listing the same 9 section
links (implementation detail — a dropdown, slide-in panel, whatever's simplest; the design doesn't
mock the open state, just the collapsed ☰ trigger in `renders/4a.png`). Now-playing chip can move
into the mobile menu or stay visible in the collapsed bar — your call, prioritize it staying visible
since it's a signature feature of the site.

This component is used on every single page via `src/pages/_app.js` — confirm it's still rendered
there once per page (check how `_app.js` currently wires `Header` in before changing anything, and
keep that wiring pattern, e.g. if `Header` is rendered inside `MatrixLayout`/per-page vs. globally
in `_app.js`).

## Done when

- `Header.js` renders the desktop pill nav + wordmark + now-playing chip, and collapses to a ☰ menu
  under a mobile breakpoint, matching the general structure in `renders/1a.png`/`renders/4a.png`.
- The pill for the current route is visually active with the correct section accent color for all 9
  sections (verify by navigating to at least 3 different sections and confirming the fill color
  changes and matches that section's accent from the design README's table).
- `NowPlaying`'s existing polling/display logic is unchanged — only its visual wrapper changed.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, navigate between at least movies/vinyl/workouts and
  confirm the active pill accent color is correct for each; resize the viewport to ~390px width and
  confirm the nav collapses to a working ☰ menu that lists all 9 sections and navigates correctly.
  Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit tasks (T165-T169).
