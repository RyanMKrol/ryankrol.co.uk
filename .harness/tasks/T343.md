## Overview
The live perfume card (src/components/perfumeVariants/Variant6Hybrid.js, rendered by src/pages/reviews/perfumes/index.js) shows its 0-10 rating as small text (`.perfume-v1-rating-number`, globals.css:3636-3640, font-size: 0.85rem) directly under the description, well below the title (`.perfume-v1-title`, 1.1rem, globals.css:3618-3622) — the owner finds it easy to overlook. The owner also wants to see the 'Best for' season-chip block (`.perfume-v4-best-for`, lines 66-77) moved further down the card instead of sitting immediately under the rating row. Rather than committing to one fix, the owner wants a small set of visual options to compare, following this repo's own established precedent for exactly this kind of request (tasks T180-T187: a dev preview page with several card variants, reviewed live, decision recorded, winner promoted) — scaled down here since this is a positional/sizing tweak to the existing card, not a full redesign from scratch.

## Do
In `src/components/perfumeVariants/Variant6Hybrid.js`, add support for rendering a small number of layout variants of the SAME card (do not fork into separate component files — this is a positional/sizing change, not a new design). Suggested approach: add a `variant` prop (e.g. `'baseline' | 'big-rating' | 'header-rating' | 'best-for-bottom'`, default `'baseline'`) that toggles which CSS classes/DOM ordering are used for (1) the rating number's size/position and (2) the 'Best for' block's position, so the JSX/CSS differences are additive and reviewable, not four fully-duplicated files. At minimum include: baseline (today's exact layout, for side-by-side reference), one variant with the rating number enlarged in its CURRENT position (bump `.perfume-v1-rating-number` toward or beyond the title's 1.1rem, e.g. 1.25-1.4rem, still under the description), one variant with the rating moved UP into the header row next to `.perfume-v1-title` (e.g. rendered inline beside or just under the title, before the description), and one variant with the 'Best for' season-chip block relocated to near the bottom of the card (after the longevity/projection `.perfume-v4-scales` block, above the Fragrantica link/date) — combine this with whichever rating treatment you judge reads best, or make Best-for position an independent toggle if that's cleaner to implement. Add new CSS rules under new, uniquely-prefixed class names (do not edit/remove any existing `.perfume-v1-*`/`.perfume-v4-*`/`.perfume-v6-*` rule the live page depends on — the live `/reviews/perfumes` page must keep rendering exactly as it does today when no variant prop is passed). Create `src/pages/dev/perfume-card-variants.js`, a dev-only page that fetches real data from `GET /api/reviews/perfumes` (same fetch pattern as `src/pages/reviews/perfumes/index.js`) and renders 2-3 real perfume records through Variant6Hybrid once per variant, labelled clearly (e.g. a heading above each column/row naming the variant), so the owner can compare them side by side with real content. Follow the localhost-auto-auth convention used by `src/pages/dev/cache.js` if you judge it's warranted for consistency, or leave it ungated if simplicity is preferred — either is acceptable since this page is temporary and will be deleted once a winner is picked (mirroring T187's cleanup of `/dev/perfume-variants`). Update `scripts/_visual-harness.mjs`: add a new `PAGES` entry for `/dev/perfume-card-variants` (waitFor the card class, description, covers listing this task's scope files) so the visual-check hook captures the new page.

## Done when
`src/components/perfumeVariants/Variant6Hybrid.js` renders at least 3 distinct variants (baseline, enlarged/relocated rating, relocated Best-for) selectable via a prop, with no existing CSS rule modified or removed and the live `/reviews/perfumes` page rendering identically to before (default/baseline variant) when no prop override is passed. `src/pages/dev/perfume-card-variants.js` exists, fetches real perfume data client-side, and renders each variant labelled and visually comparable side by side. `npm run dev`, open `/dev/perfume-card-variants`, and confirm all variants render with no console errors and visibly different rating size/position and Best-for position per variant. `npm run lint`, `npm test`, and `npm run build` all stay green. `scripts/_visual-harness.mjs` has a new PAGES entry covering the new dev page, and `node scripts/visual-check.mjs` produces a clean screenshot of it.
