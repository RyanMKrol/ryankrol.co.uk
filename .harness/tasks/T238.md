## Do

**This is the single canonical deploy checkpoint for the backlog.** At any given time there must be
at most ONE task in `TASKS.json` with `status: pending` and a title matching "Deploy pending site
changes to production" — this is it right now. Vercel's Git integration is disconnected (see root
`CLAUDE.md`'s "Deploying" section) — none of this task's dependencies' work goes live no matter how
many commits land on `main`. This task makes no code changes; it is a pure operational run (mirrors
T216's/T193's precedent — `scope: []`, diff should be empty aside from the worklog).

This task's `dependsOn` covers the 19 buildable tasks (T217-T237, excluding T228 and T232, both
`gate:"needs-human"` decision tasks with no code diff of their own) from the 2026-07-04 `/convert-ideas`
sweep: a shared English-language date-format helper + its rollout across movies/tv/books/albums/workouts
(T217-T220), migrating the 5 review edit-list pages off their legacy card style + delowercasing/
de-emoji-ing all 10 edit-page headers (T221-T222), CSS multi-column masonry for books/movies+tv/workouts
review lists (T223-T225), Projects-page forks-on-card + stars/date sort (T226-T227), the search-box
placeholder-truncation fix (T229), Projects tag-filter frequency ranking + show-more (T230), the
search-box/sort-buttons vertical-alignment fix (T231), and the workout-database write-path
consolidation + additive Exercises-table repair (T233-T237 — NOTE: T232, the "should we do a full DB
wipe" decision, is intentionally excluded since it's a needs-human task with no buildable code of its
own; T233-T237 are the safe, purely-additive write-path fixes that were shaped as autonomous regardless
of how T232 resolves).

1. `git fetch origin && git status` — confirm the local checkout's `main` matches `origin/main`
   exactly (no ahead/behind). If it doesn't, `git pull` first.
2. `npm run build` locally as a final pre-deploy sanity check (CI should already have passed this on
   every commit, but re-confirm before spending a Vercel build).
3. `vercel whoami` — confirm the CLI is authenticated. **If this fails (not logged in), STOP and
   record `failed:blocked`** with that exact reason — this is a genuine prerequisite this task
   cannot supply itself, a human needs to run `vercel login` once. Do not attempt any workaround.
4. Run the deploy non-interactively and capture its output (the production URL is the last line of
   stdout on success):
   ```sh
   vercel --prod --yes
   ```
5. Confirm the deployment actually reached a ready state — parse the CLI's own exit code AND
   independently verify by fetching the returned URL:
   ```sh
   curl -s -o /dev/null -w '%{http_code}' <the-production-url>
   ```
   must print `200`. Note: the raw per-deployment URL may 302-redirect (Vercel's deployment-
   protection/SSO on internal deployment hostnames, not a failure) — if so, verify against the
   aliased production domain (`https://www.ryankrol.co.uk`) instead, same as prior deploy tasks'
   precedent.
6. Objectively confirm the deployed site reflects real changes from this batch. Because this task is
   authored BEFORE its dependencies build, don't rely on any specific string/value guessed now — at
   the time this task actually runs, first read the real committed code on `main` for at least 2-3 of
   the dependency tasks above, then write concrete spot-checks against whatever those tasks actually
   landed (e.g. confirm a review-list date renders as "17 May 2026" rather than "17-05-2026"; confirm
   `/reviews/perfumes/edit` no longer shows the old left-border-stripe card style; confirm the books
   review list at `/reviews/books` no longer stretches every card in a row to equal height) and run
   them against the live production site.

## Done when

- `vercel whoami` succeeded (or the task correctly stopped with `failed:blocked` if it didn't).
- `vercel --prod --yes` exited 0 and printed a production URL.
- `curl -s -o /dev/null -w '%{http_code}' <url-or-www.ryankrol.co.uk>` printed `200`.
- The spot-checks in step 6 (derived from actually reading the real committed code, not guessed
  ahead of time) found matches against the live production site.
- The exact production URL, the deployment's timestamp, and the spot-check results are recorded in
  this task's worklog.
