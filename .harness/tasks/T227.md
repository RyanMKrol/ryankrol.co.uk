## Do

Second piece of the sort-options request (owner asked for sortable Projects cards; stars and date are the two dimensions already fully available with zero new API work — `repo.stars` and `repo.lastPush` are both already fetched by `GET /api/github/repos` and already rendered on every card, per `src/pages/api/github/repos.js:80-91`). Commit count and lines-of-code sorting are explicitly OUT of scope for this task — commits aren't fetched at all today (would need a GitHub GraphQL migration, gated behind its own needs-human decision task in this same batch) and lines-of-code isn't obtainable from GitHub's API at all (no task authored for it — see that decision task's notes).

Currently `src/pages/projects/index.js` has no user-facing sort control at all; `filteredRepos` (a `useMemo` over `repos`, `searchTerm`, and `selectedTags`) is rendered in the order the API already returned it (server-side, always newest-push-first, fixed in `src/pages/api/github/repos.js:69-79`). Reuse `src/components/SortButtons.js` (already used by the four review-list pages — read one of those, e.g. `src/pages/reviews/movies/index.js`, for the exact reuse pattern) rather than inventing a new control:

1. Import `SortButtons` from `'../../components/SortButtons'` (same relative depth as the existing `Badge`/`SearchInput`/`Pill` imports).
2. Add a top-level `SORT_FIELDS` const (see the `design` field on this task for the exact field list/values) and a new `const [sortBy, setSortBy] = useState('date');`.
3. Add a new `sortedRepos` derivation (a `useMemo` depending on `filteredRepos` and `sortBy`, or extend the existing `filteredRepos` useMemo — either is fine as long as search/tag filtering still happens before sorting) implementing: `'date'` → sort by `lastPush` descending (current default behaviour, `new Date(b.lastPush) - new Date(a.lastPush)`), `'date-asc'` → ascending, `'stars'` → sort by `stars || 0` descending, `'stars-asc'` → ascending. Render this sorted array in the grid instead of the raw `filteredRepos`.
4. Render `<SortButtons fields={SORT_FIELDS} sortBy={sortBy} onChange={setSortBy} />` inside the existing `.collection-review-controls` div, alongside `SearchInput` and the tag-pill group (same container the review-list pages use for `SearchInput` + `SortButtons` together) — no new wrapper div or CSS needed, this container is already a flex row.
5. Update the static meta copy 'what I've been building on github · sorted by recent activity' (`projects/index.js` ~line 113) since the sort is no longer fixed — reword to something like 'what I've been building on github' (drop the now-inaccurate 'sorted by recent activity' clause) or similar; keep it a short, factual one-liner, not a UI decision requiring further sign-off.

Do not touch `src/components/SortButtons.js` (already correct/generic) or `src/pages/api/github/repos.js` (its server-side sort becomes just the initial fetch order; client-side sort takes over from there — no need to remove or change the API's own sort).

## Done when

`/projects` renders a `SortButtons` control (stars, date) alongside the existing search box and tag pills. Clicking 'date' once keeps newest-first (unchanged initial view); clicking it again flips to oldest-first. Clicking 'stars' sorts highest-starred first; clicking it again flips to lowest-starred first. Switching from one field to the other always lands on that field's default direction (not carrying over a previous flip). Search and tag filtering continue to work identically to before (spot-check: typing in the search box still filters the sorted list; toggling a tag pill still filters by topic). The static 'sorted by recent activity' copy is removed or reworded to no longer claim a fixed sort. `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify (`verify: ["run-app"]`, no screenshot-diffing tooling): run `npm run dev`, open `/projects`, and click through both sort fields in both directions, confirming the card order changes as described above.
