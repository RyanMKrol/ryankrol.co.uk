## Do

This is a **visual audit and tweak** task, not a from-scratch build — the vinyl, listening, and
projects pages already exist (built in T156-T158) and pass their own functional Definition of Done.
Your job here is different: compare the LIVE running pages against the design renders pixel-by-pixel
and make free-form CSS/markup tweaks to close the gap.

**Reference material** (all under `/Users/ryankrol/Downloads/design_handoff/`):
- `README.md` — the token/spec doc.
- Vinyl: `renders/2b.png` (desktop), `renders/4c.png` (mobile).
- Listening: `renders/2d.png` (desktop), `renders/4e.png` (mobile).
- Projects: `renders/3e.png` (desktop), `renders/4j.png` (mobile).

**Process:**
1. `npm run dev`, load each page at desktop and mobile viewports, comparing against its paired
   render: vinyl's artist-letter section headers + `CoverTile` grid density (7-col→3-col
   responsive) + covers/list toggle; listening's ranked-row styling and the #1 grape-filled row vs.
   the rest; projects' repo card layout, language-dot colors, topic pill styling.
2. For every gap found, fix it directly — prefer fixing shared components (`CoverTile.js`,
   `Badge.js`) over page-specific overrides when the gap is systemic.

## Done when

- All 3 pages (both breakpoints) visually match their paired renders closely — exact hex colors,
  correct fonts, correct grid density/responsive collapse, correct accent treatment (vinyl pink,
  listening/grape #1 row, projects green + real GitHub language colors).
- `npm run lint`, `npm test`, and `npm run build` all pass after your tweaks.
- No functional regression: vinyl search/covers-list toggle, and real data binding on all 3 pages,
  still work correctly after your CSS/markup changes.
- Document in your worklog which specific gaps you found and fixed.
