## Overview
On the perfumes list page, make each perfume card's designer name a clickable control that fills the page's search bar with that designer's name, filtering the visible list down to all perfumes from that designer. Quality-of-life discovery — browse a designer's whole collection in one click, reusing the existing search filter (no new filtering logic).

## Do
In src/components/perfumeVariants/Variant6Hybrid.js, add an optional onDesignerClick callback prop and render the designer name (currently the plain <p className="perfume-v1-designer">{item.designer}</p> at line 64) as a real, accessible clickable control (e.g. a <button> styled to look inline, keyboard- and screen-reader-usable) that calls onDesignerClick(item.designer) on click; keep it rendering plainly when no designer or no callback is provided. In src/pages/reviews/perfumes/index.js, pass onDesignerClick={setSearchTerm} (or a small wrapper calling setSearchTerm(designer)) into <Variant6Hybrid ... /> (line 145) so a click sets searchTerm to the designer's name — the existing filter useEffect (index.js:50-53, deps line 79, already matching title OR designer case-insensitively) then narrows the list automatically. Put ONLY the designer's name into the search box (do not factor in the 'type' field) unless the pending definition-of-done answer says otherwise. Extend the existing RTL test src/components/perfumeVariants/Variant6Hybrid.test.js to assert clicking the designer invokes onDesignerClick with the designer name, and add a scripts/_visual-harness.mjs FLOWS entry (mirroring the existing perfumes-ownership-* entries around line 334) that clicks a card's designer and captures the resulting filtered list.

## Done when
The designer name on each perfume card is a keyboard- and screen-reader-accessible clickable control (a real button, not a bare onClick on a <p>); clicking it sets the perfumes page search input's value to that exact designer's name and the visible card list immediately narrows to perfumes whose title OR designer contains that name (via the existing filter — no new filtering code); the search input remains editable afterward so the user can clear or change it; a card with no designer or rendered without the callback still renders the designer text without a broken control; the RTL test in Variant6Hybrid.test.js asserts the click calls onDesignerClick(designer); and a new scripts/_visual-harness.mjs FLOWS entry captures the designer-click filtered state.
