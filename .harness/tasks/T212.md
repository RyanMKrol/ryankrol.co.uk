## Do

In `src/styles/globals.css`, remove the `background-color: var(--color-surface)` and `min-height: 100vh` declarations from BOTH `.container` (currently lines ~633-642) and `.review-container` (currently lines ~644-653) — these two classes are near-duplicate rule blocks and BOTH currently paint the entire in-bounds page content as a solid white/`--color-card`-colored rectangle (`--color-surface` resolves to `--color-card`, `#FFFFFF`) instead of letting the body's cream `--color-bg` (`#FBF7EF`) page background show through everywhere, per the design intent in `design/README.md` (bg/paper `#FBF7EF` = page background; card `#FFFFFF` = card surfaces only). Keep every other declaration on both classes unchanged — `max-width: 1400px`, `margin: 0 auto`, and the horizontal/vertical gutter padding (including their shared `@media (max-width: 768px)` override block) all stay exactly as they are; only drop the `background-color` and `min-height` lines from each. Do not touch `body`'s own `background-color: var(--color-bg)` / `min-height: 100vh` (around line 575-578) — that's already correct and is what should now show through. Already confirmed during authoring (no further investigation needed): every card-shaped component rendered inside pages using `.container`/`.review-container` — `.poster-banner-card`, `.spine-cover-card`, `.square-cover-card`, `.workout-session-card`, `.project-card`, and `.now-playing-chip` — already sets its OWN `background-color: var(--color-card)` independently of the container, so none of them rely on the container's white backdrop for contrast; do not add a background to any card component as part of this fix, it is not needed and would be scope creep.

## Done when

1. `npm run lint`, `npm test`, and `npm run build` all pass with no new errors. 2. `npm run dev`, and with the browser at a reasonably tall viewport, visually confirm on ALL of the following pages that the page content now sits directly on the cream/paper `--color-bg` background with NO surrounding white/card-colored rectangle framing the whole page: the home page (`/`), the workouts list (`/workouts`), at least one reviews list page (e.g. `/reviews/movies`), and the vinyl page (`/vinyl`). 3. On each of those pages, confirm individual cards (review cards, workout session cards, stat blocks, etc.) still show their own white card background and remain visually legible/contrasted against the cream page background — i.e. removing the container's background did not make any card look broken or blend into the page. 4. Record which pages were checked and what was observed in the task's worklog.
