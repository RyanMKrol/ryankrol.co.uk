## Overview
PrBadge labels currently read 'weight PR', '1RM PR', 'volume PR', defined identically in both src/pages/workouts/[id].js and src/pages/workouts/index.js. The owner wants the literal 'PR' suffix removed everywhere so the pills read just 'weight', '1RM', 'volume'.

## Do
Update the label strings in the `prBadges` array in src/pages/workouts/[id].js (currently ~lines 258-261: `'weight PR'`, `'1RM PR'`, `'volume PR'`) and the equivalent `prAxes` array inside `getExercisePreview` in src/pages/workouts/index.js (currently ~lines 61-64) to `'weight'`, `'1RM'`, `'volume'`. Additionally, give each PrBadge instance an accessible name carrying the dropped context: add an `aria-label` (or pass a `label`/`ariaLabel` prop through PrBadge if it supports one -- check src/components/Badge.js's PrBadge implementation and extend it if needed) reading '<axis> personal best' (e.g. 'weight personal best', '1RM personal best', 'volume personal best') -- the owner confirmed 'personal best' (not 'PR') is the right screen-reader wording, matching the term already used elsewhere in the codebase's comments/logs (src/lib/workoutQueries.js, src/scripts/backfillPersonalBests.js). Leave the `key` identifiers ('weight'/'1rm'/'volume') and the DynamoDB field names (`isWeightPR`/`is1RMPR`/`isVolumePR`) untouched -- this is a display-text/accessibility change only, not a schema change.

## Done when
On /workouts and /workouts/[id], every rendered PrBadge shows only the axis word ('weight', '1RM', or 'volume') beside the medal emoji -- no pill anywhere contains the literal text 'PR'. Grepping src/pages/workouts/index.js and src/pages/workouts/[id].js for the label strings confirms none of the three PrBadge labels end in 'PR'. Each PrBadge instance carries an aria-label (or equivalent accessible name) reading '<axis> personal best', confirmed via inspecting the rendered DOM's accessibility tree or the component's props/markup.
