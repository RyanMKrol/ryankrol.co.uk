## Do

This is the cheap first step of the card-decoration brainstorm (owner said the Projects page cards look 'a little bit bare' and wants more metadata on them). `GET /api/github/repos` (`src/pages/api/github/repos.js:80-91`) already maps and returns `repo.forks` (from GitHub's `forks_count`) for every repo, but `src/pages/projects/index.js`'s card markup never renders it — only `language` and 'updated Xago' currently appear in the `.project-card-meta-row` (lines ~186-201), even though `repo.stars` is rendered separately up top (`.project-card-stars`, lines ~177-179) using the exact same already-fetched-data pattern forks should follow.

Add a forks count to each project card's `.project-card-meta-row` (alongside the existing conditional `language` and `lastPush`/'updated Xago' spans), e.g.:
```jsx
{typeof repo.forks === 'number' && repo.forks > 0 && (
  <span className="project-card-forks">
    {repo.forks} {repo.forks === 1 ? 'fork' : 'forks'}
  </span>
)}
```
Only render it when `repo.forks > 0` (many personal repos have zero forks — showing '0 forks' on every card would add more visual noise than the 'bare' problem it's meant to fix). No new CSS class is strictly required — `.project-card-meta-row` already sets the shared font-family/size/color for its child spans (see `.project-card-language`/`.project-card-updated` — they get their typography from the parent row, not their own class) — but you may add a trivial `.project-card-forks` rule in `src/styles/globals.css` ONLY if needed for a small icon/spacing touch; if you do, keep it a pure additive rule, do not restyle `.project-card-meta-row` or its siblings. Do not add any new API call — `repo.forks` is already present in every repo object returned by `/api/github/repos`, so this is purely a rendering change in `src/pages/projects/index.js`. Do not touch `src/pages/api/github/repos.js`.

## Done when

Each project card whose repo has `forks > 0` shows a fork count in its meta row (e.g. '3 forks'), positioned alongside the existing language/updated-ago info; cards with zero forks show nothing extra (no '0 forks' clutter). `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify (`verify: ["run-app"]`, no screenshot-diffing tooling in this repo): run `npm run dev`, open `/projects`, and confirm at least one card with a nonzero fork count now displays it in the meta row, and that existing language dot / 'updated Xago' text / star count are unchanged. No new network request should fire (check devtools Network tab still shows exactly one `/api/github/repos` call).
