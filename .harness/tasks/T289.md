## Do

Read `src/lib/workoutMetrics.js` in full first (`calculateEstimated1RM` lines 12-16, `calculateExerciseMetrics` lines 23-89) to match its existing style exactly.

The owner wants personal bests tracked along THREE independent axes: heaviest weight lifted, best estimated 1RM (Epley), and best SINGLE-SET volume (one set's weight × reps — NOT the session-total `workingSetVolume` already computed here, which sums across all working sets; this is a new, distinct per-set max, directly analogous to how `heaviestWeight`/`bestEstimated1RM` are already tracked as running maxes over individual sets in the same loop).

1. **Extend `calculateExerciseMetrics`'s existing set-iteration** (the `exercise.sets.forEach` block, currently lines 35-71, which already tracks `heaviestWeight` and `bestEstimated1RM` as running maxes over WORKING sets only, skipping `type === 'warmup'`): change the callback to `.forEach((set, setIndex) => {...})` (it currently takes no index) and add a third running-max axis, `bestSetVolume`, tracking the largest single set's `volume` (already computed per-set at line 47, just never retained as a session-best today — only summed into `workingSetVolume`). For ALL THREE axes, also track the winning set's INDEX into `exercise.sets`: `heaviestWeightSetIndex`, `bestEstimated1RMSetIndex`, `bestSetVolumeSetIndex`, each initialized `-1`, updated alongside its axis's max ONLY on a strictly-greater comparison (ties keep the FIRST set reached, do not overwrite on `===`).
2. **Add to the return object**: `bestSetVolume` (same null convention as `heaviestWeight`/`bestEstimated1RM` — only non-null when `hasWeightData && bestSetVolume > 0`, else `null`) and the three `*SetIndex` fields (each `-1` when that axis has no value, e.g. bodyweight/cardio-only exercises). Do not remove, rename, or change the behavior of any field already returned.
3. **Add a NEW exported pure function `detectPersonalBests(sessionMetrics, priorBest)`** in the same file. `sessionMetrics` is (a subset of) `calculateExerciseMetrics`'s return value — needs `heaviestWeight`, `bestEstimated1RM`, `bestSetVolume` and their `*SetIndex` fields. `priorBest` is either `null`/`undefined` (this exercise has never been logged before ANYWHERE in history) or an object `{ heaviestWeight, bestEstimated1RM, bestSetVolume }` (any field itself may be `null` if that axis was never established). For EACH axis independently: it counts as a NEW personal best if `sessionMetrics`'s value for that axis is non-null AND (`priorBest` is null/undefined, OR that axis's prior value is null, OR the session's value is STRICTLY greater than the prior value — ties do NOT count as a new PR). Return exactly `{ weightPRSetIndex, oneRepMaxPRSetIndex, volumePRSetIndex }`, each either the matching `*SetIndex` from `sessionMetrics` (when that axis IS a new PR) or `null` (when it is not).
   - **Deliberate design decision (documented here, not escalated — low risk):** the FIRST time an exercise is EVER logged (`priorBest` null/undefined), every axis with a non-null session value DOES count as a new PR (there is no special-cased "no medal on your first time" behavior). Rationale: your very first heavy set of a brand-new exercise genuinely IS your current best on every axis it has data for — treating "never logged before" as "trivially becomes the new baseline" requires no extra branching in either this function or the UI that reads its output (a PR is a PR, uniformly), and mirrors how this codebase already treats `attribute_not_exists`/first-write as "establish the record" elsewhere (`storeWorkoutInDynamoDB`'s `ConditionalCheckFailedException` handling).

**Tests** (`src/lib/workoutMetrics.test.js`): extend the existing `describe('calculateExerciseMetrics', ...)` block with cases asserting `bestSetVolume` + the three `*SetIndex` fields, including a tie case (first set wins) and a bodyweight/cardio-only exercise (all three null/-1). Add a NEW `describe('detectPersonalBests', ...)` block covering: (a) `priorBest` null/undefined → every non-null axis flagged; (b) session beats prior on ALL three axes → each returns the correct set index, not just a boolean; (c) session beats prior on only ONE axis (e.g. new heaviest weight but at lower reps, so both estimated-1RM and set-volume stay below the prior record) → only that axis's index returned, the other two `null`; (d) session ties (not exceeds) prior on an axis → NOT flagged; (e) an axis with a null session value (cardio exercise, no weight data) never flagged regardless of `priorBest`.

## Done when

- `calculateExerciseMetrics` returns `bestSetVolume`, `heaviestWeightSetIndex`, `bestEstimated1RMSetIndex`, `bestSetVolumeSetIndex` in addition to every field it already returns; all existing tests for it still pass unmodified in behavior.
- `detectPersonalBests` is exported, pure (no I/O, no DB access), and implements the rules above exactly, including the documented first-occurrence-counts-as-PR decision.
- New + existing tests in `src/lib/workoutMetrics.test.js` cover every case listed above and pass.
- `npm run lint` reports no errors.
- `npm test` passes.
- `npm run build` succeeds.
