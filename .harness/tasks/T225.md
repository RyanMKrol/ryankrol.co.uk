## Do

Convert `.workout-session-grid` in `src/styles/globals.css` (currently `display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.25rem; margin-bottom: 2rem;` around line 1814, plus its `@media (max-width: 900px) { grid-template-columns: 1fr; }` override around line 1822) from CSS Grid to CSS multi-column, replicating the EXACT pattern already shipped for `.projects-grid`/`.project-card` in commit 796102b (T210 — read that commit's diff in git history for the reference implementation before starting). Replace the grid declaration with `column-count: 2; column-gap: 1.25rem;` (keep `margin-bottom: 2rem;` as-is), and change the `max-width: 900px` override from `grid-template-columns: 1fr` to `column-count: 1;`. On `.workout-session-card` (same file, around line 1827 — note this class is applied to a Next.js `<Link>` rendering an `<a>` element with `display: flex; flex-direction: column;`), add `break-inside: avoid;` and `margin-bottom: 1.25rem;` to replace the row-spacing the old grid `gap` provided, matching `.project-card`'s treatment exactly. Do not touch `src/pages/workouts/index.js` or any other CSS selector — pure CSS change, no JS/markup edits.

Accepted, INTENTIONAL tradeoff — do not treat this as something to additionally solve: CSS multi-column fills column 1 top-to-bottom before column 2, so with the workouts list paginated at `PAGE_SIZE = 10` (`src/pages/workouts/index.js:11`) across 2 columns and uneven card heights (driven by variable exercise-list length per workout), the per-column item-count split will not always be perfectly even. This is the SAME tradeoff already accepted for `.projects-grid` in T210 and is being deliberately carried over rather than solved with a JS-computed column-balancing scheme, per this repo's 'keep it simple' convention. Do NOT attempt a JS/measured masonry solution — out of scope for this task.

## Done when

`npm run lint` reports 0 errors (existing warnings tolerated, no new ones introduced). `npm run build` succeeds. `npm test` still passes in full (no new test required). `run-app` manual check: run `npm run dev`, open `/workouts` with enough workouts to fill at least one full page (10 items), and confirm (a) a workout card with a short exercise list no longer stretches to match the height of a card with a longer exercise list in the same row-position, (b) shorter cards' following siblings visually flow up into the freed space, (c) narrowing the viewport below 900px collapses to a single column with no visual breakage, and (d) each card is still a clickable link that navigates to its workout detail page (`/workouts/[id]`) — confirm `break-inside: avoid` didn't change the card's tag/interactivity. `git diff --stat` shows only `src/styles/globals.css` changed, and within it only the `.workout-session-grid` and `.workout-session-card` rules (plus their existing media-query block) differ from before.
