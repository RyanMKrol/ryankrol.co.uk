## Do

Same pattern as the movies cutover task (`review-edit-url-id-2`) but for TV — TV's routes/pages are structurally identical to movies' (same field shape, same title-based key). Precondition: `review-edit-url-id-3` has produced a human-verified `TelevisionRatingsV4` table (partition key `id`).

Current state: `src/lib/constants.js` has `TV_RATINGS_TABLE: 'TelevisionRatingsV3'`. `src/pages/api/reviews/tv/update.js` does the same `GetCommand`-by-title + conditional-delete + `PutCommand` dance as movies (lines 32-78, keyed `{ title }`/`{ title: originalTitle }`). `src/pages/api/reviews/tv/delete.js` takes `{ title, password }`, keyed `{ title }` (lines 11-29). `src/pages/reviews/tv/edit/[id].js` does `decodeURIComponent(id)` then `tvShows.find(t => t.title === decodedTitle)` (line 38), submits `originalTitle: originalData.title` (line 116) and `title: originalData.title` on delete (line 163) — note the title `<input>` is `disabled` in this edit form too, so the delete-old/recreate-new branch is effectively dead code and should be removed once keyed by `id`. `src/pages/reviews/tv/edit.js` links to `/reviews/tv/edit/${encodeURIComponent(tvShow.title)}` (line 73) and keys the list by `${tvShow.title}-${index}` (line 65).

Mirror the movies cutover (`review-edit-url-id-2`) exactly, substituting TV's table/paths, and reuse the SAME `id`-immutability rule: `id` is a random UUID generated once in `add.js` and never regenerated on update (do NOT mirror perfumes' recompute-id-on-title-change pattern).

Changes: (1) `src/lib/constants.js`: `TV_RATINGS_TABLE: 'TelevisionRatingsV3'` → `'TelevisionRatingsV4'`. (2) `add.js`: generate `id: randomUUID()` on create. (3) `update.js`: accept `originalId`, `GetCommand`/`PutCommand` keyed `{ id: originalId }`, no delete-old branch. (4) `delete.js`: accept `{ id, password }`, `DeleteCommand` keyed `{ id }`. (5) `index.js`: no logic change (table swap is automatic via constants.js). (6) `edit.js`: link + list key use `tvShow.id`. (7) `edit/[id].js`: find by `t.id === decodedId`; `originalId`/`id` in submit payloads instead of `originalTitle`/`title`. (8) `add.test.js`: assert generated UUID `id`. (9) Docs: update `CLAUDE.md`'s Data model row for `TelevisionRatingsV3`, and add a `#### TelevisionRatingsV4 Table` section to `README.md` (there is currently no TV section in README — same pre-existing gap as movies).

## Done when

`npm run lint`, `npm test`, `npm run build` all pass. Add/edit/delete for TV reviews work end-to-end keyed by `id`. `/reviews/tv/edit/<id>` resolves by `id`. `src/lib/constants.js`'s `TV_RATINGS_TABLE` points at `'TelevisionRatingsV4'`. `grep -r TelevisionRatingsV3 src/` returns no matches. `CLAUDE.md`/`README.md` updated. Diff includes a changed/added test file.
