## Overview
The homepage's random review/vinyl mosaic section is currently headed 'The collection wall' (src/pages/index.js:209, inside <section className="home-wall">). The owner felt 'collection' misframed a section that's mostly reviews (movies/tv/books/albums), not a display case of owned objects. The owner confirmed the replacement title, from a set of drafted candidates, is 'A slice of everything', and confirmed this is a text-only change.

## Do
In src/pages/index.js, replace the text content of the `<h2 className="home-section-title">The collection wall</h2>` element (around line 209, inside `<section className="home-wall">`) with 'A slice of everything'. Change ONLY the text between the `<h2>` tags — do not touch the `home-section-title` className, the surrounding `<section className="home-wall">` wrapper, the `home-wall-grid` div, the `CoverTile`/`Link` mapping logic, or any other section on the page (e.g. the 'Latest takes' h2 uses the same `home-section-title` class and must be left untouched).

## Done when
The `<h2>` inside `<section className="home-wall">` in src/pages/index.js renders 'A slice of everything' instead of 'The collection wall', with no other markup, className, or logic in the file changed. npm run lint / npm test / npm run build all pass. Visually confirmed via `node scripts/visual-check.mjs` — the `home` screenshot (covers src/pages/index.js) shows the new heading text rendered correctly above the tile grid.
