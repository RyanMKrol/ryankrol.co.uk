## Do

Set up the DynamoDB table definition for a new, standalone `hot-takes` content type (do NOT run it here — provisioning is a separate needs-human task). This is a genuinely new content type per the owner: short freeform bullet-point 'takes' (NOT an aggregation of existing review text) — each record only needs the take's text and a date, nothing else.

- Add `HOT_TAKES_TABLE: 'HotTakes'` to `DYNAMO_TABLES` in `src/lib/constants.js`.
- Add `src/scripts/createHotTakesTable.js`, mirroring `src/scripts/createPerfumeTable.js` exactly in shape: AWS SDK v3 `DynamoDBClient` (region `us-east-2`, credentials from env via `dotenv.config({ path: '.env.local' })`), a **single String partition key `id`** (no sort key, no GSIs, `PAY_PER_REQUEST` billing), waiting for status ACTIVE and logging the result (handle `ResourceInUseException` as 'already exists' like the perfume script does).
- Add an npm script `"hot-takes:create-table": "node src/scripts/createHotTakesTable.js"` to `package.json`.

`id` is a synthetic `randomUUID()` key (generated at write time in a later task) since a hot take has no natural unique key — it's just freeform text.

## Done when

`DYNAMO_TABLES.HOT_TAKES_TABLE === 'HotTakes'`. `src/scripts/createHotTakesTable.js` exists, uses the AWS SDK v3 client like `createPerfumeTable.js`, and defines the table with a single String partition key `id`. `npm run hot-takes:create-table` is wired in `package.json`. `npm run lint` and `npm run build` pass. (The script is NOT executed in this task — that is the next task.)
