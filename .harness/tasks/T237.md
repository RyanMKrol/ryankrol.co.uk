## Do

This is a pure operational run (no code changes expected beyond the worklog), mirroring how T083 ran `backfillIncompleteWorkoutMetrics.js` against production. Using the AWS credentials already available in this checkout's `.env.local` (root CLAUDE.md's standard `vercel env pull --environment production` convention): (1) Before touching anything, scan the `Workouts` table and, for the specific reported case, query the `Exercises` table's `exercise_name-workout_date-index` GSI for 'Iso-Lateral High Row (Machine)' to record the BEFORE count (expected ~3, per the original investigation) alongside the total missing-row count the new script's dry-run reports overall. (2) Run the script dry-run (`npm run workout:backfill-missing-exercises`, no LIVE=1) and sanity-check its reported missing-row total is plausible (nonzero, given the known gap) — if it reports zero missing rows unexpectedly, STOP and record `failed:blocked` with the discrepancy rather than proceeding. (3) Run it live: `LIVE=1 npm run workout:backfill-missing-exercises`. (4) After: re-query the `exercise_name-workout_date-index` GSI for 'Iso-Lateral High Row (Machine)' and confirm the row count now closely matches the ~28 sessions found via the Workouts table's denormalized exercises[] data in the original investigation (not just the original 3) — this is the direct, objective fix for the reported symptom. Also spot-check a sample of Exercises rows that existed BEFORE the run are byte-identical AFTER (the script's writes are purely additive/conditional, so pre-existing rows should never change) and that a workout with zero missing rows saw no new writes. (5) Because API reads go through `withApiCache` (a per-serverless-instance in-memory cache, 4h default TTL per root CLAUDE.md), also hit `POST /api/dev/cache-bust` (or use the `/dev/cache` dashboard) to flush the exercise/workout caches so the fix is visible on the live site immediately rather than waiting out the TTL. If any check in step 4 fails, STOP — do not attempt to hand-fix data; record `failed:blocked` with the specific mismatch(es) found so a human can investigate before any retry.

## Done when

The live backfill has been run against production (or, if the dry-run finds zero missing rows, the run is skipped as a no-op and that finding is recorded instead). The Exercises table's exercise_name-workout_date-index GSI for 'Iso-Lateral High Row (Machine)' now returns a row count consistent with the ~28 real sessions (not the original 3), with zero pre-existing rows altered. Caches have been busted so the fix is visible on the live exercise-detail page without waiting for TTL expiry. The before/after counts and verification results are recorded in this task's worklog as evidence.
