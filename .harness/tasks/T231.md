## Do

Fix a vertical-alignment bug in the shared review-list header row (present on the movies, TV, books, and albums review list pages — `src/pages/reviews/movies/index.js`, `src/pages/reviews/tv/index.js`, `src/pages/reviews/books/index.js`, `src/pages/reviews/albums/index.js`). Each of these pages renders `SearchInput` and `SortButtons` (`src/components/SortButtons.js`) as flex siblings inside a `.collection-review-controls` div (see e.g. `src/pages/reviews/movies/index.js:148-159`). `.collection-review-controls` (`src/styles/globals.css:427-432`) is `display:flex; align-items:center`, which should vertically center the search box and the sort-buttons row against each other, but `SortButtons`' own wrapper class `.sort-buttons-container` (`src/styles/globals.css:789-795`) carries a stray `margin-top: 1rem` that pushes it down relative to its sibling `.collection-search-input` (`src/styles/globals.css:2585-2593`, which has no such margin) — flex `align-items` centers a child's margin box, so the asymmetric top margin visibly shifts the sort-buttons row lower than the search box.

Confirmed via a full `src/` grep: `SortButtons` is imported and rendered in EXACTLY these four pages (movies, tv, books, albums index pages), and in every case it is rendered as a direct flex-sibling of `SearchInput` inside `.collection-review-controls`. There is no other usage anywhere in the codebase that could be relying on `.sort-buttons-container`'s `margin-top: 1rem` for spacing.

The fix: remove the `margin-top: 1rem;` declaration from the `.sort-buttons-container` rule in `src/styles/globals.css` (currently lines 789-795) entirely — do not just zero it out with an override, delete the declaration since no usage needs it. Leave the rest of the rule (`display: flex; align-items: center; gap: 1rem; flex-wrap: wrap;`) untouched. Do not touch `SortButtons.js` itself or any of the four page files — this is a single CSS-file fix.

## Done when

`.sort-buttons-container` in `src/styles/globals.css` no longer has a `margin-top` declaration (the rule now reads just `display: flex; align-items: center; gap: 1rem; flex-wrap: wrap;`). `npm run lint`, `npm test`, and `npm run build` all pass. Manually verify per this repo's UI convention (no screenshot-diffing tooling exists): run `npm run dev`, open at least two of the four affected pages (e.g. `/reviews/movies` and `/reviews/albums`) in a browser, and confirm the search input box and the 'Sort by:' row now sit at the same vertical position (visually centered against each other in the `.collection-review-controls` row) rather than the sort row appearing lower than the search box. Record the observation in the task's worklog.
