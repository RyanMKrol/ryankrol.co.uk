## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Workout detail" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/3g.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4l.png` (mobile).

Rebuild `src/pages/workouts/[id].js` (and `src/components/WorkoutCard.js` if that's where the
per-exercise set rendering currently lives — check which component owns what before restructuring):
- "← back to workouts" link, title (split name) + split-type `Badge` (same Push/Pull/Legs colors as
  T159), a row of `StatBlock`s (neutral/bordered mode) for date/duration/volume/exercises.
- One card per exercise: exercise name as a card title, then each set as a mono row showing
  "weight × reps" right-aligned. Warmup-labeled sets get the amber treatment
  (`var(--color-warmup-bg)` `#FDF4E3` background, `var(--color-warmup-text)` `#B7791F` text, per
  T142's tokens); regular working sets get the neutral row treatment (`var(--color-set-row-bg)`
  `#F4F0E8`). Reuse whatever logic already distinguishes warmup vs. working sets in the current
  implementation (check `WorkoutCard.js`/the workout data shape before assuming a field name).

## Done when

- The workout detail page shows the back link, title + split badge, stat-block row, and one card per
  exercise with correctly-colored warmup (amber) vs. working (neutral) set rows.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load a real workout detail page (e.g. click through from
  `/workouts`), confirm warmup sets are visually amber and working sets are neutral, and stats match
  the workout's real data. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit
  task T168.
