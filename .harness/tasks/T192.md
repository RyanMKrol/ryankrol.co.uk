## Do

On the `/workouts` list page (`src/pages/workouts/index.js`), each session card in the `.workout-session-grid` currently shows title, split badge, date/time range, duration, exercise count, and volume total, but no indication of WHICH exercises were done. Add a short exercise-name preview line to each card.

Data source (already resolved — do NOT add a new query or change the DynamoDB fetch shape): the `Workouts` table item already stores the FULL exercises array under its `exercises` field, written verbatim from the Hevy API payload in `storeWorkoutInDynamoDB` (`src/lib/workoutBackfill.js`, around line 23: `workoutItem.exercises = workout.exercises`). Each entry in that array has (at minimum) a `.title` (the exercise name, e.g. "Bench Press (Barbell)") and a `.sets` array. `getAllWorkouts()` and `getWorkoutsPaginated()` in `src/lib/workoutQueries.js` (lines 19 and 67) already include `exercises` in their `ProjectionExpression`, and `GET /api/workouts?mode=all` (the endpoint this page's `useEffect` fetches from) returns that field unmodified. Proof it is already present client-side: this page's own existing `getTotalVolume(exercises)` helper (around line 52) already reads `workout.exercises` per card, client-side, to compute the volume stat shown today — so exercise-name data is ALREADY sitting in the `allWorkouts` React state with zero additional DynamoDB reads required. Do NOT query the separate `Exercises` table (`getExercisesByWorkout`, the `workout_id-index` GSI used by `/workouts/[id]` and `/api/workouts/[id]/exercises`) per card — that would be the N+1 pattern the idea correctly flagged as a risk, and it is unnecessary here because the list-level `Workouts` item already embeds exercise names. No change to `src/lib/workoutQueries.js` is needed or in scope for this task.

Implementation:
1. In the card-rendering `.map` in `src/pages/workouts/index.js`, add a preview line (e.g. below the existing `workout-session-datetime` paragraph, above the stats row) that renders the first 2-3 exercise names from `workout.exercises` (each item's `.title` field), joined with a separator consistent with the rest of the card (the existing datetime line uses ' · '), e.g. "Bench Press (Barbell) · Overhead Press · Incline Dumbbell Press", with a trailing "+N more" indicator when the workout has more exercises than are shown. Handle workouts with 0 exercises gracefully — omit the line entirely rather than rendering an empty string or "+undefined more".
2. Add a new CSS class (e.g. `.workout-session-exercise-preview`) in `src/styles/globals.css`, near the existing `/* ===== Workouts list page (T159) ===== */` block (around lines 1913-1984). Style it consistently with `.workout-session-datetime` (mono font, `var(--color-ink-mute)`, ~0.85rem font-size) so it reads as secondary metadata, not competing visually with the title or the stats row. If the preview line risks overflowing the card at typical exercise-name lengths, use `text-overflow: ellipsis` / `overflow: hidden` / `white-space: nowrap` (or `-webkit-line-clamp` for 2 lines) rather than letting it break the card's border.
3. Keep this change purely additive: do not alter `getTotalVolume`, the exercises/volume stats row, pagination (`Pagination` component / `paginate()`), or the All/Push/Pull/Legs filter logic (`filterWorkouts()`).

## Done when

- `npm run lint` and `npm run build` both pass with no new errors.
- `verify: ["run-app"]`: start the dev server (`npm run dev`), open `/workouts` in a browser, and confirm every session card in the 2-column grid now shows a short list of REAL exercise names (not placeholder text, not literal "undefined", not an empty '+ more' with no names) pulled from that session's actual `exercises` data — e.g. a 'Push' card shows something like 'Bench Press (Barbell) · Overhead Press · +2 more' rather than just the exercise count. Confirm this holds across a few different cards/pages, including any workout with very few (1-2) exercises and, if one exists in the seed data, one with 0 exercises (should not render a broken/empty preview line).
- Confirm no layout regression: cards still align cleanly in the 2-column grid (1-column on narrow viewports per the existing `@media (max-width: 900px)` rule), and the new preview line does not overflow or break the card's border at typical exercise-name lengths.
- Confirm no functional regression: the All/Push/Pull/Legs filter and pagination controls still work exactly as before.
- Confirm no performance regression: the page must still make exactly the same single `GET /api/workouts?mode=all` request it does today (open browser devtools Network tab) — if satisfying this task required adding a new per-card network request, that is the WRONG implementation; stop and re-read the `## Do` data-source note instead of shipping an N+1 fetch.
- Per root `CLAUDE.md`'s Definition of Done, check whether the `File map` entry for `src/pages/workouts/index.js` (currently "Paginated workout list with All/Push/Pull/Legs filter") still accurately describes the page; update it in the same commit if not.
