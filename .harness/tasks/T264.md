## Do

1. In `src/pages/reviews/tv/edit.js` (~line 81), change the Edit `<Link>`'s className from `"edit-button-overlay"` to `"edit-button-overlay edit-button-overlay--tv-banner"`.
2. In `src/styles/globals.css`, add a new rule block after whatever the last rule in the edit-button-fix area currently is (do not remove or reorder any existing rule, including any `--movies-banner`/`--spine-cover` blocks added by sibling tasks):
```css
/* Edit button fix for tv/poster-banner: same fix as movies/poster-banner (shares the
   identical styleVariant and bug) but its own class so this task doesn't collide with
   the movies task editing the same CSS rule. */
.edit-button-overlay--tv-banner {
  background-color: rgba(0, 0, 0, 0.55);
  color: #FFFFFF;
  border-color: rgba(255, 255, 255, 0.85);
}

.edit-button-overlay--tv-banner:hover {
  background-color: rgba(0, 0, 0, 0.75);
}
```
3. Do not touch `src/pages/reviews/movies/edit.js` (it has its own equivalent fix as a sibling task). Do not touch `src/pages/reviews/books/edit.js`, `src/pages/reviews/albums/edit.js`, `src/pages/reviews/perfumes/edit.js`, or `src/components/ReviewCard.js`.

## Done when

`npm run dev`, open `/reviews/tv/edit` and scroll through enough cards to see several different banner gradient colors:
- The Edit button's "Edit" text and border are clearly legible against every banner color observed.
- The button's position (top-right of the banner) is unchanged from before.
- Clicking Edit still navigates to `/reviews/tv/edit/<id>` as before.
- `/reviews/movies/edit`, `/reviews/books/edit`, `/reviews/albums/edit`, `/reviews/perfumes/edit` are visually unaffected by this change.
- `npm run lint` and `npm run build` both succeed with no new errors.
