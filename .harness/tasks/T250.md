## Do

In `src/components/PipMeter.js`, the component already tracks a `hoveredPip` state (currently `const [hoveredPip, setHoveredPip] = useState(0);` at line 6) and uses `hoveredPip || value` to decide which pips render `filled` (lines ~40-43). It has a `handlePipHover(pipValue)` function that calls `setHoveredPip(pipValue)` on each pip's `onMouseEnter`, and a `handleMouseLeave()` that calls `setHoveredPip(0)` on the container's `onMouseLeave`. None of this hover state is exposed to the parent — the rating number shown next to the meter lives in the PARENT component, not in `PipMeter` itself, so it never updates on hover today.

Add an optional `onHoverChange` callback prop to `PipMeter` (default to a no-op, e.g. `() => {}`, so existing call sites without it don't break). Call `onHoverChange(pipValue)` inside `handlePipHover`, and call `onHoverChange(0)` inside `handleMouseLeave` (mirroring the existing `setHoveredPip` calls in each). Do NOT add this behavior to the `readOnly` branch at the top of the component — that branch has no hover handlers and is used for static display only (see `src/components/perfumeVariants/Variant6Hybrid.js:47`, which renders `<PipMeter value={item.rating} readOnly />` with no adjacent live label — out of scope, leave untouched).

Then wire it up in the two interactive call sites, which both follow the identical pattern:

1. `src/pages/reviews/perfumes/add.js` — around line 188-191:
```jsx
<label className="collection-form-label" htmlFor="rating">
  Rating (0-10): {formData.rating}
</label>
<PipMeter value={formData.rating} onChange={handleRatingChange} />
```
Add a new piece of local state, e.g. `const [hoveredRating, setHoveredRating] = useState(0);` (import `useState` if not already imported — check the top of the file first), pass `onHoverChange={setHoveredRating}` to the `PipMeter`, and change the label to display the hovered value while hovering, falling back to the committed value otherwise: `Rating (0-10): {hoveredRating || formData.rating}`.

2. `src/pages/reviews/perfumes/edit/[id].js` — the identical pattern exists at line 275-279 (`Rating (0-10): {formData.rating}` / `<PipMeter value={formData.rating} onChange={handleRatingChange} />`). Apply the exact same fix: local `hoveredRating` state, `onHoverChange={setHoveredRating}` on the `PipMeter`, label reads `{hoveredRating || formData.rating}`.

Scope note — do NOT touch `LongevitySlider` / `ProjectionSlider` in `src/pages/reviews/perfumes/add.js` (the components defined near the top of that file, also used from `edit/[id].js`): those are plain native `<input type="range">` elements, not `PipMeter`, and their labels (`Longevity: {value}` / `Projection: {PROJECTION_LABELS[value]}`) already read `value` directly, which updates continuously and live as the user drags the slider — there is no separate hover-vs-committed state gap there, so no fix is needed or in scope for this task.

## Done when

Run `npm run dev`, open `/reviews/perfumes/add`. Hover the mouse over different pips in the rating meter (without clicking) and confirm the 'Rating (0-10): N' label updates live to match whichever pip is currently hovered. Move the mouse off the pip meter entirely and confirm the label reverts to showing the actually-selected (committed) rating, not 0 or the last-hovered value. Click a pip to commit a rating, then hover a different pip and confirm the label follows the hover again, and again reverts correctly on mouse-leave. Repeat the same hover/mouse-leave check on `/reviews/perfumes/edit/[id]` for an existing perfume (edit form). `npm run lint` and `npm run build` should both still pass.
