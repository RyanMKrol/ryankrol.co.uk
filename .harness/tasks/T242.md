## Do

Fix the masonry sort-order reading-order bug on /reviews/tv by adopting the shared `MasonryColumns` component + `useResponsiveColumnCount` hook, exactly as already done for `src/pages/reviews/movies/index.js` in a separate, already-completed task — use that file as your template.

Background: `.poster-banner-grid` is shared by movies and tv (both use `styleVariant="poster-banner"`). The Movies sort-order task already migrated the CSS (`.poster-banner-grid` / `.poster-banner-grid-col` in src/styles/globals.css, `.poster-banner-card` margin-bottom removal) and converted movies/index.js to use `<MasonryColumns>` — read that file's current state before starting. `src/pages/reviews/tv/index.js` (around lines 162-172) still renders `pagedTvShows` into a plain `<div className="poster-banner-grid">`, which — now that the CSS is flex-based rather than column-count-based — needs the SAME JS wiring movies/index.js has, or its layout will simply break (a flex container with no MasonryColumns wrapping just stacks everything in one unstyled row/column depending on the exact CSS, not a masonry grid).

Do:
1. In `src/pages/reviews/tv/index.js`, import `MasonryColumns` from `../../../components/MasonryColumns` and `useResponsiveColumnCount` from `../../../hooks/useResponsiveColumnCount`. Replace the `<div className="poster-banner-grid">{pagedTvShows.map(...)}</div>` block (around lines 162-172) with `<MasonryColumns items={pagedTvShows} columnCount={useResponsiveColumnCount(3, 900)} className="poster-banner-grid" columnClassName="poster-banner-grid-col" renderItem={(tvShow, index) => (<ReviewCard key={`${tvShow.title}-${index}`} item={tvShow} type="tv" styleVariant="poster-banner" gradient={tvGradients[index]} />)} />`. Keep the existing `tvGradients` computation (already indexed against `pagedTvShows`) working as-is.
2. Do not touch `src/styles/globals.css` unless you find it wasn't actually migrated yet (in which case, do the same CSS edit described in the Movies task first).

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/tv, sort by score — item #1 top-left, item #2 to its right, item #3 further right (left-to-right-then-top-to-bottom reading order), cards packing tightly with no dead gaps. Resize below 900px and confirm single-column collapse. Also spot-check /reviews/movies still renders correctly (shared CSS).
