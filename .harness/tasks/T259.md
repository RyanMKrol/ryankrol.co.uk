## Do

Add a new page `src/pages/reviews/tv/backfill.js`, the TV counterpart of the movies bulk-backfill page (a sibling task builds `src/pages/reviews/movies/backfill.js` the same way — mirror its structure exactly, just swapping the TV-specific endpoints/fields below; if that sibling page already exists on `main` when you build this, read it first and follow its exact pattern rather than re-deriving it from scratch).

1. On mount, `fetch('/api/reviews/tv')`, filter with `needsTvBackfill` from `src/lib/backfillEligibility.js` (only TV shows currently missing `tmdbId`).
2. Single page-level password field at the top (same rationale as the movies page — entered once, reused by every row's confirm).
3. Render `<BulkBackfillList>` with the filtered shows as `items`, `pageSize=15`, and:
   - `onSearch`: `GET /api/tmdb/search?query=<title>&type=tv` (mirror `handleBackfillSearch` in `src/pages/reviews/tv/edit/[id].js:81-90`), attaching `error.retryAfterSeconds` on a 429 exactly like the movies page.
   - `renderCandidate`/`getCandidateKey`: reuse the render JSX already written in `src/pages/reviews/tv/edit/[id].js:233-259` (poster/title/year/overview snippet) — copy it into this page, same reasoning as the movies task (small, type-specific, not worth abstracting).
   - `onConfirm(show, candidate)`: POST to `/api/reviews/tv/update` with `title: show.title`, `rating: show.rating`, `gist: show.review_text`, `originalId: show.id`, `password`, plus `tmdbId`, `mediaType`, `posterPath`, `tmdbOverview: candidate.overview`, `tmdbDate: candidate.date` — the update endpoint requires the full field set every time (see `src/pages/api/reviews/tv/update.js:11-24`), not just the new metadata.
4. Add a link/button from `src/pages/reviews/tv/edit.js` to `/reviews/tv/backfill`.
5. Empty-state message when there's nothing to backfill.

## Done when

Same verification shape as the movies bulk-backfill task, applied to TV: link from `/reviews/tv/edit` works; `/reviews/tv/backfill` lists only shows missing `tmdbId`, paginated at 15/page; password entered once at the top; at least one row shows a multi-candidate picker (not single-auto-confirm), confirming a candidate updates that show's `tmdbId` etc via `/api/reviews/tv/update` while leaving `title`/`rating`/`review_text`/`date` unchanged; Network tab shows sequential, spaced-out `/api/tmdb/search` requests, not a simultaneous burst. `npm run lint` and `npm run build` both succeed.
