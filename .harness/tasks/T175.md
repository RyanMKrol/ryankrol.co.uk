## Do

Confirmed by reading `.spine-cover-card` in globals.css and `SpineCoverCard` in `src/components/ReviewCard.js`. `.spine-cover-card` (globals.css lines 254-259, under the `/* ===== Spine Cover Review Card (T152) ===== */` comment) is currently: `display: flex; gap: 1rem; padding: 1rem 0; border-bottom: var(--border-width) solid var(--color-ink);` — ONLY a bottom divider line; it has no full border, no `background-color`, and no `border-radius` (the `--radius-card` token IS used, but only on the small cover-art tile — `.spine-cover-tile`, lines 261-268 — not on the card itself). This is rendered exclusively by the `SpineCoverCard` function in `src/components/ReviewCard.js` (lines 44-71, the `styleVariant === 'spine-cover'` branch), used only by `src/pages/reviews/books/index.js` (lines 140-149, `<div className="spine-cover-list">` wrapping `<ReviewCard ... styleVariant="spine-cover" />`). `.spine-cover-list` itself (globals.css lines 306-309) is `display: flex; flex-direction: column;` with NO `gap` — items rely entirely on each card's own `border-bottom` for visual separation, which is exactly the owner-described 'plain row separated by a thin horizontal divider' symptom. Confirmed this is a genuine styling gap independent of the container-width bug: container-audit-1's `.review-container` max-width fix does not touch `.spine-cover-card`/`.spine-cover-list` at all — widening the page has no effect on whether each item looks like a card. Design spec citations from `/Users/ryankrol/Downloads/design_handoff/README.md`: line 98-99 ("Books = horizontal cards with spine cover + author/pages/year metadata" — explicitly calls these 'cards', not divider rows), line 79 ("Borders: `2px solid #1A1714` on white cards, pills, inputs"), line 77 ("Radius: cards 18-20px..."). FIX (globals.css only): rewrite `.spine-cover-card` (lines 254-259) from a bottom-divider row into a real card: `background-color: var(--color-card); border: var(--border-width) solid var(--color-ink); border-radius: var(--radius-card); padding: 1rem;` — remove the existing `border-bottom` divider rule entirely (the new full border replaces it as the visual boundary). Add `gap: 1rem;` (matching the spacing already used by `.projects-grid`/`.workout-session-grid`) to `.spine-cover-list` (lines 306-309) so cards have visible space between them now that they no longer share a divider line. Leave `.spine-cover-tile` (lines 261-268), `.spine-cover-body/-heading/-title/-meta/-snippet` (lines 270-304) unchanged — their border/radius/shadow and typography already match the design. Do not change `src/components/ReviewCard.js` or `src/pages/reviews/books/index.js` — the className hooks (`spine-cover-card`, `spine-cover-list`) already exist and are correctly applied; this is a pure CSS fix.

## Done when

verify: ["run-app"]. Run `npm run dev` and load `/reviews/books` at a desktop viewport (~1400px, ideally after container-audit-1 has landed) and a mobile viewport (~390px): visually confirm each book review renders as its own distinct white card with a visible 2px dark border and ~18px rounded corners (not a plain row separated by a thin divider line), and that there is visible gap spacing between successive cards (they no longer touch or share a border). Confirm the cover-art spine tile, title, star rating, meta line (author/pages/year), and review snippet all still render inside the card exactly as before, unchanged in content/position. Run `npm run lint` (0 errors), `npm test` (all green), and `npm run build` (succeeds).
