## Do

Read `src/lib/workoutBackfill.js` in full (`storeWorkoutInDynamoDB` lines 15-80, `storeExercises` lines 85-129) and `src/lib/workoutQueries.js`'s `getExerciseHistory` (uses the `exercise_name-workout_date-index` GSI) first.

**Why this task exists and where it hooks in (read this before writing code):** the owner explicitly does not want PR tracking to be an easily-forgotten bolt-on — they're worried about a repeat of the exact kind of data-integrity gap already found and fixed in T237 (legacy-shaped Exercises rows silently invisible to this same GSI) if the database is ever stripped and rebuilt. Critically, `storeExercises(workout)` is not just called by the live Hevy backfill path (`storeWorkoutInDynamoDB` → `storeExercises`) — it is ALSO the exact shared function already reused, unmodified, by TWO existing rebuild/migration scripts: `src/scripts/migrateLegacyExerciseRows.js` (deletes + regenerates legacy-shaped rows) and `src/scripts/backfillMissingExerciseRows.js` (creates missing rows from a workout's own embedded data) — both call `storeExercises` rather than hand-rolling their own `PutCommand`s. THIS is why `storeExercises` (not a separate optional step, and not something bolted only onto the live backfill call site) is the correct choke point: any future rebuild script that follows the already-established pattern of calling the shared `storeExercises` automatically inherits PR tracking for free, with zero additional code — exactly the "you'd have to go out of your way not to track personal bests" property the owner asked for.

1. **Add `getBestPriorMetrics(exerciseName, beforeDate)` to `src/lib/workoutQueries.js`**, alongside `getExerciseHistory`: Query the SAME `exercise_name-workout_date-index` GSI with `KeyConditionExpression: 'exercise_name = :n AND workout_date < :d'` (paginate exactly like `getExerciseHistory` already does), and reduce the returned items to `{ heaviestWeight, bestEstimated1RM, bestSetVolume }`, each the MAX of that field across all returned rows THAT HAVE IT (a row from before this task shipped won't have `bestSetVolume` at all — such rows simply don't contribute to that axis's max; do not treat a missing field as zero). Return `null` if there are no prior rows at all for this exercise name (so `detectPersonalBests` correctly treats it as "never logged before"). This is a bounded query — scoped to one exercise's history strictly before one date — run ONLY at write time when a workout is being stored, never from a page-load/read path (this is the scale-sensitive property the owner's idea explicitly flagged; verify neither `/workouts` nor `/workouts/[id]`'s API routes gained any new query as a side effect of this task).
2. **In `storeExercises(workout)`**: for each exercise, compute metrics via `calculateExerciseMetrics` as today (now yielding the new `*SetIndex` fields from the dependency task), call `getBestPriorMetrics(exercise.title, workoutDate)`, then `detectPersonalBests(exerciseMetrics, priorBest)`. For each of the three returned `*PRSetIndex` values that is non-null, set the corresponding boolean directly on `exercise.sets[thatIndex]` — `isWeightPR`, `is1RMPR`, `isVolumePR` (a single set can carry more than one `true` flag at once, e.g. simultaneously the heaviest weight AND the best 1RM). Do this BEFORE constructing `exerciseItem` so the mutated `exercise.sets` is what actually gets persisted into the `Exercises` table row.
3. **Critical ordering fix in `storeWorkoutInDynamoDB`**: today it builds `workoutItem` (embedding `exercises: workout.exercises` verbatim, unflagged) BEFORE calling `storeExercises`. Since the SAME `exercise.sets` array objects need the PR-flag mutation to land in BOTH the embedded `Workouts.exercises[]` copy (read by the `/workouts` list page) and the `Exercises` table row (read by the `/workouts/[id]` detail page) from ONE computation, reorder so `storeExercises` (or the per-exercise metrics+PR computation it does) runs and mutates `workout.exercises` in place BEFORE `workoutItem` is constructed and written — confirm this reordering is safe against the existing `ConditionalCheckFailedException`/healing logic (the two writes target different tables and can happen in either order, as long as the exercises array is mutated before EITHER write reads from it).
4. **No changes needed** to `migrateLegacyExerciseRows.js`/`backfillMissingExerciseRows.js` — read both to confirm neither reconstructs an Exercises-table item by hand outside of calling `storeExercises`; they will automatically gain PR-flag behavior once this task ships. Do not touch them (out of scope).
5. **Update docs in the same commit** (this repo's Definition of Done requires it): `CLAUDE.md`'s `Exercises` table row — document `sets[].isWeightPR` / `is1RMPR` / `isVolumePR` (booleans, present only on the specific working set(s) that set a new all-time personal best for that axis in that session) and add `getBestPriorMetrics` to the `workoutQueries.js` file-map entry. Mirror in `README.md`'s Exercises schema section. Also add a short, prominent note (Conventions or Gotchas section) stating: "PR detection lives inside the shared `storeExercises()`/`storeWorkoutInDynamoDB()` write path — any new script that (re)builds Exercises-table rows MUST go through `storeExercises`, never hand-roll a `PutCommand`, or personal-best tracking silently breaks for that path." This is the explicit architectural guarantee the owner asked for; write it down so a future contributor (or agent) doesn't regress it with a new one-off script that bypasses the shared function.

## Done when

- `getBestPriorMetrics(exerciseName, beforeDate)` exists in `workoutQueries.js`, covered by a test against a mocked docClient (multiple rows, some missing `bestSetVolume`, confirms correct max-reduction and the `null`-when-nothing-prior case).
- `storeExercises`/`storeWorkoutInDynamoDB` compute and persist `isWeightPR`/`is1RMPR`/`isVolumePR` onto the correct specific set object(s) in BOTH the `Exercises` table row's `sets[]` AND the embedded `Workouts` item's `exercises[].sets[]` — verified by a `workoutBackfill.test.js` test that stores two sequential synthetic workouts for the same exercise (mocked docClient) and asserts only the second workout's PR-breaking set(s) are flagged, correctly per-axis.
- Confirmed by inspection that `migrateLegacyExerciseRows.js`/`backfillMissingExerciseRows.js` need zero code changes since they already route through `storeExercises`.
- CLAUDE.md + README.md updated with the new fields, the new query helper, and the "always go through storeExercises" architectural rule.
- `npm run lint` / `npm test` / `npm run build` all green.
