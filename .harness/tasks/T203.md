## Do

Fix `.collection-nav-pills`' right-edge fade mask (`src/styles/globals.css:848-859`) so it only shows when the nav pill row actually has more content to reveal off-screen to the right. Currently the mask-image is unconditional, so it fades the rightmost pill even when nothing overflows, and stays faded even after scrolling all the way to the end.

1. **Extract the pure decision logic** into a new file `src/lib/scrollFade.js`, mirroring the existing `src/lib/pagination.js` pattern (pure math shared/testable, no DOM access inside the function itself). Export a function, e.g. `shouldShowScrollFade({ scrollWidth, clientWidth, scrollLeft, epsilon = 1 })`, returning `true` only when BOTH: (a) there is overflow at all — `scrollWidth > clientWidth` — AND (b) the container is not yet scrolled to its maximum — `scrollLeft + clientWidth < scrollWidth - epsilon` (the small `epsilon` absorbs sub-pixel/rounding jitter from fractional scroll positions across browsers). Return `false` otherwise (no overflow, or already at the scrolled-to-the-end position).

2. **Wire it up in `src/components/Header.js`**: add a `useRef` for the `<nav className="collection-nav-pills">` element (attach it via `ref={navRef}`) and a `useState` boolean (e.g. `showNavFade`, default `false`). Add a `useEffect` that: defines an `updateFade()` closure reading `navRef.current.scrollWidth/clientWidth/scrollLeft`, calls `shouldShowScrollFade(...)` from `src/lib/scrollFade.js`, and sets the state; calls `updateFade()` once immediately (covers the no-overflow desktop case on mount); attaches `updateFade` as a `scroll` listener on the nav element AND a `resize` listener on `window` (viewport narrowing/widening changes whether it overflows at all — this is exactly the reported bug's case 1 vs case 2); also wire a `ResizeObserver` on the nav element itself if straightforward (covers content-size changes independent of window resize, e.g. font/webfont swap changing pill widths) — skip it only if it meaningfully complicates the effect, the window-resize + scroll listeners alone already cover the two reported bugs. Clean up all listeners/observers on unmount. Conditionally add a modifier class to the nav's `className`, e.g. `` `collection-nav-pills${showNavFade ? ' has-scroll-fade' : ''}` ``, instead of applying the mask unconditionally.

3. **Restructure the CSS** in `src/styles/globals.css`: remove the two `mask-image`/`-webkit-mask-image` lines (currently 857-858) from the base `.collection-nav-pills` rule (848-859) entirely — the base rule keeps everything else (flex layout, overflow-x, hidden scrollbar). Add a new rule `.collection-nav-pills.has-scroll-fade { mask-image: linear-gradient(to right, black calc(100% - 2rem), transparent 100%); -webkit-mask-image: linear-gradient(to right, black calc(100% - 2rem), transparent 100%); }` directly after the base rule, using the exact same gradient values (only the conditionality changes, not the visual fade itself when it IS shown). Do not touch the `@media` block further down that sets `.collection-nav-pills { display: none; }` for the mobile menu breakpoint — that's unrelated (mobile uses the hamburger menu instead).

4. **Add a unit test** `src/lib/scrollFade.test.js` for `shouldShowScrollFade`, covering at minimum: no overflow (`scrollWidth === clientWidth`) → `false`; overflow at the very start (`scrollLeft: 0`) → `true`; overflow, mid-scroll → `true`; overflow, scrolled exactly to the max (`scrollLeft + clientWidth === scrollWidth`) → `false`; overflow, scrolled to within `epsilon` of the max (sub-pixel jitter case) → `false`.

5. **Update `CLAUDE.md`** (Definition of Done — docs are part of the change): add a one-line entry for the new `src/lib/scrollFade.js` in the file map's `src/lib/*` table (next to `src/lib/pagination.js`'s entry, same style: "pure function — brief purpose").

## Done when

`npm run lint` reports no new errors, `npm test` passes including the new `src/lib/scrollFade.test.js` covering the cases listed above, and `npm run build` succeeds. Manual UI verification (this is a `layer:"ui"` task — automated tests can't observe real scroll/resize/mask rendering): run `npm run dev`, open the site, and check the header nav pill row (`.collection-nav-pills`) in three window-size scenarios: (a) wide desktop viewport where every pill + the now-playing chip fits with room to spare — confirm the rightmost pill ("projects") renders fully opaque, NOT faded (no `has-scroll-fade` class present, no visible gradient cutoff); (b) narrow the window until the nav genuinely overflows and becomes scrollable — at the initial (unscrolled) position, confirm the right-edge fade IS visible; (c) with the same narrow window, scroll the nav all the way to its right end — confirm the fade DISAPPEARS once fully scrolled (the rightmost pill, e.g. "projects", renders fully opaque with nothing further to reveal), and reappears correctly if you scroll back left. Also spot-check that resizing the window live (without a reload) from the overflow case back to the fits-with-room-to-spare case correctly removes the fade (confirms the `resize` listener works, not just the initial mount check).
