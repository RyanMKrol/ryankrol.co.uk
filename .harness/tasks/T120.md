## Do

`src/pages/api/reviews/movies/update.js` is the POST handler that edits an existing movie review in the `MovieRatingsV3` DynamoDB table (partition key `title`). Today, lines 32-44 fetch the existing item via `GetCommand` specifically to preserve the original `date` field across edits (`const originalDate = existingReview.Item?.date;` then `const preservedDate = originalDate || ...`), and lines 61-71 build `reviewData` with `date: preservedDate` — so an edit currently leaves `date` completely untouched, and there is no record anywhere of when an edit happened.

Add a new field, `editedDate`, to track when the review was last edited, using the exact same `DD-MM-YYYY` string format/convention as the existing `date` field (see `add.js`'s `const dateString = now.toLocaleDateString('en-GB').replace(/\//g, '-');` for the canonical formatting one-liner). Specifically:

1. Keep the existing `originalDate`/`preservedDate` logic at lines 32-44 completely unchanged — `date` must still be preserved across edits exactly as today.
2. Compute a fresh `editedDate` value using `new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` (today's date, formatted the same way as `date`) — this represents 'now', i.e. the moment this edit is being saved.
3. Add `editedDate` to the `reviewData` object built at lines 61-71 (alongside `date: preservedDate`), so every successful edit writes both the preserved original `date` AND the new `editedDate` to DynamoDB. `editedDate` should always be set on this path — it does not need an `undefined`-guard spread like the optional TMDB fields, since every POST to this route represents an edit action.
4. Do not change any other behavior — the title-changed delete/recreate logic (lines 46-58), the cache clear (`clearApiCache('api-movies')`, line 81), password gating, and validation are all unchanged.

Also update the repo's docs in the same change per `CLAUDE.md`'s Definition of Done ("Documentation is part of every change"): in the root `CLAUDE.md`'s Data model table, the `MovieRatingsV3` row currently reads `{ title, rating(0–5), review_text, date 'DD-MM-YYYY' }` — extend it to mention the new optional `editedDate 'DD-MM-YYYY', set on every edit` field. Also check `README.md`'s schema section for the equivalent `MovieRatingsV3` row/description and update it the same way if it duplicates this shape.

Do not touch `src/pages/api/reviews/tv/update.js`, `books/update.js`, or `albums/update.js` — those are separate, independent tasks (each review type's table is edited by its own task). Do not touch `src/components/ReviewCard.js` or any `index.js` sort logic — those are separate tasks too.

## Done when

- `src/pages/api/reviews/movies/update.js` writes both `date` (preserved from the original record, unchanged behavior) and a new `editedDate` (today's date, `DD-MM-YYYY` format, computed fresh on every call) into `reviewData` on every successful update.
- A manual/curl POST to `/api/reviews/movies/update` with a valid `password`, `title`, `rating`, `gist`, and `originalTitle` for an existing movie results in the DynamoDB item having its original `date` unchanged and a new `editedDate` equal to today's date in `DD-MM-YYYY` format.
- Calling update again on the same item on a later date updates `editedDate` to the new date while `date` still stays at its original value.
- `CLAUDE.md`'s Data model table row for `MovieRatingsV3` documents the new `editedDate` field, and `README.md`'s schema section is updated to match if it independently describes this table's shape.
- `npm run lint` passes with no new errors.
- `npm test` passes.
- `npm run build` succeeds.
