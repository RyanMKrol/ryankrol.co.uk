## Overview
src/pages/index.js defines a DUMMY_TOM_TEXT constant (currently lines 15-21) — placeholder content used by every Top-of-Mind placement-comparison variant (?tom=1..5) to compare PLACEMENT only, per the T349 comment directly above it (lines 13-14, which stays as-is). It is rendered via `<Markdown>{DUMMY_TOM_TEXT}</Markdown>` at 5 locations in the file (currently lines 184, 194, 229, 242, 365). The owner confirmed: replace this placeholder's content with plain Lorem Ipsum prose, verbatim, dropping the existing heading and bullet list entirely — no heading, no bullets.

## Do
In src/pages/index.js, replace the entire body of the `DUMMY_TOM_TEXT` template literal (currently lines 15-21, currently a '## Top of mind this week' heading + intro sentence + 3 bullet points) with this exact text, verbatim, as a single plain-prose string (no markdown heading, no bullet list):

"Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum"

Keep the `DUMMY_TOM_TEXT` constant name, the T349 comment above it (lines 13-14) unchanged, and all 5 `<Markdown>{DUMMY_TOM_TEXT}</Markdown>` render call sites (the ?tom=1..5 placement variants) untouched — only the string literal's content changes.

## Done when
The `DUMMY_TOM_TEXT` constant in src/pages/index.js holds exactly the owner-supplied Lorem Ipsum sentence (no heading, no bullets) and every one of its 5 existing render sites still compiles and renders it unchanged structurally. npm run lint / npm test / npm run build all pass. Visually confirmed via `node scripts/visual-check.mjs` — the ?tom=1..5 homepage variant screenshots show the Lorem Ipsum prose rendering in place of the old placeholder text.
