## Do

Precondition: `review-edit-url-id-5` has produced a human-verified `BookRatingsV4` table (partition key `id`, replacing the old `title`+`author` composite key).

Current state — books' edit-URL scheme is the WORST offender of the fragility the owner is complaining about: `src/pages/reviews/books/edit/[id].js:35-39` does `decodeURIComponent(id)` then `const [title, author] = decodedId.split('|')` (a hand-rolled `'|'`-delimited composite id!) then `books.find(b => b.title === title && b.author === author)` — this breaks if any title or author literally contains a `|` character. `src/pages/reviews/books/edit.js:73` builds that fragile id with `encodeURIComponent(book.title + '|' + book.author)`. `src/pages/api/reviews/books/update.js` does `GetCommand`/conditional-`DeleteCommand`/`PutCommand` keyed `{ title: originalTitle, author: originalAuthor }` (lines 32-62), and accepts `originalTitle`+`originalAuthor` (line 12) — note the title/author `<input>`s are `disabled` in the edit form (`src/pages/reviews/books/edit/[id].js` lines 226,240), so the delete-old/recreate-new branch is effectively dead code. `src/pages/api/reviews/books/delete.js` takes `{ title, author, password }`, keyed `{ title, author }` (lines 11-29).

Changes: (1) `src/lib/constants.js`: `BOOK_RATINGS_TABLE: 'BookRatingsV3'` → `'BookRatingsV4'`. (2) `add.js`: generate `id: randomUUID()` on create (alongside existing `title`/`author`/etc. fields, which remain as ordinary attributes). (3) `update.js`: accept `originalId` (replacing `originalTitle`+`originalAuthor`); `GetCommand`/`PutCommand` keyed `{ id: originalId }`; remove the delete-old branch entirely; `id` is never regenerated on save (random UUID assigned once in `add.js`, immutable thereafter — do not mirror perfumes' recompute-on-edit pattern in `src/pages/api/reviews/perfumes/update.js:76-87`). (4) `delete.js`: accept `{ id, password }`, `DeleteCommand` keyed `{ id }`. (5) `index.js`: no logic change (table swap automatic via constants.js). (6) `edit.js`: replace the fragile `book.title + '|' + book.author` id-building (line 73) with plain `book.id`; update the list `key` at line 65 to `book.id`. (7) `edit/[id].js`: replace the `decodedId.split('|')` + dual-field `.find()` (lines 35-39) with a single `decodeURIComponent(id)` compared against `b.id`; replace `originalTitle`/`originalAuthor` in the submit payload (lines 118-119) with `originalId: originalData.id`; replace `title`/`author` in the delete payload (lines 166-167) with `id: originalData.id`. (8) Update `src/pages/api/reviews/books/add.test.js` and `update.test.js` to reflect id-based add/update (assert a generated UUID `id`; update any `Key`/`originalTitle`/`originalAuthor` assertions to `id`/`originalId`). (9) Docs: update `CLAUDE.md`'s Data model row for `BookRatingsV3` (now `BookRatingsV4`, partition key `id`, `title`+`author` as plain attributes), and rewrite the `#### BookRatingsV3 Table` section in `README.md` (`README.md:170-198`) to `#### BookRatingsV4 Table` reflecting the new single `id` key and the `title`/`author` fields moving into the schema body as plain attributes.

## Done when

`npm run lint`, `npm test`, `npm run build` all pass. Add/edit/delete for book reviews work end-to-end keyed by `id` — no more `'|'`-delimited composite ids anywhere in the diff. `/reviews/books/edit/<id>` resolves by `id`. `src/lib/constants.js`'s `BOOK_RATINGS_TABLE` points at `'BookRatingsV4'`. `grep -r BookRatingsV3 src/` returns no matches. `CLAUDE.md`/`README.md` updated. Diff includes changed test file(s).
