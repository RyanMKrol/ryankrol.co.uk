## Do

Fix the masonry sort-order reading-order bug on /workouts by adopting the shared `MasonryColumns` component + `distributeRoundRobin` helper + `useResponsiveColumnCount` hook built in a separate, already-completed task (`src/lib/masonryColumns.js`, `src/hooks/useResponsiveColumnCount.js`, `src/components/MasonryColumns.js` — already exist; read them, do not rebuild). That task also converted `src/pages/projects/index.js` to this pattern — use it as a reference example.

Background: `.workout-session-grid` (src/styles/globals.css around lines 1821-1830) uses `column-count: 2` (`column-count: 1` under `max-width: 900px`) for the workouts list at src/pages/workouts/index.js (around lines 163-217), which renders `pageWorkouts` (filtered by the All/Push/Pull/Legs `PillGroup`, see `filterWorkouts`/`activeFilter`) into `<div className="workout-session-grid">`, each item rendered as a `<Link className="workout-session-card">` (NOT a plain div — note this when writing `renderItem`). This page doesn't have a field-based sort control today (only the split filter), but shares the exact same column-count root cause the owner flagged as broken "anywhere we've made this fix" — worth fixing proactively for consistency and in case sort is added later.

Do:
1. In `src/styles/globals.css`, replace `.workout-session-grid` (currently `column-count: 2; column-gap: 1.25rem; margin-bottom: 2rem;` plus its `@media (max-width: 900px)` override) with a flex-row outer class (`display: flex; gap: 1.25rem; margin-bottom: 2rem; align-items: flex-start;`) and a new `.workout-session-grid-col` class (`display: flex; flex-direction: column; gap: 1.25rem; flex: 1 1 0; min-width: 0;`). Remove the `column-count: 1` mobile override (JS now drives column count via `useResponsiveColumnCount(2, 900)`). `.workout-session-card` currently has `break-inside: avoid; margin-bottom: 1.25rem;` — remove `margin-bottom` (redundant with the column's flex `gap`); keep `break-inside: avoid`.
2. In `src/pages/workouts/index.js`, import `MasonryColumns` from `../../components/MasonryColumns` and `useResponsiveColumnCount` from `../../hooks/useResponsiveColumnCount`. Replace the `<div className="workout-session-grid">{pageWorkouts.map((workout, index) => { ... return <Link ...> ... })}</div>` block with `<MasonryColumns items={pageWorkouts} columnCount={useResponsiveColumnCount(2, 900)} className="workout-session-grid" columnClassName="workout-session-grid-col" renderItem={(workout, index) => { /* exact same body as today's map callback, returning the same <Link key={workout.id || index} href=... className="workout-session-card"> ... </Link> */ }} />` — keep all existing per-card logic (`getExercisePreview`, `getTotalVolume`, split badge, etc.) unchanged, only change the wrapping mechanism.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /workouts — cards still pack tightly at their own variable heights with no dead gaps in either column, in original (date-descending) order reading left-to-right-then-top-to-bottom. Toggle the Push/Pull/Legs filter and confirm filtering still works and re-flows correctly. Resize below 900px and confirm single-column collapse.
