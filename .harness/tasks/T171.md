## Do

This is the deploy checkpoint for the whole Collection redesign (T141-T170). Vercel's Git
integration is disconnected (see root `CLAUDE.md`'s "Deploying" section) — none of the work built
across T141-T170 has gone live yet, no matter how many commits landed on `main`.

1. Confirm `main` is actually in the state you expect: `git log --oneline -20` and confirm T170's
   docs-sync commit (or whatever the last redesign commit turns out to be) is present, and
   `npm run build` succeeds locally against a fresh `git pull`.
2. From a session with the Vercel CLI authenticated (`vercel whoami` should return an account, not
   an auth error), run:
   ```sh
   vercel --prod
   ```
   This deploys the current local working tree directly — it does not depend on Git integration or
   the `[deploy]` commit-message marker (that marker only matters if Git integration ever gets
   reconnected; it's irrelevant to a direct CLI deploy).
3. Wait for the deployment to finish and report `READY`, not `ERROR`.
4. Visit the production URL and spot-check: home page, one reviews list page, the vinyl page, and a
   workout page, at both desktop and mobile viewport widths. Confirm they render the Collection
   design (Bricolage Grotesque/Nunito/Space Mono fonts, warm paper background, the section accent
   colors) rather than the old cyberpunk default or a broken unstyled page. Trigger the Matrix
   Konami code (↑↑↓↓←→←→BA) on the live site and confirm the easter egg still activates correctly
   in production, not just locally.

## Done when

- `vercel --prod` completed with deployment state `READY`.
- The production URL (check `vercel projects ls` or the Vercel dashboard for the current production
  alias) visibly reflects the Collection redesign when loaded in a real browser — this is a
  needs-human task specifically because "does this look right in production" is a judgment call,
  not something a structural check can verify.
- Matrix mode confirmed working on the live production URL, not just in local dev.
- Record the deployment URL/timestamp in this task's worklog.
