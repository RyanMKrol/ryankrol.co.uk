## Do

Fix /reviews/albums/edit (src/pages/reviews/albums/edit.js) so it visually matches the real albums list page (/reviews/albums, src/pages/reviews/albums/index.js) instead of rendering full-width cards, while keeping the existing top-right 'Edit' button overlay.

Root cause: src/pages/reviews/albums/edit.js (around lines 63-80) wraps each `ReviewCard` in a `<div className="reviews-wrapper">` (src/styles/globals.css around lines 759-762 — just background/border-radius, no grid layout) instead of `<div className="square-cover-grid">`, which is what src/pages/reviews/albums/index.js:171 uses (`.square-cover-grid` is real CSS Grid — `display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;`, src/styles/globals.css around lines 387-397 — unaffected by the column-count sort-order bug, so this is a simple class swap, no JS masonry logic needed here).

Do:
1. In `src/pages/reviews/albums/edit.js`, change the wrapping `<div className="reviews-wrapper">` (around line 63) to `<div className="square-cover-grid">`. Leave everything inside it exactly as-is: each item stays wrapped in `<div key={album.id} className="review-card-with-edit">` containing the `ReviewCard` (`styleVariant="square-cover"`) and the `<Link href={...} className="edit-button-overlay">Edit</Link>`.
2. Do NOT add search/sort/pagination controls — only the wrapping container class changes.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/albums/edit — cards render in the same 2-column CSS Grid layout as /reviews/albums (not full-width), each card still shows the pill-shaped 'Edit' button top-right, and clicking it still navigates to /reviews/albums/edit/<id>.
