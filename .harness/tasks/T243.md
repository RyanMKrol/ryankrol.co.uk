## Do

Fix the masonry sort-order reading-order bug on /reviews/books by adopting the shared `MasonryColumns` component + `distributeRoundRobin` helper + `useResponsiveColumnCount` hook built in a separate, already-completed task (`src/lib/masonryColumns.js`, `src/hooks/useResponsiveColumnCount.js`, `src/components/MasonryColumns.js` — already exist; read them, do not rebuild). That task also converted `src/pages/projects/index.js` to this pattern — use it as a reference example.

Background: `.spine-cover-list` (src/styles/globals.css around lines 311-320) uses `column-count: 2` (`column-count: 1` under `max-width: 700px` — NOTE this breakpoint is 700px, not 900px like the other grids) for the books list at src/pages/reviews/books/index.js (around lines 164-175), which renders `pagedBooks` (sorted via SortButtons — date/title/score, `sortBy` branches around lines 76-107) into `<div className="spine-cover-list">`. Same bug: CSS multi-column fills column 1 top-to-bottom before column 2, breaking left-to-right reading order after a sort.

Do:
1. In `src/styles/globals.css`, replace `.spine-cover-list` (currently `column-count: 2; column-gap: 1.5rem;` plus its `@media (max-width: 700px)` override) with a flex-row outer class (`display: flex; gap: 1.5rem;`) and a new `.spine-cover-list-col` class (`display: flex; flex-direction: column; gap: 1.5rem; flex: 1 1 0; min-width: 0;`). Remove the `column-count: 1` mobile override (JS now drives column count via `useResponsiveColumnCount(2, 700)` — keep the 700px breakpoint, matching today's behavior). `.spine-cover-card` currently has `break-inside: avoid; margin-bottom: 1.5rem;` — remove `margin-bottom` (redundant with the column's flex `gap`); keep `break-inside: avoid`.
2. In `src/pages/reviews/books/index.js`, import `MasonryColumns` from `../../../components/MasonryColumns` and `useResponsiveColumnCount` from `../../../hooks/useResponsiveColumnCount`. Replace the `<div className="spine-cover-list">{pagedBooks.map(...)}</div>` block with `<MasonryColumns items={pagedBooks} columnCount={useResponsiveColumnCount(2, 700)} className="spine-cover-list" columnClassName="spine-cover-list-col" renderItem={(book, index) => (<ReviewCard key={`${book.title}-${book.author}-${index}`} item={book} type="book" isLast={index === pagedBooks.length - 1} styleVariant="spine-cover" gradient={bookGradients[index]} />)} />` — note the existing `isLast` prop (`index === pagedBooks.length - 1`) must still be computed against the ORIGINAL `pagedBooks` array length/index, not a per-column index (MasonryColumns passes the original index, so this should just work — verify it does). Keep `bookGradients` working as-is (already indexed against `pagedBooks`).

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/books, sort by score — item #1 top-left, item #2 to its right (2-column left-to-right-then-top-to-bottom order), cards packing tightly with no dead gaps. Resize below 700px and confirm single-column collapse. Confirm the last book in the list still gets whatever special `isLast` styling it had before (check ReviewCard's spine-cover variant for what isLast does).
