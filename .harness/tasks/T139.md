## Do

In `src/components/ReviewCard.js`, the component conditionally renders a second date line when a review has been edited:

```jsx
{item.editedDate && (
  <p className="review-date">
    Edited: {item.editedDate}
  </p>
)}
```

(this block is around lines 92-96, immediately after the `item.date` / `Date: {item.date}` block). Change the literal text `Edited:` to `Updated:` so it reads:

```jsx
{item.editedDate && (
  <p className="review-date">
    Updated: {item.editedDate}
  </p>
)}
```

This is a pure display-text change. Do NOT rename the `editedDate` prop/field itself, do NOT touch any API route (`src/pages/api/reviews/**/update.js`), and do NOT touch any DynamoDB data or the sort logic in `src/pages/reviews/{movies,tv,albums}/index.js` that reads `item.editedDate` — those all keep the name `editedDate` exactly as-is. Only the visible label string in this one JSX file changes.

Note: the uppercase 'EDITED:' appearance in some card style variants comes from a generic CSS rule (`.review-style-2 .review-date { text-transform: uppercase; }` in `src/styles/globals.css`) that applies to the whole `.review-date` paragraph (covering both the 'Date:' and this line) — it is not review-card-specific to this text and should NOT be changed; it will automatically apply to 'Updated:' too once the JSX text changes.

## Done when

`src/components/ReviewCard.js` shows `Updated: {item.editedDate}` (not `Edited:`) in the conditional block that renders when `item.editedDate` is truthy. No other file changed — `editedDate` as a field/prop name is untouched everywhere else (API routes, sort comparators, tests). `verify: ["run-app"]` — run the app locally (or `npm run dev`) and manually confirm in the browser: open a review list page for a type that has edited items (movies, tv, or albums — e.g. `/reviews/movies`), find a card that shows the second date line, and confirm it now reads "Updated: DD-MM-YYYY" (or "UPDATED: DD-MM-YYYY" under the uppercase style variant) instead of "Edited: ...". `npm run lint` and `npm test` should still pass unchanged (no test currently asserts on this exact label string, so none should need updating — if one does, update it as part of this task).
