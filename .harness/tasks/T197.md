## Do

In `src/pages/index.js`, thread a secondary-line value through the home page's 'collection wall' tile construction so vinyl and album tiles show the artist name, and book tiles show the author, underneath the title — using `CoverTile`'s existing (currently unused) `subtitle` prop. Do NOT modify `src/components/CoverTile.js` or `src/styles/globals.css` — `CoverTile` already accepts and renders an optional `subtitle` prop as a second caption line (`.cover-tile-subtitle` CSS class already exists and is already styled, distinct/smaller from `.cover-tile-title`), so this is pure data-wiring inside `index.js`, no component or style changes needed.

Specifically:

1. In the `wallItems` `useMemo` (the `wallCandidates` array, currently mapping `movies`/`tv`/`books`/`albums`/`vinyl` into `{key, title, imageUrl, kind, href}` objects), add a `subtitle` field to each mapped object:
   - albums: `subtitle: a.artist || null`
   - vinyl: `subtitle: v.artist || null`
   - books: `subtitle: b.author || null`
   - movies: no `subtitle` field added (movies have no artist/author-equivalent field in the `MovieRatingsV4` schema)
   - tv: no `subtitle` field added (same reasoning — `TelevisionRatingsV4` has no artist/author-equivalent field)

2. In the wall grid render (the `wallItems.map(...)` block that renders `<Link><CoverTile title={item.title} imageUrl={item.imageUrl} id={item.key} /></Link>`), add `subtitle={item.subtitle}` to the `<CoverTile>` call so it's passed through alongside the existing props.

Scope note: only `src/pages/index.js` needs to change — `CoverTile` already handles an undefined/null `subtitle` gracefully (renders nothing extra), so movie/tv tiles need no special-casing beyond simply not setting the field.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass, per this repo's Definition of Done.
- `verify: ["run-app"]`: run `npm run dev`, open `/` in a browser, and reload a few times (the wall samples randomly on each load) until you see at least one vinyl tile, one album tile, and one book tile in the collection wall. Confirm: vinyl and album tiles show the artist name as a smaller second line under the title; book tiles show the author as a smaller second line under the title; movie and tv tiles show only the title, with no blank second line or layout gap (matching their current appearance before this change).
