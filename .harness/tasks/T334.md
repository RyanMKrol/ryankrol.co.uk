## Overview
Every perfume card's ownership pill (SAMPLE / Travel size / Full bottle) currently has no label explaining what it represents; add a hover/focus tooltip reading exactly 'What I own' above it, using the same Tooltip pattern already used twice in this file for the Longevity/Projection bars.

## Do
In src/components/perfumeVariants/Variant6Hybrid.js, wrap the ownership Badge (~line 46) in `<Tooltip label="What I own">`, with no `position` prop and no extra className, so it renders above the pill via Tooltip's existing default behavior.

## Done when
Every perfume card with an `item.ownership` value shows a tooltip bubble reading exactly 'What I own' positioned above the ownership pill on hover/focus (verify via `VISUAL_CHECK_ONLY=perfume node scripts/visual-check.mjs` plus reading Tooltip.js/globals.css to confirm the bubble's `bottom: 100%` placement, since `:hover`/`:focus-within` states aren't captured by static screenshots); no other perfume card content or the Longevity/Projection tooltips regress.
