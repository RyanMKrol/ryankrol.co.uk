## Do

Fix /reviews/movies/edit (src/pages/reviews/movies/edit.js) so it visually matches the real movies list page (/reviews/movies, src/pages/reviews/movies/index.js) instead of rendering full-width cards, while keeping the existing top-right 'Edit' button overlay.

Root cause: src/pages/reviews/movies/edit.js (around lines 63-80) wraps each `ReviewCard` in a `<div className="reviews-wrapper">` (a plain full-width container, src/styles/globals.css around lines 759-762 — just background/border-radius, no grid/column layout), instead of using the same masonry-column layout the real list page uses. By the time this task runs, `src/pages/reviews/movies/index.js` has already been converted (in a separate, already-completed task) to render its cards via the shared `<MasonryColumns>` component (`src/components/MasonryColumns.js`) + `useResponsiveColumnCount` hook (`src/hooks/useResponsiveColumnCount.js`) instead of a single `.poster-banner-grid` div — read that file NOW to see the exact current pattern before making this change (this spec was written before that task's final code landed, so treat movies/index.js as the source of truth for the exact `MasonryColumns` props/usage).

Do:
1. In `src/pages/reviews/movies/edit.js`, replace the `<div className="reviews-wrapper">{movies.map((movie, index) => ( <div key={movie.id} className="review-card-with-edit"> ... </div> ))}</div>` block (around lines 63-80) with the SAME `<MasonryColumns>` usage pattern movies/index.js uses (`items={movies}`, `columnCount={useResponsiveColumnCount(3, 900)}`, `className="poster-banner-grid"`, `columnClassName="poster-banner-grid-col"`), except each `renderItem` returns the EXISTING `<div className="review-card-with-edit">` wrapper containing the `ReviewCard` (`styleVariant="poster-banner"`) AND the existing `<Link href={...} className="edit-button-overlay">Edit</Link>` — i.e. keep the edit-button-overlay markup exactly as it is today, just change what CONTAINS it (from a `reviews-wrapper` div to a `MasonryColumns`-rendered column cell).
2. Import `MasonryColumns` and `useResponsiveColumnCount` the same way movies/index.js does (adjust the relative import path for this file's location: `../../../components/MasonryColumns`, `../../../hooks/useResponsiveColumnCount`).
3. Do NOT add search/sort/pagination controls — this edit page intentionally has none of those; it just lists everything sorted by date (unchanged, already sorted client-side by the existing `useEffect`). Only the wrapping/layout technique changes.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/movies/edit — cards render in the same multi-column packed masonry layout as /reviews/movies (not full-width), each card still shows the pill-shaped 'Edit' button top-right, and clicking it still navigates to /reviews/movies/edit/<id>.
