## Do

## Do
The perfume review forms' Type `<select>` (native browser dropdown, options from `PERFUME_TYPES` = `['EDP','EDT','Parfum']`) renders with too little space between its option text and the field's right border, because the browser's native dropdown-arrow chrome ignores the element's right `padding` (it's part of the OS/browser widget's own reserved area), worsened by the `style={{ width: 'auto' }}` shrink-to-fit sizing already on the element. Fix both instances of the identical pattern:
- `src/pages/reviews/perfumes/add.js:171-186` — the `<select id="type">` element (currently `className="collection-form-input"` + `style={{ width: 'auto' }}`).
- `src/pages/reviews/perfumes/edit/[id].js:259-274` — the identical `<select id="type">` element, same className/style.

1. In `src/styles/globals.css`, immediately after the `.collection-form-input, .collection-form-textarea` rule (currently lines 2677-2689), add a new class, e.g. `.collection-form-select`:
   - Turn off native appearance: `appearance: none;` plus `-webkit-appearance: none;` and `-moz-appearance: none;` for cross-browser support.
   - Draw a custom caret via `background-image` (an inline SVG data-URI chevron, coloured with a fixed neutral tone consistent with `--color-ink-mute`), `background-repeat: no-repeat;`, `background-position: right 1rem center;`, `background-size` around `0.6em` (small chevron).
   - Override just the right side of the inherited padding to clear the new caret, e.g. `padding-right: 2.5rem;` (leave top/bottom/left padding as inherited from `.collection-form-input`).
2. On both `<select>` elements, add the new class alongside the existing one, e.g. `className="collection-form-input collection-form-select"`. Do not remove or change the existing `style={{ width: 'auto' }}` inline style, the `required` attribute, or the `PERFUME_TYPES.map(...)` options — this task is scoped purely to the caret/padding visual issue.
3. Leave the surrounding `.collection-form-group` flex wrapper and label markup unchanged on both pages.

## Done when

## Done when
- On `/reviews/perfumes/add`, the Type dropdown shows a custom (non-native) caret with clear, even spacing between the selected option text, the caret, and the field's right border — checked visually by running `npm run dev` and opening the add-perfume form.
- The same visual check passes on `/reviews/perfumes/edit/[id]` (open the edit form for any existing perfume) — the Type select looks identical in spacing to the add-page fix.
- The native browser dropdown arrow is no longer visible on either page (replaced by the CSS-drawn chevron from `.collection-form-select`).
- No regression to other fields on either form (labels remain inline next to the Type field, other `.collection-form-input` fields — designer, rating, etc. — are visually unaffected).
- `npm run lint` and `npm run build` both pass.
