## Do

Apply the shared header-strip pattern from perfumes-edit-redesign-2 to the movies edit page, replacing T263's contrast-only fix (movies never had an overlap problem, only a contrast one against the poster-banner gradient — the new in-flow strip with an opaque `--color-surface-alt` background makes the contrast fix moot since the button no longer sits on top of the gradient at all). In src/pages/reviews/movies/edit.js's `renderItem` callback: change the wrapper div's className from `"review-card-with-edit"` to `"review-edit-card"`; move the Edit `<Link>` to be the FIRST child, wrapped in a new `<div className="review-edit-header">`, with the Link's className changed from `"edit-button-overlay edit-button-overlay--movies-banner"` to `"review-edit-button"`; the `<ReviewCard .../>` (styleVariant="poster-banner") becomes the second child. In src/styles/globals.css, delete the now-dead T263 rules: `.edit-button-overlay--movies-banner` and `.edit-button-overlay--movies-banner:hover`, plus their explanatory comment, located between the spine-cover fix block and the tv-banner fix block — do NOT touch the base `.review-card-with-edit`/`.edit-button-overlay` rules or the tv/albums/perfumes fix blocks.

## Done when

`npm run dev`, visit `/reviews/movies/edit`: every card shows the neutral header strip (background/border per the shared pattern) with only the Edit button, right-aligned, above the poster-banner image — the button is no longer overlaid on the gradient banner at all, so the old dark-opaque-for-contrast styling is gone and unnecessary. Clicking Edit still navigates correctly. Card content below the strip (poster banner, title, rating, date, snippet) renders the same as `/reviews/movies` (view page) for the same movie. `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/movies/edit.js` returns NOTHING. `npm run lint`, `npm test`, `npm run build` all pass.
