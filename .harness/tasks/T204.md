## Do

Fix the application-spot list text in `src/components/perfumeVariants/Variant6Hybrid.js` (currently lines 76-78) so that when an entry's `sprays === 1`, spots that occur in anatomically-paired/multiple locations on the body get an " each" qualifier appended, while spots that occur only once on the body do not.

**Classification of all 7 `VALID_APPLICATION_SPOTS` values (from `src/lib/perfumes.js:19-27`) — decided explicitly, do not re-derive:**

- PAIRED (append "each" when sprays === 1): `Wrists`, `Elbows`, `Clavicles`, `Behind ears` — each of these exists as two anatomical locations on a body (both wrists, both elbows, both collarbones, both ears), so "1 spray" is ambiguous between "one spray total" and "one spray per side"; "each" disambiguates to the latter, which is the intended meaning.
- SINGULAR (no "each", ever): `Beard`, `Back of neck`, `Clothes` — each of these is a single location/target, so no pairing ambiguity exists. (`Beard` and `Clothes` are singular targets even though a beard covers an area and clothes can be plural garments — there's no bilateral "one on each side" reading for either, matching the owner's explicit `Back of neck` example.)

Implement as a `PAIRED_APPLICATION_SPOTS` (or equivalently named) constant array containing exactly `['Wrists', 'Elbows', 'Clavicles', 'Behind ears']`, exported from `Variant6Hybrid.js` alongside a pure function (e.g. `formatApplicationSpotLine({ spot, sprays })`) that returns:
- `sprays === 1` AND `spot` is in the paired list → `` `1 spray each — ${spot}` ``
- `sprays === 1` AND `spot` is NOT in the paired list → `` `1 spray — ${spot}` `` (unchanged from today)
- `sprays !== 1` (any other count) → `` `${sprays} sprays — ${spot}` `` (unchanged from today — the owner's ask was scoped to the `sprays === 1` case only; do not modify plural-count rendering)

Update the `<li>` render in the `applicationSpots.map(...)` block to call this helper instead of the current inline ternary, so the displayed text matches the string exactly (keep the existing `key={spot}` and `className="perfume-v4-checklist-item"` on the `<li>`).

Add a co-located Jest test file (e.g. `src/components/perfumeVariants/Variant6Hybrid.test.js`) that imports only the named pure-function export (not the default component) and asserts the exact output string for all 7 enum spots at `sprays === 1`, plus at least one `sprays > 1` case (e.g. `sprays: 2`) confirming plural rendering is unchanged and never gets "each" appended.

## Done when

- `Variant6Hybrid.js` exports a `PAIRED_APPLICATION_SPOTS`-equivalent constant containing exactly `Wrists`, `Elbows`, `Clavicles`, `Behind ears` (and none of `Beard`, `Back of neck`, `Clothes`).
- For `sprays === 1`: `Wrists`, `Elbows`, `Clavicles`, `Behind ears` render as `"1 spray each — <Spot>"`; `Beard`, `Back of neck`, `Clothes` render as `"1 spray — <Spot>"` (no "each").
- For `sprays !== 1` (e.g. 2), every spot renders unchanged as `"<N> sprays — <Spot>"` with no "each" appended to any spot.
- A new Jest test (co-located `*.test.js`, importing the named pure-function/constant export only) covers all 7 enum spots at `sprays === 1` plus at least one `sprays > 1` case, asserting exact string output.
- `npm run lint`, `npm test`, and `npm run build` all pass with no new errors.
- Manual verify (facets.layer=="ui" requires this): run `npm run dev`, open `http://localhost:3000/reviews/perfumes` (or the dev fallback port), find a perfume review whose application spots include at least one paired spot (e.g. Wrists/Elbows/Clavicles) at 1 spray and one singular spot (e.g. Back of neck) at 1 spray — confirm "each" appears only on the paired-spot line, matching the owner's original Althaïr screenshot example. Record the observation in the task's worklog.
