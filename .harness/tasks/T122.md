## Do

`src/pages/api/reviews/books/update.js` is the POST handler that edits an existing book review in the `BookRatingsV3` DynamoDB table (composite key `title` + `author`). Today, lines 33-46 fetch the existing item via `GetCommand` (keyed on `originalTitle`+`originalAuthor`) specifically to preserve the original `date` field across edits (`const originalDate = existingReview.Item?.date;` then `const preservedDate = originalDate || ...`), and lines 66-83 build `reviewData` with `date: preservedDate` — so an edit currently leaves `date` completely untouched, and there is no record anywhere of when an edit happened.

Add a new field, `editedDate`, to track when the review was last edited, using the exact same `DD-MM-YYYY` string format/convention as the existing `date` field:

1. Keep the existing `originalDate`/`preservedDate` logic at lines 33-46 completely unchanged — `date` must still be preserved across edits exactly as today.
2. Compute a fresh `editedDate` value using `new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` (today's date, formatted the same way as `date`).
3. Add `editedDate` to the `reviewData` object built at lines 66-83 (alongside `date: preservedDate`), unconditionally on every successful edit — do not wrap it in the `...(x !== undefined && {x})` optional-spread pattern used for the search-metadata fields (`source`, `olid`, etc.); `editedDate` is always known at write time, unlike those.
4. Do not change any other behavior — the title/author-changed delete/recreate logic (lines 48-63), the cache clear (`clearApiCache('api-books')`, line 93), password gating, and validation are all unchanged.

Also update the repo's docs in the same change per `CLAUDE.md`'s Definition of Done: in the root `CLAUDE.md`'s Data model table, the `BookRatingsV3` row currently lists `{ title, rating(0–5), review_text, date }` plus a long list of optional search fields (`source`, `coverUrl`, `volumeId`, `olid`/`coverId`, `bookAuthors`, `firstPublishedYear`, `isbn`, `subjects`, `pageCount`, `publisher`) — extend that optional-fields list to also mention `editedDate` ('DD-MM-YYYY', set on every edit). Also check `README.md`'s schema section for the equivalent row and update it the same way if it duplicates this shape.

Do not touch `src/pages/api/reviews/movies/update.js`, `tv/update.js`, or `albums/update.js` — those are separate, independent tasks. Do not touch `src/components/ReviewCard.js` or any `index.js` sort logic — those are separate tasks too.

## Done when

- `src/pages/api/reviews/books/update.js` writes both `date` (preserved from the original record, unchanged behavior) and a new `editedDate` (today's date, `DD-MM-YYYY` format, computed fresh on every call) into `reviewData` on every successful update.
- A manual/curl POST to `/api/reviews/books/update` with a valid `password`, `title`, `author`, `rating`, `overview`, `originalTitle`, `originalAuthor` for an existing book results in the DynamoDB item having its original `date` unchanged and a new `editedDate` equal to today's date in `DD-MM-YYYY` format.
- Calling update again on the same item on a later date updates `editedDate` to the new date while `date` still stays at its original value.
- `CLAUDE.md`'s Data model table row for `BookRatingsV3` documents the new `editedDate` field, and `README.md`'s schema section is updated to match if it independently describes this table's shape.
- `npm run lint` passes with no new errors.
- `npm test` passes.
- `npm run build` succeeds.
