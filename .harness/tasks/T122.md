## Do

`src/pages/api/reviews/books/update.js` is the POST handler that edits an existing book review in
the `BookRatingsV4` DynamoDB table (`id`-keyed — a random UUID assigned once at creation and never
regenerated; this spec previously described the older, now-retired `BookRatingsV3` composite
title+author key, which is stale — T113 already migrated this table to V4, and the current handler
looks the item up via `Key: { id: originalId }`, not by title/author). Today, lines 33-45 fetch the
existing item via `GetCommand` (keyed on `originalId`, one of the POST body fields alongside `title`,
`author`, `rating`, `overview`, `password`, and the optional search-metadata fields) specifically to
preserve the original `date` field across edits (`const originalDate = existingReview.Item?.date;`
then `const preservedDate = originalDate || new Date().toLocaleDateString('en-GB').replace(/\//g,
'-')`), and lines 48-66 build `reviewData` (starting `{ id: originalId, title, author, rating,
review_text: overview, date: preservedDate, ... }`) with `date: preservedDate` — so an edit currently
leaves `date` completely untouched, and there is no record anywhere of when an edit happened. There
is no delete/recreate logic in this handler (that would only be needed for a composite-key table;
V4's stable `id` key means a title/author change is just a normal field update via the same
`PutCommand`) — do not go looking for one.

Add a new field, `editedDate`, to track when the review was last edited, using the exact same
`DD-MM-YYYY` string format/convention as the existing `date` field:

1. Keep the existing `originalDate`/`preservedDate` logic at lines 33-45 completely unchanged —
   `date` must still be preserved across edits exactly as today.
2. Compute a fresh `editedDate` value using `new Date().toLocaleDateString('en-GB').replace(/\//g,
   '-')` (today's date, formatted the same way as `date`).
3. Add `editedDate` to the `reviewData` object built at lines 48-66 (alongside `date: preservedDate`),
   unconditionally on every successful edit — do not wrap it in the `...(x !== undefined && {x})`
   optional-spread pattern used for the search-metadata fields (`source`, `olid`, etc.); `editedDate`
   is always known at write time, unlike those.
4. Do not change any other behavior — the cache clear (`clearApiCache('api-books')`, line 76),
   password gating, and validation are all unchanged.

Also update the repo's docs in the same change per `CLAUDE.md`'s Definition of Done: in the root
`CLAUDE.md`'s Data model table, the `BookRatingsV4` row currently lists `{ id, title, author,
rating(0–5), review_text, date 'DD-MM-YYYY' }` plus a long list of optional search fields (`source`,
`coverUrl`, `volumeId`, `olid`/`coverId`, `bookAuthors`, `firstPublishedYear`, `isbn`, `subjects`,
`pageCount`, `publisher`) — extend that optional-fields list to also mention `editedDate`
(`'DD-MM-YYYY'`, set on every edit). Also check `README.md`'s schema section for the equivalent row
(`#### BookRatingsV4 Table`) and update it the same way — it does independently duplicate this shape.

Do not touch `src/pages/api/reviews/movies/update.js`, `tv/update.js`, or `albums/update.js` — those
are separate, independent tasks. Do not touch `src/components/ReviewCard.js` or any `index.js` sort
logic — those are separate tasks too (T126 handles sorting the books list by `editedDate`).

## Done when

- `src/pages/api/reviews/books/update.js` writes both `date` (preserved from the original record,
  unchanged behavior) and a new `editedDate` (today's date, `DD-MM-YYYY` format, computed fresh on
  every call) into `reviewData` on every successful update.
- A manual/curl POST to `/api/reviews/books/update` with a valid `password`, `title`, `author`,
  `rating`, `overview`, and `originalId` for an existing book results in the DynamoDB item having its
  original `date` unchanged and a new `editedDate` equal to today's date in `DD-MM-YYYY` format.
- Calling update again on the same item on a later date updates `editedDate` to the new date while
  `date` still stays at its original value.
- `CLAUDE.md`'s Data model table row for `BookRatingsV4` documents the new `editedDate` field, and
  `README.md`'s `#### BookRatingsV4 Table` schema section is updated to match.
- `npm run lint` passes with no new errors.
- `npm test` passes.
- `npm run build` succeeds.
