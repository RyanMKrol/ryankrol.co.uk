## Do

Build `src/components/MarkdownEditor.js` per the design notes above: pick and wire either `@uiw/react-md-editor` (preferred if it reskins cleanly) or a hand-rolled selection-based toolbar (fallback), added to `package.json` only if the library path is taken. The component must be a drop-in replacement for the plain `<textarea>` currently used identically across all 10 review add/edit forms (do not wire it into those forms yet - that's tasks 3 and 4; this task only builds and unit-tests the component in isolation, e.g. via a throwaway/demo usage in its own test file). At minimum support: bold, italic, bullet list, and link toolbar actions that operate on the current text selection and insert valid Markdown syntax (`**text**`, `*text*`, `- item`, `[text](url)`). Style it (in `src/styles/globals.css`, reusing/extending the existing `.collection-form-textarea` treatment) so it visually matches the Collection theme's paper/cream aesthetic AND correctly re-themes when `html.matrix-active` is toggled (test both manually - see Done when).

## Done when

`src/components/MarkdownEditor.js` exists, accepts the `{ id, name, value, onChange, placeholder, required, className }` prop contract described above, and its toolbar's Bold/Italic/List/Link buttons correctly wrap/insert Markdown syntax around the current textarea selection. `src/components/MarkdownEditor.test.js` (React Testing Library) renders the component, simulates selecting text and clicking each toolbar button, and asserts the resulting value contains the expected Markdown syntax, plus asserts `onChange` fires with the new value. `npm run lint`, `npm test`, `npm run build` pass. If a new dependency was added, `package.json` reflects it and the PR/commit notes call out that this is a deliberate, owner-requested new dependency per CLAUDE.md's 'no new infra unless explicitly asked' convention. Manually verify with `npm run dev` (e.g. temporarily drop the component into any one existing add page, or a scratch page, to click through it): the editor visually matches the Collection theme by default, and toggling Matrix mode (Konami code ↑↑↓↓←→←→BA) re-themes it to the green-on-black look without a page reload, matching how every other themed element behaves.
