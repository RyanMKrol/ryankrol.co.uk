## Do

Run `LIVE=1 node src/scripts/backfillPersonalBests.js` (added by the dependency task) once against production DynamoDB, after reviewing its dry-run output for sanity (spot-check a handful of exercises' expected PR history against what you remember actually happened). This performs a one-time retroactive write of `isWeightPR`/`is1RMPR`/`isVolumePR` flags across all ~378 existing workouts / 1,314+ exercise rows — nothing else retroactively derives PRs for pre-existing data; the live write path only ever compares new workouts against already-flagged history. Requires real AWS credentials (`.env.local` via `vercel env pull`), which the autonomous harness deliberately does not have — hence `needs-human`.

## Done when

- Dry run reviewed and looks sane (spot-checked a few known exercises).
- `LIVE=1 node src/scripts/backfillPersonalBests.js` run once against production.
- A few known exercises' history spot-checked post-run to confirm the flagged sets look right (e.g. a workout you remember hitting a new heaviest weight in).
