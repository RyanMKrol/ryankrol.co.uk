## Do

Run `LIVE=1 node src/scripts/backfillPersonalBests.js` (added by the dependency task) once against production DynamoDB, using the AWS credentials already available in this checkout's `.env.local` (root `CLAUDE.md`'s standard `vercel env pull --environment production` convention) — the same precedent as T083's autonomous production DynamoDB operation. This performs a one-time retroactive write of `isWeightPR`/`is1RMPR`/`isVolumePR` flags across all ~378 existing workouts / 1,314+ exercise rows — nothing else retroactively derives PRs for pre-existing data; the live write path only ever compares new workouts against already-flagged history.

Before running live: do a full `ScanCommand` of the `Exercises` table (or reuse the dependency script's own dry-run output) and save it as a BEFORE snapshot. Run the script WITHOUT `LIVE=1` first and confirm it completes without error and reports a sane-looking set of changes (not zero, not the entire table). Only then re-run with `LIVE=1`.

If the command fails because `.env.local` is missing or the AWS credentials it contains are invalid/expired, record `failed:blocked` naming the specific error — do not attempt to work around a genuine credentials gap.

## Done when

- Dry run completes without error and reports a plausible (non-zero, non-total) set of changes.
- `LIVE=1 node src/scripts/backfillPersonalBests.js` run once against production, all writes succeed with 0 errors.
- **Objective post-run verification** (not a subjective spot-check): for at least 3 different `exercise_name` values, independently recompute the chronological running-best (heaviest weight, best estimated 1RM, best set volume) directly from a fresh `ScanCommand`/GSI query of the `Exercises` table's raw `sets[]` data, walking oldest-to-newest exactly as the backfill script does. Confirm the sets the script flagged as `isWeightPR`/`is1RMPR`/`isVolumePR` for each of those 3 exercises exactly match the independently-recomputed running-best transitions — i.e. re-derive the answer from the same source data using the same method, rather than comparing against personal recollection.
- A full post-run scan confirms every row the dry-run identified for change now has its flags set, and no row outside that set was modified (compare against the BEFORE snapshot).
