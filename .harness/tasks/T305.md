## Do

src/pages/reviews/perfumes/edit.js currently renders every perfume card as a direct child of a bare `<div className="perfume-card-grid">` (lines 62-72 at time of writing) with no column-wrapping mechanism at all — that's why it happens to lay out as however-many-fit-per-row (typically 3 on desktop) instead of a deliberate column count. Fix it to match the sibling view page (src/pages/reviews/perfumes/index.js:1-7,20,115-123), which already does this correctly: import `MasonryColumns` from `../../../components/MasonryColumns` and `useResponsiveColumnCount` from `../../../hooks/useResponsiveColumnCount`; add `const columnCount = useResponsiveColumnCount(2, 700);` alongside the existing `useState`s; replace the bare `<div className="perfume-card-grid">{perfumes.map(...)}</div>` block with a `<MasonryColumns items={perfumes} columnCount={columnCount} className="perfume-card-grid" columnClassName="perfume-card-grid-col" renderItem={(perfume, index) => (...)} />`, moving the existing per-card JSX (the `review-card-with-edit review-card-with-edit--perfume` wrapper div containing `Variant6Hybrid` + the Edit `Link`) into the `renderItem` callback exactly as `src/pages/reviews/books/edit.js` already does it for its own MasonryColumns usage (use that file as the closest working reference — same `useResponsiveColumnCount(2, 700)` call, same wrapping pattern). Do not change `.perfume-card-grid` / `.perfume-card-grid-col` CSS (globals.css:416-424) — they're already correct and shared with the view page; the fix is purely in edit.js's JSX structure. NOTE: this file is also touched later by task perfumes-edit-redesign-7 (the header-strip redesign for perfumes) — that task depends on this one landing first so its own line-anchor references stay accurate.

## Done when

`npm run dev`, visit `/reviews/perfumes/edit` at a desktop viewport (>=700px wide) side-by-side with `/reviews/perfumes`: both now render exactly 2 columns of cards (count the `.perfume-card-grid-col` wrapper divs in devtools — exactly 2 on both pages), and resizing the window below 700px collapses both to 1 column identically (same breakpoint behavior, since both now call `useResponsiveColumnCount(2, 700)`). All existing perfume cards still appear (none dropped), each still has a working Edit button/link to `/reviews/perfumes/edit/<id>`. `npm run lint`, `npm test`, and `npm run build` all still pass.
