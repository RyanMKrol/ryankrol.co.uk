## Do

This repo (ryankrol.co.uk, Next.js pages router + DynamoDB, region us-east-2 hardcoded in src/lib/dynamo.js) currently keys the `MovieRatingsV3` DynamoDB table on `title` alone (a String partition key, no other key attribute). The owner wants a real synthetic `id` to become the ACTUAL DynamoDB partition key (not an additive attribute alongside title) so that `src/pages/reviews/movies/edit/[id].js` can stop looking movies up by decoded title. DynamoDB partition keys cannot be changed in place, so this requires a NEW table with `id` as its key, populated from a full migration of the existing table's data.

Write a new one-shot ops script at `src/scripts/migrateMoviesToIdTable.js`, following this repo's existing migration-script conventions — mirror `src/scripts/migrateAlbumsToV2.js` (self-contained `DynamoDBClient`/`DynamoDBDocumentClient` setup, `dotenv.config({ path: '.env.local' })`, region `'us-east-2'`, credentials from `process.env.AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY`, a `createTableIfNotExists(tableName, keySchema)` helper using `DescribeTableCommand`+`CreateTableCommand`+`waitUntilTableExists`, emoji-prefixed `console.log` progress messages) and `src/scripts/createPerfumeTable.js` (the `PAY_PER_REQUEST` billing mode / single-`id`-HASH-key `CreateTableCommand` shape — see its `KeySchema: [{ AttributeName: 'id', KeyType: 'HASH' }]`).

The script must:
1. Create a new table `MovieRatingsV4` with `id` (String) as the sole HASH partition key, `PAY_PER_REQUEST` billing — idempotent (skip creation if it already exists, like `migrateAlbumsToV2.js`'s `createTableIfNotExists`).
2. Scan ALL items from the existing `MovieRatingsV3` table (paginate past DynamoDB's 1MB scan page limit if needed).
3. Guard against accidental double-migration: before writing, scan `MovieRatingsV4` — if it already has items, log a warning and abort (non-zero exit) unless a `--force` CLI flag is passed.
4. For every old item, generate `id: randomUUID()` (Node's built-in `require('crypto').randomUUID()` — Node 22 is this repo's runtime per `.harness/HARNESS.md` §10) and write a NEW item into `MovieRatingsV4` containing every existing field from the old item UNCHANGED (`title`, `rating`, `review_text`, `date`, and the optional `tmdbId`/`mediaType`/`posterPath`/`tmdbOverview`/`tmdbDate` fields — see `src/pages/api/reviews/movies/update.js:61-71` for the full field list) plus the new `id` field. This is a pure copy + id-append — do not rename, drop, or transform any existing field.
5. Batch-write in chunks of 25 (`BatchWriteCommand`'s limit), matching `migrateAlbumsToV2.js`'s batching approach.
6. Log a final summary comparing old-table vs new-table item counts so a human can visually confirm a 1:1 migration.
7. Do NOT delete or modify `MovieRatingsV3` — it must stay fully intact as a rollback safety net.
8. Do NOT touch `src/lib/constants.js`, any file under `src/pages/**`, or `src/lib/dynamo.js` in this task. The live app must keep reading/writing `MovieRatingsV3` unchanged until the separate, dependent cutover task (`review-edit-url-id-2`) ships — this task only adds two new files.
9. Extract the pure "build the migrated item" transform (old item -> new item with a generated id) into an exported function so it's unit-testable without hitting DynamoDB — mirror `src/scripts/migrateOpenLibraryBooksToGoogleBooks.test.js`'s pattern of testing pure transform logic pulled out of a migration script. Add `src/scripts/migrateMoviesToIdTable.test.js` asserting: every existing field is preserved unchanged, an `id` field is added, and the generated `id` is a valid UUID (v4 shape).

Document the run command in a top-of-file comment exactly like `migrateAlbumsToV2.js` does (`Run with: node src/scripts/migrateMoviesToIdTable.js`).

## Done when

`npm test` passes, including new assertions in `src/scripts/migrateMoviesToIdTable.test.js` covering: field preservation, `id` presence, and UUID shape.
`npm run lint` passes on the new script.
The diff touches ONLY `src/scripts/migrateMoviesToIdTable.js` and its test file — no file under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js` is modified (grep the diff to confirm).
This task is `needs-human`: after the script + test are written, committed, and passing CI, a HUMAN (the site owner) must actually run `node src/scripts/migrateMoviesToIdTable.js` against the real production `MovieRatingsV3`/`MovieRatingsV4` tables (using real `AWS_ACCESS_KEY_ID`/`AWS_SECRET_ACCESS_KEY` pulled via `vercel env pull`), manually confirm the logged old-count/new-count match, and only THEN mark this task done via `.harness/mark-done.sh`. The building agent must NOT attempt to run the script against production DynamoDB itself, and must end its turn per the `needs-human` protocol (`failed:blocked`) once the script and test exist and are committed.
