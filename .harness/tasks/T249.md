## Do

Fix /reviews/perfumes/edit (src/pages/reviews/perfumes/edit.js) so it visually matches the real perfumes list page (/reviews/perfumes) instead of rendering full-width cards, while keeping the existing top-right 'Edit' button overlay.

Confirmed during exploration (not mentioned/screenshotted by the owner, but the same structural bug): src/pages/reviews/perfumes/edit.js (around lines 62-74) wraps each perfume card (`<Variant6Hybrid item={perfume} />`, NOT `ReviewCard` — perfumes bypass ReviewCard entirely, see src/components/perfumeVariants/Variant6Hybrid.js) in a `<div className="reviews-wrapper">` instead of `<div className="perfume-card-grid">`, which is what the real perfumes list page (src/pages/reviews/perfumes/index.js) uses. `.perfume-card-grid` (src/styles/globals.css around lines 401-410) is real CSS Grid (`display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;`) — unaffected by the column-count sort-order bug, so this is a simple class swap, no JS masonry logic needed.

Do:
1. In `src/pages/reviews/perfumes/edit.js`, change the wrapping `<div className="reviews-wrapper">` (around line 62) to `<div className="perfume-card-grid">`. Leave everything inside it exactly as-is: each item stays wrapped in `<div key={`${perfume.id}-${index}`} className="review-card-with-edit">` containing `<Variant6Hybrid item={perfume} />` and the `<Link href={...} className="edit-button-overlay">Edit</Link>`.
2. Do NOT add search/sort/pagination controls — only the wrapping container class changes.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/perfumes/edit — cards render in the same 2-column CSS Grid layout as /reviews/perfumes (not full-width), each card still shows the pill-shaped 'Edit' button top-right, and clicking it still navigates to /reviews/perfumes/edit/<id>.
