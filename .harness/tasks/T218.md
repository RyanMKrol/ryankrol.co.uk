## Do

In `src/components/ReviewCard.js`, replace every place a raw `item.date`/`item.editedDate` string is currently rendered verbatim with the same value passed through `formatReviewDate` from the new `src/lib/dateFormat.js` helper (added by a prior task in this cluster — import it, e.g. `import { formatReviewDate } from '../lib/dateFormat';`).

There are TWO separate render spots in this file, both need the fix:

1. The `poster-banner` variant (used by the movies and TV list pages, `styleVariant="poster-banner"`) — currently `{item.date && <span className="poster-banner-date">{item.date}</span>}` around line 160. Keep the truthy check on the raw `item.date` (so an empty/missing date still hides the span), but render `formatReviewDate(item.date)` inside the span instead of the raw string.
2. The default (no matching `styleVariant`) fallback render branch near the bottom of the file (`item.date` around line 204-206 rendered as `Date: {item.date}`, and `item.editedDate` around line 210-212 rendered as `Updated: {item.editedDate}`). This branch is NOT dead code — every review type's `edit.js` list page (movies, tv, books, albums) passes `styleVariant={2}` (a numeric value matching none of the three string variants), so all four types' edit-list pages hit this exact branch today with raw dates. Apply the same fix: keep the truthy checks on the raw values, render `formatReviewDate(item.date)` and `formatReviewDate(item.editedDate)` inside the paragraphs instead of the raw strings.

Do not touch `SpineCoverCard` or `SquareCoverCard` (books/albums list-view cards) — adding a date to those is a separate follow-up task in this same cluster. Do not touch any other file.

## Done when

`src/components/ReviewCard.js`'s `poster-banner` variant and its default-branch fallback both render dates via `formatReviewDate(...)` instead of a raw string. `npm run lint`, `npm test`, and `npm run build` all pass. Manual verify: `npm run dev`, open `/reviews/movies` (or `/reviews/tv`) and confirm each card's date now reads like '17 May 2026' instead of '17-05-2026'; then open `/reviews/movies/edit` (or any of the tv/books/albums edit list pages) and confirm the 'Date:'/'Updated:' line there also now reads in the new written format instead of the raw DD-MM-YYYY string. Record both observations in the worklog.
