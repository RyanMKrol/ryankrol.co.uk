## Do

The owner's original ask included 'a full wipe of the DynamoDB workout data + a full reset,' suspecting a schema issue is corrupting or blocking the automatic Hevy fetch. A live, read-only production investigation has since found a much narrower and non-destructive explanation: `getExerciseHistory()` (src/lib/workoutQueries.js:181, the exact query the exercise-detail page reads from via the `exercise_name-workout_date-index` GSI) only finds 3 rows for 'Iso-Lateral High Row (Machine)', but a full scan of the `Workouts` table shows this exercise's real per-set data already exists, denormalized on the `exercises[]` field, on 28 separate Workouts items spanning the same date range. The data was never lost and the Hevy fetch itself is not failing — a specific bug in `src/lib/workoutBackfill.js`'s `storeWorkoutInDynamoDB` (see the sibling `wdi-shared-writes` task) causes the standalone `Exercises` table rows for a given workout to never get (re)written once a Workouts item with that id already exists, even if some of that workout's `Exercises` rows are still missing. Two buildable, purely-additive follow-up tasks (`wdi-repair-exercises` / `wdi-run-repair-exercises`) reconstruct every missing `Exercises` row directly from each Workouts item's own already-present `exercises[]` array — no Hevy re-fetch, no deletion of anything, no full wipe. Given this, a full production wipe-and-reset is very likely unnecessary to fix the reported symptom, and is a genuinely risky, hard-to-reverse action on live user workout history that should not be executed autonomously. Present this reasoning to the owner and get an explicit decision: (a) accept the targeted, additive repair as sufficient and drop the full-wipe idea entirely, keeping this task's resolution as a documented 'no action needed, repair path used instead'; or (b) the owner still wants a full wipe + Hevy re-fetch for some other reason (e.g. wanting a guaranteed clean slate against possible NOT-YET-DISCOVERED corruption beyond the Exercises-table gap) — if so, do NOT execute it in this task; instead scope out what a safe wipe would require (an explicit DynamoDB export/backup step of both `Workouts` and `Exercises` BEFORE any `DeleteCommand`/table-clear, and a human-reviewed dry-run of the resulting re-fetch) as a new follow-up task, which itself must also stay `gate: needs-human` / owner-directed rather than autonomous, given it is a destructive, hard-to-reverse production action.

## Done when

The owner has made and the worklog records an explicit decision between options (a) and (b) above. If (a): no wipe happens, and the reported symptom is left to be fixed by the additive repair tasks. If (b): a new needs-human task has been scoped (not built) describing the required backup-before-wipe safety steps, and this task is closed referencing it.
