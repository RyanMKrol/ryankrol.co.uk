## Do

- Import `MasonryColumns` and `useResponsiveColumnCount` into `src/pages/reviews/perfumes/index.js`.
- Add `const columnCount = useResponsiveColumnCount(2, 700);` inside `Perfumes()` alongside the other page state.
- Replace the plain grid block at `src/pages/reviews/perfumes/index.js:112-116` — currently `<div className="perfume-card-grid">{filteredPerfumes.map((perfume, index) => <Variant6Hybrid key={`${perfume.id}-${index}`} item={perfume} />)}</div>` — with `<MasonryColumns items={filteredPerfumes} columnCount={columnCount} className="perfume-card-grid" columnClassName="perfume-card-grid-col" renderItem={(perfume, index) => <Variant6Hybrid key={`${perfume.id}-${index}`} item={perfume} />} />`.
- In `src/styles/globals.css`, change `.perfume-card-grid` (lines 403-413) the same way as the Albums task: flex row (`display: flex; gap: 1.5rem;`) plus a new `.perfume-card-grid-col` column rule (`display: flex; flex-direction: column; gap: 1.5rem; flex: 1 1 0; min-width: 0;`), mirroring `.poster-banner-grid`/`.poster-banner-grid-col` (`src/styles/globals.css:241-252`). Remove the now-dead `@media (max-width: 700px)` block.
- Do not change the existing sort/filter logic (`sortBy` state at line 17, the sort branches at lines 44-46) or the `PillGroup` sort control, and do not add pagination — this is a pure layout-mechanism swap.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass.
- On `npm run dev`, `/reviews/perfumes` renders as two masonry columns above a 700px viewport width and collapses to one column below it, with cards sized to their own content (no forced stretch when one perfume's `description` is much longer than its neighbor's).
- Both `sortBy` options (title, score) plus the default date sort preserve strict left-to-right, top-to-bottom reading order across the two columns.
