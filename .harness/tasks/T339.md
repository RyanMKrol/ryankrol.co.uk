## Overview
The home page (src/pages/index.js) currently renders all six stat blocks, the collection wall, latest takes, and the rail's workout/shelf/hot-takes panels with zero/empty placeholder values (0, empty arrays, 'No hot takes yet.') the instant the page mounts, then pops to real data all at once when a single batched Promise.all across 7 API endpoints resolves. Replace that zero-value flash with proper skeleton/wireframe placeholders shown while loading, revealed independently per section as each section's own data dependency resolves. This is a re-spec of T330, which blocked for 14 consecutive attempts (both sonnet and opus, every effort rung) purely on a scope-authoring bug: T330's Done when required editing scripts/_visual-harness.mjs (per this repo's mandatory visual-check convention — every new interactive/loading state needs a FLOWS/PAGES entry there) but T330's own `scope` array never listed that file, so the harness's structural scope-creep guard hard-failed every single attempt the moment it touched _visual-harness.mjs, regardless of whether the actual home-page implementation was correct. This follow-up is identical in substance to T330 except scripts/_visual-harness.mjs is now included in scope.

## Do
In src/pages/index.js, replace the single batched Promise.all (currently ~lines 49-78, across movies/tv/books/albums/vinyl/workouts-stats/hot-takes) with 7 independent fetches, each firing immediately and each tracked by its OWN loading flag (default true, flipped false independently in that fetch's own resolution — e.g. moviesLoading/tvLoading/booksLoading/albumsLoading/vinylLoading/workoutStatsLoading/hotTakesLoading). Read the current code carefully to establish the exact dependency mapping before implementing: each of the six stat blocks (Movies/TV/Books/Albums/Vinyl/Workouts counts) almost certainly depends on exactly one fetch each — confirm by reading how each stat block's count is computed. 'The collection wall' mosaic samples across movies/tv/books/albums/vinyl — verify its exact dependency set in the current code and show its skeleton until ALL of those have resolved (a valid random sample needs the full pool). 'Latest takes' depends on the hot-takes fetch only. The rail's workout-stats panel depends on workouts-stats; the shelf panel's actual data source (vinyl, albums, or a combination) must be read from the current code, not assumed, and mapped to the correct flag(s). Build a small reusable skeleton component, src/components/HomeSkeleton.js (following the existing StatBlock/CoverTile presentational-component pattern), covering at least: stat-block skeleton, tile-grid skeleton, card-row skeleton. Style it from existing Collection CSS custom properties in globals.css (no new hardcoded colours) — globals.css already has a `.spinner`/`@keyframes spin` utility (~line 769) with its own `prefers-reduced-motion: reduce` guard (~line 3306); REUSE or extend that existing animation utility for the skeleton's pulse/shimmer effect rather than inventing a new one from scratch, and reuse its existing reduced-motion guard rather than duplicating the CRTOverlay/MatrixRain pattern. Each section reveals its real content the instant its own dependency (or dependencies) resolve — sections must not wait for the slowest fetch. Do not touch the hero/tagline/NowPlaying section (independent data flow, unaffected). Update scripts/_visual-harness.mjs with a new FLOWS/PAGES entry that stages at least one endpoint's fixture response as delayed relative to the others, to prove independent per-section reveal — this file IS in scope for this task, unlike T330.

## Done when
Loading the home page with artificially staggered/delayed fixture responses shows each section revealing independently as soon as its own underlying fetch(es) resolve (e.g. if movies resolves fast and hot-takes resolves slow, the Movies stat block shows real data well before 'Latest takes' does, rather than everything popping in together). No section ever shows '0', an empty grid, or 'No hot takes yet.' as a fake real-data value — only an explicit skeleton placeholder or real data. No visible layout shift when any section's skeleton is replaced by real content. npm run lint and npm test stay green; HomeSkeleton.js ships with a co-located test. scripts/_visual-harness.mjs is updated with a new FLOWS/PAGES entry capturing at least one section's loading state, per this repo's visual-check convention — and scripts/_visual-harness.mjs is explicitly listed in this task's scope so the change does not trip the harness's scope-creep guard.
