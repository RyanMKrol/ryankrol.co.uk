## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/1a.png` for the footer reference (bottom of
the page): social links (instagram / facebook / github / linkedin / last.fm) on the left, "© ryan
krol · made with too much free time" on the right, separated by a hairline border above.

Build `src/components/Footer.js`, extracting the social-links block that currently lives inline in
`src/pages/index.js` (lines ~70-117 — instagram/facebook/github/linkedin/last.fm links with the same
hrefs) into this shared component, restyled per the design (pill-row or plain-text links per the
render, hairline top border, muted ink-soft color, mono or body font per the design's footer
treatment) plus the copyright line. Wire `Footer` into `src/pages/_app.js` so it renders once per
page (same pattern as how `Header`/`Nav` from T146 is wired in) rather than being re-added to every
page individually — do NOT leave the old inline footer markup in `index.js`; task T149 (home page
rebuild) will consume the new shared `Footer` instead, but removing the duplicate now avoids two
footers rendering on the home page in the meantime.

## Done when

- `Footer.js` exists, renders the 5 social links (same hrefs as the current `index.js` inline
  version) + a copyright line, and is wired into `_app.js` so it appears on every page once.
- The old inline footer markup is removed from `src/pages/index.js` (or at minimum doesn't render
  twice on the home page after this change).
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load several different pages (home, a reviews list, the
  vinyl page) and confirm the footer appears exactly once per page with working links. Detailed
  pixel-fidelity tuning is deferred to the Phase 6 visual-audit tasks (T165-T169).
