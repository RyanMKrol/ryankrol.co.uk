## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md` in full (the "Design tokens" section is
the authoritative spec) before starting. This task installs the "Collection" design system as the
site's single palette, replacing whatever now-orphaned default `:root` block T141 left behind.

**Owner-confirmed tradeoff (do not re-litigate):** replacing `:root`'s token vocabulary wholesale
means every page/component not yet rebuilt by a later task (T149-T170) will render unstyled/visually
broken — dozens of existing CSS rules and ~47 page/component files still reference the OLD token
names (`--color-bg`, `--color-text`, `--radius-base`, `--btn-bg`, etc.) that this task deletes. The
owner was presented with an additive alternative (keep old tokens, add new ones alongside, clean up
later) and explicitly chose to replace now and accept the temporary breakage rather than carry
legacy-token debt through the whole migration. Proceed with a full replace as originally specified —
do not add back the old token names "just in case."

**Fonts** — add Google Fonts for `Bricolage Grotesque` (weights 600/700/800), `Nunito` (weights
500-800, plus italic 500 for review text), and `Space Mono` (400/700, italic 400) in
`src/pages/_document.js`'s `<Head>`, alongside the existing `JetBrains Mono` link (Matrix mode's
font — confirm it's still linked; if the old multi-theme font link block T141 left in place still
includes JetBrains Mono, keep that reference, just drop the fonts that were only used by retired
themes: Share Tech Mono, VT323 — unless grep shows they're still referenced anywhere in `src/`, in
which case leave that font link alone and flag it). Also update the hardcoded
`<Html style={{backgroundColor:'#0d1b2a'}}>` in the same file to the new paper background
(`#FBF7EF`) so there's no flash of dark background before CSS loads.

**CSS custom properties** — in `src/styles/globals.css`'s `:root` block, define the full Collection
token set from the README's Design tokens section:
- Color: `--color-bg`/paper `#FBF7EF`, `--color-ink` `#1A1714`, `--color-ink-soft` `#5A544C`,
  `--color-ink-mute` `#8A837A`, `--color-card` `#FFFFFF`, `--color-hairline` `#F0EADF` (and
  `#E7E0D4` for table rows — pick sensible names, e.g. `--color-hairline` and
  `--color-hairline-strong`), `--color-star-gold` `#F4A72C`, `--color-star-empty` `#D8D2C8`.
- Section accents — one custom property per content type: `--accent-movies` `#FF5C39`,
  `--accent-tv` `#4B4DED`, `--accent-books` `#159A8C`, `--accent-albums` `#F4A72C`,
  `--accent-vinyl` `#F25CA2`, `--accent-workouts` `#1A1714`, `--accent-listening`/`--accent-perfumes`
  `#7A5AF8`, `--accent-projects` `#2FA96B`.
  Perfume pip meter: `--pip-filled` `#7A5AF8`, `--pip-empty` `#E1D9F5`. Warmup row:
  `--color-warmup-bg` `#FDF4E3`, `--color-warmup-text` `#B7791F`. Working set row:
  `--color-set-row-bg` `#F4F0E8`.
- Typography: `--font-display: 'Bricolage Grotesque', sans-serif`, `--font-body: 'Nunito', sans-serif`,
  `--font-mono: 'Space Mono', monospace`.
- Shape/elevation: `--radius-card: 18px`-`20px` (pick one sensible default, e.g. `18px`),
  `--radius-pill: 999px`, `--radius-cover: 10px`-`12px`, `--radius-stat: 16px`,
  `--border-width: 2px`, `--border-color: var(--color-ink)`,
  `--shadow-cover: 0 6px 16px rgba(0,0,0,.14)`.
- Spacing: `--gutter-desktop: 40px`, `--gutter-mobile: 18px`.

Keep these as **plain, non-namespaced** custom properties on `:root` (no `data-theme` attribute
needed anymore — Collection is the only palette). The Matrix override block
(`html.matrix-active { ... }`) already overrides a subset of CSS custom property NAMES to produce
the green-on-black terminal look when active — check that block's variable names still line up with
whatever you name things here (rename Matrix's overridden var names too if you rename any base
token it targets, so Matrix mode keeps working; do not change Matrix's actual color values). The
Matrix block never explicitly declared a font — it always inherited whatever `:root` set (previously
Share Tech Mono, incidentally terminal-appropriate). Since the new `--font-body` (Nunito) is not
terminal-appropriate, add an explicit typography override inside `html.matrix-active` pinning
`--font-display`/`--font-body`/`--font-mono` to `'JetBrains Mono', monospace` so the terminal feel
survives the base font change.

Update `CLAUDE.md`'s theming description (the "Theming" bullet under Conventions, and anywhere else
that references the old multi-theme system if any remain) and `README.md`'s equivalent section to
describe the new single Collection palette instead of the retired multi-theme system.

## Done when

- `globals.css`'s `:root` defines the full Collection token set described above (color, section
  accents, typography, shape, spacing) with the exact hex/px values from the design README.
- The three new fonts load via `_document.js` and are usable via `var(--font-display)` /
  `var(--font-body)` / `var(--font-mono)`.
- `<Html>`'s inline background hardcode matches the new paper background.
- `CLAUDE.md` and `README.md` describe the new token system, not the retired one.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load any page, open devtools and confirm
  `getComputedStyle(document.documentElement).getPropertyValue('--color-ink')` (etc.) resolves to
  the new values. Trigger Matrix mode (Konami code) and confirm it still activates correctly on top
  of the new base tokens (green-on-black look, not broken by a token rename).
- This task is gated (`"gate": "gate"`) — every later task in this backlog depends on the token
  names landed here, so get the names right; downstream tasks will reference these exact custom
  property names.
