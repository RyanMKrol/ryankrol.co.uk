## Do

This is the deploy checkpoint for the perfume card variants effort. Vercel's Git integration is disconnected (see root CLAUDE.md's 'Deploying' section) — nothing built by the earlier tasks in this sequence has gone live yet no matter how many commits landed on main. This task makes no code changes; it is a pure operational run (mirrors T083/T171's precedent — scope: [], diff should be empty aside from the worklog). Steps: (1) `git fetch origin && git status` to confirm the local checkout's main matches origin/main exactly, `git pull` first if not. (2) `npm run build` locally as a final pre-deploy sanity check. (3) `vercel whoami` to confirm the CLI is authenticated — if this fails, STOP and record `failed:blocked` with that exact reason (a human must run `vercel login` once; do not attempt a workaround). (4) Run `vercel --prod --yes` non-interactively and capture the production URL from stdout. (5) Confirm the deploy reached a ready state: the CLI's own exit code must be 0, AND `curl -s -o /dev/null -w '%{http_code}' <the-production-url>/reviews/perfumes` must print 200. (6) Objectively confirm the LIVE perfumes page is actually serving the promoted redesign (not a stale build) by fetching its HTML and grepping for a marker unique to whichever outcome shipped — if a variant was promoted, grep for a CSS class/string unique to that variant's markup (e.g. its `.perfume-v{N}-*` class prefix); if the decision was to promote none, grep instead for evidence the page is unchanged (e.g. the pre-existing `perfume-card` class is still present) — pick the exact marker based on what `perfume-variants-promote`'s worklog actually shipped.

## Done when

`vercel whoami` succeeded (or the task correctly stopped with `failed:blocked` if it didn't — either outcome is a valid completion of this task's own scope, but only the former counts as `done`). `vercel --prod --yes` exited 0 and printed a production URL. `curl -s -o /dev/null -w '%{http_code}' <url>/reviews/perfumes` printed 200. The grep check in step 6 found a match against the live production URL's perfumes-page HTML, confirming the deploy actually picked up whatever this effort shipped. The exact production URL, deployment timestamp, and grep result are recorded in this task's worklog.
