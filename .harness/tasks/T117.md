## Do

Same shape as `review-edit-url-id-1`/`-5`/`-7` but for vinyl. Source table `VinylCollection` is currently keyed on a COMPOSITE key `title`+`artist` (both String) — see `src/pages/api/vinyl/add.js` (`Item: { title, artist, thumbnail, lastfm? }`, no explicit key params because it's a `PutCommand`, but the table's actual `KeySchema` is `title`+`artist` per root `CLAUDE.md`'s Data model table). NOTE: unlike the four review types, vinyl currently has NO edit or delete page/route at all (`src/pages/vinyl/` only has `index.js` and `add.js` — confirmed via file listing; there is no `src/pages/api/vinyl/delete.js` or `src/pages/vinyl/edit*`). This idea is specifically about edit-page URL robustness, so vinyl's migration is being done purely for data-model consistency/robustness across all five review-adjacent tables (per the owner's explicit 'let's do all of them' instruction) — it is NOT adding any new edit/delete UI; that would be a different, out-of-scope feature.

Fields to preserve per item: `title`, `artist`, `thumbnail`, and the optional `lastfm` object (same shape as albums — see `README.md:145-169`).

Write `src/scripts/migrateVinylToIdTable.js`, mirroring `src/scripts/migrateAlbumsToV2.js`'s structure. Create `VinylCollectionV2` with `id` (String) as the SOLE HASH partition key (replacing the `title`+`artist` composite); scan all of `VinylCollection`; guard against double-migration; generate `id: randomUUID()` per item, copying every existing field (including nested `lastfm`) unchanged, with `title`/`artist` becoming ordinary non-key attributes; batch-write in chunks of 25; log old-count vs new-count; leave `VinylCollection` fully intact; touch nothing under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js`.

Extract the pure transform into a testable function and add `src/scripts/migrateVinylToIdTable.test.js` covering field preservation, `id` presence, and UUID shape.

## Done when

`npm test` and `npm run lint` pass. Diff touches only the two new script files. `needs-human`: a human must run `node src/scripts/migrateVinylToIdTable.js` against production `VinylCollection`/`VinylCollectionV2`, confirm counts match, and mark this task done via `.harness/mark-done.sh`.
