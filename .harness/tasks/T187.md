## Do

Read the `## Decision` section of `.harness/designs/perfume-card-variants-decision.md` (written by the review task) to find which variant (1-5) won, or whether none did. IF a variant won: in `src/pages/reviews/perfumes/index.js`, replace the current `<ReviewCard item={perfume} type="perfume" ... styleVariant="perfume-card" />` usage with the winning `PerfumeCardVariant{N}` component from `src/components/perfumeVariants/`, rendering each perfume directly (adjust the surrounding container's CSS class/layout in `src/styles/globals.css` if the winning variant needs a different container shape than the current `.perfume-card-grid`, e.g. a list container instead of a grid for the table-row variant — only add/adjust rules needed for this integration, don't restyle unrelated parts of the page). Remove the now-dead `perfume-card` styleVariant branch and its `PerfumeCard` helper function from `src/components/ReviewCard.js` (confirmed safe: `src/pages/reviews/perfumes/edit.js` uses `styleVariant={2}`, which already falls through to ReviewCard's generic default card, not the `perfume-card` branch — verify this is still true before removing). Delete the four LOSING variant files under `src/components/perfumeVariants/` (keep only the winner's file) and remove their now-unused CSS classes from `src/styles/globals.css`. Delete the preview page `src/pages/dev/perfume-variants.js` (no longer needed once a winner is promoted). Confirm search/sort on the perfumes list page (`src/pages/reviews/perfumes/index.js`'s existing search/sort state) still works correctly against the new card. Update `CLAUDE.md`'s file map / conventions and `README.md` if the perfumes list page's presentation or the ReviewCard component's responsibilities changed in a way worth documenting (per this repo's Definition of Done). IF the decision instead says none of the five variants were good enough to promote: do NOT force a promotion — instead delete ALL FIVE files under `src/components/perfumeVariants/`, delete `src/pages/dev/perfume-variants.js`, remove all their CSS classes from `src/styles/globals.css`, leave `src/pages/reviews/perfumes/index.js` and `src/components/ReviewCard.js` exactly as they are today (mirroring T092's precedent of a clean rejection/deletion rather than a forced promotion), and record in the worklog that this task closed out as a rejection, not a promotion.

## Done when

Either (a) the live `/reviews/perfumes` page renders real data through the winning variant's component (confirmed by running `npm run dev`, opening `/reviews/perfumes`, and visually confirming the new presentation, with search and sort still functioning), the four losing variant files and the preview dev page no longer exist, and `ReviewCard.js` no longer contains the dead `perfume-card` branch; or (b) if the decision was to promote none, all five variant files and the preview dev page no longer exist and the live `/reviews/perfumes` page is unchanged from before this whole effort. Either way: `npm run lint` reports no new errors, `npm test` passes, `npm run build` succeeds, and `CLAUDE.md`/`README.md` reflect whatever actually shipped (or explicitly need no change, if outcome (b)).
