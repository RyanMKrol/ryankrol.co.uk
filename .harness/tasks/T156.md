## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Vinyl" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/2b.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4c.png` (mobile).

Rebuild `src/pages/vinyl/index.js`:
- Header: big "vinyl" title, "N records on the shelf · sorted by artist" meta line, `SearchInput`
  (from T143) for search, and a covers/list view toggle (two-state `Pill`-style toggle — "covers"
  shows the `CoverTile` grid described below, "list" shows a plain text row per record; the design
  only mocks the covers view in the renders, list view can be a simple table/row layout of your own
  reasonable design since there's no explicit mock for it).
- Records grouped by artist surname first letter: a section header per letter — big accent-colored
  letter (`var(--accent-vinyl)`, `#F25CA2`) + a hairline rule + "N records" count — reuse the
  existing artist-surname-grouping logic already in this page (check how it currently derives the
  grouping/sort-by-artist behavior before changing the visual treatment).
- A `CoverTile` grid (from T145) under each letter header: 7 columns desktop, 3 columns mobile
  (responsive breakpoint), title + artist caption beneath each tile (via `CoverTile`'s
  `title`/`subtitle` props) — vinyl records already have real cover URLs in the DB for 188/190
  records per the design brief, so most tiles should show real images via `CoverTile`'s `imageUrl`
  prop, falling back to the gradient placeholder only for the few missing covers.

## Done when

- The vinyl page shows the new header/meta/search/covers-list-toggle, artist-letter section headers
  with the vinyl accent color, and a responsive `CoverTile` grid (7-col desktop / 3-col mobile) using
  real cover URLs where available.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/vinyl`, confirm records are grouped by artist
  letter with correct counts, real covers render for records that have them, search filters
  correctly, and the covers/list toggle switches views. Resize to ~390px and confirm the grid
  collapses to 3 columns. Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task
  T167.
