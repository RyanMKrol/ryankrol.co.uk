## Do

ROOT CAUSE (confirmed by reading src/styles/globals.css in full and grepping every className="container"/className="review-container" usage under src/pages/): `.container` (globals.css lines 721-726: `max-width: 800px; margin: 0 auto; padding: var(--spacing-container-padding); background-color: var(--color-surface); min-height: 100vh;`) and `.review-container` (lines 729-734, byte-identical rules) are the pre-Collection-redesign narrow-column wrapper. `--spacing-container-padding` (globals.css line 83) resolves to `var(--gutter-desktop)` (40px) with NO mobile override anywhere, unlike the newer pattern below. Grepping `src/pages/**` shows virtually every page in the site still wraps its content in a bare `className="container"` or `className="review-container"` with no override: `src/pages/vinyl/index.js:73`, `src/pages/projects/index.js:78`, `src/pages/workouts/index.js:132` + `src/pages/workouts/[id].js` (4 uses), `src/pages/exercises/[exerciseName].js` (4 uses), `src/pages/listening/index.js:41`, `src/pages/dev/cache.js`, `src/pages/programmes/index.js`, and every `src/pages/reviews/<type>/{index,add,edit,edit/[id]}.js` for movies/tv/books/albums/perfumes (each ~3 uses per type) plus `src/pages/vinyl/add.js`. Only `src/pages/index.js:107` (`className="container home-container"`) is fixed: T165's worklog (`.harness/worklog/T165.md`, gap #1) added a home-page-only `.home-container` override class (globals.css lines 2853-2864: `max-width: 1400px; padding-left/padding-right: var(--gutter-desktop);` plus a `@media (max-width: 768px)` block swapping those to `var(--gutter-mobile)`) applied ALONGSIDE `.container` rather than widening the shared class, and the worklog explicitly says: "other pages still using the narrow `.container` are out of this task's scope and weren't touched." T166/T167/T168/T169's worklogs (read in full) confirm none of them ever revisited this — T166 fixed page-title font/pill colors, T167 fixed vinyl/listening/projects header alignment + badge casing + link underline (never touched the outer `.container` wrapper width), T168 fixed workout chart/stat styling, T169 fixed dead-CSS-variable resolution for `.container`'s `background-color`/padding VALUE but never touched its `max-width`. So this is a real, confirmed systemic gap, not 3 independent bugs: T165 made a deliberate, scoped decision to fix only home, and no follow-up task was ever filed to do the rest — until now. The design brief (`/Users/ryankrol/Downloads/design_handoff/README.md` line 30: "Desktop (1400px design width...)"; line 83: "Page gutters 40px desktop / 18px mobile") confirms every page should be full-bleed to 1400px bounded only by the gutter tokens, not an 800px centered column. Forms are safe to widen the outer wrapper around: `.collection-form-card` (globals.css ~line 2685) has its OWN independent `max-width: 640px; margin: 0 auto;`, so widening `.review-container` around it only adds breathing room, it does not stretch the form itself. FIX (globals.css only): change both `.container` (lines 721-726) and `.review-container` (lines 729-734) to mirror `.home-container`'s already-proven pattern exactly: set `max-width: 1400px` (up from 800px); replace the shorthand `padding: var(--spacing-container-padding)` with explicit `padding-left: var(--gutter-desktop); padding-right: var(--gutter-desktop);` (retain equivalent top/bottom padding via `padding-top`/`padding-bottom` set to the same `var(--gutter-desktop)` value, so no vertical spacing regresses); add a `@media (max-width: 768px)` block for both selectors swapping `padding-left`/`padding-right` to `var(--gutter-mobile)`, matching the existing `.home-container` breakpoint at globals.css lines 2859-2864. Leave `margin: 0 auto`, `background-color: var(--color-surface)`, and `min-height: 100vh` unchanged on both. Leave `.home-container` and the `className="container home-container"` usage in `src/pages/index.js` exactly as-is (now redundant with `.container` itself, but harmless — removing it is optional cleanup, not required, keep this diff minimal/low-risk). Do not touch any file under `src/pages/**` — every page picks this up automatically via the shared class names.

## Done when

verify: ["run-app"]. Run `npm run dev` and, at a desktop viewport (~1400-1600px wide), load each of: `/`, `/vinyl`, `/projects`, `/reviews/movies`, `/reviews/tv`, `/reviews/books`, `/reviews/albums`, `/reviews/perfumes`, `/workouts`, `/listening` — for every one of these, visually confirm the page's cream background and content (nav pills, heading, grids/lists) now span near-full-viewport-width bounded only by the ~40px gutters, with NO wide pale-cream side margins flanking a narrow centered column (the original `/vinyl` bug the owner screenshotted). Reload at a narrow mobile viewport (~390px) and confirm the side gutters visibly tighten to ~18px instead of staying at 40px. Load an add/edit form page (e.g. `/reviews/movies/add`) and confirm the form itself still renders as a centered ~640px card (via `.collection-form-card`'s own max-width), not stretched full-width — only the page background/gutter widened, not the form. Run `npm run lint` (0 errors), `npm test` (all green), and `npm run build` (succeeds) and keep them green.
