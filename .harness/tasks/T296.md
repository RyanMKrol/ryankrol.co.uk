## Do

Root cause (confirmed in code, not a guess): `src/pages/_app.js` has no persistent layout beyond `MatrixLayout`/`Footer` — every one of the 34 page files above imports `Header` from `../../components/Header` (or equivalent relative path) and renders a bare, prop-less `<Header />` itself, inline in its own JSX. Because there is no shared DOM node bridging pages, Next.js's client-side router fully unmounts the outgoing page's `<Header />` (including its `navRef` scroll position, tracked in `src/components/Header.js`'s `useEffect` at the top of the component, feeding `src/lib/scrollFade.js`'s `shouldShowScrollFade`) and mounts a brand-new one on every navigation — so a nav pill row scrolled to the far right on a narrow viewport always resets to `scrollLeft: 0` after clicking a link.

Fix — promote `Header` to a genuine persistent layout in `_app.js`:

1. In `src/pages/_app.js`, import `Header` from `../components/Header` and render it ONCE, wrapping `<Component {...pageProps} />` the same way `MatrixLayout` already wraps everything (this codebase's existing precedent for a persistent `_app.js`-level wrapper — `MatrixRain`/`CRTOverlay` already survive navigation this way). Header takes zero props anywhere in the codebase today, so no prop-plumbing is needed. Place it inside `MatrixLayout` at the same level `<Component/>` currently sits (i.e. `<MatrixLayout active={matrixActive}><Header /><Component {...pageProps} /><Footer /></MatrixLayout>` — adjust exact placement/wrapper markup only as needed to preserve existing layout CSS; the goal is one Header DOM instance for the lifetime of the app, not per-page).
2. Remove EVERY per-page `<Header />` JSX render site and its now-unused `import Header from ...` in all 34 files listed in scope, INCLUDING every conditional branch in the multi-branch pages:
   - `src/pages/workouts/index.js` — 3 render sites (loading / error / success returns)
   - `src/pages/workouts/[id].js` — 4 render sites
   - `src/pages/exercises/[exerciseName].js` — 4 render sites (loading / error / empty / success)
   - `src/pages/reviews/{albums,books,movies,tv,perfumes}/edit.js` — 3 render sites each (loading / error / success)
   - `src/pages/reviews/{albums,books,movies,tv,perfumes}/edit/[id].js` — 2 render sites each
   - `src/pages/reviews/{albums,books,movies,tv}/backfill.js` — 3 render sites each
   - All other files in scope — 1 render site each
   For each removed `<Header />`, leave the surrounding wrapper `<div>`/`<>...</>` and its other children (Head, loading spinners, error text, page content) exactly as they are — only the `<Header />` line and its now-dead import go away. Do not otherwise restructure these pages.
3. Leave `src/components/Header.js` and `src/lib/scrollFade.js` COMPLETELY untouched — the existing `navRef`/`showNavFade` scroll-fade logic is correct as-is and will simply keep working (and now the scroll position will naturally persist too) once Header stops being torn down on every navigation. Do not add any `sessionStorage` or other explicit persistence — the fix is structural, not a workaround.
4. Known accepted side effect (owner already signed off): `NowPlaying` (rendered inside `Header`) will go from re-fetching on every navigation to polling continuously in the background since it no longer remounts — this is intended, not a bug to prevent.
5. Double check no page in scope ends up with either zero Headers (a removed page erroring, or a branch missed) or two Headers (a leftover render site) after the edit — grep for `<Header` across `src/pages/**/*.js` afterward and confirm the ONLY remaining match is the one in `src/pages/_app.js`.

## Done when

`npm run lint`, `npm test`, and `npm run build` all pass with no new errors. `grep -rn "<Header" src/pages/` returns exactly one match, in `src/pages/_app.js` — every one of the 34 former per-page render sites (including all conditional branches in `workouts/index.js`, `workouts/[id].js`, and `exercises/[exerciseName].js`) is gone, and none of those files retain a dead `import Header from ...`. Then run `npm run dev` and manually verify in a real browser at a narrow viewport (or with devtools narrowed) where the nav pill row overflows and shows the scroll-fade: (1) scroll the nav pill row all the way right so a far-right pill (e.g. "listening" or "projects") is reachable, click it, and confirm the resulting page's nav is STILL scrolled to the same position (no reset to the start) — this is the core bug fix and must visibly work; (2) spot-check that Header (wordmark, nav pills with correct active-section highlighting, NowPlaying widget, mobile hamburger menu) still renders correctly and in the same visual position on at least: the home page, one plain review index page, one add page, and — specifically, since these were multi-branch — `workouts/index.js` (confirm Header appears correctly during its loading spinner, its error state if reachable, and its final loaded state) and `exercises/[exerciseName].js` (confirm Header appears correctly during loading, and for a valid exercise's loaded state); (3) confirm the mobile hamburger menu still opens/closes correctly and still closes itself when a mobile nav link is clicked and navigation occurs; (4) confirm the Matrix easter egg (Konami code) still works with Header now living inside `MatrixLayout`'s wrapper — toggling it still shows/hides `MatrixRain`/`CRTOverlay` without breaking Header's layout or the nav pills' accent colours.
