## Do

## Do
1. In `src/pages/workouts/index.js`: keep the existing `activeFilter` state (line 78, `useState('all')`) as the ONLY Push/Pull/Legs control on the page — do not add a second one. Add ONE new local state, `dateRange` (default `'1y'`), using `DateRangeFilter` (`src/components/DateRangeFilter.js`) and `DATE_RANGES`/`filterByDateRange` from `src/lib/dateRange.js` (both already used by `src/pages/programmes/index.js` today — copy that usage). This `dateRange` control applies ONLY to the new stats/charts block, not to the paginated session list below (which stays exactly as it behaves today — unfiltered by date, filtered only by `activeFilter`).
2. Add a new section between the existing `<div className="collection-review-header">...</div>` block (ends ~line 161) and the `MasonryColumns` session grid (~line 166): import `aggregateProgramme` from `../../lib/programmeStats` and `ProgrammeOverviewCharts` from `../../components/ProgrammeOverviewCharts`. Compute `const statsData = aggregateProgramme(filterByDateRange(allWorkouts, dateRange, w => w.start_time), activeFilter)`. Render the `dateRange` control, then the 3 stat blocks currently at `src/pages/programmes/index.js:100-117` (Total Workouts / Total Volume (kg) / Avg Volume per Session (kg), same markup/classes), then `<ProgrammeOverviewCharts data={statsData} />` (same as `programmes/index.js:119`).
3. Remove the `<Link href="/programmes">view programme stats →</Link>` and its preceding middot at `src/pages/workouts/index.js:149` — the content is now inline on the same page, the link no longer makes sense.
4. Delete `src/pages/programmes/index.js` entirely. Do not add a redirect (personal low-traffic site; simplicity convention). `src/components/ProgrammeOverviewCharts.js` and `src/lib/programmeStats.js` are NOT deleted — they are reused as-is, just imported from `src/pages/workouts/index.js` instead.
5. Confirm `/workouts` still fires only ONE `fetch('/api/workouts?mode=all')` per page load (the existing `useEffect` at workouts/index.js:82-101, already fetching into `allWorkouts`) — both the new stats block and the existing list must read from that same already-fetched `allWorkouts` state; do not add a second fetch.
6. Update `CLAUDE.md`'s file map row for `src/pages/workouts/index.js` (currently: "Paginated workout list with All/Push/Pull/Legs filter") to also mention it now shows programme stats/charts inline at the top, sharing the same Push/Pull/Legs filter plus its own date-range control. There is no existing `src/pages/programmes` file-map row to remove (checked, none exists) — no README.md changes were found referencing `/programmes` either (checked, none exist), so no removal needed there, but confirm this is still true at the time of implementing (re-grep for "programmes" in both files) in case it changed since this task was authored.
7. No new pure logic is introduced (this task is UI composition of already-tested functions — `aggregateProgramme`, `filterByDateRange`, `filterWorkouts` all already have coverage) — do not add speculative new tests, but do NOT break any existing test that references the old `/workouts` header meta line, the `/programmes` link, or `src/pages/programmes`' own test file if one exists (search for and delete/update any co-located `programmes` test file so the suite doesn't reference a deleted page).

## Done when
- Visiting `/workouts` on the local dev server shows, at the top of the page (above the paginated session list): the single shared Push/Pull/Legs filter, a date-range control (3 months / 1 year / all-time) that affects only the section below it, the 3 stat blocks (Total Workouts / Total Volume / Avg Volume per Session), and the 3 `ProgrammeOverviewCharts` charts — all computed for the currently-selected Push/Pull/Legs value, defaulting to 'all' on first load (not 'push').
- Toggling the shared Push/Pull/Legs pill changes BOTH the stats/charts block above AND the paginated session list below at the same time.
- Changing the date-range control changes only the stats/charts block's numbers and chart data — the session list and its pagination are unaffected.
- Visiting `/programmes` returns Next.js's 404 page; nothing anywhere in the app links to `/programmes` any more (grep the repo to confirm zero remaining references outside git history).
- Confirm via the browser Network tab that only one request to `/api/workouts?mode=all` fires per `/workouts` page load.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- `CLAUDE.md`'s file map row for `src/pages/workouts/index.js` describes the merged behavior; a final grep for "programmes" across `CLAUDE.md`/`README.md` finds no stale reference to a standalone page.

## Done when

undefined
