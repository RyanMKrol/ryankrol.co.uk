## Do

## Do

Replace every occurrence of the 🧴 (lotion bottle) emoji with 👃 (nose emoji) at these 5 exact sites. Do NOT change any surrounding text or wording — this is a like-for-like emoji swap only.

1. `src/pages/reviews/perfumes/add.js` line 127 — change:
   `<h1 className="page-title">🧴 Add Perfume Review</h1>`
   to:
   `<h1 className="page-title">👃 Add Perfume Review</h1>`

2. `src/pages/reviews/perfumes/edit.js` line 60 — change:
   `<h1 className="page-title">🧴 Edit Perfume Reviews</h1>`
   to:
   `<h1 className="page-title">👃 Edit Perfume Reviews</h1>`

3. `src/pages/reviews/perfumes/edit/[id].js` line 222 — change:
   `<h1 className="page-title">🧴 Edit Perfume Review</h1>`
   to:
   `<h1 className="page-title">👃 Edit Perfume Review</h1>`

4. `src/components/PerfumeCharacteristics.js` line 120 — inside the `SpraySpotCounter` component, the button glyph itself is currently a bare `🧴` on its own line inside a `<span>` (with a `💦` poof animation rendered elsewhere in the same span via `.map`). Change that bare `🧴` line to `👃`. Do not touch the `💦` poof elements or the animation logic.

5. `src/components/PerfumeCharacteristics.js` line 172 — inside `ApplicationSpotsSprayer`, change:
   `<span className="form-label">Application spots — tap the bottle to spray 🧴💦</span>`
   to:
   `<span className="form-label">Application spots — tap the bottle to spray 👃💦</span>`
   (Leave the wording "tap the bottle to spray" as-is — only the emoji glyph changes. The 💦 stays exactly where it is.)

After editing, run `npm run lint` and confirm no new errors, and `grep -rn "🧴" src/` to confirm zero remaining occurrences of the lotion-bottle emoji anywhere in `src/`.

## Done when

## Done when

- `grep -rn "🧴" src/` returns no matches anywhere in the repo — 🧴 has been fully replaced by 👃 at all 5 sites listed in `## Do`.
- The exact wording/structure around each site is otherwise unchanged (e.g. "Edit Perfume Reviews", "tap the bottle to spray", the 💦 poof animation) — only the 🧴 glyph itself was swapped for 👃.
- `npm run lint` passes with no new errors.
- `npm run build` succeeds.
- Verify per `verify: ["run-app"]`: start the dev server and manually check in a browser that 👃 renders correctly (not a broken/tofu glyph) in all 3 contexts — the `/reviews/perfumes/add` page title, the `/reviews/perfumes/edit` page title, an individual `/reviews/perfumes/edit/[id]` page title, and the spray-spot counter buttons + "Application spots" label on the add/edit forms (tap a spray-spot button and confirm the 👃 glyph is what animates/squeezes, and the 💦 poof still appears above it as before).
