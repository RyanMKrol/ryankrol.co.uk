## Do

Use the shared `Tooltip` component built in the dependency task (`src/components/Tooltip.js`) to add hover/focus tooltips to the two badges in `.project-card-top-right` at `src/pages/projects/index.js:237-246`. Import it the same way sibling components are imported in this file (e.g. `import Tooltip from '../../components/Tooltip';`, matching the existing `../../components/Header` style at the top of the file). Wrap the stars span: `<Tooltip label={`${repo.stars || 0} ${(repo.stars || 0) === 1 ? 'star' : 'stars'}`}><span className="project-card-stars">★ {repo.stars || 0}</span></Tooltip>`, and the commit badge (keep the existing `typeof repo.commitCount === 'number'` guard around the whole `Tooltip`+span, not just the span): `<Tooltip label={`${repo.commitCount} ${repo.commitCount === 1 ? 'commit' : 'commits'}`}><span className="project-card-commits-badge">⎇ {repo.commitCount}</span></Tooltip>`. Tooltip content is deliberately just the spelled-out count noun (e.g. '42 stars', '7 commits') — not richer content — since the glyphs are already self-explanatory to most users and the tooltip's job is purely to spell out what the glyph+number means for clarity/accessibility, not to add new information. Since these badges sit at the top of a card (`.project-card-top-right`), pass `position="bottom"` to the `Tooltip` if visual testing shows the default top-positioned bubble gets clipped by the page/card edge for cards in the first grid row — otherwise leave the default.

## Done when

`npm run lint`, `npm test`, and `npm run build` pass. In `npm run dev`, open `/projects`: hovering the star badge on any card shows a tooltip reading '<N> star(s)' matching the visible count; hovering the commit badge (on a card that has one — `typeof repo.commitCount === 'number'`) shows '<N> commit(s)'; cards without a commit count show no second tooltip and no layout gap/shift. Tab-focusing each badge via keyboard also reveals its tooltip. Confirm the tooltip bubble is not clipped/cut off by the viewport or card edge for cards in the top row of the grid (use `position="bottom"` if it is). Confirm the archived-tag corner badge (`.project-card-archived-tag`) and the rest of the card layout (name, description, language dot, meta row) are visually unaffected — no overlap, no shifted spacing.
