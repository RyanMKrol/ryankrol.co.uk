## Do

Owner confirmed (in response to a conversion-time question): yes, the new `ownership` field (`'Sample' | 'Travel size' | 'Full bottle'`, added to the `PerfumeRatings` schema by the sibling task `perfume-ownership-field-1`) should be surfaced on the read-only perfume review card, not just captured on the form. Today `src/components/perfumeVariants/Variant6Hybrid.js` has zero references to it (nor to the old `considerTravelSize`/`considerFullBottle` booleans it replaces) — this task adds the display only; it does not touch the form pages or the API.

Read `src/components/perfumeVariants/Variant6Hybrid.js` first. Place the new value in the existing rating row (currently lines 47-50):
```jsx
<div className="perfume-v1-rating-row">
  <PipMeter value={item.rating} readOnly />
  <span className="perfume-v1-rating-number">{item.rating}/10</span>
</div>
```
Add a third element to that row, conditionally rendered only when `item.ownership` is set (existing un-migrated rows won't have it yet — render nothing for those, not an empty badge):
```jsx
{item.ownership && (
  <Badge accentColor="var(--accent-perfumes)" variant="soft">{item.ownership}</Badge>
)}
```
`Badge` is already imported at the top of this file (line 1, used for the `item.type` badge in the header) — reuse the same import, no new import needed beyond confirming it's already there. Use `variant="soft"` (tinted background, coloured text) specifically to visually distinguish it from the header's solid `item.type` badge — this card already establishes solid-for-type, and `Badge`'s own doc comment (`src/components/Badge.js`) describes `soft` as the alternate tinted style for exactly this kind of secondary label. The existing `.perfume-v1-rating-row` CSS rule (`src/styles/globals.css`, `display:flex; align-items:center; gap:0.6rem`) already lays out its children in a row with spacing, so the new Badge drops in without any new CSS.

Do not add any new CSS classes/rules, do not touch the form pages (`src/pages/reviews/perfumes/**`), and do not touch the API routes or `src/lib/perfumes.js` — this task is display-only, scoped to the one file listed.

## Done when

- A perfume review item with `ownership: 'Full bottle'` (or `'Sample'`/`'Travel size'`) renders a soft-variant `Badge` showing that exact text in the card's rating row, next to the `X/10` rating number.
- A perfume review item with no `ownership` field (an un-migrated existing row) renders the card exactly as it did before this task — no empty badge, no `undefined` text, no layout shift beyond the badge's own absence.
- The new badge is visually distinguishable from the header's `item.type` badge (soft/tinted vs. the header badge's solid fill) rather than looking like a duplicate.
- `npm run lint` reports no errors.
- `npm run build` succeeds.
- Manual check via the `run-app` verifier: run `npm run dev`, open `/reviews/perfumes`, and visually confirm at least one card shows the ownership badge in the rating row (use a review that already has `ownership` set from testing task 3, or temporarily seed one) and that cards without the field still render cleanly.
