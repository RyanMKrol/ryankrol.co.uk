## Overview
A human decision is required: which of the variants built in medal-restyle-a (Compact tint / Dot indicator / Outline chip, or whichever set actually got built) should become the real PrBadge style. (Capitalization is already resolved — see below, no longer part of this gate.)

## Do
Visit /dev/pr-badge-variants (from medal-restyle-a) locally and pick one variant as the winner. OWNER-RESOLVED, not part of this gate: the weight/volume capitalization tension is settled — capitalize the first letter only ('Weight'/'Volume', sentence-case), superseding the earlier no-uppercase stance (which was about ALL-CAPS text-transform specifically, not sentence-case). '1RM' stays as-is.

## Done when
The chosen variant name/letter is recorded for the hardcode task to consume (medal-restyle-c already has the capitalization treatment locked in and doesn't need it from this gate).
