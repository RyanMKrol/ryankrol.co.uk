## Overview
The workout detail page shows its date StatBlock in stale numeric 'DD-MM-YYYY' form (e.g. '07-07-2026') via its own inline formatter, while the workouts list page already shows the same kind of date correctly in English long-form ('7 July 2026') using the shared formatEnglishDate helper.

## Do
In src/pages/workouts/[id].js, remove the local `formatDate` function (lines ~68-73, the `.toLocaleDateString('en-GB', { day: '2-digit', month: '2-digit', year: 'numeric' }).replace(/\//g, '-')` one) and import/use `formatEnglishDate` from `../../lib/dateFormat` in its place, passing `workout.start_time` straight through (it accepts a Date-parseable value, same as the list page already does at src/pages/workouts/index.js lines 13/42-43). Update the StatBlock at line ~218 (`value={formatDate(workout.start_time)}`) to call the shared helper directly (or keep a one-line local alias if that reads better) so the rendered date StatBlock shows '7 July 2026' style.

## Done when
Visiting a workout detail page (/workouts/[id]) for a workout with a given start_time renders the date StatBlock in English long-form (e.g. '7 July 2026'), not numeric dashed form; the page imports and uses formatEnglishDate from src/lib/dateFormat.js rather than a local inline formatter; no other StatBlock/value on the page changes.
