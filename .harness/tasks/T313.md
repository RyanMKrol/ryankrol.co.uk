## Do

src/pages/reviews/perfumes/index.js is the ONLY review-type list page still using the generic PillGroup component (src/components/PillGroup.js) for its sort control, with a static `SORT_OPTIONS` array (lines 9-13: `{ value: 'date', label: 'date ↓' }, { value: 'title', label: 'title' }, { value: 'score', label: 'score' }` — note the '↓' is hardcoded dead text, not a real arrow) and a sort `useEffect` (lines 47-56) that only ever implements ONE direction per field (title always ascending via localeCompare, score always descending, date/else always descending — no ascending date, no reversed title/score).

This is EXACTLY the bug already fixed on every other review page in commit d3b44c9 (`T215: fix sort pill no-op by switching to flip-direction SortButtons`), which did the identical swap on movies/tv/books/albums but never touched perfumes (perfumes was evidently added or overlooked separately). Mirror that commit's pattern exactly on `src/pages/reviews/perfumes/index.js`:

1. Replace the `PillGroup` import with `SortButtons` (`import SortButtons from '../../../components/SortButtons';`), matching e.g. `src/pages/reviews/movies/index.js:6`.
2. Replace the `SORT_OPTIONS` array (lines 9-13) with a `SORT_FIELDS` array of the `{ key, label, defaultValue, flippedValue, defaultArrow, flippedArrow }` shape used by every other review page, e.g.:
```js
const SORT_FIELDS = [
  { key: 'date', label: 'date', defaultValue: 'date', flippedValue: 'date-asc', defaultArrow: '↓', flippedArrow: '↑' },
  { key: 'title', label: 'title', defaultValue: 'title', flippedValue: 'title-desc', defaultArrow: '↓', flippedArrow: '↑' },
  { key: 'score', label: 'score', defaultValue: 'score', flippedValue: 'score-asc', defaultArrow: '↓', flippedArrow: '↑' },
];
```
3. In the sort `useEffect` (lines 42-60), add the three missing reverse-direction branches (`title-desc`, `score-asc`, `date-asc`) alongside the existing `title`/`score`/else(date) branches, following `src/pages/reviews/movies/index.js:77-105`'s exact branching structure (an `else if` per value, trailing `else` still covers plain `date` and any unrecognized value as newest-first, to preserve current default behavior). Do NOT add the `.replace(/^The\s+/i, '')` title-prefix stripping that movies/albums/books use — perfumes' existing title comparator is a plain `localeCompare` with no such stripping, and that specific behavior isn't part of either reported bug, so keep it as plain `localeCompare`/reversed `localeCompare` only. Use `perfume.rating` for score (0-10 scale here vs 0-5 elsewhere — irrelevant to the comparator, `(b.rating || 0) - (a.rating || 0)` and its reverse work unchanged) and `perfume.date` (no `editedDate` field exists on perfume records per the data model in root CLAUDE.md, so date comparisons use `date` only, not `editedDate || date`).
4. Replace the `<PillGroup options={SORT_OPTIONS} value={sortBy} onChange={setSortBy} />` JSX (lines 101-105) with `<SortButtons fields={SORT_FIELDS} sortBy={sortBy} onChange={setSortBy} />`, matching the prop names `SortButtons` expects (see `src/components/SortButtons.js`).
5. Leave everything else in the file untouched: the search-filter logic in the same `useEffect`, the fetch effect, loading/error states, `MasonryColumns` rendering, and the page's overall structure are all independent of the sort control and must not change.

This single component swap also fixes the hover-styling complaint (idea 2) as a side effect, with no CSS changes needed: `SortButtons` renders `.filter-button` elements, which already have a correct `:hover` rule at `src/styles/globals.css:1545-1548` (`background-color: var(--color-filter-hover-bg); border-color: var(--color-accent);`), unlike `.collection-pill` (rendered by `PillGroup`/`Pill`), which has no `:hover` rule at all (confirmed via grep, `globals.css:2670-2696`). Do not touch `globals.css`, `PillGroup.js`, `Pill.js`, or `SortButtons.js` — the fix is entirely swapping perfumes' page-level usage, not changing any shared component or style. No docs update is needed for this change: neither `CLAUDE.md`'s file map nor `README.md` calls out per-review-type sort-component choice at that granularity, and the precedent fix (T215, also purely a bugfix at this same abstraction level) did not touch docs either.

## Done when

All of the following hold:

1. **Code correctness (read-verify against the movies-page precedent):** `src/pages/reviews/perfumes/index.js` imports `SortButtons` (not `PillGroup`), defines a `SORT_FIELDS` array with exactly three fields (`date`, `title`, `score`) each carrying `defaultValue`/`flippedValue`/`defaultArrow: '↓'`/`flippedArrow: '↑'`, and its sort `useEffect` has an explicit branch for every value `SortButtons` can emit: `title`, `title-desc`, `score`, `score-asc`, `date-asc`, plus a trailing `else` covering plain `date`/unrecognized values as newest-first (mirrors `src/pages/reviews/movies/index.js:77-105` in structure). The JSX renders `<SortButtons fields={SORT_FIELDS} sortBy={sortBy} onChange={setSortBy} />` in place of the old `PillGroup` usage.
2. **Manual click-through verification (`npm run dev`, visit `/reviews/perfumes`):**
   - Starting state: `date` is active with a `↓` arrow shown, newest-first order.
   - Click `date` again: arrow flips to `↑`, order flips to oldest-first (a real reorder is visible, not a no-op).
   - Click `title`: becomes active with `↓` arrow, title ascending (A→Z, `localeCompare`); click `title` again: arrow flips to `↑`, order reverses to descending (Z→A) — both directions must visibly work and the arrow must move.
   - Click `score`: becomes active with `↓` arrow, highest-rated first; click `score` again: arrow flips to `↑`, lowest-rated first — both directions must visibly work.
   - At every point exactly one pill shows an arrow (the active one) and it is the correct glyph for the current direction — no permanently-hardcoded 'date ↓' text remains anywhere.
   - The search box still filters the visible set correctly while a non-default sort is active (confirms the shared `useEffect`'s filter+sort interaction wasn't broken by the restructure).
3. **Hover styling parity (manual, same dev session):** hovering any perfumes sort pill shows the same grey hover background as the reference page `/reviews/movies`'s sort pills (visually compare side-by-side or via devtools computed `background-color` — both should resolve to `var(--color-filter-hover-bg)`), and the active pill's hover state also matches (`--color-filter-active-hover-bg`). No CSS file was modified to achieve this — it must be a natural consequence of both pages now rendering the same `.filter-button` markup.
4. **Standard DoD gates green:** `npm run lint` (0 errors), `npm test` (all passing, no regressions), `npm run build` (succeeds, `/reviews/perfumes` route generated).
5. **No unrelated regressions:** loading state, error state, empty-search behavior, and the `MasonryColumns` card grid for perfumes are unchanged from before the fix (diff should be scoped to the import, `SORT_FIELDS`/sort-branch, and JSX swap only — no other lines touched).
