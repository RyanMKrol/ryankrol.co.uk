## Do

In `src/components/Header.js`, add `'/programmes'` to the `workouts` section's `prefixes` array in `NAV_SECTIONS` (currently `{ key: 'workouts', label: 'workouts', href: '/workouts', prefixes: ['/workouts', '/exercises'] }`) so it becomes `prefixes: ['/workouts', '/exercises', '/programmes']`. This is the ONLY change — do not add a new `NAV_SECTIONS` entry, do not change `getActiveSection`, do not touch routing. `getActiveSection(pathname)` already does `pathname.startsWith(prefix)` against each section's `prefixes`, so this one-line addition is sufficient to make `/programmes` match the `workouts` section.

## Done when

1. `npm run dev`, open `http://localhost:3000/programmes` (or whatever port the dev server picks), and confirm the "workouts" nav pill is now visually highlighted/active (both desktop pill row and, at a narrow viewport, the mobile menu link) — matching how it looks when visiting `/workouts` directly. 2. Confirm no other nav pill (e.g. a phantom "programmes" entry) appears or gets highlighted. 3. `npm run lint` and `npm run build` still pass. 4. Diff is exactly the one-line array change in `src/components/Header.js` (plus any auto-generated lockfile/test noise, which there should be none of here).
