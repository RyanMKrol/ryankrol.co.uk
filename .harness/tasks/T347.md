## Overview
Independent of the sizing/context task above (does not depend on it, ships separately, per the owner's explicit request for two separate tasks — this SPACING unit chain is unaffected by the SIZE unit chain's rework and was already confirmed as-is by the owner: 0.3rem gap, applies to both /workouts and /workouts/[id]). On /workouts (src/pages/workouts/index.js:238-247), when an exercise sets more than one kind of PR in the same session (e.g. Weight+1RM+Volume together, as seen on the Legs page's Hip Thrust row), multiple <PrBadge> elements are mapped back-to-back inside the same <li className="workout-session-exercise-item"> with zero gap/margin between them, so adjacent pills sit visually flush against each other. Confirmed in src/styles/globals.css: .pr-badge (currently gap: 0.2rem; padding: 0.1rem 0.45rem; font-size: 0.7rem) styles the CONTENTS of a single pill (the internal gap between its medal emoji and label) but there is no rule governing spacing BETWEEN sibling .pr-badge elements — .workout-session-exercise-item has no flex/gap layout of its own (it's a plain inline text-flow <li> with white-space: nowrap), and .workout-session-exercise-item .collection-badge only sets vertical-align: middle. NOTE: the sibling sizing task (pr-pill-size-round2-spacing-size-align) introduces a new `.workout-session-exercise-badges` wrapper around the badge group on this same page — if that task has already landed when this one is built, check whether a `gap` on that wrapper already provides inter-pill spacing before adding a second, redundant rule; apply this task's 0.3rem spacing via `margin-left` on `.pr-badge` itself (scoped as `.workout-session-exercise-item .pr-badge` and, if the wrapper exists, also applies fine there since margin-left on the pill still works inside a flex wrapper) so behaviour is correct regardless of build order, and prefer leaving only one non-conflicting source of gap (adjust the wrapper's `gap` down to 0 or remove this rule's redundant margin if both would otherwise stack) — use judgement to avoid doubled spacing, the visual result (even, ~0.3rem gap between adjacent pills) is what matters, not which exact rule produces it.

## Do
In src/styles/globals.css, add a spacing rule between adjacent PR pills within the same exercise-name line. The owner confirmed this should apply everywhere .pr-badge renders (both /workouts and /workouts/[id]), so add `margin-left: 0.3rem;` directly to the existing general `.pr-badge` rule — NOT scoped to `.workout-session-exercise-item` only. This naturally only affects spacing BEFORE each pill (including the first one after the exercise name, which already reads fine since text and pill are visually distinct) and stacks correctly between multiple consecutive pills. Do not restructure workout-session-exercise-item into a flex container and do not touch src/pages/workouts/index.js or src/components/Badge.js — this is a pure CSS-only fix. Do not touch the .pr-badge font-size/padding/gap/background/border properties that the sizing chain (separate task chain) owns. If pr-pill-size-round2-spacing-size-align has already landed and introduced a `.workout-session-exercise-badges` wrapper with its own gap, reconcile as noted above rather than blindly stacking a second margin.

## Done when
On both /workouts AND /workouts/[id], an exercise row with 2+ PR pills (e.g. Hip Thrust on the Legs filter, or any multi-axis-PR exercise in fixture/sample data) shows visible, even spacing between adjacent pills — they no longer sit flush against each other. A single-PR-pill row (e.g. Iso-Lateral High Row) is unaffected in spacing from the exercise-name text before it. Verify via VISUAL_CHECK_ONLY=workouts AND VISUAL_CHECK_ONLY=workout-detail node scripts/visual-check.mjs and visually inspect the resulting screenshot's multi-PR exercise rows. npm run lint / npm test / npm run build all stay green.
