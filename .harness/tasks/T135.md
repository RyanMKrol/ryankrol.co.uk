## Do

`src/components/WorkoutCard.js` currently has NO link anywhere to the single-workout detail page `src/pages/workouts/[id].js` (confirmed by reading the whole file — the only `<Link>` elements are the per-exercise links around lines 95-107, `href={`/exercises/${encodeURIComponent(exercise.title)}`}`, and those only render at all inside `{exercises.length > 0 && (...)}` at line 90, i.e. only when `workout.exercises` is a non-empty array). This means whenever a workout's denormalized `exercises` field is empty/missing on the `Workouts` DynamoDB item — which is currently true for a real, confirmed set of production records (see the sibling task in this same backlog batch for the root cause and forward-fix) — the card offers NO way to click through to that workout's detail page at all, even though `/workouts/[id].js` fetches its exercise breakdown independently via `/api/workouts/[id]/exercises` (which queries the `Exercises` DynamoDB table directly by `workout_id` via the `workout_id-index` GSI, per `src/lib/workoutQueries.js`'s `getExercisesByWorkout`) and can therefore show full exercise detail for a workout REGARDLESS of whether the `Workouts` item's own `exercises` field happens to be populated.

Add a workout-detail link to `WorkoutCard` that does NOT depend on `exercises` being present — e.g. wrap the card's title (`<h3 className="review-title">` around line 65) in a `next/link` `Link` to `/workouts/${workout.id}` (the `[id]` route already exists and works today), or add an equivalent always-visible 'View details' affordance elsewhere on the card. This must render for EVERY workout card, complete or not, so a viewer can always click through to see full detail even when the card's own summary stat line / per-exercise breakdown is a zero-state.

Keep the existing per-exercise links to `/exercises/${exercise.title}` exactly as they are (those jump to a specific exercise's cross-workout history page — a different feature) — this task only ADDS a workout-level link, it doesn't replace or restructure anything else on the card.

## Done when

- `WorkoutCard` renders a link to `/workouts/${workout.id}` for every workout, regardless of whether `workout.exercises` is present/empty/undefined.
- Verify manually in `npm run dev`: render/inspect a workout card for a workout with no `exercises` key (e.g. construct a fixture `{id: 'x', title: 'Legs', start_time: '...', end_time: '...'}` in a scratch render, or use a real zero-stat card on `/workouts` if one currently exists in dev data) and confirm a link is present and clicking it navigates to `/workouts/<id>` without a console error; remove any scratch verification code before finishing (no component-test harness exists yet in this repo, matching the precedent set by task T081).
- The existing per-exercise links / stat line rendering for workouts that DO have `exercises` is unchanged (no regression).
- `npm run lint`, `npm test`, and `npm run build` all pass.
