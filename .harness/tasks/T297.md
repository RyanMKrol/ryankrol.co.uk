## Do

The `/hot-takes` page (shipped by tasks T269-T272) is not reachable from the persistent header nav. `src/components/Header.js` lines 8-18 define a hardcoded `NAV_SECTIONS` array — the single source of both the desktop pill row (`.collection-nav-pills`, rendered ~line 74-83) and the mobile menu (`.nav-mobile-menu`, rendered ~line 102-115) — and it has no entry for hot-takes. Its 9 existing entries all share one object shape: `{ key, label, href, prefixes }` (e.g. the `vinyl` entry: `{ key: 'vinyl', label: 'vinyl', href: '/vinyl', prefixes: ['/vinyl'] }`). `getActiveSection` (line 20-25) matches `router.pathname` against each entry's `prefixes` array to decide which pill is highlighted active, and each pill's colour comes from `accentColor={`var(--accent-${section.key})`}` (line 78) — i.e. it looks up a CSS custom property named `--accent-<key>` generically, with no per-entry special-casing.

Do the following:

1. In `src/styles/globals.css`, add a new CSS custom property `--accent-hot-takes: #C1443B;` to the `:root` block, directly alongside the existing `--accent-*` tokens (currently lines 26-34: `--accent-movies`, `--accent-tv`, `--accent-books`, `--accent-albums`, `--accent-vinyl`, `--accent-workouts`, `--accent-listening`, `--accent-perfumes`, `--accent-projects`). Use exactly this value (a brick-red, distinct from every existing accent, including `--accent-movies: #FF5C39` which is a brighter orange-red) so the new pill has a visually distinct identity. Do NOT add an `html.matrix-active` override for this token — checked the existing pattern (~line 460 onward, the `/* Matrix Theme Override */` block): none of the other per-section `--accent-*` tokens get a matrix override, only the semantic `--color-accent`/`--color-accent-secondary` tokens do, so this new token should follow that same precedent and be left alone under matrix mode.
2. In `src/components/Header.js`, add one new entry to the `NAV_SECTIONS` array (lines 8-18), following the exact same shape as the existing 9 entries: `{ key: 'hot-takes', label: 'hot takes', href: '/hot-takes', prefixes: ['/hot-takes'] }`. Position: append it as the last entry (after `projects`) — ordering isn't load-bearing, but appending is the simplest, lowest-risk placement and avoids reshuffling the existing row.

Do not touch `getActiveSection`, the pill-rendering JSX, or the mobile-menu JSX at all — both already map generically over `NAV_SECTIONS`, so the one array addition is picked up by both surfaces automatically with no other code change required.

No documentation update is required for this task: it does not add/remove/rename a page, change an API route or data flow, or introduce a new convention — `CLAUDE.md`'s file map and data model sections are unaffected by adding one nav entry.

## Done when

- `src/styles/globals.css`'s `:root` block defines `--accent-hot-takes: #C1443B;` alongside the other per-section accent tokens.
- `src/components/Header.js`'s `NAV_SECTIONS` array contains exactly one new entry: `{ key: 'hot-takes', label: 'hot takes', href: '/hot-takes', prefixes: ['/hot-takes'] }`.
- Manual verification (required — `verify: ["run-app"]`): run `npm run dev`, open the site in a browser. On any page, the top nav pill row now shows a "hot takes" pill rendered in its own distinct colour (not reusing another section's accent). Clicking it navigates to `/hot-takes`. While on `/hot-takes`, the "hot takes" pill shows the same active-state visual treatment (e.g. filled/highlighted) that other pills show when their own section is active (compare against e.g. the "vinyl" pill while on `/vinyl`). Also open the mobile menu (the ☰ toggle button, or resize the viewport to a narrow/mobile width) and confirm the "hot takes" link appears there too, in the same position relative to the other links as it does in the desktop row.
- `npm run lint`, `npm test`, and `npm run build` all succeed with no new errors or warnings beyond the pre-existing tolerated `react-hooks/exhaustive-deps` warnings.
