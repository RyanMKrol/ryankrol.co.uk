## Do

This is a **visual audit and tweak** task, not a from-scratch build — the reviews list pages already
exist (built in T150-T155) and pass their own functional Definition of Done. Your job here is
different: compare the LIVE running pages against the design renders pixel-by-pixel and make
free-form CSS/markup tweaks to close the gap. This is where visual fidelity actually gets enforced in
this backlog.

**Reference material:**
- `/Users/ryankrol/Downloads/design_handoff/README.md` — the token/spec doc.
- Movies: `renders/2a.png` (desktop), `renders/4b.png` (mobile).
- Books: `renders/3a.png` (desktop), `renders/4f.png` (mobile).
- TV: `renders/3b.png` (desktop), `renders/4g.png` (mobile).
- Albums: `renders/3c.png` (desktop), `renders/4h.png` (mobile).
- Perfumes: `renders/3d.png` (desktop), `renders/4i.png` (mobile).
(all under `/Users/ryankrol/Downloads/design_handoff/`)

**Process:**
1. `npm run dev`, load each of the 5 list pages at desktop and mobile viewports, comparing against
   its paired render: header/title treatment, meta line, search box, sort/filter pills (active-state
   fill), card variant styling for each type (poster-banner for movies/tv, horizontal-spine for
   books, 2-col-square for albums, pip-meter card for perfumes), pagination control styling.
2. Pay particular attention to the 4 `ReviewCard` variants living in one component
   (`src/components/ReviewCard.js`) — confirm each variant's spacing/typography/color independently
   matches its render, and that no variant accidentally leaked styling into another.
3. Confirm the perfumes page's 0-10 scale explainer chip and pip meter match the design exactly
   (grape/lilac colors, dot count, chip copy) and that the dashed empty-state card is present and
   styled correctly.
4. For every gap found, fix it directly — prefer fixing shared components (`ReviewCard.js`,
   `Pagination.js`, `Pill.js`) over page-specific overrides when the gap is systemic.

## Done when

- All 5 list pages (both breakpoints) visually match their paired renders closely — exact hex colors,
  correct fonts, correct card-variant treatment, correct pill/pagination styling.
- `npm run lint`, `npm test`, and `npm run build` all pass after your tweaks.
- No functional regression: search, sort, pagination still work correctly for all 4 paginated types
  after your CSS/markup changes.
- Document in your worklog which specific gaps you found and fixed.
