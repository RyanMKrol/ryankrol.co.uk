## Overview
The dev-only comparison page src/pages/dev/perfume-card-variants.js (T343, 'delete once a winner is picked' for the pending T344 review) renders each of its 4 layout variants in its own raw CSS grid (`display: grid; gridTemplateColumns: repeat(auto-fit, minmax(280px, 1fr)); gap: 1rem`), which packs 3 cards per row at typical desktop widths. The real production perfume page (src/pages/reviews/perfumes/index.js) uses a different mechanism entirely to render exactly 2 equal-width columns above 700px (1 below): `const columnCount = useResponsiveColumnCount(2, 700)` (src/hooks/useResponsiveColumnCount.js) feeding a `<MasonryColumns items={...} columnCount={columnCount} className="perfume-card-grid" columnClassName="perfume-card-grid-col" renderItem={...} />` (src/components/MasonryColumns.js + src/lib/masonryColumns.js's distributeRoundRobin), styled by `.perfume-card-grid` (flex, 1.5rem gap) and `.perfume-card-grid-col` (flex column, flex: 1 1 0, 1.5rem gap) in src/styles/globals.css (lines ~417-428). The user asked for 'the same width as the actual perfume card page... two per row, not three' -- the fix should adopt this exact same mechanism (hook + component + existing CSS classes) rather than just tweaking the raw grid's minmax value, so the comparison page is a true width/gap match and doesn't risk misleading the upcoming T344 layout-winner review with drifted styling.

## Do
In src/pages/dev/perfume-card-variants.js: import `MasonryColumns` from '../../components/MasonryColumns' and `useResponsiveColumnCount` from '../../hooks/useResponsiveColumnCount' (mirror the relative-path style already used for the `Variant6Hybrid` import in this same file). Call `const columnCount = useResponsiveColumnCount(2, 700);` once at the top of the component (same call the real perfume page makes). For each of the 4 VARIANTS sections, replace the raw `<div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))', gap: '1rem' }}>...</div>` block with a `<MasonryColumns items={perfumes} columnCount={columnCount} className="perfume-card-grid" columnClassName="perfume-card-grid-col" renderItem={(perfume, index) => (<Variant6Hybrid key={`${key}-${perfume.id}-${index}`} item={perfume} variant={key} />)} />`, preserving the `variant={key}` prop each variant section already passes (the real page's usage doesn't pass `variant` since it only ever renders one baseline layout -- this dev page still needs to, per-section, so each section keeps its own distinct `key`/`variant` closure). Do not touch src/styles/globals.css or src/components/MasonryColumns.js/src/lib/masonryColumns.js/src/hooks/useResponsiveColumnCount.js -- reuse them as-is, they already produce the 2-column/1.5rem-gap layout the real page uses. Do not change src/pages/reviews/perfumes/index.js.

## Done when
The dev page at /dev/perfume-card-variants renders each of the 4 variant sections through MasonryColumns + useResponsiveColumnCount(2, 700), using the perfume-card-grid/perfume-card-grid-col classes, so above a 700px viewport it shows exactly 2 equal-width columns per section (matching the real /reviews/perfumes card width and 1.5rem gap) instead of 3; `npm run lint` and `npm run build` pass; `node scripts/visual-check.mjs` is run and the dev page's screenshot is reviewed to confirm 2 equal-width columns (this page/style change is auto-covered by VISUAL_VERIFY_HOOK).
