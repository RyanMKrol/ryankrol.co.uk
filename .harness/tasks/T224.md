## Do

Convert `.poster-banner-grid` in `src/styles/globals.css` (currently `display: grid; grid-template-columns: repeat(3, 1fr); gap: 1.5rem;` around line 240, plus its `@media (max-width: 900px) { grid-template-columns: 1fr; }` override around line 247) from CSS Grid to CSS multi-column, replicating the EXACT pattern already shipped for `.projects-grid`/`.project-card` in commit 796102b (T210 — read that commit's diff in git history for the reference implementation before starting). This ONE class is shared by BOTH `src/pages/reviews/movies/index.js` and `src/pages/reviews/tv/index.js` (via the `poster-banner` styleVariant), so this single CSS change fixes both pages simultaneously — do not duplicate the fix per page. Replace the grid declaration with `column-count: 3; column-gap: 1.5rem;`, and change the `max-width: 900px` override from `grid-template-columns: 1fr` to `column-count: 1;`. On `.poster-banner-card` (same file, around line 183 — note it already has `display: flex; flex-direction: column; overflow: hidden;` etc., leave those untouched), add `break-inside: avoid;` and `margin-bottom: 1.5rem;` to replace the row-spacing the old grid `gap` provided, matching `.project-card`'s treatment exactly. Do not touch `src/pages/reviews/movies/index.js`, `src/pages/reviews/tv/index.js`, or any other CSS selector — pure CSS change, no JS/markup edits.

Accepted, INTENTIONAL tradeoff — do not treat this as something to additionally solve: CSS multi-column fills column 1 top-to-bottom, then column 2, then column 3, so with movies/TV paginated at `PAGE_SIZE = 9` (`src/pages/reviews/movies/index.js:17`, `src/pages/reviews/tv/index.js:17`) across 3 columns and uneven card heights, the per-column item-count split will not always be perfectly even. This is the SAME tradeoff already accepted for `.projects-grid` in T210 and is being deliberately carried over rather than solved with a JS-computed column-balancing scheme, per this repo's 'keep it simple' convention. Do NOT attempt a JS/measured masonry solution — out of scope for this task.

## Done when

`npm run lint` reports 0 errors (existing warnings tolerated, no new ones introduced). `npm run build` succeeds. `npm test` still passes in full (no new test required). `run-app` manual check: run `npm run dev`, open `/reviews/movies` AND `/reviews/tv` (both must be checked, since they share the one CSS class) with enough reviews to fill at least one full page (9 items), and confirm on both pages (a) a short-review card in the same row-position as a long-review card no longer stretches to match the long card's height, (b) shorter cards' following siblings visually flow up into the freed space, and (c) narrowing the viewport below 900px collapses the list to a single column with no visual breakage. `git diff --stat` shows only `src/styles/globals.css` changed, and within it only the `.poster-banner-grid` and `.poster-banner-card` rules (plus their existing media-query block) differ from before.
