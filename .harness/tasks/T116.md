## Do

Precondition: `review-edit-url-id-7` has produced a human-verified `AlbumRatingsV3` table (partition key `id`, replacing the old `title`+`artist` composite key).

Current state — same fragile `'|'`-delimited id scheme as books: `src/pages/reviews/albums/edit/[id].js:36-40` does `decodeURIComponent(id)` then `const [title, artist] = decodedId.split('|')` then `albums.find(a => a.title === title && a.artist === artist)`. `src/pages/reviews/albums/edit.js:73` builds it with `encodeURIComponent(album.title + '|' + album.artist)`. UNLIKE movies/tv/books, `src/pages/api/reviews/albums/update.js` uses an `UpdateCommand` (not delete+`PutCommand`) keyed `{ title: originalTitle, artist: originalArtist }` (lines 53-63), updating only `rating`/`highlights` and optionally `thumbnail`/`lastfm` — it does NOT currently support changing `title`/`artist` at all (both inputs are `disabled` in `src/pages/reviews/albums/edit/[id].js`, confirmed at the `title`/`artist` `<input>`s). `src/pages/api/reviews/albums/delete.js` takes `{ title, artist, password }`, keyed `{ title, artist }` (lines 11-30).

Changes: (1) `src/lib/constants.js`: `ALBUM_RATINGS_TABLE: 'AlbumRatingsV2'` → `'AlbumRatingsV3'`. (2) `add.js`: generate `id: randomUUID()` on create (alongside existing `title`/`artist`/`rating`/`highlights`/`date`/`thumbnail`/`lastfm` fields). (3) `update.js`: accept `originalId` (replacing `originalTitle`+`originalArtist`); keep using `UpdateCommand` but with `Key: { id: originalId }` — DO NOT add title/artist editability (that's a separate, out-of-scope concern; the fields stay `disabled` in the UI and this task must not change that). (4) `delete.js`: accept `{ id, password }`, `DeleteCommand` keyed `{ id }`. (5) `index.js`: no logic change (table swap automatic via constants.js). (6) `edit.js`: replace the fragile `album.title + '|' + album.artist` id-building (line 73) with plain `album.id`; update the list `key` at line 65 to `album.id`. (7) `edit/[id].js`: replace the `decodedId.split('|')` + dual-field `.find()` (lines 36-40) with a single `decodeURIComponent(id)` compared against `a.id`; replace `originalTitle`/`originalArtist` in the submit payload (lines 133-134) with `originalId: originalData.id`; replace `title`/`artist` in the delete payload (lines 181-182) with `id: originalData.id`. (8) Update `src/pages/api/reviews/albums/add.test.js` to assert a generated UUID `id`. (9) Docs: update `CLAUDE.md`'s Data model row for `AlbumRatingsV2` (now `AlbumRatingsV3`, partition key `id`), and rewrite the `#### AlbumRatingsV2 Table` section in `README.md` (`README.md:117-144`) to `#### AlbumRatingsV3 Table` reflecting the new single `id` key with `title`/`artist` as plain attributes.

## Done when

`npm run lint`, `npm test`, `npm run build` all pass. Add/edit/delete for album reviews work end-to-end keyed by `id` — no more `'|'`-delimited composite ids anywhere in the diff. `/reviews/albums/edit/<id>` resolves by `id`. `src/lib/constants.js`'s `ALBUM_RATINGS_TABLE` points at `'AlbumRatingsV3'`. `grep -r AlbumRatingsV2 src/` returns no matches. `CLAUDE.md`/`README.md` updated. Diff includes a changed test file. Title/artist remain non-editable in the UI exactly as before (no scope creep into making them editable).
