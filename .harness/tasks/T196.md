## Do

Change the books review list (`src/pages/reviews/books/index.js`) from a single-column stack of horizontal spine-cover cards to a 2-column grid of the same cards, matching the pattern already used for albums' 2-col square-cover-grid.

Context: `.spine-cover-list` (`src/styles/globals.css:308-312`) is currently `display:flex; flex-direction:column; gap:1rem` — one full-width horizontal `SpineCoverCard` per row (`src/components/ReviewCard.js`'s `SpineCoverCard`, rendered via `styleVariant="spine-cover"` at `src/pages/reviews/books/index.js:147`, wrapped by the `.spine-cover-list` div at line 140). This was an intentional 1-col design (see `design/README.md`: 'Books = horizontal cards with spine cover + author/pages/year metadata') but the owner now wants it as two columns of tiles.

Decision (already made — do not re-litigate): keep the EXISTING `SpineCoverCard` shape (spine-cover-tile image + title/rating + author·pages·year meta + italic review snippet, all in a horizontal flex row) rather than switching books to the `square-cover` variant used by albums. Reasoning: `SpineCoverCard`'s internal DOM is ALREADY a horizontal flex row (image left, body right) — structurally identical to `SquareCoverCard`'s layout, just with a portrait spine image and book-specific metadata instead of square art and a highlights line. Achieving 'two columns of tiles' therefore only requires changing the CONTAINER from a flex column to a 2-col grid — exactly mirroring `.square-cover-grid` (`src/styles/globals.css:373-383`): `display:grid; grid-template-columns:repeat(2,1fr); gap:1.5rem`, collapsing to `1fr` under a narrow-viewport media query, matching the album grid's breakpoint convention. Switching to the square-cover variant instead was rejected: it would drop the book-specific pageCount/year metadata line (`SquareCoverCard` has no such field, only author + a highlights-style excerpt built for albums' comma/period-split track lists), and would make the books list visually indistinguishable from the albums list, losing books' distinct portrait-spine visual identity.

Steps:
1. In `src/styles/globals.css`, change the `.spine-cover-list` rule (currently flex/column, ~line 308) to a 2-column CSS grid: `display:grid; grid-template-columns:repeat(2,1fr); gap:1.5rem` (match `.square-cover-grid`'s gap value), plus a media query (check `.square-cover-grid`'s own breakpoint at ~line 379, `max-width:700px`, and reuse the same breakpoint for consistency) collapsing to `grid-template-columns:1fr` on narrow viewports.
2. Load `/reviews/books` (`npm run dev`) and visually check the existing `SpineCoverCard` (`src/components/ReviewCard.js`) still reads well at roughly half its previous width: title, star rating, 'Author · Npp · Year' meta line, and the italic review snippet. If the review snippet text wraps awkwardly or makes cards in the same grid row uneven/cramped at the narrower width, make a minimal CSS adjustment inside `.spine-cover-card`/`.spine-cover-body`/`.spine-cover-snippet` (e.g. font-size, line-height, or truncation) — do not restructure the card's layout or its underlying data fields, and do not touch any other `ReviewCard` variant.
3. Optional, only if it improves clarity and you choose to do it: rename `.spine-cover-list` to `.spine-cover-grid` for accuracy (update both `globals.css` and the className reference in `src/pages/reviews/books/index.js:140`) — purely cosmetic, not required for the task to be done.
4. Leave `SpineCoverCard`'s rendered fields, the books page's search/sort/pagination logic, and every other `ReviewCard` variant (`square-cover`, `poster-banner`, default) untouched.
5. Per root CLAUDE.md's Definition of Done: this is a pure CSS layout change with no data-model or architecture implications, so no `CLAUDE.md`/`README.md` update is expected — but if you notice either doc now describing a stale fact as a side effect of this change, fix it in the same commit.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass with no new errors.
- verify: ["run-app"]: start the dev server (`npm run dev`), open `/reviews/books`, and confirm books now render as a 2-column grid of spine-cover tiles (not a single-column stack) at desktop width, collapsing back to a single column on a narrow/mobile viewport (resize the window or use devtools device emulation below the chosen breakpoint). Confirm each card still shows the cover tile, title, star rating, 'Author · Npp · Year' metadata, and review snippet legibly at the narrower per-card width — no text overflow breaking the card border, no visually broken/overlapping rows.
- Confirm search, sort (date/title/score), and pagination on `/reviews/books` still work exactly as before (enter a search term, switch sort pills, page through if more than one page of results).
- Confirm no other `ReviewCard` variant/page (`/reviews/movies`, `/reviews/tv`, `/reviews/albums`, `/reviews/perfumes`) regressed — spot check at least the albums page (`square-cover` variant) since its CSS pattern is being mirrored here.
