## Do

Same shape as the movies migration task (`review-edit-url-id-1`) but for TV: the source table is `TelevisionRatingsV3` (keyed on `title` alone — see `src/pages/api/reviews/tv/update.js:32-38`, `delete.js:24-29`), the fields to preserve are `title`, `rating`, `review_text`, `date`, plus optional `tmdbId`/`mediaType`/`posterPath`/`tmdbOverview`/`tmdbDate` (see `src/pages/api/reviews/tv/update.js:61-71`).

Write `src/scripts/migrateTvToIdTable.js`, mirroring `src/scripts/migrateAlbumsToV2.js`'s structure (self-contained `DynamoDBClient`, `dotenv.config({ path: '.env.local' })`, region `'us-east-2'`, `createTableIfNotExists`) and `src/scripts/createPerfumeTable.js`'s single-`id`-HASH-key `CreateTableCommand` shape (`PAY_PER_REQUEST` billing). The script must: create `TelevisionRatingsV4` (id as sole HASH String key) idempotently; scan all of `TelevisionRatingsV3`; guard against double-migration (abort if the target already has items, unless `--force`); generate `id: randomUUID()` per item (Node's built-in `require('crypto').randomUUID()`) and copy every existing field unchanged into the new table; batch-write in chunks of 25; log a final old-count vs new-count summary; leave `TelevisionRatingsV3` fully intact; touch NOTHING under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js` — this task only adds the two new files.

Extract the pure old-item -> new-item-with-id transform into a testable function (mirror `src/scripts/migrateOpenLibraryBooksToGoogleBooks.test.js`'s pattern) and add `src/scripts/migrateTvToIdTable.test.js` asserting field preservation, `id` presence, and UUID shape.

## Done when

`npm test` and `npm run lint` pass. Diff touches only `src/scripts/migrateTvToIdTable.js` + its test file. This is a `needs-human` task: a human must actually run `node src/scripts/migrateTvToIdTable.js` against production `TelevisionRatingsV3`/`TelevisionRatingsV4`, confirm the item counts match, and mark this task done via `.harness/mark-done.sh` — the agent must not run it against production itself.
