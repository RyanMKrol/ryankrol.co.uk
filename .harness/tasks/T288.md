## Do

Archived repos currently get only a small soft-text `Badge` in `src/pages/projects/index.js`'s meta row (lines 268-272): `{repo.archived && (<Badge accentColor="var(--color-ink-mute)" variant="soft" mono={false}>archived</Badge>)}`, conditional on `repo.archived` (`{ archived: Boolean(repo.archived) }` already threaded through from `src/pages/api/github/repos.js:91` — no API changes needed here).

1. In `src/styles/globals.css`, add `position: relative;` to the existing `.project-card` rule (~lines 1767-1777).
2. In `src/pages/projects/index.js`, as the FIRST child inside the `<div key={repo.fullName} className="project-card">` (line 224) — i.e. before the existing `.project-card-top` div — add: `{repo.archived && (<div className="project-card-archived-tag">archived</div>)}`.
3. Remove the existing archived `Badge` block at lines 268-272 from `.project-card-meta-row` — the corner tag fully replaces it; do not show the archived signal twice on one card.
4. In `src/styles/globals.css`, add a new rule near the `.project-card` rules (~line 1777):
   `.project-card-archived-tag { position: absolute; top: 0; left: 0; padding: 0.25rem 0.6rem; background-color: var(--color-ink-mute); color: var(--color-bg); font-family: var(--font-mono); font-size: 0.7rem; letter-spacing: 0.05em; text-transform: uppercase; border-top-left-radius: var(--radius-card); border-bottom-right-radius: var(--radius-card); }`
   — this reuses existing design tokens only (`--color-ink-mute`, `--color-bg`, `--font-mono`, `--radius-card`); do not introduce a new color.
5. Leave every non-archived-related part of the card (name, stars, commit count, language, date, forks, tags) untouched, and leave `src/pages/api/github/repos.js` untouched (the `archived` field is already threaded through).

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass.
- Running `npm run dev` and opening `/projects`: any archived repo's card shows a solid grey 'ARCHIVED' flag flush in its top-left corner (an archived repo can be identified by which cards currently render the old soft-text 'archived' badge, before this change removes it), clearly distinguishable at a glance from non-archived cards.
- The old inline soft-text 'archived' Badge no longer appears in the meta row of any card.
- Non-archived cards are visually unaffected — no stray corner tag, no layout shift, no changed spacing in the meta row.
