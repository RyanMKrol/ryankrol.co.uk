## Overview
On /workouts/[id], a set row's weight x reps text shifts left whenever that row also carries a PrBadge, because the number and the badge(s) currently share one right-hand flex span (`.workout-set-row-value`) -- sibling rows with no badge stay flush right, so the numeric column doesn't line up down the exercise card.

## Do
In src/pages/workouts/[id].js (the set-row render around the `prBadges` array and the two-span `.workout-set-row-label` / `.workout-set-row-value` row, currently ~lines 244-278), restructure each `.workout-set-row` so the numeric value text (`formatSetDisplay(set)`) always renders in the same fixed horizontal position regardless of whether that row has PR badges. Per the owner's confirmed direction, place the row's `prBadges` in a new region BETWEEN the existing left set-label span and the right-hand value span, rather than inline after the number. Add any new CSS needed in src/styles/globals.css (e.g. a 3-column flex layout for `.workout-set-row`, or a fixed-width/right-aligned value span) instead of inline styles, following the existing `.workout-set-row*` class naming.

## Done when
On /workouts/[id] for a workout with a PR'd set (e.g. Chest Press or Chest Fly in a 'Push' workout), the weight x reps text lines up at the same horizontal position across every set row in that exercise card -- rows with a PR badge and rows without one -- verified by comparing rendered/computed x-position of the value text between a badge row and a non-badge row in the same card. Non-PR rows are visually unchanged from today (same spacing/position).
