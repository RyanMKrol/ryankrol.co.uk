## Do

`src/scripts/migrateWorkoutData.js`'s own `storeWorkoutInDynamoDB` (lines ~82-115) and `storeExerciseInDynamoDB` (lines ~117-155) are a third independent reimplementation of the same write logic, called per-exercise in a loop by `migrateWorkoutData()` (lines ~157-200). Import the shared `storeWorkoutInDynamoDB` (which internally calls the shared `storeExercises` for the full `exercises[]` array) from `../lib/workoutBackfill.js` (now exported by the `wdi-shared-writes` task) and delete this file's own two local functions plus the per-exercise loop that calls `storeExerciseInDynamoDB` — `migrateWorkoutData()`'s per-workout loop should just call the shared `storeWorkoutInDynamoDB(workout)` once per workout. As a direct, positive side-effect this script also inherits the shared function's self-heal behavior (from `wdi-shared-writes` and the earlier T134) for free, purely by delegating instead of reimplementing — which is exactly the point of consolidating onto one write path. As in the sibling `wdi-consolidate-audit` task, simplify the end-of-run summary rather than trying to preserve the exact current per-exercise stored/skipped counters, since the shared function doesn't expose that granularity.

## Done when

migrateWorkoutData.js no longer defines its own storeWorkoutInDynamoDB/storeExerciseInDynamoDB functions or issues its own PutCommands — migrateWorkoutData() calls the imported storeWorkoutInDynamoDB from src/lib/workoutBackfill.js once per workout. A new src/scripts/migrateWorkoutData.test.js mocks '../lib/workoutBackfill' and asserts migrateWorkoutData() calls storeWorkoutInDynamoDB exactly once per workout with the raw Hevy workout object. The workout:migrate-data npm script still resolves. npm run lint, npm test, and npm run build all pass.
