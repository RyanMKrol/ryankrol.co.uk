## Do

Precondition: task `review-edit-url-id-1` has already created `MovieRatingsV4` (partition key `id`, String) and a human has verified it contains a full, correct copy of `MovieRatingsV3`'s data with a generated `id` per item. This task switches the live app over to that new table and to `id`-based lookups, replacing the current title-based scheme.

Current state (for reference): `src/lib/constants.js` has `MOVIE_RATINGS_TABLE: 'MovieRatingsV3'`. `src/pages/api/reviews/movies/update.js` does a `GetCommand` keyed `{ title: originalTitle }` to preserve `date`, conditionally `DeleteCommand`s the old item if `title !== originalTitle`, then `PutCommand`s the new item (lines 32-78). `src/pages/api/reviews/movies/delete.js` takes `{ title, password }` and `DeleteCommand`s keyed `{ title }`. `src/pages/reviews/movies/edit/[id].js` fetches the whole list and does `movies.find(m => m.title === decodedTitle)` (lines 34-38), submits `originalTitle: originalData.title` on update (line 116) and `title: originalData.title` on delete (line 163). `src/pages/reviews/movies/edit.js` links to `/reviews/movies/edit/${encodeURIComponent(movie.title)}` (line 73) and keys the React list by `${movie.title}-${index}` (line 65). Note the movie title `<input>` is already `disabled` in the edit form (`src/pages/reviews/movies/edit/[id].js` — the `title` field), so in practice titles are never actually changed via the UI today — the delete-old/recreate-new dance in `update.js` is effectively dead code, which this task removes.

The reference pattern to mirror already exists in this codebase for the `perfumes` review type, which already uses a real synthetic `id` as its DynamoDB partition key: `src/pages/api/reviews/perfumes/add.js` (sets `id` on create), `src/pages/api/reviews/perfumes/delete.js` (keys `DeleteCommand` by `{ id }`), and `src/pages/reviews/perfumes/edit/[id].js` (`decodeURIComponent(id)` then `perfumes.find(p => p.id === decodedId)`, submits `originalId: originalData.id`). IMPORTANT DIFFERENCE: perfumes' `id` is a DETERMINISTIC hash recomputed from `title+designer+type` on every save (`perfumeId(...)` in `src/lib/perfumes.js`, recomputed in `src/pages/api/reviews/perfumes/update.js:76-87`, with an `idChanged` delete-old branch). Do NOT copy that recompute pattern here — movies' new `id` must be a RANDOM UUID assigned once and NEVER regenerated on update, because the whole point of a real id is that it stays stable even if the title is later edited. This means `update.js` needs no delete-old/recreate-new branch at all once keyed by `id`.

Changes to make:
1. `src/lib/constants.js`: change `MOVIE_RATINGS_TABLE: 'MovieRatingsV3'` to `MOVIE_RATINGS_TABLE: 'MovieRatingsV4'` (keep the constant's NAME the same — everything importing `DYNAMO_TABLES.MOVIE_RATINGS_TABLE` now automatically points at the new table).
2. `src/pages/api/reviews/movies/add.js`: generate `id: randomUUID()` (`require('crypto').randomUUID()` or `import { randomUUID } from 'crypto'`) on every new item, alongside the existing fields.
3. `src/pages/api/reviews/movies/update.js`: accept `originalId` instead of `originalTitle` in the request body; `GetCommand` keyed `{ id: originalId }` to preserve `date`; `PutCommand` the full item (same `id: originalId`, never a new one) — remove the title-changed delete-old branch entirely.
4. `src/pages/api/reviews/movies/delete.js`: accept `{ id, password }` instead of `{ title, password }`; `DeleteCommand` keyed `{ id }`.
5. `src/pages/api/reviews/movies/index.js`: no logic change needed (it already does `scanTable(DYNAMO_TABLES.MOVIE_RATINGS_TABLE)`, which now resolves to `MovieRatingsV4` via the constants.js change) — just confirm returned items include `id`.
6. `src/pages/reviews/movies/edit.js`: change the edit link at line 73 to `` `/reviews/movies/edit/${encodeURIComponent(movie.id)}` ``; change the list `key` at line 65 to `movie.id`.
7. `src/pages/reviews/movies/edit/[id].js`: replace the title-decode-and-find (lines 34-38) with `decodeURIComponent(id)` compared against `m.id`; change the update submit payload's `originalTitle: originalData.title` (line 116) to `originalId: originalData.id`; change the delete payload's `title: originalData.title` (line 163) to `id: originalData.id`.
8. Update `src/pages/api/reviews/movies/add.test.js` to assert the created item has a generated, UUID-shaped `id`.
9. Docs (same commit, per root `CLAUDE.md`'s Definition of Done): update `CLAUDE.md`'s Data model table row for `MovieRatingsV3` to describe `MovieRatingsV4` (partition key `id`, plus the existing fields as regular attributes). Add a `#### MovieRatingsV4 Table` section to `README.md`'s Database Schema (there is currently NO movies/tv section in README at all — this is a pre-existing docs gap; add one now, mirroring the format of the existing `#### AlbumRatingsV2 Table` / `#### BookRatingsV3 Table` sections at `README.md:117-198`).

## Done when

`npm run lint`, `npm test`, and `npm run build` all pass.
Adding, editing (rating + review text), and deleting a movie review all work end-to-end keyed by `id` (manually traceable through the diff/tests — no DynamoDB access needed to verify the code paths).
`/reviews/movies/edit/<id>` resolves the correct review by `id`; the old title-shaped URL scheme (e.g. `/reviews/movies/edit/Annihilation`) is no longer how lookups work — this is the intended breaking change.
`src/lib/constants.js`'s `MOVIE_RATINGS_TABLE` now points at `'MovieRatingsV4'`.
`grep -r MovieRatingsV3 src/` returns no matches (nothing reads/writes the old table any more, though the old table itself is left alone at rest for rollback).
`CLAUDE.md` and `README.md` both describe the new `MovieRatingsV4` table and its `id` key.
The diff includes a changed/added test file (`add.test.js` asserting a generated `id`).
