## Overview
src/components/WorkoutCard.js is dead code — imported nowhere in the app (verified: no import of WorkoutCard outside the file itself). The /workouts per-session card is rendered inline in src/pages/workouts/index.js, NOT via this component. It also carries a stale warmup-excluding volume calc that surfaced during idea #4 investigation. Remove the file so there is no confusing dead surface, and update the docs that reference it.

## Do
Delete src/components/WorkoutCard.js in full. It is imported nowhere and has no co-located test file, so nothing else needs updating in code. Do NOT remove any CSS classes it referenced (e.g. exercise-item, exercise-title, workout-meta, workout-stats, workout-detail-link, review-title) — those are shared with the live workout pages (src/pages/workouts/index.js and src/pages/workouts/[id].js) and remain in use. In CLAUDE.md, remove WorkoutCard from the Components list in the file map so the docs no longer reference a deleted file.

## Done when
src/components/WorkoutCard.js no longer exists; nothing in src/ imports WorkoutCard (grep confirms zero references); no CSS classes were removed (they are shared and still used by the live workout pages); CLAUDE.md no longer lists WorkoutCard as a component; npm run lint, npm test, and npm run build all pass.
