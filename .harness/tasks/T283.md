## Do

Replace the two independent 'Consider travel size' / 'Consider full bottle' checkboxes on both perfume review forms with a single mutually-exclusive picker for the new `ownership` field (`'Sample' | 'Travel size' | 'Full bottle'`, added to the backend by the dependency task `perfume-ownership-field-1` — that task adds `validateOwnership` to `src/lib/perfumes.js` and makes both `/api/reviews/perfumes/add` and `/api/reviews/perfumes/update` accept an optional `ownership` field instead of the two old booleans).

1. **Add a new shared component to `src/components/PerfumeCharacteristics.js`**, following the exact pattern already used there for `LongevitySlider`/`ProjectionSlider`/`SeasonsCheckboxes` (each is a small exported function component taking `{ value, onChange }` and used identically by both `add.js` and `edit/[id].js`). Add:
   ```js
   import PillGroup from './PillGroup';

   const OWNERSHIP_OPTIONS = [
     { value: 'Sample', label: 'Sample' },
     { value: 'Travel size', label: 'Travel size' },
     { value: 'Full bottle', label: 'Full bottle' },
   ];

   export function OwnershipPicker({ value, onChange }) {
     return (
       <div className="form-group">
         <span className="form-label">What I own</span>
         <PillGroup options={OWNERSHIP_OPTIONS} value={value} onChange={onChange} />
       </div>
     );
   }
   ```
   `PillGroup` (`src/components/PillGroup.js`) is this site's existing single-select controlled component (used today for sort/filter pill rows) — it renders one `Pill` button per option and calls `onChange(option.value)` on click; it needs no adaptation to work inside a form (its buttons are already `type="button"`, so clicking one won't submit the form).

2. **`src/pages/reviews/perfumes/add.js`**: import `OwnershipPicker` alongside the other imports from `'../../../components/PerfumeCharacteristics'` (currently line 5). In the `formData` initial state (currently lines 10-24), replace `considerTravelSize: false,\n    considerFullBottle: false,` with `ownership: 'Sample',` (default to the lightest/most-common case, matching how `type` already defaults to `PERFUME_TYPES[0]`). Delete the two checkbox `<div className="collection-form-group">` blocks (currently lines 228-250). In their place, render `<OwnershipPicker value={formData.ownership} onChange={(ownership) => setFormData({ ...formData, ownership })} />` — position it in the same spot the checkboxes occupied (between the Fragrantica URL field and `LongevitySlider`).

3. **`src/pages/reviews/perfumes/edit/[id].js`**: import `OwnershipPicker` the same way (alongside line 5's import). In the `formData` initial state (currently lines 13-27), replace `considerTravelSize: false,\n    considerFullBottle: false,` with `ownership: 'Sample',`. In the fetched-data hydration (currently lines 53-67, the `setFormData({...})` call inside `fetchPerfumeReview`), replace `considerTravelSize: !!perfume.considerTravelSize,\n          considerFullBottle: !!perfume.considerFullBottle,` with `ownership: perfume.ownership || 'Sample',` — existing rows that haven't been migrated yet (no `ownership` field) fall back to `'Sample'` rather than showing nothing selected. Delete the two checkbox blocks (currently lines 315-337) and render `<OwnershipPicker value={formData.ownership} onChange={(ownership) => setFormData({ ...formData, ownership })} />` in the same spot.

4. `handleInputChange` in both files is generic (`name`/`value`/`checkbox` based) and is no longer used for this field since `OwnershipPicker` calls `onChange` directly with the picked value (same pattern as `handleRatingChange`/`handleSeasonsChange` already do for `PipMeter`/`SeasonsCheckboxes`) — no changes needed to `handleInputChange` itself, just don't wire the deleted checkboxes to it anymore.

Do not touch the API routes, `src/lib/perfumes.js`, or `Variant6Hybrid.js` (the read-only card) in this task.

## Done when

- Neither `src/pages/reviews/perfumes/add.js` nor `src/pages/reviews/perfumes/edit/[id].js` references `considerTravelSize`/`considerFullBottle` anywhere.
- Both forms render a single row of three pill buttons ('Sample' / 'Travel size' / 'Full bottle') via the new `OwnershipPicker`, exactly one of which is highlighted/active at a time, and clicking a different one switches the active pill without submitting the form.
- On the add form, 'Sample' is active by default on page load.
- On the edit form, loading an existing review whose stored data has no `ownership` field shows 'Sample' as active (not all three unselected); loading one that does have `ownership` shows that value active.
- Submitting either form sends `ownership: '<picked value>'` in the POST body (verify via the network tab or by reading the code path) and no longer sends `considerTravelSize`/`considerFullBottle`.
- `npm run lint` reports no errors.
- `npm run build` succeeds.
- Manual check via the `run-app` verifier: run `npm run dev`, open `/reviews/perfumes/add`, confirm the three-pill picker renders and behaves as a single-select (clicking each pill in turn always leaves exactly one active), fill out the rest of the required fields plus the site password and submit, then confirm the new review appears with the picked ownership value round-tripped correctly by opening it via `/reviews/perfumes/edit`.
