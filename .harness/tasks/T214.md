## Do

In `src/pages/workouts/index.js`, replace `formatVolumeTonnes(volumeKg)` (currently `(volumeKg / 1000).toFixed(1)`, rendered as e.g. "8.6t volume") with a kg-based formatter instead. Use a rounded integer kg display (matching the convention the home page's gym panel already uses via `Math.round`, e.g. `src/pages/index.js`'s `bestSessionVolume` display) — i.e. `Math.round(volumeKg)` rendered with a `kg` suffix (e.g. "8600kg volume"), not a decimal. Rename the function to something accurate (e.g. `formatVolumeKg`) and export it so it's unit-testable, and update its one call site (currently around line 199, `{formatVolumeTonnes(getTotalVolume(exercises))}t`) to the new function + `kg` suffix. This is local to this one file only — no other page references the old tonnes formatter. This task depends on the workouts session-card layout task (`workouts-session-card-layout`), which relocates where/how the volume figure is rendered in this same file — build this task's kg-formatting change against whatever the volume markup looks like after that layout change lands, not the current bottom-stats-row layout.

## Done when

1. `npm run lint`, `npm test`, and `npm run build` all pass. 2. A colocated test (e.g. `src/pages/workouts/index.test.js`, extending the same file used for the exercise-preview test if that task's test file already exists) covers the new formatter: given a volume in kg (e.g. `8600`), it returns the correctly rounded integer kg value/string (e.g. `"8600"` for exact and rounding behavior for a fractional kg input like `8600.4` vs `8600.6`). 3. `npm run dev`, open `/workouts`, and visually confirm each session card now shows its total volume in kg (integer, e.g. "8600kg") rather than tonnes (e.g. no more "8.6t"). Record what was checked in the worklog.
