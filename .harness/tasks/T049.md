# T049 — Variant 1 — vinyl pages

## Do

Read the **"## Variant 1"** section of `.harness/designs/T042-ui-rewrite-briefs.md` (produced
by T042) for this variant's layout/IA/visual direction — only that section, not the other two
variants' briefs. T043 already built this variant's shared layout wrapper
(`src/components/v1/**`) and home page (`src/pages/v1/index.js`) — reuse that wrapper here,
do not build a second one.

Build the vinyl collection list and the add-vinyl form — currently src/pages/vinyl/index.js and vinyl/add.js in production, under this variant's route namespace: /v1/vinyl and /v1/vinyl/add. Fetch data from
the EXISTING API routes, unchanged (e.g. `GET /api/reviews/movies`, `GET /api/vinyl`,
`GET /api/workouts`, `GET /api/listening`-equivalent, `GET /api/github/repos` as applicable to
this chunk) — do not duplicate, modify, or add any file under `src/pages/api/**`. Style and
organise these pages per Variant 1's brief (list-view/detail-view/form description,
information-density approach, typography/hierarchy) — this is the actual creative work for this
chunk; the brief is the spec for HOW these pages should look and be organised.

This task's scope is strictly `src/pages/v1/**` and `src/components/v1/**` — do not modify
any file under the equivalent production paths (e.g. `src/pages/reviews/**`) or any file under
`src/pages/api/**`.

## Done when

- The pages at /v1/vinyl and /v1/vinyl/add render real data (the same data the production equivalent pages
  show, fetched from the same unchanged API routes) using Variant 1's shared layout wrapper
  from T043, organised/styled per that variant's brief (list-view and detail/form-view
  descriptions, density, typography/hierarchy).
- Loading each of these routes in a running dev server (`npm run dev`) shows working pages with
  no console errors and no 404s for routes this task is responsible for.
- No file outside `src/pages/v1/**` or `src/components/v1/**` was modified, and no file
  under `src/pages/api/**` was touched.
