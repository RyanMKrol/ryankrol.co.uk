## Do

Owner decision (already settled): reuse the existing hour-count / `PROJECTION_LABELS` copy verbatim in the new tooltips — no new copywriting. Step 1 — `src/components/PerfumeCharacteristics.js:23-28`: change `const PROJECTION_LABELS = {...}` to `export const PROJECTION_LABELS = {...}` (it's currently private; `ProjectionSlider` at line 55 keeps using it unqualified, unaffected by adding `export`). Step 2 — `src/components/perfumeVariants/Variant6Hybrid.js`: import the shared `Tooltip` (`import Tooltip from '../Tooltip';`, matching this file's existing `import Badge from '../Badge';` relative style) and `import { PROJECTION_LABELS } from '../PerfumeCharacteristics';`. In the `hasLongevity` block (lines 72-83), wrap the `.perfume-v4-scale-track` div (not just the fill) so hovering anywhere along the bar's width triggers the tooltip, not only over the filled portion: compute `const longevityHours = item.longevity >= LONGEVITY_MAX ? `${LONGEVITY_MAX}+` : item.longevity;` and `const longevityLabel = `${longevityHours} ${item.longevity === 1 ? 'hour' : 'hours'}`;`, then `<Tooltip label={longevityLabel}><div className="perfume-v4-scale-track">...</div></Tooltip>` (keep the existing `.perfume-v4-scale-fill` div exactly as-is inside). In the `hasProjection` block (lines 85-96), same pattern: `<Tooltip label={PROJECTION_LABELS[item.projection]}><div className="perfume-v4-scale-track">...</div></Tooltip>`. This reuses exactly the copy the owner already sees on the add/edit forms (`LongevitySlider`'s `{value >= 8 ? '8+' : value}` and `ProjectionSlider`'s `{PROJECTION_LABELS[value]}`) with no new strings authored.

## Done when

`npm run lint`, `npm test`, and `npm run build` pass. In `npm run dev`, open a perfume review with both longevity and projection set (`/reviews/perfumes`): hovering the Longevity bar shows a tooltip with the hour count matching what that same value would show on the edit form's `LongevitySlider` (e.g. a longevity of 8 shows '8+ hours', a longevity of 3 shows '3 hours', a longevity of 1 shows '1 hour' — singular handled correctly); hovering the Projection bar shows the matching `PROJECTION_LABELS` word (e.g. projection 3 → 'Strong'), identical to what `ProjectionSlider` shows while editing that same perfume. Tab-focusing each bar via keyboard also reveals its tooltip. Confirm hovering ANYWHERE along the bar's track width (not just over the coloured fill portion) triggers the tooltip. Confirm the endpoint labels above each bar ('Light ↔ All-day', 'Skin scent ↔ Room-filling') and the rest of the card (rating row, seasons chips, application spots, Fragrantica link) are visually unaffected. Confirm the add/edit perfume form (`/reviews/perfumes/add`) still works unchanged — `LongevitySlider`/`ProjectionSlider` behavior is untouched by the `export` keyword addition.
