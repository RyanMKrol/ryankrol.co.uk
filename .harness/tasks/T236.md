## Do

This is the safe, targeted alternative to a full DynamoDB wipe (see `wdi-decision-wipe`): the real per-set data for every affected exercise already exists on the `Workouts` table's own `exercises[]` field, it just never got copied into the standalone `Exercises` table for most sessions. Create `src/scripts/backfillMissingExerciseRows.js`, following the dry-run-by-default / `LIVE=1`-for-real-writes pattern already used by `src/scripts/backfillIncompleteWorkoutMetrics.js` (same `.env.local` dotenv loading, same paginated `ScanCommand` helper style). Logic: (1) scan the full `Workouts` table; (2) for every item with a non-empty `exercises` array, query the `Exercises` table's `workout_id-index` GSI for that `workout_id` (same pattern as `backfillIncompleteWorkoutMetrics.js`'s `getExerciseRowsForWorkout`) to find which of the workout's `exercise_id`s (`${workout.id}_${index}` for each index < exercises.length) already exist; extract this comparison into a small pure, directly-testable function, e.g. `missingExerciseIndexes(exerciseCount, existingRows)` returning the list of indexes with no matching row; (3) in dry-run mode (default), only report counts — total workouts scanned, total missing exercise-row count, per-workout breakdown for any workout missing more than zero rows — and make NO writes; (4) in `LIVE=1` mode, for every Workouts item that has at least one missing index, call the shared `storeExercises` (exported by the `wdi-shared-writes` task from `../lib/workoutBackfill.js`) with that Workouts item's own shape (`{id, start_time, end_time, title, exercises}` — a Workouts item already carries all of these fields directly) — `storeExercises` already loops every exercise in the array and does a per-exercise conditional `PutCommand` (`attribute_not_exists(exercise_id)`), so calling it is purely additive: rows that already exist fail their condition harmlessly and are left untouched, and only the genuinely missing rows get created; add a `sleep(150)` between workouts to match the existing rate-limiting convention; log a final summary (workouts processed, exercise rows created). Add the npm script `"workout:backfill-missing-exercises": "node src/scripts/backfillMissingExerciseRows.js"` to package.json, matching the existing `workout:backfill-metrics` entry's naming convention. This task only builds and tests the script — it does NOT run it against production; that is the separate, dependent `wdi-run-repair-exercises` operational task.

## Done when

src/scripts/backfillMissingExerciseRows.js exists, defaults to a dry-run (no writes) unless LIVE=1 is set, and its pure comparison function (e.g. missingExerciseIndexes) is exported and covered by a new src/scripts/backfillMissingExerciseRows.test.js with cases for: no rows missing, some rows missing, all rows missing, and a workout with zero exercises (nothing to check). package.json has the new workout:backfill-missing-exercises script. The script makes no DeleteCommand calls anywhere (purely additive). npm run lint, npm test, and npm run build all pass.
