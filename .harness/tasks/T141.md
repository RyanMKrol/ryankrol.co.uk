## Do

This repo is mid-way through a full visual redesign to a new design language nicknamed
"Collection" (spec at `/Users/ryankrol/Downloads/design_handoff/README.md` + the pixel-exact
`/Users/ryankrol/Downloads/design_handoff/Ryan Krol Homepage.dc.html`). Collection is becoming the
site's ONE AND ONLY design — there is no plan to keep multiple selectable themes.

Delete the existing multi-theme "AppearancePicker" system entirely:
- `src/components/AppearancePicker.js` (the modal itself) — delete the file.
- The global 🎨 button and `<AppearancePicker isOpen=.../>` render in `src/components/MatrixLayout.js`
  — remove both, and the `pickerOpen` state that drives them. `MatrixLayout` should go back to just
  rendering `MatrixRain`/`CRTOverlay`/children based on the `active` prop it already receives — do
  not change how `active` is computed or passed in (that's owned by `useKonamiCode`, unaffected by
  this task).
- `src/pages/_app.js` — this file directly imports and calls `useTheme()` (`import useTheme from
  '../hooks/useTheme'` + a bare `useTheme();` call in the component body, to fire the appearance
  system's side effects on mount). Remove that import and the call entirely. Do NOT touch this
  file's `useKonamiCode()` call, its `MatrixLayout` usage, or anything else in it — this is a
  2-line removal, not a rewrite.
- `src/hooks/useTheme.js` — delete the file.
- `src/lib/appearance.js` and `src/lib/appearance.test.js` — delete both files.
- `src/styles/globals.css` — remove the theme-palette-override blocks entirely: the `:root`
  cyberpunk default block, the `html[data-theme="X"]` blocks for all 5 themes (terraria, cyberpunk,
  cotton-candy, sunset, ocean) including their `[data-mode="light"]` sub-overrides, the
  `data-font` overrides section, and the `.ap-*` "Appearance Picker Modal" styles. Do NOT touch the
  Matrix theme override block (`/* ===== Matrix Theme Override ===== */`) — leave it byte-for-byte
  as-is. After this task, `globals.css` should have NO theme-selection machinery left — just a
  single default `:root` block (which task T142 will repopulate with the new Collection tokens;
  leaving whatever minimal `:root` remains here is fine, T142 owns getting the values right) plus
  the untouched Matrix override.
- `src/pages/_document.js` — remove the `prePaintScript`'s theme/mode/font/motion logic entirely
  (the `localStorage.getItem('appearance-theme')`, `data-theme`/`data-mode`/`data-font`/`data-motion`
  attribute-setting lines). KEEP the Matrix-mode restore line
  (`if(sessionStorage.getItem('matrix-active')==='true'){h.classList.add('matrix-active')}`) — that
  line must survive completely unchanged, in a still-synchronous pre-paint script (do not defer it
  into a `useEffect`, it exists specifically to avoid FOUC). It's fine if the resulting script is
  much shorter — just the matrix-active restore wrapped in the same try/catch IIFE pattern. Leave
  the `<Html style={{backgroundColor:'#0d1b2a'}}>` inline background hardcode as-is for now — task
  T142 (which installs the new palette) will update that value, not this task.

Grep the whole `src/` tree for `useTheme`, `AppearancePicker`, `appearance-theme`, `appearance-mode`,
`appearance-font`, `appearance-motion`, `data-theme`, `data-font`, `data-motion` and confirm nothing
outside the files listed above references any of this machinery. `data-mode` and
`prefers-reduced-motion` handling: confirm `MatrixRain.js` reads
`window.matchMedia('(prefers-reduced-motion: reduce)')` directly (it does, independent of
`data-motion`) — nothing to change there.

## Done when

- `AppearancePicker.js`, `useTheme.js`, `lib/appearance.js`, `lib/appearance.test.js` no longer
  exist. `grep -rn "AppearancePicker\|useTheme\|appearance-theme\|appearance-mode\|appearance-font\|appearance-motion" src/` returns nothing.
- `globals.css` has no `html[data-theme=` blocks and no `.ap-` classes remaining, except the
  Matrix override block which is untouched.
- `_document.js`'s pre-paint script no longer sets `data-theme`/`data-mode`/`data-font`/`data-motion`,
  but still restores `matrix-active` from `sessionStorage` synchronously pre-paint.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load the home page, confirm the 🎨 button is gone and no
  console errors reference the deleted modules. Trigger the Konami code (↑↑↓↓←→←→BA) and confirm the
  Matrix rain + CRT overlay still activate exactly as before — this is the one behavior that must be
  100% preserved. Reload the page after activating Matrix mode and confirm it's still active
  (sessionStorage restore still works).
- This task is pure deletion/removal — do not add any new Collection-palette CSS values here, that's
  T142's job. It's fine (expected) if the page looks visually broken/unstyled after this task; the
  next task fixes that.
