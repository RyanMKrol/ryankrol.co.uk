## Overview
The homepage's 'This year at the gym' rail panel renders totalVolume, bestSessionVolume, and totalWorkouts as raw digit strings with no thousands separators (e.g. '1109621kg' instead of '1,109,621kg'), making large numbers hard to read at a glance.

## Do
In src/pages/index.js, apply `.toLocaleString()` to the three stat values rendered in the 'home-gym-panel' block (around lines 227, 232, 239): `workoutStats?.totalWorkouts` (sessions), `totalVolume` (total volume), and `bestSessionVolume` (best session volume). Follow the exact same inline `.toLocaleString()` pattern already used elsewhere in this codebase for volume numbers (see src/pages/workouts/index.js:196/202, src/pages/workouts/[id].js:218, src/pages/exercises/[exerciseName].js:231, src/components/WorkoutCard.js:87) — there is no shared formatting helper in src/lib today, so do not introduce one; call `.toLocaleString()` directly at each render site. Preserve the existing null/undefined handling exactly as-is: `totalVolume ?? null` / `bestSessionVolume || null` at lines 129-130 stay unchanged, and the `{totalVolume ?? '—'}` / `{totalVolume && <span className="home-gym-stat-unit">kg</span>}` conditional rendering at lines 232-233 and 239-240 stays structurally the same — only the number itself gets `.toLocaleString()` called on it before display (e.g. `totalVolume?.toLocaleString() ?? '—'`). Do the same for `workoutStats?.totalWorkouts ?? 0` at line 227, i.e. `(workoutStats?.totalWorkouts ?? 0).toLocaleString()`.

## Done when
All three stat values in the 'This year at the gym' panel (sessions, total volume, best session volume) are rendered via `.toLocaleString()` so a value like 1109621 displays as '1,109,621' and 11851 displays as '11,851', while the '—' placeholder (when totalVolume/bestSessionVolume is null) and the 'kg' unit suffix still render exactly as before. Small numbers (e.g. 176 sessions) still display correctly as '176' with no stray formatting. Confirm visually via the project's visual-check flow (homepage screenshot) that the panel now shows comma-separated numbers and nothing else on the page shifted or broke.
