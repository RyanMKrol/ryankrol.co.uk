## Do

On `/reviews/perfumes` (`src/pages/reviews/perfumes/index.js`), the search input and the Date/Title/Score sort pills sit directly on top of each other with almost no visual gap, because this page places them inside `<div className="search-container">` — a class defined in `src/styles/globals.css` as just `.search-container { margin-bottom: 2rem; }`, with no `display:flex`/`gap` of its own. Both `SearchInput` (`src/components/SearchInput.js`, renders `<div className="collection-search-input">`) and `PillGroup` (`src/components/PillGroup.js`, renders `<div className="collection-pill-group">`) are plain block-level elements with no margin of their own, so stacked directly inside `.search-container` they end up touching.

This IS perfumes-specific, not a shared bug — confirmed by checking every other review-list page: `src/pages/reviews/movies/index.js`, `src/pages/reviews/tv/index.js`, `src/pages/reviews/books/index.js`, `src/pages/reviews/albums/index.js`, and `src/pages/vinyl/index.js` all place their `SearchInput` + `PillGroup` inside `<div className="collection-review-controls">` (CSS in `src/styles/globals.css`: `display:flex; align-items:center; gap:1rem; flex-wrap:wrap;`), nested INSIDE `<div className="collection-review-header">` as a sibling of `<div className="collection-review-title-group">` — NOT in a separate `.search-container` block below the header. `.search-container` as a class is otherwise only used by the unrelated `src/pages/programmes/index.js` (a workouts filter page, not a review list — leave that page alone, it is out of scope). Perfumes is the one review-list page that never adopted the shared `.collection-review-controls` pattern; that's the root cause, and the established convention already solves it correctly elsewhere, so bring perfumes in line with it rather than inventing new CSS.

Concrete edit — use `src/pages/vinyl/index.js` (around its `collection-review-header`/`collection-review-controls`/`search-results-count` markup) as the exact reference pattern to match:
1. Inside the `<div className="collection-review-header">` block, add a `<div className="collection-review-controls">` as a sibling of `<div className="collection-review-title-group">`, and move the existing `<SearchInput ... />` and `<PillGroup ... />` elements (currently inside `<div className="search-container">`) into it, unchanged (same props). Note: another task in this same backlog may independently remove a `<p className="perfume-scale-chip">` element that currently occupies that same sibling slot — if it's still present when you make this edit, replace it with the new `collection-review-controls` div; if it's already been removed, just add `collection-review-controls` as the sole sibling of `collection-review-title-group`. Either way, the end state is: `collection-review-header` contains exactly `collection-review-title-group` and `collection-review-controls`, nothing else.
2. Move the conditional `{searchTerm && (<div className="search-results-count">…</div>)}` block out of `search-container` so it sits as a top-level sibling directly after the closing `</div>` of `collection-review-header` (i.e. before the `perfume-card-grid` div), matching exactly where `src/pages/vinyl/index.js` places its own `search-results-count` block.
3. Delete the now-empty `<div className="search-container">` wrapper entirely — it should no longer appear anywhere in `src/pages/reviews/perfumes/index.js`.

No CSS changes are needed — `.collection-review-controls`, `.collection-search-input`, `.collection-pill-group`, and `.search-results-count` all already exist in `src/styles/globals.css` (T143/T150) and already produce correctly-spaced output on every other review-list page; this task is a pure JSX/markup relocation on the perfumes page only.

## Done when

- `grep -n "search-container" src/pages/reviews/perfumes/index.js` returns no matches (the wrapper div is gone).
- `grep -n "collection-review-controls" src/pages/reviews/perfumes/index.js` returns a match (the SearchInput+PillGroup now live inside it).
- `npm run lint` reports no new errors.
- `npm run build` succeeds.
- `verify: ["run-app"]` — start the dev server (`npm run dev`), open `/reviews/perfumes` in a browser, and confirm: the search box and the Date/Title/Score sort pills now sit side by side (or wrap with a clear visible gap on narrow widths) with visible breathing room between them — matching the spacing already seen on `/reviews/movies`, `/reviews/books`, or `/vinyl` — instead of touching/overlapping as before. Typing a search term still filters the list and still shows the 'Found N perfume(s)' count beneath the header, in the same position as before.
