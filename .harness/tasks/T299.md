## Do

Owner decision (already settled, do not re-litigate): build ONE small reusable `Tooltip` component rather than using native `title` attributes, generalizing the existing bespoke CSS idiom at `.home-sparkline-tooltip` (`src/styles/globals.css` ~line 3203-3232, used by the homepage workouts sparkline at `src/pages/index.js:223-226`) into a proper reusable primitive. That existing rule is: an absolutely-positioned `<span>` sitting above its hover parent, shown via `:hover { opacity: 1 }`, styled `background: #FFFFFF; color: var(--color-ink); font-family: var(--font-mono); font-size: 0.7rem; padding: 0.25rem 0.5rem; border-radius: 4px;` with an `opacity 0.15s ease` transition — reuse that visual language (swap the hardcoded `#FFFFFF` for `var(--color-card, #FFFFFF)` and add a `1px solid var(--color-hairline-strong)` border + small box-shadow for definition against busy card backgrounds) but do NOT copy its hardcoded `nth-child`/`nth-last-child` edge-avoidance — that's specific to the sparkline's row layout and out of scope here; both new call sites (a small badge in a card corner, and a horizontal scale bar) don't need it. IMPORTANT theming note: `--color-card` and `--color-ink` are Collection-only tokens NOT overridden by `html.matrix-active` (confirmed: matrix-active's block, `globals.css` lines ~460-587, redefines `--color-surface`/`--color-text`/etc. but never `--color-card`/`--color-ink`) — so a tooltip built on `--color-card`/`--color-ink` will render as the SAME small white/ink label chip under the Matrix theme too, exactly matching how `.home-sparkline-tooltip` already behaves today (a deliberate, pre-existing choice — a floating label that stays legible regardless of active theme, not a bug to fix). Build: (1) `src/components/Tooltip.js` — `export default function Tooltip({ label, children, position = 'top', className })` that renders `<span className={`tooltip-wrapper${className ? ' ' + className : ''}`} tabIndex={0} aria-label={label}>{children}<span className={`tooltip-bubble${position === 'bottom' ? ' tooltip-bubble--bottom' : ''}`} aria-hidden="true">{label}</span></span>` — the `tabIndex={0}` + `aria-label` make it keyboard-focusable and screen-reader-accessible even without the visual bubble (the bubble itself is `aria-hidden` to avoid double-announcing); `position` lets a call site flip the bubble below the trigger if 'top' would be clipped. (2) New CSS in `src/styles/globals.css` (add near the existing `.home-sparkline-tooltip` rules for locality): `.tooltip-wrapper { position: relative; display: inline-flex; }`, `.tooltip-bubble { position: absolute; bottom: 100%; left: 50%; transform: translateX(-50%); margin-bottom: 0.4rem; padding: 0.25rem 0.5rem; border-radius: 4px; background: var(--color-card, #FFFFFF); color: var(--color-ink, #1A1714); font-family: var(--font-mono); font-size: 0.7rem; white-space: nowrap; opacity: 0; pointer-events: none; transition: opacity 0.15s ease; z-index: 10; border: 1px solid var(--color-hairline-strong); box-shadow: 0 2px 6px rgba(0,0,0,0.12); }`, `.tooltip-bubble--bottom { bottom: auto; top: 100%; margin-bottom: 0; margin-top: 0.4rem; }`, `.tooltip-wrapper:hover .tooltip-bubble, .tooltip-wrapper:focus-within .tooltip-bubble { opacity: 1; }`, and a `@media (prefers-reduced-motion: reduce) { .tooltip-bubble { transition: none; } }` block per this repo's reduced-motion convention (CLAUDE.md: 'guard any new animation the same way' as the Matrix rain). (3) Update `CLAUDE.md`'s Components bullet list (the paragraph starting 'Components:') to add `Tooltip` (small hover/focus-shown label bubble; CSS-only, generalizes the home-sparkline tooltip idiom) — docs are part of Definition of Done per this repo's CLAUDE.md. Do NOT touch `.home-sparkline-tooltip` itself or the homepage sparkline — leave that as-is; this task only adds the new generalized component alongside it.

## Done when

`npm run lint`, `npm test`, and `npm run build` all still pass. Add a tiny throwaway usage (or use Storybook-less manual check) is not required, but the component must be exercised for real by the two dependent tasks (T{apply-projects}/T{apply-perfumes}) before final owner sign-off — for THIS task alone, verify in isolation: temporarily render `<Tooltip label="Test tooltip">hover me</Tooltip>` on any existing page during `npm run dev`, confirm (a) the label bubble is hidden by default, (b) hovering the wrapped content fades it in above the trigger within ~150ms, (c) tabbing to the element via keyboard (Tab key, no mouse) also shows the bubble (focus-within), (d) toggling the Matrix easter egg (Konami code ↑↑↓↓←→←→BA) keeps the bubble a legible light chip (not invisible/black-on-black), (e) `prefers-reduced-motion: reduce` (via devtools rendering emulation) removes the fade transition, then remove the throwaway usage before finishing (the two dependent tasks provide the real call sites). CLAUDE.md's Components list mentions `Tooltip`.
