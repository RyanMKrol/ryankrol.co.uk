## Overview
The password-gated Top-of-Mind editor page currently lives at src/pages/top-of-mind/edit.js (route /top-of-mind/edit). It is not linked from anywhere in the UI — it is reached only by typing the URL — and the owner wants it at a short, memorable route: /tom ("Top Of Mind"). This is a pure page rename/move: the /api/top-of-mind API routes and the TopOfMind DynamoDB table are separate and must not change. DEFAULT DECISION (pending owner confirmation via the paired question): lowercase route /tom, and a clean MOVE (remove the old file, no redirect) since nothing links to the old route. If the owner instead chooses to keep the old route via a redirect, add src/pages/top-of-mind/edit.js back as/keep it plus a next.config.js redirect and add next.config.js to scope.

## Do
Move the Top-of-Mind editor page to the new short route: create src/pages/tom.js containing the exact page component currently in src/pages/top-of-mind/edit.js (same imports, same MarkdownEditor form, same useEffect fetch of /api/top-of-mind, same POST to /api/top-of-mind/update, same password gate, same Head/title, byte-for-byte identical behaviour), then delete src/pages/top-of-mind/edit.js (leaving src/pages/top-of-mind/ empty of pages). Adjust the relative import paths in the moved file for its new depth: src/pages/tom.js is one directory shallower than src/pages/top-of-mind/edit.js, so imports that were '../../components/...' / '../../lib/...' become '../components/...' / '../lib/...'. Do NOT change any component's behaviour, do NOT touch the /api/top-of-mind routes or the TopOfMind table, and do NOT add/change any internal link (there are none pointing at the old route). Then add a file-map row to CLAUDE.md documenting the new route (the Top-of-Mind edit page is not currently in the file map): e.g. a row for `src/pages/tom.js` describing it as the password-gated Markdown editor for the home-page 'Top of mind' blurb, served at /tom, backed by /api/top-of-mind.

## Done when
The Top-of-Mind editor renders at /tom (from src/pages/tom.js) with fully identical behaviour to the old page — loads the current text via GET /api/top-of-mind, saves via password-gated POST /api/top-of-mind/update — and src/pages/top-of-mind/edit.js no longer exists (so /top-of-mind/edit 404s). All relative imports in src/pages/tom.js resolve correctly at the new path (npm run build succeeds). CLAUDE.md's file map includes a row for src/pages/tom.js documenting the /tom route. No changes were made to any /api/top-of-mind route or the TopOfMind table.
