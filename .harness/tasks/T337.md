## Overview
The owner-confirmed scope for idea #4 ('all of the workouts') extends to the per-exercise history table on /exercises/[exerciseName], which shows each session's date via a bare `toLocaleDateString()` call with no format options (browser-default numeric, e.g. '07/07/2026') — the same English-long-form fix as the workout detail page, applied here too. The dead, unimported src/components/WorkoutCard.js (same stale pattern) is explicitly OUT of scope per the owner's answer, since nothing renders it.

## Do
In src/pages/exercises/[exerciseName].js, find `{new Date(session.workout_date).toLocaleDateString()}` (currently line ~299, inside the history table). Import `formatEnglishDate` from `../../lib/dateFormat` (same helper workout-detail-date-a wires into the workout detail page) and replace this call with `{formatEnglishDate(session.workout_date)}` so the rendered date reads '7 July 2026' style instead of the browser-default numeric format.

## Done when
Visiting an exercise history page (/exercises/[exerciseName]) for an exercise with logged sessions renders every session's date in the history table in English long-form (e.g. '7 July 2026'), not numeric slash-separated form; the page imports and uses formatEnglishDate from src/lib/dateFormat.js. src/components/WorkoutCard.js is left untouched (confirmed dead code, out of scope).
