## Do

`src/lib/workoutBackfill.js`'s `storeWorkoutInDynamoDB(workout)` is the live, production write path (triggered on API cache-miss via `triggerBackfillAsync`). Its `ConditionalCheckFailedException` branch (around lines 51-61) does a `GetCommand` for the existing item and, when `existingItem && existingItem.exercises` is truthy, logs 'already exists, skipping' and returns `false` WITHOUT ever calling `storeExercises(workout)`. This is the confirmed root cause of the reported bug: any Workouts item that already exists with a non-empty `exercises[]` field is permanently exempt from ever having its per-exercise rows (re)written into the standalone `Exercises` table by this function again — which is exactly how 28 real sessions of 'Iso-Lateral High Row (Machine)' ended up denormalized correctly on `Workouts` items but only 3 of those 28 ever got their `Exercises`-table rows written (whatever originally wrote those 25 Workouts items — likely one of the other 3 write mechanisms below — evidently didn't also write all their Exercises rows, and this function's unconditional early-return has silently prevented the live sync from ever correcting that since). Fix: in that same branch (existingItem has `exercises`), call `storeExercises(workout)` before returning `false` — do not change the `false` return value (that keeps `backfillWorkouts()`'s `newWorkouts`/`consecutiveExisting` counter semantics exactly as-is, since the *workout* record itself genuinely didn't change). `storeExercises` is already safe to call repeatedly: it loops every exercise and does a per-exercise `PutCommand` with `ConditionExpression: 'attribute_not_exists(exercise_id)'`, already tolerating `ConditionalCheckFailedException` per exercise (lines ~120-126) — so calling it on an already-fully-populated workout is a harmless no-op storm of conditional-check failures, and calling it on a workout with some missing Exercises rows fills exactly the gap. Also export `storeWorkoutInDynamoDB` and `storeExercises` from this file (currently only `backfillWorkouts` and `triggerBackfillAsync` are exported) so the two sibling one-off scripts (`wdi-consolidate-audit`, `wdi-consolidate-migrate`) can import and call the same functions instead of maintaining their own reimplementations — this is the single-write-path consolidation the owner asked for.

## Done when

storeWorkoutInDynamoDB's 'already exists AND has exercises' branch now also calls storeExercises(workout) before returning false (return value and all other existing behavior in that branch unchanged). storeWorkoutInDynamoDB and storeExercises are both exported from src/lib/workoutBackfill.js. src/lib/workoutBackfill.test.js (existing file) keeps its current passing cases and adds a new case: mock docClient.send so the initial workout PutCommand rejects with a ConditionalCheckFailedException, the follow-up GetCommand resolves an item that already has a non-empty exercises field, and assert that per-exercise PutCommand calls for the Exercises table still happen (i.e. storeExercises actually ran) even though the function still returns a falsy result and does not call UpdateCommand to heal the Workouts item. npm run lint, npm test, and npm run build all pass.
