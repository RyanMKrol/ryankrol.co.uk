## Do

Create a new standalone presentational component at `src/components/perfumeVariants/Variant1StatBlock.js`, default-exported, accepting props `{ item }` where `item` is a raw record from the `PerfumeRatings` table (see README.md's 'PerfumeRatings Table' section and src/pages/api/reviews/perfumes/add.js for the exact shape: `title`, `designer`, `type`, `rating` 0-10 integer, `description`, `fragranticaUrl`, optional `longevity` 0-8, `projection` 1-4, `seasons` array from ['Winter','Spring','Summer','Autumn','Day','Night'], `applicationSpots` array of `{spot, sprays}`, `date`). This variant's direction is a DATA-DENSE 'stat block' card: title + designer + a `type` Badge (reuse `src/components/Badge.js` with `accentColor="var(--accent-perfumes)"`) up top, the rating shown via `PipMeter` (`src/components/PipMeter.js`, `readOnly` prop, value=item.rating) plus the raw '/10' number, then a compact stat row/table beneath showing longevity (label it e.g. 'Longevity: 6/8' — do not just dump the raw number with no label), projection (e.g. 'Projection: 3/4'), every entry in `seasons` as small inline chips, and every entry in `applicationSpots` as a compact list like '3× Wrists, 2× Clavicles'. Render a small 'View on Fragrantica' link to `item.fragranticaUrl` (open in new tab, `rel="noopener noreferrer"`). If `item.description` is present show it as a short paragraph beneath the stats. Only render optional-field rows when the field is actually present on `item` (do not show 'Longevity: undefined'). Add new, uniquely-prefixed CSS classes (e.g. `.perfume-v1-*`) to `src/styles/globals.css` for this variant's layout — you may ONLY ADD new rules, never edit or remove any existing rule in that file (other variants and the live site depend on what's already there). This component must render standalone (no dependency on ReviewCard.js or on any particular parent container) so it can be dropped into both a preview page and, later, the real perfumes list page.

## Done when

`src/components/perfumeVariants/Variant1StatBlock.js` exists, exports a component that renders real perfume fields (title, designer, type badge, pip-meter rating, longevity/projection/seasons/applicationSpots when present, a Fragrantica link, and description when present) in a visibly data-dense stat-block layout distinct from the current generic ReviewCard perfume rendering. `npm run dev`, temporarily mount the component with a couple of real records from `GET /api/reviews/perfumes` (e.g. in a scratch page or the browser console) or wait for the paired preview-page task to do this — either way, confirm in a running dev server that it renders with no console errors and the optional fields correctly appear/disappear based on whether the source record has them. `src/styles/globals.css` still parses (`npm run build` succeeds) and no pre-existing rule in that file was modified or removed.
