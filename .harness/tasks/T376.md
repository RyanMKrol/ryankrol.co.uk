## Overview
The three panels in the home-page right-hand rail (src/pages/index.js, inside .home-rail ~lines 234-320) are inconsistent: the GYM header is a plain <p> while the SHELF and HOT TAKES headers are links; a stray horizontal divider line appears above the first hot-take because a :first-of-type reset never matches; and the hot-takes header link renders default browser blue/purple. Make all three headers links (gym -> /workouts), remove that stray divider, and colour the hot-takes header link with the panel text colour in every state.

## Do
Two files, no logic changes.

src/pages/index.js:
- GYM HEADER (line 236): replace the plain <p className="home-gym-panel-title">This year at the gym</p> with a Next.js <Link href="/workouts" className="home-gym-panel-title home-gym-panel-link">This year at the gym</Link>, mirroring the shelf header pattern at lines 278-280 (which is <Link href="/vinyl" className="home-shelf-panel-title home-shelf-panel-link">). Link is already imported and used elsewhere in this file. Keep the text 'This year at the gym' unchanged. Do NOT touch the gym stats/sparkline markup below it.
- Leave the shelf header (already a /vinyl link) and hot-takes header (already a /hot-takes link) markup unchanged, and leave the '↻ Skim the shelf' refresh button (lines 281-289) unchanged.

src/styles/globals.css:
- ADD a link-variant rule for the gym title, mirroring .home-shelf-panel-title.home-shelf-panel-link (line 3413) and its :hover (3419): add `.home-gym-panel-title.home-gym-panel-link { margin: 0; text-decoration: none; cursor: pointer; }` and `.home-gym-panel-title.home-gym-panel-link:hover { text-decoration: underline; }` near the .home-gym-panel-title block (3243). The existing .home-gym-panel-title base rule keeps the mono/uppercase/opacity styling; the link variant only needs to reset margin and handle link decoration (the gym panel uses the default page ink colour, same as the shelf panel, so no explicit color is required there).
- FIX THE STRAY DIVIDER: the reset .home-hot-takes-item:first-of-type { border-top: none; } (lines 3487-3489) is ineffective because :first-of-type is scoped to element type (div) and the panel's first div is the header (.home-hot-takes-panel-header), so the first .home-hot-takes-item is never :first-of-type and keeps its border-top (line 3484). Change the reset to target the first item reliably regardless of the preceding header div — e.g. replace `.home-hot-takes-item:first-of-type` with `.home-hot-takes-panel-header + .home-hot-takes-item` (adjacent-sibling: the item immediately after the header), so exactly the first item's border-top is removed while dividers between successive items stay. Keep the between-items border-top on .home-hot-takes-item (3484) intact.
- FIX THE HOT-TAKES LINK COLOUR: the hot-takes header link .home-hot-takes-panel-title.home-hot-takes-panel-link (3470) sets no color, so it inherits the default <a> blue/purple even though the panel is color:#FFFFFF. Add `color: #FFFFFF;` to that rule and ensure hover/visited stay the same colour (add a :visited that also sets color:#FFFFFF, and keep the existing :hover at 3475 which only toggles underline). Match how .home-hot-takes-viewall explicitly sets color:#FFFFFF (3515). The link text must render white in normal, hover, and visited states — never blue/purple.

Do NOT change any data fetching, the panel background colours, the between-items divider styling, or any other page. No new CSS custom properties.

## Done when
On the home page: (1) all three rail-panel headers are links — the gym header 'This year at the gym' navigates to /workouts, the shelf header to /vinyl, and the hot-takes header to /hot-takes; the gym header is styled consistently with the shelf/hot-takes headers (mono/uppercase title, no default underline, underline on hover). (2) There is NO horizontal divider line between the 'Hot takes' header and the first take, while the divider lines BETWEEN successive takes remain. (3) The 'Hot takes' header link text renders in the panel's white text colour in normal, hover, and visited states — never the default blue/purple. The visual-check screenshots of the home page's rail panels reflect all three.
