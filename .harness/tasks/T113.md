## Do

Same shape as `review-edit-url-id-1` but for books. Source table `BookRatingsV3` is currently keyed on a COMPOSITE key `title`+`author` (both String) — see `src/pages/api/reviews/books/update.js:32-40` (`Key: { title: originalTitle, author: originalAuthor }`) and `delete.js:24-29`. Fields to preserve per item: `title`, `author`, `rating`, `review_text`, `date`, and the optional provider-enrichment fields `source`, `olid`, `coverId`, `coverUrl`, `volumeId`, `bookAuthors`, `firstPublishedYear`, `isbn`, `subjects`, `pageCount`, `publisher` (full list in `src/pages/api/reviews/books/update.js:66-83`).

Write `src/scripts/migrateBooksToIdTable.js` mirroring `src/scripts/migrateAlbumsToV2.js`'s structure and `src/scripts/createPerfumeTable.js`'s table-creation shape. Create `BookRatingsV4` with `id` (String) as the SOLE HASH partition key — this REPLACES the current `title`+`author` composite key entirely (no sort key on the new table); scan all of `BookRatingsV3`; guard against double-migration; generate `id: randomUUID()` per item and copy every existing field (including `title` and `author`, which become ordinary non-key attributes) unchanged into the new table; batch-write in chunks of 25; log old-count vs new-count; leave `BookRatingsV3` fully intact; touch nothing under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js`.

Extract the pure transform into a testable function and add `src/scripts/migrateBooksToIdTable.test.js` covering field preservation (including that `title`+`author` are retained as plain attributes, not a key), `id` presence, and UUID shape.

## Done when

`npm test` and `npm run lint` pass. Diff touches only the two new script files. `needs-human`: a human must run `node src/scripts/migrateBooksToIdTable.js` against production `BookRatingsV3`/`BookRatingsV4`, confirm counts match, and mark this task done via `.harness/mark-done.sh`.
