## Do

Same shape as `review-edit-url-id-1` but for books. Source table `BookRatingsV3` is currently keyed on a COMPOSITE key `title`+`author` (both String) — see `src/pages/api/reviews/books/update.js:32-40` (`Key: { title: originalTitle, author: originalAuthor }`) and `delete.js:24-29`. Fields to preserve per item: `title`, `author`, `rating`, `review_text`, `date`, and the optional provider-enrichment fields `source`, `olid`, `coverId`, `coverUrl`, `volumeId`, `bookAuthors`, `firstPublishedYear`, `isbn`, `subjects`, `pageCount`, `publisher` (full list in `src/pages/api/reviews/books/update.js:66-83`).

Write `src/scripts/migrateBooksToIdTable.js` mirroring `src/scripts/migrateAlbumsToV2.js`'s structure and `src/scripts/createPerfumeTable.js`'s table-creation shape. Create `BookRatingsV4` with `id` (String) as the SOLE HASH partition key — this REPLACES the current `title`+`author` composite key entirely (no sort key on the new table); scan all of `BookRatingsV3`; guard against double-migration; generate `id: randomUUID()` per item and copy every existing field (including `title` and `author`, which become ordinary non-key attributes) unchanged into the new table; batch-write in chunks of 25; log old-count vs new-count; leave `BookRatingsV3` fully intact; touch nothing under `src/pages/**`, `src/lib/constants.js`, or `src/lib/dynamo.js`.

Extract the pure transform into a testable function and add `src/scripts/migrateBooksToIdTable.test.js` covering field preservation (including that `title`+`author` are retained as plain attributes, not a key), `id` presence, and UUID shape.

The script must also support a `--verify` mode: re-scan both `BookRatingsV3` and `BookRatingsV4`, and for every old item locate its corresponding new item (match on `title`+`author`, the original composite key, which remains unique in the new table as plain attributes) and assert every original field is byte-for-byte identical (ignoring the new `id`). Log any mismatch or unmatched old item with enough detail to diagnose it, and exit non-zero on any failure. Running with no flags performs the migration then automatically runs this same verification, printing a `VERIFY: PASS` / `VERIFY: FAIL (<n> mismatches)` summary. `--verify` alone just re-runs the comparison. Extract the comparison into an exported pure function (e.g. `diffMigratedItem`) and cover it in `migrateBooksToIdTable.test.js` (clean match / changed field / missing item).

## Done when

`npm test` and `npm run lint` pass, including tests for field preservation, `id` presence, UUID shape, and `diffMigratedItem`. Diff touches only the two new script files. Actually run the migration: using AWS credentials from this checkout's `.env.local` (root `CLAUDE.md`'s `vercel env pull --environment production` convention), run `node src/scripts/migrateBooksToIdTable.js` against REAL production `BookRatingsV3`/`BookRatingsV4`, and confirm the printed summary reports `VERIFY: PASS` — record that final summary line in the worklog. Leave `BookRatingsV3` fully intact (read-only). On `VERIFY: FAIL` or any error, STOP — do not hand-fix data, record `failed:blocked` with the details for a human to investigate.
