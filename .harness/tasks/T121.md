## Do

`src/pages/api/reviews/tv/update.js` is the POST handler that edits an existing TV review in the `TelevisionRatingsV3` DynamoDB table (partition key `title`) — structurally identical to `src/pages/api/reviews/movies/update.js`. Today, lines 32-44 fetch the existing item via `GetCommand` specifically to preserve the original `date` field across edits (`const originalDate = existingReview.Item?.date;` then `const preservedDate = originalDate || ...`), and lines 61-71 build `reviewData` with `date: preservedDate` — so an edit currently leaves `date` completely untouched, and there is no record anywhere of when an edit happened.

Add a new field, `editedDate`, to track when the review was last edited, using the exact same `DD-MM-YYYY` string format/convention as the existing `date` field:

1. Keep the existing `originalDate`/`preservedDate` logic at lines 32-44 completely unchanged — `date` must still be preserved across edits exactly as today.
2. Compute a fresh `editedDate` value using `new Date().toLocaleDateString('en-GB').replace(/\//g, '-')` (today's date, formatted the same way as `date`).
3. Add `editedDate` to the `reviewData` object built at lines 61-71 (alongside `date: preservedDate`), unconditionally on every successful edit (no `undefined`-guard needed, unlike the optional TMDB fields).
4. Do not change any other behavior — the title-changed delete/recreate logic (lines 46-58), the cache clear (`clearApiCache('api-tv')`, line 81), password gating, and validation are all unchanged.

Also update the repo's docs in the same change per `CLAUDE.md`'s Definition of Done: in the root `CLAUDE.md`'s Data model table, the `TelevisionRatingsV3` row currently reads "same shape as movies" — extend that note (or the row itself) to mention the new optional `editedDate 'DD-MM-YYYY', set on every edit` field, matching whatever wording the movies-review-edited-date task used for its `MovieRatingsV3` row (if that task has already landed, keep phrasing consistent; if not, describe it the same way independently). Also check `README.md`'s schema section for the equivalent row and update it the same way if it duplicates this shape.

Do not touch `src/pages/api/reviews/movies/update.js`, `books/update.js`, or `albums/update.js` — those are separate, independent tasks. Do not touch `src/components/ReviewCard.js` or any `index.js` sort logic — those are separate tasks too.

## Done when

- `src/pages/api/reviews/tv/update.js` writes both `date` (preserved from the original record, unchanged behavior) and a new `editedDate` (today's date, `DD-MM-YYYY` format, computed fresh on every call) into `reviewData` on every successful update.
- A manual/curl POST to `/api/reviews/tv/update` with a valid `password`, `title`, `rating`, `gist`, and `originalTitle` for an existing TV show results in the DynamoDB item having its original `date` unchanged and a new `editedDate` equal to today's date in `DD-MM-YYYY` format.
- Calling update again on the same item on a later date updates `editedDate` to the new date while `date` still stays at its original value.
- `CLAUDE.md`'s Data model table row for `TelevisionRatingsV3` documents the new `editedDate` field, and `README.md`'s schema section is updated to match if it independently describes this table's shape.
- `npm run lint` passes with no new errors.
- `npm test` passes.
- `npm run build` succeeds.
