## Do

In `src/components/perfumeVariants/Variant6Hybrid.js`, move the `ownership` Badge out of `.perfume-v1-rating-row` (currently lines 50-56, rendered alongside the `PipMeter` and the `X/10` rating number) and into `.perfume-v1-header` (lines 34-42), so it reads as a pill at the same visual level as the `type` Badge, not down in the rating row.

Concretely:
1. In the `.perfume-v1-header` block (around line 34-42), after the existing `{item.type && (<Badge accentColor="var(--accent-perfumes)">{item.type}</Badge>)}`, add the ownership badge as a sibling: `{item.ownership && (<Badge accentColor="var(--accent-perfumes)" variant="soft">{item.ownership}</Badge>)}`. Keep `variant="soft"` — that's how it already renders today (T284) and it gives ownership slightly less visual weight than the primary `type` pill, which is a sensible hierarchy to preserve (own has an established sample/travel/full-bottle vocabulary that's secondary to the perfume's actual type).
2. Wrap both badges (type + ownership) in a new inline container, e.g. `<div className="perfume-v1-header-badges">...</div>`, placed where the lone `item.type` Badge currently sits as the header's second flex child (so `.perfume-v1-header`'s existing `display:flex; justify-content:space-between` still puts title/designer on the left and this badge group on the right).
3. Remove the ownership Badge from `.perfume-v1-rating-row` (lines 53-55) entirely — after the move, that row goes back to just the `PipMeter` + the `X/10` rating number, matching its pre-T284 shape.
4. In `src/styles/globals.css`, add a `.perfume-v1-header-badges` rule near the existing `.perfume-v1-header` rule (around line 3411-3416): `display: flex; align-items: center; flex-wrap: wrap; gap: 0.4rem;` (small gap between the two pills, consistent with the `0.6rem` gap already used in `.perfume-v1-rating-row`). `flex-wrap: wrap` is a deliberate defensive choice: `.perfume-v1-header` itself is `justify-content: space-between` with a long title possibly on the left, so on a narrow card the two badges should wrap onto their own second line together rather than overflowing or squeezing the title — this only matters when BOTH `type` and `ownership` are present on the same perfume; the common case of only `type` being set is unaffected (single badge, nothing to wrap).
5. Do not touch `.perfume-v1-rating-row`'s CSS rule itself (it's still used by `PipMeter` + the rating number) — only remove the Badge JSX that used to live inside it.

Judgment call made during conversion (not re-litigated by this task): render ownership as its own adjacent `Badge` next to `type` (two pills, not merged into one), keep `variant="soft"` on ownership so it stays visually secondary to the primary `type` pill, and add a wrapping flex container so the pair can safely wrap on narrow viewports without disturbing the title/designer block on the left. This is a self-contained visual move — no data model or API change (ownership already exists on `item`).

## Done when

`npm run dev`, open `/reviews/perfumes` in a browser. Confirm a perfume that has BOTH `type` and `ownership` set now shows two pills together in the card header (top-right, alongside/near the title and designer), replacing the old placement where ownership sat down next to the pip-meter/rating number. Confirm the rating row (`PipMeter` + `X/10`) still renders correctly with no leftover ownership badge or layout gap where it used to be. Confirm a perfume with `type` set but no `ownership` value still renders exactly as before (single pill, no empty gap or wrapper artifact). Resize the browser to a narrow/mobile width (e.g. ~375px) with a perfume that has both a long title and both pills set, and confirm the two badges wrap together cleanly rather than overlapping the title or getting clipped. Then `npm run lint`, `npm test`, and `npm run build` must all pass clean, and update `CLAUDE.md`'s `Variant6Hybrid.js` / perfume card description if the header's contents are described anywhere (search for 'perfume-v1-header' / 'ownership' in `CLAUDE.md` first — if nothing describes this level of layout detail today, no doc change is needed, but note that explicitly rather than skipping the check silently).
