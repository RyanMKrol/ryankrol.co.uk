## Do

Add API routes for the new `hot-takes` content type under `src/pages/api/hot-takes/`. Per the owner: this is deliberately minimal — just freeform text + a date, no rating, no update flow needed (mirror the `vinyl` precedent, which is this repo's simplest existing content type and ships with ONLY `index.js` + `add.js`, no `update.js`/`delete.js` — see `src/pages/api/vinyl/{index,add}.js`). Do the same here: no update/delete route in this task.

- `index.js` (GET) — `withApiCache(generateCacheKey('hot-takes'), () => scanTable(DYNAMO_TABLES.HOT_TAKES_TABLE))`, then sort the results by `date` descending (newest first) before returning — parse the stored `DD-MM-YYYY` date the same way `src/pages/index.js`'s `parseUkDate` does (either import/reuse that logic or duplicate the small parse, whichever keeps this route self-contained; prefer factoring the parse into `src/lib/hotTakes.js` if reused, to avoid duplicating date-parsing logic).
- `add.js` (POST) — password-gate on `RYANKROL_SITE_KEY` (401 on mismatch); require a non-empty `text` field (see validation below); build the record `{ id: randomUUID(), text, date: new Date().toLocaleDateString('en-GB').replace(/\//g, '-') }`; `PutCommand`; then `clearApiCache('api-hot-takes')`.

Add `src/lib/hotTakes.js` with `validateHotTakeText(value)` — a pure function returning true only for a non-empty string after trimming, with a sane max length (500 chars) to keep a 'take' short per the owner's 'bullet point' framing; reject non-strings, empty/whitespace-only strings, and anything over the max length. `add.js` must call this and return HTTP 400 with a clear message when it fails validation.

## Done when

`GET /api/hot-takes` is wrapped in `withApiCache` under key `api-hot-takes` and returns hot takes sorted newest-first by `date`. `POST /api/hot-takes/add` rejects a missing/incorrect password with 401, rejects an empty/whitespace-only or over-max-length `text` with 400 via `validateHotTakeText`, and on success stores `{ id, text, date }` via `PutCommand` and calls `clearApiCache('api-hot-takes')` (exact key). No `update.js`/`delete.js` route is added in this task (matches the vinyl precedent for this repo's simplest content type). `src/lib/hotTakes.js` exports `validateHotTakeText`, covered by `src/lib/hotTakes.test.js` (empty string, whitespace-only, non-string, over-max-length all invalid; a normal short sentence valid). `npm test`, `npm run lint`, `npm run build` all pass.
