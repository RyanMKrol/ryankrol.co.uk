## Overview
Re-attempt of T367 (Remove Google Books provider — dead code + config cleanup). T367 blocked because its `## Done when` demanded `grep -rE 'googlebooks|GOOGLE_BOOKS_API_KEY' src/` return ZERO matches, which is unsatisfiable: (a) the historical one-time migration script `src/scripts/migrateOpenLibraryBooksToGoogleBooks.js` + its test still `require('../lib/googlebooks')` and read `GOOGLE_BOOKS_API_KEY` (they were out of T367's scope, so the builder correctly blocked rather than deleting a lib out from under a live reference), and (b) several test fixtures legitimately keep `source: 'googlebooks'` string literals to exercise handling of historical book records — those must stay. This re-attempt widens the scope to also delete the obsolete migration script (+ test) and its npm script, and replaces the zero-grep bar with a satisfiable one that excludes those legitimate test-fixture literals.

## Do
Fully remove the dead Google Books search-provider path (search fully migrated to Hardcover in T361–T364; Google Books is no longer a provider). LESSON FROM T367: do NOT aim for a literal zero-grep across src/ — `source: 'googlebooks'` string literals legitimately remain in test fixtures (`src/scripts/stripOldBookProviderMetadata.test.js`, `src/scripts/migrateBooksToIdTable.test.js`, `src/pages/api/reviews/books/add.test.js`, `src/pages/api/reviews/books/update.test.js`) to test handling of historical records — leave those untouched. Concretely, do ALL of the following:
1. Delete `src/lib/googlebooks.js` and its test `src/lib/googlebooks.test.js`.
2. Delete the obsolete one-time migration script `src/scripts/migrateOpenLibraryBooksToGoogleBooks.js` and its test `src/scripts/migrateOpenLibraryBooksToGoogleBooks.test.js`. This script is safe to delete: it is a DRY-RUN-BY-DEFAULT one-time migration that targets the OLD `BookRatingsV3` table (the current table is `BookRatingsV4` since T077–T080), it already ran, nothing live imports it (only its own test + the package.json npm script below), and it is the sole remaining `require('../lib/googlebooks')`, so it blocks removing the lib. Verify this yourself before deleting (`grep -rn migrateOpenLibrary src/ package.json`, confirm only its own test + the package.json script reference it).
3. Remove the now-dead npm script `"books:migrate-openlibrary"` line from `package.json`'s `scripts` block (it invokes the script deleted in step 2).
4. In `src/pages/reviews/books/edit/[id].js`, remove the dead `provider: 'googlebooks',` property (~line 79) from the `URLSearchParams` in `handleBackfillSearch` — the `/api/books/search` endpoint has ignored this param since T361's Hardcover-only migration, so this is a no-behaviour-change cleanup.
5. In `CLAUDE.md`, remove the entire `GOOGLE_BOOKS_API_KEY` row from the Key env vars table (leave the `HARDCOVER_API_TOKEN` and `TMDB_API_TOKEN` rows). Double-check CLAUDE.md's file-map row for `src/pages/api/books/search.js` still describes it accurately (it should already be Hardcover-only — do not reintroduce a Google Books mention).
6. In the worklog, note that the OWNER should also remove `GOOGLE_BOOKS_API_KEY` from Vercel's project environment variables — this repo/task cannot do that itself.
If you discover any OTHER live (non-test) reference to `googlebooks`/`GOOGLE_BOOKS_API_KEY` beyond the ones enumerated above, stop and report `failed:blocked` naming it rather than deleting underneath it.

## Done when
`src/lib/googlebooks.js`, `src/lib/googlebooks.test.js`, `src/scripts/migrateOpenLibraryBooksToGoogleBooks.js`, and `src/scripts/migrateOpenLibraryBooksToGoogleBooks.test.js` no longer exist; `grep -rEn 'googlebooks|GOOGLE_BOOKS_API_KEY' src/ | grep -v '\.test\.js:'` returns NO matches (i.e. no non-test file under src/ imports googlebooks or reads GOOGLE_BOOKS_API_KEY — remaining `source: 'googlebooks'` literals inside `*.test.js` fixtures are expected and fine); the `provider: 'googlebooks'` property is gone from `src/pages/reviews/books/edit/[id].js`; `package.json` no longer contains a `books:migrate-openlibrary` script; CLAUDE.md's Key env vars table no longer lists `GOOGLE_BOOKS_API_KEY`; and `npm run lint`, `npm test`, `npm run build` all pass (no broken imports from the deletions).
