## Do

Remove confirmed-dead code left over from an old `/api/workouts?page=&pageSize=` pagination scheme that nothing in the live app calls anymore.

Independently reconfirmed via `grep -rn` over the whole `src/` tree (including `src/scripts/**` and every `*.test.js`) immediately before authoring this task:
- `getWorkoutsPaginated` (defined in `src/lib/workoutQueries.js`, currently lines 11-54, including its JSDoc comment) has exactly ONE caller in the entire codebase: `src/pages/api/workouts.js` line 32. No script, no test, no other page or API route references it.
- The cache key it drives, `generateCacheKey('workouts-dynamo', { page, pageSize })` (e.g. `api-workouts-dynamo-page:1-pageSize:10`), is never referenced anywhere else. (Note: `src/lib/workoutBackfill.js` line 235 does `clearApiCache('api-workouts-dynamo*')` — a wildcard prefix match. This ALSO matches the still-live `api-workouts-dynamo-all` cache key used by the `mode=all` path, so leave that `clearApiCache` call exactly as-is; do not touch `workoutBackfill.js`.)
- The ONLY page that fetches `/api/workouts` is `src/pages/workouts/index.js` (line 83: `fetch('/api/workouts?mode=all')`) and `src/pages/programmes/index.js` (line 22, same `?mode=all`). Neither ever sends `page`/`pageSize`. Both already do pagination and Push/Pull/Legs filtering entirely client-side via `paginate()` (`src/lib/pagination.js`) and `filterWorkouts()` (`src/lib/workoutPagination.js`) over the full list returned by `mode=all`.
- `src/lib/workoutQueries.test.js` only imports and tests `bucketVolumeByMonth` — it has ZERO coverage of `getWorkoutsPaginated`, so deleting the function does not remove any existing test coverage and no test file needs to change.
- Grep hits for `pageSize`/`page_count` in `src/scripts/migrateWorkoutData.js`, `src/scripts/auditWorkouts.js`, and `src/lib/workoutBackfill.js` are all about the external Hevy API's OWN `?page=&pageSize=` pagination (a completely different, still-live concern) — do not touch those files, they are unrelated.

Make these changes:

1. In `src/pages/api/workouts.js`: the handler currently has an early-return branch for `req.query.mode === 'all'` (lines 12-22) followed by a 'Legacy paginated path (kept for backwards compatibility)' branch (lines 24-43) that parses `page`/`pageSize`, builds the `'workouts-dynamo'` cache key, and calls `getWorkoutsPaginated`. Since `mode=all` is the only path any real caller ever takes, collapse the handler so it ALWAYS does what the `mode==='all'` branch did — i.e. remove the `if (req.query.mode === 'all') { ... }` conditional and its early return, and just run that body unconditionally as the handler's only logic (still wrapped in the same try/catch). Delete the entire legacy paginated branch (the `page`/`pageSize`/`workouts-dynamo` cache key/`shouldTriggerBackfill` logic and its two `console.log` branches). Remove the now-unused `getWorkoutsPaginated` import from the top of the file (keep `getAllWorkouts`).

2. In `src/lib/workoutQueries.js`: delete the `getWorkoutsPaginated` function entirely, including its JSDoc comment block immediately above it (currently lines 5-54). Leave every other exported function (`getAllWorkouts`, `getWorkoutById`, `getWorkoutsByDateRange`, `getExercisesByWorkout`, `getExerciseHistory`, `bucketVolumeByMonth`, `getWorkoutStats`) untouched.

3. Docs (Definition of Done requires updating docs in the same change): `CLAUDE.md`'s '## Gotchas' section currently has a bullet starting '**Workout pagination scans the whole table then slices in memory.** `getWorkoutsPaginated` (`workoutQueries.js`) ... (This is the root of the known pagination bug.)' — this describes the exact function being deleted and a bug path that no longer exists (the current `mode=all` + client-side `filterWorkouts`-then-`paginate` flow in `src/pages/workouts/index.js` filters the FULL list before slicing a page, so the 'filter applied to the current page only' bug it describes does not apply to the current code). Remove this bullet entirely from `CLAUDE.md`'s Gotchas section — do not leave it referencing a deleted function.

## Done when

`grep -rn "getWorkoutsPaginated" src/` returns zero matches anywhere in the repo (function deleted from `workoutQueries.js`, import removed from `api/workouts.js`). `src/pages/api/workouts.js` no longer parses `req.query.page`/`req.query.pageSize` or builds a `'workouts-dynamo'` (non-`-all`) cache key — `grep -n "workouts-dynamo'" src/pages/api/workouts.js` (note the closing quote, to exclude `'workouts-dynamo-all'`) returns nothing. The handler still serves the full workout list for any GET request the same way it did before for `?mode=all` (i.e. hitting `/api/workouts` with no query params, or with `?mode=all`, both return `{ workouts: [...] }` via `getAllWorkouts` under the `'workouts-dynamo-all'` cache key with the backfill trigger). `src/lib/workoutBackfill.js` is unmodified. `CLAUDE.md`'s Gotchas section no longer contains the 'Workout pagination scans the whole table' bullet. `npm run lint`, `npm test`, and `npm run build` all pass with no new failures; `workoutQueries.test.js` still passes unmodified (it never covered the deleted function, so no test changes are required).
