## Do

1. In `src/pages/reviews/perfumes/edit.js` (~line 64), change the wrapper `<div>`'s className from `"review-card-with-edit"` to `"review-card-with-edit review-card-with-edit--perfume"`.
2. In `src/styles/globals.css`, add a new rule block after whatever the last rule in the edit-button-fix area currently is (do not remove or reorder any existing rule added by sibling tasks in this batch):
```css
/* Edit button fix for perfumes (Variant6Hybrid): the shared .edit-button-overlay's
   absolute top-right position overlaps perfume-v1-header's type Badge, which also sits
   at the card's top-right corner. Take this page's button out of absolute flow entirely. */
.review-card-with-edit--perfume {
  display: flex;
  flex-direction: column;
}

.review-card-with-edit--perfume .edit-button-overlay {
  position: static;
  align-self: flex-end;
  margin-top: 0.5rem;
}
```
3. Do not touch `src/components/perfumeVariants/Variant6Hybrid.js`, `src/pages/reviews/books/edit.js`, `src/pages/reviews/movies/edit.js`, `src/pages/reviews/tv/edit.js`, `src/pages/reviews/albums/edit.js`, or `src/components/ReviewCard.js`.

## Done when

`npm run dev`, open `/reviews/perfumes/edit`:
- For every perfume card that has a `type` badge (e.g. "Eau de Parfum"), that badge is fully visible in the top-right of the card — no longer covered by the Edit button.
- The Edit button appears below each card's content, aligned to the right edge, with a small gap above it.
- Clicking Edit still navigates to `/reviews/perfumes/edit/<id>` as before.
- `/reviews/books/edit`, `/reviews/movies/edit`, `/reviews/tv/edit`, `/reviews/albums/edit` are visually unaffected by this change.
- `npm run lint` and `npm run build` both succeed with no new errors.
