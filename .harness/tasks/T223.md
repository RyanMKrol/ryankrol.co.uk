## Do

Convert `.spine-cover-list` in `src/styles/globals.css` (currently `display: grid; grid-template-columns: repeat(2, 1fr); gap: 1.5rem;` around line 308, plus its `@media (max-width: 700px) { grid-template-columns: 1fr; }` override around line 315) from CSS Grid to CSS multi-column, replicating the EXACT pattern already shipped for `.projects-grid`/`.project-card` in commit 796102b (T210 — read that commit's diff in git history for the reference implementation before starting): replace the grid declaration with `column-count: 2; column-gap: 1.5rem;` (keep any other existing property on the rule unchanged), and change the `max-width: 700px` override from `grid-template-columns: 1fr` to `column-count: 1;`. On `.spine-cover-card` (same file, around line 254), add `break-inside: avoid;` and `margin-bottom: 1.5rem;` — CSS multi-column has no `row-gap` equivalent between stacked items within a column, so this margin replaces the vertical spacing the old grid `gap` provided (match `.project-card`'s treatment exactly: it has the same two properties added for the same reason). Do not change `.spine-cover-card`'s internal flex layout/children, `src/pages/reviews/books/index.js`, or any other CSS selector — this is a pure CSS layout change with no JS or markup edits.

Accepted, INTENTIONAL tradeoff — do not treat this as something to additionally solve: CSS multi-column fills the first column top-to-bottom before starting the second, so with the books list paginated at `PAGE_SIZE = 12` (`src/pages/reviews/books/index.js:17`) across 2 columns and uneven card heights (driven by variable review-text length), the left/right column item-count split will not always be perfectly even — e.g. a page could end with 7 items in column 1 and 5 in column 2. This is the SAME tradeoff already accepted for `.projects-grid` in T210 (3-column version) and is being deliberately carried over here rather than solved with a JS-computed column-balancing scheme, per this repo's 'keep it simple' convention. Do NOT attempt a JS/measured masonry solution — out of scope for this task.

## Done when

`npm run lint` reports 0 errors (existing warnings tolerated, no new ones introduced). `npm run build` succeeds. `npm test` still passes in full (no new test required — this is a pure CSS layout change with no new logic to pin). `run-app` manual check: run `npm run dev`, open `/reviews/books` with enough reviews to fill at least one full page (12 items), and confirm (a) a short-review card sitting in the same row-position as a long-review card no longer stretches to match the long card's height — each card's visible height now matches only its own content, (b) shorter cards' following siblings visually flow up to fill the freed vertical space (real masonry reflow, not just per-card auto-height with a gap left behind), and (c) narrowing the viewport below 700px collapses the list to a single column with no visual breakage or overlap. `git diff --stat` shows only `src/styles/globals.css` changed, and within it only the `.spine-cover-list` and `.spine-cover-card` rules (plus their existing media-query block) differ from before.
