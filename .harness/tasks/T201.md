## Do

In `src/components/CoverTile.js`:
1. Add `export function assignGradients(keys)` next to the existing `GRADIENT_PAIRS`, `hashString`, and `gradientForKey`. Implement the sliding-window, no-repeat-within-4 algorithm described in the `design` field above, reusing the existing `hashString`/`GRADIENT_PAIRS`. Keep it a pure function (no React, no DOM) taking and returning plain arrays.
2. Add an optional `gradient` prop to the `CoverTile` component. In the `tileStyle` computation, when `!imageUrl`, use `gradient || gradientForKey(id || title || '')` as the `background` value (i.e. prefer the passed-in override, fall back to the current per-item hash exactly as today when no override is passed).

In `src/components/ReviewCard.js`:
3. Add an optional `gradient` prop to the `ReviewCard(...)` function signature.
4. `SpineCoverCard`: accept a `gradient` prop (thread it down from `ReviewCard`'s call site) and use `gradient || gradientForKey(item.id || getTitle())` as the tile's background, instead of always calling `gradientForKey`.
5. The inline `poster-banner` branch inside `ReviewCard`: same pattern — use `gradient || gradientForKey(item.id || getTitle())` for the `poster-banner` div's background.
6. `SquareCoverCard`: accept a `gradient` prop and pass it straight through as `gradient={gradient}` on its internal `<CoverTile ... />` call (do not call `gradientForKey` inside `SquareCoverCard` — let `CoverTile`'s own fallback handle the no-override case, per the CoverTile change above).
7. Make sure `ReviewCard`'s own render call sites for each variant (`spine-cover`, `square-cover`, `poster-banner`) pass their received `gradient` prop down to the corresponding sub-component/inline branch.

Add a new test file `src/components/CoverTile.test.js` (there is no existing test file for this component) covering `assignGradients` and the backward-compatible defaulting behaviour:
- **No-repeat-within-4**: build a reasonably long array of distinct synthetic keys (e.g. 30+ arbitrary strings), call `assignGradients`, and assert that for every position `i`, the assigned gradient does not equal any of the gradients assigned at positions `i-1..i-4` (or fewer if near the start).
- **Determinism**: calling `assignGradients` twice with the identical input array produces identical output arrays.
- **Preferred colour preserved when uncollided**: construct a case where a key's `gradientForKey(key)` output does not collide with the trailing window and assert `assignGradients` returns exactly that value at that position (spot-checks that the function isn't needlessly rotating away from the pure per-item hash in the common non-colliding case).
- **CoverTile backward compatibility**: a lightweight render/behavioural check (using the existing test tooling in this repo — React Testing Library is already an installed dependency per CLAUDE.md's 'Known gaps' note) confirming `CoverTile` with no `gradient` prop still resolves to the same background `gradientForKey(id || title)` would produce, and that passing an explicit `gradient` prop overrides it. If component-render testing proves awkward to wire up cleanly in the time available, it is acceptable to test this specific piece by extracting/asserting the style object logic in a unit-test-friable way, or by testing `assignGradients`/`gradientForKey` interaction purely — but the no-repeat and determinism tests above are the load-bearing ones and must be real, runnable Jest tests.

## Done when

- `npm run lint`, `npm test`, and `npm run build` all pass, per this repo's Definition of Done.
- `src/components/CoverTile.test.js` exists and passes, with at least the no-repeat-within-4, determinism, and preferred-colour-preserved-when-uncollided assertions described above.
- `assignGradients` is exported from `src/components/CoverTile.js` alongside the existing `gradientForKey`/`GRADIENT_PAIRS` exports (check `ReviewCard.js`'s existing `import CoverTile, { gradientForKey } from './CoverTile'` continues to work unchanged, and note `assignGradients` will need to be imported by page-level call sites in the follow-up wiring task).
- `CoverTile` accepts an optional `gradient` prop that overrides its computed background; when omitted, rendering is byte-identical to current behaviour (verify no visual change by running `npm run dev` and comparing the home page collection wall / vinyl grid before and after — every existing caller passes no `gradient` prop yet, so nothing should look different).
- `ReviewCard` accepts an optional `gradient` prop threaded to `SpineCoverCard`, the inline `poster-banner` branch, and `SquareCoverCard` (which forwards it to its internal `CoverTile`); when omitted, all three variants render identically to current behaviour. `verify: ["run-app"]`: run `npm run dev`, open `/reviews/books`, `/reviews/movies`, `/reviews/albums`, and `/` and confirm nothing looks different yet (no page has been wired to pass `gradient` in this task — that's task 2).
