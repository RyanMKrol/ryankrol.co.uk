## Do

Add a new page `src/pages/reviews/books/backfill.js`, the books counterpart of the shared bulk-backfill pattern (see the movies bulk-backfill task's spec for the full shape — mirror it, swapping the books-specific endpoints/fields below).

1. On mount, `fetch('/api/reviews/books')`, filter with `needsBookBackfill` from `src/lib/backfillEligibility.js` (only books missing BOTH `volumeId` and `olid` — i.e. no rich metadata from any source yet).
2. Single page-level password field at the top, reused by every row's confirm.
3. Render `<BulkBackfillList>` with the filtered books as `items`, `pageSize=15`, and:
   - `onSearch`: `GET /api/books/search?title=<title>&author=<author>` (mirror `handleBackfillSearch` in `src/pages/reviews/books/edit/[id].js:76-86`; note the existing per-item page also sends a `provider=googlebooks` query param that the current `/api/books/search` handler (`src/pages/api/books/search.js`) does not actually read/use anywhere — harmless to include for consistency with the existing pattern, but don't rely on it doing anything), attaching `error.retryAfterSeconds` on a 429.
   - `renderCandidate`/`getCandidateKey`: reuse the render JSX from `src/pages/reviews/books/edit/[id].js:247-273` (cover thumbnail, title, year, authors).
   - `onConfirm(book, candidate)`: POST to `/api/reviews/books/update` with `title: book.title`, `author: book.author`, `rating: book.rating`, `overview: book.review_text`, `originalId: book.id`, `password`, plus the candidate's fields exactly as `handleBackfillConfirm` maps them in `src/pages/reviews/books/edit/[id].js:88-101` (`source`, `coverUrl`, `volumeId`, `bookAuthors: candidate.authors`, `firstPublishedYear`, `isbn`, `subjects`, `pageCount`, `publisher`) — the update endpoint requires the full field set every time (see `src/pages/api/reviews/books/update.js:11-24`), not just the new metadata.
4. Add a link/button from `src/pages/reviews/books/edit.js` to `/reviews/books/backfill`.
5. Empty-state message when there's nothing to backfill.

## Done when

Same verification shape as the movies bulk-backfill task, applied to books: link from `/reviews/books/edit` works; `/reviews/books/backfill` lists only books missing BOTH `volumeId` and `olid`, paginated at 15/page; password entered once; at least one row shows a multi-candidate picker, confirming a candidate updates that book's Google Books fields via `/api/reviews/books/update` while leaving `title`/`author`/`rating`/`review_text`/`date` unchanged; Network tab shows sequential, spaced-out `/api/books/search` requests. `npm run lint` and `npm run build` both succeed.
