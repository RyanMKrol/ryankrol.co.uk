## Do

Fix the masonry sort-order reading-order bug on /reviews/movies by adopting the shared `MasonryColumns` component + `distributeRoundRobin` helper + `useResponsiveColumnCount` hook built in a separate, already-completed task (`src/lib/masonryColumns.js`, `src/hooks/useResponsiveColumnCount.js`, `src/components/MasonryColumns.js` — these already exist by the time this task runs; read them, do not rebuild them). That task also converted the Projects page (`src/pages/projects/index.js`) to this pattern — use it as a reference example.

Background: `.poster-banner-grid` (src/styles/globals.css around lines 242-251) uses `column-count: 3` (`column-count: 1` under `max-width: 900px`) for the movies list at src/pages/reviews/movies/index.js (around lines 162-172), which renders `pagedMovies` (already sorted by the SortButtons control — date/title/score, see the `sortBy` branches around lines 74-105) into `<div className="poster-banner-grid">`. Same bug as Projects: CSS multi-column layout fills column 1 top-to-bottom before column 2, breaking left-to-right reading order after a sort, even though the masonry packing (variable-height `ReviewCard`s) itself is fine. NOTE: TV's own fix (a separate task) depends on THIS task and will reuse the CSS you change here — do not leave `.poster-banner-grid`/`.poster-banner-card` half-migrated.

Do:
1. In `src/styles/globals.css`, replace `.poster-banner-grid` (currently `column-count: 3; column-gap: 1.5rem;` plus its `@media (max-width: 900px)` override) with a flex-row outer class (`display: flex; gap: 1.5rem;`) and a new `.poster-banner-grid-col` class (`display: flex; flex-direction: column; gap: 1.5rem; flex: 1 1 0; min-width: 0;`). Remove the `column-count: 1` mobile override (JS now drives column count via the same 900px breakpoint). `.poster-banner-card` currently has `break-inside: avoid; margin-bottom: 1.5rem;` — remove `margin-bottom` (redundant with the column's flex `gap`, would double the spacing); keep `break-inside: avoid`.
2. In `src/pages/reviews/movies/index.js`, import `MasonryColumns` from `../../../components/MasonryColumns` and `useResponsiveColumnCount` from `../../../hooks/useResponsiveColumnCount`. Replace the `<div className="poster-banner-grid">{pagedMovies.map(...)}</div>` block with `<MasonryColumns items={pagedMovies} columnCount={useResponsiveColumnCount(3, 900)} className="poster-banner-grid" columnClassName="poster-banner-grid-col" renderItem={(movie, index) => (<ReviewCard key={`${movie.title}-${index}`} item={movie} type="movie" styleVariant="poster-banner" gradient={movieGradients[index]} />)} />`. Keep the existing `movieGradients` computation working — `MasonryColumns`'s `renderItem` receives each item's ORIGINAL index in the full `items` array, so `movieGradients[index]` still lines up correctly.

## Done when

- `npm run lint` and `npm run build` clean.
- Manual verify (`run-app`): `npm run dev`, open /reviews/movies, sort by score — item #1 (highest score) top-left, item #2 to its right in the same row, item #3 further right (not below item #1); cards still pack tightly at their own variable heights with no dead gaps in any column. Resize below 900px and confirm single-column collapse.
