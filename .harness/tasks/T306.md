## Do

This lays the shared foundation for a design decision the owner already made (settled 2026-07-06, superseding T262-T266's narrower per-variant overlay-repositioning fix): every edit-capable review card gets a dedicated header strip at the TOP of the card containing only the Edit button, visually separated from the card's own content by a border, with the card's existing content pushed below it. This task ONLY adds the new shared CSS to src/styles/globals.css — it must NOT touch any edit.js page and must NOT delete/modify any existing `.review-card-with-edit`/`.edit-button-overlay`/T262-T266 modifier rules (those still back the 5 unconverted pages until tasks -3..-7 land; deleting shared base rules early would break every edit page still using them). Add, near the existing '/* Edit page styling */' section (immediately after the current `.edit-button-overlay:hover` block, before the T262-T266 fix comments — i.e. as new rules, not replacing anything): (1) `.review-edit-card { display: flex; flex-direction: column; }` — the new card wrapper (replaces `.review-card-with-edit` conceptually; each per-type task swaps its wrapper's className to this, never both at once for the same card). (2) `.review-edit-header { display: flex; align-items: center; justify-content: flex-end; min-height: 2.75rem; padding: 0.5rem 1rem; margin-bottom: 0.75rem; background: var(--color-surface-alt); border-bottom: 1px solid var(--color-border-strong); border-radius: var(--radius-card) var(--radius-card) 0 0; }` — the strip itself (settled: neutral Collection-palette background + border, not a reddish tint — background/border tokens per owner's decision). (3) `.review-edit-button { background-color: var(--btn-secondary-bg); color: var(--color-accent-secondary); border: 1px solid var(--color-accent-secondary); padding: 0.4rem 0.9rem; text-decoration: none; border-radius: var(--radius-base); font-size: 0.875rem; font-weight: 500; transition: background-color 0.2s; }` + `.review-edit-button:hover { background-color: var(--btn-secondary-hover-bg); }` — the Edit link itself, in-flow (no `position`), reusing the exact same `--btn-secondary-*`/`--color-accent-secondary` tokens the old `.edit-button-overlay` already used (settled: same button look-and-feel, just repositioned into the strip instead of absolutely placed) so the button's visual appearance is unchanged from today, only its position/container is new. Add a short comment above the new block explaining it's the shared pattern that tasks perfumes-edit-redesign-3..7 apply per type, and that once all five have landed the old `.review-card-with-edit`/`.edit-button-overlay`/T262-T266 modifier rules become fully dead and are removed by a final cleanup task.

## Done when

`.review-edit-card`, `.review-edit-header`, `.review-edit-button` (+`:hover`) exist in globals.css with exactly the properties above (spot-check background is `var(--color-surface-alt)` and border-bottom is `var(--color-border-strong)`, not a red/danger token). No existing CSS rule bytes changed or removed — `git diff` on globals.css shows a pure addition (no `-` lines other than possibly blank-line context). Confirm via `grep -rn 'review-edit-card\|review-edit-header\|review-edit-button' src/pages` that NO page references the new classes yet (still inert/unused — that's expected and correct at this point). `npm run build` succeeds (unused CSS doesn't break anything); `npm run lint` and `npm test` still pass unchanged.
