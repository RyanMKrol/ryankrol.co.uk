## Overview
Land the "Compact tint" medal-pill style (the variant chosen in T332) as the real `PrBadge`, then delete the now-unneeded dev comparison route. **Update:** the label-text work this spec originally planned (dropping the "PR" suffix, sentence-case "Weight"/"Volume") was already built and shipped separately in T322 (commit `15ab725`) — do NOT redo it here; this task is now CSS-only plus the deletion.

## Do
`src/pages/dev/pr-badge-variants.js` (T331) built 3 comparison variants; the owner picked **"Compact tint"** (`CompactTint` component in that file — soft `color-mix` tint background + `currentColor` border + medal emoji, shrunk padding/font vs. the current `PrBadge`).

1. **Resize `.pr-badge` in `src/styles/globals.css`** (currently ~line 2746) to match `.pr-variant-compact`'s exact dimensions (~line 2758, the "Compact tint" variant's own CSS):
   - `padding: 0.1rem 0.45rem;` (current `.pr-badge` has no padding override, so it inherits `.collection-badge`'s `0.25rem 0.75rem` — override it down)
   - `font-size: 0.7rem;` (currently inherits `.collection-badge`'s `0.75rem`)
   - `gap: 0.2rem;` (currently `0.3rem`)
   Do NOT touch `background-color`/`border` on `.pr-badge` — those already exactly match `.pr-variant-compact`'s tint/border treatment (`color-mix(in srgb, currentColor 22%, transparent)` / `1px solid currentColor`), so no change needed there. `border-radius` is already `var(--radius-pill)` via the shared `.collection-badge` base — no change needed.
   - `src/components/Badge.js`'s `PrBadge` component (~line 39-46) needs NO changes — it already renders the medal emoji + label + `className="pr-badge"`; this task is a pure CSS resize of the class it already applies.
2. **Delete `src/pages/dev/pr-badge-variants.js`** (its only purpose, comparing variants, is now resolved).
3. **Remove the now-dead comparison CSS** from `src/styles/globals.css`: the `/* ===== T331: dev-only PrBadge restyle comparison ===== */` block and everything under it — `.pr-variant-compact`, `.pr-variant-dot`, `.pr-variant-dot-mark`, `.pr-variant-outline` (~lines 2756-2794) — none of these classes are referenced anywhere outside the page being deleted in step 2.

## Done when
The PR medal pills on `/workouts` and `/workouts/[id]` render visibly smaller (tighter padding, smaller font) than before this change, keeping the medal emoji, tint background, and border treatment unchanged. `src/pages/dev/pr-badge-variants.js` no longer exists, and its `.pr-variant-*` CSS rules are removed from `globals.css` (confirm via `grep -rn "pr-variant-" src/` returning no matches). No label-text changes are made (that's already done, per T322) — this task's diff should be CSS-only plus the page deletion. `npm run lint` / `npm test` / `npm run build` all pass. Visually confirm via this repo's hermetic visual-check harness (`VISUAL_CHECK_ONLY=workout node scripts/visual-check.mjs` or the relevant manifest name) that the pills render smaller with no layout regression.
