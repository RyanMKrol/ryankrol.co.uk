## Overview
Land the single winning medal-pill style (chosen in medal-restyle-b) as the real PrBadge, dropping the literal 'PR' suffix from every label and capitalizing 'Weight'/'Volume' (first letter only, owner-confirmed), then delete the now-unneeded dev comparison route.

## Do
Update src/components/Badge.js's PrBadge (and its .pr-badge/.collection-badge CSS in src/styles/globals.css) to match the winning variant recorded in medal-restyle-b's outcome — smaller pill, smaller label font, matching the chosen icon/border/tint treatment. Update the label strings built in src/pages/workouts/index.js (lines ~62-64: { key: 'weight', label: 'weight PR' } etc.) and src/pages/workouts/[id].js (lines ~259-261, same pattern) to: 'weight PR' → 'Weight', 'volume PR' → 'Volume', '1RM PR' → '1RM' (unchanged casing, already fully capitalized, only the ' PR' suffix drops). This is OWNER-CONFIRMED sentence-case capitalization (first letter only) — NOT `text-transform: uppercase` and not ALL-CAPS; do not apply any CSS uppercase transform, just capitalize the literal label strings themselves. Delete src/pages/dev/pr-badge-variants.js since its only purpose was the now-decided comparison.

## Done when
The PR medal pills on /workouts and /workouts/[id] render smaller than before this change, with no visible 'PR' word anywhere, labels reading exactly 'Weight'/'Volume'/'1RM' (sentence-case, no CSS uppercase transform applied), and src/pages/dev/pr-badge-variants.js no longer exists; existing PR-related tests (search for isWeightPR/isVolumePR/is1RMPR label assertions in *.test.js) are updated to match the new label text rather than deleted wholesale.
