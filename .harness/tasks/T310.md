## Do

Apply the shared header-strip pattern from perfumes-edit-redesign-2 to the albums edit page, replacing T265's fix. Note src/pages/reviews/albums/edit.js currently does NOT use `MasonryColumns` (unlike books/movies/tv/perfumes) — it renders cards directly into a bare `<div className="square-cover-grid">`; that structure is OUT OF SCOPE here (no complaint was raised about albums' column count, only perfumes' — leave the outer grid untouched, only change the per-card wrapper markup). Change the wrapper div's className from `"review-card-with-edit review-card-with-edit--square-cover"` to `"review-edit-card"`; move the Edit `<Link>` to be the FIRST child, wrapped in a new `<div className="review-edit-header">`, with the Link's className changed from `"edit-button-overlay"` to `"review-edit-button"`; the `<ReviewCard .../>` (styleVariant="square-cover") becomes the second child. In src/styles/globals.css, delete the now-dead T265 rules: `.review-card-with-edit--square-cover` and `.review-card-with-edit--square-cover .edit-button-overlay`, plus their explanatory comment, located between the tv-banner fix block and the perfume fix block — do NOT touch the base rules or the books/movies/tv/perfumes fix blocks.

## Done when

`npm run dev`, visit `/reviews/albums/edit`: every card shows the header strip with only the right-aligned Edit button — no more absolutely-positioned button overlapping the square-cover heading/star-rating. Clicking Edit navigates correctly. Card content below the strip (cover tile, title, artist, date, highlights) matches `/reviews/albums` (view page) for the same album. `grep -n 'edit-button-overlay\|review-card-with-edit' src/pages/reviews/albums/edit.js` returns NOTHING. `npm run lint`, `npm test`, `npm run build` all pass.
