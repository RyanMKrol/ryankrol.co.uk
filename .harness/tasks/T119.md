## Do

`ReviewCard` (`src/components/ReviewCard.js`) is a shared presentational component used by every review type's list page (movies, TV, books, albums, and perfumes) via `src/pages/reviews/<type>/index.js`. It receives an `item` prop (the raw DynamoDB record for that review) and a `type` prop. Currently, lines 86-90 render a single date line:

```js
{item.date && (
  <p className="review-date">
    Date: {item.date}
  </p>
)}
```

Separately, other in-flight work is adding a new optional `editedDate` field (a `DD-MM-YYYY` string, same format as the existing `date` field, e.g. `'02-07-2026'`) to review records in DynamoDB — written by each review type's `update.js` API route whenever a review is edited (added field name to standardize on: `editedDate`). That field is not present on records that have never been edited; it appears once an edit happens.

Your job in THIS task is purely the display side: add a second, independent conditional line directly after the existing `Date:` paragraph (still inside the same `<div className={getCardClass()}>` card, after the block at lines 86-90) that renders `Edited: {item.editedDate}` only when `item.editedDate` is present/truthy, using the same `review-date` CSS class as the existing date line (no new CSS class or styling needed — reuse `className="review-date"` so it inherits the existing look). When `item.editedDate` is absent (the common case — most reviews are never edited), nothing new should render; the card looks exactly as it does today with just the single `Date:` line.

Do not touch the `date` line itself, do not rename or restructure any other function in this file (`getTitle`, `getAuthor`, `getThoughts`, etc.), and do not add any prop-drilling or new required props — `editedDate` is read directly off the existing `item` object, same as `item.date` already is. Do not touch any `update.js`/API files, any `index.js` sort logic, or any type-specific page — those are separate tasks. This component is shared across ALL review types (movies/tv/books/albums/perfumes), so this single change covers displaying the edited date everywhere once the per-type API tasks start writing `editedDate`.

## Done when

- `src/components/ReviewCard.js` renders an additional `<p className="review-date">Edited: {item.editedDate}</p>` line immediately after the existing `Date: {item.date}` line, gated on `item.editedDate` being truthy.
- When `item.editedDate` is undefined/absent, the rendered output is byte-identical to today's output (only the single `Date:` line, no `Edited:` line, no empty paragraph).
- When both `item.date` and `item.editedDate` are present (e.g. an item with `date: '01-01-2026', editedDate: '02-07-2026'`), both lines render, `Date:` first then `Edited:`.
- `npm run lint` passes with no new errors.
- `npm run build` succeeds.
- Manual verification (`verify: ["run-app"]`): run `npm run dev`, open any review list page (e.g. `/reviews/movies`). Since no record has `editedDate` populated yet (that's written by separate, still-pending API tasks), use React DevTools (or a temporary local edit reverted before finishing) to confirm that a `ReviewCard` instance given an `item` with an `editedDate` value renders a second 'Edited: ...' line below the existing 'Date: ...' line, and that cards without `editedDate` are visually unchanged from current production behavior.
