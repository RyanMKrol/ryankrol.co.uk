## Do

Open `/Users/ryankrol/Downloads/design_handoff/renders/3a.png` (desktop books list) and
`/Users/ryankrol/Downloads/design_handoff/renders/4f.png` (mobile) for reference.

Add a new **horizontal spine-cover** presentation to `src/components/ReviewCard.js` (alongside the
poster-banner variant from T150 — this component now needs to dispatch between at least 2 visual
variants by `type` or an explicit variant prop; use whichever mechanism keeps the component
readable, e.g. small per-variant sub-render-functions or extracting each variant into its own
internal sub-component): a tall book-spine-shaped cover tile on the left (use `CoverTile` in its
spine aspect ratio if that's a clean fit, or a locally-styled tile matching the design's ~88x128px
proportions), and on the right: title + read-only star rating on one line, "Author · Npp · Year"
metadata line beneath (mono or body font per the design, muted color), then an italic review-text
snippet.

Rebuild `src/pages/reviews/books/index.js` with this variant: same header/meta-line/search/sort
pattern as T150's movies page (reuse `SearchInput`/`PillGroup`), but a single-column stack of
horizontal cards instead of a 3-col grid. No pagination in this task (T155).

## Done when

- `ReviewCard.js` supports the horizontal spine-cover variant with author/pages/year metadata and
  works correctly for book data without breaking the poster-banner variant from T150.
- The books list page shows the header/meta/search/sort pattern and a stacked list of horizontal
  cards bound to real data from `/api/reviews/books`.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/books`, confirm search/sort work, cards
  show author/pages/year correctly, and the movies/tv pages from T150/T151 still render correctly
  (poster-banner variant unaffected). Detailed pixel-fidelity tuning is deferred to the Phase 6
  visual-audit task T166.
