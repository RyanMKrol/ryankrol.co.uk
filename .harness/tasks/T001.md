# T001 — Fix workouts-page pagination + filtering

## Do

Make the All/Push/Pull/Legs filter on the workouts list page
(`src/pages/workouts/index.js`) drive BOTH the displayed results AND the
pagination/count across the user's entire workout history — not just the current
page. Switch the page to **client-side** pagination: fetch the full workout list
once and filter + paginate in the browser.

- Add a non-paginated fetch path: e.g. a `getAllWorkouts()` in
  `src/lib/workoutQueries.js` (it can reuse the existing `paginatedScan`, which
  `getWorkoutsPaginated` at `src/lib/workoutQueries.js:11` already uses to scan the
  whole `Workouts` table), surfaced through `src/pages/api/workouts.js` via a query
  param / mode. **Keep the existing `withApiCache` wrapper** on the API route, and
  **preserve the page-1 Hevy backfill trigger** that route currently fires on a
  cache miss.
- Extract the pure filter + paginate logic into a new
  `src/lib/workoutPagination.js` (so it is unit-testable) and call it from the page.
- Keep the filter **title-based**: case-insensitive substring match of
  `push`/`pull`/`legs` against `workout.title` (there is no structured push/pull/legs
  field — `workoutType` is `strength`/`cardio`/`mixed`/`bodyweight` and is unrelated).

## Done when

- Selecting **Push / Pull / Legs** shows ALL workouts of that type across history
  (case-insensitive title-substring match), paginated, with the indicator showing
  the correct "page X of N" where `N = ceil(filteredCount / pageSize)` (pageSize 10).
- The **All** filter behaves exactly as today: start_time-descending order, 10 per page.
- Changing the filter **resets/clamps the current page** to a valid page — you never
  land on an empty out-of-range page (e.g. reset to page 1 on filter change).
- The pure logic lives in `src/lib/workoutPagination.js` and is covered by a
  co-located `src/lib/workoutPagination.test.js` asserting: (a) filtering by `push`
  returns only title-matching items, case-insensitively; (b) page count =
  `ceil(filtered.length / pageSize)`; (c) an out-of-range page request clamps to the
  last valid page; (d) the `all` filter returns the full set unfiltered. `npm test` passes.
- Manual check: load `/workouts`, click Push/Pull/Legs, and confirm the count +
  results update and no filter shows zero items when that type has matching workouts.
- DoD green: `npm run lint`, `npm test`, `npm run build` all pass. If the data flow
  changes, update the workout-pagination gotcha in the root `CLAUDE.md`.
