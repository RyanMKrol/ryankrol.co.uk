## Do

Add a short client-side cooldown to the search-trigger button in each of these three components, independent of and in addition to their existing `searching` loading state (which only covers the in-flight fetch — a fast response lets a user re-click immediately today):

- `src/components/TmdbSearch.js` — the `search` function (around line 19) and its button (around line 63, `disabled={searching || !query.trim()}`).
- `src/components/BookSearch.js` — same `search`/button shape (see the analogous lines).
- `src/components/LastfmAlbumSearch.js` — same `search`/button shape (its button also factors in `loadingInfo`; preserve that condition).

In each, add a `cooldown` boolean state (`useState(false)`). At the START of the `search()` function (or right after the existing guard clause that returns early if the query is empty), set `setCooldown(true)` and schedule `setTimeout(() => setCooldown(false), 2000)` — 2 seconds, per the owner's suggested cooldown length. Add `cooldown` to the button's `disabled` expression (e.g. `disabled={searching || cooldown || !query.trim()}`, preserving any other existing conditions like `loadingInfo` in `LastfmAlbumSearch.js`). This does not need cleanup-on-unmount handling (none of the sibling state in these components does either — matching existing style); a stray `setCooldown` after unmount is a harmless no-op warning at worst for this low-traffic personal site, not worth extra ref-tracking machinery.

Do not change the button's visible label/text for the cooldown state (only `searching` drives the 'Searching…' label) — the ask is specifically to prevent rapid repeat-clicks, not to add new UI copy.

Also add a short bullet to root `CLAUDE.md`'s Conventions or Gotchas section noting that these three search-trigger components enforce a 2s post-click cooldown (client-side) on top of the backend per-route rate limit (src/lib/rateLimit.js, added in a sibling task) — so a future add-form author knows both layers exist and why.

## Done when

Run the app (`npm run dev`) and manually verify on at least ONE of the three flows (e.g. `/reviews/movies/add`): typing a title and clicking 'Search TMDB' immediately disables the button; clicking again within ~2 seconds (even after results have loaded) keeps it disabled; after ~2 seconds it re-enables (assuming a non-empty query and no in-flight search). Repeat spot-check is not required on all three components, but the code change must be applied identically to all three files. `npm run lint` and `npm run build` succeed. Root CLAUDE.md has a new bullet documenting the 2s cooldown convention.
