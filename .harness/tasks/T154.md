## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Perfumes" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/3d.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4i.png` (mobile).

**Important — match the design mock's card fields exactly.** The perfume schema (see
`src/pages/api/reviews/perfumes/add.js`) stores more fields than the design shows on the card
(`longevity`, `projection`, `seasons`, `applicationSpots`) — do NOT add those to the card in this
task; they stay stored but unused in this UI pass (there's a separate, deliberately-deferred idea
about richer perfume card variants — not this task).

Add a **perfume card** presentation to `src/components/ReviewCard.js` (a fourth variant): a cover
tile on the left (reuse `CoverTile`, perfume-bottle-ish tall aspect per the render), and on the
right: title, "Designer · [EDP/EDT chip]" line (use the `Badge` component from T143 in its soft
variant for the type chip), a large numeric score (`item.rating`, styled big + accent-colored per
the design's "7.0" treatment) next to a read-only `PipMeter` (from T144, 10-dot), then an italic
review-text snippet (`item.description`, per `ReviewCard.js`'s existing `getThoughts()` for the
perfume type).

Rebuild `src/pages/reviews/perfumes/index.js`:
- Header: big "perfumes" title, "rated out of 10 · a new & growing shelf" subtitle, plus an
  accent-colored explainer chip (grape background, white text) stating the 0-10/pip-meter rule
  explicitly, per the design README: "perfumes use a 0-10 scale — shown as a pip meter, not stars,
  so it never gets confused with the /5 reviews." This explainer chip is UI-only, no interaction.
- A 2-column grid of perfume cards (reuse existing search/sort logic already in this page — check
  whether to swap `SortButtons` for `PillGroup` here too, for consistency with the other rebuilt
  list pages).
- A dashed-border "+ your next review lands here" empty-state card as an extra grid item (always
  present, not conditional on an empty list — per the design it appears alongside the one real
  review shown in `renders/3d.png`).

## Done when

- `ReviewCard.js` supports the perfume-card variant (score + pip meter + EDP/EDT chip), without
  breaking the poster-banner/horizontal-spine/2-col-square variants from T150/T152/T153.
- The perfumes list page shows the header with the explicit 0-10 scale explainer chip, a 2-col grid
  of perfume cards bound to real data from `/api/reviews/perfumes`, and the dashed empty-state card.
- The card does NOT surface `longevity`/`projection`/`seasons`/`applicationSpots`.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/reviews/perfumes`, confirm the scale-explainer
  chip is visible, the pip meter renders the correct filled-dot count for the real rating value, and
  the empty-state card is present. Detailed pixel-fidelity tuning is deferred to the Phase 6
  visual-audit task T166.
