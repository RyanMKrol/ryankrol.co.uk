## Do

Replace the plain `<textarea>` review-text input with the new `src/components/MarkdownEditor.js` (built in the prior task - import it the same way every other shared component is imported in these files, e.g. `import MarkdownEditor from '../../../components/MarkdownEditor';`) in each of these 5 files, at the exact `<textarea ... className="collection-form-textarea" .../>` block:
- `src/pages/reviews/movies/add.js` line ~133-141 (field name `gist`)
- `src/pages/reviews/tv/add.js` line ~133-141 (field name `gist`)
- `src/pages/reviews/books/add.js` line ~153-161 (field name `overview`)
- `src/pages/reviews/albums/add.js` line ~169-177 (field name `highlights`)
- `src/pages/reviews/perfumes/add.js` line ~201-209 (field name `description`)

Each existing textarea already passes `id`, `name`, `value={formData.<field>}`, `onChange={handleInputChange}`, `className="collection-form-textarea"`, `placeholder`, and `required` - since `MarkdownEditor` was built with an identical prop contract (task 2), this should be a like-for-like swap with no changes to each page's `formData` state, `handleInputChange`, or submit logic. Do not change any other part of these forms (search widgets, star ratings, password gating, etc.) - scope is strictly the textarea-to-editor swap.

## Done when

All 5 `add.js` files import and render `MarkdownEditor` in place of the old `<textarea>`, with the same `id`/`name`/`value`/`onChange`/`placeholder`/`required` props as before (field names unchanged: `gist` for movies/tv, `overview` for books, `highlights` for albums, `description` for perfumes). `npm run lint`, `npm test`, `npm run build` pass. Manually verify with `npm run dev`: for each of the 5 add pages (`/reviews/movies/add`, `/reviews/tv/add`, `/reviews/books/add`, `/reviews/albums/add`, `/reviews/perfumes/add`), type multi-line text with a bit of markdown (a bold word, a list) using the new editor's toolbar, submit the form successfully (site password required), and confirm the new review appears correctly on its list page with formatting rendered (relies on the rendering task's Markdown component already being wired into the display side).
