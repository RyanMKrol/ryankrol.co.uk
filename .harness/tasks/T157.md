## Do

Read `/Users/ryankrol/Downloads/design_handoff/README.md`'s "Listening" screen section and open
`/Users/ryankrol/Downloads/design_handoff/renders/2d.png` (desktop) and
`/Users/ryankrol/Downloads/design_handoff/renders/4e.png` (mobile).

Rebuild `src/pages/listening/index.js`:
- Header: big "listening" title, "top 50 albums · last 90 days · via last.fm" meta line.
- Ranked rows, one per album: rank number, a small cover thumbnail (`CoverTile` in a small/thumb
  size, or a simpler plain `<img>` with placeholder fallback if `CoverTile` doesn't comfortably scale
  down that far — your call), title + artist, a horizontal playcount bar whose filled width is
  proportional to playcount relative to the top track, and the raw playcount number on the right.
- The #1 row is visually distinct: filled solid with the listening/perfumes accent (`#7A5AF8`
  grape), white text, white-on-translucent bar. Rows 2+ are white bordered cards with the bar in the
  grape accent color on a light track.

Reuse whatever Last.fm data-fetching this page already does (check the existing implementation before
changing anything beyond markup/styling) — this is a visual rebuild, not a data-layer change.

## Done when

- The listening page shows the new header/meta line and ranked rows with cover thumbnails,
  proportional playcount bars, and the #1 row visually distinct (grape-filled) from the rest.
- `npm run lint`, `npm test`, and `npm run build` all pass.
- Manual verify (`run-app`): `npm run dev`, load `/listening`, confirm the bars are proportional to
  real playcount data (longer bar = higher count) and rank #1 is visually distinct from the rest.
  Detailed pixel-fidelity tuning is deferred to the Phase 6 visual-audit task T167.
